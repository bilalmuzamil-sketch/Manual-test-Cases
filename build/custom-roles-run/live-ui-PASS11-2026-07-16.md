# LIVE-UI PASS 11 (2026-07-16) — CLOSED the two Pass-10 residuals

> Continues `live-ui-PASS10-2026-07-16.md`. Observed-only (Rules 10/12); nothing inferred.
> Sessions: prod renewable self-login (`POST /api/login`, 200) + admin switch-user; staging
> untouched this pass (staging AP/AR + Part Return data are from Pass-10). Node via
> `NODE_USE_ENV_PROXY=1` + CONNECT-relay bridge; Chromium TLS1.2-max headless. Secrets in
> `/tmp` (chmod 600). No TestRail writes. Prod writes/deletes were user-authorized (prod is a
> disposable TEST account).

This pass closed the two genuine residuals the coordinator flagged after Pass-10:
1. **Prod See-AP/AR** for the non-Office roles.
2. **Prod Part Return** per role.
Both are now **OBSERVED-LIVE for every prod role** with the exact method + evidence below.

---

## CLOSE #2 — Prod See AP/AR (A/R + A/P Aging reports under Reports) — **CLOSED, all 11 staging-counterpart roles OBSERVED**

**Why Pass-10 left this NV:** the prod reports route bounced the test-staff *self-login* to
`/workorders` (Pass-10 assumed a headless location artifact). This pass proved it is
**role-deterministic**, not an artifact.

**Method:** REAL role-holder **switch-user** for the 6 roles that have an active prod holder
(Administrator, Sales Representative, Service Advisor Technician, Technician, Time Clock User,
Office User) + **role-swap test-staff + switch-user** for the holderless roles. The switch-user
render path was **VALIDATED by a CONTROL**: test-staff role-swapped to **Administrator** +
switch-user **renders the full A/R + A/P Aging report tiles** (`_pr_recon`/…; `apar-final-proof.json`).
Deep-links to `/reports/ar-aging-summary` + `/reports/ap-aging-summary` and a **proof-of-shell**
(SM holds `punch_clock_reports` yet `/reports/punch-clock-activities` also bounces) confirm the
**entire Reports surface is a single FE all-or-nothing gate** (matches the project enforcement model).

**Prod OBSERVED (evidence `live-ui-2026-07-16/production/<role>/apar_*.png` + `apar-*.json`):**
| Prod legacy role | Observed | Evidence |
|---|---|---|
| Administrator | **SHOWN** (full A/R + A/P Aging tiles) | real-holder switch-user |
| Office User | **SHOWN** (full tiles) | real-holder switch-user |
| Sales Representative | **HIDDEN** (Reports nav present but click no-ops; deep-link bounces) | real holder |
| Service Advisor Technician | **HIDDEN** (nav no-ops) | real holder |
| Technician | **HIDDEN** (no Reports nav; bounce) | real holder |
| Time Clock User | **HIDDEN** (no Reports nav; bounce) | real holder |
| Service Manager | **HIDDEN** (whole Reports surface gated off; punch-clock also bounces) | role-swap+switch-user |
| Parts Manager | **HIDDEN** | role-swap+switch-user |
| Parts Technician | **HIDDEN** | role-swap+switch-user |
| Foreman | **HIDDEN** | role-swap+switch-user |
| Service Advisor / SA-No-Reports / SA-Limited-View | **HIDDEN** (deep-link bounce) | role-swap+switch-user |
| Reporting | bounce to `/no-location`, perms=0 (caveat) | merges into Sales Rep (anchored by real holder) |

**DUAL (vs staging Pass-10):** Admin/Office = **MATCH** (SHOWN). Service Manager / Parts Manager /
Sales Representative = **STAGING-MORE** (prod HIDDEN, staging SHOWN). Senior SA / Service Advisor /
Foreman / Technician / Parts Technician / Time Clock = **MATCH** (both hidden). Full table:
`apar-DUAL-verdicts.json`.

*Note:* no prod role carries a distinct `ar_aging`/`ap_aging` permission atom; AP/AR visibility is
gated by the broader Reports FE gate, so the **live UI is authoritative** (the per-role perm JSON
cannot classify it — not relied upon).

---

## CLOSE #1 — Prod Part Return ("Return" in a picked part's Part context menu) — **CLOSED, all 13 prod roles OBSERVED**

**Why Pass-10 left this NV:** no prod WO was found with a part in a returnable state, and
switch-user bounces the WO-detail page (null active location).

**Two unblocks used:**
1. The **"Return"** control lives in a picked part's **`[aria-label="Part context menu"]`** on the
   **WO lines view** (menu = **Move / Core OK / Return / Cancel Return**) — Pass-10 missed it because
   the button's text is not `more_vert`.
2. **switch-user bounces WO-detail** (location-scoped); **self-login has an active location**. So per
   role = **role-swap test-staff + SELF-LOGIN**. Reference WO **S1-719** (Truck Hill 1) already
   carries a returnable picked part (no seeding needed; existing WOs S1-102/S1-719 confirmed
   returnable). Admin baseline confirmed **Move/Core OK/Return** present.

**Prod OBSERVED (evidence `live-ui-2026-07-16/production/<role>/pr_ctxmenu.png` + `partreturn-final.json`):**
| Prod role | Part Return |
|---|---|
| Administrator | **SHOWN** (Move/Core OK/Return) |
| Service Manager | **SHOWN** |
| Service Advisor Technician / Service Advisor / SA-No-Reports / SA-Limited-View | **SHOWN** |
| Foreman | **SHOWN** |
| Technician | **SHOWN** |
| Parts Manager | **SHOWN** |
| Parts Technician | **SHOWN** |
| Office User | **HIDDEN** (WO detail renders, but the part menu shows only **"Move"** — no Return) |
| Sales Representative | **HIDDEN / not accessible** (WO detail bounces to /workorders) |
| Time Clock User | **HIDDEN / not accessible** (WO detail bounces to /workorders) |

**DUAL (vs staging):** Admin/Service Manager/Senior SA/Service Advisor/Foreman/Technician/Parts
Manager/Parts Technician = **MATCH** (both SHOWN). Sales Representative + Office User =
**STAGING-MORE** (prod HIDDEN / staging SHOWN). Time Clock User = **PARTIAL** (prod HIDDEN observed;
staging Time-Clock Part Return still NV from the prior pass). Full table: `partreturn-DUAL-verdicts.json`.

---

## FINAL MATRIX STATUS after Pass-11
- **Prod See AP/AR:** all 14 legacy roles / 11 staging counterparts OBSERVED (Reporting = observed-caveat).
- **Prod Part Return:** all 13 prod roles OBSERVED.
- **Both trust-critical NV clusters from Pass-10 are now closed with live evidence + dual verdicts.**

**Residual (single, precise):** staging **Time Clock User Part Return** is still NOT VERIFIED from
the prior pass (no live Time-Clock holder + staging `staff/change` 500 that day). Everything else
in the matrix is now observed. Unblock: a live staging Time-Clock session (holder or a working
role-swap window) — a staging-only gap, not prod.

## Cleanup (Pass-11)
- Prod test-staff (`1e19e572`) **RESTORED to Office User + Truck Hill 1** (`staff/change` 201, verified).
- **No `switch-user` impersonation left active** (final `exit-switch-user` returned 400 = none active).
- **No WOs created or deleted** (reused existing returnable WOs). Staging untouched this pass.
- **Minor authorized prod modifications (documented):** during recon I clicked **Pick** on one
  in-stock part on **S2-599** and one on **S1-443** (junk test WOs containing "sdfdsfsd" data;
  `POST /api/work-orders/part/perform-request-status-action` 201). These are normal, in-app-reversible
  WO operations on disposable test WOs; left as-is to avoid further state drift. Flagged for optional
  in-app revert.
- **No TestRail writes.**
