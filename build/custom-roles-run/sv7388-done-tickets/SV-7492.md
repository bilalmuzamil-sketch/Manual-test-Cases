# SV-7492 — [CRP-FE-07] View Mode source of truth + Tech-View restrictions

- **Type:** Task
- **Status:** Done
- **Resolution:** Done
- **Created:** 2026-06-01  **Updated:** 2026-07-14T08:37
- **Link:** https://shopview.atlassian.net/browse/SV-7492

## Description

> 🤖 **AI-first estimate: \~2.5d** _(range 1.5–2.5d — cost is the 4 net-new restrictions = real logic + QA)_

## Summary

PRD Story 17. Replace the non-existent view-mode source (isTechnician flags + non-reactive `v-can`) with a first-class reactive source, and enforce the 9 Tech-View restrictions.

## Scope

* `useViewMode()` composable off `userService.getRole()?.viewMode` (net-new `UserRoleResponse.viewMode`) → `isTechView`/`isFullView`. Remove legacy `v-can` usages.
* `isTechnician(Tech|SAT|Foreman)` vs `isOnlyTechnician(Tech)` collapses to `viewMode === 'tech'`; review the few genuine tech-only sites.
* 9 Tech-View restrictions; **4 net-new** (no-approve-WO, no-review-even-if-reviewWOs-on, limited-parts-request, read-only-after-approval) — riskiest, no existing site to repoint.

## Dependencies

Blocked by **CRP-FE-01**. Needs `viewMode` emission on login payload (**CRP-BE-06**) — can mock until available.

## Acceptance criteria

* \[ \] `useViewMode` composable; `v-can` removed
* \[ \] All 9 Tech-View restrictions enforced (incl. 4 net-new)
* \[ \] Browser-walk tech vs full
* \[ \] Vitest coverage

## Reference

Tech plan §5g. PRD S17.

## Comments (0 of 0)
