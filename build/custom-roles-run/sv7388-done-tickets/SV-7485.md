# SV-7485 — [CRP-BE-10] Legacy→v2 role migration rework (SSA/JSA split, no SalesRep escalation, dry-run diff)

- **Type:** Task
- **Status:** Done
- **Resolution:** Done
- **Created:** 2026-06-01  **Updated:** 2026-07-14T08:37
- **Link:** https://shopview.atlassian.net/browse/SV-7485

## Description

> 🤖 **AI-first estimate: \~1.5d** _(range 1–1.5d — cost is dry-run diff review + safe prod run; excl. exec window)_

## Summary

Rewrite `AssignV2RolesToExistingUsers` to migrate all existing users 15→12 correctly.

## Defects to fix (current command)

* Collapses all SA variants → must do the SSA/JSA split per PRD.
* **Escalates SalesRep/Reporting → Administrator** → must map both to `salesrep`.
* No DB transaction wrapper → add per-org/chunk transaction.
* Silent-skips leave `role_v2_id` NULL → **fail loud** on unmapped/missing target.

## Scope (corrected 15→12 map)

Owner→owner, Administrator→admin, ServiceManager→sm, ServiceAdvisor→ssa, SA-Technician→ssa, SA-NoReports→ssa, SA-LimitedView→jsa, Foreman→foreman, Technician→tech, PartsManager→pm, PartsTechnician→pt, SalesRepresentative→salesrep, Reporting→salesrep, Office→office, TimeClock→timeclock. Idempotent (`WHERE role_v2_id IS NULL`); org-scoped; **never reassign users already on a custom role**; `--dry-run` prints per-user `email | old→new | atom-count before→after`.

## Dependencies

Blocked by **CRP-BE-05** (role defaults) + **CRP-BE-07** (seeder, so 12 roles exist per org). Gated by **CRP-BE-08** atom-diff CLI before prod run.

## Acceptance criteria

* \[ \] Corrected mapping; no SalesRep/Reporting escalation
* \[ \] Transaction-wrapped, idempotent, fail-loud, custom-role-safe
* \[ \] `--dry-run` before/after diff
* \[ \] Tests: SA-Limited-View→jsa, SalesRep→salesrep (not admin), custom-role user untouched

## Reference

Tech plan §4g. PRD S23.

## Comments (0 of 0)
