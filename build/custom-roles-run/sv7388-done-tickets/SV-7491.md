# SV-7491 — [CRP-FE-06] Enforcement repoint: split mega-groups, ~150 call-sites, Locations isAdmin purification

- **Type:** Task
- **Status:** Done
- **Resolution:** Done
- **Created:** 2026-06-01  **Updated:** 2026-07-14T08:37
- **Link:** https://shopview.atlassian.net/browse/SV-7491

## Description

> 🤖 **AI-first estimate: \~3d** _(range 2–3d — AI does the \~150-site typing fast; cost is Locations blast-radius + auth-routing browser-walk)_

## Summary

The largest behavioral surface (PRD Stories 8–16 enforcement). **Not blind grep-replace.** Repoint cluster-by-cluster behind the CRP-FE-01 helpers, retiring each mega-group only at zero importers.

## Scope

* **Mega-group split:** `workOrdersGroup`→WO/WOLines/Timesheets + cross-toggles + woSettings; `partsGroup`→catalog/vendor; `customerGroup`→customers/partSales/AP-AR; `administrationGroup`→settings+subs; `scheduleGroup` clean (**pilot first**).
* `actionPermissionMap` (`permissionGuard.ts:15-166`, \~35 entries) — highest-leverage single file.
* `has('Locations')` isAdmin-proxy purification: implement real `userService.isAdmin()` off the BE admin-capability signal; repoint the 4 proxy sites (axios interceptor / impersonation / login redirect / routingService); leave the genuine Locations-page meta.
* `OrderPart`/`PickPart`: grep the **string literals** in config tables (not `has(`) — map to `woSettings.orderParts/pickParts`; add unit coverage.
* Sequence: schedule pilot → actionPermissionMap → nav → WO cluster → Customers/Parts → retire mega-groups + delete `PermissionsGroup.ts` at zero importers. Hide nav per no-access (PRD S15-N1 etc.).

## Dependencies

Blocked by **CRP-FE-01** + **CRP-FE-02**. Admin purification needs **CRP-BE-06** (admin-capability signal).

## Acceptance criteria

- [ ] Mega-groups retired; PermissionsGroup.ts deleted (zero importers)
- [ ] \~150 sites repointed; nav hides per area no-access
- [ ] Locations isAdmin proxy replaced; no auth-routing regressions (browser-walk)
- [ ] OrderPart/PickPart config gates covered by unit tests

## Reference

Tech plan §5f. PRD S8–S16.

## Comments (0 of 0)
