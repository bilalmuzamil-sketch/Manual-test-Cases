# SV-7490 — [CRP-FE-05] Permission Summary + roles list table updates + grouped Staff selector

- **Type:** Task
- **Status:** Done
- **Resolution:** Done
- **Created:** 2026-06-01  **Updated:** 2026-07-14T08:37
- **Link:** https://shopview.atlassian.net/browse/SV-7490

## Description

> 🤖 **AI-first estimate: \~1.5d** _(range 1–1.5d — cost is a few flows to browser-walk)_

## Summary

PRD Stories 1, 4, 5, 6 (FE). Read-only summary, list-table changes, and the Staff role selector.

## Scope

* `PermissionSummary.vue` (new; reuse `PermissionGrid :readonly`). Route `roles-permissions/:id/summary`. Entry points: list-page "View Permissions" action + Staff selector button (lazy dialog).
* `RolesPermissonsTable.vue`: add Template column (needs `templateId`+name), System badge, per-row Actions menu (Edit / Delete / View Permissions) replacing whole-row click, route Office/TimeClock → read-only summary, hide Delete for system roles, **Cannot-Delete modal** (confirm disabled when usersCount ≥ 1).
* `StaffDialog.vue`: grouped System/Custom selector + "View Permissions" button + optional logout toast. Recommend minimal in-place edit with grouping/summary extracted to a composable.

## Dependencies

Blocked by **CRP-FE-01** + **CRP-FE-03** (summary reuses the grid). Needs `templateId` on list payload (**CRP-BE-06**) — mock until available.

## Acceptance criteria

- [ ] Summary renders all settings ON/OFF; both entry points work
- [ ] List: Template column, badges, actions menu, Cannot-Delete modal
- [ ] Staff selector grouped System/Custom + View Permissions
- [ ] Vitest coverage

## Reference

Tech plan §5e. PRD S1/S4/S5/S6.

## Comments (0 of 0)
