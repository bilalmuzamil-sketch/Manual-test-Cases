# SV-7488 — [CRP-FE-03] Rebuild permission editor as CRUD V/E/D grid (+ cascade + slide-hide + financial modal + admin banner)

- **Type:** Task
- **Status:** Done
- **Resolution:** Done
- **Created:** 2026-06-01  **Updated:** 2026-07-14T08:37
- **Link:** https://shopview.atlassian.net/browse/SV-7488

## Description

> 🤖 **AI-first estimate: \~3d** _(range 2–3d — biggest FE build; pattern-driven, cost is cascade coverage + UX verification)_

## Summary

Replace the tree editor (`PermissionsGroup.{ts,vue}`) with the PRD CRUD View/Edit/Delete grid. Implements Stories 7, 12, 16, 18, 19 (editor behavior).

## Scope

* **Retire** `PermissionsGroup.{ts,vue}` + `permissionOptions`/`permissionLabels`.
* New components under `components/ts/administration/permissions/`: `PermissionEditor.vue` (container) + `PermissionGrid.vue` (9-area V/E/D) + `PermissionAreaRow.vue` (CRUD / WOLines-E-D / Timesheets-V-E variants) + `WoSettingsRow.vue` (always-visible; disabled when WO View off) + `PartsDepartmentSection.vue` (slide-hide children; OFF→ON resets) + `SettingsSection.vue` (slide-hide 6 subs; OFF→ON resets) + `CrossTogglesSection.vue` + Admin-warning banner. Mount in `CreateNewRole.vue`/`EditRole.vue`.
* **Cascade engine** — pure `cascade.ts`: `applyCrud` (Delete⇒Edit⇒View), `applyWorkOrders` (WO→WOLines coupling), `applyParentToggle` (resets), `applySeeFinancialData` (clears PartSales+Invoicing), `requiresFinancialConfirm`.
* `FinancialDataConfirmModal.vue` (via `BaseDialog`, lazy): "\[Area\] requires 'See Financial Data' to be enabled. Enable it?" Invoicing Delete column label = "Delete / Reverse".
* `data-test-id` on every checkbox/toggle; run `npx tsx scripts/e2e-precheck.ts`.

## Dependencies

Blocked by **CRP-FE-01**. Save-diff preview (`PreviewPermissionsModal`/`PermissionsGroupPreview`) must be rewritten to diff `RoleDraft` (coupled to the deleted tree).

## Acceptance criteria

- [ ] Grid renders 9 areas with correct column variants
- [ ] Cascade engine fully unit-tested (all branches)
- [ ] Slide-hide + reset behavior for Parts Dept & Settings
- [ ] Financial-data confirm modal + admin banner
- [ ] Save-diff preview rebuilt

## Reference

Tech plan §5c. PRD S7/S12/S16/S18/S19.

## Comments (0 of 0)
