# SV-7486 — [CRP-FE-01] Permission model foundation: RoleDraft + bundle catalog + permissionService helpers

- **Type:** Task
- **Status:** Done
- **Resolution:** Done
- **Created:** 2026-06-01  **Updated:** 2026-07-14T08:37
- **Link:** https://shopview.atlassian.net/browse/SV-7486

## Description

> 🤖 **AI-first estimate: \~1.5d** _(range 1–1.5d — cost is the union+_`satisfies` migration; vue-tsc green)

## Summary

Compile-blocking FE foundation. Everything else FE depends on this. Build against the CRP-00 contract (mock BE if needed).

## Scope

* New `RoleDraft` type + `src/services/permissions/bundleCatalog.ts` (`AREA_BUNDLES`/`TOGGLE_BUNDLES`/`WO_SETTING_BUNDLES`/`CROSS_TOGGLE_BUNDLES` + `serializeDraft`/`deserializeToDraft`) — the FE mirror of the BE mappings, names from CRP-00.
* Extend the `permissions` union (`permissionService.ts`) with area×level + cross-toggle + settings-sub + `partsDepartment` names; add intent helpers `canView/canEdit/canDelete(area)`, `seeFinancialData()/seeApArData()/viewHistoryLogs()`. **Keep** `has/hasAny/hasAll` public API unchanged (92 files). Keep mega-group arrays alive until the repoint ticket retires them.
* ⚠️ `permissionLabels satisfies Record<Permission,string>` (`PermissionsGroup.ts`) breaks the instant the union changes — delete it in the **same commit**.
* Add `type`/`templateId`/`viewMode`/cross-toggles to `UserRoleResponse` + `RoleResponse` + `RoleListItem` contract types.

## Dependencies

Blocked by **CRP-00**. Blocks all other FE tickets (FE-02..FE-08).

## Acceptance criteria

- [ ] Bundle catalog + serialize/deserialize round-trip (unit-tested)
- [ ] Union extended; project type-checks (vue-tsc green)
- [ ] permissionService helpers + tests
- [ ] Contract types updated

## Reference

Tech plan §5a.

## Comments (0 of 0)
