# LIVE-UI PASS 10 (2026-07-16) — closing the Pass-9 NOT-VERIFIED residuals

> Continues `live-ui-PASS9-2026-07-16.md`. Observed-only (Rules 10/12); nothing inferred.
> Sessions: staging quick-login (200) + fe-permissions (200); prod renewable self-login
> (`POST /api/login`, 200). Node via `NODE_USE_ENV_PROXY=1` + CONNECT-relay bridge;
> Chromium TLS1.2-max headless. All secrets in `/tmp` (chmod 600). No TestRail writes.

This pass attacked the three Pass-9 residual clusters with the specific techniques the
coordinator directed. Every verdict below is LIVE-OBSERVED (UI screenshot and/or captured
API response that run) or explicitly NOT VERIFIED with the precise remaining blocker.

---

## RESIDUAL 1 — Prod finance for Service Manager / Parts Manager / Parts Technician — **CLOSED (OBSERVED-LIVE)**

**Technique:** role-swap the prod test-staff (`1e19e572`) to each role → **self-login** as it
→ open invoiced WO **S1-518** (`19c185ed`) → click **Finance** → capture the panel's network
calls (esp. the estimate 400 body) + a direct API probe of the finance-data endpoints.

**Captured estimate-400 body (Service Manager, live):**
`POST /api/work-orders/invoices/estimate → 400 {"errors":[{"work_order_id":"Missing required parameter"}]}`
— a **generic "missing parameter" data error, NOT a permission/forbidden error.** The SAME
400 fires for **Service Advisor - Limited View**, which nonetheless **renders** its finance
panel — so the estimate-400 is universal and is NOT the cause of the bounce.

**Root cause is a role-deterministic FE branch (observed):**
- **SA-Limited-View (renders):** finance panel calls `GET /api/invoices/preview?invoice_id=… → 200`,
  stays on `/…/finance`, and the **New Payment / Reverse / Issue Credit** controls render.
  Screenshot `production/Service_Advisor_Limited_View/fin_classify.png` (150 KB, full panel).
- **Service Manager / Parts Manager / Parts Technician (bounce):** panel **never** calls
  `/api/invoices/preview`; it POSTs `…/invoices/estimate → 400` and the SPA **redirects to
  `/no-location`**. The payment controls never render. Screenshots
  `production/{Service_Manager,Parts_Manager,Parts_Technician}/fin_classify.png` (22 KB each,
  the "No location / Not existing page / Bad request" page). Everything else identical (same
  test-staff, workplace, WO) — only `role_id` changed — so this is **role-deterministic, not a
  headless artifact.**

**Direct finance-data API probe (live, per role) — `production/fin-apiprobe-results.json`:**
| Role | `/api/invoices/{id}/view` | `/api/invoices/{id}/settings/view` |
|---|---|---|
| Service Manager | **200** | 200 |
| Parts Manager | **200** | 200 |
| Parts Technician | **200** | 200 |
| Service Advisor - Limited View (control) | 200 | 200 |
| Office User (control) | **403 "Insufficient permissions"** | 200 |

**Observed verdict (prod):** SM / PM / PT can **read** invoice DATA via API (200) but the
**finance payment controls are NOT usable in the prod UI** — the Finance route deterministically
bounces to `/no-location`. Office User = hard 403 DENY (data too).

**DUAL verdicts (prod UI-observed vs staging Pass-8/9 live):**
| Staging role | Prod (finance payment controls, UI) | Staging (New Payment / Reverse / Credit) | Dual verdict |
|---|---|---|---|
| **Service Manager** | NOT usable (finance route → /no-location) | SHOWN / SHOWN / SHOWN | **STAGING-MORE** (payment controls) |
| **Parts Manager** | NOT usable (finance route → /no-location) | SHOWN / hidden / SHOWN | **STAGING-MORE** |
| **Parts Technician** | NOT usable (finance route → /no-location) | SHOWN / hidden / SHOWN | **STAGING-MORE** |

(Prod invoice-DATA read = 200 for all three — a nuance to note; the payment ACTIONS are the FE-gated controls that don't render.)

---

## RESIDUAL 2 — Staging Core OK/Not-OK + Part Return, 4 holderless roles — **CLOSED (OBSERVED-LIVE)**

**Technique:** instead of fragile headless seeding, an API scan found existing non-invoiced WOs
that already carry a **picked cored part** (`stg-cored-found.json`). Reference WO used:
**S9-25051** (`a4b0198d`), status **approved**, line `326eee0f`, picked cored part **AF26154**
(`core_charge=23`, `core_part_id` set, `core_resolved=false`). Role-swapped throwaway **+20**
(`0336686b`) through each role → **switch-user** → observed the picked-part `⋮` context menu live.

Admin baseline confirmed the controls exist on this WO: part menu = **Move / Return / Core OK**.

| Staging role | perms | Part Return ("Return") | Core OK/Not-OK ("Core OK") | Confidence |
|---|---|---|---|---|
| Service Manager | 36 | **SHOWN** | **SHOWN** | OBSERVED-LIVE |
| Foreman | 23 | **SHOWN** | **SHOWN** | OBSERVED-LIVE |
| Office User | 25 | **SHOWN** | **SHOWN** | OBSERVED-LIVE |
| Parts Technician | 19 | **SHOWN** | **SHOWN** | OBSERVED-LIVE |

Per-role perms differ and WO-Delete varies (SM shows "Delete Work Order", the others don't) —
proving genuine per-role sessions, not a leaked Admin session. Evidence:
`staging/<role>/core_lines.png` + `core_menus.png` + `core-return.json`. Throwaway restored to Admin.

**DUAL (prod side from Pass-8 `prod-run-all.log`, live-observed reference WO):**
- **Core OK/Not-OK:** prod SHOWN (`core: ok=2 / notok=2`) for SM/PM/PT/Foreman/Office → **MATCH** (both SHOWN).
- **Part Return:** staging SHOWN (this pass); **prod side NOT VERIFIED** — the prod reference WO
  had no part in a returnable lifecycle state (`partReturn=false` for all prod roles), a prod-DATA
  limitation, not a role gate. Precise residual (prod only).

---

## RESIDUAL 3 — Assign Vendor / Fix Part# / Bulk Receive / See AP-AR

### See AP/AR — **CLOSED staging (all 11 roles); prod = Office User observed, rest route-bounce**
"See AP/AR" = visibility of the **A/R + A/P Aging** reports under **Reports** (routes
`/reports/ar-aging-summary`, `…/ap-aging-summary`, etc.). Observed live per role.

**Staging (throwaway role-swap + switch-user; perms differ = genuine) — `staging/apar-per-role.json`:**
| Role | perms | See AP/AR (A/R + A/P Aging) |
|---|---|---|
| Administrator | 42 | **SHOWN** |
| Service Manager | 36 | **SHOWN** |
| Parts Manager | 31 | **SHOWN** |
| Sales Representative | 8 | **SHOWN** |
| Office User | 25 | **SHOWN** |
| Senior Service Advisor | 31 | hidden |
| Service Advisor | 25 | hidden |
| Foreman | 23 | hidden |
| Technician | 6 | hidden |
| **Parts Technician** | 19 | **hidden** |
| Time Clock User | 3 | hidden |

**Prod (test-staff self-login):** the reports route deterministically bounces every prod role to
`/workorders` EXCEPT **Office User** (reports rendered, **A/R + A/P Aging SHOWN**) — the same
headless location-store warm-up limitation seen with finance (roles even keep the Reports nav yet
the route bounces). An **API probe confirms AP/AR is FE display-gated, not backend-enforced** (all
prod roles return an identical `400` on the aging-report endpoints — matches the project enforcement
model), so the API cannot classify per-role. **Prod AP/AR left OBSERVED only for Office User; the
other prod roles are NOT VERIFIED (route bounce) — not inferred.**

Finalizable dual: **Office User See AP/AR = MATCH** (prod SHOWN / staging SHOWN).

### Bulk Receive — **BUILD FINDING: control does not exist in the Custom Roles build**
Admin exploration of **Purchase Orders** (`/parts/orders`) and **/parts/deliveries**: only a
single **"Receive"** button + **"New PO"**; **0 checkboxes, no multi-select / "receive selected" /
"bulk receive"** control anywhere. Bulk Receive is a Simple-Flow feature (Story 8), **not present**
in this build. Evidence: `_R3_Admin_explore/parts_orders.png`, `parts_deliveries.png`, `r3b-explore.json`.

### Assign Vendor / Fix Part# — **BUILD FINDINGS (located)**
Admin exploration on a **requested** part-request ("Brake pot", S9-25394):
- **Assign Vendor** does not appear as a standalone per-part control on the WO part-request row
  (row menu = Move / Return only). It surfaces inside the **New PO / Purchase-Order creation flow**
  (vendor selection) = part of the **Order Parts** capability, which was already observed per-role
  (22/22 dual MATCH) in the earlier Parts-Module pass.
- **Fix Part#** — **no distinct labeled control** found anywhere in the build (WO lines,
  part-request rows, PO detail). Evidence: `_R3_Admin_explore/assignvendor*.png`, `assignvendor.json`.

---

## FINAL MATRIX STATUS after Pass-10

**Newly OBSERVED-LIVE / resolved this pass:**
- Prod finance SM/PM/PT (9 payment-control cells) → OBSERVED (controls NOT usable, role-deterministic /no-location) + invoice-DATA 200 captured → 3 new STAGING-MORE duals.
- Staging Core OK + Part Return for SM/Foreman/Office/PartsTech (8 cells) → all SHOWN → Core OK MATCH×4 dual.
- See AP/AR staging all 11 roles (observed) + prod Office User (observed) → Office-User MATCH dual.

**Genuinely still NOT VERIFIED (precise, non-inferable blockers) — the short list:**
1. **Prod Part Return** (per role) — no prod WO has a part in a returnable lifecycle state; needs a
   dev/human-seeded received+picked returnable part on prod (prod is read-only for us).
2. **Prod See AP/AR for the non-Office roles** — the prod reports route bounces the test-staff
   self-login to `/workorders` for every role except Office User (headless location-store artifact);
   AP/AR is FE-gated so the API cannot substitute. Needs a real per-role credentialed prod login or
   an attended headful prod session.

**Build findings (not "NOT VERIFIED" — the control is absent by design):**
- **Bulk Receive**: no multi-select/bulk-receive control in the Custom Roles build (Simple-Flow feature).
- **Fix Part#**: no distinct labeled control in the build.
- **Assign Vendor**: only within the New-PO/Order-Parts flow (already covered by Order Parts observations).

## Cleanup (Pass-10)
- Prod test-staff (`1e19e572`) **RESTORED to Office User** (`d238a892`) — verified after each loop (201).
- Staging throwaway **+20** (`0336686b`) **RESTORED to Admin** (`5e2da32a`) — verified `role_label=Admin`.
- All `switch-user` impersonations exited (a lingering one from an interrupted run was cleared; exit 200).
- No data seeded (used existing WOs); **no WOs created/deleted** this pass; **no prod writes** (GET/impersonation-read only). **No TestRail writes.**
