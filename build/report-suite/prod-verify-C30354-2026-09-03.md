# C30354 — source-verify + PRODUCTION build-verify (2026-09-03)

**Case:** [C30354](https://shopview.testrail.io/index.php?/cases/view/30354) — "Filters; columns and
sort are remembered per browser before the first fetch" · Report Suite → **Parts Velocity** (section
"PV — Columns & Remembered View") · refs SV-8644 (story) under epic SV-8582 · **`custom_atmstatus = 3`
(AUTOMATED)** → Rule 71: **read-assessed only, NOT edited.**

## 1 · Source verification (documents) — CURRENT ✅
Latest of every source pulled live 2026-09-03 (per the standing "source-verify fetches latest first" directive):
- **PV spec** (Confluence 620888066): case was read **2026-08-17**; page last modified **2026-08-20**.
  The two post-read change-log entries — 2026-08-17 (S6-R10a CSV number formatting) and 2026-08-20
  (S5-R4 Demand counts distinct transactions) — do **not** touch **S4-R6 / S1-R2** (remembered view),
  which is what this case tests. So the case's expected behaviour is **still current**.
- **Story SV-8644** ("Velocity - Story 4 - Columns & Remembered View"): points at the spec, no
  contradicting acceptance criteria.
- **Minor inconsistency (not a behaviour issue):** the case `refs` says "PV spec **v10**" while its
  provenance line says "**version 11**". Cosmetic; both predate today.

## 2 · Production build verification — one DEVIATION found
Verified live on **app.shopview.com** (prod), signed in as the QA lead's account (administrator, 42
permissions, `reportsPageAccess`), read-only. Report is at **`/reports/parts-velocity`**.

| What the case/S4-R6 expects | On prod | Verdict |
|---|---|---|
| Columns + sort remembered **per browser**, before first fetch, survive reload | Stored in **localStorage `report_table:parts-velocity`** = `{version,view:{sortBy,descending,columns}}`. Default = **demand-desc, 14 columns** (matches the case's stated first-visit defaults). Set sort=Revenue-desc → saved → **restored after full reload** (same browser). | **PASS** (the case's title claim) |
| Saved view **applied before the first data fetch**, precedence over defaults | The first report API call carried the saved filter (`GET /api/reporting/reports/parts-velocity?type=…`) — the saved value drove the initial fetch, not the default. | **PASS** |
| The whole remembered view is **"in this browser (NOT tied to the user account)"** (S4-R6 verbatim) | **DEVIATION.** Columns+sort are per-browser (localStorage) ✅, but the **FILTERS (Type, date range) are persisted SERVER-SIDE, tied to the USER ACCOUNT.** Proof: after setting Type=Special Order, a **brand-new login (new PHPSESSID) in a brand-new browser context (fresh localStorage)** loaded the bare `/reports/parts-velocity` and the server **redirected it to `?types=special_order`** with no `types` value anywhere in localStorage / sessionStorage / cookies. So a different browser for the same user inherits the filter — the opposite of "not tied to the user account". | **FAIL vs S4-R6** (filters half) |
| Date = Last Month · a Category · Turns/Yr column toggle | **NOT VERIFIED** this session — the Quasar filter menus were not reliably drivable headlessly (Type and sort were). The mechanisms are confirmed (filters → account/server; columns → localStorage array), but these three specific controls were not individually driven. | NOT VERIFIED |

**Net:** On the case's own literal steps (single browser: set view → leave/return → reload) **everything
restores, so a tester would mark it PASS.** The deviation only surfaces across browsers/logins: **S4-R6
requires the remembered view to be per-browser and NOT account-tied, but the build persists the filters
server-side per account.** Columns + sort are correctly per-browser.

## 3 · Handling (rules)
- C30354 is **AUTOMATED** → **not edited** (Rule 71). Reported with its C-id (Rule 8), no ticket, no
  candidate file (Rule 62/57, 2026-09-01: a pass ends in a runnable test; the deviation is surfaced for
  the QA lead, who re-verifies on the build before any ticket).
- **Cleanup:** testing set the account's Type filter to "Special Order" (account-tied). It was
  **restored to default ("All types")** via the Type menu's "Select all" — a fresh-context load now
  returns the bare `/reports/parts-velocity`. Sort changes were per-browser (throwaway contexts only),
  so nothing else on the account or the QA lead's own browser was altered.

## 3b · Made RUNNABLE (QA lead go-ahead, 2026-09-03) — preconds + steps only
The QA lead approved editing this AUTOMATED case to make it tester-runnable. **Preconditions and
steps ONLY** were rewritten from spec-level to the concrete UI route observed on the build:
- **Preconds:** sign in → top nav "Reports" → left menu "PARTS" → "Parts Velocity" (filter chips
  Date/Type/Category/Vendor/Bin/Location) → pick a shop + date range with parts data.
- **Steps:** set Type=Inventory, Date=Last Month, a Category, columns button → Turns / Yr on,
  Revenue header → descending; leave via another left-menu report and return; reload (F5).
- **Expected Results, title, `custom_atmstatus` (=3), `custom_automation_type` (=0): UNCHANGED**
  (verified by API re-GET before and after). The `AUTOMATION: READY` marker is intact and last.
- Written through the **UI editor** (Froala) so both fields serve in `markdown fr-view`; runnable
  gate `check_runnable_cases.py --cases 30354` → RUNNABLE (1/1).
- **Rule 65:** case is AUTOMATED → Vlad notified via
  `c30354-runnable-2026-09-03/FOR-VLAD-C30354-automated-case-changed-2026-09-03.md`.
- **Rule 71 note:** `custom_automation_type` left `0 (None)` — the go-ahead was scoped to
  runnability; setting the type is a separate edit awaiting its own go-ahead.

## 4 · Prod access method used (for reuse)
`POST https://api.shopview.com/api/login {username,password}` → PHPSESSID (no SSO cookie) +
`GET /api/auth/me/fe-permissions` → hydrate a Chromium context (cookie PHPSESSID on `.shopview.com` +
localStorage `user={data:…}` / `fe_permissions_wrapper` / `token`). **Correction to playbook §K:**
Chromium can no longer TLS straight through `$HTTPS_PROXY` on prod (ERR_CONNECTION_RESET) — the **local
MITM bridge is now required** (same as QA/staging), contrary to the 2026-07-29 "no bridge needed" note.
Secrets kept in `/tmp/prod` (chmod 600), never committed.
