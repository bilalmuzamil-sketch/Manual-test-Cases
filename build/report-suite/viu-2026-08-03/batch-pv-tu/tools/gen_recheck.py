#!/usr/bin/env python3
"""Render RECHECK-ROWS.md — the rows to merge into the master RECHECK-QUEUE.md (Rule 49)."""
import csv, os
from collections import Counter
B = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
rows = list(csv.DictReader(open(os.path.join(B, 'verdicts.csv'))))
BUILD = 'v3.4.1-0ed4433'
L=[]; w=L.append
w('# RE-CHECK ROWS — Parts Velocity + Technician Utilization (2026-08-03/04)')
w('')
w('> **STATUS: OPEN.** These rows belong in the master `../RECHECK-QUEUE.md`. **Do not merge them')
w('> yourself if another worker is editing that file** — hand this file to the coordinator.')
w('>')
w('> **CHECK THIS AT EVERY SESSION START** for the Report Suite, before and after any work on it,')
w('> and **immediately** when the branch is declared final or the app-version marker changes')
w('> (`curl -s https://sv8582.qa.shopview.com/ | grep app-version`).')
w('>')
w('> **NEITHER REPORT MAY BE CALLED VIU-COMPLETE WHILE THIS QUEUE IS OPEN** (Standing Rule 49).')
w('')
w('## BUILD MARKER (the thing that makes a re-check meaningful)')
w('')
w('| Marker | Value at run START | Value at run END |')
w('|---|---|---|')
w('| App version | **v3.4.1-0ed4433** | **v3.4.1-0ed4433** |')
w('| `index.html` last-modified | Mon, 03 Aug 2026 13:40:38 GMT | Mon, 03 Aug 2026 13:40:38 GMT |')
w('| `index.html` etag | 02091e9dc11f187d7739b4efa166ea21 | 02091e9dc11f187d7739b4efa166ea21 |')
w('| Observed (UTC) | 2026-08-04 ~00:50 | 2026-08-04 ~02:16 |')
w('')
w('**The build did NOT change during the run**, so every observation in this batch belongs to one')
w('single build state.')
w('')
tally = Counter(r['verdict'] for r in rows)
w('## SCOPE')
w('')
w('**%d rows — every case in this batch is queued**, because every verdict was taken from a' % len(rows))
w('non-final build: Parts Velocity 71 + Technician Utilization 60. Breakdown: %d PASS, %d DEVIATION, %d EXTERNAL-DEPENDENCY.'
  % (tally['VIU-Observed-PASS'], tally['DEVIATION'], tally['EXTERNAL-DEPENDENCY']))
w('')
w('**Priority order for the re-run** (highest value first):')
w('')
w('1. The **4 EXTERNAL-DEPENDENCY** rows — they are blocked on environment configuration, so they')
w('   are the rows most likely to become answerable. TU-ELL-04, TU-ELL-05, TU-SORT-05, PV-PREC-02.')
w('2. The **32 DEVIATION** rows — each will either be FIXED (flip to PASS and report the fix) or')
w('   CONFIRMED (escalate as a defect or a PO question). **A row that flips is a finding in its own')
w('   right and must be reported, not quietly corrected.**')
w('3. The **11 PASS rows that carry a stated narrower limit** (listed below) — these have a specific')
w('   thing left to drive.')
w('4. The remaining PASS rows — re-confirm.')
w('')
w('## THE 11 PASS ROWS WITH A SPECIFIC OUTSTANDING SUB-CHECK')
w('')
w('| Case | C-id | What is still owed |')
w('|---|---|---|')
for r in rows:
    if r['verdict'] == 'VIU-Observed-PASS' and r['recheck_obligation'] != 'Re-run this observation when the branch is declared final or the app-version marker changes.':
        w('| %s | [%s](%s) | %s |' % (r['internal_id'], r['testrail_case_id'], r['testrail_link'], r['recheck_obligation']))
w('')
w('## FULL QUEUE (%d rows)' % len(rows))
w('')
w('| Internal ID | C-id | Link | Verdict this run | Observed on | Re-check obligation |')
w('|---|---|---|---|---|---|')
for r in rows:
    w('| %s | %s | [open](%s) | %s | %s | %s |' % (r['internal_id'], r['testrail_case_id'], r['testrail_link'], r['verdict'], BUILD, r['recheck_obligation']))
w('')
w('## HOW TO RE-RUN (nothing needs re-deriving)')
w('')
w('All the drivers are in `tools/` and are secret-free — they read credentials from')
w('`/tmp/report-suite-viu/cookies.json` at runtime. Run node with `NODE_USE_ENV_PROXY=1`.')
w('')
w('| Purpose | Command |')
w('|---|---|')
w('| Re-read the build marker | `curl -s https://sv8582.qa.shopview.com/ \\| grep app-version` |')
w('| Any single export, with a timeout and a request-id capture | `node tools/dl.mjs <slug> <name> "<query string>" 120000` |')
w('| PV UI sweeps | `node tools/pv_ui.mjs`, `pv_ui2.mjs`, `pv_ui3.mjs`, `pv_ui4.mjs` |')
w('| PV calculation contract from live payloads | `node tools/pv_calc.mjs`, `pv_extra.mjs`, `last_gaps.mjs` |')
w('| Core exclusion | `node tools/core_check3.mjs`, `core_final.mjs` (re-seed with `seed_core*.mjs` if needed) |')
w('| TU UI sweeps | `node tools/tu_ui1.mjs`, `tu_ui2.mjs`, `tu_ui3.mjs`, `tu_coltoggle.mjs` |')
w('| TU clock seeding (This-Month data, the "Multiple" row, an open clock) | `node tools/tu_seed2.mjs`, then clean up with `cleanup.mjs` + `cleanup2.mjs` |')
w('| Permission matrix, single-location subject | `node tools/perms.mjs`, `perms2.mjs`, `singleloc2.mjs` |')
w('| First-visit defaults, deep links, visual, dark mode | `node tools/final_ui.mjs` |')
w('| The em-dash Est. Lost Labor blocker | `node tools/unrated.mjs` (safe — it restores itself and, on this build, the flip does not even persist) |')
w('| Rebuild these deliverables | `python3 tools/gen_verdicts.py && python3 tools/gen_docs.py && python3 tools/gen_recheck.py` |')
w('| PDF text extraction | `pdftotext -layout <file>.pdf <file>.txt` (poppler-utils; `pdfinfo` for page size, `pdfimages -list` for an embedded logo) |')
w('')
w('## POST-RE-RUN CHECKLIST')
w('')
w('1. Re-capture the build marker first and write it beside the old one.')
w('2. Work the priority order above; flip each row to **CONFIRMED** or **CHANGED** with fresh evidence.')
w('3. Report every **CHANGED** row explicitly — it is a finding.')
w('4. Close the queue only when **100%% of the %d rows** are re-verified (no sampling).' % len(rows))
w('5. Update `VERDICTS.md`, `verdicts.csv` and `STAGED-CHANGES.md` in the same pass, and update the')
w('   cross-project outstanding register.')
open(os.path.join(B, 'RECHECK-ROWS.md'), 'w').write('\n'.join(L) + '\n')
print('wrote RECHECK-ROWS.md')
