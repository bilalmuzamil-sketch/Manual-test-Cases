# Report Suite — Current Confluence Spec Capture & Diff SUMMARY (2026-07-28)

**Phase 1 deliverable** — captured the six current Confluence spec pages verbatim and diffed each against our ingested spec (`build/report-suite/specs/*.md`, ingested 2026-07-22). Access method: **Atlassian MCP `getConfluencePage`** for page bodies (authenticated as Bilal Muzamil) + **Confluence REST** (`/wiki/rest/api/content/{id}?expand=version`) via the live session cookie for exact version metadata. All 6 pages fetched. NO TestRail writes, no case edits.

## Table A — per-report current version / date / diff result

| Report | pageId | Current version | Last updated | Sections ADDED / REMOVED / CHANGED since our ingest |
| --- | --- | --- | --- | --- |
| SBC (Sales By Customer) Report | 577634305 | v11 | 2026-07-21T07:12:55Z | **0 / 0 / 0** — identical requirement-ID set + identical change-log latest entry; no material change since 2026-07-22 ingest |
| SBR (Sales By Representative) Report | 585629698 | v14 | 2026-07-21T07:13:08Z | **0 / 0 / 0** — identical requirement-ID set + identical change-log latest entry; no material change since 2026-07-22 ingest |
| Parts Velocity Report | 620888066 | v3 | 2026-07-17T03:41:45Z | **0 / 0 / 0** — identical requirement-ID set + identical change-log latest entry; no material change since 2026-07-22 ingest |
| Technician Utilization Report | 641400833 | v4 | 2026-07-17T03:44:06Z | **0 / 0 / 0** — identical requirement-ID set + identical change-log latest entry; no material change since 2026-07-22 ingest |
| WIP (Work In Progress) Report | 703660034 | v5 | 2026-07-21T07:13:22Z | **0 / 0 / 0** — identical requirement-ID set + identical change-log latest entry; no material change since 2026-07-22 ingest |
| Inventory Value Report | 720142338 | v2 | 2026-07-21T07:13:15Z | **0 / 0 / 0** — identical requirement-ID set + identical change-log latest entry; no material change since 2026-07-22 ingest |

**Diff method (Rule 15/17):** normalized text similarity 0.92–0.99 across all six (residual = formatting only: our ingest used BeautifulSoup pipe-tables, this capture used html2text); and an exact requirement-ID set comparison found **0 IDs added and 0 IDs removed** on every report; and each report's change-log **latest entry matches** what our ingested spec recorded. Conclusion: **the current Confluence specs are UNCHANGED (content-wise) since our 2026-07-22 ingest.** The reconciliation work ahead is therefore driven by the KICKOFF VIDEO deltas, not by spec drift.

Per-report latest change-log entry (current == ingested):

- **SBC (Sales By Customer) Report** — 2026-07-21 (Milan review resolution: Story-18 server-backed type-ahead Customer filter + explicit all-customers state; 10,000-row export cap on CSV/PDF/Print; asset grouping by vehicle id; expand-all scoped to current page)
- **SBR (Sales By Representative) Report** — 2026-07-21 (Milan re-review: 5 payment states -> 3 display values mapping as single source of truth for badge + Invoice-Status filter; Expanded-View-PDF 10,000-row cap; expand-all/detail-page-size tech notes)
- **Parts Velocity Report** — 2026-07-16 (server-side data model per Milan: server-paginated; all filters + sort + null-placement server-side; Custom capped 366 days; page-local toolbar search confirmed)
- **Technician Utilization Report** — 2026-07-16 (Milan review: All Time removed [Story 10 deleted]; Custom capped 366 days; per-day breakdown lazy-loads on expand; single report-level timezone = active workplace)
- **WIP (Work In Progress) Report** — 2026-07-21 (Milan review + Chris override: nightly WIP snapshot capture moved IN SCOPE [new Story 11]; WO# drill-through opens SAME tab; "All Time" removed, default now "This Week")
- **Inventory Value Report** — 2026-07-21 (server-side data model per Milan, mirroring Parts Velocity: server-paginated; server-side sort; Category/Vendor filters server-side; totals row server-computed over full filtered set; page-level part search; server-side exports + 10k row cap; snapshot retention S11-R6)

## Table B — video-delta items vs the CURRENT spec (which are spec-confirmed to apply vs still ambiguous)

For each kickoff-video delta (`chris-answers-2026-07-28/video-deltas-2026-07-28.md`): is it already in the CURRENT Confluence spec? Yes = spec-confirmed (safe to apply at reconciliation). No/Partial = pending Chris's spec update (flag; do NOT edit cases until the spec catches up — Rule 23).

| # | Video delta item | In current spec? | Verdict + citation (per report) |
| --- | --- | --- | --- |
| 1 | "All time" date filter removed? | YES | **SBC:** YES — S2-R2 lists 11 presets (Today…Custom), NO "All Time"; change-log 2026-07-16 "Removed the 'All Time' date range".<br>**SBR:** YES — S2-R2 presets, NO "All Time"; change-log 2026-07-16 "Removed All Time (D1)".<br>**PV:** YES — §2 Out of Scope: "An 'All Time' date range … deliberately not offered"; S2-R2 preset list has no All Time.<br>**TU:** YES — S1-R4 Custom capped 366d, no All Time; change-log 2026-07-16 "Removed All Time (deleted Story 10)".<br>**WIP:** YES — S7-R6 "'All Time' is not offered"; §3 + change-log 2026-07-21 removed All Time, default now This Week.<br>**IV:** YES — S5-R1 "It does not offer 'All Time'"; §2 Known Limitations "No 'All Time' date option".<br>**Overall:** YES (suite-wide). Matches video P9. Note Stefan's ~365-day backend cap as a VIU/data caveat. |
| 2 | Asset identifier = SERIAL NUMBER (not unit number)? | NO | **SBC:** NO — S8-R8 asset-label suffix priority = "· Unit {unit}" -> plate -> "VIN …{last 8}" -> none. Still UNIT NUMBER; "serial number" appears nowhere.<br>**WIP:** NO — S4-R7 Asset cell = unit number (line 1) + VIN (line 2); S4-R9 "sorts by unit number"; §4 "Asset … identified by its unit number and its VIN". Still UNIT NUMBER.<br>**Overall:** NO — current spec still uses UNIT NUMBER (SBC + WIP). Serial-number change (video P24, FIRM) is NOT yet in the spec; pending Chris's local + spec update. |
| 3 | Print button on Sales By Customer removed? | NO | **SBC:** NO — Story 16 "Print the report" is fully present (S16-R1..R6); S14-R1 overflow menu holds "Print"; §7 lists "Print PDF fails". Print STILL PRESENT.<br>**Overall:** NO — Print is STILL in the current SBC spec (Story 16). Removal (video P25, FIRM pending Chris spec-cut) not yet reflected. |
| 4 | "Sales by Representative" naming (not Associate)? | YES | **SBR:** YES — S1-R1 "'Sales By Representative' … the full word 'Representative,' not the 'Rep' shorthand"; S1-R5 page title "Sales By Representative"; §1 naming note. "Associate" appears NOWHERE.<br>**Overall:** YES — current spec already mandates "Sales By Representative"; "Associate" is only Chris's wrong LOCAL nav label (video P5). Confirmed / no case change needed. |
| 5 | Compressed/expanded download view on Sales By Customer? | NO | **SBC:** NO — SBC exports are a single FLAT shape (Customer then Invoice rows, no asset layer) — S14-R6/S14-R10 (CSV), S15-R16 (PDF). There is NO Summary-vs-Expanded / compressed-vs-expanded option.<br>**SBR:** YES (already, different report) — SBR HAS Summary + Expanded for BOTH PDF and CSV (S14-R1: four downloads). This is where the "compressed/expanded" concept already lives.<br>**TU:** YES (already, different report) — TU has Summary PDF + Expanded-View PDF (S7-R2/R3) + one CSV.<br>**Overall:** NO for SBC — current SBC spec has only the single flat export. Adding a compressed/summary download (video P21, FIRM, Parth's idea Chris agreed live) is NOT yet in the spec. |
| 6 | "All locations" option + location label on every report? | PARTIAL | **SBC:** PARTIAL — All-locations option YES (S4-R3 "All locations" pinned top). Per-row location LABEL: NO (tree is Customer->Asset->Invoice; no Location column).<br>**SBR:** PARTIAL — All-Locations option YES (S21-R1). Per-row location label: NO (no Location column).<br>**PV:** PARTIAL — All-Locations option YES (S2-R9 rightmost, + "All Locations"). Per-row location label: NO explicit Location column in the 20-column set (S4-R4), though inventory rows are per-location (S3-R1a).<br>**TU:** PARTIAL — All-Locations option YES (S9-R1). Per-row location label: NO (hours pooled across locations into one row per tech, S9-R4 — no per-row location identifier).<br>**WIP:** YES — All-locations option YES (S7-R9). Per-row location label YES — a "Location" column exists (S4-R1, off by default; export header "Branch", S9-E1).<br>**IV:** PARTIAL — All-locations option YES (S7-R1). Per-row location label: NO (one row per part per location, S2-R3, but no Location identifier column in S3-R1).<br>**Overall:** PARTIAL (suite-wide). The "All locations" OPTION + accessible-location scoping is on ALL SIX (YES). The per-row/section LOCATION LABEL when All-locations is active (video P10 second half, FIRM) exists only on WIP; the other five have no per-row location identifier — pending add. |
| 7 | Snapshot "taken X days ago" label removed? | YES | **PV:** YES — No "snapshot taken N days ago" label anywhere in PV v3. ("Last Sale = N days", S5-R4, is a different per-part metric, not a snapshot-age label.)<br>**WIP:** YES — Story 11 nightly snapshot is capture-only; S11-R7 "No screen in this version reads the snapshot" — no age label displayed.<br>**IV:** YES (label absent) — but keep "As of"  — No "taken N days ago" label. IV's S5-R5 "As of <date>" indicator is a DIFFERENT indicator the video says to KEEP.<br>**Overall:** YES — no "snapshot taken X days ago" label exists in any current spec (consistent with video P32 removal). IV "As of <date>" indicator is retained (correct). P32 is PENDING-SPEC but the label is genuinely absent from current text. |
| 8 | Catalog naming (special-order parts) — renamed? | NO | **PV:** NO — Current spec uses "Catalogue" throughout — Type filter Both/Inventory/Catalogue (S2-R1); §4 Terminology "Catalogue part". NOT renamed.<br>**Overall:** NO — PV still uses "Catalogue". A rename/truncation (video P31) is an OPEN DECISION, not yet decided or in the spec. |
| 9 | Location filter gated by custom roles/permissions (hidden when <=1 location)? | NO | **SBC:** NO — Location scoping by accessible locations present (S4-R2/R9), but no "hide filter when <=1 location" rule stated.<br>**SBR:** NO / contradicted — S21-R5 scopes to accessible locations, but S21-N1: "A single-location user STILL SEES the filter with one selectable location" — opposite of hide-when-<=1.<br>**PV:** NO / contradicted — S2-R9 scopes to accessible locations; S2-E4: "A user with access to only one location STILL SEES the Location filter with a single selectable location."<br>**TU:** NO / contradicted — S9-R5 scopes to accessible; S9-N1: "A user with access to only one location still sees the filter with a single selectable location."<br>**WIP:** NO — S7-R11 scopes to accessible locations; no visibility-gating (hide-when-<=1) rule.<br>**IV:** NO / contradicted — S7-R4 scopes to accessible; S7-N1: "A user with access to only one location still sees the filter with a single selectable location."<br>**Overall:** NO — permission SCOPING (only accessible locations) is present everywhere, but the custom-role VISIBILITY GATING (hide the filter when the user has <=1 permitted location, video P33) is NOT in the spec and is directly CONTRADICTED by the single-location "still sees the filter" requirements on PV/TU/IV/SBR. P33 is PENDING-SPEC — Chris to add the permission-gating section. |
| 10 | Column selector present/absent per report (esp. TU, IV)? | TU absent / IV present / others present | **SBC:** PRESENT — Story 13 "Show or hide columns" (S13-R1..R8); 9 toggleable columns.<br>**SBR:** PRESENT — Story 20 column selector (S20-R1); 7 toggleable metric columns.<br>**PV:** PRESENT — Story 4 column picker (S4-R1) over all 20 columns; 14 default-visible / 6 hidden.<br>**TU:** ABSENT — No column-selector story; toolbar S8-R3 = download menu, technician filter, date picker, location filter only. Matches video P18 (Chris vetoed).<br>**WIP:** PRESENT — Story 8 "Column Selection and Persistence" (S8-R1); Total column always on.<br>**IV:** PRESENT — Story 8 "Column Selection and Persistence" (S8-R1); Margin + Total Sell off by default (S3-R13); Total Cost always on.<br>**Overall:** TU = ABSENT (matches video P18 — Chris vetoed a selector). IV = PRESENT (has a column selector). All other four reports = PRESENT. (Video P18 says Chris is "not married to it / will follow the spec" — spec currently has none on TU.) |
| 11 | Labor Delta green(+)/black(0.0)/red(-) rules? | YES | **SBC:** YES — S12-R3 "+" green / S12-R4 "-" red / S12-R5 "0.0" default(black); column "Inv. Hrs" = hours invoiced - hours worked (§4). Matches video P14.<br>**SBR:** YES — S9-R3 "+" green / S9-R4 "-" red / S9-R5 "0.0" default; "Inv. Hrs" = hours invoiced - hours worked (§3).<br>**WIP:** YES (color) / basis differs — S4-R24 green positive / red negative / default at 0.0 for "Inv. Hrs"; BUT S4-R23/§4 basis = quoted (Est.) labor hours - worked, i.e. vs ESTIMATE, whereas video P14 says clocked/tech vs INVOICED. WIP is a pre-invoice report so uses quoted hours — flag for VIU.<br>**Overall:** YES for the green(+)/black(0.0)/red(-) rendering on SBC, SBR, WIP. Formula basis: SBC/SBR = hours invoiced - hours worked (matches video P14); WIP = quoted(Est.) - worked (documented WIP-specific difference — confirm at VIU whether P14's invoiced-basis should apply). |

### How the next phase should read Table B
- **YES items (apply):** V1 All-Time removed; V4 "Sales By Representative" naming; V7 no "snapshot taken X days ago" label; V10 TU has no column selector / IV has one; V11 labor-delta color rules (SBC/SBR/WIP). These are spec-confirmed — cases already match or need only VIU confirmation.
- **NO / PARTIAL items (flag, do NOT edit yet — pending Chris's spec update, Rule 23):** V2 serial-number asset identifier (P24 FIRM, still "unit number"); V3 remove SBC Print (P25 FIRM, still present); V5 add SBC compressed/expanded download (P21 FIRM, not present); V6 per-row location label on the five non-WIP reports (P10 FIRM); V8 rename PV "Catalogue" (P31 OPEN); V9 hide location filter when <=1 permitted location (P33 PENDING, currently contradicted by "still sees the filter" rules).

### Honesty / limits (Rules 12/17/22)
- This phase is spec-capture + text-diff only. **No live-build (VIU) check was run** — every "present/absent" above is read from the SPEC TEXT, not observed in the QA build. Live VIU is a later phase and needs the QA branch + fresh cookies.
- The six `*-current.md` files are the verbatim page bodies (REST storage-format → markdown via html2text, unicode-preserving); minor list/table rendering differs from the Confluence WYSIWYG but wording, tables, and the full change-log are preserved.

