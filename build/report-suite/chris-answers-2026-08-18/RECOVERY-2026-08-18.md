# RECOVERY — Report Suite WIP / Chris Ward grain follow-up (crashed worker) — 2026-08-18

A worker crashed mid-run while doing the **C30528 grain reword** + **run 359 sync** follow-up to the
Chris Ward WIP ruling pass. This recovery established truth from live (Rule G), finished the two
interrupted actions, and byte-verified everything. **Build DEFERRED (app not opened). No Jira. Foreign
(Vladimir, id 1) untouched.**

---

## 1. What the crashed worker had actually done (established from the committed oplog + live)

The **main pass** (commits `8b635246` → `45c8ec17` → `a6fe9e38` → `80a47281`) was complete and committed
**before** the crash. Its authoritative oplog (`testrail-execution-log.md`) records exactly **5 ops, all
byte-verified**:

- `update_case` **C30456, C30458, C30464, C30493** (WIP Q1=A / Q2=B edits) — landed, committed.
- `add_case` **WIP-PLACE-05 = C43979** (section 4352) — landed, committed.

The crash happened during the **follow-up**: reword **C30528** (WIP nightly-snapshot case) to Chris's new
grain, and sync **run 359** to add C43979. Neither was committed. Live check confirmed the crash point:

- **C30528 was NOT written pre-crash** — no oplog entry; live `refs` still cited **WIP spec v21** and the
  expected still asserted the OLD grain *"one row per work order per calendar date"*.
- **Run 359 was NOT synced** — C43979 was not a member (507 tests, matching the STAGED snapshot).

The worker had, however, fully prepared the correct payload (`/tmp/build_c30528.py`) and written the
authoritative source file `chris-answer-snapshot-grain-2026-08-18.txt` (previously **untracked** — now
committed, because the C30528 provenance line links to it).

## 2. The "SBR / v22" anomaly — CHECKED AND RESOLVED: it was NOT an error

The recovery brief warned the worker's last log referenced *"S11-R1/R2 v22"* and feared **v22 = the SBR
spec**, i.e. a wrong anchor about to be written to C30528. **That premise is wrong.** Established from
**live Confluence** (`GET /wiki/rest/api/content/703660034?expand=version` → HTTP 200):

> **WIP page 703660034 is at version 22**, published by **Chris Ward** at **2026-08-18T17:01:44Z**,
> change message *"Story 11 snapshot grain -> per work order per tab (Bilal Q)"*.

So **v22 IS the current WIP spec version** (not SBR). Chris moved the WIP spec himself during the pass, in
response to Bilal's grain question. Live v22 **S11-R1** now reads: *"Once per day the system records one row
per then-open work order per tab … Rows are keyed by work order, tab, and calendar date."* Under Standing
Rules 32 / 57 / 59 (latest authoritative source wins), **v22 is the correct anchor** — the worker's prepared
payload was right, not a mistake.

## 3. C30528 — final correct state (WRITTEN + byte-verified this recovery)

`update_case/30528` executed with all text fields sent (to defeat TestRail's omit-field re-render).
Re-GET byte-verified: **title / custom_expected MATCH; refs MATCH (normalised); preconds + steps
byte-identical to pre-snapshot; section / template / type / atmstatus / automation_type / priority /
created_by / created_on all byte-identical to pre.** ALL fields verified, 0 collateral.

| Field | Final live value |
|---|---|
| Title | `Nightly snapshot records one row per then-open job per tab per calendar date` |
| Grain | **per-work-order-per-tab** — one row per work order per tab per calendar date (quotes Chris; agrees with C30456/58/64 and new C43979) |
| refs | `SV-8667 (WIP spec v22 2026-08-18 Story 11 S11-R1; tech-plan-2026-07-29 B1.2 …; Story 11 S11-R2)` — single entry, 154 chars ≤248, no commas |
| Provenance (Rule 54 sentence 1) | epic SV-8582 + story SV-8667 (WIP Story 11) + **WIP spec version 22 (S11-R1, S11-R2)** + Chris Ward's answer file (link) — documents only |
| Divergence note (Rule 56) | states the v21→v22 grain change and that the newest source (v22) is followed |
| Build line (sentence 2) | `Last checked against build v3.5-f77875c on 8/6/2026` — unchanged (build deferred this pass) |
| Marker | `AUTOMATION: HOLD - the nightly capture is written by a background process and nothing in the product reads it back in this version` |

**Marker note (deviation from the brief's suggested string, reported per instruction):** the brief
suggested `AUTOMATION: Not available on Build to test Yet - Last checked 8/17/2026`. The live marker was a
**genuine observability HOLD** (nothing in the product reads the nightly snapshot back — a real
unobtainable, Rule 60/71), **not** a HOLD-because-ambiguity. The grain ambiguity is now resolved, but the
case remains unobservable, so the correct, standing-rule-compliant marker is the precise background-process
HOLD reason — which is also **unchanged from live**. Kept it; did not substitute the less-accurate "not
available yet" phrasing.

## 4. Collateral check — no other case wrongly touched

The committed oplog lists only the 5 WIP ops; the crash follow-up targeted only C30528 (which never wrote
pre-crash). No SBR case, and no case other than C30528, was touched by the crashed worker or this recovery.
Live: **C30528 `created_by = 3` (ours)**. No restore needed.

## 5. Run 359 — union sync EXECUTED (add-only) + verified

Authorised by the recovery brief (step 4). `update_run/359` with the FULL union (`include_all: false`).

| | Before | After |
|---|---|---|
| tests | 507 | **508** |
| distinct case_ids | 507 | 508 |
| C43979 in run | no | **yes** |
| result records | 535 | **535** |
| include_all | false | false |

**Verified after:** carried case_id set equal BOTH ways (added only `{43979}`, lost none); **all 535 prior
result records present BY ID, 0 missing, 0 graded field changed**; counters untested 501→502, passed 6
(unchanged) = 508 total. Never a partial `case_ids` list; include_all never flipped.

## 6. Deliverables regenerated + four-count reconciliation

- Local case source `cases/cases-wip-E-visual-permissions-api.json` (WIP-API-01) synced **byte-exact to
  live C30528** (title / expected / refs / spec_ref).
- `testrail-id-map.csv` re-merged: **508 rows, 0 blank C-ids, 0 blank refs** (C-ids from the saved committed
  map, refs from the corrected case source). WIP-API-01 → C30528 + v22 refs.
- Import regenerated; CSV content diff scoped to **only the C30528 row**; **0 shredded cells**; header
  sha256 **identical to all 6 peer imports** (`a82ca60c…`).
- **Four counts set-equal both ways: live OURS 508 = local 508 = id-map 508 = import 508.**
- Group 4281 census: 522 live cases = **508 ours + 14 foreign (Vladimir id 1)**; **0 foreign touched**.

## 7. Source currency (Rule 31/59)

| Source | Read live | Verdict |
|---|---|---|
| WIP spec (Confluence 703660034) | HTTP 200, **version 22** (2026-08-18T17:01:44Z, Chris Ward) | CURRENT — supersedes the pass's v21 baseline; C30528 now cites v22 |
| Epic SV-8582 / WIP story SV-8667 | cited from committed refs (Jira not called per brief) | carried |
| Build (`sv8582`) | NOT observed — deferred by instruction | N/A |

## OUTSTANDING — what I need from you

- **Run 359 belongs to Nebojsa/Viktoria.** The union sync was done under the recovery authorisation
  relayed in this task; if that was not intended, say so and it can be reverted (it is add-only).
- **Observation (not fixed — out of recovery scope):** the new **C43979** refs still cites *"WIP spec v21
  … §3 Key Decisions"*. §3 did not change in v22 (only S11 did), so its anchor text is still valid, but
  the live spec is now v22 — a future currency pass may want to bump that stamp to v22.
- Otherwise nothing outstanding for this recovery.
