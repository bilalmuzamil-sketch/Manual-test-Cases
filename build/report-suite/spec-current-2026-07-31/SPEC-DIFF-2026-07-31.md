# Report Suite — SPEC RE-DIFF 2026-07-31 (the promised Chris Ward changelog HAS LANDED)

> **This is the SPEC-WATCH-2026-07-28 re-check** (Rule 31: always pull the latest spec before
> touching any case). All six Confluence pages were re-fetched LIVE on 2026-07-31 and diffed
> against our last capture (`../spec-current-2026-07-28/`).
>
> **Access:** Confluence REST (`/wiki/rest/api/content/{id}?expand=body.storage,version,space`)
> with the Atlassian session cookies in `/tmp` — `/rest/api/3/myself` → **HTTP 200** verified
> first (account bilal.muzamil@shopview.com). Nothing fabricated.
>
> **Pipeline honesty:** the same capture pipeline as 2026-07-28 (storage-format → markdown via
> html2text, unicode-preserving), plus a cosmetic escape-normalization (`\.` `\!` `\{` `\}`,
> trailing whitespace) so the two captures are diff-comparable. **Validated: re-fetching the
> six PRIOR versions through this pipeline reproduced all six committed 2026-07-28 files
> byte-identically (6/6 MATCH)** — therefore every difference below is a REAL content change by
> Chris, not a capture artifact. Raw evidence: `*-raw-unified.diff` (one per report).

---

## 1. Version table — every page moved, all on 2026-07-29

| Report | pageId | Version 2026-07-28 | Version NOW | Last updated (live) | By |
|---|---|---|---|---|---|
| Sales By Customer (SBC) | 577634305 | v11 (2026-07-21) | **v12** | 2026-07-29T06:44:06Z | Chris Ward |
| Sales By Representative (SBR) | 585629698 | v14 (2026-07-21) | **v15** | 2026-07-29T06:38:33Z | Chris Ward |
| Parts Velocity (PV) | 620888066 | v3 (2026-07-17) | **v4** | 2026-07-29T06:41:59Z | Chris Ward |
| Technician Utilization (TU) | 641400833 | v4 (2026-07-21) | **v5** | 2026-07-29T06:45:11Z | Chris Ward |
| Work In Progress (WIP) | 703660034 | v5 (2026-07-17) | **v6** | 2026-07-29T06:33:58Z | Chris Ward |
| Inventory Value (IV) | 720142338 | v2 (2026-07-21) | **v3** | 2026-07-29T06:32:54Z | Chris Ward |

Every page carries a **new dated Change Log row (2026-07-29)** — the promised changelog
discipline was honoured. **The deadline item (2026-08-04) is therefore MET in substance** for the
items he actually edited; three watch items remain unratified (see §3).

**⚠️ Timing fact that matters for the WIP identifier:** all six edits were saved at **06:32–06:45Z
on 2026-07-29**, i.e. BEFORE his same-day answer "A is the correct answer" on the WIP identifier
question (relayed to us later on 2026-07-29). His answer is therefore the NEWER source (Rule 32).

---

## 2. What changed, per report (every ADDED / REMOVED / CHANGED requirement)

### 2.0 The suite-wide theme of this changelog

Chris's 2026-07-29 pass is dominated by ONE suite-wide feature he rolled out to all six reports:
a **per-row `Location` column with automatic visibility** (shown only when more than one location
is in scope; hidden for a single location; NOT in the column selector), a **`"Locations:"` line in
every export**, and a **constant-width Location filter control**. Plus per-report renames.

### 2.1 Sales By Customer — v11 → v12 (the biggest change; 4 of our watch items ratified)

**ADDED (§2 Feature Overview, verbatim):**
> "Each asset row is labeled by the vehicle's VIN, falling back to Unit number, then plate."

> "When more than one location is in scope, each row shows a Location column — the row's
> location, or "Multiple" for a customer or asset row that spans more than one location; the
> column is hidden when only one location is in scope. Every export names the location or
> locations in scope on a "Locations:" line."

**CHANGED (§2 export sentence) — REMOVED:**
> "The user can download the report as CSV or PDF, **or print it**, from a single overflow menu…"

**→ NOW:**
> "The user can download the report from a single overflow menu in the toolbar — a Summary
> version (customer totals only) or an Expanded version (the full Customer → Asset → Invoice
> tree), each as a PDF or a CSV…"

**ADDED (§3 Key Decisions):** "**Exports come in Summary and Expanded versions.**" ·
"**Assets are identified by VIN.** The asset label is the vehicle's VIN, falling back to Unit
number, then plate — … rather than a year/make/model description." ·
"**Location is a per-row column, shown only when more than one location is in scope.**"

**ADDED (§4 Terminology):** "Summary export", "Expanded export", "Location (column)".

**CHANGED — the asset-label requirements were REWRITTEN (this is the VIN ratification):**
| Was (v11) | Now (v12) |
|---|---|
| **S8-R7:** "The asset label starts with the vehicle's year, make, and model." | **S8-R7 (asset label — primary):** "The asset label is the vehicle's VIN." |
| **S8-R8:** identifier suffix priority " · Unit {unit}" → " · {plate}" → " · VIN …{last 8}" | **S8-R8 (first fallback):** "When the vehicle has no VIN, the label is the vehicle's Unit number." |
| **S8-R9:** "When the vehicle has no year, make, or model, the label is the VIN on its own." | **S8-R9 (second fallback):** "When the vehicle has no VIN and no Unit number, the label is the vehicle's plate." |
| **S8-R10:** "no year, make, model, or VIN → 'Unknown Asset.'" | **S8-R10:** "no VIN, Unit number, or plate → 'Unknown Asset.'" |

**ADDED — new location requirements:** **S4-R12** (per-row Location column shown only when >1
location in scope), **S4-R12a** ("Multiple" on aggregating customer/asset rows; invoice rows show
their exact location), **S4-R13** (every export includes a "Locations:" line, or "All locations"),
**S20-R19** (column position immediately after Date; constant-width Location filter).

**CHANGED — Story 14 retitled "Export as CSV" → "Download as CSV (Summary and Expanded)"**, fully
rewritten: TWO menu items **"Download Summary (CSV)"** and **"Download Expanded View (CSV)"**
(S14-R1/R2); Summary = one row per customer, columns *Customer, Inv. Hrs, Labor Invoiced, Labor
Margin, Parts Invoiced, Parts Margin, Shop Supplies, Margin, Margin %, Subtotal* (S14-R4);
Expanded = full tree, columns as Summary **plus Asset, Invoice #, Date** (S14-R5); row shape per
level (S14-R6); "Parts Sales" bucket appears as an asset row (S14-R7); **"Locations:" line**
(S14-R13); filenames **`sales-by-customer-summary-{range}.csv` / `sales-by-customer-expanded-{range}.csv`**
(S14-R14 — replaces the old flat `sales-by-customer-{range}.csv` map); 10,000-row cap now per
version (S14-R16). **REMOVED:** the old twelve-column flat CSV (old S14-R6) and the context note
"the CSV is flat — Customer then Invoice rows… asset layer is not represented".

**CHANGED — Story 15 retitled "Download as PDF (Summary and Expanded)"**: TWO menu items
**"Download Summary (PDF)" / "Download Expanded View (PDF)"** (S15-R1/R2); Summary body = one row
per customer (S15-R4), Expanded body = full tree, one block per customer (S15-R5); filenames
`sales-by-customer-summary-{range}.pdf` / `-expanded-{range}.pdf` (S15-R6); header text block now
also carries the **location line "Locations: {value}"** (S15-R10, S15-R14). **REVERSED requirement,
verbatim context note:**
> "this reverses the earlier rule that hid the location from the PDF header; the export must now
> carry its own location context so a printed copy is not ambiguous about which location's data it
> shows."
(old **S15-R11** "The location filter is not shown in the PDF header" is GONE.)

**REMOVED — Story 16 "Print the report" is retired**, verbatim:
> "### Story 16: (removed — Print retired) … The Print action that previously occupied this story
> has been removed from this report. Users produce a printable copy with the "Download Summary
> (PDF)" or "Download Expanded View (PDF)" item (Story 15) and print from their PDF viewer. The
> story number is retained as a placeholder so the later story numbers (17, 18, 20, 21) and every
> "See Story 20" design reference remain stable."
All of old S16-R1…R6 / S16-N1 and the §7 rows "Print PDF fails / Print starts" are REMOVED.

**Change Log row (verbatim):**
> "2026-07-29 | @chris / @claude | Assets identified by VIN (VIN → Unit # → plate → "Unknown
> Asset"); removed Print; split exports into Summary and Expanded (CSV + PDF, four menu items);
> added a per-row Location column (shown when more than one location is in scope; "Multiple" on
> aggregating rows, exact on invoice rows) plus a "Locations:" export line."

**NOT changed in SBC (matters):** **S1-R2** still reads *"The report is gated by a dedicated Sales
By Customer report View permission — it is not tied to a generic 'all reports' permission."* — see
§4 (the permission answer). And the SBC export-cap message still reads **"This export is too large
to generate…"** (see §4, Q2).

### 2.2 Sales By Representative — v14 → v15

**ADDED:** the per-row **Location** column — §2 overview line; §3 Key Decision
("Location is surfaced per row, not as a standalone scope stamp"); §4 Terminology "Location
(column)"; **S21-R7** (shown only when the view spans more than one location, hidden for a single
location); **S21-R8** (rep summary row = the single location name, or the literal **"Multiple"**
when the rep's invoices span locations; invoice detail row = its own exact location; the
Unassigned row follows the same rule); **S14-R20** (Location carried into all four exports +
a "Locations:" line in each); **S18-R13** (column sits immediately after **Status** and before
Inv. Hrs, never displacing the pinned Subtotal; Location filter is constant-width).

**Change Log row (verbatim, includes a padding watch note):**
> "2026-07-29 | @chris / @claude | Added a per-row **Location** column, shown only when the
> current view spans more than one location … Padding watch: the report's display label "Sales By
> Representative" is longer than the "Sales By Rep" shorthand — verify the nav entry, toolbar
> title, and PDF header strip fit the full label without truncation (S1-R7)."

**NOT changed in SBR (matters):** **S21-N1** still reads *"A single-location user still sees the
filter with one selectable location"* (contradicts Chris's Q1 answer — §4). **S19-R1 / S19-R7 /
S19-R8** still say **"Sales Rep"** (selector, customer-record row, accessible name) and Story 15
still says **"Sales Rep Assignments"** (9 occurrences) — contradicts Chris's Q5 answer (§4).

**⚠️ Spec-text defect to report to Chris (cosmetic, no case change):** v15 introduced **mojibake** —
every `⋯` (overflow ellipsis) now renders as `â‹¯` in S14-R1, S14-N3, S17-R1/R3/R6, S18-R7.2,
S18-R9 and the §2 overview, and the 2026-07-11 change-log row. The BUILD label is unaffected; our
cases keep `⋯`. Same class of defect in PV (`â ‹¯` for `⋯`, `â “˜` for `ⓘ`).

### 2.3 Parts Velocity — v3 → v4 (the Catalogue rename ratified)

**CHANGED — "Catalogue" → "Special Order" everywhere** (display value AND prose): **S2-R1** Type
filter options are now **Both / Inventory / Special Order**; **S3-R5** *"The **Type** column
displays `Inventory` or `Special Order` as plain text."*; §4 Terminology entry renamed
"Catalogue part" → "**Special Order part**"; **Type** term now *"`Inventory` or `Special Order`"*;
S2-R8 bin-filter exclusion, S3-R1a/R2/R9/N1, S5-R2/R3/R4/R4a/R4b/R7, §1 Business Case, §2, §3 all
reworded to "special-order". (Note the underlying data reference is still described as "a
catalogue part reference" in S5-R2 / Assumptions — internal, not a UI label.)

**CHANGED — two column labels renamed (NEW, not on our watch list):**
> **"Sold via WO" → "Sold (WO)"** and **"Sold via Parts Sale" → "Sold (Parts Sale)"**
in S3-R9, **S4-R3** (the 6 default-hidden columns), **S4-R4** (canonical 20-column order),
S5-R4 metric table, S5-R4b, S5-R7, and the §4 work-order-date definition.

**ADDED — the Location column:** §2 overview line; §3 Key Decision; §4 Terminology; **S2-R12**
(shown when the Location filter resolves to more than one location, hidden for a single);
**S3-R10** (inventory row = its own location's name; a merged special-order row = the literal
**"Multiple"**; NOT one of the 20 picker columns, not user-toggleable); **S6-R11** (both exports
include the column when shown + a "Locations:" line); **S7-R8** (renders **leftmost, before Type**;
constant-width Location filter).

**Change Log row (verbatim):**
> "2026-07-29| @chris / @claude| Renamed the "Catalogue" part type to "Special Order" (display +
> value); renamed the "Sold via WO" / "Sold via Parts Sale" columns to "Sold (WO)" / "Sold (Parts
> Sale)"; added a per-row Location column … plus a "Locations:" export line and a constant-width
> Location filter."

**NOT changed in PV (matters):** **S1-R1** still says PV is the Parts section's *"first (and, in
this release, only) report"* (the intra-suite inconsistency with IV — watch #11). **S2-E4** still
says a single-location user *"still sees the Location filter"* (contradicts Q1). PV still has **no
export row-cap / no "too large" message** (Q3).

### 2.4 Technician Utilization — v4 → v5 (the TU column selector ratified)

**ADDED — a whole new story, verbatim heading:** `### Story 10: Column Selection and Persistence`
> **S10-R1:** column-selection icon button, tooltip **"Column Selection"**, in the toolbar action
> cluster immediately after the three-dot download menu.
> **S10-R2:** "The **Technician** column is always shown and cannot be turned off."
> **S10-R3:** "Every other data column — Total Hours, WO Hours, Internal Hours, Utilization %, and
> Est. Lost Labor — is available in the control and can be toggled on or off. All five are shown by
> default on a first visit. (Est. Lost Labor was previously always-on; it is now a hideable column
> like the others.)"
> **S10-R4:** the Location column is NOT toggleable (auto-managed by scope, never listed).
> **S10-R5:** fixed left-to-right order preserved; Location leftmost, Est. Lost Labor last.
> **S10-R6:** column selection remembered per browser, defensive fallback to the default set.

**CHANGED — Est. Lost Labor is no longer always-on:** §3 Key Decision now *"**Est. Lost Labor is
the bold, pinned headline column when shown** … but it is now a user-hideable column (shown by
default; S10-R3)"*; **S2-R10 / S2-R11 / S8-R4 / S8-R6** each gained the qualifier **"When shown"**.

**CHANGED — toolbar order (NEW, not on our watch list):** old **S8-R3** *"the three-dot download
menu, the technician filter, the date-range picker, and the location filter"* → now *"the
three-dot download menu, **the Column Selection control**, **the date-range picker, the technician
filter**, and the location filter (rightmost)"* — i.e. the Column Selection control is inserted
AND **the date-range picker and technician filter swap places**. **S8-R2** now adds *"followed by
the Column Selection control."*

**CHANGED — the PDF logo rule (NEW):** old **S7-R11** *"When the shop has a logo set, both PDF
views show that logo"* + **S7-N2** *"If the shop has no logo set, the PDF views show no logo"* →
now **S7-R11** *"Both PDF views show a logo … the organization's uploaded logo when it has one,
otherwise the **bundled ShopView default logo**. (Only if even the bundled default is unavailable
does a PDF render with no logo.)"* and **S7-N2** *"If the organization has no uploaded logo, the
PDF views show the bundled ShopView default logo (not a blank space)."*

**CHANGED — persistence widened:** §3 + **S1-R8** now also remember the **column selection**.
**CHANGED — S2-R1** column order now notes Location precedes Technician when shown; **S7-R10**
exports now mirror the selected columns.
**ADDED:** **S9-R9** (auto Location-column visibility), **S9-R10** (technician row = its single
location, or **"Multiple"**; per-day detail row likewise; **the Summary row leaves Location
blank**), **S7-R13** ("Locations:" line in both PDFs and the CSV), **S8-R15** (leftmost, before
Technician; constant-width filter), **S8-R16** (accessible name on the Column Selection control),
§4 Terminology "Location (column)".

**NOT changed in TU (matters):** **S9-N1** still says a single-location user *"still sees the
filter"* (contradicts Q1). TU still has **no export row-cap / no "too large" message** (Q3).
**S1-R1** already named the **Performance** group at v4 — but the "additive / below existing
items" wording from the kickoff video is still absent (watch #6).

### 2.5 Work In Progress — v5 → v6

**ADDED — Location-column auto-visibility (WIP already had a Location column; it is now
scope-driven, not user-toggled):** §2 overview line; §3 Key Decision (*"The per-row Location
column is automatic, not a manual toggle … Because a work order belongs to exactly one location,
each WIP row names its own location — **a WIP row never shows 'Multiple'**"*); §4 Terminology
"Location (column)" (*"In every export it is headed **"Branch"** (S9-E1)"*); **S7-R13** /
**S7-R14**; **S9-R10a** ("Locations:" line in PDF header / CSV leading metadata line);
**S10-R5a** (constant-width Location filter control).

**CHANGED — S4-R3 (Location removed from the column selector):**
| Was (v5) | Now (v6) |
|---|---|
| "Every other column (**VIN, Location**, Last Activity, Labor Earned, Labor Remaining, Parts Earned, Parts Remaining, Inv. Hrs) is available in the column selector and off by default (Story 8)." | "Every other column (VIN, Last Activity, …, Inv. Hrs) is available in the column selector and off by default (Story 8). **The Location column is not offered in the column selector; its visibility is automatic — shown only when more than one location is in scope (Story 7).**" |

**Change Log row (verbatim):**
> "2026-07-29| @chris / @claude| Aligned the per-row Location column to the suite auto-visibility
> rule (shown only when more than one location is in scope) and added a "Locations:" export line
> and a constant-width Location filter.| Suite location-column consistency."

**NOT changed in WIP — and this is the important one:** the **asset-identifier text is UNTOUCHED**.
§4 Terminology still reads *"**Asset** → The vehicle or unit the job is for, identified by its unit
number and its vehicle identification number."*; **S4-R7** still *"a two-line cell: **the unit
number on the first line in bold**, and the vehicle identification number on the second line"*;
**S4-R8** *"no unit number → '(no unit #)' … no VIN → '— no VIN —'"*; **S4-R9** *"The Asset column
**sorts by unit number**"*; **S7-R4** asset filter *"Each option shows the unit number and the
vehicle identification number"*. → Chris's "A is the correct answer" (VIN → Unit # → plate for WIP
too) is **NOT in the WIP spec text**. Exactly the risk the watch flagged ("he updated the spec
before bed but has NOT hand-reviewed it").

### 2.6 Inventory Value — v2 → v3

**ADDED — the Location column:** §2 overview line; §3 Key Decision; §4 Terminology "Location
(column)" (*"every row maps to exactly one location name — this report **never shows an aggregated
'Multiple'** in the column"*); **S7-R6** (auto visibility, NOT in the column-selection control);
**S7-R7** (each row names its part's location); **S10-R15** (Location in every export + a
"Locations:" line); **S12-R10** (position + constant-width filter).

**CHANGED — S3-R1 column order:** now *"When the report is scoped to more than one location, a
**Location** column (S7-R6) is inserted **between Vendor and Qty on Hand**; it is hidden for a
single-location scope."*

**Change Log row (verbatim):**
> "2026-07-29 | @chris / @claude | Added a per-row Location column (shown only when more than one
> location is in scope) plus a "Locations:" line and a constant-width Location filter. | Suite
> location-column consistency."

**NOT changed in IV (matters):** **S7-N1** still says a single-location user *"still sees the
filter with a single selectable location"* (contradicts Q1). IV keeps **S10-R12** — the 10,000-row
cap with the message **"This report is too large to export. Narrow the date range or filters, then
try again."** (this is the wording Chris's Q2 answer = A selects for the whole suite).

---

## 3. SPEC-WATCH verdicts — per item

| # | Watch item | Verdict | Evidence |
|---|---|---|---|
| 1a | **Asset identifier = VIN → Unit # → plate — SBC** | ✅ **RATIFIED** | SBC v12 S8-R7/R8/R9/R10 rewritten to VIN → Unit # → plate → "Unknown Asset"; §2 + §3 Key Decision added; change-log row names it. |
| 1b | **Asset identifier — WIP** | ❌ **STILL-MISSING** (spec text CONTRADICTS his answer) | WIP v6 leaves §4 Terminology, S4-R7/R8/R9, S7-R4 unit-number-first and "sorts by unit number". His later same-day answer ("A is the correct answer") is the newer source and stands (Rule 32) — the WIP SPEC TEXT still needs his edit. |
| 2 | **SBC Print removed** | ✅ **RATIFIED** | SBC v12: `### Story 16: (removed — Print retired)`; S14-R1 menu no longer lists Print; §7 Print rows deleted; change-log "removed Print". |
| 3 | **SBC compressed / Summary download** | ✅ **RATIFIED and EXTENDED** | Stories 14 & 15 rewritten into **four** menu items: "Download Summary (CSV)", "Download Expanded View (CSV)", "Download Summary (PDF)", "Download Expanded View (PDF)" (S14-R1/R2, S15-R1/R2) + Summary/Expanded contents + new filenames. |
| 4 | **Location FILTER hidden when ≤1 permitted location** | ❌ **CONTRADICTED in the spec text** — but Chris's Q1 answer (2026-07-31) = **A, hidden**, and that ruling WINS | The "still sees the filter" notes SURVIVE: SBR **S21-N1**, TU **S9-N1**, IV **S7-N1**, PV **S2-E4**. Chris's answer is newer than v12–v15 → cases follow A; the four spec notes must be corrected by Chris. |
| 5 | **Per-row Location label in the all-locations view** | ✅ **RATIFIED on all six** (and gone further than the video) | SBC S4-R12/R12a, SBR S21-R7/R8, PV S2-R12/S3-R10, TU S9-R9/R10, IV S7-R6/R7, WIP S7-R13/R14 — plus the "Locations:" export line everywhere and a constant-width filter. |
| 6 | **Nav placement — TU below existing links; Parts section** | ⚠️ **PARTIAL / STILL-MISSING** | TU S1-R1 names the **Performance** group (already true at v4) but the video's "additive, below existing items, not interruptive" wording is still absent. PV/IV Parts section unchanged and already correct. |
| 7 | **Catalogue rename** | ✅ **RATIFIED with the exact label "Special Order"** | PV v4: S2-R1 filter options Both/Inventory/**Special Order**; S3-R5 Type column value; §4 term "Special Order part"; change-log row. |
| 8 | **Asset-dropdown = native style + toggle (WIP)** | ❌ **STILL-MISSING** | WIP S7-R4 unchanged; no native-style/toggle wording anywhere. (No case asserts either style — note-only, zero case impact.) |
| 9 | **Customer-card label "Sales Representative"** | ❌ **STILL-MISSING** (spec CONTRADICTS) | SBR S19-R7 still *"a single "Sales Rep" row"*; S19-R1/R8 still "Sales Rep"; Story 15 still "Sales Rep Assignments" (9×). Chris's Q5 answer (2026-07-31) = **A, "representative everywhere"** and WINS. |
| 10 | **SBC Performance group + named nav anchors** | ❌ **STILL-MISSING** | SBC S1-R1 still names no nav group; TU/WIP/SBR anchors (Sales, Technician Efficiency, Advisor Analysis, Shop Efficiency) still unnamed. |
| 11 | **PV S1-R1 "only report" inconsistency** | ❌ **STILL-MISSING** | PV S1-R1 verbatim still: *"Parts Velocity is its first (and, in this release, only) report"* while IV S1-R1 also puts IV under Parts. |
| 12 | **Rep-label scope (how far "Representative" reaches)** | ✅ **ANSWERED BY CHRIS (not by the spec)** | Q5 = **A** — full word everywhere, including the WO selector and the assignments export/file/columns. The SBR spec text still says "Sales Rep" → needs his edit (same as #9). |
| — | **Pagination stands on every page** | ✅ no change | No infinite-scroll revisit; PV-API-01/02 unaffected. |

**Watch status:** **4 of 12 CLOSED as ratified** (1a, 2, 3, 5) + **1 CLOSED by answer** (12) +
**#7 CLOSED as ratified**. **The watch CANNOT retire yet** — 6 items still need Chris's spec text:
**1b (WIP identifier — the one he thought he had done), 4, 6, 8, 9, 10, 11.** The 2026-08-04
deadline item is **partly met**: the changelog LANDED on time and on all six pages, but the WIP
identifier edit he believed he made is NOT there.

---

## 4. New spec deltas that were NOT on the watch list (these need case work)

| # | Report | Delta | Impact |
|---|---|---|---|
| N1 | PV | **"Sold via WO" → "Sold (WO)"** and **"Sold via Parts Sale" → "Sold (Parts Sale)"** | Any case quoting the old labels is now spec-stale (Rule 9 build-accurate wording). |
| N2 | TU | **Story 10 Column Selection** added; **Est. Lost Labor is now hideable** (was always-on) | New coverage needed; existing "always pinned/bold" cases need the "when shown" qualifier. |
| N3 | TU | **Toolbar order changed** — Column Selection inserted, and the **date-range picker and technician filter swap** (S8-R3) | Toolbar-order case is stale. |
| N4 | TU | **PDF logo rule changed** — bundled ShopView default logo when the org has none (S7-R11 / S7-N2, was "no logo") | The "no logo → no logo" negative case now asserts the OPPOSITE of the spec. |
| N5 | all 6 | **Per-row Location column + "Locations:" export line + constant-width filter** | Substantial NEW coverage (auto visibility, "Multiple" rules that differ per report, export inclusion). |
| N6 | SBC | Export **filenames changed** to `sales-by-customer-{summary\|expanded}-{range}.{csv\|pdf}`; the PDF header now carries a **"Locations:"** line (reversing old S15-R11 "location not shown in the PDF header") | Filename + PDF-header cases are stale. |
| N7 | WIP | Location column **removed from the column selector** (S4-R3) | Any case that toggles Location in the selector is now wrong. |
| N8 | SBR / PV | **Mojibake in the spec text** (`â‹¯` for `⋯`, `â “˜` for `ⓘ`) | Spec-text defect to report to Chris; **no case change** — the build label is `⋯`/`ⓘ`. |

---

## 5. Honesty / limits

- Everything above is a **document diff**, not a live-build observation. **Nothing here is
  VIU-Verified** (Rule 12): the Report Suite QA branch/env is still not available to us, so no
  spec item — ratified or not — has been observed in the build this pass.
- The Confluence read was **read-only**; no page was edited.
- Full verbatim evidence for every claim: `*-current.md` (the six captures) and
  `*-raw-unified.diff` (the six diffs). Prior baseline: `../spec-current-2026-07-28/`.
