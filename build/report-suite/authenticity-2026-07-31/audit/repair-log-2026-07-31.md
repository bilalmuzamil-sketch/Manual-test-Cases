# Report Suite — Phase-4 audit repairs (2026-07-31)

6 repairs, all found by the three-dimension Rule-28 re-run. Local only.

| Case | Repair |
|---|---|
| PV-EXP-05 | R1 FIX-WORDING: non-actionable "confirmed in the build" replaced with a definite check; spec-silence moved to the metadata layer |
| SBR-PERM-01 | R2 title now names its own report / drops the "nav" abbreviation so the title reads sensibly on its own (was: 'Anyone who can see another Performance report also sees this report') |
| WIP-PERM-02 | R2 title now names its own report / drops the "nav" abbreviation so the title reads sensibly on its own (was: 'Without the permission the report is absent from the reports navigation') |
| TU-NAV-07 | R2 title now names its own report / drops the "nav" abbreviation so the title reads sensibly on its own (was: 'Without the timesheet-reports permission the report is absent from the nav') |
| SBC-NAV-01 | R3 plain tester note added — this case fails against today's build BY DESIGN (Chris Ward Q4=A ruling vs the shipped dedicated atom); only SBC-PERM-01 had the note, so a tester hitting this one would have thought the case was wrong |
| SBC-PERM-02 | R3 plain tester note added — this case fails against today's build BY DESIGN (Chris Ward Q4=A ruling vs the shipped dedicated atom); only SBC-PERM-01 had the note, so a tester hitting this one would have thought the case was wrong |
| PV-CALC-04 | R7 spec anchor removed from tester-facing expected[5] (Rule 20) |
| PV-ROW-10 | R7 spec anchor removed from tester-facing expected[3] (Rule 20) |
| PV-VIS-01 | R7 spec anchor removed from tester-facing expected[3] (Rule 20) |
| SBR-BADGE-01 | R7 spec anchor removed from tester-facing expected[3] (Rule 20) |
| TU-SUM-04 | R7 spec anchor removed from tester-facing preconditions[1] (Rule 20) |
| TU-TECH-04 | R7 spec anchor removed from tester-facing expected[3] (Rule 20) |
| WIP-API-03 | R7 spec anchor removed from tester-facing expected[1] (Rule 20) |
| WIP-API-06 | R7 spec anchor removed from tester-facing expected[2] (Rule 20) |
