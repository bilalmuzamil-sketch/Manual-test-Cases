# FOR VLAD (Rule 65) — Automated case C26577 corrected 2026-09-10

C26577 is flagged **Automated** (`custom_atmstatus = 3`) and is ours (`created_by = 3`). The QA lead gave
explicit go-ahead (2026-09-10) to correct it because it was failing in automation.

| C-id | Link | What changed |
|---|---|---|
| **C26577** | https://shopview.testrail.io/index.php?/cases/view/26577 | Made source-correct + runnable. **No change to the tested behaviour.** |

**What changed (all traceability/precision, not the expectation):**
- **Pinned the PDF endpoint.** The step "hit GET on a WO PDF download endpoint" was vague; it now names the
  concrete Work-Order "Print / Download PDF" action and the PDF-controller / `LinesDetailProvider` path that
  CRP-BE-09 added the stripper to (from SV-7484), with the exact URL to be confirmed at build-verification.
  This is the most likely reason it was failing in automation (the harness could not reliably find the
  endpoint — same issue its sibling C26578 hit).
- **Added the provenance line** — epic **SV-7388**, story **SV-7523** (Permission: See Financial Data), task
  **SV-7484** ([CRP-BE-09] enforcement chokepoint), read 10 Sep 2026.
- **Set the automation type** to **E2E (1)** (was None/0) — PDF-delivery security check.
- Rendered `fr-view`, runnable-gate 1/1.

**What did NOT change:** the expected behaviour (See Financial Data OFF ⇒ the PDF carries no pricing) — that
was already exactly SV-7484's acceptance criterion. `custom_atmstatus` stays **3** (still Automated). Title
unchanged. The build-verification session will run it (pin the exact URL, confirm no pricing / catch any leak).
