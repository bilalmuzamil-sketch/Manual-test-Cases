<p><strong>ShopView &middot; Technician Scheduling Module</strong></p><table data-table-width="760" data-layout="default"><tbody><tr><th><p /></th><th><p /></th></tr><tr><td><p><strong>Status</strong></p></td><td><p>Complete</p></td></tr><tr><td><p><strong>Author</strong></p></td><td><p>Product Team</p></td></tr><tr><td><p><strong>Last Updated</strong></p></td><td><p>July 15, 2026</p></td></tr><tr><td><p><strong>Version</strong></p></td><td><p>1.0</p></td></tr><tr><td><p><strong>Stakeholders</strong></p></td><td><p>Engineering, Design, Shop Operations</p></td></tr><tr><td><p><strong>Design</strong></p></td><td><p><a href="https://claude.ai/design/p/d3cdcf5c-83df-45ea-ba75-7ddedb5124b5?file=Schedule.dc.html&amp;via=share">Schedule</a>, <a href="https://claude.ai/design/p/d3cdcf5c-83df-45ea-ba75-7ddedb5124b5?file=Edit+Modals.dc.html&amp;via=share">Business and Tech hours settings</a></p></td></tr><tr><td><p><strong>Epic</strong></p></td><td><p><ac:structured-macro ac:name="jira" ac:schema-version="1" ac:macro-id="837301ae-2d91-4f11-9d89-f446340be947"><ac:parameter ac:name="key">SV-8685</ac:parameter><ac:parameter ac:name="serverId">267fb633-cc25-3f2d-8011-7f1e9199ad21</ac:parameter><ac:parameter ac:name="server">System Jira</ac:parameter></ac:structured-macro> </p></td></tr></tbody></table><hr /><h2>1. Overview</h2><p>The Schedule module gives shop managers a visual calendar to assign technicians to work order lines across days and weeks. It replaces manual whiteboards and spreadsheets with a drag-and-drop interface that respects technician capacity, surfaces conflicts, and keeps the work order system of record in sync.</p><h3>1.1 Problem statement</h3><p>Heavy-duty shops manage dozens of open work orders simultaneously, each with multiple repair lines requiring different technicians and hours. Without a dedicated scheduling tool, managers rely on memory, whiteboards, or ad-hoc spreadsheets, which leads to double-bookings, overtime surprises, and unbalanced workloads across the team.</p><h3>1.2 Goals</h3><ul><li><p>Reduce scheduling errors (double-bookings, weekend assignments, after-hours shifts) to near zero with automatic conflict detection.</p></li><li><p>Give managers a single screen to see the full week's technician allocation at a glance.</p></li><li><p>Support multi-day &quot;spread&quot; scheduling for large jobs (engine rebuilds, frame work) that span 40 to 160+ hours across days and weeks.</p></li><li><p>Keep the work order roster in sync, so scheduling a technician on the schedule automatically adds them to the line's labor roster.</p></li></ul><hr /><h2>2. User personas</h2><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Persona</p></th><th><p>Role</p></th><th><p>Key needs</p></th></tr><tr><td><p>Service Manager</p></td><td><p>Owns the daily schedule for 5 to 15 techs</p></td><td><p>Drag-and-drop scheduling, capacity visibility, conflict alerts</p></td></tr><tr><td><p>Service Advisor</p></td><td><p>Creates work orders, communicates ETAs to customers</p></td><td><p>See when their work orders are scheduled, which techs are assigned</p></td></tr><tr><td><p>Shop Foreman</p></td><td><p>Oversees floor execution</p></td><td><p>Day view of who's doing what, department filtering</p></td></tr><tr><td><p>Technician</p></td><td><p>Performs the repair work on assigned lines</p></td><td><p>A clear view of their own assigned shifts and what to work on next, plus their hours</p></td></tr></tbody></table><hr /><h2>3. Information architecture</h2><p>The Schedule lives as a top-level nav item alongside Work Orders, Customers, Parts, and Reports. The screen is split into two regions.</p><h3>3.1 Left panel: work order sidebar</h3><p>The panel can be collapsed and expanded from the grid toolbar (&sect;5.3), handing its width to the grid without losing panel state.</p><ul><li><p><strong>Mini calendar.</strong> A month picker with week-highlight and a collapsible grid. Clicking a date navigates the main grid.</p></li><li><p><strong>Work order list.</strong> A flat, scrollable list of work order cards. Searchable and filterable (see &sect;5). There are no Assigned/Unassigned tabs; assignment is a filter.</p></li><li><p><strong>Line drill-down.</strong> Clicking a work order replaces the list, in place, with that order's lines. Only <strong>approved</strong> work order lines are visible in the schedule sidebar; unapproved lines do not appear. Includes a back control, the WO id plus line count, a line search box, and &quot;All / Unscheduled&quot; filter chips with counts. Each line row is independently draggable (drag handle) and shows its title, estimated hours, and current technician roster (avatar stack plus count, with no cap). Lines with no technician assigned show a &quot;Needs techs&quot; badge so the manager can see at a glance which lines still require someone.</p></li></ul><p><strong>Work order card anatomy.</strong> Each card shows, from top to bottom: WO number (in accent color, top left) and line count plus hours estimate (top right); customer name (bold); unit number; and a lead technician row (avatar plus name). A colored left border indicates the work order's status. All of these fields are visible on the card and are matched by the sidebar search.</p><p><strong>Sidebar search</strong> (&quot;Search work orders&quot;) matches against: WO number, customer name, unit number, and technician name. It filters the card list in real time as the user types.</p><p><strong>Line search</strong> (&quot;Search lines&quot;), visible in the drill-down, matches against line title/name only (the list is already scoped to one work order, so customer/unit/WO fields would be redundant).</p><h3>3.2 Main area: schedule grid</h3><ul><li><p><strong>Day view.</strong> A 24-hour timeline per technician row with time-positioned blocks.</p></li><li><p><strong>Week view.</strong> A 7-column grid Mon to Sun (Saturday and Sunday each toggleable) with stacked shift chips per cell.</p></li><li><p><strong>Month view.</strong> A compact calendar with per-day capacity bars and shift chips.</p></li></ul><p><strong>Grid grouping.</strong> Rows are grouped by department under collapsible group headers (e.g. SERVICE/PARTS, ADMINISTRATION), with the department's technicians listed beneath each header. This is the only grid grouping; because the department view already lists technicians, there is no separate technician-only view or Tech/Dept toggle.</p><p><strong>Unassigned placeholder.</strong> An unassigned row sits within the grid (not a separate tray) and holds shifts that are not yet tied to a technician. Dragging a shift from this row down onto a technician assigns it. Unassigned shifts use the same three-line block anatomy as regular shifts (see &sect;4.4); they simply have no technician yet (see &sect;4.2).</p><hr /><h2>4. Core features</h2><h3>4.1 Drag-and-drop scheduling</h3><p>The primary interaction model. Users drag a work order card (or an individual line) from the sidebar and drop it onto a technician x day/time cell in the grid.</p><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Scenario</p></th><th><p>Behavior</p></th></tr><tr><td><p>Single-line work order</p></td><td><p>Creates a shift immediately, skipping the scope picker.</p></td></tr><tr><td><p>Multi-line work order</p></td><td><p>Opens the scope picker to choose whole order, a single line, or several lines.</p></td></tr><tr><td><p>Specific line drag</p></td><td><p>Dragging a line from the drill-down creates a single-line shift directly.</p></td></tr><tr><td><p>Large job (exceeds the tech's daily hours)</p></td><td><p>After scope is chosen, opens the spread step to distribute hours across consecutive working days.</p></td></tr></tbody></table><p>The spread step is conditional: a scope that fits within one of the technician's working days skips it and creates a single shift.</p><h3>4.2 Shift start times and unassigned shifts</h3><p><strong>Every shift has a start time.</strong> It is derived from a hierarchy:</p><ol start="1"><li><p>The technician's configured working hours take precedence.</p></li><li><p>If those are not set, the shop's business hours are used.</p></li><li><p>If neither is set, a general default of 7:00 AM to 7:00 PM applies.</p></li></ol><p>In day view, the start time instead comes from where the shift is dropped on the timeline.</p><p><strong>Unassigned shifts</strong> are created by dropping a work order (or line) onto the grid's Unassigned placeholder row (an in-grid lane, not a separate tray). They follow the same start-time rules except technician hours (there is no technician yet), so they fall back to business hours or the default. When an unassigned shift is later dragged onto a technician row in the grid, that technician's hours apply.</p><p><strong>Hours settings (tech and business hours).</strong> Working hours are defined in two places: a technician's custom schedule in Edit Staff Member, and the shop's business hours in Edit Location. Both use the same pattern:</p><ul><li><p><strong>Behind a toggle, off by default.</strong> Each section sits behind a toggle (&quot;Set custom hours for this technician&quot; / &quot;Set business hours for this shop&quot;). The per-day editor appears only when the toggle is on. A technician with no custom hours inherits the shop's business hours (per the hierarchy above).</p></li><li><p><strong>Per-day editor.</strong> One row per day (Mon&ndash;Sun): day name, with From &rarr; To ranges on the right. Each day starts with a single range; &quot;Add hours&quot; appends more to support split shifts, each removable. Added ranges start empty so the user explicitly sets the times.</p></li><li><p><strong>Overlap validation.</strong> If a day's ranges overlap, the offending range is flagged in red with an inline message (&quot;These hours overlap. Adjust the times so they don't conflict.&quot;) and Save is disabled until it is resolved. Incomplete rows (empty From/To) are ignored by the check.</p></li></ul><h3>4.3 Scope picker</h3><p>When a multi-line work order is dropped, a popover anchored to the drop cell lets the manager choose what to schedule:</p><ul><li><p><strong>&quot;Schedule whole work order&quot;</strong> is pinned at the top, visually distinct, and labeled with the line count and total hours. It assigns the technician to all lines and creates one whole-order shift.</p></li><li><p><strong>Individual line rows.</strong> Tapping a row is the fast path: it immediately creates a single-line shift with no confirmation step. Each row shows the line title, estimated hours, and current technician roster (avatar stack plus count).</p></li><li><p><strong>&quot;Select multiple&quot;</strong> is an opt-in control that switches the line rows into checkboxes and shows a confirm bar with a running tally (&quot;Create shift &middot; 2 lines &middot; 6h&quot;), a &quot;Select all&quot; shortcut (equivalent to whole order), and Cancel (returns to the fast single-tap list).</p></li></ul><p>There is no technician cap and no swap flow. Scheduling a technician onto a line simply adds them to that line's roster.</p><h3>4.4 Shift block anatomy and scope labeling</h3><p>Every shift block on the grid shows three lines of text (four when VIN is toggled on), with a default blue color (users can optionally assign a custom color per shift via the color picker in the detail modal, see &sect;10):</p><ul><li><p><strong>Line 1:</strong> customer name, plus the conflict icon if the shift is conflicted.</p></li><li><p><strong>Line 2:</strong> unit number.</p></li><li><p><strong>Line 3 (optional):</strong> VIN number, visible only when the VIN toggle is on in Filter and Display (&sect;6). Shown in day and week views only; month view omits it due to space constraints.</p></li><li><p><strong>Last line:</strong> the line name for a single-line shift, or &quot;N Lines&quot; when the shift covers more than one line.</p></li></ul><p>There is no work order number and no scope icons on the block; the conflict icon is the only icon. Whole-order and multi-line-subset shifts both read as &quot;N Lines&quot; on the block, and the detail modal spells out the exact scope.</p><h3>4.5 Multi-day spread scheduling</h3><p>For jobs exceeding a technician's daily capacity, the spread step distributes the work across consecutive working days. It appears as step 2 of the same modal, with a header showing the chosen scope and a &quot;Change scope&quot; back-link.</p><ul><li><p><strong>How much to schedule</strong> is set by a single selector that defaults to <strong>Full estimate</strong> (the most common choice). Most options apply on selection with no extra fields; only the custom ones reveal a control:</p><ul><li><p><strong>Full estimate</strong>, <strong>1 week</strong>, and <strong>2 weeks</strong> apply immediately with nothing to fill in.</p></li><li><p><strong>Until a date&hellip;</strong> reveals a single &quot;finish by&quot; date field.</p></li><li><p><strong>Specific hours&hellip;</strong> reveals an hours stepper.<br />This progressive disclosure (Google Calendar style) keeps the modal to one line in the common case and expands only for the custom options.</p></li></ul></li><li><p><strong>Start date.</strong> Defaults to the earliest working day. Adjusting it is how a second technician's series can be made sequential (starting after the first) rather than parallel.</p></li><li><p>Uses the technician's own working hours. Automatically skips weekends when business hours are not set for them. Shop closures and public holidays are not skipped in V1..</p></li><li><p><strong>Preview</strong>, collapsed by default: a one-line summary (&quot;20 shifts &middot; Jun 15 to Jul 13 &middot; skips weekends + 2 days&quot;), expandable to a week-by-week breakdown with skipped days struck through and their reasons.</p></li><li><p>Confirming creates a linked series of daily shifts.</p></li></ul><p><strong>Each drop spreads the full estimate for that technician, independently.</strong> Dropping the same work order on a second technician spreads the full estimate again for them. There is no shared &quot;remaining&quot; counter across technicians and no splitting of a shift. Because progress is driven by clocked-in time, scheduled hours, the estimate, and actual hours are three separate quantities and are not forced to reconcile.</p><h3>4.6 Linked series and banners</h3><p>A series is a group of shifts created by the spread step; all shifts in a series share a series id. <strong>The series is a render-time grouping, not a special record.</strong> Underneath it is N individual daily shifts, each keeping its own day and hours, so capacity, overtime, and conflict logic all operate on the individual shifts unchanged.</p><p>Shifts sharing a technician plus series id render as one connected banner:</p><ul><li><p><strong>Month view:</strong> a continuous bar wrapping across week rows, labeled once at the start (with the technician), with a faded &quot;continues&quot; label on later weeks, empty weekend columns (when business hours are not set for weekends).</p></li><li><p><strong>Week view:</strong> one banner spanning the working days of that week in the technician's row, with chevrons at the edges indicating continuation beyond the visible week, a &quot;week N of M&quot; cue.</p></li><li><p><strong>Day view:</strong> that day's single time-positioned block with a &quot;part of an M-week job&quot; cue (only one day is visible, so there is no spanning bar).</p></li></ul><h3>4.7 Overlap and lane stacking</h3><p>Overlapping shifts for the same technician never visually collide:</p><ul><li><p>Shifts whose time ranges do not intersect share a single lane, so sequential or back-to-back work keeps the row at normal height.</p></li><li><p>Shifts whose time ranges do intersect split into stacked lanes, and the row grows to fit.</p></li><li><p>Visible lanes are capped at 3. Additional overlapping shifts collapse into a &quot;+N more&quot; affordance that opens a popover listing the hidden shifts. This applies in day, week, and month views (week and month reach the overflow much sooner because cells are narrower).</p></li><li><p>Overlap on the same technician is a conflict (see &sect;4.11) and is flagged, so stacking reads as &quot;resolve me,&quot; not &quot;two normal jobs.&quot;</p></li></ul><h3>4.8 Day view: timeline interactions</h3><ul><li><p><strong>Auto-scroll to business hours.</strong> On initial day-view load and when navigating to a new day, the timeline auto-scrolls so the working-day start sits at the left edge of the visible area (with a small 30 to 60 minute buffer before it). The start time comes from the same hierarchy shifts use: the earliest technician's configured start if tech hours are set, otherwise business hours, otherwise 7:00 AM. If technicians have different start times, the earliest one is used so no shifts are off-screen. The auto-scroll fires only on load or day navigation; if the user scrolls manually, their position is not overridden. The full 24-hour timeline remains intact and scrollable.</p></li><li><p><strong>Sticky header bar.</strong> Date and time headers stick to the top of the viewport during vertical scroll, so the user always knows which time column they are looking at. This applies in both day and week views.</p></li><li><p><strong>Horizontal drag</strong> to move a shift's start time (snaps to 15-minute intervals).</p></li><li><p><strong>Edge resize.</strong> Drag the left or right edge to adjust duration.</p></li><li><p><strong>Lane stacking.</strong> Overlapping shifts split into parallel lanes per &sect;4.7.</p></li><li><p><strong>Lane height with VIN.</strong> When the VIN toggle is on (&sect;9), lane heights in day view grow to accommodate the additional VIN line so block text is not clipped.</p></li><li><p><strong>Now line.</strong> A vertical indicator showing the current time, with a label on hover over the grid.</p></li><li><p><strong>Business-hours shading.</strong> An optional grey overlay outside working hours.</p></li></ul><h3>4.9 Shift detail modal</h3><p>Clicking a shift block opens a detail panel showing:</p><ul><li><p>Customer name, unit number, VIN (always visible, below unit and asset), and work order id.</p></li><li><p>Scheduled date and start/end time pickers (15-minute increments).</p></li><li><p>Technician.</p></li><li><p>Time logged vs estimate (progress).</p></li><li><p>Scope summary and the scheduled line(s) with labor/status figures.</p></li><li><p>Estimated hours with inline edit.</p></li><li><p>Color picker (see &sect;10).</p></li><li><p>Notes: add, edit, and delete per work order.</p></li><li><p>A conflict banner with an &quot;Adjust&quot; action when the shift is conflicted.</p></li><li><p>Actions: Delete (series-aware, &sect;7)</p></li></ul><h3>4.10 Events</h3><p>Non-work-order time blocks (meetings, training, stand-ups) that occupy technician time:</p><ul><li><p>Create via left-click on empty grid space, which opens a menu with 'Create event' and 'New work order'..</p></li><li><p>Event modal: name, date, start/end time, all-day toggle, color category.</p></li><li><p>Drag-and-drop to reassign between technicians or move between days.</p></li><li><p>Day view shows a live preview block while creating, with drag-to-resize.</p></li></ul><p><strong>Event card anatomy.</strong> Event cards are styled to be structurally distinct from shift cards, so the two types are separable at a glance (not by color alone): a white/neutral card with a thin even border on all four sides and no colored left rail (the left rail is the shift's cue), a small grey-filled rounded chip on the left containing a calendar icon, and two lines of text beside it (event name, then the time range in secondary text). Shifts read as tinted color-filled blocks with a colored left rail; events read as quieter, white outlined cards.</p><p><strong>Event color.</strong> The default event color is neutral/grey. Events use the same custom color palette as shifts (the shared color picker with editable labels, see &sect;10); choosing a color from the event modal tints the card and icon chip in the matching tones, the same way a colored shift is tinted.</p><h3>4.11 Conflict detection</h3><p>The system continuously scans for scheduling issues and surfaces them in a toolbar pill:</p><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Conflict type</p></th><th><p>Description</p></th></tr><tr><td><p>Double-booked</p></td><td><p>Two different work orders overlap on the same technician at the same time.</p></td></tr><tr><td><p>Weekend shift</p></td><td><p>Shift scheduled on Saturday or Sunday (outside working days).</p></td></tr><tr><td><p>Before hours</p></td><td><p>Shift starts before the working-day start.</p></td></tr><tr><td><p>After hours</p></td><td><p>Shift extends past the working-day end.</p></td></tr></tbody></table><p>Conflicts appear as a warning icon on the affected block and are listed in a dropdown from the toolbar. Clicking a conflict navigates to the relevant technician and day. Red and other alarming styling is reserved for conflicts and genuine errors, never for overtime.</p><p>Events are not conflict-checked for now: an event overlapping a shift (or another event) does not raise a conflict. Their time still counts toward capacity (see &sect;4.12).</p><h3>4.12 Capacity visualization</h3><p>When enabled in View Options, each day column header shows a capacity bar. <strong>Fill represents aggregate utilization; overtime is a separate per-technician signal, and the two are independent.</strong> Event time is included in the utilization total alongside shifts, so meetings and training consume capacity even though they are not conflict-checked (see &sect;4.11).</p><ul><li><p><strong>Blue fill:</strong> aggregate technician-hours booked (shifts plus events) divided by total available (the sum of all techs' working hours). Clamped at 100%. The track width equals capacity and is identical across all days, so bars stay comparable at a glance (no per-day rescaling).</p></li><li><p><strong>Amber spill:</strong> when aggregate hours exceed capacity, an amber segment extends past the right edge of the track, with a tick at the 100% line.</p></li><li><p><strong>&quot;OT&quot; tag:</strong> appears whenever any individual technician exceeds their own daily hours, even when the day's aggregate is under capacity. It is a text tag, not a color-only signal.</p></li><li><p><strong>Hover tooltip:</strong> a per-assigned technician breakdown (assigned vs that tech's capacity), with overtime technicians highlighted in amber.</p></li></ul><h3>4.13 Hover tooltips (read-only)</h3><p>Hovering a block shows a quick peek without opening the modal.</p><p><strong>Shift tooltip:</strong> customer name (plus the conflict icon if conflicted); unit, vehicle, and VIN; date and time range; technician; scope summary (&quot;N lines &middot; Xh&quot;); the individual line names as a short list capped at 3 with a &quot;+N more lines&quot; row (no line statuses); a time-logged progress bar (&quot;X / Yh&quot;); and the conflict reason in amber when conflicted.</p><p><strong>Event tooltip:</strong> event name (plus its grey category dot); date and time range; technician.</p><p>Behavior: open after a roughly 300 to 500ms hover delay; dismiss on mouse-leave; read-only, so clicking the block still opens the full modal. Because the shift tooltip's height varies with the line list, it flips to open above the block when there is not room below and shifts horizontally to stay within the viewport, rather than being clipped.</p><hr /><h2>5. Sidebar features</h2><h3>5.1 Work order filters</h3><p>Filters live behind a &quot;Filter&quot; button (with an active-count badge); there are no assignment tabs. Applying a filter narrows the flat card list, and &quot;Clear all&quot; resets in one click.</p><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Filter group</p></th><th><p>Options</p></th></tr><tr><td><p>Assignment</p></td><td><p>Assigned, Unassigned</p></td></tr><tr><td><p>Status</p></td><td><p>All work order statuses currently supported in the app</p></td></tr><tr><td><p>Priority</p></td><td><p>High, Medium, Low</p></td></tr></tbody></table><p>Search and filter work together: the search field (see &sect;3.1) narrows by text match, and the filter button narrows by structured attributes. Both can be active at the same time.</p><p>The line drill-down has its own filters: All and Unscheduled (lines with no shifts yet), plus a line-name search (see &sect;3.1).</p><h3>5.2 Mini calendar</h3><ul><li><p>Month/year picker (grid of month buttons, year nav arrows).</p></li><li><p>Collapsible: a chevron toggle hides the calendar grid to maximize work order list space.</p></li><li><p>Selected date highlighted; today indicated; week row highlighted on hover.</p></li></ul><h3>5.3 Panel collapse</h3><p>An icon button collapses and expands the left panel. It is the first item in the grid toolbar, left of Today, sitting in the same left gutter as the grid's row labels and avatars so it reads as belonging to the panel it controls, and grouping with the date controls.</p><ul><li><p><strong>Control.</strong> A borderless panel-left icon in secondary text color. The icon does not change between states; the tooltip carries the meaning &mdash; &quot;Hide panel&quot; when open, &quot;Show panel&quot; when collapsed.</p></li><li><p><strong>Behavior.</strong> The panel animates closed over a short width transition, its divider disappears so no seam remains, and the grid reflows into the reclaimed space.</p></li><li><p><strong>State preservation.</strong> Contents are hidden rather than discarded. Calendar date, work-order scroll position, panel search text, drill-down state, and the selected work order all survive a collapse/expand cycle, and reopening returns to whichever panel mode was active.</p></li><li><p><strong>Narrow viewports.</strong> Below the 960px minimum supported width (&sect;11) the panel auto-collapses. The toggle still works, so the user can expand it manually at any width; that manual choice holds until the next resize across the breakpoint.</p></li><li><p><strong>Popovers and modals.</strong> Anything that positions itself clear of the panel falls back to a normal viewport margin while the panel is collapsed.</p></li><li><p><strong>Persistence.</strong> Not persisted in the prototype. Session-scoped per user for build &mdash; this is a working-mode preference, not a saved view.</p></li></ul><hr /><h2>6. Grid toolbar</h2><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Control</p></th><th><p>Function</p></th></tr><tr><td><p>Panel toggle</p></td><td><p>Collapses and expands the left work order panel (&sect;5.3).</p></td></tr><tr><td><p>Today button</p></td><td><p>Jumps the grid to the current date.</p></td></tr><tr><td><p>Left/right arrows</p></td><td><p>Navigate by day, week, or month depending on the active range.</p></td></tr><tr><td><p>Date label</p></td><td><p>Shows the current range (e.g. &quot;Jul 14 to 20, 2026&quot;).</p></td></tr><tr><td><p>Conflict pill</p></td><td><p>Shows the issue count; click opens the conflict detail dropdown.</p></td></tr><tr><td><p>Search</p></td><td><p>Filters grid blocks by matching against customer name, WO number, unit number, technician name, and line name.</p></td></tr><tr><td><p>Filter and Display</p></td><td><p>Dropdown (checkbox style) combining department visibility toggles, My Shifts, and VIN. Replaces the former &quot;Departments&quot; control.</p></td></tr><tr><td><p>View Options</p></td><td><p>Toggles: Business hours, Capacity bars, Events, Tech hours, Saturday, Sunday.</p></td></tr><tr><td><p>Day / Week / Month</p></td><td><p>Segmented control to switch the grid range.</p></td></tr></tbody></table><hr /><h2>7. Interactions and micro-interactions</h2><ul><li><p><strong>Drag feedback.</strong> Drop-target cells highlight, and a ghost block shows the line name and hours.</p></li><li><p><strong>Shift reassignment.</strong> Dragging a shift block from one technician row to another reassigns it: the target technician is added to the affected line's roster and the source technician is removed. A confirmation modal handles cross-tech moves.</p></li><li><p><strong>Left-click on empty grid space</strong> opens a menu with: Create event, New work order.</p></li><li><p><strong>Toast notifications.</strong> Every create, delete, move, and reassign action produces a toast with an Undo option. The toast persists for 4 to 7 seconds, stays while the cursor is over it, and dismisses on mouse-leave.</p></li><li><p><strong>Keyboard support.</strong> Global shortcuts work anywhere on the schedule page:</p><ul><li><p><strong>Escape</strong> closes the topmost open modal or popover, following a defined stacking order (delete scope, reassign, spread, capacity, event modal, event view, line picker, shift detail, cell menu, calendar picker, customize, filters, search). Within the shift modal, Escape first dismisses any open sub-picker (color picker, time picker, note edit) before closing the modal itself.</p></li><li><p><strong>Enter</strong> confirms the active confirmable dialog (delete scope, reassign, spread, event create/edit). It does not fire inside textareas, so multiline note editing still works normally.</p></li><li><p>Drag-and-drop has a click-to-arm alternative for users who cannot drag.</p></li></ul></li><li><p><strong>Series-aware deletion.</strong> Deleting a shift that belongs to a series asks for scope. This is routine, lightweight editing (undo toast, not the alarming destructive styling):</p><ul><li><p><strong>This shift only:</strong> removes that day. The series keeps the gap (it is not auto-closed), and the hours return to the estimate's remaining.</p></li><li><p><strong>This and everything after:</strong> removes from the clicked shift onward, keeping earlier shifts.</p></li><li><p><strong>The whole series:</strong> removes all of the series' shifts for that technician.</p></li><li><p>The options adapt to position: on the first shift, &quot;this and after&quot; equals &quot;whole series&quot; (show two options); on the last shift, &quot;this and after&quot; equals &quot;this only&quot; (two options); only middle shifts show all three. Each option states its consequence in hours returned (&quot;returns 8h&quot; / &quot;returns 56h&quot;).</p></li></ul></li></ul><hr /><h2>8. Data model</h2><h3>8.1 Key entities</h3><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Entity</p></th><th><p>Key fields</p></th><th><p>Relationships</p></th></tr><tr><td><p>Shift</p></td><td><p><code>sid</code>, <code>woId</code>, <code>rowKey</code> (tech), <code>date</code>, <code>startHour</code>, <code>blockDuration</code>, <code>lines[]</code>, <code>seriesId</code></p></td><td><p>Belongs to Work Order; assigned to Technician; optionally part of a Series</p></td></tr><tr><td><p>Event</p></td><td><p><code>eid</code>, <code>name</code>, <code>rowKey</code> (tech), <code>date</code>, <code>startHour</code>, <code>endHour</code>, <code>allDay</code>, <code>color</code></p></td><td><p>Assigned to Technician</p></td></tr><tr><td><p>Work Order</p></td><td><p><code>id</code>, <code>customer</code>, <code>unit</code>, <code>asset</code>, <code>status</code>, <code>priority</code>, <code>hrs</code>, <code>color</code></p></td><td><p>Has many Lines</p></td></tr><tr><td><p>Line</p></td><td><p><code>num</code>, <code>title</code>, <code>status</code>, <code>est</code>, <code>actual</code>, <code>total</code>, <code>labor[]</code></p></td><td><p>Belongs to Work Order; has many Technicians via the labor roster (no cap)</p></td></tr><tr><td><p>Technician</p></td><td><p><code>key</code>, <code>name</code>, <code>role</code>, <code>dept</code>, <code>hours</code> (working start/end plus working weekdays)</p></td><td><p>Belongs to Department; has many Shifts and Events</p></td></tr><tr><td><p>Department</p></td><td><p><code>key</code>, <code>name</code></p></td><td><p>Has many Technicians</p></td></tr></tbody></table><p>The Line's <code>labor[]</code> roster has no maximum; any number of technicians may be on a line. An unassigned shift has an empty or placeholder <code>rowKey</code> until it is moved onto a technician.</p><h3>8.2 Series</h3><p>A series groups shifts created by the spread step; all shifts in a series share a <code>seriesId</code>. The series supports scoped deletion (this / this-and-after / whole) and renders as a connected banner in month, week, and day views. It is a grouping over ordinary daily shifts, not a distinct persisted entity beyond the shared id, and each daily shift carries its own hours for capacity math.</p><hr /><h2>9. View options and customization</h2><p>Display settings are split across two toolbar controls:</p><p><strong>Filter and Display dropdown</strong> (checkbox style, &sect;6):</p><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Option</p></th><th><p>Default</p></th><th><p>Effect</p></th></tr><tr><td><p>Department toggles</p></td><td><p>All on</p></td><td><p>Show or hide individual department groups in the grid.</p></td></tr><tr><td><p>My Shifts</p></td><td><p>Off</p></td><td><p>Filters the grid to show only shifts assigned to the current user. All other technician rows and their shifts are hidden. This is a personal convenience filter, not a permission boundary.</p></td></tr><tr><td><p>VIN</p></td><td><p>Off</p></td><td><p>Shows the VIN number as an additional line on shift blocks (day and week views) and in hover tooltips. The VIN is always visible in the shift detail modal regardless of this toggle.</p></td></tr></tbody></table><p><strong>View Options popover:</strong></p><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Option</p></th><th><p>Default</p></th><th><p>Effect</p></th></tr><tr><td><p>Business Hours</p></td><td><p>Off</p></td><td><p>Shades non-working hours in day view.</p></td></tr><tr><td><p>Capacity Bars</p></td><td><p>On</p></td><td><p>Shows per-day capacity utilization bars in column headers.</p></td></tr><tr><td><p>Events</p></td><td><p>On</p></td><td><p>Shows non-WO event blocks on the grid.</p></td></tr><tr><td><p>Tech Hours</p></td><td><p>Off</p></td><td><p>Displays each technician's working hours next to their name.</p></td></tr><tr><td><p>Saturday</p></td><td><p>On</p></td><td><p>Includes the Saturday column.</p></td></tr><tr><td><p>Sunday</p></td><td><p>On</p></td><td><p>Includes the Sunday column.</p></td></tr></tbody></table><hr /><h2>10. Color system</h2><ul><li><p><strong>Blue</strong> is the default color for all shifts, including long or multi-week jobs.</p></li><li><p><strong>Grey</strong> is the default color for events.</p></li><li><p>All other colors are optional and chosen by the user from the color picker in the shift or event detail modal, to distinguish shifts however the shop likes.</p></li><li><p>Color labels are editable per shop.</p></li></ul><p>Each color provides three tones: background fill, text color, and accent (left border). There are no fixed semantic meanings tied to specific colors beyond the two defaults above.</p><hr /><h2>11. Non-functional requirements</h2><ul><li><p><strong>Performance.</strong> The grid must render smoothly with up to 15 technicians x 7 days x several shifts per cell. The sidebar work order list virtualizes at 50+ items, as does the line drill-down for orders with many lines.</p></li><li><p><strong>Responsiveness.</strong> Minimum supported width is 960px (the grid scrolls horizontally below that), and the sidebar collapses on narrow viewports (&sect;5.3).</p></li><li><p><strong>Accessibility.</strong> All interactive elements are keyboard-reachable; focus rings follow the design system; modals trap focus and close on Escape; drag-and-drop has a click-to-arm alternative. Overtime and conflict signals are not color-only (OT uses a text tag; the overflow uses shape).</p></li><li><p><strong>Undo.</strong> Every destructive action (delete, move, reassign) is undoable for 4 to 7 seconds via a toast that persists while hovered.</p></li><li><p><strong>Dark theme.</strong> The Schedule supports a user-selectable Light / Dark theme, chosen from the user menu and persisted per user. It is built on the design-system color tokens, so surfaces, borders, text, and accents remap automatically; elevation/shadow tokens also swap so depth reads correctly on dark surfaces.</p></li></ul><hr /><h2>12. Edge cases and constraints</h2><ul><li><p>A technician can have multiple shifts on the same day (different work orders); overlapping times render in parallel lanes with a 3-lane cap and &quot;+N more&quot; overflow (&sect;4.7).</p></li><li><p>Every shift has a start time, resolved from the hierarchy in &sect;4.2 or from the drop position in day view; unassigned shifts use the same rules minus technician hours until they are assigned.</p></li><li><p>Shop closures (holidays, inventory days) are defined at the shop level and block the spread step from placing shifts on those days.</p></li><li><p>Dropping the same work order on multiple technicians creates independent series, each spreading the full estimate, so planned hours across technicians may exceed the estimate. This is expected, since clocked-in time drives progress.</p></li><li><p>Whole-order and multi-line-subset shifts both render as &quot;N Lines&quot; on the block; the detail modal and hover tooltip provide the specifics.</p></li><li><p>Dragging a shift between technicians reassigns it, adding the target technician to the affected line's roster and removing the source technician.</p></li></ul><hr /><h2>13. Success metrics</h2><table data-table-width="760" data-layout="default"><tbody><tr><th><p>Metric</p></th><th><p>Target</p></th><th><p>Measurement</p></th></tr><tr><td><p>Scheduling conflicts</p></td><td><p>Under 5% of shifts</p></td><td><p>Flagged shifts divided by total shifts per week</p></td></tr><tr><td><p>Time to schedule</p></td><td><p>Under 30s per shift</p></td><td><p>Median time from drag start to shift creation</p></td></tr><tr><td><p>Adoption</p></td><td><p>80% of active shops</p></td><td><p>Shops with at least 1 shift created per week, 90 days post-launch</p></td></tr><tr><td><p>Undo usage</p></td><td><p>Under 10% of actions</p></td><td><p>Undo clicks divided by total scheduling actions</p></td></tr></tbody></table><hr /><h2>14. Roles and permissions</h2><p>The Schedule is a CRUD area in ShopView's custom roles and permissions system. Access is controlled by three independent permission levels (View, Edit, Delete), where Delete requires Edit and Edit requires View. The schedule also depends on permissions from other areas, particularly Work Orders.</p><h3>14.1 Permission tiers</h3><p><strong>Schedule: View.</strong> The user can see the schedule page, navigate between day/week/month views, use the mini calendar, search, filter, view all technicians' shifts and events, hover for tooltips, and open shift/event detail modals in read-only mode. All editing affordances (drag handles, drop targets, right-click context menu, resize handles, creation entry points, edit fields in modals, reassign and delete actions) are hidden or disabled. This is the experience for roles like Technician, Parts Manager, Parts Tech, Office, and Time Clock. When Schedule: View is OFF, the Schedule top-level nav item is hidden entirely.</p><p><strong>Schedule: Edit</strong> (requires View). Unlocks all creation and modification interactions: drag-and-drop from the sidebar, the scope picker, the spread modal, shift and event creation (including via right-click context menu and day-view click-to-create), shift reassignment between technicians, edge-resize and horizontal drag in day view, and editing fields in the shift/event detail modals. This is the level for Service Manager, Senior Service Advisor, Service Advisor, and Foreman roles.</p><p><strong>Schedule: Delete</strong> (requires Edit). Unlocks deleting shifts and events, including series-aware deletion with its three scopes (this shift / this and after / whole series). Without Delete, a user with Edit can create and modify but cannot remove shifts or events. The delete action and the trash icon are hidden.</p><h3>14.2 Work order sidebar dependency</h3><p>The left panel sidebar displays work order data (customer, unit, lines, lead tech) and requires <strong>Work Orders: View</strong> to populate. If a user has Schedule access but Work Orders: View is OFF, the sidebar hides the work order list and line drill-down (the mini calendar remains available). The user can still view and interact with shifts already on the grid, but cannot drag new ones from the sidebar since the WO list is not visible.</p><h3>14.3 No permission-level &quot;own only&quot; restriction</h3><p>The schedule always shows all technicians' shifts and events for every user who has Schedule: View. There is no role-based restriction to &quot;own shifts only.&quot; This is intentional: the schedule is a shared coordination resource that service advisors and managers use to orchestrate work across the team. The &quot;My Shifts&quot; toggle in the Filter and Display dropdown (&sect;9) provides this as an optional personal convenience filter, not a security boundary.</p><h3>14.4 Technician grid rows are department-based</h3><p>Whether a user appears as a row in the schedule grid is controlled by their <strong>department assignment on their staff record</strong>, not by their role permission. Any staff member assigned to a department that is visible on the schedule appears as a technician row, regardless of their role. Similarly, the ability to clock into work order line tasks is controlled by the &quot;Time Clock&quot; setting on the staff record, not by the permission model.</p><hr /><h2>15. Future considerations</h2><ul><li><p><strong>Technician availability and PTO.</strong> Block out vacation, sick time, and training that are not Events, and have the spread step flow around them.</p></li><li><p><strong>Auto-scheduling.</strong> Suggest optimal technician assignments based on skills, availability, and workload balance.</p></li><li><p><strong>Recurring events.</strong> Repeating calendar blocks for stand-ups and safety meetings.</p></li><li><p><strong>Skill matching.</strong> Warn when a technician is assigned to a line requiring certifications they do not hold.</p></li><li><p><strong>Spread around existing bookings.</strong> Have the spread step automatically flow around days the technician is already booked (skipping them and extending the end date), rather than requiring the manager to handle conflicts manually.</p></li><li><p><strong>Long-job cap.</strong> Past a certain length, instead of materializing every daily shift, represent a very long job as a single assignment span across a date range and let clock-ins fill the actuals, reducing board clutter and the number of shift records to manage.</p></li></ul>