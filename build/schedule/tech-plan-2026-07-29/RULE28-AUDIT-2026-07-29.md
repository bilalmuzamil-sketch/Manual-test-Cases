# Rule-28 Ruthless Usefulness Audit — Schedule tech-plan reconciliation pass (2026-07-29)

**Scope of this audit:** 100% of what this pass touched — the **13 new cases**
(cases-H-tech-plan.json) and the **16 edited cases** (2 tester-facing + 14 notes-only).
The 177-case baseline was already adversarial-reviewed CLEAN (2026-07-21) and
re-audited through the 2026-07-22 and 2026-07-27 passes; this pass changes nothing
tester-facing in it beyond the 2 edits below, so it is not re-scored here (stated
honestly per Rule 17: total in scope this pass = 29; processed = 29; baseline 161
untouched-cases excluded with that reason).

## Dimension 1 — USEFUL (per-case verdicts, 13 new)

| Case | Verdict | Why it earns its place |
|---|---|---|
| SCH-SPREAD-11 (series caps UI) | **KEEP** | Distinct observable (warn+ack at 8 weeks; hard refusal past 120); failure = real bug (runaway series was an actual prototype bug the plan calls out) |
| SCH-DEL-10 (commit-immediately undo) | **KEEP** | Distinct observable (refresh-before-Undo keeps the change); prevents a false bug report AND catches a real data-loss class (tab close losing actions) |
| SCH-EDGE-07 (DST series) | **KEEP** | Real calculation contract (local wall-clock stability); runnable any day by spanning the next real clock-change date; classic silent-corruption bug class |
| SCH-EDGE-08 (dark mode) | **WEAK-KEEP** | Legitimate (new module + the plan mandates the dark-mode pass) but broad-sweep/low-severity; ONE consolidated case, not a per-dialog explosion — flagged low-value, kept at Low priority |
| SCH-REG-01 (data survives rewrite) | **KEEP** | Release-critical regression for a full rewrite with data migration; failure = customer-visible data loss |
| SCH-REG-02 (dashboard one row per WO) | **KEEP** | Intended-change guard — without it QA files the aggregated row as a missing-rows bug (the plan explicitly asks for this QA note) |
| SCH-REG-03 (WO-create appointment on board) | **KEEP** | Cross-module path being rewired; explicit cutover smoke in the plan; zero prior coverage |
| SCH-REG-04 (multi-location single-board) | **KEEP** | Intended-change guard (plan: "Call this out in QA notes so it isn't filed as a bug"); zero prior coverage |
| SCH-REG-05 (WO Priority field) | **KEEP** | New user-facing field the existing filter case (C29945) silently depends on; failure = real bug |
| SCH-API-01 (permission matrix) | **KEEP** | One consolidated BE-enforcement matrix (deliberately NOT per-endpoint/per-tier slop); the BE half our 12 UI permission cases lack |
| SCH-API-02 (caps 409/422) | **KEEP** | BE-authoritative caps ("API consumers must not bypass warn" — the plan's own requirement); distinct from the UI half |
| SCH-API-03 (no pricing / WO:View omission) | **KEEP** | Load-bearing security contract (D6/NFR-002) — response-level proof, stronger than the FE-masking UI case C30614 |
| SCH-API-04 (cross-location 404) | **KEEP** | The tenant-scoping improvement the rewrite exists to add (legacy had the gap); failure = data leak |

**Usefulness tally (new cases): 12 KEEP / 1 WEAK-KEEP / 0 MERGE / 0 CUT.** Slop patterns
hunted and avoided by construction: no per-endpoint permission explosion (one matrix
case), no per-view/per-dialog dark-mode split (one case), no happy-path-read API filler
(reads covered by the UI cases that consume them), no restore-endpoint duplicate
(covered inside the undo cases).

**The 16 edits:** 2 tester-facing additions (SCH-WOL-05 paged-loading expected line;
SCH-VIEW-03 My-Shifts-hidden line) both add a distinct observable / prevent a false
bug — KEEP-improving. 14 notes-only conflict/confirmation flags are QA-metadata (never
reach TestRail or the tester) — no usefulness impact, they *protect* the suite from
wrong pass/fail calls while NQ-1..NQ-5 are pending.

## Dimension 2 — MAKES SENSE (cold-read, 6 fail conditions)

All 13 new + 2 tester-facing edits read cold: **15 SENSIBLE / 0 FIX-WORDING / 0
NONSENSE.** Checks applied: steps executable in order (REG cases explicitly precondition
on the rewrite release being deployed — reachable, stated); expected follows from steps;
no internal contradictions (the closure-skip and double-booking contradictions live in
FLAGGED existing cases, deliberately not decided here); every control referenced traces
to the tech plan or spec (none invented — each case's notes cite the exact plan anchor);
no domain nonsense (caps, DST wall-clock, 404-not-403-for-foreign-id all match the plan's
stated contract); every case states what to DO and what PASS looks like.
KEEP-but-NONSENSE cross-check: none.

## Dimension 3 — GENUINE + LAYMAN-RUNNABLE

- **Genuine (Rule 20):** all 13 carry `refs` = story/epic ticket + spec anchor +
  tech-plan anchor (`SV-8691 (§4.5 + tech-plan D8 series caps)` etc.); cross-cutting
  ones use epic SV-8685 explicitly. No unsourced case.
- **Layman-runnable (Rules 7/9):** the 9 UI cases are plain-language, numbered, with
  tester-facing "expected, not a bug" notes where the behaviour could be misread
  (SCH-DEL-10, SCH-REG-02, SCH-REG-04). The 4 API cases use endpoints/status codes BY
  DESIGN — they sit in the "API — Schedule" section per Rule 4, same convention as every
  other project's API cases (run by API-capable testers, not the layman lane).
- **Wording honesty (Rule 12):** every tech-plan-pinned label/threshold carries a
  confirm-LIVE-at-VIU flag; nothing here is VIU-Verified — all 13 are VIU-Pending.

## Is the critic right? (honest answer for this pass)

Waste: **1/13 flagged WEAK-KEEP (7.7%), 0 cut-worthy** — nowhere near a "70% useless"
pattern; every other case maps to a real bug class (data loss, security leak, silent
time-shift, false-bug prevention, release regression). Makes-no-sense: **0%** on cold
read. The pass deliberately consolidated where explosion was possible (permissions,
dark mode), which is the opposite of the named slop patterns.

**Suite headline after this pass: 190 active authored (177 + 13), all traceable, all
VIU-Pending for the new/edited content; no TestRail writes.**
