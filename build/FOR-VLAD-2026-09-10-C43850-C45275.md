# FOR VLAD (Rule 65) — two Automated cases corrected 2026-09-10

Both cases below are **Vladimir Tomovic's** (`created_by = 1`) and are flagged **Automated**
(`custom_atmstatus = 3`). They are normally HANDS-OFF (Rule 38). They were corrected **only** because
Vladimir gave explicit permission for this session (relayed by the QA lead); the Rule 38 conflict was
surfaced first (Rule 63). **Neither case's tested behaviour was changed** — the edits are
traceability, wording, and step/expected structure so they stop failing in automation and read cleanly.
`custom_atmstatus` stays **3** on both.

| C-id | Link | What changed (no behaviour change) |
|---|---|---|
| **C43850** | https://shopview.testrail.io/index.php?/cases/view/43850 | Location filter follows the signed-in user's **enrollments**. Preconds + 6 steps reworded to "locations the user can access"; dropped an unsupported "no browser reload" claim ("a browser reload is fine"); added provenance (epic SV-8582, WIP PRD page 703660034 S7-R9/S7-R11, read 10 Sep) + `AUTOMATION: READY`; automation type set **Functional (2)**. Preconds repaired to render cleanly (`fr-view`). |
| **C45275** | https://shopview.testrail.io/index.php?/cases/view/45275 | Authorizer clears on customer change, not on contact change. The 5 expected outcomes were **redistributed one-per-step** (they were lumped and did not match their steps); wording aligned to the spec labels; dropped an unsupported "always first" claim; added split provenance (picker → SV-8218 / page 755990532 S3-R5/S3-R6; customer-change behaviour = "Manually added (QA lead) from product knowledge" because Story 3 is silent) + `AUTOMATION: READY`; automation type kept **E2E (1)**. |

**Why they were failing in automation (most likely):**
- **C43850** — the steps summarised a state ("scope follows enrollments") rather than naming the concrete
  UI route (Administration → Staff → your row → Departments → "Manage staff enrollments"), so the harness
  could not follow them. Now every step is a UI action a runner can execute.
- **C45275** — the expected outcomes were not attached to the steps that produce them, so an automated run
  asserting "this step ⇒ this outcome" mismatched at every line. Now one outcome per step.

**Source note:** C45275's *customer-change-clears-the-Authorizer* behaviour is **not in the spec** (Story 3
is silent). It is recorded as product-knowledge, not invented from the build. If the build disagrees at
build-verification, that becomes a PO question, not a silent case change.

**Reconciliations:** `build/report-suite/source-verify-2026-09-10/RECONCILE-C43850.md` ·
`build/inline-add-edit-parts/source-verify-2026-09-10/RECONCILE-C45275.md`.
