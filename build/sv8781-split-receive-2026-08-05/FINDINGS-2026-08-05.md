# SV-8781 — split work-order line → receive on one vendor invoice

**Ticket:** [SV-8781](https://shopview.atlassian.net/browse/SV-8781) — "Vendor invoice receiving splits
parts from same vendor into separate orders/receipts for parts (WO S2-16887)" · Bug · Medium ·
**QA Severity: High** · status **TESTING QA** · assignee **Dipesh Changawala**
**Reported by:** Curtis Scragg, Foothills Group Inc (409 users), via Intercom — escalated by Mike Freeman
**Written:** 2026-08-05

---

## ⛔ BLOCKED BEFORE THE FIRST STEP — and it is the environment, not the test

**I have not run this test, and I am deliberately not running it on `sv7324`.** Two things stand in
the way, and the first one matters more than the second.

### 1. `sv7324.qa.shopview.com` almost certainly does NOT contain this fix

The brief said to test on `sv7324.qa.shopview.com`. That is the **SV-7324 HEIC branch environment**
from the previous task. The evidence says the SV-8781 fix is not in it:

| | `sv7324.qa.shopview.com` | `sv8781.qa.shopview.com` |
|---|---|---|
| App version | **`v3.4.2-fc52c44`** | **`v3.5-fb6371c`** |
| `index.html` last-modified | Tue **04 Aug** 2026 12:36:01 GMT | Wed **05 Aug** 2026 13:18:47 GMT |
| Purpose | the HEIC `accept`-attribute hotfix (SV-7324) | **named after this ticket** |

- **A dedicated `sv8781.qa.shopview.com` environment EXISTS** and was deployed **today at 13:18 UTC**.
- Dipesh marked SV-8781 **"Ready for QA" at 2026-08-05 08:35 −0500 = 13:35 UTC** — **17 minutes after**
  that environment was built. They go together.
- `sv7324` was built **25 hours earlier**, and on an **older minor line** (`3.4.2` vs `3.5`).
- The SV-7324 QA handoff records that branch as *"Base: origin/main (4481d819e0) — main-based hotfix"*
  cut on 3 August — before this fix existed.

**Testing SV-8781 on `sv7324` would produce a FAILED verdict on a fix that simply is not deployed
there.** That is worse than no result: it is a false accusation against a colleague's work, and it
would send Dipesh hunting a bug that does not exist. So I stopped and asked instead.

### 2. The supplied session does not authenticate on either host

| Host | Result |
|---|---|
| `sv7324api.qa.shopview.com` `/api/auth/me/fe-permissions` | **401** `sso_required` |
| `sv7324api.qa.shopview.com` `/api/quick-login` | **401** `sso_required` |
| `sv8781api.qa.shopview.com` `/api/auth/me/fe-permissions` | **302** (redirect to login — Cloudflare passed, session not valid there) |
| `sv8781api.qa.shopview.com` `/api/quick-login` | **403** (Cloudflare — `cf_clearance` is issued per hostname) |

The `sv_sso_session` value supplied is the same one as the earlier HEIC pass, and it is no longer
being accepted. **`cf_clearance` is host-bound**, so a clearance minted on `sv7324` will not clear
`sv8781` — cookies must be captured **from the environment being tested**.

---

## Why I could not settle the environment question from the code

I tried, so that this would not need to come back to you. Both environments' frontend bundles were
downloaded and the receive screen compared directly:

| Chunk | `sv8781` (v3.5) | `sv7324` (v3.4.2) | Verdict |
|---|---|---|---|
| `AcceptDelivery` | 15,078 bytes | 15,078 bytes | same size |
| `BulkReceive` | 4,192 | 4,190 | 2-byte chunk-reference difference |
| `MultiVendorReceive` | 17,002 | 16,266 | 736 bytes apart |

But the 736 bytes turn out to be **minifier variable renaming only** (`s.id` vs `t.id`,
`l.groupKey` vs `i.groupKey`). Functionally the two receive screens are **identical**: the same
`vendor-group` / `purchaseOrders` rendering, the same `groupKey` grouping, the same
`MergeVendorConfirmDialog` (3 references in both), and **the same `data-test-id` set and API
parameters**.

**That is itself a useful finding:** the visible "one block per vendor" grouping is **pre-existing**,
not new in this fix. Which means **SV-8781's fix is predominantly BACKEND** — *"splitting a line now
moves its ordered parts onto the new work order's purchase order"* — and **no amount of frontend
bundle inspection can confirm or deny it.** It has to be tested by actually driving the flow, on the
environment that has the backend change. Hence the ask.

---

## What the fix is, in plain words

From Dipesh's 2026-08-03 explanation and his 2026-08-05 *Ready for QA* note:

**The old behaviour:** when a work-order line was **split** onto a new work order, any parts already
ordered for that line **stayed attached to the original work order's purchase order**. The parts
looked right on the new work order, but at receive time they were not available there — because the
purchase order they belonged to still sat on the old work order. So they appeared to "split away" from
the rest of the vendor invoice and could not be received together.

**What changed:** splitting a line now **moves its already-ordered, unreceived parts onto the new work
order's own purchase order**, and the receive screen **merges a vendor's purchase orders into one
block** for a work-order-scoped receive — so everything for that work order can be received on one
vendor invoice.

**Note:** moving a part between lines already worked correctly; only **Split** was affected.
**Out of QA scope** (Dipesh's own words): the CLI repair command for already-affected work orders is a
dev/ops action.

---

## The test I will run once the environment and session are sorted

Nothing here needs new information — this is ready to execute. Test data will be seeded by me
(Standing Rule 5/14), tagged **ZZAUTOTEST**, and cleaned up afterwards.

**Reproduction data will be named exactly** (Standing Rule 50) — the canned line, customer, vendor,
part numbers and both work-order numbers — so anyone can re-run it and land on the same result.

| # | Step | What proves PASS |
|---|---|---|
| 1 | Create work order **A** with at least two lines | — |
| 2 | Add parts to one line and **order** them from a single vendor **V**; leave them **unreceived** | a purchase order for V exists on WO **A** |
| 3 | **Split** that line onto a new work order **B** | — |
| 4 | Inspect WO **B**'s parts and its purchase order | ✅ the ordered parts now sit on **B's own purchase order**, not stranded on A's — *this is the core of the fix* |
| 5 | Open the receive screen scoped to WO **B** | ✅ **one** vendor block for V, holding the items, receivable under **a single vendor invoice** |
| 6 | Receive everything under one invoice number | ✅ correct **per-item posting** |
| 7 | Repeat with a **partial** receive — take some items, leave others | ✅ **partially-received parts stay behind** and remain receivable |
| 8 | Regression: **move** a part between lines (not split) | ✅ still behaves correctly (Dipesh says this path was always right) |

Steps 4, 5 and 7 are the three Dipesh explicitly asked QA to focus on. Each gets its own **inline
screenshot** as evidence, and the verdict will be a plain **Passed** or **Failed** per step plus
overall.

---

## OUTSTANDING — what I need from you

1. **Confirm the environment.** My strong recommendation is **`sv8781.qa.shopview.com`** — it is named
   after this ticket, was built 17 minutes before the ticket went to QA, and runs `v3.5`. If you
   genuinely want `sv7324`, say so and I will run it there, but I expect a FAILED that means nothing.
2. **Cookies captured from that environment** — `sv_sso_session`, `PHPSESSID` and `cf_clearance` taken
   **after signing in to `sv8781.qa.shopview.com` itself**, because `cf_clearance` is host-bound and
   the `sv7324` set is rejected there.
3. **Optional but useful:** the **Powertools test plan** Dipesh linked
   (`powertools.shopview.com/qa/test-plans/92d1d2d1-7258-4a0f-88dc-5a9b4268a6ef`) is behind a login I
   do not have. My plan above is derived from his ticket notes; if his plan has extra cases I would
   rather match it than guess.

Nothing was written anywhere: no TestRail write, no Jira write, no comment on the ticket, no test data
created.
