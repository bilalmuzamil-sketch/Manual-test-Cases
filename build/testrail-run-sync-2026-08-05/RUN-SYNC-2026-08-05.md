# TestRail run sync — 2026-08-05

**Standing Rules 6 · 8 · 12 · 17 · 27 · 29 · 34 · 38 · 47 · 50.**
**Authorisation, verbatim (the QA lead, 2026-08-05):** *"Please run this sync and the syncs for
other projects too."*

**One-line outcome:** **one run needed a write and got one.** Run 359 (Report Suite) went
**469 → 473 tests**, gaining the four cases authored earlier today; **runs 357 and 352 were already
complete and NO WRITE WAS MADE TO EITHER**. **All 1 407 previously recorded result records across the
three runs are present BY ID and byte-identical on every field** — not one graded value moved, and
not even the two declared read-time echoes moved.

---

## 1. Scope — three runs, and only three (Rule 47)

| Run | Project | TestRail group | In scope? |
|---|---|---|---|
| **359** — *Reports Suite - Nebojsa/Viktoria (VIU Pending)* | Report Suite | 4281 | **YES** |
| **357** — *Schedule - Ayesha (VIU Pending)* | Schedule | 4254 | **YES** |
| **352** — *Filters - Ahtasham (Awaiting QA- ENV)* | Filters | 4110 | **YES** |
| 278 · 324 · 325 · every other run | — | — | **OUT OF SCOPE** |

**Runs 278, 324 and 325 were not read, not audited for gaps, and not written to.** The executor
cannot reach them: the scope list is hard-coded to the three runs above, and there is no override
flag. Rule 47's wording is that out-of-scope runs are *"not synced, not written to, and NOT AUDITED
for missing cases"* — so their absence from this document is deliberate, not an omission.

---

## 2. What was found, per run

Every figure below was read live from TestRail, fully paged, before anything was written.

| | Run 359 Report Suite | Run 357 Schedule | Run 352 Filters |
|---|---|---|---|
| `include_all` | **false** | **false** | **false** |
| Tests before | **469** | **165** | **110** |
| Result records before | **535** | **429** | **443** |
| Live cases in the group | 478 | 165 | 110 |
| — of them **ours** (`created_by = 3`) | **473** | **165** | **110** |
| — of them **foreign** | **5** | 0 | 0 |
| Union we computed | **473** | 165 | 110 |
| Cases added | **4** | **0** | **0** |
| **Write performed?** | **YES — one `update_run`** | **NO** | **NO** |
| Tests after | **473** | 165 (untouched) | 110 (untouched) |

**`include_all` is `false` on all three.** That is the whole reason this task exists: a
fixed-selection run **never** picks up a newly added case on its own, so a case can be authored,
pushed and byte-verified and still be invisible to the tester who has to execute it.

### The four cases added to run 359

All four are ours (`created_by = 3`, verified live per case, not assumed):

| Internal ID | Case | Title |
|---|---|---|
| SBC-COL-04 | [C43550](https://shopview.testrail.io/index.php?/cases/view/43550) | A one-location user never sees Location in the column-selection list |
| WIP-PERS-05 | [C43551](https://shopview.testrail.io/index.php?/cases/view/43551) | A hand-made Location column choice is remembered like any other column |
| TU-EXP-10 | [C43552](https://shopview.testrail.io/index.php?/cases/view/43552) | Both spreadsheet downloads hold the summary rows and no per-day rows |
| SBC-EXP-17 | [C43553](https://shopview.testrail.io/index.php?/cases/view/43553) | A logo that is set but will not load falls back to the ShopView logo |

This clears **item 5** of the OUTSTANDING-ITEMS register, raised 2026-08-04.

### The five foreign cases (Rule 38)

Group 4281 holds five cases authored by **Vladimir Tomovic** (`created_by = 1`):
**C38919 · C38920 · C38921 · C38922 · C38923**.

**Authorship was derived LIVE from `created_by`, not from the hard-coded list in the brief** — so a
foreign case created today would have been caught. None was: the five are exactly the five already
known, and no case in any of the three groups has any author other than user 1 or user 3.

**None of the five is currently in run 359**, so:
- they were **excluded from the union** we sent — we never add another author's case to a run; and
- there was **nothing to preserve**, because they were already absent. Had any been present it
  would have stayed in the union, since dropping it would have deleted his test and any result on
  it. The executor asserts both conditions before writing.

**Consequence, stated plainly so it is not mistaken for a gap:** run 359 now holds **473** tests
against **478** live cases in the group. The five-case difference **is** the five foreign cases and
is **correct** — it is Rule 38 working, not an incomplete sync.

### Runs 357 and 352 — complete already, and confirmed rather than assumed

The brief said these were *reported* complete and told me to confirm it myself. I did, and they are:
Schedule **165 of 165** ours in the run, Filters **110 of 110**. **Set equality was proven in both
directions** (nothing live is missing from the run, and nothing in the run is absent from the
group), so **no `update_run` was called on either**. A no-op write would still have been a write,
and on a fixed-selection run holding 872 combined result records there is no such thing as a
harmless one.

---

## 3. Verification — Rule 50, exhaustive then exact

The write was verified twice: once inside the executor, and then **independently**, by re-reading
TestRail from cold and diffing against the **committed** BEFORE snapshot rather than the script's
own in-memory copy. Both agree exactly.

### Run 359 — the run that was written

| Check | Result |
|---|---|
| `update_run` HTTP status | **200** |
| The run's own record, field by field | **35 fields compared.** The only fields that moved are **`untested_count`** (**463 → 467**, exactly the four new tests) and **`updated_on`** — both derived. **`passed_count` 6 → 6, `failed_count` 0 → 0, `blocked_count` 0 → 0, `retest_count` 0 → 0**; name, description, milestone (`None`), assignee (`None`), `include_all` (`false`) and every other field **byte-identical**. |
| `case_id` set vs the union | **Equal in BOTH directions** — `got − want` empty, `want − got` empty. Not compared by count. |
| Test count | **469 → 473**, expected 473 |
| Prior tests present **by id** | **469 of 469**, `lost = 0`, and **`rebound = 0`** — no test id was re-pointed at a different case |
| Prior result records present **by id** | **535 of 535**, `missing = 0` |
| Graded fields on those 535 | **0 changes** across `status_id · comment · defects · elapsed · version · assignedto_id · created_by · created_on · test_id · case_id · id · attachment_ids` |
| Declared echoes `case_title` / `case_refs` | **0 moved** (see §4) |
| New result records during the window | **0** |

### Runs 357 and 352 — the runs that were not written

Both were re-read after the run-359 write and diffed against their own committed BEFORE snapshots,
because *"we did not write to it"* is an assertion and a byte-identical snapshot is evidence:

| | Run 357 Schedule | Run 352 Filters |
|---|---|---|
| Tests | 165 → **165** | 110 → **110** |
| Prior tests lost / rebound | **0 / 0** | **0 / 0** |
| Prior results present by id | **429 of 429** | **443 of 443** |
| Graded-field changes | **0** | **0** |
| Any other field changed | **0** | **0** |
| New results in the window | **0** | **0** |

**Total: 535 + 429 + 443 = 1 407 result records, every one present by its own id and byte-identical
field for field.**

---

### A note on the two pre-write captures, so the evidence chain is not ambiguous

The executor snapshots every run at the start of **each** invocation, so the read-only `--audit` pass
and the `--authorized` pass each produced a pre-write capture, **two minutes apart**. The first was
the one committed to git before any write (commit `529a3fa`); the second is what now sits in
`snapshots-before/`. **Both are genuinely pre-write, and they were diffed against each other:**

| Run | Test id sets equal | Result id sets equal | Fields differing on shared results | New results in the gap |
|---|---|---|---|---|
| 359 | yes | yes (535) | **0** | none |
| 357 | yes | yes (429) | **0** | none |
| 352 | yes | yes (443) | **0** | none |

**Identical apart from `captured_utc`** — no tester graded anything in the interval — so the
verification in §3 holds against the committed capture and the re-capture alike. This is spelled out
rather than glossed because a re-captured "before" file is exactly the kind of thing that can quietly
weaken an evidence chain, and it should be checked rather than assumed harmless.

## 4. The declared normalisations, and why neither fired

`case_title` and `case_refs` on a result record are **read-time echoes** of the case's *current*
title and references — playbook §J declared normalisations **#2** and **#2b**. They legitimately
differ when a case has been retitled or had its `refs` edited, which happened on all three projects
earlier today.

**Neither moved on any of the 1 407 records in this pass**, and that is exactly what should be
expected: **this task made no case writes at all**, so no case title or `refs` changed under the
run. The echoes are recorded here so the *zero* is understood as a positive result rather than a
check that was skipped — and, per the brief, they were **not** available as an excuse: any field
other than those two moving on a prior result would have stopped the batch.

---

## 5. What was NOT done — the prohibitions, itemised

| Operation | Count |
|---|---|
| `add_result` / any result write | **0** — the QA lead's standing instruction is *"For now keep on asking me for running the tests."* This task syncs **which cases are in** a run; it grades nothing. |
| `add_run` · `delete_run` · `close_run` | **0** |
| `update_case` · `add_case` · `delete_case` · `add_section` | **0** — this task touches runs only |
| `update_run` | **1**, on run 359 only |
| Any run field other than the case selection | **0.** The payload was exactly `{"include_all": false, "case_ids": [<the full 473-item union>]}`. `include_all` was sent explicitly at its **existing** value, so it is not a change; the field-by-field diff of the run record proves nothing else moved. |
| Runs 278 / 324 / 325 touched or read | **0** |

**The union was a union.** `sorted(set(current) | set(ours))`, asserted to be a superset of the
current selection before the request was built. Never a replacement list, never a diff of what was
missing.

---

## 6. Four-counts reconciliation, per project

| Project | Live cases (ours) | Tests in the run | id-map rows | Import rows | Blank C-ids |
|---|---|---|---|---|---|
| Report Suite | **473** | **473** | **473** | **473** | 0 |
| Schedule | **165** | **165** | **165** | **165** | 0 |
| Filters | **110** | **110** | **110** | **110** | 0 |

All set-equal. Run 359's test count now equals our live case count for the first time since the four
cases were authored.

---

## 7. Files in this folder

| File | What it is |
|---|---|
| `RUN-SYNC-2026-08-05.md` | this document |
| `tools/run_sync_2026_08_05.py` | the executor — `--audit` is read-only, `--authorized` writes |
| `snapshots-before/run-{352,357,359}-before.json` | **every test and every result record in full**, captured and **committed to git before any write** |
| `snapshots-after/run-359-after.json` | the same, re-read after the write |
| `sync-plan.json` | the computed plan: union, additions, foreign exclusions, per run |
| `verification.json` | the machine-readable verdict for the write |
| `run-sync-execution-log-2026-08-05.md` | per-operation audit log with before → after counts |

## 8. What I changed in the reusable checker, and why

The prior pass left `build/testrail-run-sync-2026-07-31/run_sync_audit.py` (read-only) and
`sync_runs_EXECUTOR.py` (never executed). I read both first and built on them rather than starting
over (Rule 27). Seven things are different, and two of them matter a great deal:

1. **Scope is hard-coded to the three ACTIVE runs.** The old `PLAN` also listed 278, 324 and 325.
   Rule 47 now puts those out of scope, so they are unreachable from the new script.
2. **⚠️ Foreign-case exclusion is derived live from `created_by`** instead of a hard-coded id list.
   The old executor had no concept of a foreign case at all — it would have added
   **C38919–C38923 straight into run 359**, breaching Rule 38 on its first use.
3. **⚠️ Verification is by id and field by field.** The old one accepted
   `len(res2) >= len(results)` — which **cannot detect a destroyed result**, because a run that
   loses 30 old records and gains 30 new ones passes that test. It also compared only
   `status_id`, and stored a 3-field digest rather than the records, so the evidence to
   re-check later did not exist.
4. **Snapshots are full and persisted into the repo**, not a digest in a scratch folder.
5. **Case-id sets are proven equal in both directions**, never by matching totals.
6. **The run's own record is diffed field by field**, so a stray change to name, description,
   milestone or assignee would be caught rather than assumed absent.
7. **The sequence stops dead on the first failed verification** and does not touch a later run.
   The old loop carried on and merely printed `*** CHECK ***`.

The 2026-07-31 read-only auditor is still the right tool for the *cross-project* question ("which
runs exist and how complete is each"); this one is the narrower, safer executor for the three active
runs.

---

## OUTSTANDING — what I need from you

**Nothing outstanding for the run sync itself.** All three in-scope runs are complete and verified,
and register item 5 is cleared.

Two things worth knowing, neither of them a request:

1. **Run 359 will fall out of sync again the moment another case is authored** — `include_all` is
   `false` and TestRail will not pick new cases up on its own. This is a standing duty under Rule
   34/47, not a one-off task, so it needs re-running after every authorised `add_case`. If you would
   prefer it never to drift, the alternative is to have the run's owners recreate it with
   `include_all: true` — **that is their decision and their run, and it would need Nebojsa's or
   Viktoria's agreement, so I have not proposed it to them.**
2. **Run 352 now holds 443 result records, not the ~438 quoted in the brief.** Ahtasham has graded
   five more since that figure was taken, and he has been working in it today. Nothing was disturbed
   — all 443 verified byte-identical — but it is worth noting that these runs are **live** while we
   sync them, which is precisely why the snapshot-then-union-then-verify sequence is not optional.
