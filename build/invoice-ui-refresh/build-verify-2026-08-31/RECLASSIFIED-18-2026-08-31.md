# The 18 — re-classified. Most were never blockers.

**QA lead correction, 2026-08-31:** *"'I cannot observe this on the build' is NOT 'blocked'."*
He is right, and my previous `REMAINING-18` file was wrong in its framing. This replaces it.

**The routing I skipped:** `build/skills/03-RUN-CHECK.md` **§7** (Rule 69 — a not-built feature is a
FINISHED case with a defined marker, a dated re-check line and a `DEFERRED-RUN.md` row), **§8**
(Rule 74 — a data state or a login is NEVER a blocker; seed it, log in as the role) and **§8.0-a**
(the section I wrote myself this morning — a failing check is a statement about my check).

## The corrected picture

| Class | Cases | Is it a blocker? |
|---|---|---|
| **Rule 69 — feature not built. FINISHED case.** | **6** | **No.** Documented expectation kept, dated marker, deferred-run row. |
| **Staging-only — portal. FINISHED case.** | **3** | **No.** Defined marker already stamped. |
| **Rule 58 — ambiguous source. HELD case + PO question.** | **2** | **No.** The deliverable is the held case and the question row. |
| **MINE — testable, my unfinished work.** | **7** | **No.** Nothing external stops these. |
| **Genuinely blocked** | **0** | — |

**So: 11 of the 18 are finished work in a defined state, and 7 are mine to do. None is blocked.**

---

## 1 · Payment reversal — I failed the Rule 97 drill (C45177, C45196, C45197)

**The recipe was already written down and I did not grep for it.** `build/APP-ACTIONS-PLAYBOOK.md`:

- **Reverse a payment** (line 2342) — UI: Customers → customer → **Payments** tab → row delete/trash
  icon → confirmation (*"reverse the payment for all invoices … record preserved for audit"*) →
  **Reverse**. API: `POST /api/customer-account/reverse-customer-payment` → **201**. Gated by
  **`invoicingPaymentsDelete`**. *Confidence: High (executed 201).*
- **Reverse an invoice** (line 2325) — a different action: WO → Finance → invoice **three-dot** menu →
  **Reverse**. `POST /api/invoices/reverse-invoice {id}` → 200. Gated by **Work Orders: Delete**.

I concluded "no reversal action exists" after reading the Finance tab and guessing four route shapes.
**Both were wrong moves.** The action is not on the Finance tab and nothing says "reverse" until the
dialog opens — and the control is **permission-gated**, so its absence may be my role, not the build
(**Rule 26**: reset the role to template and retry *before* concluding absence).

**⇒ These three are MINE, not blocked.**

## 2 · Customer portal — and one case I mis-scoped (C44947)

Re-checked all four **from their preconditions**, per core §5.0-b:

| Case | Precondition needs | Verdict |
|---|---|---|
| C44951 | *"an Invoice PDF generated by the customer portal"* | **Staging-only.** HOLD correct. |
| C44952 | *"a portal-generated Invoice PDF with a single payment…"* | **Staging-only.** HOLD correct. |
| C45175 | *"Portal invoices … Generate each portal PDF"* | **Staging-only.** HOLD correct. |
| **C44947** | *"An Invoice has a payment with a shop-configured method name, one with an unconfigured code (for example credit_card), and one made online (SHOPPAY)"* | **🔴 IN SCOPE — I was wrong.** |

**C44947 is about the METHOD NAME on the invoice's Payments rows (S8-R2), not the paid banner.** Those
rows render on a shop-app document — the captured Parts Sale invoice already shows
`Jun 9, 2026 - Visa`. It needs three seeded payments (a configured method, an unconfigured code, and
SHOPPAY), not a portal PDF. **Its HOLD marker is wrong and must come off.**

## 3 · Not built — Rule 69, and these are FINISHED cases

| Case | Feature absent | Evidence |
|---|---|---|
| C44937 · C44938 · C44939 | `"Show declined work"` toggle | Absent from the Invoice Details dialog with a **positive control firing in the same read** (7 sibling settings FOUND). Story 6 **SV-9145** In Progress. |
| C44942 | `"Show % on Estimates and Invoices"` | Absent from the dialog; a source read found no backend field either. Story 7 **SV-9146**. |
| C44987 | invoice **import / batch** | `/api/invoices/import`, `/api/invoices/batch`, `/api/work-orders/import`, `/api/imported-work-orders` → all **404**; nothing in the playbook; templates explicitly deferred to **SV-9193**. *(Route guesses are weak evidence alone — this rests mainly on the deferral and the absent nav.)* |
| C45185 | pre-redesign **snapshot** | `historyEvent` = none/1/2/5/99 all return a **byte-identical** document (one sha across five values). See the defect candidate. |

**Each gets:** the documented expectation kept (Rule 57), the marker
`AUTOMATION: Not available on Build to test Yet - Last checked 8/31/2026`, the §7.3 tester-facing
re-check line, and a row in `DEFERRED-RUN.md`. **They are excluded from the ready-to-automate count.
That is a finished case, not a gap.**

## 4 · IBS Approval Code — Rule 58 (C44913, C44916)

The build cannot settle whether `changeIBSApproval` accepts an arbitrary code, and **Rule 58 says an
ambiguous source is never resolved by looking at the build.** So the deliverable is **the held case
plus a PO question row**, which is now written: `questions-2026-08-31/PO-QUESTIONS-IBS.md`.

## 5 · C45185 — a finding, not an obstacle

`historyEvent` binds and changes nothing. Written up as a defect candidate with the byte evidence.

## 6 · Mine, and correctly named (C44923, C45190, C45191)

Unchanged — my own unfinished work, plus C44947, C45177, C45196, C45197 moved here.

---

## What I actually have left to do: 7 cases

| Case | What it needs | Recipe |
|---|---|---|
| C44947 | 3 payments: configured method, unconfigured code, SHOPPAY | seed via `create-customer-payment` (playbook :2330s) |
| C45177 | reverse a payment so Balance > $0.00 | playbook **:2342** |
| C45196 | part cash + part customer credit on one invoice | seed payments; CM-101 exists to apply |
| C45197 | a credit whose origin invoice was reversed | playbook **:2325** then the credit document |
| C44923 | a contact with `Approves Work` newly ticked | `input_checkbox_is_authorizer` on Edit Contact |
| C45190 | an **imported** work order (2 of 3 surfaces already done) | may be genuinely absent — same check as C44987 |
| C45191 | login without work-order edit permission | **§8.2a** — swap the **Technician** role, never the Admin |
