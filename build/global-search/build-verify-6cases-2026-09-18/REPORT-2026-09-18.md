# Build verification — Global Search: 6 new cases C55718–C55723
## sv9160.qa.shopview.com build `v26.36.7-29ca209`, 2026-09-18

**Scope:** the 6 newly authored Global Search cases from the 2026-09-18 seed note —
Permissions & Role-Based Scoping (section **6734**): C55718, C55719, C55720, C55721;
Ranking & Prioritization (section **6726**): C55722, C55723. All `created_by=3`, none Automated
(`custom_atmstatus=1`), automation type Functional (`custom_automation_type=2`). None are Vladimir's.

**Source gate:** PRD v1.5 (Confluence page 576978945), source-verified 2026-09-16, unchanged — within
the Rule 77 validity window; each case's provenance cites v1.5. **Build:** marker read live
`v26.36.7-29ca209`, matching the seeding session's recorded marker (branch not redeployed; seed data
present). Seed note: `build/global-search/SEED-NOTE-6-NEW-CASES-2026-09-18` (attached by the QA lead).

### What was done to the cases (build-verify deliverables, Rules 54/102)
- **`<br>` removed** from every precondition and step; rewritten as clean `<p>` blocks (the API-write
  literal-render landmine, §J/L0028). Confirmed structure-preserving: served container stayed
  `markdown fr-view` after the write (L0028 holds).
- **Provisional line replaced with the build stamp** in Expected: "Last checked against build
  v26.36.7-29ca209 on 18 September 2026." (Rule 54 sentence 2); the "not yet build-verified" line is gone.
- **Two steps anchored for runnability** (Rule 102 / skill 18): C55719 and C55721 step 2 was the bare
  "Read the results." (fails the runnable gate, R4 — nothing to aim at). Now "Read the grouped results
  in the search palette, looking at the Customers / Parts group." **The Expected assertion was not
  changed** — only the step was told where to look.
- Every Expected still ends with `AUTOMATION: READY` as the last line; markers and metadata unchanged.

### Table 1 — DONE (observed live on the build)
| Case | What was observed live (as admin, all-access) | Evidence |
|---|---|---|
| **C55722** — more open work orders ranks higher | Search `ZZOPENCOUNT` → Customers group order: **`ZZOPENCOUNT Freight Busy` (badge "5 open") ABOVE `ZZOPENCOUNT Freight Quiet` (badge "1 open")**. The ordering rule holds. **PASS.** | order-ZZOPENCOUNT png + order-lines json |
| **C55723** — name match beats secondary-field match | Search `ZZNAMEBONUS` → Customers group order: **`ZZNAMEBONUS Cartage` (name match) ABOVE `Sterling Brothers Freight` (matched only on its address "40 ZZNAMEBONUS Road")**. **PASS.** | order-ZZNAMEBONUS png |
| **C55718** — exact-number positive side | Search `S2-15430` → 1 result across 1 category (the permitted user gets the exact-identifier hit). The negative side is the reduced-role run. | q-S2_15430 png |
| **C55719** — contact-match positive side | Search `2643286723` (contact phone) → 1 result across 1 category: the parent **company** row returned via a contact match (the "Contact match" row indicator is live — also seen on the ZZOPENCOUNT customer rows). | q-2643286723 png |
| **C55720** — all areas present for a full-access user | Search `Fib` → **all 8 groups** returned (Work orders 20 · Customers 3 · Assets 6 · Parts 4 · Vendors 1 · Part sales 1 · Purchase orders 6 · Vendor invoices 4). This is the baseline the reduced-role run subtracts from. | order-Fib png |
| **C55721** — typo fuzzy positive side | Search `Altenator` (typo) → Parts group returns fuzzy hits with the on-screen **`≈close match:`** indicator (e.g. "…105a alternator", "ZZPARTBUSY Alternator Active/Quiet"). Fuzzy match is live. | order-Altenator png |
| Gates | Runnable 6/6 · Precondition-label 6/6 (33/33 in both sections) · stored render clean · served render 6/6 `fr-view`, 0 escaping, 0 literal tags | check_runnable_cases / check_precond_labels / check_case_render / served-scan.json |

### Table 2 — LEFT (tester-run with the seeded reduced roles — by design, seed-and-observe)
| Item | How it is finished, concretely |
|---|---|
| **C55718 negative** — restricted user gets nothing | Sign in with role **`ZZAUTOTEST No Work Orders View`** (recorded id `87ba8d85-…`), search `S2-15430`; the work order must NOT appear (not pinned, not in any group). Positive side already observed as admin. |
| **C55719 negative** — contact match hidden without Customers | Sign in with role **`ZZAUTOTEST No Customers View`** (`530eb5d2-…`), search the contact phone `2643286723` (or `zzrankf@northgate-cartage.test`); no Customers row for `Northgate Cartage Company` (the company name contains neither value). |
| **C55720 negative** — several areas hidden together | Sign in with a role missing **both** Work Orders **and** Vendor & Order Management, search `Fib`; four groups vanish (Work orders, Vendors, Purchase orders, Vendor invoices), the other four stay. **See Table 3 note on the role.** |
| **C55721 negative** — typo doesn't leak a part | Sign in with role **`ZZAUTOTEST No Parts View`** (`745590e6-…`), search `Altenator`; the part must not appear even though the typo would normally fuzzy-match it. |
| **C55722 / C55723** — read the order | Already observed live as admin (Table 1). The tester re-confirms with the seeded controls in place. |

### Table 3 — BLOCKED (and what it does NOT block)
| Blocker | Does NOT block |
|---|---|
| **None for the build-verify.** All render/gate/observe work is complete on the current build. | — |
| **Watch-out, not a blocker — the combined role for C55720.** The recorded role file (`build/global-search/seeding/roles-gsv2-qa.json`) lists the single-bundle `ZZAUTOTEST` roles but **not** a combined "No Work Orders + No Vendors" role. The 2026-09-18 seed note says `ZZAUTOTEST No Work Orders Or Vendors` was created for C55720 — the run/seeding session should confirm it exists on the branch (or build one by removing both bundles from a copy) before running C55720's negative side. | the other five cases; the C55720 positive baseline (all-8 groups) already observed |

### Table 4 — HOW TO UNBLOCK
- C55720 combined role: run `build/global-search/seeding/status.py` / check the roles list on the branch;
  if the two-bundle role is absent, `seed_roles.py` builds `ZZAUTOTEST` roles from template — add one
  missing both `workOrdersView` and `vendorOrderManagementView`.

### Table 5 — HANDOFF-READY
**YES.** All 6 are runnable (6/6), precondition-label-clean, stored + served render clean (`fr-view`),
build-stamped `v26.36.7-29ca209`, marker READY, metadata intact. The two ranking behaviours (C55722,
C55723) were observed PASS live; the four permission cases' positive surfaces were observed live and
their negative sides are tester-run with the named seeded reduced roles (by design). Run **R415** holds
these cases. A run handoff accompanies this report.
