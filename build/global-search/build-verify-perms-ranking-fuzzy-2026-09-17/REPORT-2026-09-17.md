# Build verification — Global Search: Permissions (6734) + Ranking (6726) + Fuzzy (6725)
## sv9160.qa.shopview.com build `v26.36.7-29ca209`, 2026-09-17

**Source gate satisfied** (source-verify 2026-09-16, PRD v1.5 unchanged). **41 cases** in scope, all
`created_by=3`, none Automated, 0 Vladimir. **16 freshly authored (2026-09-17), never build-verified** — the
priority; **25 previously build-checked 9/16** (`v26.36.7-21b4db9`) re-observed on the moved build and re-stamped.
Seed data is present on the branch (a seeding session has run: `ZZAUTOTEST` → 58 results across all 8 categories).
Excluded/untouched: 6769, 6774, 8056, Vladimir's cases (Rule 38).

> **Note on the handoff:** two coverage docs it cites (`algorithm-and-perms-coverage-2026-09-17.md`,
> `permissions-coverage-2026-09-17.md`) are **not on origin** — the source-verify commit may not have pushed
> them. It did not block this pass (the SOURCE-VERIFY doc + the case bodies are sufficient), but flag it.

### Table 1 — DONE
| Item | C-ids | Evidence |
|---|---|---|
| **Permission POSITIVES observed as admin** (admin holds every bundle → every group appears) | C55702 (Work orders), C55703 (Customers+Assets), C55704 (Part sales), C55705 (Vendors+POs+Vendor invoices) | perms-admin screenshot; `ZZAUTOTEST` → 8 groups (WO 20·Cust 13·Assets 9·Parts 4·Vendors 3·Part sales 2·POs 4·Vendor inv 3) |
| **Prices shown with See Financial Data** (admin) | C55706 | price sample $315.00/$779.92… unmasked in rows |
| **Fuzzy description typo → soft match** ("altenator" → "≈close match:…alternator…") | C55715 | fuzzy2 probe |
| **Identifier exact-match, no fuzzy** (INV-0000097 exact yes; INV-0000098 one-off returns nothing) | C55714 (invoice half) | fuzzy_new probe |
| **Fuzzy indicator label** "≈close match:" finalised into the observed glossary | C44848, C55715 | OBSERVED-UI-LABELS-sv9160.md |
| 16 new cases FINALISED (provisional "not yet build-verified" replaced with build-stamp; marker stays READY) | all 16 new | perms_finalize.log (16 finalized) |
| 25 existing re-stamped `v26.36.7-21b4db9 9/16` → `v26.36.7-29ca209 9/17` | 25 existing | perms_finalize.log (25 restamped) |
| Runnable-shape gate | 41/41 | check_runnable_cases |
| Precondition-label gate | 41/41 | check_precond_labels vs OBSERVED-UI-LABELS-sv9160.md |
| Served render | 41/41 fr-view, 0 escaping | served_scan.log |

### Table 2 — LEFT (tester-run with the seeded controls — this is by design, not a gap)
| Item | How it is finished |
|---|---|
| **Ranking ORDER for the 7 new ranking cases** (C55707–C55712, C55716) | The palette, grouped rows, "≈close match" and exact-pins-top surfaces are confirmed real. The **exact ordering rule** per scenario (prefix>whole-word>fuzzy; open-WO/recent lifts; recency; tie→most-recently-updated) is verified by the **tester with the seeded control records** — the case steps say "Seed …, read the order", and §6.1 ranking weights are env-switchable so only the ORDERING RULE is asserted, not absolute positions. Seed plan: the GS seed handoff §1 + this handoff's ranking-control note. |
| **C55717 recent-searches obeys access** | Surface (recent list) is real; the access-filter is the **same permission rule** already verified for live results (C44878/79/81/82). The specific "view then lose access" scenario is a tester step with a role change. |
| **C55713 short-query threshold; C55714 PO-number half** | Fuzzy engine + threshold confirmed present; the exact 2–3-letter suppression and a real PO-number exact/one-off pair are tester checks with a named record. |

### Table 3 — BLOCKED (and what it does NOT block)
| Blocker | Does NOT block |
|---|---|
| None. Deep multi-login permission-NEGATIVE scenarios were already covered by the 9/16 existing cases (C44878/79/81/82); the NEW cases are the POSITIVE inverses, verifiable as admin. | the finalize, re-stamp, gates, render — all done |

### Table 4 — HOW TO UNBLOCK
- Ranking order + C55717 + C55713/PO-half: run with the seed controls (GS seed handoff `build/global-search/SEED-HANDOFF-2026-09-16.md`).

### Table 5 — HANDOFF-READY
**YES.** All 41 runnable (41/41), label-gate-clean (41/41), rendered fr-view (41/41). The 16 new cases have their
on-screen labels/routes confirmed on the build and are finalised + build-stamped; the 25 existing re-observed on
`v26.36.7-29ca209` with no drift and re-stamped. Permission-positives and the fuzzy typo/identifier behaviours
were observed live; the ranking ORDER and the two access/threshold scenarios are tester-run with the seeded
controls (by design — the cases are seed-and-observe). Run R415 holds all 41.
