# Build verification — Global Search: Quick Actions (6774) + V1 Regression derived (8056)
## sv9160.qa.shopview.com build `v26.36.7-21b4db9`, 2026-09-16

**Source gate satisfied** (source-verify 2026-09-16, commit 3c0be82c). Seed data confirmed present on the
branch (ZZAUTOTEST returns results; "star" → 85 across 6 categories) — no reseed needed.

## 6774 — Quick Actions on Hover (v1): 8 cases C44866–C44873
**FINDING: the hover quick-actions are NOT on the build — as expected from the obsolete epic SV-9173.**
Observed live on v26.36.7-21b4db9: searching a broad term and **hovering result rows of every type** (Work
Order, Customer, Asset) shows **no action buttons**; the **keyboard-highlight path** shows none either; and the
quick-action labels ("Add new line", "New work order", "New contact", "View part history", "Add contact")
appear **nowhere** in the palette DOM. Instrument proven: `elementFromPoint` confirmed the mouse was over the
row, and arrow-key navigation highlighted rows — the hover/focus landed, and still no action appeared.

- This is the **expected consequence** of **SV-9173 being OBSOLETE / "later release" (2026-09-10)**, and each
  case already **discloses** the PRD §5.4-vs-epic conflict. Per the handoff: absence = expected, **record/flag,
  do NOT fail the cases.**
- All 8 correctly **stay parked**: marker `AUTOMATION: Not available on Build to test Yet`. The build-check date
  was **re-stamped 9/9 → 9/16/2026** (checked on the current build today). No READY flip — the feature is not built.
- **PO decision item** (already flagged by source-verify): PRD §5.4 lists quick actions in v1, but the epic
  defers them. When/if quick actions ship, these 8 finalize per §5.4 with labels read off the build.

| Case | Expected quick action (PRD §5.4) | On build v26.36.7 |
|---|---|---|
| C44866 | Work Order → "Add new line" | absent |
| C44867 | Asset → "New work order" + history/invoices icons | absent |
| C44868 | Customer → "New work order" + "New contact" | absent |
| C44869, C44871 | Part → "View part history" only | absent |
| C44870 | Vendor → "Add contact" | absent |
| C44872 | quick actions never destructive | n/a (none present) |
| C44873 | a quick action per entity that has one | absent |

## 8056 — V1 Regression derived: 1 case C55684
Rule-109 parity case (V1 code is the spec; **not re-sourced against the PRD**). Already build-checked **READY**
by the parity lane on 2026-09-15 (within the Rule-77 validity window). **Data precondition re-confirmed on the
current build:** the four "Bridgeport" work orders (S9160-17625/26/27/28, ZZAUTOTEST Bridgeport Hauling) exist
on the Heavy Duty location. The case's own steps say to **watch the results closely as they load** — the
"no-flash on location switch" assertion is a human-eye, sub-second observation, not automatable; it stands on
the parity lane's 9/15 build-check. **Marker left unchanged** (handoff: left unchanged; parity-lane owned).

## Gates
- Runnable-shape gate: **9/9** (8 quick-action + C55684).
- Served-page render: **9/9 fr-view, 0 escaping**.
- Run R415 already holds all 9 (source-verify confirmed).

## Five-table summary
**DONE:** quick-actions confirmed absent on v26.36.7 (hover + keyboard + DOM text, instrument proven); 8 cases
re-stamped 9/16 and kept parked; C55684 data precondition re-confirmed. Gates 9/9, render 9/9.
**LEFT:** nothing in this pass — the 8 cases stay parked until the feature is built; C55684's no-flash is a
human-eye tester step on already-verified data.
**BLOCKED:** none.
**HOW TO UNBLOCK:** n/a.
**HANDOFF-READY:** YES — 6774 correctly parked with a fresh build-check date and a flagged PO decision item;
8056 already build-checked, data confirmed present.
