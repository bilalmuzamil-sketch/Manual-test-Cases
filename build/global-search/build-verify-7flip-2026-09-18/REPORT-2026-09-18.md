# Build verification — Global Search: 7 "flip access" cases C55731–C55737
## sv9160.qa.shopview.com build `v26.36.8-d146c39`, 2026-09-18

**Scope:** the 7 strict same-record permission-toggle cases in folder **6734** (per
`DATA-SEEDING-HANDOFF-PERMISSIONS-6734` / `perm-strict-cases-2026-09-18.json`): C55731 (Parts),
C55732 (Work Orders), C55733 (Customers+Assets), C55734 (Part Sales), C55735 (Vendor+PO+Invoice),
C55736 (See Financial Data / price), C55737 (count path). All `created_by=3`, none Automated
(`custom_atmstatus=1`), type Functional, none Vladimir's. **Source:** PRD v1.5 (Confluence 576978945),
§9, unchanged. **Build marker read live `v26.36.8-d146c39`.**

Each case is a **same-record toggle**: with one access bundle the record appears; without it the SAME
record is gone. The "with access" side is observable as admin; the "without access" side is a
reduced-role tester run (the seeded `ZZAUTOTEST No …` roles, 7/7 present per status.py).

### Build-verify deliverables applied (Rules 54/102, §J)
- `<br>` removed from every precondition/step → `<p>` blocks; served container `fr-view` (13… 7/7).
- **Concrete seed keyword named in each precondition** (runnability, skill 18): C55731 `ZZTOGPART`,
  C55732 `ZZTOGWO`, C55733 `ZZTOGCUST`, C55734 `ZZTOGPS`, C55735 `ZZTOGVEN`, C55736 `ZZTOGPRICE`,
  C55737 `ZZCOUNT` — written as the seed target (no inline styling tags, §J).
- Build stamp on every case: "Last checked against build v26.36.8-d146c39 on 18 September 2026."; the
  "not yet build-verified" line removed. `AUTOMATION: READY` last. Metadata unchanged.

### Table 1 — DONE
| Item | C-ids | Evidence |
|---|---|---|
| Runnable-shape gate | 7/7 | check_runnable_cases |
| Precondition-label gate | clean | check_precond_labels vs OBSERVED-UI-LABELS-sv9160.md |
| Stored render clean (no `<br>`, no inline tags) | 7/7 | check_case_render |
| Served render `fr-view`, 0 escaping | 7/7 | served_scan |
| Search route/surfaces executable on the build (palette, grouped results, per-group counts, scope tabs) | all | observe-7flip json + screenshots |
| **C55731 record present** — `ZZTOGPART` returns a Parts group (Parts (2)) | C55731 | q-ZZTOGPART png |
| **C55737 data present** — `ZZCOUNT` returns 24 across 4 groups (Parts 13, POs 9, Customers 1, Assets 1) — enough matching records for the count-restriction test | C55737 | q-ZZCOUNT png |

### Table 2 — LEFT (tester-run — by design, seed-and-observe)
| Item | How it is finished |
|---|---|
| The **without-access** half of every toggle case | Sign in as the matching `ZZAUTOTEST No …` role (7/7 present) and confirm the same record/price is gone. Roles named in the run handoff. |
| Pass/fail recording in R415 | Run session's lane. |

### Table 3 — 🔴 BLOCKED: DATA GAP — the toggle records are not fully seeded on this build
The permission-filter *mechanism* is verified (folder 6734's C44878/79/81/82 and today's cases). What
is missing is the **specific seeded record each toggle case names**. Observed live on `d146c39`:

| Case | Keyword | On the build now | Needed |
|---|---|---|---|
| C55731 | `ZZTOGPART` | **PRESENT** — Parts group returns | ok |
| C55737 | `ZZCOUNT` | **PRESENT** — 24 records across 4 groups | ok |
| C55732 | `ZZTOGWO` | **PARTIAL** — a customer + asset return, but **Work orders (0)**: the target work order is not findable | a work order matching `ZZTOGWO` |
| C55733 | `ZZTOGCUST` | **ABSENT** — No results | a customer + its vehicle |
| C55734 | `ZZTOGPS` | **ABSENT** — No results | a part sale |
| C55735 | `ZZTOGVEN` | **ABSENT** — No results | a vendor + its PO + its vendor invoice |
| C55736 | `ZZTOGPRICE` | **ABSENT** — No results | a priced part |

**Root cause:** these ZZTOG* toggle records are **not a registered seeding universe** — there is no
committed idempotent seeder for them (`reseed_everything.sh` covers V1-regression, Fibridge, ranking/
fuzzy, roles, PO/invoices, recent-activity — not these). `status.py` confirms "everything this branch
has ever been seeded with is present", i.e. these were never seeded by a committed script (or were
hand-seeded and lost to a redeploy). The branch redeployed **three times on 2026-09-18**
(`29ca209 → 069b8c2 → d146c39`), so any ad-hoc seed will not survive; a **committed seeder + manifest
registered in `reseed_everything.sh`** is required (skill 20). This is the data-seeding lane's
deliverable (`DATA-SEEDING-HANDOFF-PERMISSIONS-6734`).

**Does NOT block:** the build-verify itself (routes/surfaces/gates/render all done, 7/7), nor C55731
and C55737 whose data is present, nor any of the 25 duplicate cases (their universes are all PRESENT).

### Table 4 — HOW TO UNBLOCK
- Seed the five records above under a committed, idempotent kit (`scaffold_seeding.py`, skill 20),
  register it in `reseed_everything.sh`, and add spine probes to `status.py` so a redeploy is caught.
  Then re-observe C55732–C55736 with access (present) and without (gone).

### Table 5 — HANDOFF-READY
**PARTIAL.** All 7 are build-verified as **runnable, render-clean, label-clean, build-stamped
`d146c39`** — the case structure is tester-ready. **Two (C55731, C55737) have their data present now;
five (C55732–C55736) need their ZZTOG* records seeded first** (Table 3). The run handoff names the
role per case and the data gap so the run session knows exactly what must be seeded before it can run
those five. Run **R415** holds all 7.
