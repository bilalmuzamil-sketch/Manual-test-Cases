# Build verification — Global Search: same-record permission toggle (7 cases)
## sv9160.qa.shopview.com build `v26.36.8-d146c39`, 2026-09-20

**Scope:** the 7 toggle cases in section **6734**: C55731, C55732, C55733, C55734, C55735, C55736,
**C55737**. All `created_by=3`, none Automated (`custom_atmstatus=1`), none Vladimir's. **Source:**
PRD v1.5 (Confluence 576978945) §9. Each case = the same search run twice, once with an access bundle
and once without; the record appears then must be gone.

**Data:** now fully seeded (the 2026-09-20 seeding pass — previously 5 of the 7 records were absent).
Every keyword returned its record live in this pass (below). Build marker read live `v26.36.8-d146c39`.

### Build-verify deliverables (Rules 54/102, §J)
- `<br>`-free, `<p>`-balanced, served/stored render `fr-view` clean (verified).
- Concrete seed keyword named in each precondition; build-stamped `v26.36.8-d146c39`.
- **C55737 marker changed to "Not available on Build to test Yet - Last checked 9/20/2026"** (Rule 69) — see Table 3.
- Gates: runnable 7/7, precondition-label clean, render clean.

### Table 1 — DONE (positive side observed live on `v26.36.8-d146c39`, as admin)
| Case | Keyword | Observed | 
|---|---|---|
| **C55731** | `ZZTOGPART` | Parts group present incl. **`ZZTOGPART Brake Kit`** (see §2 note 1 — two stray brake parts also match; harmless) |
| **C55732** | `ZZTOGWO` | Work Orders group present incl. **`S9160-17699`** (see §2 note 2 — a customer/asset also match and correctly STAY when WO access is removed) |
| **C55733** | `ZZTOGCUST` | Customers (**`ZZTOGCUST Freight`**) AND Assets (its vehicle) both present — one permission gates both |
| **C55734** | `ZZTOGPS` | Part sales tab shows (1) — the part sale `P9160-265` is present |
| **C55735** | `ZZTOGVEN` | Vendor (**`ZZTOGVEN Supply`**), Purchase order (**`I9160-1399`**) and Vendor invoice all present (see §2 note 3 — a part row also stays; correct) |
| **C55736** | `ZZTOGPRICE` | Part **`ZZTOGPRICE Filter`** (`ZZTOGPRICE-1002`) present. **Price on the row is the tester's read** — the seeding session measured **137.45** (the purchase price, not the 305.44 sell price) on this build; my generic palette capture surfaced the row but not the price column, so the run session confirms the price renders on the row before judging the mask. |
| **C55737** | `ZZTALLYQ` | 2 rows present (keyword corrected from `ZZCOUNT` by the seeding session). **Held — see Table 3.** |

### Table 2 — LEFT (tester-run — the without-access half + the price mask)
Each case's second half is a reduced-role run (the six `ZZAUTOTEST No …` roles). Run the WITH-access
half first (records above); then sign in WITHOUT the one bundle and confirm the SAME record is gone:
- C55731 → `ZZAUTOTEST No Parts View` (Parts group gone)
- C55732 → `ZZAUTOTEST No Work Orders View` (Work Orders group gone; the customer/asset stay — §2)
- C55733 → `ZZAUTOTEST No Customers View` (customer AND vehicle both gone)
- C55734 → `ZZAUTOTEST No Part Sales View` (part sale gone)
- C55735 → `ZZAUTOTEST No Vendor Order View` (vendor + PO + invoice **all three** gone — all-or-nothing)
- C55736 → `ZZAUTOTEST No Financial Data` (**row stays, price masked** — a disappearing row here is the WRONG answer)

### Table 3 — 🔴 C55737 HELD — NOT AVAILABLE ON BUILD (Rule 69)
C55737 needs a role that can see *some* records of a type but is blocked from *one specific record of
that same type*. Permissions on this build are **per entity type, not per record** — a role sees all
customers or none. The preconditions cannot be executed, so the marker is
**"Not available on Build to test Yet - Last checked 9/20/2026"** and a reason paragraph was added; the
documented Expected is kept intact (Rule 57). A PO question is drafted and with the QA lead. The case
asserts a NEGATIVE (a hidden record is not counted) — if no record can be hidden it would pass while
proving nothing, so it must NOT be run as a pass.

### Table 4 — 🔴 THREE THINGS THAT LOOK LIKE BUGS AND ARE NOT (read before raising any defect)
1. **`ZZTOGPART` also returns two unrelated brake parts** (`ZZSTOCKPART`, a near-spelling). Harmless — removing Parts access hides every part. Pick our row by name.
2. **`ZZTOGWO` also returns "Stock Diesel Services Inc" + its vehicle, and they STAY after the flip.** Those are Customers/Assets rows, which the Work Orders permission doesn't govern — supposed to remain. C55732 is about the Work Orders group only. Read the row, not the screen.
3. **`ZZTOGVEN` leaves a Parts row after the flip.** The part the PO was raised for is governed by a different permission — correct, not a leak.
Rule behind all three: judge by *"is OUR named record there?"*, never by row count. The search is deliberately fuzzy.

### Table 5 — HANDOFF-READY
**6 of 7 YES; C55737 not-available.** All 7 runnable, label-clean, render `fr-view`, stamped
`v26.36.8-d146c39`. The six runnable cases' positive-side records are present and observed live; the
without-access halves are tester reduced-role runs (roles named above); C55736's price is a tester read
(seeding measured 137.45); C55737 is held (per-record permission absent). Run **R415** holds all 7. A run
handoff accompanies this report.

### Note — session credentials (durable)
This session started in a fresh container with `/tmp` wiped; the TestRail **API key** (`/tmp`-only per
§0.3) was gone. Confirmed the seeding session's finding: TestRail's `/index.php?/api/v2/` accepts the
**committed web login password** (`ENVIRONMENT-CREDENTIALS.md` §4) via Basic auth. Rebuilt
`/tmp/testrail/creds.json` from it. The `sv9160` QA cookie was re-supplied by the QA lead.
