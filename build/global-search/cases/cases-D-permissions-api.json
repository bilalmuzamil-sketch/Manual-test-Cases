[
 {
  "id": "GS-PERM-01",
  "area": "Permissions and Role-Based Scoping",
  "title": "A user WITH Parts access sees Parts results in the palette",
  "priority": "High",
  "type": "Functional",
  "permissions_required": "A role that includes Parts / inventory access.",
  "preconditions": [
   "1. You are signed in as a user whose role includes Parts access.",
   "2. Parts exist that match the query."
  ],
  "steps": [
   "1. Open the palette and type a query that matches parts.",
   "2. Read the results (on the 'All' tab and the 'Parts' tab)."
  ],
  "expected": "1. A 'Parts' group appears with matching part rows.\n2. The 'Parts' scope tab shows the part results.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 9 (all result fields respect role-based-access checks), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 9 (role-based access)",
  "spec_ref": "requirements.md 9 (all result fields respect role-based-access checks)",
  "viu_status": "VIU-Pending",
  "notes": "Positive permission case; pair with the negative GS-PERM-02. Requires role setup at VIU time - assign the test user a role WITH Parts access, then restore afterward (Standing Rule 5). [⚠ v2 REVIVAL 2026-08-21: SUPERSEDED for scope by GS-PERM-06 (v2 nine-entity bundle mapping + price masking, NFR-004/005). Retire/rewrite is the QA lead's call - see revival report.]",
  "api_related": false,
  "refs": "SV-9160 (PRD 9 (all result fields respect role-based-access checks))"
 },
 {
  "id": "GS-PERM-02",
  "area": "Permissions and Role-Based Scoping",
  "title": "A technician WITHOUT Parts access does NOT see Parts results",
  "priority": "Critical",
  "type": "Negative",
  "permissions_required": "A role WITHOUT Parts / inventory access (for example a restricted technician).",
  "preconditions": [
   "1. You are signed in as a user whose role does NOT include Parts access.",
   "2. Parts exist that would match the query (verified visible to a Parts-enabled user)."
  ],
  "steps": [
   "1. Open the palette and type a query that matches parts.",
   "2. Read the results (on the 'All' tab and check the 'Parts' tab)."
  ],
  "expected": "1. No 'Parts' group appears and no part rows are shown.\n2. The user cannot see part results they are not permitted to access, even though those parts exist.\n3. Results for entity types the user CAN access still appear normally.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 9 (a technician without Parts access does not see Parts results), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 9 (technician without Parts access does not see Parts results)",
  "spec_ref": "requirements.md 9 (a technician without Parts access does not see Parts results)",
  "viu_status": "VIU-Pending",
  "notes": "Spec's explicit negative example. Requires role setup at VIU time - assign the test user a role WITHOUT Parts access, verify, then restore (Standing Rule 5). VIU-confirm whether the 'Parts' scope tab is hidden entirely or shows empty. [⚠ v2 REVIVAL 2026-08-21: SUPERSEDED for scope by GS-PERM-06 (v2 nine-entity bundle mapping). Retire/rewrite is the QA lead's call - see revival report.]",
  "api_related": false,
  "refs": "SV-9160 (PRD 9 (a technician without Parts access does not see Parts results))"
 },
 {
  "id": "GS-PERM-03",
  "area": "Permissions and Role-Based Scoping",
  "title": "A user WITHOUT Work Orders access does NOT see Work Order results",
  "priority": "High",
  "type": "Negative",
  "permissions_required": "A role WITHOUT Work Orders access.",
  "preconditions": [
   "1. You are signed in as a user whose role does NOT include Work Orders access.",
   "2. Work Orders exist that would match the query."
  ],
  "steps": [
   "1. Open the palette and type a query that matches Work Orders.",
   "2. Read the results (the 'All' tab and the 'Work Orders' tab)."
  ],
  "expected": "1. No 'Work orders' group appears and no Work Order rows are shown.\n2. Results for entity types the user CAN access still appear normally.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 9 (all result fields respect role-based-access checks), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 9 (role-based access, all entity types)",
  "spec_ref": "requirements.md 9 (all result fields respect role-based-access checks)",
  "viu_status": "VIU-Pending",
  "notes": "Second negative permission example (a different entity). Requires role setup + restore at VIU time (Standing Rule 5). [⚠ v2 REVIVAL 2026-08-21: SUPERSEDED for scope by GS-PERM-06 (v2 nine-entity bundle mapping). Retire/rewrite is the QA lead's call - see revival report.]",
  "api_related": false,
  "refs": "SV-9160 (PRD 9 (all result fields respect role-based-access checks))"
 },
 {
  "id": "GS-PERM-04",
  "area": "Permissions and Role-Based Scoping",
  "title": "Results are limited to the signed-in user's own tenant",
  "priority": "High",
  "type": "Functional",
  "permissions_required": "Any signed-in user.",
  "preconditions": [
   "1. You are signed in to one tenant/organization.",
   "2. Records with the same searchable text exist under a different tenant."
  ],
  "steps": [
   "1. Open the palette and type a query.",
   "2. Read the results."
  ],
  "expected": "1. Only records belonging to your own tenant/organization are returned.\n2. No records from any other tenant are shown (tenant isolation is respected).\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 9 (all result fields respect existing tenant-isolation checks), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 9 (tenant-isolation)",
  "spec_ref": "requirements.md 9 (all result fields respect existing tenant-isolation checks)",
  "viu_status": "VIU-Pending",
  "notes": "Cross-tenant data setup needed at VIU time. [⚠ v2 REVIVAL 2026-08-21: SUPERSEDED for scope by GS-PERM-06 (v2 nine-entity bundle mapping). Retire/rewrite is the QA lead's call - see revival report.]",
  "api_related": false,
  "refs": "SV-9160 (PRD 9 (all result fields respect existing tenant-isolation checks))"
 },
 {
  "id": "GS-PERM-05",
  "area": "Permissions and Role-Based Scoping",
  "title": "An entity type with no accessible records shows no group (and its scope tab shows the empty state)",
  "priority": "Medium",
  "type": "Functional",
  "permissions_required": "A signed-in user whose data set has zero records of at least one entity type (for example a tenant with no Part Sales yet).",
  "preconditions": [
   "1. You are signed in as a user who has zero accessible records of one entity type (for example the tenant has no Part Sales yet, or the user's role scopes an entity type down to no records).",
   "2. Other entity types do have matching records for the query."
  ],
  "steps": [
   "1. Open the palette and type a query on the 'All' tab.",
   "2. Read the grouped results and confirm the empty entity type has no group.",
   "3. Open the scope tab for the empty entity type (for example the 'Part Sales' tab)."
  ],
  "expected": "1. On the 'All' tab, no group appears for the entity type that has zero accessible results (the empty group is simply not shown).\n2. Groups for the entity types that do have matches still appear normally.\n3. The empty entity type's scope tab shows its empty / no-results treatment rather than an error.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 9 (must work for a single-tenant dataset where any entity type is empty, e.g. no part sales yet), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 9 (must work for a single-tenant dataset where any entity type is empty)",
  "spec_ref": "requirements.md 9 (must work for a single-tenant dataset where any entity type is empty, e.g. no part sales yet)",
  "viu_status": "VIU-Pending",
  "notes": "Sparse-data / empty-entity coverage. Requires a sparse-data setup at VIU time - use a tenant/user whose data set has zero records of one entity type (for example no Part Sales), or seed the environment to that state (Standing Rule 14). VIU-confirm whether the empty entity type's scope tab is hidden entirely or shows an empty-state message. [⚠ v2 REVIVAL 2026-08-21: SUPERSEDED for scope by GS-PERM-06 (v2 nine-entity bundle mapping). Retire/rewrite is the QA lead's call - see revival report.]",
  "api_related": false,
  "refs": "SV-9160 (PRD 9 (must work for a single-tenant dataset where any entity type is empty, e.g. no part sales yet))"
 },
 {
  "id": "GS-API-01",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search returns grouped results for scope=all",
  "priority": "Critical",
  "type": "Functional",
  "permissions_required": "A valid API session for a user with access to all six entity types.",
  "preconditions": [
   "1. You can call the backend search endpoint with a valid session.",
   "2. Test data exists so a query matches multiple entity types."
  ],
  "steps": [
   "1. Send GET /api/search with q set to the query and scope set to 'all'.",
   "2. Inspect the HTTP status and the response body."
  ],
  "expected": "1. The response returns HTTP 200.\n2. The body is a grouped, ranked, scored payload containing groups for the matching entity types.\n3. Each group includes the per-item metadata needed to render rows without extra fetches.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 10 Phase 1 (GET /api/search; scope=all), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 10 Phase 1 (GET /api/search); 6 (grouped, ranked, scored)",
  "spec_ref": "requirements.md 10 Phase 1 (GET /api/search; scope=all)",
  "viu_status": "VIU-Pending",
  "notes": "Response shape/field names not fully specified in the doc - VIU-confirm the exact JSON structure against the live API.",
  "api_related": true,
  "refs": "SV-9160 (PRD 10 Phase 1 (GET /api/search; scope=all))"
 },
 {
  "id": "GS-API-02",
  "area": "API - Global Search Endpoint",
  "title": "The scope parameter restricts GET /api/search to a single entity type",
  "priority": "High",
  "type": "Functional",
  "permissions_required": "A valid API session with access to the entity type.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A query matches multiple entity types."
  ],
  "steps": [
   "1. Send GET /api/search with q set and scope set to 'work_orders'.",
   "2. Inspect the response.",
   "3. Repeat with scope set to each of 'customers', 'assets', 'parts', 'vendors', 'part_sales'."
  ],
  "expected": "1. Each call returns HTTP 200.\n2. Each response contains results only for the requested scope's entity type.\n3. The accepted scope values are all, work_orders, customers, assets, parts, vendors, part_sales.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 10 Phase 1 (scope parameter), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 10 Phase 1 (scope: one of all/work_orders/customers/assets/parts/vendors/part_sales)",
  "spec_ref": "requirements.md 10 Phase 1 (scope parameter)",
  "viu_status": "VIU-Pending",
  "notes": "",
  "api_related": true,
  "refs": "SV-9160 (PRD 10 Phase 1 (scope parameter))"
 },
 {
  "id": "GS-API-03",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search caps each group at the limit (default 5) and returns per-group totals",
  "priority": "High",
  "type": "Functional",
  "permissions_required": "A valid API session with access to the entity type.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A query matches more than five records of one entity type (for example twelve Work Orders)."
  ],
  "steps": [
   "1. Send GET /api/search with q set and no limit parameter.",
   "2. Inspect the number of rows returned for the over-full group and the total field.",
   "3. Repeat with limit set to a smaller value (for example 3)."
  ],
  "expected": "1. With no limit, each group returns at most 5 rows (the default per-group cap).\n2. Each group reports its full total count (for example total 12) even though only 5 rows are returned.\n3. Setting limit changes the per-group cap accordingly.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 10 Phase 1 (limit default 5; response includes per-group totals), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 10 Phase 1 (limit: per-group cap, default 5; per-group totals)",
  "spec_ref": "requirements.md 10 Phase 1 (limit default 5; response includes per-group totals)",
  "viu_status": "VIU-Pending",
  "notes": "",
  "api_related": true,
  "refs": "SV-9160 (PRD 10 Phase 1 (limit default 5; response includes per-group totals))"
 },
 {
  "id": "GS-API-04",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search matches identifier fields exactly after normalization",
  "priority": "High",
  "type": "Functional",
  "permissions_required": "A valid API session with access to Work Orders.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A Work Order 'S2-15276' exists."
  ],
  "steps": [
   "1. Send GET /api/search with q set to 'S2-15276'.",
   "2. Send GET /api/search with q set to 's215276' (no dash).",
   "3. Send GET /api/search with q set to 'S2 15276' (with a space).",
   "4. Compare the matched Work Order in each response."
  ],
  "expected": "1. All three requests return HTTP 200 and match the same Work Order 'S2-15276'.\n2. The identifier is normalized (case, diacritics, non-alphanumerics stripped) before matching.\n3. The exact-match result scores high enough to be flagged as the pinned top match (score above 0.95).\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 7 (identifier normalization); 6.2 (ID match pinned), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 6.2, 7 (normalization; ID match > 0.95)",
  "spec_ref": "requirements.md 7 (identifier normalization); 6.2 (ID match pinned)",
  "viu_status": "VIU-Pending",
  "notes": "",
  "api_related": true,
  "refs": "SV-9160 (PRD 7 (identifier normalization); 6.2 (ID match pinned))"
 },
 {
  "id": "GS-API-05",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search does NOT apply fuzzy matching to identifier fields",
  "priority": "High",
  "type": "Negative",
  "permissions_required": "A valid API session with access to Work Orders.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A Work Order 'S2-15276' exists and 'S2-15286' does not."
  ],
  "steps": [
   "1. Send GET /api/search with q set to 'S2-15286' (one digit off a real WO number).",
   "2. Inspect the response for a Work Order match."
  ],
  "expected": "1. The real Work Order 'S2-15276' is NOT returned as a fuzzy identifier match.\n2. Identifier fields (VIN, WO number, P-number, part number) require exact match after normalization; fuzzy logic is bypassed for them.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 7 (What is NOT fuzzy), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 7 (exact identifier fields bypass fuzzy logic)",
  "spec_ref": "requirements.md 7 (What is NOT fuzzy)",
  "viu_status": "VIU-Pending",
  "notes": "",
  "api_related": true,
  "refs": "SV-9160 (PRD 7 (What is NOT fuzzy))"
 },
 {
  "id": "GS-API-06",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search enforces role-based access on the server (no forbidden entity results)",
  "priority": "Critical",
  "type": "Negative",
  "permissions_required": "A valid API session for a role WITHOUT Parts access.",
  "preconditions": [
   "1. You can call the backend search endpoint as a user whose role does NOT include Parts access.",
   "2. Parts exist that match the query."
  ],
  "steps": [
   "1. Send GET /api/search with q set to a query that matches parts, and scope set to 'all'.",
   "2. Inspect the response for a parts group.",
   "3. Send GET /api/search with scope set to 'parts' as the same restricted user."
  ],
  "expected": "1. The response contains no parts results for the restricted user (permission filtering is enforced server-side, not just hidden in the UI).\n2. Scoping directly to 'parts' still returns no forbidden parts (empty or a permission-appropriate response).\n3. Entity types the user is allowed to access are still returned.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 9 (results respect role-based-access checks - enforced server-side), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 9 (role-based-access checks); 10 Phase 5 (run as low-permission user)",
  "spec_ref": "requirements.md 9 (results respect role-based-access checks - enforced server-side)",
  "viu_status": "VIU-Pending",
  "notes": "Server-side enforcement is the key check (do not rely on the UI hiding it). Requires a restricted role at VIU time; restore afterward (Standing Rule 5).",
  "api_related": true,
  "refs": "SV-9160 (PRD 9 (results respect role-based-access checks - enforced server-side))"
 },
 {
  "id": "GS-API-07",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search returns an error response the UI can degrade on",
  "priority": "Medium",
  "type": "Negative",
  "permissions_required": "A valid API session.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. You can force an error condition (for example an invalid scope value, or a backend/search-infra failure)."
  ],
  "steps": [
   "1. Send GET /api/search with an invalid parameter (for example an unknown scope value).",
   "2. Inspect the HTTP status and error body.",
   "3. Separately, simulate a backend/search failure and observe the response."
  ],
  "expected": "1. An invalid request returns an appropriate 4xx status with an error body.\n2. A backend failure returns a 5xx or error payload that the frontend maps to the inline 'Search unavailable, retry' banner (see GS-ERR-01).\n3. The endpoint fails gracefully rather than returning malformed data.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 8 (offline/network failure handling), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 8 (tolerate failures -> 'Search unavailable, retry')",
  "spec_ref": "requirements.md 8 (offline/network failure handling)",
  "viu_status": "VIU-Pending",
  "notes": "Exact error status codes/shapes are not specified in the doc - VIU-confirm against the live API.",
  "api_related": true,
  "refs": "SV-9160 (PRD 8 (offline/network failure handling))"
 },
 {
  "id": "GS-API-08",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search groups results in the fixed group order with ranking metadata",
  "priority": "Medium",
  "type": "Functional",
  "permissions_required": "A valid API session with access to all six entity types.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A query matches all six entity types."
  ],
  "steps": [
   "1. Send GET /api/search with q set and scope set to 'all'.",
   "2. Inspect the order of the groups and the per-item score/rank fields in the response."
  ],
  "expected": "1. Groups are returned in the v2 nine-entity order: Work Orders, Customers, Contacts, Assets, Parts, Vendors, Part Sales, Purchase Orders, Vendor Invoices (D16).\n2. Within each group items are ordered by score descending; ties break by recency.\n3. Each item carries the metadata needed to render its row (entity type, primary/secondary fields, badges/quantity, score) without extra fetches, and true per-group totals.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 6.2 (group order); 10 Phase 1 (per-item metadata), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 6.1, 6.2, 10 Phase 1 (grouped, ranked, scored payload; group order)",
  "spec_ref": "requirements.md 6.2 (group order); 10 Phase 1 (per-item metadata)",
  "viu_status": "VIU-Pending",
  "notes": " [v2 CONTENT UPDATED 2026-08-21 to the current authoritative design/tech-plan.]",
  "api_related": true,
  "refs": "SV-9160 (PRD 6.2 (group order); 10 Phase 1 (per-item metadata))"
 },
 {
  "id": "GS-API-09",
  "area": "API - Global Search Endpoint",
  "title": "GET /api/search supports cursor pagination for a scoped tab",
  "priority": "Medium",
  "type": "Functional",
  "permissions_required": "A valid API session with access to the entity type.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A query matches many records of one entity type (more than one page)."
  ],
  "steps": [
   "1. Send GET /api/search with q set, scope set to a single entity type, and a limit.",
   "2. Take the cursor from the response and send GET /api/search again with that cursor.",
   "3. Compare the two pages of results."
  ],
  "expected": "1. The first response returns the first page plus a cursor.\n2. Passing the cursor returns the next page of scoped results (no duplicates, no gaps).\n3. Pagination applies to the scoped-tab view.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 10 Phase 1 (cursor parameter), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 10 Phase 1 (cursor: scoped-tab pagination)",
  "spec_ref": "requirements.md 10 Phase 1 (cursor parameter)",
  "viu_status": "VIU-Pending",
  "notes": "",
  "api_related": true,
  "refs": "SV-9160 (PRD 10 Phase 1 (cursor parameter))"
 },
 {
  "id": "GS-API-10",
  "area": "API - Global Search Endpoint",
  "title": "The context parameter biases GET /api/search ranking to the current page",
  "priority": "Medium",
  "type": "Functional",
  "permissions_required": "A valid API session with access to the entity types.",
  "preconditions": [
   "1. You can call the backend search endpoint.",
   "2. A specific customer owns some assets/work orders; other customers have similar matches."
  ],
  "steps": [
   "1. Send GET /api/search with q set and no context, and note the ranking of assets/work orders.",
   "2. Send GET /api/search with q set and context set to {type: customer, id: [that customer]}.",
   "3. Compare the rankings."
  ],
  "expected": "1. With the customer context, assets and work orders owned by that customer are boosted in the ranking versus the no-context call.\n2. The context parameter takes an optional {type, id} for page-context bias.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 6.3 (contextual bias); 10 Phase 1 (context parameter), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 6.3, 10 Phase 1 (context optional {type,id} for page-context bias)",
  "spec_ref": "requirements.md 6.3 (contextual bias); 10 Phase 1 (context parameter)",
  "viu_status": "VIU-Pending",
  "notes": "",
  "api_related": true,
  "refs": "SV-9160 (PRD 6.3 (contextual bias); 10 Phase 1 (context parameter))"
 },
 {
  "id": "GS-API-11",
  "area": "API - Global Search Endpoint",
  "title": "Recent-entities endpoints record and return recent activity",
  "priority": "Medium",
  "type": "Functional",
  "permissions_required": "A valid API session.",
  "preconditions": [
   "1. You can call the recent-entities endpoints with a valid session."
  ],
  "steps": [
   "1. Send POST /user/recent-entities/touch for a record (for example a work order you opened).",
   "2. Send GET /user/recent-entities.",
   "3. Inspect the returned list and its time bucketing."
  ],
  "expected": "1. POST /user/recent-entities/touch records the touch (HTTP 2xx).\n2. GET /user/recent-entities returns the recently touched records, retained for 30 days.\n3. The results support bucketing into Today / Yesterday / Past week / Past 30 days at render time.\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 8 (persist recent-entity-views 30 days); 10 Phase 4, read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 10 Phase 4 (GET /user/recent-entities; POST /user/recent-entities/touch)",
  "spec_ref": "requirements.md 8 (persist recent-entity-views 30 days); 10 Phase 4",
  "viu_status": "VIU-Pending",
  "notes": "VIU-confirm the exact bucketing logic (server vs client-side at render time).",
  "api_related": true,
  "refs": "SV-9160 (PRD 8 (persist recent-entity-views 30 days); 10 Phase 4)"
 },
 {
  "id": "GS-API-12",
  "area": "API - Global Search Endpoint",
  "title": "Search input is debounced before the backend is queried",
  "priority": "Low",
  "type": "Functional",
  "permissions_required": "Any signed-in user.",
  "preconditions": [
   "1. The palette is open.",
   "2. You can observe outgoing search requests (for example via browser network tools)."
  ],
  "steps": [
   "1. Type a multi-character query quickly without pausing.",
   "2. Observe how many GET /api/search calls are sent while typing.",
   "3. Pause typing and observe."
  ],
  "expected": "1. The backend is not queried on every keystroke; input is debounced (about 150ms) so a request fires after a short pause.\n2. Grouped results render quickly once the response arrives (target within about 200ms at p95).\n\n---\nThis is the expected behaviour as per the Global Search PRD (Confluence page 576978945), section 8 (debounce input at 150ms; render within 200ms p95), read on 16 July 2026, and epic SV-9160, read on 21 August 2026.\n\nAUTOMATION: Not available on Build to test Yet - Last checked 8/21/2026\n",
  "design_ref": "requirements.md 8 (debounce input at 150ms); 9 (p95 latency)",
  "spec_ref": "requirements.md 8 (debounce input at 150ms; render within 200ms p95)",
  "viu_status": "VIU-Pending",
  "notes": "Debounce is a frontend behavior observed via the network calls to the API endpoint - kept in the API section because it is verified by watching GET /api/search traffic. Exact timing VIU-confirm.",
  "api_related": true,
  "refs": "SV-9160 (PRD 8 (debounce input at 150ms; render within 200ms p95))"
 }
]