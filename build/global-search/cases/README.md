# Global Search — Test Cases (authoring PENDING)

Test-case authoring has **NOT started** for Global Search.

Authoring is deferred until:
1. Design capture is complete — **DONE: 10 of 10 Figma screenshots captured**
   (see `../design-notes.md`; 2 states are OUT OF SCOPE: AI search-all, header
   component proposal).
2. The user confirms the onboarding open questions in `../requirements.md` §11 —
   notably **PO name**, **canonical spec URL**, and the **AI / "ask a question"**
   scope question (OQ-3).
3. The user confirms which process(es) to run per Standing Rule 11.

When authoring begins, cases go here as per-case JSON (Global Search prefix, e.g.
`GS-<AREA>-NN`) and are mapped in `../testrail-id-map.csv`. Do NOT author cases for
the two out-of-scope states. Per Standing Rule 6, nothing is pushed to TestRail
without explicit user permission.
