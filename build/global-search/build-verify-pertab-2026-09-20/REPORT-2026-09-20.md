# Build verification — Global Search: per-tab prefix ranking (C72120 · C72121 · C72122)
## sv9160.qa.shopview.com build `v26.36.8-d146c39`, 2026-09-20

**Scope:** 3 cases in *Ranking and Prioritization* (section 6726): **C72120** (Parts tab), **C72121**
(Vendors tab), **C72122** (Assets tab). All `created_by=3`, none Automated (`custom_atmstatus=1`), none
Vladimir's. **Source:** PRD v1.5 (Confluence 576978945) §6.1 and §4 (a prefix match on the primary name
scores +0.70 vs +0.50 for a whole-word match). **Stage 1 seed-verify: GREEN** (14/14 records, each case
returns exactly its three rows). Build marker read live `v26.36.8-d146c39`.

Each case seeds three records matching one keyword three ways — (A) name begins with it, (B) name
contains it mid-string, (C) typo — identical in every other respect, so match strength alone decides order.

### Build-verify deliverables (Rules 54/102, §J)
- `<br>` removed from preconds/steps → `<p>` blocks; served + stored render `fr-view` clean (3/3).
- Build-stamped "Last checked against build v26.36.8-d146c39 on 20 September 2026."; provisional line removed.
- **C72120 carries the EXPECT-FAIL treatment** (symptom + three outcomes) and marker
  `AUTOMATION: READY - EXPECT FAIL (SV-10279)`; C72121/C72122 stay `AUTOMATION: READY`. Expected text
  unchanged on all three (Rule 57).
- Gates: runnable 3/3, precondition-label clean, render clean.

### Table 1 — DONE (match label observed live on `v26.36.8-d146c39`; the label is the reliable check, §4)
| Case | Keyword / tab | Begins-with (A) | Contains (B) | Typo (C) | Verdict |
|---|---|---|---|---|---|
| **C72120** | `ZZKRYPTON` / Parts | `ZZKRYPTON Brake Kit` — **`word`** 🔴 | `Heavy Duty ZZKRYPTON Filter` — `word` | `ZZKRYPTOM Wheel Seal` — `fuzzy` | **A and B tie (both `word`) → the prefix is not credited on Parts → EXPECT FAIL against SV-10279, confirmed live** |
| **C72121** | `ZZMAGENTA` / Vendors | `ZZMAGENTA Supply Co` — **`prefix`** ✅ | `Northgate ZZMAGENTA Parts` — `word` | `ZZMAGENTO Traders` — `fuzzy` | prefix credited above whole-word → matches expected |
| **C72122** | `ZZOBSIDIAN` / Assets | `2019 ZZOBSIDIAN Trucks Hauler` — **`prefix`** (field=make) ✅ | `2019 Western Heavy ZZOBSIDIAN Hauler` — `word` (field=model) | `2019 ZZOBSIDIAM Trucks Hauler` — `fuzzy` | prefix credited above whole-word → matches expected |
| Gates | | runnable 3/3 · precond-label clean · stored+served render `fr-view` 3/3 | | | |

### Table 2 — LEFT (run session records the result in R415)
| Case | How it is judged |
|---|---|
| C72120 | **FAILED against SV-10279** — read the match LABEL, not row order (A and B tie, so order is a recency artefact that can flip). Do not raise a new defect; do not rewrite Expected. |
| C72121, C72122 | Should PASS — begins-with row ranks above contains, prefix credited. |

### Table 3 — BLOCKED
None. All 3 runnable, seeded, observed live.

### Table 4 — 🔴 C72120 EXPECT-FAIL — the whole point
SV-10279 (open): the Parts tab does not credit a begins-with match — it scores it as `word`, the same as
a contains match, so A and B tie and their order falls to recency. The case's marker is
`AUTOMATION: READY - EXPECT FAIL (SV-10279)` and it carries the three outcomes: (1) A/B tie by label →
FAILED against SV-10279, raise nothing new; (2) fails differently → new problem, report; (3) A ranks
clearly above B with a prefix credit → the fix shipped, tell the QA lead. Two traps the seed-verify
already cleared (part numbers kept neutral so the description is what matches; make/model indexed
separately) are in the seed-verify doc §3.

### Table 5 — HANDOFF-READY
**YES.** All 3 runnable, label-clean, render `fr-view`, stamped `v26.36.8-d146c39`. Match labels observed
live and match the Stage-1 seed-verify exactly: C72120 fails against SV-10279 (Parts scores begins-with as
`word`); C72121 and C72122 pass (prefix credited). Run **R415** holds all three.

### R415 run-case links
- C72120 → https://shopview.testrail.io/index.php?/tests/view/3088559
- C72121 → https://shopview.testrail.io/index.php?/tests/view/3088560
- C72122 → https://shopview.testrail.io/index.php?/tests/view/3088561
