# Run plan — C55718–C55723, the six new Global Search cases (2026-09-18)

Handoff: `RUN-HANDOFF-6-NEW-CASES-2026-09-18.md` (uploaded by the QA lead; the copy on
`origin/claude/slack-session-0sxnd9`). Seed note recovered from
`origin/claude/global-search-v1-baseline-6ax9ul` and checked out here, together with the seeding
tooling this branch was missing (`status.py`, `apply_ranking_signals.py`, `build_ranking_manifest.py`
and their manifests).

**Rule 111 — the handoff is a work list, not a standard.** Its cases get run and recorded; anything it
says about ticket shape, screenshots or filing is overridden by my own rules, silently, and named here.
Nothing in it asked for a different pattern, so there is nothing to override beyond the general point.

## What is already confirmed without the branch

| Check | Result |
|---|---|
| All six exist in TestRail | yes — C55718–C55721 in section 6734, C55722–C55723 in 6726 |
| Authored by us (`created_by = 3`) | yes, all six |
| Flagged Automated (Rule 71) | no, all six are `custom_atmstatus: 1`, `custom_automation_type: 2` |
| Already in run 415 | **yes, all six** — the run is now **186** tests (153 passed · 15 failed · 11 retest · 0 blocked · **7 untested**: the six plus C45159). No run sync is needed. |
| Their Expected agrees with the requirements | yes, against the **17 September** live read (Confluence 576978945 v17) held in `build/global-search/source-verify-2026-09-17/`. §4 requires every result to respect role-based access; §6.1 gives customers +0.20 for having an open work order plus up to +0.15 log-scaled on the count, and +0.10 for a primary-name match over a secondary-field match. **Not re-read today** — Rule 81 gates that on the QA lead's go-ahead. |

## Blocked on one thing only: a session for sv9160

`build/global-search/run415-execution/six-new-cases/ACCESS-BLOCKER-CLAIM-2026-09-18.json` — passes
`blocker_gate.py`, 7 of 7 proofs.

The **DEV MODE quick-login panel is gone** from `https://sv9160.qa.shopview.com/login`; the page is
now **Sign in with Google**, and driving it reaches a **CAPTCHA**. Quick-login is refused at the API
too — `GET /api/quick-login/users`, `POST /api/quick-login` and `GET /api/api/sso/check` all return
**HTTP 401 `{"error":"sso_required"}`**. The same harness signed in on this branch last night, and
the bridge is healthy (`ensure_bridge.sh` → OK; Google's own page loads through it), so this is the
branch, not the tooling. Playbook §A records the root cause and the remedy: **cookies for a QA branch
are minted only by the QA lead.**

**What unblocks it:** a fresh `sv_sso_session` for `sv9160` into `/tmp/qa-cookies/sv9160-sso.txt`
(`chmod 600`, `/tmp` only, never committed — Rule 82).

**What it does NOT block (Rule 68):** everything above; reading and reconciling the cases; the
readiness report; the Jira work; and the run's existing 180 results, which are unaffected.

## The moment the cookie lands — the order of work

1. `cd build/global-search/seeding && python3 status.py` — is the data still there? Marker must read
   `v26.36.7-29ca209`; if it has moved, `./reseed_everything.sh qa`, then allow indexing time.
2. `python3 verify_ranking.py` — the ordering controls, before judging any ranking case.
3. **C55722** `ZZOPENCOUNT` → in Customers, *Freight Busy* (5 open) above *Freight Quiet* (1 open).
   Assert the RULE, never a position.
4. **C55723** `ZZNAMEBONUS` → *ZZNAMEBONUS Cartage* (name match) above *Sterling Brothers Freight*
   (address match only).
5. The four permission cases, each as the reduced role, with the role read back after switching:
   **C55718** `ZZAUTOTEST No Work Orders View` + `S2-15430` · **C55719** `ZZAUTOTEST No Customers View`
   + `(264) 400-0199` · **C55720** `ZZAUTOTEST No Work Orders Or Vendors` + `Fib` (confirm the
   combined role exists first — the handoff flags it) · **C55721** `ZZAUTOTEST No Parts View` +
   `Altenator`.
   Each needs its **positive side as admin** as the control (Rule 104: a "nothing appeared" result is
   inadmissible without one).
6. Results into run 415 the moment each section finishes — Passed and Blocked with no permission,
   a Failed recorded with the ticket held (Rule 113).

## Already answered by the seeding note

It independently reproduces **C55707** (prefix ranked no higher than whole-word) and confirms the data
is sound. That finding was raised this morning as **SV-10211** on the QA lead's go-ahead, so no second
report is needed.
