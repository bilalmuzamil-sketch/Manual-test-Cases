# Run 415 close-out — every test now carries a result

**Date** 21 September 2026 · **Branch** `sv9160` · **Build marker read live `v26.36.8-d146c39`** ·
Run **415** → https://shopview.testrail.io/index.php?/runs/view/415

**Before:** 179 Passed · 12 Failed · 8 Blocked · 3 Retest · 1 Untested.
**After:** **179 Passed · 13 Failed · 11 Blocked · 0 Retest · 0 Untested** — 203 of 203 resolved.

---

## 1 · THE FINDING THAT CHANGED HOW FOUR OF THESE ARE RECORDED — FIVE OWNING STORIES ARE CANCELLED

Read **live from Jira on 21 September 2026**, not from `story-status/epic-children.json` (Rule 100):

| Story | Status | Resolution | Updated | What it owned |
|---|---|---|---|---|
| **SV-9167** | **OBSOLETE** | Done | 2026-09-17 | Search telemetry — `search_event` schema, impression and click logging |
| **SV-9169** | **OBSOLETE** | Done | 2026-09-17 | FE — scope tab strip with live counts, grouped results, scoped-tab load-more |
| **SV-9173** | **OBSOLETE** | Done | 2026-09-17 | FE — **contextual quick actions on result rows** |
| **SV-9306** | **OBSOLETE** | Done | 2026-09-17 | BE — **page search cutover** (WO / Inventory Parts / Customers list search) |
| **SV-9310** | **OBSOLETE** | Done | 2026-09-16 | Verify Phase 4 — modal states, rows, keyboard, persistence, quick actions |

**Rule 112 bars a Story Defect against a story that is not Ready for QA or Testing QA**, so nothing
here becomes a ticket. **Rule 57 is equally clear that a cancelled ticket is not a specification
change** — PRD page 576978945 v17 §8 still reads *"show contextual quick actions on hover for each
entity (desktop only)"*, and the v1.3 change log still reads *"Quick actions are back in v1 scope for
every entity, shown unconditionally"*. So the **specification and the delivered scope now disagree**,
and that is a product-owner question, not a defect. The Expected results are **unchanged** (Rule 114).

## 2 · WHAT WAS RECORDED, AND ON WHAT EVIDENCE

### Blocked — quick actions, 8 cases (C44866 · C44867 · C44868 · C44869 · C44870 · C44871 · C44872 · C44873)

Measured before the story status was known, so the observation stands on its own:

- **40 row-hovers**, two rounds × two queries (`ZZAUTOTEST Fibridge` 27 rows, `a` 96 rows), covering
  work orders, customers, assets, parts and vendors — **no quick action on any row**.
- A second pass watched the **whole document**, not just the row, before and after each of 12 hovers
  (`button`, `[role=button]`, `[class*="action"]`, `[class*="quick"]`) — **nothing added anywhere**.
- The **keyboard** path checked too (ArrowDown) — nothing added.
- **Positive controls:** every hover DID change the row's class/innerHTML, and the same scan already
  saw **23 buttons** elsewhere on the page. The instrument can see buttons and hover registers.
- **No navigation, no new tab, panel stayed open** (`hover3.log`: url and tab count unchanged).

### Blocked — C44896, list search served by the unified engine
Owning story **SV-9306 OBSOLETE**. Observed alongside: the work orders list page still fires **its
own** `page_search` event, which global search never fires — the two paths are separate.

### Blocked — C45140, telemetry out of v1 scope
Its own Expected says *"Do not execute this against the v1 build"*; **SV-9167 OBSOLETE** confirms it.
Recorded Blocked rather than left Untested so the run does not read as unfinished.

### Blocked — C44826, clicking 'Show all N'
**Cannot be executed:** the link the first step asks you to click is not in the panel. Searched with a
wide net (*show all · see all · view all · more · N more*) — only unrelated controls on the page
behind. The scope tabs themselves are present and work, which is the positive control. **Blocked by
the same missing link as C44825, which is already Failed** — and it blocks nothing else (Rule 68).

### Failed — C45160, selecting a result records a usage analytics event
Selecting a result sends **no usage event at all** — only an ordinary `page_view` for the record that
opens. Two independent runs agree. **Positive control:** on the same branch, in the same session, the
work orders list page fires a named **`page_search`** event, so a named custom event of this kind
*would* have been seen. This is a **V1 regression** (invariant INV-48, `global_search_use` fired from
`GlobalSearch.vue:211-217` at commit 55767168). **Ticket written and HELD** at the Jira button
(Rules 62/113).

## 3 · ONE TOOL NOTE

`push_results_to_run.py` refuses non-Passed results with *"A shared run takes Passed only unless the
QA lead expressly lifts that"*. **Rule 113 (QA lead, 2026-09-16) has since lifted it** — Passed and
Blocked go in with no permission, and a Failed is recorded immediately with `ticket_held`. The guard
text predates the rule; `--allow-non-passed` was used, and the message should be updated to cite
Rule 113 so the next session is not stopped by a stale gate.

## 4 · EVIDENCE IN THIS FOLDER

`hover.log` · `hover2.log` · `hover3.log` · `ga.log` · `retests.log` · `retests2.log` ·
`RESULTS.json` · pictures `hover-a.png`, `hover-ZZAUTOTESTFibridge.png`, `hover-whole-panel.png`,
`c44826-scoped-tab.png`, `c44826-panel-all.png`.
