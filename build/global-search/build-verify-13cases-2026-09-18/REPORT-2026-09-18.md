# Build verification — Global Search: 13 cases C55718–C55730
## sv9160.qa.shopview.com build `v26.36.8-d146c39`, 2026-09-18 (re-verified after redeploy)

**Scope:** the 13 cases from the 13-new-cases handoff — Permissions & scoping C55718–C55723 (6734, 6726)
and Search algorithm C55724–C55730 (6726, 6725). All `created_by=3`, none Automated
(`custom_atmstatus=1`), type Functional, none Vladimir's. **Source:** PRD v1.5 (Confluence 576978945),
unchanged.

**Re-verification note (Rule 91/101):** these 13 were first build-verified earlier today on
`v26.36.7-069b8c2`. The branch has since redeployed twice (`069b8c2 → d146c39`). All 13 were
**re-observed live on `v26.36.8-d146c39`** and the build line re-stamped `069b8c2 → d146c39`. Seed data
confirmed present on `d146c39` (status.py: all universes PRESENT, roles 7/7; every keyword returned
records in this pass).

### Build-verify deliverables (Rules 54/102, §J)
- `<br>` removed from every precondition/step → `<p>` blocks; served container `fr-view`.
- Build stamp on every case: "Last checked against build v26.36.8-d146c39 on 18 September 2026."
- `AUTOMATION: READY` last on every case; metadata unchanged. Gates all clear (below).

### Table 1 — DONE (observed live on `v26.36.8-d146c39`)
| Case | Observed | Verdict |
|---|---|---|
| **C55722** more open WOs ranks higher | `ZZOPENCOUNT`: Customers group `ZZOPENCOUNT Freight Busy` above `ZZOPENCOUNT Freight Quiet` | matches expected |
| **C55723** name beats secondary-field | `ZZNAMEBONUS`: `ZZNAMEBONUS Cartage` (name) above `Sterling Brothers Freight` (address only) | matches expected |
| **C55724** prefix > whole-word > typo | `ZZPREFIX`: `ZZPREFIX Freight Ltd` (prefix) → `Bolton ZZPREFIX Services` (whole-word) → `ZZPREFIY Cartage` (typo) | matches expected — the handoff's earlier "expected to fail" warning does NOT reproduce on this build |
| **C55725** unrelated query → nothing | `Zqwxpol` → No results; control `Aabridge` → 62 results | matches expected |
| **C55726** accent-insensitive | `ZZACC Jose Martinez` and `ZZACC José Martínez` each → the one customer `ZZACC José Martínez` | matches expected |
| **C55727** dash/apostrophe optional | `ZZPUNC OBrien` & `ZZPUNC Smith Jones` → 1 each | matches expected |
| **C55729** exact-id pin beats name match | `S2-15430`: WO `S2-15430 Continental Fleet Services` pinned as the single top row above the Customers group holding `S2-15430 Holdings` | matches expected |
| **C55730** top-20 cap + narrowing | `ZZBROAD`: Parts tab exactly 20 rows, `ZZBROAD Target Widget` absent; `ZZBROAD Target` → 1 row, `ZZBROAD Target Widget` (ZZBROAD-4999) surfaces | matches expected |
| **C55718–C55721** permission positive sides | `S2-15430` WO found · `2643286723` company via contact · `Fib` → all 8 groups · `Altenator` → Parts `≈close match` | positives confirmed; negatives are tester reduced-role runs |
| Gates | Runnable 13/13 · precond-label clean · stored render clean · served render `fr-view` 13/13 | check_* + served_scan |

### Table 2 — LEFT (tester-run — seed-and-observe by design)
| Item | How it is finished |
|---|---|
| Permission NEGATIVE sides (C55718/19/20/21) | Sign in as the seeded `ZZAUTOTEST No …` role (7/7 present) and confirm the hidden record/area does not appear. Roles named in the run handoff. |
| C55720 combined role | `ZZAUTOTEST No Work Orders Or Vendors` — confirm present on the branch (status.py roles list). |
| Pass/fail recording in R415 | Run session's lane. |

### Table 3 — 🔴 C55728 — CORRECTED (was wrongly flagged as a deviation on the earlier pass)
The earlier `069b8c2` pass used the search term **`Olternaytor`** and reported the part being returned as
a candidate deviation. **That was wrong.** The case's own step 1 excludes a **close spelling**, and
`Olternaytor` is a close-spelling typo of "Alternator" — which the case's Expected explicitly says
**would still match** (ordinary typo tolerance). So the part returning was correct behaviour, not a defect.

**Corrected observation on `d146c39`** with a genuine sound-alike (per the seeding session's §2A):
- **Control `Knight`** → returns the seeded customer `ZZPHON Knight Haulage` and the part (Knight Bracket). Control good.
- **`Nyte`** → **No results.** **`Nait`** → **No results.** A genuine sound-alike surfaces nothing at all.

**Finding (for the run session → PO, Rules 57/58):** no observable phonetic/sound-alike matching on this
build — only fuzzy edit-distance. C55728 asserts a NEGATIVE (the part must not come back), so it will
read as **PASS** — but the pass must be **qualified**: the control cannot demonstrate that phonetic
name-matching exists, so the pass is not proof the feature works. The missing name-side behaviour is a
**PO decision candidate**; the Expected is kept intact (not rewritten). The case body is unchanged
except the build re-stamp.

### Table 4 — HOW TO UNBLOCK
- C55728: run with `Nyte` (or `Nait`), control on `Knight` first; qualify the PASS in the R415 comment and raise the missing phonetic capability to the PO. Never use `Olternaytor`.

### Table 5 — HANDOFF-READY
**YES.** All 13 runnable (13/13), precond-label-clean, stored + served render `fr-view`, re-stamped
`v26.36.8-d146c39`, marker READY, metadata intact. Eight behaviours observed to match expected live on
the current build; four permission positives observed (negatives tester-run); C55728 corrected and its
qualified-PASS + PO candidate documented. Run **R415** holds these cases. A run handoff accompanies this report.
