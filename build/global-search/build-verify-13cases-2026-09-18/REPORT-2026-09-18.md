# Build verification — Global Search: 13 cases C55718–C55730
## sv9160.qa.shopview.com build `v26.36.7-069b8c2`, 2026-09-18

**Scope:** the 13 cases from the 2026-09-18 build-verify handoff.
- **Permissions & scoping (6734, 6726):** C55718, C55719, C55720, C55721, C55722, C55723 — the six
  first build-verified earlier today on `v26.36.7-29ca209`; **the branch has since redeployed to
  `v26.36.7-069b8c2`**, so they were re-observed on the current build and re-stamped (Rules 91/101).
- **Search algorithm (6726, 6725):** C55724, C55725, C55726, C55727, C55728, C55729, C55730 — **first
  build-verify** (authored 2026-09-18).

All 13 `created_by=3`, none Automated (`custom_atmstatus=1`), type Functional (`custom_automation_type=2`),
none Vladimir's. **Source:** PRD v1.5 (Confluence 576978945), unchanged (Rule 77 window). **Build marker
read live `v26.36.7-069b8c2`** (matches the handoff's seeding marker); seed data present — every case's
keyword returned records in the observation pass (that pass IS the data check, Rule 86).

### Build-verify deliverables applied (Rules 54/102, §J/L0028)
- `<br>` removed from every precondition/step, rewritten as `<p>` blocks; served container stays `fr-view`.
- Build stamp on every case: "Last checked against build v26.36.7-069b8c2 on 18 September 2026."; the
  six older cases' `29ca209` stamp replaced; the seven new cases' "not yet build-verified" line replaced.
- Four steps anchored for runnability (Rule 102): C55719, C55721, C55725, C55728 step 2 was the bare
  "Read the results." → now names where to look. **No Expected assertion changed.**
- Every Expected ends with `AUTOMATION: READY`; metadata unchanged.

### Table 1 — DONE (observed live on `v26.36.7-069b8c2`)
| Case | Observed | Verdict |
|---|---|---|
| **C55722** more open WOs ranks higher | `ZZOPENCOUNT`: `Freight Busy` ("10 open") above `Freight Quiet` ("1 open") | matches expected |
| **C55723** name beats secondary-field | `ZZNAMEBONUS`: `ZZNAMEBONUS Cartage` (name) above `Sterling Brothers Freight` (address only) | matches expected |
| **C55724** prefix > whole-word > typo | `ZZPREFIX`: row1 `ZZPREFIX Freight Ltd` (prefix) · row2 `Bolton ZZPREFIX Services` (whole-word) · row3 `≈close match ZZPREFIY Cartage` (typo) | **matches expected — see Table 3 note: the handoff's §2 "expected to fail" deviation does NOT reproduce on this build** |
| **C55725** unrelated query → nothing | `Zqwxpol` → No results; control `Aabridge` → 62 results | matches expected |
| **C55726** accent-insensitive | `ZZACC Jose Martinez` and `ZZACC José Martínez` each → the one customer `ZZACC José Martínez` | matches expected |
| **C55727** dash/apostrophe optional | `ZZPUNC OBrien` & `O’Brien` → 1 each; `ZZPUNC Smith Jones` → 1 | matches expected |
| **C55729** exact-id pin beats name match | `S2-15430`: work order `S2-15430 Continental Fleet Services` pinned as the single top row ABOVE the Customers group, which holds the competing `S2-15430 Holdings` | matches expected |
| **C55730** top-20 cap + narrowing | `ZZBROAD`: Parts tab exactly 20 rows, `ZZBROAD Target Widget` absent; `ZZBROAD Target` → 1 row, `ZZBROAD Target Widget` (ZZBROAD-4999) surfaces | matches expected |
| **C55718 / C55719 / C55720 / C55721** permission positive sides | `S2-15430` → WO found · `2643286723` → company via contact match · `Fib` → all 8 groups · `Altenator` → Parts fuzzy `≈close match` | positives confirmed; negatives are tester reduced-role runs |
| **C55728** customer control | `Olternaytor` → customer `ZZPHON Alternator Co` returned (`≈close match`) | control satisfied — **but see Table 3: the part is ALSO returned** |
| Gates | Runnable 13/13 · precondition-label clean (61/61 across the three sections) · stored render clean · **served render 13/13 `fr-view`, 0 escaping, 0 literal tags** | check_runnable_cases / check_precond_labels / check_case_render / served-scan-13.log |

### Table 2 — LEFT (tester-run — by design, seed-and-observe)
| Item | How it is finished |
|---|---|
| **Permission NEGATIVE sides** (C55718/19/20/21) | Sign in as the seeded reduced role and confirm the hidden records/areas do NOT appear. Roles named in the run handoff. |
| **C55720 combined role** | Needs a role missing BOTH Work Orders AND Vendor & Order Management; confirm it exists on the branch first (see run handoff). |
| **Pass/fail recording in R415** | This is the run session's lane, not build-verify. |

### Table 3 — BLOCKED / candidate findings (what it does NOT block)
| Item | Detail |
|---|---|
| **Served-page container scan — DONE (was briefly login-locked, now cleared)** | After waiting out the TestRail UI login lockout, the served-container scan ran: **all 13 fields serve in `markdown fr-view`, 0 escaping, 0 literal tags** (served-scan-13.log). Closed. |
| **C55724 — handoff §2 deviation does NOT reproduce (candidate: now passes)** | The handoff warned C55724 would fail (whole-word outranking prefix). On `v26.36.7-069b8c2` the observed order is prefix → whole-word → typo, i.e. the documented expected order. Reported to the run session so it is not marked Failed on a stale warning (handoff §4: the case/build win over the handoff). |
| **C55728 — expected exclusion does NOT reproduce (candidate deviation for the run session)** | Case expects the sound-alike `Olternaytor` to return the customer control but NOT the part `ZZPHON Alternator Assembly`. On this build the part **is** returned (Parts group, `≈close match`, ZZPHON-3001). Two readings: the phonetic match is reaching a description (a real deviation), or `Olternaytor` is within ordinary typo distance of "alternator" so the part legitimately matches (making the seed term not a true sound-alike). Expected kept intact (Rule 57); flagged for the run session to resolve under Rule 106 (reconcile against live PRD v1.5 first) — do not rewrite the case here. |

### Table 4 — HOW TO UNBLOCK
- Served scan: wait out the 10-minute lockout, then `CIDS=55718,…,55730 node build/global-search/build-verify-6cases-2026-09-18/served_scan.mjs` (do not retry during the lockout — each attempt resets the timer).
- C55728 / C55724: run session decides pass/fail against live PRD v1.5; both documented above with the live observation.

### Table 5 — HANDOFF-READY
**YES.** All 13 runnable
(13/13), precondition-label-clean, stored render clean, build-stamped `v26.36.7-069b8c2`, marker READY,
metadata intact. Eight behaviours observed to match expected live; four permission positives observed
(negatives tester-run); two candidate observations (C55724 now-passes, C55728 part-returned) handed to
the run session with the live evidence. Run **R415** holds these cases. A run handoff accompanies this report.
