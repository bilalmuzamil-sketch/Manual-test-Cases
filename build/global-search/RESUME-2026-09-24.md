# GLOBAL SEARCH V2 — STATE SAVED 2026-09-24 (paused by the QA lead; resume tomorrow)

**Paused on his instruction:** *"Save the state of your existing work we will get back to it later
tomorrow."* Nothing is mid-flight. Nothing is waiting on a running job. The lane is safe to leave.

## 1 · Where the run stands (read LIVE from TestRail 2026-09-24, not from memory)

Run **415** "Global Search V2 — Full Suite (2026-08-25)" — https://shopview.testrail.io/index.php?/runs/view/415

| Result | Count |
|---|---|
| Passed | **185** |
| Failed | **4** |
| Retest | **13** |
| Blocked | 0 |
| Untested | 0 |
| **Total tests** | **202** |

> Note: an earlier note in this lane recorded 184 passed / 5 failed. The live figures above supersede it
> (Rule 100 — measure, never recall). If the difference matters, diff the results, do not argue from notes.

## 2 · The epic's live shape (read 2026-09-24 via `/rest/api/3/search/jql`)

Epic **SV-9160** → **26** children → **51** grandchildren, **all 51 of type `Story Defect`** (subtasks,
so the tree ends there). `isLast: true` on both pages — nothing unpaged.

**Stories in `TESTING QA` (5):** SV-9162 · SV-9164 · SV-9165 · SV-9168 · SV-9174

**Everything under a TESTING QA story that is NOT finished — this is the resume list:**

| Story | Open item | Its status |
|---|---|---|
| SV-9162 | SV-10217 open work-order count does not match | `Ready to Fix` |
| SV-9164 | SV-10385 vendor contact near-spelling not marked fuzzy | `Open` (raised by us 23 Sep) |
| SV-9165 | SV-10340 most-recently-updated tie-break ignored | `Open` (raised by us, widened to every tab) |
| SV-9168 | SV-10068 "UI / UX Fixes" (clear button) | **`Ready for QA` — the only one waiting on US** |

**SV-9174 is the one story with nothing live outstanding** — SV-10159 and SV-10345 QA Complete,
SV-10320 and SV-10330 OBSOLETE. (Strictly read, "all QA Complete or Done" admits no story, because
OBSOLETE is neither; I treated OBSOLETE/cancelled as not blocking and said so to him.)

**Ready for QA across the whole epic: SV-10068 ONLY.** `customfield_10385` (QA Assignee) EMPTY,
assignee Nikola Milosevic, updated 22 Sep. **No case in our suite covers it** — it needs a run AND a
case written.

**Moved since 23 Sep:** SV-10181 is now `QA Complete` (was Ready for QA). C44865 already Passed it on
build `v26.36.9-a90a3f0`.

**Two further Open defects sit under SV-9170, which is already QA Complete:** SV-10439 · SV-10440.

## 3 · The next actions in this lane, in order, when it resumes

1. **SV-10068** — run the clear-button fix on the branch (two behaviours: the clear control appears only
   after typing; pressing it empties the box instead of closing the panel). Then **write the missing
   case** for it (Rule 117 shape: general instruction + example in the same step) and union-sync it into
   run 415 (Rule 34 — a partial `case_ids` list DELETES tests and their results).
2. **Chase SV-10385 and SV-10340** — both raised by us, both still `Open`, nobody assigned.
3. **Put the QA Assignee question to him for SV-10068** — it is empty and he has previously wanted
   himself named where QA is ours.

## 4 · Decisions still sitting with HIM (carry these into the next report, Rule 36/98)

| # | Item | What we need |
|---|---|---|
| D1 | Four checks whose reports were closed **OBSOLETE** | Retire the cases, or amend the requirement — his call |
| D2 | C45160 usage-recording exclusion note | Whether to add it |
| D3 | C45160 judgement call | Whether to reverse it |
| D4 | **Rule 54 vs Rule 114 collision** | Rule 54 requires re-stamping the build line, which lives INSIDE `custom_expected`, which Rule 114 bars editing. Flagged, NOT resolved. Do not silently pick a side. |
| D5 | Spec backfill (Rule 115) | Riding along per project, never a sweep — no action needed, just do not forget it |

## 5 · Standing holds that survive the pause

- **No Jira ticket without his explicit permission, per ticket** (62 / 113). Passed and Blocked run
  results need no permission; a Failed records the result and holds ONLY the Jira button.
- **No TestRail case writes without his go-ahead** (6) — case CREATION is expressly carved out (62-a).
- **Vladimir Tomovic's cases (`created_by == 1`) are never changed** (38). Automated cases held (71).
- **`custom_expected` is NEVER edited** (114) — not even with his go-ahead.
- **Secrets: `/tmp` only, `chmod 600`, never committed — this repo is public** (82).
  `python3 build/testing-tools/scan_secrets.py --staged` before every commit; exit 1 ⇒ REFUSE.

## 6 · Evidence on disk for this lane (nothing to re-derive)

- `build/global-search/retest-2026-09-23/` — the Retest-vs-Failed split and the rule applied both ways
- `build/global-search/fuzzy-indicator-sweep-2026-09-23/` — the 30-combination matrix and the PROVED section
- `build/global-search/sv10340-sinisa-2026-09-23/ASSESSMENT.md` — his ruling *"Ok leave it, do not re-open"*
- `build/global-search/sv10025-recheck-2026-09-22/` — includes the Show-all-count report he DECLINED
  (`DECLINED-showall-count-2026-09-22.txt`, banner-marked, never filed — do not file it)

## 7 · Access facts (so the next session does not re-discover them)

- Branch entry is a **cookie supplied by the QA lead** — there is no password for a QA branch. The three
  cookies in use expire overnight; when every route redirects to a Google sign-in, that is an expired
  cookie, NOT the sleeping-branch trap (the trap shows `sleep.qa.shopview.com` and a `Wake Up` button).
- `source build/testing-tools/ensure_bridge.sh` — the local relay; its port rotates into
  `/tmp/atlassian/bridge-port.txt` and it dies with the container.
- Jira MCP create/edit take MARKDOWN; only `PUT /rest/api/2/issue/{KEY}` via
  `build/atlassian-login/jira.sh` takes WIKI markup and embeds images. `customfield_10385` is a
  **multi-user array** — a bare object is rejected. `/rest/api/2/search` is gone; use
  `/rest/api/3/search/jql`.
