# Build verification — Global Search: Permissions & Role-Based Scoping (6734), 12 cases
## sv9160.qa.shopview.com build `v26.36.8-d146c39`, 2026-09-18

**Scope:** the 12 permission cases in folder **6734** per `HANDOFF-BUILD-VERIFY-PERMISSIONS-6734`:
C44877, C44878, C44879, **C44880**, C44881, C44882, C55702, C55703, C55704, C55705, C55706, C55717.
All `created_by=3`, none Automated (`custom_atmstatus=1`), none Vladimir's. **Source:** PRD v1.5
(Confluence 576978945) §9. These were previously build-verified 2026-09-17 on `v26.36.7-29ca209`;
re-verified and re-stamped on the current build `v26.36.8-d146c39` after the redeploys.

### Build-verify deliverables (Rules 54/102, §J)
- `<br>` removed from preconditions/steps → `<p>` blocks; **also repaired pre-existing malformed HTML**
  (an unclosed leading `<p>` in the Expected of several cases, and a doubled trailing `</p>`) so every
  field is now tag-balanced. **No wording changed** (Rule 57) — only the tag structure.
- Build stamp re-stamped `29ca209 → v26.36.8-d146c39` on every case; `AUTOMATION: READY` last.
- Roles confirmed present live on `d146c39` (status.py: permission role fixtures 7/7; 6 stock roles ship with the product).

### Table 1 — DONE
| Item | C-ids | Evidence |
|---|---|---|
| **Positives observed as admin** — a broad query (`Fib`) returns all 8 groups with prices for a full-access user | C44877 (Parts), C55702 (Work orders), C55703 (Customers+Assets), C55704 (Part sales), C55705 (Vendors+POs+Vendor invoices), C55706 (prices shown) | reverify observe json (`Fib` → 45 across 8 groups) |
| Role fixtures present (7 seeded + 6 stock) | all | status.py roles 7/7 |
| Runnable-shape gate | 12/12 | check_runnable_cases |
| Precondition-label gate | clean | check_precond_labels |
| Stored + served render `fr-view`, tag-balanced | 12/12 | check_case_render + served_scan |
| Build line re-stamped to `d146c39` | 12/12 | final verify |

### Table 2 — LEFT (tester-run — reduced-role, by design)
| Case | How it is finished |
|---|---|
| **C44878** (no Parts) | Sign in as Technician (or `ZZAUTOTEST No Parts View`); the Parts group is absent — no group, no count, no scope tab. |
| **C44879** (no Work Orders) | `ZZAUTOTEST No Work Orders View`; Work Orders group absent, and the WO not pinned even on an exact number (check the pin separately). |
| **C44881** (empty type) | Technician; a type with zero accessible rows shows the **empty state**, not an error — distinguish "permitted but empty" from "no permission". |
| **C44882** (bundles) | **Six sign-ins, one case** — one bundle removed at a time (Part Sales · Customers→Assets · Vendor & Order Mgmt→Vendors/POs/VIs · Financial Data→prices masked, groups stay · Time Clock→nothing). |
| **C55717** (recent list) | Open a record, remove that area's bundle, re-check the recent list — the record drops off. Two observations with a role edit between. |

### Table 3 — 🔴 BLOCKED (and what it does NOT block)
| Blocker | Detail |
|---|---|
| **C44880 — tenant isolation — NOT AVAILABLE ON BUILD** | Needs a second organisation's sign-in. `ZZAUTOTEST Second Org Ltd` exists but has no working login inside it (the org is carried by a `PHPSESSID` captured while signed into that org; the SSO token is shared). Mark **"not available on build"** — do not fake it, do not fail it. Build-verified as runnable + re-stamped; the tenant-isolation run itself waits on the second-org sign-in. |
| **Does NOT block** | the other 11 cases — all runnable, stamped, roles present. |

### Table 4 — HOW TO UNBLOCK
- C44880: a QA-lead-provided sign-in (or PHPSESSID) inside `ZZAUTOTEST Second Org Ltd`; then search a token both orgs share and confirm this org's user sees only its own.
- Reduced-role runs: reset each stock role To Template before assigning (the seeded `ZZAUTOTEST` roles are already template-clean); confirm the permission took effect (re-login) before reading.

### Table 5 — HANDOFF-READY
**11 of 12 YES; C44880 not-available (second org).** All 12 runnable, precond-label-clean, render
`fr-view` and tag-balanced, re-stamped `v26.36.8-d146c39`. The seven positives + the fixtures are
observed/present; the five reduced-role negatives are tester runs with the named roles; C44880 waits on
a second-organisation sign-in. Run **R415** holds these cases. A run handoff accompanies this report.
