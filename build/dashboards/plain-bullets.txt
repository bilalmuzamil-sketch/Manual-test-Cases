S1-R1 ::= A "Dashboard" entry appears in the top navigation.
S1-R2 ::= Selecting "Dashboard" opens the dashboard screen.
S1-R3 ::= A user with the Reports permission lands on the dashboard when they log in.
S1-R4 ::= The dashboard shows figures for the workplace currently selected in the top navigation.
S1-R5 ::= The "Dashboard" entry sits immediately to the right of the "Reports" entry.
S1-R6 ::= The "Dashboard" entry appears only when both gates are true: the user has the reportsPageAccess permission (the same one that shows "Reports") and the organization has the DashboardAdministrator feature turned on.
S1-R7 ::= The gates are enforced by the server: a request to a dashboard endpoint from a user without reportsPageAccess, or an org without DashboardAdministrator, is refused with an authorization failure and returns no dashboard data — hiding the entry is not the control.
S1-R8 ::= A user who fails either gate lands on Work Orders, both after login and when entering the dashboard address directly (it redirects to Work Orders rather than showing an error page).
S1-N1 ::= Without the Reports permission, the "Dashboard" entry is not shown and the user cannot open the dashboard.
S1-N2 ::= Without the Reports permission, the user does not land on the dashboard at login.
S1-N3 ::= If the org does not have the Dashboard feature on, the "Dashboard" entry is not shown, even to a user with the Reports permission.
S1-N4 ::= If the org does not have the Dashboard feature on, the user cannot open the dashboard by any route.
S1-N5 ::= The shop logo in the top navigation is not a link, at any screen size.
S1-N6 ::= Selecting the shop logo does nothing, at any screen size.
S1-N7 ::= On a small screen the shop logo is not part of the menu button, so tapping the logo does not open the menu.
S1-N8 ::= On a small screen the menu button has its own menu icon and opens the menu when tapped.
S1-N9 ::= Selecting the shop logo sends no analytics event.
S1-E1 ::= Switching the selected workplace in the top navigation reloads the dashboard's figures for the newly selected workplace.
S1-E2 ::= If no workplace is selected, the dashboard does not load data.
S2-R1 ::= The four KPI tiles sit in a fixed top row, left to right: Revenue, Billing Efficiency, Technician Efficiency, Technician Utilization.
S2-R2 ::= The two count tiles sit below the KPI row, left to right: Sales by Customer, At Risk Customers.
S2-R3 ::= The tile set and order are the same for every user.
S2-R4 ::= Every user who can open the dashboard sees all six tiles.
S2-N1 ::= There is no control to add a tile.
S2-N2 ::= There is no control to remove a tile.
S2-N3 ::= There is no control to reorder tiles.
S2-N4 ::= There is no control to hide or show tiles.
S2-N5 ::= No saved layout carries over between sessions.
S3-R1 ::= Every dashboard figure equals its matching report's figure for the same workplace, date range and filters, at every moment — any difference caused by one being computed more recently than the other is a defect, with no freshness allowance.
S3-R2 ::= Each measure is calculated exactly as the spec's formula table defines it (Revenue, Billing Efficiency, Technician Efficiency, Technician Utilization, Sales by Customer count, At Risk Customers count, and ELR), each against its named matching report.
S3-N1 ::= If a measure's report shows a value, the matching tile shows the same value; the two never disagree.
S3-E1 ::= Revenue can be negative when credit memos exceed sales in the range, and the dashboard shows the true negative figure rather than flooring it at zero.
S3-E2 ::= A voided invoice is excluded from every dashboard figure — Revenue, the parts/labor split, invoiced hours, invoiced tech hours, the Sales by Customer count and the At Risk count (including when deciding a customer's most recent invoice) — matching the reports.
S3-E3 ::= When a ratio measure's denominator for the range is zero (worked hours, clocked hours, or work-order+internal hours), the measure has no value and the tile headline reads exactly "n/a" (lower case, no periods) rather than "0.00%".
S4-R1 ::= Each KPI tile shows a label, a headline value, a sparkline, a date-range control, and a report link.
S4-R2 ::= The Revenue tile headline is the total revenue for the selected range, shown as a dollar amount to the cent.
S4-R3 ::= The Revenue tile's supporting line reads "Parts {parts}% · Labor {labor}%", each percent a whole number.
S4-R4 ::= The Billing Efficiency tile headline is the billing efficiency percentage for the selected range, to two decimal places.
S4-R5 ::= The Billing Efficiency tile's supporting line reads "{invoiced hours} Invoiced / {clocked hours} Clocked".
S4-R6 ::= The Technician Efficiency tile headline is the technician efficiency percentage for the selected range, to two decimal places.
S4-R7 ::= The Technician Efficiency tile's supporting line reads "{invoiced tech hours} Invoiced Tech Hrs / {clocked hours} Clocked".
S4-R8 ::= The Technician Utilization tile headline is the technician utilization percentage for the selected range, to two decimal places.
S4-R9 ::= The Technician Utilization tile's supporting line reads "{work-order hours} WO Hrs / {clocked hours} Clocked".
S4-R10 ::= The sparkline shows the tile's measure trending over the selected range.
S4-R11 ::= The sparkline draws one point per bucket, with bucket size chosen by range length (≤31 days by day; 32-182 by week; 183-731 by month; longer by quarter); each point recomputes the measure the same way as the headline; a bucket with no value contributes no point and interior gaps are preserved; the line is not drawn when fewer than two points have values.
S4-N1 ::= No KPI tile shows a delta indicator; the sparkline is the only comparison.
S4-N2 ::= If the Revenue tile has no parts-versus-labor split to show, its supporting line reads "{count} Invoices" instead.
S4-E1 ::= If the underlying report has an extreme value, the tile headline still shows the true figure to the report's precision.
S5-R1 ::= The Sales by Customer tile headline is the count of distinct customers with sales in the selected range.
S5-R2 ::= The Sales by Customer headline reads "{count} customer" when the count is one and "{count} customers" otherwise.
S5-R3 ::= The Sales by Customer tile's supporting line reads "Top: {top customer name}".
S5-R4 ::= The Sales by Customer tile has its own date-range control.
S5-R5 ::= The Sales by Customer sparkline shows the customer count over the tile's selected range, bucketed the same way as the KPI sparklines, each point the count of distinct customers with sales in that bucket, so headline and sparkline describe the same window.
S5-R6 ::= The Sales by Customer tile shows a report link to the Sales By Customer report.
S5-R7 ::= The At Risk Customers tile headline is the count of at-risk customers for the selected inactivity window, shown as a plain number.
S5-R8 ::= When exactly one customer is at risk, the At Risk supporting line reads "${revenue} (12mo) · {customer name}".
S5-R9 ::= When more than one customer is at risk, the At Risk supporting line reads "${revenue} (12mo) · {count} customers".
S5-R10 ::= The At Risk Customers tile has an inactivity-window control offering 30, 60, 90, 120 and 180 days.
S5-R11 ::= The inactivity-window control defaults to 120 days.
S5-R12 ::= Changing the inactivity window updates the At Risk headline, supporting line, sparkline and detail table for the new window.
S5-R13 ::= The At Risk sparkline shows the at-risk count over the trailing twelve months as twelve monthly points (each as of that month's last day in the workplace timezone using the current window; the twelfth point is as of now so it equals the headline), and every point is recomputed from current data so a later void changes past points.
S5-R14 ::= The inactivity window the user chose is remembered and applied on their next visit in place of the 120-day default (per the browser-storage rule).
S5-R15 ::= "Some revenue in the last twelve months" means revenue greater than $0.00 (measured as the Revenue tile does), so exactly $0.00 or a negative total fails that part (but can still qualify on two-or-more lifetime invoices); and "at least the selected number of days old" is whole calendar days in the workplace timezone from the most recent invoice to now.
S5-N1 ::= No count tile shows a delta indicator; the sparkline is the only comparison.
S5-N2 ::= The At Risk Customers tile shows no report link, because there is no At Risk report.
S5-E1 ::= If no customers had sales in the selected range, the Sales by Customer headline shows 0.
S5-E2 ::= If no customers are at risk in the selected window, the At Risk Customers headline shows 0.
S5-E3 ::= Sales on invoices with no company are counted together as a single "no company" customer in the Sales by Customer count.
S6-R1 ::= Each tile shows a "View details" control that expands the tile.
S6-R2 ::= Expanding a tile reveals that tile's detail content — its chart, its detail table, or both.
S6-R3 ::= The Revenue tile expands to the Revenue chart.
S6-R4 ::= The Billing Efficiency tile expands to the Billing Efficiency chart and the Advisor Analysis table.
S6-R5 ::= The Technician Efficiency tile expands to the Technician Efficiency chart and the Technician Efficiency table.
S6-R6 ::= The Technician Utilization tile expands to the Technician Utilization chart and the Technician Utilization table.
S6-R7 ::= The Sales by Customer tile expands to the Sales by Customer table.
S6-R8 ::= The At Risk Customers tile expands to a table of every at-risk customer, ordered by trailing-twelve-month revenue highest first (name as tiebreaker); a 500-row safety cap shows the first 500 and says "Showing 500 of N at-risk customers" while the headline still counts every one, never truncating silently.
S6-R9 ::= At most one tile's detail is open at a time.
S6-R10 ::= Expanding a tile while another's detail is open closes the previously open detail.
S6-R11 ::= Only the "View details" control expands or collapses a tile.
S6-R12 ::= Selecting the date-range or inactivity-window control on a tile does not expand the tile.
S6-R13 ::= Selecting the report link on a tile does not expand the tile.
S6-R14 ::= A tile still loading shows a placeholder in place of its headline, supporting line and sparkline while its label and controls stay readable — never a blank tile and never a stale figure from a previous range.
S6-R15 ::= Tiles load independently: a tile that cannot load shows it could not load while the other five still render, and no toast or alert is shown.
S6-R16 ::= Every remembered choice (the expanded tile, a chart's technician/advisor selection, the At Risk window, and an embedded chart's show/hide state) is stored in the browser — per browser and device, not on the account — and clearing browser data returns each to its default.
S6-N1 ::= The dashboard has no "expand all" or "collapse all" control.
S6-N2 ::= The dashboard has no Panel-versus-Inline view choice.
S6-E1 ::= On a small screen, a tile cannot be expanded in place.
S6-E2 ::= On a small screen, in place of "View details", the tile's footer is a "View Report" link that opens the tile's full report.
S6-E3 ::= On a small screen, the At Risk Customers tile shows no footer link (it has no report), and its detail table is reachable only on a desktop screen.
S7-R1 ::= Each KPI tile and the Sales by Customer tile has its own date-range control.
S7-R2 ::= The date-range control offers, in order: Last 12 Months, This Year, Last Year, This Quarter, Last Quarter, This Month, Last Month, This Week, Last Week.
S7-R3 ::= Changing a tile's date range updates that tile's headline value for the new range.
S7-R4 ::= Changing a tile's date range redraws that tile's sparkline for the new range.
S7-R5 ::= Changing a tile's date range updates that tile's expanded chart and table for the new range.
S7-R6 ::= Changing one tile's date range does not change any other tile's date range.
S7-R7 ::= The four KPI tiles open on This Month; the Sales by Customer tile opens on Last 12 Months.
S7-R8 ::= A tile's date range is not carried between visits — every visit opens each tile on its default.
S7-E1 ::= If the selected range has not finished, the tile's headline and sparkline both describe the same partial window.
S8-R1 ::= The Technician Efficiency and Technician Utilization charts each have a Technician filter.
S8-R2 ::= The Technician filter lets the user select any number of technicians and defaults to all selected.
S8-R3 ::= The Billing Efficiency chart has an Advisor filter.
S8-R4 ::= The Advisor filter lets the user select a single advisor, and shows all advisors when none is chosen (the default).
S8-R5 ::= Changing a chart filter reloads the chart scoped to the selected technicians or advisor.
S8-R6 ::= A tile's chart filter selection is remembered for that tile between visits.
S8-N1 ::= If the user deselects every technician on a technician chart, the chart is hidden rather than shown empty.
S9-R1 ::= A percentage chart's value axis fits the range of the data shown.
S9-R2 ::= When one value is far above the rest (above 200% while the pooled median across all drawn lines stays at or below 200%), the axis caps at 200%; when the highest value is at or below 200% the axis just fits the data and no cap applies.
S9-R3 ::= A value drawn above the axis cap is clipped at the top of the chart but still shows its true value on hover.
S9-R4 ::= The Billing Efficiency chart draws one billing-efficiency line per advisor.
S9-R5 ::= Hovering a point on the Billing Efficiency chart shows a tooltip with that advisor's billing efficiency percent and ELR dollar amount.
S9-R6 ::= The Billing Efficiency chart does not draw ELR as its own line or axis.
S9-R7 ::= A chart draws only the lines its story defines (one per selected technician, or one per advisor) — no shop-average, aggregate or other reference line.
S10-R1 ::= Each KPI tile and the Sales by Customer tile shows a report link.
S10-R2 ::= Selecting a tile's report link opens that tile's matching report.
S10-R3 ::= The report opens with the date range the tile currently shows.
S10-R4 ::= Hovering a tile's report link shows a tooltip naming the report the link opens.
S10-N1 ::= The At Risk Customers tile shows no report link, because there is no At Risk report.
S11-R1 ::= The report page shows its matching chart above the report's detail table.
S11-R2 ::= The report page shows a "Show chart" / "Hide chart" control for the chart.
S11-R3 ::= The embedded chart reflects the report's own active filters, including its date range and technician or advisor selection.
S11-R4 ::= The embedded chart has no date-range control of its own.
S11-R5 ::= The embedded chart has no technician or advisor filter of its own.
S11-R6 ::= The embedded chart shows no report link.
S11-R7 ::= The embedded chart shows the same figures as the report it sits on.
S11-E1 ::= The user's choice to show or hide the embedded chart is remembered per report between visits.
S12-R1 ::= On a desktop screen (1024 pixels or wider), the four KPI tiles sit in one row and the two count tiles in a row below.
S12-R2 ::= The tiles' descriptive text (labels, supporting lines, and the date-range/inactivity-window controls) is a near-black colour in the light theme.
S12-R3 ::= The same descriptive text is a near-white colour in the dark theme.
S12-R4 ::= The count tiles show their headline count in a larger size than the KPI tiles' headline.
S12-R5 ::= The dashboard renders correctly in both the light and the dark theme.
S12-N1 ::= On a small screen (viewport under 1024 pixels wide), the tiles stack in a single column.
