# Simple Flow — Spec + Design Delta & Action Doc (2026-07-13 uploads)

> **Project:** Simple Flow ONLY (Epic SV-7301, PO = Milos, app `sv7301.qa.shopview.com`).
> **New inputs (both uploaded 2026-07-13, upload dir `dd1d42ba-…`):**
> - Spec doc `1a94a925-SimpleMode_StreamlinedWorkOrderCompletionBulkReceiving_2.doc`
>   (Confluence MHTML). Extracted → `spec-source-2026-07-13.md`.
> - Design bundle `2f9d4f23-Simple_Flow_Design_3.zip` → unzipped `/tmp/simple-flow/design3/`;
>   preserved (fonts/metadata excluded) at `build/simple-flow/design3-2026-07-13/`.
>
> **Baselines compared:** `requirements.md` (our authoring baseline) ·
> `spec-source-2026-07-10.md` (the prior silent-V2.4 revision) ·
> `build/simple-flow/design2-2026-07-10/` (prior design copy) + the full
> 2026-07-10 unzip.
>
> **Scope: INGEST + DIFF + PROPOSE ONLY.** This worker did **NOT** edit
> `requirements.md` body, `cases/*.json`, `PROJECT-STATE.md`, or any TestRail
> artifact. Everything below is a **proposal** for the apply/VIU worker.

---

## 0. TL;DR verdict

| Input | Verdict | Evidence |
|---|---|---|
| **Spec doc (2026-07-13)** | **SAME content — byte-identical re-delivery of the 2026-07-10 doc** (which was itself the silent V2.4 revision) | Extracted text md5 identical (`23442085…3612`) to `b91efa39`(Jul-10); `diff` = 0 lines; raw `.doc` md5 differs only because the outer MHTML export wrapper was re-generated (same 115 157 bytes) |
| **Design bundle (2026-07-13)** | **SAME — byte-identical re-delivery** (3rd time) | `diff -rq` of the full unzip vs the 2026-07-10 bundle = exit 0, ZERO differences (134 files each) |
| **4 prior deltas Δ1–Δ4** | **ALL still present, ALL unchanged** | verbatim in the extract — see §A |
| **NEW deltas beyond Δ1–Δ4** | **NONE** | line-for-line identical to Jul-10 |
| **Round-3 PO questions resolved** | **NONE newly resolved** | §C — §8 Open Questions verbatim identical; nothing new |

**Bottom line:** the 2026-07-13 uploads carry **no new information**. The spec is a
re-export of the same V2.4 silent revision already diffed on 2026-07-10; the design
is the same set delivered for a third time. The action list in §D is therefore
**unchanged from `spec-diff-2026-07-10.md`** — Δ1–Δ4 remain the only outstanding
spec deltas to apply, and the full Milos Round-3 sheet still stands unanswered.

---

## A. Full unapplied delta list (Δ1–Δ4 confirmed) + NEW deltas

The new doc is line-for-line identical to `spec-source-2026-07-10.md`, so the four
deltas first catalogued on 2026-07-10 are **re-confirmed present and unchanged**.
Each is quoted from the 2026-07-13 extract to prove it.

### Δ1 — S4-R3: VIN removed from the required-invoice completion modal — **STILL PRESENT, UNCHANGED**
> S4-R3 — Required vehicle fields (mileage + engine hours, **when missing**); tech story separate (Story 17).

VIN is **not** in the Story-4 completion modal (baseline `requirements.md` L385 still
lists "mileage + VIN + engine hours"). VIN now captured by the reviewer (Story 16).
**Status: unapplied.** Affected SF-* cases (proposal): **SF-COMP-16** (drop VIN from
required set for the Story-4 modal), **SF-VAL-02** (VIN no longer a Story-4 modal
blocker — re-scope to Mark Reviewed / Story 16), sanity-check **SF-REV-03** /
**SF-UX-02** (should not assert VIN in the Story-4/general modal).

### Δ2 — S4-R8: unapproved line → Complete button DISABLED + tooltip (Story 4 ONLY) — **STILL PRESENT, UNCHANGED**
> S4-R8 — … Any unapproved line → the **Complete Work Order button is disabled, with a tooltip describing the reason (which line needs approval)**; it enables once all lines are approved.

**Flow divergence confirmed:** this disabled-button+tooltip model applies to **Story 4
(PO + required-invoice) only**. Stories 2/3 (S3-R9) and Story 16 (R11) still use the
**error-message / CTA-stays-active** model (their text is unchanged in this doc).
Baseline `requirements.md` L395 still has the error-toast model for Story 4.
**Status: unapplied.** Affected SF-* cases (proposal): **SF-COMP-21 / SF-COMP-22**
(Story-4 required-invoice → expected result becomes disabled Complete + tooltip
naming the line; must disambiguate flow), **SF-VAL-11** (still valid for Stories
2/3/16 error model; add a Story-4 variant or note the split), **SF-REV-13**
(Story 16 unchanged — confirm no accidental edit), **SF-PERM-02** (unaffected).

### Δ3 — NEW S13-R6 + S13-R7 receive-time gates (part# / cost-sell) — **STILL PRESENT, UNCHANGED**
> S13-R6 — **Part number required.** If a part number is missing, the user gets an indication to enter one; receiving is blocked until it's filled.
> S13-R7 — **Cost / sell price required.** If cost / sell price is missing, the user gets an indication to enter one; receiving is blocked until it's filled.

Baseline `requirements.md` Story 13 has only S13-R1…R5 — **R6/R7 absent.** Two matching
acceptance-criteria lines are also present in the doc.
**Status: unapplied.** Affected SF-* cases (proposal): **SF-VEND-04** (add S13-R6/R7
preconditions on assign-vendor group), **SF-VAL-06** (extend to part# AND cost/sell
required at receive), **SF-RCV-06** (extend gate list), **SF-PNFIX-05** (now has an
explicit spec anchor S13-R6 — align wording); consider a **new case** for S13-R7
(cost/sell required to receive) if not cleanly covered.

### Δ4 — Mark Reviewed dialog: "optional note" removed — **STILL PRESENT, UNCHANGED**
> R7 — Mark Reviewed = manager/foreman only; dialog captures VIN (required if missing); Confirm disabled until VIN.

The **"+ optional note"** phrasing (baseline `requirements.md` L560-561) is **absent**
from R7 and the Story-16 acceptance line. **Caveat — a leftover:** R10 still lists the
test id `input_review_note` (unchanged since the Jul-10 doc), so the note *test id*
survives even though the *field description* was dropped. Flag this internal
inconsistency to Milos/dev when applying — do not assume the note is fully removed
from the build; **re-VIU the Mark Reviewed dialog** to confirm whether the note field
is present in the actual build.
**Status: unapplied.** Affected SF-* cases (proposal): **SF-REV-06** (drop the note
assertion), **SF-REV-10** (remove any optional-note element), sanity **SF-VAL-07**
(Confirm-disabled-until-VIN unchanged, still correct).

### NEW deltas beyond Δ1–Δ4
**NONE.** A full identical-extractor diff of the 2026-07-13 doc vs the 2026-07-10 doc
returns zero differences. No story text, rule, permission-mapping (§ SV-8183 table),
review-flow, receive/bulk gate, $0-sell (S5-R1), taxable, change-log, or §8 line
changed. §9 permissions matrix (11 roles × 10 actions), the BE-atom-collapse caveat,
and the reviewer≠completer NET-NEW rule are all byte-identical to Jul-10 (and already
reflected in `requirements.md` §9 — no action). S5-R1 ("sell price validated at save,
not deferred to completion") is byte-identical — no completion-time $0-sell rule added.

---

## B. Design verdict + impacts

**Verdict: SAME — byte-identical re-delivery (NOT newer). Third identical delivery.**

```
diff -rq /tmp/sf-design-prior(=890a4d0a Jul-10 bundle)  /tmp/simple-flow/design3(=Jul-13)  →  exit 0, ZERO differences
```

All 134 entries (15 HTML, 3 MD, 4 JSX, 2 CSS, 2 top-level PNG, 6 `screenshots/` PNG,
45 `uploads/` PNG, 54 Inter TTF, `assets/symbol-primary.svg`, 2 tooling-metadata) are
identical in name AND content to the 2026-07-10 bundle — which was itself byte-identical
to the 2026-07-09 bundle already cataloged in `design-latest-catalog.md`. The new repo
copy (fonts + dotfile metadata excluded, 78 files) is byte-identical to
`build/simple-flow/design2-2026-07-10/` (`diff -rq` exit 0).

**Genuinely new/changed screens: NONE. Case impacts from the design: NONE.** Every
prior design-driven observation still stands as recorded (incl. the `HANDOFF.md`
settings-default gap #3 — Require-review toggle still omitted; and the 2026-07-08
"$0.00 sell — no action needed to continue" completion-modal screenshot, already in
the baseline, still the one re-VIU-vs-S5-R1 item — see `design-latest-catalog.md`).

Preserved at `build/simple-flow/design3-2026-07-13/` for the record; no downstream
design action warranted.

---

## C. Round-3 PO questions — resolved vs still open

The spec §8 Open Questions and the §9 SV-8183 permissions section are **verbatim
identical** to the Jul-10 doc, and the design is unchanged. Therefore **no Round-3
question is newly resolved** by these uploads. Send the Milos Round-3 sheet **as-is**.

| # | Round-3 question (`PO-Questions-Round3.md`) | Newly resolved? | Note |
|---|---|---|---|
| Q1 | Review/approval **required before invoicing**? (mandatory vs each-shop-choose) | **No** | Spec treats it as a per-org **setting** (S1-R4 "Default per cohort — see §8"); "invoicing blocked until reviewed" only describes the review-ON path, not whether review is mandatory. Unchanged — still a PO decision. |
| Q2 | New-org **Require-Review default** ON/OFF | **No** | S1-R4 default still "per cohort (see §8)"; §8 still lists it open; `HANDOFF.md` still omits the toggle. |
| Q3 | **Close/cancel confirmation modal** behavior | **No** | S15-R4 still "⚠️ Design pending for the close-confirm specifically"; no confirm-modal artboard in the (identical) design bundle. |
| Q4 | **Vendor-missing group ordering** on the receive list (top/bottom/mixed) | **No** | Spec S12-R3 says "vendor-missing group leads"; S12-R1/S8 say "at the bottom" — the internal top-vs-bottom ambiguity is **unchanged** (design `Receive Vendor Parts - v2.html` identical). Still a PO decision. |
| Q5 | **$0 sell price at completion** — allow vs block | **No** | S5-R1 ("sell validated at save, not deferred to completion") byte-identical; the doc adds **no** completion-time $0-sell rule. Note: S13-R7 adds a *receive-time* cost/sell gate (different surface) but does NOT answer the *completion* question. The design's "$0.00 — no action needed to continue" screenshot (2026-07-08, already in baseline) keeps the tension. **Keep Q5.** |
| Q6 | Who may add a **vendorless part** (See-Financial-Data gate vs any WO-editor) | **No** | §9 permissions table still asserts option A (WO Lines C&E **+ See Financial Data**; footnote 2: Technician lacks SFD → cannot add) — but this is the unchanged SV-8183 content, and it still **conflicts with Milos R2 Q4** (which said Sell is NOT enforced on the request form — see PROJECT-STATE §5.F/§D). The doc does not reconcile that conflict. **Keep Q6; flag as spec-vs-answer contradiction (last-update-wins).** |

**No question can be dropped or pre-answered on the strength of these uploads.**

---

## D. ACTION LIST (feeds the apply + wording/VIU pass)

> Unchanged from `spec-diff-2026-07-10.md` §3/§5 — this upload adds nothing, so the
> outstanding work is the same four deltas plus the standing re-VIU/PO items.

### D1. Case-content updates (expected-result / precondition edits — proposal)
- **Δ1 (VIN drop, Story-4 modal):** SF-COMP-16 (drop VIN from required set); SF-VAL-02
  (re-scope VIN to Mark Reviewed/Story 16); sanity SF-REV-03, SF-UX-02.
- **Δ2 (disabled Complete + tooltip, Story 4 ONLY):** SF-COMP-21, SF-COMP-22 (Story-4
  expected → disabled button + tooltip naming the line; disambiguate from the Stories
  2/3/16 error model); SF-VAL-11 (add Story-4 variant / note the split); confirm
  SF-REV-13 unchanged.
- **Δ3 (S13-R6/R7 receive gates):** SF-VEND-04 (add part#+cost/sell preconditions);
  SF-VAL-06 (extend to part# AND cost/sell at receive); SF-RCV-06 (extend gate list);
  SF-PNFIX-05 (align to S13-R6 anchor).
- **Δ4 (Mark-Reviewed note removed):** SF-REV-06, SF-REV-10 (drop the optional-note
  assertion); sanity SF-VAL-07 (unchanged).

### D2. New cases needed
- **S13-R7 (cost/sell required to receive)** — add a dedicated case if not cleanly
  covered by extending SF-VAL-06 / SF-RCV-06. (Proposal — not created here.)

### D3. Re-VIU needed (verify on sv7301 build)
- **Δ4 note contradiction:** R10 still lists test id `input_review_note` though R7
  dropped the field — re-VIU the Mark Reviewed dialog to confirm build reality.
- **Q5 / S5-R1 tension:** re-VIU whether sell price is blocked at part-save (S5-R1) or
  merely warned at completion (design "$0.00 — no action needed" screenshot). Cases:
  SF-VAL-* (sell validation), SF-VPART-*/Story 5 (vendorless part add).
- **Δ1–Δ3 build reality:** when applying, VIU-confirm the Story-4 modal field set
  (mileage+engine hours, no VIN), the Story-4 disabled+tooltip affordance, and the
  S13-R6/R7 receive blocks on the assign-vendor group.

### D4. API-section placement (CLAUDE.md standing rule 4)
- Any Δ3 case (S13-R6/R7) whose expected result asserts a **receive is blocked at the
  API / a specific HTTP status** (vs a pure UI affordance) MUST live in a TestRail
  section whose title includes **'API'**. Pure UI-affordance gate cases (indication
  shown, Receive button disabled) stay in the functional Accept-Delivery/Bulk-Receive
  section. Apply this split when the S13-R6/R7 case content is authored/updated.

### D5. Housekeeping
- Record in `PROJECT-STATE.md` that the 2026-07-13 spec + design are **byte-identical
  re-deliveries** (spec = re-export of the Jul-10 V2.4 silent revision; design = 3rd
  identical delivery). Future same-label uploads may still carry uncatalogued edits —
  **always diff, never trust the version string / filename.**
- No change to the "4 UNAPPLIED deltas pending" status — Δ1–Δ4 remain the top apply task.
