# Simple Flow — Spec + Design Delta & Action Doc (2026-07-17 uploads — spec `_4` / "V2.6")

> **Project:** Simple Flow ONLY (Epic SV-7301, PO = Milos, app `sv7301.qa.shopview.com`).
> **New inputs (both uploaded 2026-07-17, upload dir `dd1d42ba-…`):**
> - Spec doc `652f5198-SimpleMode_StreamlinedWorkOrderCompletionBulkReceiving_4.doc`
>   (Confluence MHTML, 131,417 bytes, md5 `de6fa115…`). Decoded →
>   `spec-v4-2026-07-17/requirements-v4.md`.
> - Design bundle `68650a5d-Simple_Flow_Design.zip` → extracted `/tmp/sf-design-new/`;
>   inventory + verdict → `spec-v4-2026-07-17/design-new-notes.md`.
>
> **Baselines compared:** `requirements.md` (authoring baseline, V2.4 + `_3` Δ5-Δ7 applied)
> · the `_3` doc `533141e9-…_3.doc` decoded with the **SAME extractor** (python
> email/quopri + BeautifulSoup `html.parser`, tables + `<pre>` markdown-macro blocks
> preserved) so the content diff isolates real deltas · a fresh full unzip of design `_4`
> (`49bdacbe-…_4.zip`) for the per-file design compare · `cases/*.json` +
> `testrail-id-map.csv` for case impact · the Milos Round-3 answer set
> (`milos-round3-answers-2026-07-16/`).
>
> **Scope: INGEST + DIFF + PROPOSE ONLY.** This worker did **NOT** edit
> `requirements.md` body, `cases/*.json`, `PROJECT-STATE.md`, or any TestRail artifact.
> Everything below is a **proposal** for the (separately authorized, separately reviewed)
> apply + TestRail phase.

---

## 0. TL;DR verdict

| Input | Verdict | Evidence |
|---|---|---|
| **Spec doc (2026-07-17, `_4`)** | **NEWER — MAJOR content update, self-labeled V2.6** | Status header now reads "Draft for build — **V2.6**"; **4 new change-log rows** (2026-07-15 ×2, 2026-07-16 ×2, all @Milos Vasic); same-extractor diff vs `_3` = 159 delta lines of real requirement text incl. an entirely **NEW Story 18** |
| **Design bundle (2026-07-17)** | **CONTENT-IDENTICAL re-delivery of design `_4`** — zero new/changed files | per-file md5 across ALL 135 files in both zips: manifest diff EMPTY (zip container metadata differs only) |
| **Prior deltas Δ1–Δ7** | **ALL still present / still applied-state-consistent** | Δ1-Δ4 text unchanged; R12/R13 (Δ5), S1-R9 reopen (Δ6), S10-R2 strike (Δ7) all verbatim in `_4` |
| **NEW deltas beyond Δ1–Δ7** | **NINE — Δ8…Δ16** (1 new story, 4 new requirements, 3 rewritten requirements, 1 scope confirm) | see §A |
| **Milos Round-3 rulings (2026-07-16)** | **NOT encoded in `_4`** — no override, both rulings stand | S12-R1(bottom)/S12-R3(leads) contradiction persists verbatim; Story 16 default still "Open" (§C) |
| **5 awaiting-Milos cases** | **NONE answered by `_4`** | SF-SET-08 / SF-COMP-06 / SF-REV-11 / SF-UX-04 / SF-QB-02 source text unchanged (§C) |
| **SF-VMIS-06 Deviation** | **LEGITIMIZED by `_4`** (S6-R6 rewritten to match code — no report exists) | §A Δ12; Deviation resolves via rescope, bug-route drops |
| **SF-QB-09 Open-Question** | **PARTIALLY RESOLVED** (part-sale investigation → "confirmed Jul 14") | §A Δ15; rescope to testable part-sale-PO cases |

**Bottom line:** the `_4` spec is the biggest delta since V2.4 — it **consolidates all core
handling into a NEW Story 18 (SV-8353) with a pre-resolve-BEFORE-receive model** that
obsoletes the invoice-gate core model our SF-CORE cases (and the 2026-07-14 design-`_4`
SF-CORE-03 flip) are written to, **removes the Story-9 Apply button**, **restricts Bulk
Receive cost-editability to $0 cost only**, **rewrites S6-R6** (vendor-missing reporting →
QB-export/Vendors-Expenses exclusion, legitimizing our SF-VMIS-06 deviation), adds **3 new
UX requirements** (S11-R4 return-to-line, S12-R6 vendorless-group-on-single-receive,
S13-R8 vendor/PN changeable until receive), and **confirms part-sale POs in scope** for
Stories 7/8. The design zip adds nothing (identical to `_4`) — but Story 18 re-reads the
already-present `Resolve Cores Flow.html` as the operative core design and exposes one
stale design branch (mandatory-flow resolve-AFTER-receive vs C-R6 resolve-first).

---

## A. Full delta list — Δ1–Δ7 confirmed + NEW Δ8–Δ16 (verbatim, cited, latest-wins)

### Δ1–Δ7 — ALL STILL PRESENT / UNCHANGED (no action)
Same-extractor check: Δ1 (S4-R3 VIN drop), Δ2 (S4-R8 disabled+tooltip), Δ3 (S13-R6/R7
receive gates), Δ4 (Mark-Reviewed note removed), Δ5 (Story 16 R12/R13 auto-complete +
2026-07-14 change-log row), Δ6 (S1-R9 reopen clause), Δ7 (S10-R2 struck through — still
rendered `~~S10-R2~~ — When a part number is added, the part becomes a first-class
inventory/catalog part…~~`) are all verbatim-identical in `_4`. The Δ7 **self-contradiction
persists unchanged**: Story-10 AC still says *"New number on receive → new inventory/catalog
part + stock + Part History"*, the Technical-guardrails paragraph still requires the inline
endpoint to *"drive catalog creation/linking"*, and the §10 permission matrix still carries
the row *"Inline part-number fix → first-class inventory/catalog part | 10 | Catalog &
Inventory: Create & Edit"*. → The existing spec-cleanup flag for Milos stays open; the Δ7
rescope of SF-PNFIX-02/03/06 + SF-QB-08 stands (QA-lead last-update-wins ruling, 2026-07-14).

### Δ8 (NEW, BIGGEST) — Story 18: Core Parts (SV-8353) — pre-resolve-BEFORE-receive, BOTH flows — **NET-NEW STORY, UNAPPLIED**
Change-log rows (verbatim):
> **2026-07-15 @Milos Vasic** — "Core parts consolidated into a dedicated story (Story 18 /
> SV-8353) with Dipesh's pre-receive plan: the optional flow now resolves special-order
> cores BEFORE receiving (decision persisted on work_order_part_request.core_resolution,
> charge follows immediately, auto-applied at receive) instead of at the Create Invoice
> gate; receive-time quantity locked to full remaining once invoiced/paid (FE-only).
> Per-story core sections (Stories 3/4/8/10/16) + §4/§8 slimmed to point at Story 18."
> **2026-07-16 @Milos Vasic** — "Core resolution unified to resolve-before-receive for
> special-order parts in BOTH flows (mandatory now matches optional — Story 18 C-R6;
> Stories 4/16, §4/§8)."

New Story 18 requirements (verbatim, condensed cites — full text in `requirements-v4.md`):
- **C-R1 — Dedicated resolve screen.** "Right before the **Complete without receiving /
  Receive parts** choice, a separate consolidated screen lists every un-received vendor
  core (part info + core charge + **OK / Not OK**), with a message that resolving now is
  for **invoice accuracy**."
- **C-R2 — Decision persisted on the request.** "Saved on the core `work_order_part_request`
  (`core_resolution = ok | not_ok | NULL`) — no WorkOrderPart, statement item, or vendor
  return created at this point." (API: `POST /api/work-orders/{id}/pre-resolve-cores`.)
- **C-R3 — Charge follows the decision immediately.** "Not OK → core charge priced into the
  WO total → customer invoice; OK → no charge (vendor core return created automatically at
  receive)."
- **C-R4 — Gates use the decision.** "Completion and invoice creation are blocked **only for
  undecided (NULL) cores**; `cores_pending` reflects this."
- **C-R5 — Auto-apply at receive.** "The saved decision is applied automatically at receive —
  the user is **not asked again** (Not OK → core created already resolved, no return; OK →
  existing Core-OK: resolve + vendor PartReturnRequest)." AC adds idempotency: "retries
  create no duplicates"; "OK → exactly one vendor return"; "invoice never changes at receive".
- **C-R6 — Mandatory flow (UI change only).** "As now we have ability to show the users
  resolve cores first we will ask them to **resolve, and then to receive** to be consistent
  with the optional flow as well."
- **C-R7 — Inventory cores at pick (both flows, unchanged).**
- **C-R8 — Receive-time quantity rule.** "When the WO is **invoiced/paid**, the receive
  dialog **locks quantity to the full remaining amount** (core auto-selected; tooltip *'This
  part is on a customer invoice and should be received in full'*). Before invoice, quantity
  stays editable… **FE-only lock**."
- **C-R9 — Lines-tab read model.** "NULL → 'Core decision pending'; ok → 'Core OK — return to
  vendor, no charge'; not_ok → 'Core Not OK — customer charged'. No duplicate prompts;
  received pre-resolved cores show the resolved state with no OK/Not-OK buttons."
- **C-R10 — Sync-back.** "Resolving a received core via the existing handle-core(s)
  endpoints **also writes the value to the linked core PartRequest**."
- AC also: "Resolution cannot change after the WO has an active invoice."

**REPLACED/REMOVED `_3` text (obsoleting our current case model):** S3-C1..C4 (invoice-gate
"Cores pending" flag / resolve-module routes-to-receive / cancel leaves cores-pending), the
Story-3 core AC + the PartRequest invoice-gate **guardrail incl. "Part-sale WOs auto-resolve
at receive; service WOs need manual Ok/Not OK"**, S4-C1/C2 (required-flow resolve AFTER the
receive round-trip), S8-C1/C2 core AC (Ok/Not-OK "becomes available" once received), the
Story-16 core paragraph (now: "Special-order cores (both required and optional) are
**pre-resolved before receiving on the resolve screen before Send to Review** (no longer
deferred to the invoice gate)"), and §4/§8 core summaries.

**Affected cases (proposal):**
- **REWORD (expected-result changes):** **SF-CORE-03 (C29315)** — currently worded to the
  design-`_4` "Complete Without Receiving DISABLED while a core waits" model; Story 18 C-R1/
  C-R4 = resolve screen before the choice, gate only on UNDECIDED cores (decided → complete
  without receiving proceeds). **SF-CORE-04 (C29316)** — "Cores pending" now = undecided-only
  (C-R4). **SF-CORE-07 (C29319)** — required flow flips to resolve-BEFORE-receive (C-R6).
  **SF-CORE-08 (C29320)** — PartRequest-only detection now via `core_resolution` NULL gate
  (C-R2/C-R4). **SF-BULK-10 (C29359)** — receive no longer *makes resolution available*; a
  pre-resolved decision AUTO-APPLIES with no re-prompt (C-R5); core-only partial receive
  retained. **SF-REV-14 (C29399)** — cores pre-resolved before receiving before Send To
  Review (invoice-gate leg dropped). **SF-COMP-11 (C29300) / SF-COMP-14 (C29303)** — the
  2026-07-14 core-waiting caveats need the C-R1/C-R4 re-alignment.
- **RETIRE CANDIDATES (spec basis removed):** **SF-CORE-05 (C29317)** + **SF-CORE-06
  (C29318)** — the Create-Invoice-gate resolve module (route-to-receive, cancel →
  cores-pending) no longer exists in the spec. **SF-CORE-09 (C29321)** — the part-sale
  auto-resolve vs service manual guardrail text was deleted and Story 18 is silent on it
  ("spec silent" — confirm retire vs rescope with QA lead/Milos, per the Δ7 precedent of
  not silently dropping).
- **Sanity (likely no edit):** SF-CORE-01 (C29313, inventory cores at pick = C-R7),
  SF-CORE-02 (C29314, no cores → no step), SF-CORE-10 (C29322, +$ at line level = C-R3;
  optionally add the C-R9 three-state labels), SF-VAL-09 (C29423, sell-lock unchanged).
- **NEW cases required:** see §D2 (resolve screen, pre-resolve API, gate-on-undecided,
  auto-apply-at-receive/idempotency, mandatory resolve-first, C-R8 qty lock, C-R9 states,
  C-R10 sync-back API).
- **Status note:** the whole SF-CORE special-order set stays **Blocked-Env** for VIU
  (vendor-sourced core still not seedable — needs the dev-seeded core; and SV-8353 is a new
  dev plan, so the pre-resolve build likely isn't on sv7301 yet — probe before re-VIU).

### Δ9 (NEW) — S11-R4 + S3-R5 amendment: return to the originating WO line after receive — **NET-NEW, UNAPPLIED**
> **S11-R4 — Return to the originating line.** "After receiving (Receive → Accept Delivery →
> back to the work order), the user is returned to the **exact WO line they receded from**
> (scroll/focus preserved), not the top of the WO — for instant visual confirmation of the
> received part." (New AC: "On return from receiving, the work order scrolls/focuses to the
> originating line (not the top).")
> **S3-R5 (amended)** appends: "On return, the user lands on the originating line
> (scroll/focus preserved), not the top of the WO — see Story 11 (S11-R4)."

Affected: **NEW case** (Story-11 section). Sanity: SF-COMP-13 (C29302 — add the return-
anchor nuance or leave to the new case), SF-RCV-01/02 (C29369/C29370) unchanged.

### Δ10 (NEW) — S12-R6: vendorless group surfaced when receiving a single WO part — **NET-NEW, UNAPPLIED**
> **S12-R6** — "Clicking **Receive** on a single WO part opens Accept Delivery showing all
> to-receive parts **for that part's vendor** (FE sends the vendor ID; BE returns only that
> vendor's parts) **plus the vendorless group** — so the user can assign a vendor / merge a
> vendorless part into this receive on the spot (reusing the same invoice number) rather
> than going back." (New AC: "Receiving a single WO part shows that vendor's other
> to-receive parts + the vendorless group; the user can assign/merge a vendorless part there.")

Affected: **NEW case(s)** (Accept Delivery Story-12 section; 1 UI scope + optionally 1 for
the assign/merge-with-same-invoice action). Sanity: SF-RCV-02 (C29370), SF-WOP-02 (C29384).
NOTE: does NOT resolve the SF-RCV-05/07 placement deviation (see §C-1) — S12-R6 is about
*content* of the single-part receive view, not group ordering.

### Δ11 (NEW) — S13-R8: vendor & part number stay changeable until receive (SV-8343) — **NET-NEW, UNAPPLIED**
> **S13-R8** — "An assigned vendor is **not locked on selection** — it stays changeable (same
> dropdown) **until a part is received or the WO is invoiced/paid**, so a wrong pick can be
> corrected. The part number is likewise **editable (edit icon)** after entry, under the same
> condition. Prevents receiving against the wrong vendor (SV-8343)." (New AC: "An assigned
> vendor can be changed until a part is received or the WO is invoiced/paid; part number
> editable via an edit icon under the same condition.")

Affected: **NEW case(s)** (Assign Vendor Story-13 section; vendor re-change + PN re-edit —
1-2 cases). Sanity/wording touch: SF-VEND-01 (C29378 — "assigns a vendor at PO level" may
add "not locked on selection"), SF-PNFIX-01 (C29363 — persists immediately; re-edit now
explicitly allowed), SF-VEND-05 (C29382 — invoiced/paid lock condition, consistent).

### Δ12 (NEW) — S6-R6 REWRITTEN: no "needs vendor" report — QB-export/Vendors-Expenses exclusion — **LEGITIMIZES the SF-VMIS-06 DEVIATION**
`_3` said: *"S6-R6 — Reports. Reports mark vendor-missing POs as 'needs vendor.'"* `_4` says:
> **S6-R6 — Reporting impact (no PO report exists).** "There is **no dedicated purchase-order
> report and no 'needs vendor' marker today**, so nothing 'marks' these POs. What actually
> happens: a vendor-missing PO has no vendor, so its spend is **excluded from the QuickBooks
> Vendor Bill export** (inner-join on vendor) and is **not counted in the Vendors Expenses
> report** (grouped by vendor) until a vendor is assigned — then it flows in normally.
> (Separate, cost-not-vendor note: a $0-cost vendor-missing part can skew the Inventory
> report's average cost.)"
> Change-log 2026-07-16: "S6-R6 (reports) corrected to match code… (verified in
> VendorBillExportQueryHandler / VendorsExpensesQueryHandler). SV-7701 updated to match."

Affected: **SF-VMIS-06 (C29343, currently Deviation)** — our finding ("the Reports area has
NO Vendor-Missing/'needs vendor' PO report") is now the SPEC ITSELF → **Deviation resolves**.
Rescope the case to the new AC ("A vendor-missing PO's spend is excluded from the QuickBooks
Vendor Bill export and from Vendors Expenses until a vendor is assigned; once assigned it
appears normally. (No 'needs vendor' marker…)") — the **Vendors Expenses report leg** may be
VIU-able in-app (seed a costed vendor-missing PO, check the report); the **QB Vendor Bill
export leg** stays Blocked-Env (QB not connected). Drop the SF-VMIS-06 dev-route item from
the open-threads list. Sanity: SF-VMIS-03 (C29340) consistent, unchanged.

### Δ13 (NEW) — Story 9 + S8-R9: the Apply button is REMOVED — invoice number typed & remembered — **CHANGED, UNAPPLIED**
> **S9-R1** — "Under the vendor name, a **field to enter one invoice number** for that
> vendor's POs, available when **≥1 PO under that vendor is selected**. There is **no
> 'Apply' button** — the number is remembered as typed."
> **S9-R2** — "Select PO(s) and type one invoice number → it is filled into only the
> selected POs of that vendor (still editable per PO). Then Receive all for that vendor."
> **S8-R9** — "Set one invoice number across a vendor's selected POs (Story 9) — the number
> is typed and remembered per PO; **no separate Apply button**."
> New AC set: field "available only with ≥1 selected PO under that vendor"; typed number
> fills only that vendor's selected POs, per-PO editable; other vendors/unselected POs
> unaffected; "Vendorless group shows no invoice-number field (assign a vendor first)."
> Change-log 2026-07-16: "Apply button removed from Bulk Invoice (Story 9…)."

Affected: **SF-INV-01 (C29360), SF-INV-02 (C29361), SF-INV-03 (C29362)** — all three are
currently VIU-Verified against a build whose live label IS **"Apply to selected POs"**
(wording glossary 2026-07-13). Reword all three to the no-button model; on re-VIU the
current build will DEVIATE until dev removes the button → expect Verified→Deviation (or
hold as "awaiting dev build") on this trio. Sanity: SF-BULK-05 (per-PO Receive-parts gate,
unchanged), SF-VAL-10 (C29424, invoice-number reuse — AC retained verbatim, unchanged).

### Δ14 (NEW) — S8-R7: cost editable ONLY when cost is 0 — **CHANGED, UNAPPLIED**
`_3` said: *"Quantity editable; cost editable (pulled from WO/PO)…"* `_4` says:
> **S8-R7** — "Quantity is editable (supports partial receive). **Cost is editable (if the
> cost is 0, if cost is not 0 cost filed should not be editable)**, pulled from the work
> order / PO when available. Sell price is editable until the WO is invoiced/paid, then
> locked — lock icon + tooltip 'Locked — this part is already invoiced or paid.' After it
> locks, only cost remains editable."

**Spec-inconsistency to flag:** the last sentence ("after it locks, **only cost** remains
editable") survives from the old text and now contradicts the new only-if-$0 rule for a
non-zero-cost part — and S10-R3/S12-R5 ("editable when $0/missing") now align with the new
rule while the S12-R5 "parity" note already matched. Flag for Milos rather than pick a side
silently (Standing Rule 15.5).
Affected: **SF-BULK-06 (C29355)** — currently "qty and cost editable; sell locks…"; reword
to the $0-only cost rule + partial-receive qty note; re-VIU (if the build allows editing a
non-zero cost on Bulk Receive → new deviation). Sanity: SF-RCV-10 (C29440, already
"$0 or missing" parity — unchanged), SF-PNFIX-04 (C29366, locking parity — unchanged).

### Δ15 (NEW) — Part-sale POs CONFIRMED IN SCOPE for Stories 7/8 — **SCOPE CONFIRM + NEW AC, UNAPPLIED**
New AC bullets: Story 7 — "Part-sale-originated POs appear in the list and are
selectable/receivable like WO-originated POs."; Story 8 — "Part-sale-originated POs can be
received on this page like WO-originated POs." §8 flips the old *"Part Sales impact
(investigation — BE)"* to:
> "**Part Sales — confirmed (Jul 14).** Adding a part on a Part Sale already creates a PO
> that appears in the PO list and is received via the same PO/receive pipeline as work
> orders. So part-sale POs are in scope for PO multi-select + Bulk Receive (Stories 7/8)
> and Accept Delivery. Remaining check: verify part-sale POs behave in Bulk Receive +
> Waiting-on-Parts, and that the new order/status logic does not regress part-sale status
> transitions."

Affected: **SF-QB-09 (no C-ID, Open-Question)** — the investigation it tracks is now
CONFIRMED in-spec; rescope SF-QB-09 from "Investigate…" to the residual regression check
(part-sale status transitions) or close it and cover via the new cases. **NEW cases:**
part-sale PO on the PO list + multi-select (Story-7 section), part-sale PO receivable on
Bulk Receive (Story-8 section), optionally part-sale WO in Waiting-on-Parts (Story-14
section). Seeding is self-serviceable (create a Part Sale with a vendor part — Rule 14).

### Δ16 — Status header V2.4 → V2.6 + change-log — **METADATA**
Header now cites SV-8353 and the resolve-before-receive model. (The `_3` doc's header still
said "V2.4" while being de-facto V2.5 — so "V2.6" finally matches content; keep the
never-trust-the-version-string rule regardless.)

### NEW deltas beyond Δ8–Δ16
**NONE further.** The full same-extractor diff is 159 lines; every hunk is accounted for
above (the remaining hunks are the §4 core summary + §8 part-sale rewrites quoted in Δ8/Δ15
and the 4 change-log rows). §9/SV-8183 permissions, Story 1 settings, Stories 2/5/14/15/17,
S12-R1..R5, S13-R1..R7, Story 16 R1–R13 and §8 Open Questions (other than the part-sale
item) are **byte-identical** to `_3`.

---

## B. Design verdict + impacts

**Verdict: CONTENT-IDENTICAL RE-DELIVERY of design `_4` — zero new or changed files.**
Full-population check: per-file md5 of ALL 135 files in `68650a5d-Simple_Flow_Design.zip`
vs a fresh unzip of `49bdacbe-Simple_Flow_Design_4.zip` — **manifest diff EMPTY** (the two
zips differ only in container metadata; same 14,141,003-byte size). Full inventory +
detail: `design-new-notes.md`.

**No design-driven case impact from the zip itself.** However, spec `_4` Story 18 re-reads
the already-present design files:
1. **`Resolve Cores Flow.html` + Handoff** = the operative Story-18 "dedicated resolve
   screen" design (step order `Details → Pick parts → Resolve cores → Receive`, gate =
   Continue disabled until every core decided, inline-and-wizard decisions share one state,
   "Complete Without Receiving is safe to leave unblocked" because cores resolve before
   Receive) — **matches C-R1/C-R4**.
2. **STALE design branch:** the same file's required-invoice logic comments "Resolve cores
   comes AFTER Receive" — **overridden by C-R6** (2026-07-16, resolve-first in BOTH flows).
   Latest-wins: spec governs; flag the design for cleanup.
3. **`Resolve Cores to Invoice.html` / `WO Review Flow.html`** (source of the 2026-07-14
   SF-CORE-03 "un-skippable / receive the core part first" flip) — **superseded on that
   point by C-R4** (gate only on UNDECIDED cores; a pre-resolved core does not block
   Complete Without Receiving). Drives the SF-CORE-03 reword in §A Δ8.

---

## C. Milos rulings & open items — what `_4` resolves vs overrides vs leaves open

### C-1. Round-3 rulings (2026-07-16 answer sheet) — do they survive `_4`? **YES, both.**
| Ruling | Encoded in `_4`? | Latest-wins call |
|---|---|---|
| **Q1 — vendor-missing split (Bulk Receive = TOP, Receive/Accept-Delivery = BOTTOM)** | **NO.** S12-R1 still says "(vendor-missing in their own group **at the bottom**)" while S12-R3 still says "vendor-missing group **leads**" — the same unscoped self-contradiction, verbatim from `_3`. | `_4` neither confirms nor contradicts the split; the answer-sheet ruling (same-day 2026-07-16, more specific) **STANDS**. **SF-RCV-05 (C29373) / SF-RCV-07 (C29375) stay Deviation**; bug draft #5 (Receive-screen group should be BOTTOM) stays valid; the S12-R1-vs-R3 spec-cleanup flag for Milos stays open. |
| **Q2 — new-org Require-Review default = ON** | **NO.** Story 16 still reads "Open. Setting default (on for bigger/existing shops?); role-gating tied to custom roles vs open for v1." | Ruling **STANDS** (spec merely leaves the question open — no contradiction). **SF-REV-15 (C29400) stays Blocked-Env** (new-org not provisionable). |

### C-2. Prior-pass rulings — still holding in `_4`?
- **R12/R13 auto-complete (Δ5/SV-8303):** present verbatim, unchanged → the **SF-AUTO-01..07
  set (C29461–C29467) stays valid as-is.**
- **S10-R2 deprecation (Δ7):** still struck through → rescope of SF-PNFIX-02/03/06 +
  SF-QB-08 stands. The AC/guardrail/permission-matrix self-contradiction **persists in
  `_4`** — spec-cleanup flag stays open.
- **Reviewer≠completer descope, S1-R9 reopen (Δ6), Δ1-Δ4:** all unchanged.
- **Design-`_4` core-blocking reading (SF-CORE-03 flip):** **OVERRIDDEN by Story 18 C-R4**
  (see Δ8) — the one prior call `_4` genuinely reverses.

### C-3. The 5 awaiting-Milos cases — answered by `_4`? **NONE.**
| Case | Question | `_4` says |
|---|---|---|
| SF-SET-08 (C29282) | brand-new-org first-use defaults | S1-R4/§8 unchanged — **not answered** |
| SF-COMP-06 (C29295) | Create-POs toggle absent / POs always-on | Story 1/2 unchanged — **not answered** |
| SF-REV-11 (C29396) | direct sign-off + invoicing-block leg | Story 16 text unchanged (invoicing blocked until Reviewed + cores resolved — same as `_3`) — **not answered** |
| SF-UX-04 (C29404) | close/cancel confirmation modal | S15-R4 unchanged ("design pending") — **not answered** |
| SF-QB-02 (C29427) | Create-POs OFF → no QB artifacts | unchanged — **not answered** |

### C-4. Bug drafts / dev items — resolved by `_4`?
- **SF-VMIS-06 dev-route item — RESOLVED** (Δ12: spec corrected to match code; no bug).
- **Bug draft #5 (Receive-screen vendor-missing at TOP, should be BOTTOM) — NOT resolved,
  still valid** (C-1).
- **OBS-6 (Part-History 500), OBS-7, SF-AUTO-04 API-500, SF-SET-03 Create-POs-toggle
  Deviation — untouched** by `_4`.

---

## D. ACTION LIST (feeds the apply + wording/VIU + TestRail phase — SEPARATE, ALREADY-USER-AUTHORIZED STEP; not started here)

> Standing Rule 11: confirm with the user which process(es) to run —
> (1) `BUILD-ACCURATE-WORDING-VIU-PROCESS.md` and/or (2) `SPEC-RELEVANCE-RECONCILIATION-PROCESS.md`.

### D1. Case-content updates (reword / expected-result edits) — **14 cases**
| Case | C-ID | Delta | Edit |
|---|---|---|---|
| SF-CORE-03 | C29315 | Δ8 C-R1/C-R4 | resolve screen before the Complete-without-receiving/Receive-parts choice; gate = UNDECIDED cores only (replaces the design-`_4` "disabled while core waits" wording) |
| SF-CORE-04 | C29316 | Δ8 C-R4 | "Cores pending" reflects undecided (NULL) cores only |
| SF-CORE-07 | C29319 | Δ8 C-R6 | required flow: resolve FIRST, then receive (was after the round-trip) |
| SF-CORE-08 | C29320 | Δ8 C-R2/C-R4 | gate detects via `core_resolution=NULL` on the PartRequest |
| SF-BULK-10 | C29359 | Δ8 C-R5 | pre-resolved decision auto-applies at receive, no re-prompt; core-only partial receive retained |
| SF-REV-14 | C29399 | Δ8/S16 | cores pre-resolved before receiving before Send To Review (drop the invoice-gate leg) |
| SF-COMP-11 | C29300 | Δ8 | re-align the 2026-07-14 core-waiting caveat to C-R1/C-R4 |
| SF-COMP-14 | C29303 | Δ8 | same caveat re-alignment |
| SF-INV-01 | C29360 | Δ13 | no Apply control — invoice-number field available when ≥1 PO selected |
| SF-INV-02 | C29361 | Δ13 | typed number fills selected POs (no Apply click) |
| SF-INV-03 | C29362 | Δ13 | per-vendor scoping + vendorless group shows no invoice-number field |
| SF-BULK-06 | C29355 | Δ14 | cost editable ONLY when $0; qty partial-receive note |
| SF-VMIS-06 | C29343 | Δ12 | rescope: QB Vendor Bill export + Vendors Expenses exclusion until vendor assigned (Deviation resolves) |
| SF-QB-09 | — (unmapped) | Δ15 | rescope from "Investigate part-sale impact" to the residual status-transition regression check (or close in favor of the new part-sale cases) |

Sanity-confirm (expect NO edit): SF-CORE-01/02/10 (C29313/14/22), SF-VAL-09/10 (C29423/24),
SF-RCV-10 (C29440), SF-PNFIX-01/04 (C29363/66), SF-VEND-01/05 (C29378/82), SF-COMP-13
(C29302), SF-RCV-02 (C29370), SF-BULK-05.

### D2. New cases needed — **~14-18**
- **Δ8 Story 18 (~8-10, place special-order set in a "Core parts" leaf; API-asserting cases
  in an 'API'-titled section per Standing Rule 4):** (1) C-R1 resolve screen appears before
  the complete/receive choice listing un-received vendor cores + charge + OK/Not-OK +
  invoice-accuracy message; (2) C-R3 Not-OK prices the charge into the WO total/invoice
  immediately, OK adds none; (3) C-R4 completion + invoice-creation blocked only while a
  core is undecided; (4) C-R5 receive auto-applies the saved decision with no re-prompt
  (OK → exactly one vendor return; Not-OK → resolved core, no return; retry-idempotent);
  (5) C-R6 mandatory flow asks resolve-then-receive; (6) C-R8 invoiced/paid receive dialog
  locks qty to full remaining + tooltip (likely Blocked-Env: invoiced/paid WO); (7) C-R9
  lines-tab three states + no duplicate prompts; (8) API — `POST
  /api/work-orders/{id}/pre-resolve-cores` persists `core_resolution` with no
  WorkOrderPart/statement/return side-effects; (9) API — C-R10 handle-core sync-back writes
  to the linked PartRequest; (10) resolution immutable once an active invoice exists.
  *(Most will be Blocked-Env until a special-order core is seedable / SV-8353 is deployed.)*
- **Δ9 (1):** return-to-originating-line after the receive round-trip (Story-11 section).
- **Δ10 (1-2):** single-WO-part Receive shows that vendor's to-receive parts + the
  vendorless group; assign/merge a vendorless part into the receive reusing the invoice
  number (Story-12 section).
- **Δ11 (1-2):** assigned vendor re-changeable via the same dropdown until received/
  invoiced-paid; part number re-editable via the edit icon under the same condition
  (Story-13 section).
- **Δ15 (2-3):** part-sale-originated PO appears/selectable on the PO list (Story-7);
  part-sale PO receivable on Bulk Receive (Story-8); optional: part-sale WO in
  Waiting-on-Parts (Story-14).

### D3. Retire candidates — **2-3** (need QA-lead/Milos confirmation, not silent)
- **SF-CORE-05 (C29317)** + **SF-CORE-06 (C29318)** — the invoice-gate resolve module they
  test no longer exists in the spec (Δ8).
- **SF-CORE-09 (C29321)** — its guardrail sentence was deleted and Story 18 is silent
  ("spec silent" — flagged, don't drop unilaterally).

### D4. Re-VIU needed (live, evidence-backed — after the apply pass; sv7301 cookies required)
- **Δ8:** whether the SV-8353 pre-resolve build exists on sv7301 yet (probe the completion
  wizard for the resolve step + the `pre-resolve-cores` endpoint); special-order-core legs
  stay Blocked-Env until a core is seedable.
- **Δ13:** Bulk Receive — is the "Apply to selected POs" button still present? (If yes →
  SF-INV-01..03 become build-vs-spec Deviations pending dev.)
- **Δ14:** can a non-zero cost be edited on Bulk Receive? (If yes → deviation.)
- **Δ12:** seed a costed vendor-missing PO → check the Vendors Expenses report exclusion
  in-app (QB Vendor Bill export leg stays Blocked-Env).
- **Δ9/Δ10/Δ11/Δ15:** all seedable/self-serviceable per Rule 14 (part sale with vendor part;
  vendor-missing part; single-part receive; vendor re-pick).

### D5. Spec-inconsistency flags for Milos (Standing Rule 15.5 — cite, don't pick silently)
1. **S12-R1 "bottom" vs S12-R3 "leads"** — STILL unscoped (Round-3 Q1 split ruling not
   folded in).
2. **S8-R7 tail** — "after it locks, only cost remains editable" vs the new "cost not
   editable when non-zero" rule.
3. **Story-10 Δ7 residue** — struck S10-R2 vs surviving AC bullets + technical guardrails +
   §10 permission-matrix row (unchanged from `_3`).
4. **Design-vs-spec:** `Resolve Cores Flow.html` required-flow branch still codes
   resolve-AFTER-receive (vs C-R6); `Resolve Cores to Invoice.html` still shows the
   receive-first block (vs C-R4). Design bundle re-shared unchanged — needs a design rev.

### D6. Housekeeping (at apply time)
- Merge `requirements-v4.md` content into `requirements.md` (V2.6), preserving the
  interpretation-notes appendix; mark the S3-C1..C4 / S4-C1..C2 / S8-C1..C2 invoice-gate
  core model **SUPERSEDED by Story 18** (dated).
- Update `PROJECT-STATE.md`: spec `_4` = V2.6 with Δ8-Δ16; design re-delivery = no-op;
  SF-VMIS-06 deviation resolved-by-spec; SF-QB-09 rescope; Deviation tally 4 → 3 (+
  whatever Δ13/Δ14 re-VIU yields).
- Deliverable-regeneration rule applies after case edits (`gen_import.py`, `gen_update.py`,
  `gen_blockers.py`, `build_results_workbook.py`, id-map titles).
- **TestRail: 0 writes done this pass; the apply-phase push runs under the user's existing
  authorization as a separate reviewed step.**

---

## E. Update-scope estimate

| Bucket | Count | Detail |
|---|---:|---|
| Case EDITS (reword/expected) | **14** | D1 table (13 mapped + SF-QB-09) |
| Sanity confirms (expect no edit) | ~12 | D1 list |
| NEW cases | **~14-18** | Story 18 ~8-10 (incl. 2-3 API) + Δ9 1 + Δ10 1-2 + Δ11 1-2 + Δ15 2-3 |
| RETIRE candidates | **2-3** | SF-CORE-05/06 (+ SF-CORE-09 pending confirm) |
| Status flips | 1 now + up to 4 pending re-VIU | SF-VMIS-06 Deviation→rescoped; SF-INV-01..03 + SF-BULK-06 Verified→likely Deviation until dev ships Δ13/Δ14 |
| TestRail writes projected (apply phase) | ~30-38 | ~14-17 update_case + ~14-18 add_case + 1-2 add_section (Story-18 leaf + API leaf) + retire handling per QA-lead ruling |

Post-apply projected suite: **170 → ~182-188 cases** (before any retire ruling).
