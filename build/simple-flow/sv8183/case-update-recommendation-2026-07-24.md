# Simple Flow — SV-8183 Findings → Case-Update Recommendation (READ-ONLY analysis)

- **Date:** 2026-07-24
- **Author scope:** analysis only — **no case edited, no TestRail write, no authoring.** This doc is the only artifact.
- **Question answered:** for each SV-8183 finding from the exhaustive live re-runs + residuals, is it ALREADY captured by an existing case (correct wording + status)? If not, is an EDIT / NEW case / FLAG-only warranted?
- **Basis (Rules 15 / 20 / 25):** derived from the actual case bodies (`build/simple-flow/cases/group-C-review-permissions-validation-edge.json`), `testrail-id-map.csv`, the three FINDINGS files (`rerun-2026-07-24`, `rerun2-2026-07-24`, `residuals-2026-07-24`) incl. `be-matrix-11roles.json`, the QA reconciliation (`SimpleFlow_SV-8183_vs-QA-Issues_Analysis_2026-07-24.md`), and the verbatim spec `sv8183/requirements-SV8183_1.md` §9.1 (action→atom) / §9.2 (per-role matrix) / §9.4 (atom-collapse). NOT from memory.
- **Governing ruling — Standing Rule 24 (strengthened 2026-07-24):** FE-blocks + BE/API-allows = a **PASSED** test case (accepted ShopView enforcement model); the INVERSE (FE exposes what BE blocks) = an FE-exposure **defect**. This decides most cells below.
- **Already-done corrective cases (context):** SF-PERM-01 / **C29405** (BE atom-family refinement) · SF-PERM-03 / **C29407** (both Bulk-Receive entry points) · SF-PERM-11 / **C30646** (SV-8515 FE-exposure Deviation) · SF-PERM-12 / **C30647** (SV-8516 Rule-24 PASS). Confirmed contents below.

---

## What the already-done cases already cover (confirmed from the live bodies)

| Case (C-id / link) | Status | What it asserts (verbatim from the body) |
|---|---|---|
| **SF-PERM-01 / C29405** — https://shopview.testrail.io/index.php?/cases/view/29405 | VIU-Verified | WO Settings page gated by App Settings (`settingsApp`); non-App-Settings role redirected + cannot save (incl. a direct API save attempt). |
| **SF-PERM-03 / C29407** — https://shopview.testrail.io/index.php?/cases/view/29407 | VIU-Verified | Which roles can Bulk Receive — **TIGHTENED 2026-07-24 to drive BOTH entry points** (per-PO Receive button + multi-select "Receive Selected") AND attempt to actually complete the receive, per role. Office = open but cannot receive by any path; Sales Rep + Time Clock cannot reach. |
| **SF-PERM-05 / C29409** — https://shopview.testrail.io/index.php?/cases/view/29409 | VIU-Verified | Per-PO Receive button hidden for Office / read-only (no Order Parts), incl. on a fulfilled PO. |
| **SF-PERM-06 / C29410** — https://shopview.testrail.io/index.php?/cases/view/29410 | VIU-Verified | **API — Permissions / BE enforcement:** settings save 403 for unauthorized; completion/review NOT BE-enforced ("app blocks / backend allows"); step 4 = "any role with WO Create & Edit can **receive parts onto a WO** via API" (the receive-onto-WO OR gate). |
| **SF-PERM-09 / C29413** — https://shopview.testrail.io/index.php?/cases/view/29413 | VIU-Verified | Technician cannot add a vendorless part (lacks See Financial Data) — FE gate. |
| **SF-PERM-10 / C29414** — https://shopview.testrail.io/index.php?/cases/view/29414 | VIU-Verified | Per-role **Complete Work Order** matrix — Time Clock (+ Tech/Parts Tech/Office/Sales Rep) cannot complete. (This is the FE gate that also makes the completion-flow **resolve-cores wizard** unreachable for Time Clock.) |
| **SF-PERM-11 / C30646** — https://shopview.testrail.io/index.php?/cases/view/30646 | VIU-Deviation | Vendor & Order Mgmt View-only (Office) cannot receive POs by ANY path; the multi-select "Receive Selected" route currently EXPOSES the editable `/bulk-receive` screen (SV-8515 FE-exposure defect); BE blocks the actual receive (`accept` → **403**). |
| **SF-PERM-12 / C30647** — https://shopview.testrail.io/index.php?/cases/view/30647 | VIU-Verified | Time Clock part-row ⋮ menu hides Edit / Cancel / Change Vendor (only Return shows) = PASS; notes flag per Rule 24 that the same edit succeeds via API (`part/change-request` → 200) = accepted, not a bug. |
| **SF-REV-14 / C29399** — https://shopview.testrail.io/index.php?/cases/view/29399 | VIU-Verified | Cores decided before receiving ahead of Send to Review; invoicing blocked until Reviewed + every core decided (functional, **not** per-role permission). |

SF-VEND-01..08 cover assign/change-vendor **functionality** (not per-role permission gating). There is **no** dedicated per-role change-vendor negative, per-role part add/delete negative, or per-role return-create negative in the suite.

---

## Recommendation table

| # | Finding | Already covered? (case + C-id) | Recommendation | Driver ticket + spec anchor (Rule 20) | Rationale (Rule 25 — verbatim spec) | Priority |
|---|---|---|---|---|---|---|
| 1 | **NEW-1** — `change-item` (edit PO line / change vendor) is BE-gated by `seeFinancialData`, **not** by Vendor & Order Mgmt: Create & Edit as §9.2 requires → **Office + Sales Rep** pass the endpoint (400) via API. FE hides it for both (Office `edit_note` HIDDEN; Sales Rep route-blocked). | **Partially.** SF-PERM-12 / C30647 covers the Time-Clock part-**menu** change-vendor (different surface/role) + the Rule-24 API-possible concept. No case covers the **PO-page** change-vendor per role for Office/Sales Rep, nor the wrong-atom (SFD vs VOM C&E) observation. | **FLAG-only** (no defect). Optional light **EDIT** to SF-PERM-12 / C30647 notes to record the NEW-1 wrong-atom observation for traceability. | SV-8183 / **§9.1** line "Assign vendor to a vendor-missing PO / merge / keep-separate … **Gated by Vendor & Order Mgmt: Create & Edit**"; **§9.4** atom-collapse. | FE correctly hides the control for both negative roles → **PASS per Rule 24**. The BE applying the looser SFD gate instead of VOM C&E is a spec-conformance note, but with the FE blocking it there is **no tester-facing behaviour gap** and no defect. Rule 25: the expectation ("VOM C&E gates vendor edit") IS in §9.1, but §9.4 anticipates BE non-enforcement, so it is a flag, not a bug. | Low |
| 2 | **NEW-2** — `part/make-request` (add part) + `parts/delete` (remove/cancel part) NOT BE-enforced for ANY role (400 for all 11), incl. Time Clock / Sales Rep. FE hides for negatives. | **Yes, conceptually.** SF-PERM-12 / C30647 already documents the identical Rule-24 pattern (FE hides part actions; API-possible; SV-7864 atom-collapse). SF-PERM-09 / C29413 covers the FE add-part gate for Technician. | **NO CHANGE** (FLAG-only). Optional one-line note in SF-PERM-12 / C30647 to extend the flag to add/delete endpoints. | SV-8183 / **§9.1** "Add a vendorless / no-part-number part … WO Lines: C&E + SFD"; "…removing follows WO Lines: Create & Edit"; **§7864** atom-collapse (Done). | **PASS per Rule 24** — the same accepted enforcement model already captured by SF-PERM-12. FE hides these controls for the negative roles; BE non-enforcement is the spec-sanctioned collapse (§9.4). No new tester-facing behaviour. | Low |
| 3 | **Resolve-cores per role** — Foreman drives the wizard to the Resolve-cores step (OK·Returned / Not OK·Keep+Charge enabled); **Time Clock has NO Complete button → wizard unreachable**; `pre-resolve-cores` BE not enforced (400/201 for all) = **SV-8541**, held per user. | **Yes (FE gate).** The FE gate = the completion gate, already covered by SF-PERM-10 / C29414 (Time Clock cannot Complete → cannot reach the wizard). SF-REV-14 / C29399 covers cores functionally. No per-role permission-negative on core-resolution — but that is the **held SV-8541** angle. | **NO CHANGE** now (FLAG-only). Revisit ONLY if Sasha rules SV-8541 requires BE enforcement. | SV-8541 / SV-8183 — **§9.1** "Resolve inventory / special-order cores (Ok/Not OK) … **WO Lines: Create & Edit**"; **§9.2** Time Clock = "No"; **§9.4** collapse caveat. | The should/should-not FE split is already implied by SF-PERM-10's completion matrix; BE non-enforcement is **PASS per Rule 24** and is the **held SV-8541** (user: "ignore for now" / Open for Sasha). Authoring a case now would pre-empt the PO/dev ruling. | Low (deferred to SV-8541) |
| 4 | **Return flow per role** — Foreman reaches `/parts/returns` → Create Return; **Time Clock route-BLOCKED + `inventory/returns/create` → 403 ENFORCED** (also 403 for Sales Rep / Tech). | **No** dedicated per-role return case. SF-PERM-12 / C30647 notes "only Return appears" on the Time-Clock part-**menu** (different surface). | **FLAG-only.** Optional future API regression case if the user wants the returns gate locked. | SV-8183 — **spec-SILENT**: §9.2 has **no "Return" column**; there is no dedicated return/credit atom in §9.1. | Rule 25: there is **no spec wording** to deviate from — rerun2 §1 explicitly flags this "spec-silent, not a deviation." The BE gate is well-behaved (blocks the negatives, 403) → no exposure, **PASS**. Outside the documented §9.2 matrix, so no required coverage. | Low |
| 5 | **Receive / `accept` BE enforcement** — 11 roles: **400 (allowed) for the 7 "Yes" roles / 403 (blocked) for the 4 "No" roles (Office, Sales Rep, Tech, Time Clock)** — matches §9.2 EXACTLY (the best-behaved gate in the feature). | **Yes, at the negative + UI level.** SF-PERM-03 / C29407 drives "attempt to actually complete the receive" per role (both entry points); SF-PERM-11 / C30646 captures Office `accept` → 403. The full **11-role `accept` 403/400 matrix as one API assertion** is not locked in a single case. | **OPTIONAL EDIT** SF-PERM-06 / C29410 — add the Bulk-Receive `accept` per-role BE-enforcement result (403 for the 4 No roles / allowed for the 7 Yes) as a concrete assertion. **NOT a gap.** | SV-8183 / **§9.1** "Bulk Receive page … **Vendor & Order Mgmt: Create & Edit** … route gate `hasPartsPermissions`"; **AC** "BE accepts the OR of `ROLE_DELIVERY_CREATE_AND_EDIT` / `ROLE_WORK_ORDER_PART_CREATE` / `ROLE_WORK_ORDER_CREATE_AND_EDIT`". | Not a coverage gap — the negatives are already covered (SF-PERM-03 UI + SF-PERM-11 Office BE). Value is **regression protection** on the one endpoint that genuinely BE-enforces per role; SF-PERM-06 is the natural home (it is the API — BE-enforcement case). Purely additive. | Low |
| 6 | **Wording/status conflicts** — any existing case whose current wording/status now conflicts with a live finding. | **None found.** SF-PERM-01/03/05/06/09/10/11/12 + SF-REV-14 + SF-VEND-01..08 all read consistently with the live findings; the 3 non-permission Deviations (SF-SET-03, SF-RCV-05/07) are untouched by SV-8183. | **NO CHANGE.** | — | Every asserted behaviour (Office receive-blocked, per-PO button hidden, completion matrix, settings gate, cores-before-receive) matches the live re-runs. `accept` 403 for negatives is consistent with SF-PERM-03/11. No case asserts old/contradicted behaviour. | — |

---

## Summary — counts

- **NEW cases warranted: 0.**
- **Mandatory EDITs: 0.**
- **Optional EDITs (regression / traceability value only, no defect): up to 3** — (a) SF-PERM-06 / C29410 add the `accept` per-role BE matrix (Finding 5); (b) SF-PERM-12 / C30647 note extending the Rule-24 flag to NEW-1 (change-vendor wrong-atom, Finding 1) and NEW-2 add/delete endpoints (Finding 2). All metadata/notes-level, no status change.
- **FLAG-only (documented, no case): 3** — NEW-1 (F1), NEW-2 (F2 — also conceptually covered), return-per-role (F4, spec-silent).
- **Deferred (held pending a ruling): 1** — resolve-cores per-role BE (F3 = SV-8541, awaiting Sasha).
- **NO CHANGE confirmed: Findings 3 & 6**, plus every existing case verified consistent.

## Bottom line (plain English)

**No new test cases are needed, and no case edit is required.** The SV-8183 findings from the three exhaustive re-runs are **already captured** by the corrective cases pushed on 2026-07-24 (SF-PERM-11 / C30646 for SV-8515, SF-PERM-12 / C30647 for SV-8516) plus the tightening of SF-PERM-03 / C29407 (both Bulk-Receive entry points) and the existing SF-PERM-01/05/06/09/10 + SF-REV-14 coverage.

The remaining new observations — **NEW-1** (change-vendor via API for Office/Sales Rep, BE using the wrong atom), **NEW-2** (part add/delete not BE-enforced), and the **resolve-cores** (SV-8541) BE non-enforcement — are all **FE-blocked + BE/API-allowed = PASS under Standing Rule 24**, i.e. accepted-for-now, **not defects**. They warrant a **documented flag** (and SV-8541 stays **held pending Sasha's ruling**), not a case.

The only genuinely additive opportunity is **optional**: extend **SF-PERM-06 / C29410** to lock the per-role `accept` (Bulk-Receive) BE-enforcement matrix (403 for the 4 "No" roles / allowed for the 7 "Yes"), since that is the single endpoint in the feature that BE-enforces exactly per §9.2 — pure regression insurance, not a gap. And optionally add the NEW-1/NEW-2 flags to SF-PERM-12's notes for traceability.

**Decision for the user:** proceed with **no case changes** (the findings are covered), OR authorize the optional low-priority regression EDIT to SF-PERM-06 (+ the NEW-1/NEW-2 traceability notes on SF-PERM-12) if you want the accept-enforcement matrix and the two Rule-24 flags explicitly recorded in TestRail. No FE-exposure defect and no true FE-allows+BE-allows gap was found beyond the already-captured SV-8515.
