# Simple Flow — Spec Diff & Assessment (new 2026-07-10 doc vs V2.4 baseline)

> **Project:** Simple Flow ONLY (Epic SV-7301, PO = Milos, app `sv7301.qa.shopview.com`).
> **New input:** `b91efa39-SimpleMode_StreamlinedWorkOrderCompletionBulkReceiving_1.doc`
> (Confluence MHTML export, upload dir `dd1d42ba-…`, doc Date header
> **Fri, 10 Jul 2026 16:28**). Extracted → `spec-source-2026-07-10.md`.
> **Baselines compared:** `requirements.md` (our authoring baseline = V2.4 +
> 2026-07-08 design delta + SV-8183 §9) and the prior spec upload
> `991acdb4-…BulkReceiving.doc` (Jul-9), which produced `spec-latest-source.md`.
>
> **Scope: INGEST + DIFF + PROPOSE ONLY.** This worker did **NOT** edit
> `requirements.md`, `cases/*.json`, `PROJECT-STATE.md`, or any deliverable
> (another worker owns live VIU + `cases/*.json` `viu_status` right now).
> Everything in §3 is a **proposal** for that worker.

---

## 1. Version verdict — NEWER CONTENT, same "V2.4" label (a silent revision)

| Signal | Prior baseline (991acdb4, Jul-9) | New doc (b91efa39, Jul-10) |
|---|---|---|
| Version string | `Draft for build — V2.4` | `Draft for build — V2.4` (unchanged) |
| §9 Change-Log last row | 2026-07-08 @Milos Vasic | **2026-07-08 @Milos Vasic (unchanged — no new row)** |
| Status-line "in sync with Jira" | …SV-7876 | …SV-7876 **+ SV-8183** |
| Actual body content | — | **differs — real additions/edits (see §2)** |

**Verdict: NOT the same doc re-uploaded, and NOT a formal version bump either.**
The version STRING and the change-log are frozen at V2.4 / 2026-07-08, but the
body genuinely changed: it **folds the SV-8183 permissions story into the spec
doc**, adds two receive-gate requirements (S13-R6/R7), changes the Story-4
unapproved-line behaviour, drops VIN from the Story-4 completion modal, and drops
the "optional note" from Mark Reviewed. Treat it as a **silent revision of V2.4**
that is content-newer than the Jul-9 upload. Per the Simple Flow last-update-wins
rule it is the most recent spec input as of 2026-07-10.

Diff method: both docs re-parsed with the identical extractor and `diff`'d, so all
pure spacing/bold artifacts (e.g. `DefaultON`→`Default ON`, `S2-R1—`→`S2-R1 —`)
were isolated and are **ignored** below.

---

## 2. Substantive deltas vs our baseline

Two buckets: **(A) already in our baseline** — the doc merely catches the spec doc
up to material we ingested separately; **(B) genuinely new vs our baseline** —
material we have NOT yet reflected in `requirements.md`.

### 2A. Already in our baseline (no new action — spec-doc catch-up)

- **NEW "Permissions — Enforcement Mapping (SV-8183)" section** inserted between §7
  and §8: the action→atom map, the 11-role × 10-action per-role matrix, the four
  footnotes, the **BE atom-collapse** caveat (`woOrderParts` /
  `workOrderLinesCreateAndEdit` / `woFullViewMode` / `woTechViewMode` /
  `workOrdersCreateAndEdit` all → `ROLE_WORK_ORDER::VIEW + CREATE_AND_EDIT`), and
  the **NET-NEW reviewer ≠ completer** hard rule.
  → **We already ingested all of this from `SV-8183.doc` into `requirements.md`
  §9 (9.1 map, 9.2 matrix, 9.3 new-vs-reused, 9.4 BE-vs-FE).** The new spec doc
  just merges SV-8183 into the spec body. **No new requirement.** (Status-line
  `+ SV-8183` is the cosmetic marker of this merge.)

### 2B. Genuinely new vs our baseline (proposal actions in §3)

- **Δ1 — S4-R3: VIN removed from the required-invoice completion modal.**
  Baseline (`requirements.md` L385) & Jul-9 doc: *"Required vehicle fields
  (mileage + VIN + engine hours)."* New doc: *"Required vehicle fields (mileage +
  engine hours, **when missing**); tech story separate (Story 17)."* **VIN dropped**
  from the Story-4 completion modal and a **"when missing"** qualifier added.
  (Consistent with VIN now being captured by the reviewer in Story 16, but this is
  a real change to the Story-4 completion-modal field set.)

- **Δ2 — S4-R8: unapproved-line behaviour changed to DISABLED button + tooltip.**
  Baseline (`requirements.md` L395) & Jul-9 doc: unapproved line → the existing
  *"you need to approve the line to complete the work order"* **error**, CTA stays
  active (no new disabled state). New doc **for Story 4 only**: *"the Complete Work
  Order button is **disabled, with a tooltip describing the reason (which line
  needs approval)**; it enables once all lines are approved."* Both S4-R8 and its
  acceptance-criteria line changed.
  **IMPORTANT — flow divergence:** this new disabled-button+tooltip model applies
  to **Story 4 (PO + required-invoice) only**. Stories 3, 2 and 16 still use the
  *error-message / CTA-stays-active* model (their S3-R9 / R11 text is unchanged).
  So Story-4 completion now behaves differently from Stories 3/16 on this gate.

- **Δ3 — NEW S13-R6 + S13-R7 (receive-time part# / cost-sell gates).** Story 13
  (Assign Vendor + Merge, at Accept-Delivery) gains two brand-new requirements:
  - **S13-R6 — Part number required:** if missing, user is prompted; **receiving is
    blocked until it's filled.**
  - **S13-R7 — Cost / sell price required:** if missing, user is prompted;
    **receiving is blocked until it's filled.**
  Plus two matching acceptance-criteria lines. Our `requirements.md` Story 13 only
  has S13-R1…R5 — **R6/R7 are absent.**

- **Δ4 — Mark Reviewed dialog: "optional note" removed.** Baseline
  (`requirements.md` L560-561) & Jul-9 doc R7: *"dialog captures VIN (required if
  missing) **+ optional note**; Confirm disabled until VIN."* New doc R7 (and the
  Story-16 acceptance line): *"dialog captures VIN (required if missing); Confirm
  disabled until VIN."* — the **optional note field is dropped**.

**Unchanged (explicitly checked, NOT deltas):** §5 calc/data-integrity contract;
**S5-R1 $0/sell-mandatory-at-save rule (byte-identical)**; §8 Open Questions list
(verbatim identical — see §4); Story 6 S6-R7 orderable-from-line; editable cost on
Accept-Delivery (S10/S12); core-resolution placement; first-use defaults
(auto-approve OFF / create-POs ON / vendor-invoice REQUIRED).

---

## 3. Affected SF-* cases — PROPOSAL ONLY (do not edit cases/*.json)

> Case IDs sourced from `cases/*.json`. These are **proposed** expected-result /
> precondition edits for the worker who owns the cases. Nothing edited here.

**Δ1 (S4-R3 VIN dropped from Story-4 modal):**
- **SF-COMP-16** — "optional-invoice modal collects required vehicle fields" →
  drop VIN from the required set for the Story-4 modal; keep mileage + engine hours
  ("when missing"). *Expected-result change.*
- **SF-VAL-02** — "completion blocked when a required VIN is missing" → re-scope:
  VIN is no longer a Story-4 completion-modal blocker; VIN enforcement now lives at
  Mark Reviewed (Story 16, SF-VAL-07 / SF-REV-06/10). *Expected-result change /
  possible re-scope.*
- Sanity-check (likely already correct, no change): **SF-REV-03** ("Details step
  collects only mileage and engine hours") already matches the new wording;
  **SF-UX-02** (centralized required-fields modal) — verify it doesn't assert VIN.

**Δ2 (S4-R8 disabled-button+tooltip, Story-4 only):**
- **SF-COMP-21 / SF-COMP-22** — unapproved-line-blocks-completion → for the
  **Story-4 required-invoice flow** expected result becomes *Complete button
  disabled + tooltip naming the line*, not the error toast. *Must specify which
  flow* — Stories 2/3/16 keep the error-toast/active-CTA model. *Expected-result
  change + flow disambiguation.*
- **SF-VAL-11** — "approve-line error text appears when completing" → still valid
  for Stories 2/3/16; add a Story-4 variant (disabled + tooltip) or note the split.
- **SF-REV-13** — "all lines approved to Send to Review" (Story 16) → **unchanged**
  (Story 16 still uses the error model); confirm no accidental edit.
- **SF-PERM-02** — roles-can-complete → unaffected by the UI-affordance change.

**Δ3 (new S13-R6/R7 receive gates):**
- **SF-VEND-04** — "assign a different vendor, no collision, auto-assign" → add
  the S13-R6/R7 preconditions (part# + cost/sell must be present before Receive
  enables on the assign-vendor group). *Precondition + expected-result addition.*
- **SF-VAL-06** — "vendor-missing part cannot be received without both [vendor +
  …]" → extend to cover **part# AND cost/sell** required at receive (S13-R6/R7).
- **SF-RCV-06** — "Accept Delivery receive gates: vendor set, missing part…" →
  extend gate list with the S13-R6/R7 part#/cost-sell blocks. *Expected-result add.*
- **SF-PNFIX-05** — "part cannot be received without a part number" → now has an
  explicit spec anchor (S13-R6) at the assign-vendor/receive surface; align wording.
- Consider a **new case** for S13-R7 (cost/sell required to receive) if not cleanly
  covered — flag for the case owner (proposal, not created here).

**Δ4 (Mark Reviewed optional note removed):**
- **SF-REV-06** — "Mark Reviewed dialog captures VIN (required) and [note]" →
  drop the note assertion. *Expected-result change.*
- **SF-REV-10** — "Mark Reviewed dialog includes VIN / Serial # (required)…" →
  remove any optional-note element. *Expected-result change.*
- Sanity: **SF-VAL-07** (Confirm disabled until VIN) unchanged and still correct.

**No case impact from 2A** (SV-8183 permissions) — those cases (SF-PERM-01…10,
SF-VMIS-*, SF-BULK-*, etc.) were already authored to the SV-8183 model now in
`requirements.md` §9.

---

## 4. Does the new doc answer any open Round-3 PO question or the $0-sell / SFD tension?

**Round-3 questions (`PO-Questions-Round3.md`): NONE newly resolved.** §8 Open
Questions in the new doc is **verbatim identical** to the Jul-9 doc — all six
Round-3 items still stand. Per-question:

- **Q1 (require-review default on for bigger/existing shops):** unchanged
  (S1-R4 still "default per cohort — see §8"). **Not resolved.**
- **Q2–Q4 (auto-approve default / navigation-warning / Waiting-on-Parts order):**
  no relevant change. **Not resolved.**
- **Q5 ($0 sell price at completion — allow vs block):** **NOT resolved.** S5-R1
  (sell mandatory at save) is **byte-identical** to baseline — the doc adds no
  completion-time $0-sell rule, so the design-vs-spec tension recorded in
  PROJECT-STATE §5.F.2 (screenshot "$0.00 sell, no action needed" vs
  "sell mandatory at save"; and Milos-R2-Q4 "sell NOT enforced on the request
  form") is **unchanged**. *Caveat:* the new **S13-R7** does add a *receive-time*
  cost/sell-required gate — a different surface from Q5 (completion). It is
  relevant context but does **not** answer whether completion may finish at $0.
  **Keep Q5 on the Milos sheet.**
- **Q6 (who may add a vendorless part — See-Financial-Data gate vs any WO-editor):**
  **NOT newly resolved.** The new doc's permissions table *asserts option A*
  ("Add a vendorless / no-part-number part … WO Lines: C&E **+ See Financial
  Data**"; footnote 2: Technician lacks SFD → cannot add) — **but this is the
  unchanged SV-8183 content already in our `requirements.md` §9**, and it
  **conflicts with Milos R2 Q4** (which overturned the mandatory-sell premise the
  SFD gate rests on). The doc does **not** acknowledge or reconcile that conflict,
  so the tension in PROJECT-STATE §5.F.3 stands. **Do NOT drop Q6** — if anything,
  flag it as a spec-vs-answer contradiction (last-update-wins: this Jul-10 doc
  reasserts A, but its §5/§9 predate/ignore the Jul-9 R2 answer).

**§8 questions the permissions section claims to resolve** (completion / bulk-
receive / settings / review roles / cost-at-completion / BE-enforcement): those
were **already resolved in our baseline via SV-8183 §9** — no new PO decision is
unlocked or closed by this upload.

**Bottom line:** send the Milos Round-3 sheet **as-is**; this upload answers none
of it and does not moot the $0-sell (Q5) or SFD-gate (Q6) tensions.

---

## 5. Recommended follow-ups (for the coordinator / case owner — not done here)

1. Apply the four Δ (§3) as expected-result/precondition edits once the live-VIU
   worker releases `cases/*.json`.
2. When `requirements.md` is next editable, add S13-R6/R7, update S4-R3 (VIN),
   update S4-R8 (Story-4 disabled+tooltip, note the flow split), and drop the
   Mark-Reviewed optional note — citing this Jul-10 doc.
3. Record in PROJECT-STATE that V2.4 was silently revised on 2026-07-10 (no
   version bump / no change-log row) — future uploads under the same label may
   also carry uncatalogued edits, so always diff, never trust the version string.
