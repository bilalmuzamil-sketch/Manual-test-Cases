# Simple Flow (Simple Mode) — RESUME STRATEGY

## 0. TWO-PHASE FINALIZATION PLAN (import files are INTERIM)

The TestRail import files already on disk are **interim, not final**:
- `testrail-import/simple-flow-v1-testrail-import.csv` (+ `.xlsx`) — all 156 cases,
  generated by `build/simple-flow/gen_import.py`. They are **VIU-word-free and
  feature-flag-free by user rule** (internal `viu_status`/`notes`/`design_ref` are
  not emitted). Note Simple Flow is **settings-driven, not flag-driven**, so genuine
  functional settings preconditions (e.g. "Auto-approve Lines is ON", "Require Review
  Before Completion is enabled") are legitimate and are KEPT. Sections are leaf names
  (no "Simple" prefix); References carry the Jira story id(s) (SV-####) plus the
  spec-rule reference.

Finalize in TWO phases:

1. **Phase 1 — VIU when the env is ready.** The feature is under development, so
   Stories **7 / 8 / 9 / 14 are NOT built** and role-gating negatives are unverified
   (on the sv7301 QA env only the **admin** quick-login works — `{key:'tech'}`
   returns 403, so non-admin/role-gating cannot be verified there). Once the feature
   is fully built and a non-admin session is available, run VIU against the live app,
   then **REGENERATE both deliverables** by re-running `gen_import.py` (rebuilds the
   TestRail import CSV **and** the Excel workbook in one pass). Fix any case where
   live behavior differs from the spec (see `viu-findings.md` for the current VIU
   deviations/bugs).
2. **Phase 2 — dev answers.** The open questions (see below and `requirements.md §8`)
   get sent to the user → dev answers → folded back into the cases, then
   **regenerate the import + workbook again**. Only the regenerated post-VIU +
   dev-answered files are the FINAL TestRail import.

Do NOT write to TestRail without explicit user permission at either phase.

---

## 1. OPEN QUESTIONS BLOCKING FINALIZATION

### Permissions matrix (the big one — see `build/PERMISSIONS-ASSESSMENT.md`)
Simple Flow has **no permissions/role matrix** (spec §8 explicitly lists it as
UNRESOLVED). These role-gating cases carry a best-defensible functional expected
today, but need the matrix to become definitive:
- **SF-PERM-02** — which roles can complete a work order (Simple completion).
- **SF-PERM-03** — which roles can perform Bulk Receive.
- **SF-PERM-04** — which roles can Mark Reviewed (review sign-off).
- **SF-PERM-07** — whether review role-gating uses custom roles or is open for v1.
- Related, spec-worded but matrix must formalize: **SF-RCV-03 / SF-PERM-05**
  (Story 11 "hidden for office/readonly"), **SF-REV-09** (Story 16 "manager/foreman
  only"). The **Story-16 review sign-off gating (R7) is specifically undefined**.

### Other spec/design open items (from §8 + completeness gaps)
- Require-review **default** cohort rule + new-org preset (SF-REV-15).
- **BE enforcement** of Simple-Flow settings — enforce or front-end only (SF-PERM-06).
- **Cost at completion** to avoid $0-cost margins (SF-QB-06).
- Close-confirm modal design pending (SF-UX-04); tech-story placement (Story 17 vs
  older S15-R2); default-value conflict spec vs design (settings first-use defaults);
  spec-version drift (doc V2.3 vs design V1.4).

---

## 2. QUICK-REFERENCE (non-secret; detail in CLAUDE.md)
- **QA env:** app `https://sv7301.qa.shopview.com`; API `https://sv7301api.qa.shopview.com`.
- **Auth:** `POST /api/quick-login {key:'admin'}` (tech = 403 here); cookies in `/tmp` only.
- **Settings-driven, NO feature flag.** WO settings `/administration/settings` → Work
  Orders tab. `GET /api/organizations/settings`; save `POST /api/organizations/settings/change`.
- **NOT built yet:** Stories 7, 8, 9, 14.
- Story ↔ Jira: S1 SV-7696 … S15 SV-7710, S16 SV-7870, S17 SV-7876.
- Case IDs `SF-<AREA>-NN`; org `d55bc308-…`; VIU tools `/tmp/simple-flow/tools/`.
