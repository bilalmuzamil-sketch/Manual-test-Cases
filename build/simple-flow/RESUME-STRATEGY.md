# Simple Flow (Simple Mode) — RESUME STRATEGY

> **SV-8183 APPLIED (2026-07-07).** Permissions are now **DEFINED** (Simple Flow
> reuses existing Custom Roles atoms — no new atom — plus the NET-NEW
> reviewer≠completer rule; see `requirements.md §9`/`§10` and
> `SV-8183-permissions-source.md`). All role-gating cases were rewritten to the
> SV-8183 matrix; **3 new cases added** (SF-PERM-08 reviewer≠completer,
> SF-PERM-09 Technician-can't-add-vendorless, SF-PERM-10 per-role completion
> matrix) → **159 cases total** (was 156). Deliverables **regenerated**
> (workbook + TestRail import + `.xlsx`). The Open Questions tab now marks the 3
> SV-8183-resolved items RESOLVED and keeps 11 still open. A clean shareable
> `OpenQuestions-for-Milos.md`/`.xlsx` (11 questions, generated by
> `gen_milos_questions.py`) captures what is still **pending Milos** (Product
> Owner). Role-gating negatives remain VIU-Pending (admin-only session on
> sv7301). The two-phase plan below still stands.

## 0a. UNBLOCK → UPDATE LOOP (source of truth = Blockers Tracker) — 2026-07-07

The package is now built for an iterative loop:
1. User uploads `testrail-import/simple-flow-v1-testrail-import.csv` (all 159).
2. TestRail assigns Case IDs → user exports the id map (or authorizes a read-only
   API pull) into `build/simple-flow/testrail-id-map.csv` (`sf_id,testrail_case_id`).
3. As each blocker clears, flip the case's `viu_status`/`expected` in `cases/*.json`,
   re-run `gen_blockers.py`, then `gen_update.py <cleared SF ids>` → an ID-matched
   UPDATE CSV (`testrail-import/simple-flow-UPDATE.csv`; XML on request).
4. User imports the UPDATE file (update-existing, matched on ID). Repeat.

**`SimpleFlow_Blockers_Tracker.xlsx`/`.md` is the source of truth.** Current
counts (of 159): READY 35 · DEV-NOT-BUILT 25 · VIU-PENDING (QA) 78 · MILOS 15 ·
BUG/RULING 6. See `UPDATE-LOOP-README.md` for the full process and `bugs-log.md`
for the 3 new BE-enforcement bugs (BUG-5/6/7).

**What to send next to unblock each batch:**
- Milos's answers to the 11 Open Questions → unblocks the 15 MILOS cases.
- Dev deploys Story 8 → 12 cases; Story 7 → 6; Story 9 → 4; Story 14 → 3.
- Fresh sv7301 cookies (admin+tech) + seeded data → bulk of the 78 QA-pending.
- A 2nd/3rd role account (Office/Service Manager/Foreman, some without See
  Financial Data) → SF-PERM-09, SF-PERM-10.
- A dev/PO ruling on FE-only enforcement + reviewer≠completer → the 6 BUG/RULING.

## 0. TWO-PHASE FINALIZATION PLAN (import files are INTERIM)

The TestRail import files already on disk are **interim, not final**:
- `testrail-import/simple-flow-v1-testrail-import.csv` (+ `.xlsx`) — all 159 cases,
  generated by `build/simple-flow/gen_import.py`. They are **VIU-word-free and
  feature-flag-free by user rule** (internal `viu_status`/`notes`/`design_ref` are
  not emitted). Note Simple Flow is **settings-driven, not flag-driven**, so genuine
  functional settings preconditions (e.g. "Auto-approve Lines is ON", "Require Review
  Before Completion is enabled") are legitimate and are KEPT. Sections are leaf names
  (no "Simple" prefix); References carry the Jira story id(s) (SV-####) plus the
  spec-rule reference.

Finalize in TWO phases:

1. **Phase 1 — VIU when the env is ready.** The feature is under development, so
   Stories **7 / 8 / 9 / 14 are NOT built** and role-gating negatives are unverified
   (on the sv7301 QA env only the **admin** quick-login works — `{key:'tech'}`
   returns 403, so non-admin/role-gating cannot be verified there). Once the feature
   is fully built and a non-admin session is available, run VIU against the live app,
   then **REGENERATE both deliverables** by re-running `gen_import.py` (rebuilds the
   TestRail import CSV **and** the Excel workbook in one pass). Fix any case where
   live behavior differs from the spec (see `viu-findings.md` for the current VIU
   deviations/bugs).
2. **Phase 2 — dev answers.** The open questions (see below and `requirements.md §8`)
   get sent to the user → dev answers → folded back into the cases, then
   **regenerate the import + workbook again**. Only the regenerated post-VIU +
   dev-answered files are the FINAL TestRail import.

Do NOT write to TestRail without explicit user permission at either phase.

---

## 1. OPEN QUESTIONS

### Permissions matrix — RESOLVED by SV-8183 (DEFINED)
Simple Flow **no longer** has a missing permissions matrix. SV-8183 maps every
action to an existing Custom Roles atom (no new atom) and supplies a per-role
matrix (`requirements.md §9`). Expected results now stated on: **SF-PERM-01..10**,
**SF-SET-11**, **SF-RCV-03**, **SF-REV-09**, **SF-VPART-01/02**, **SF-QB-06**.
Key: Settings = App Settings (Admin/Service Manager/Office); Complete = Work
Orders C&E + WO Lines C&E/Full View; Bulk Receive = Vendor & Order Mgmt C&E + See
Financial Data; Receive-on-WO = Order Parts (FE); Mark Reviewed = Review Work
Orders + reviewer≠completer; vendorless add = WO Lines C&E + See Financial Data.
BE **does** enforce settings + atoms (atom-collapse caveat: any WO C&E role can
receive). These are authored to spec but **role-gating negatives are still
VIU-Pending** (admin-only session on sv7301).

### Still-open questions — PENDING MILOS (Product Owner)
See the shareable `OpenQuestions-for-Milos.md`/`.xlsx` (11 questions). NOT
resolved by SV-8183:
- Require-review **default** cohort rule + new-org preset (SF-REV-15).
- **Auto-receive** of in-stock parts on simple completion (SF-COMP-07, SF-QB-01).
- Settings first-use **default conflict** spec vs design (SF-SET-08).
- Spec **V2.3 vs design V1.4** drift (global).
- 4 build deviations: no Create-POs toggle (SF-SET-03/COMP-06/QB-02); Save always
  enabled (SF-SET-13); Mark-Reviewed missing note (SF-REV-10); review→Complete
  jump / no distinct Reviewed state (SF-REV-08/REV-11).
- Tech-story placement Story 17 vs S15-R2 (SF-TECH-*); close-confirm design
  pending (SF-UX-04); Accept-Delivery vendor-missing ordering (SF-RCV-05/07).

---

## 2. QUICK-REFERENCE (non-secret; detail in CLAUDE.md)
- **QA env:** app `https://sv7301.qa.shopview.com`; API `https://sv7301api.qa.shopview.com`.
- **Auth:** `POST /api/quick-login {key:'admin'}` (tech = 403 here); cookies in `/tmp` only.
- **Settings-driven, NO feature flag.** WO settings `/administration/settings` → Work
  Orders tab. `GET /api/organizations/settings`; save `POST /api/organizations/settings/change`.
- **NOT built yet:** Stories 7, 8, 9, 14.
- Story ↔ Jira: S1 SV-7696 … S15 SV-7710, S16 SV-7870, S17 SV-7876.
- Case IDs `SF-<AREA>-NN`; org `d55bc308-…`; VIU tools `/tmp/simple-flow/tools/`.
