# TestRail Run-Sync — 2026-08-20 (Standing Rule 34 + Rule 47)

**Scope:** the THREE ACTIVE runs ONLY — Report Suite run **359** (group 4281),
Schedule run **357** (group 4254), Filters run **352** (group 4110). No other run touched.
**TestRail-API only** (Basic auth). **Case-membership sync only — ZERO result writes** (Rule 6:
results belong to the run owners). Method: audit (read-only) → snapshot → UNION `update_run`
(add-only, never partial) → by-id verify. Tooling: `sync_2026_08_20.py` (adapts the proven
`build/testrail-run-sync-2026-07-31/` checker + executor to the union-only, snapshot-first,
by-id-verify discipline). Our ACTIVE cases = `created_by == 3` under the group; foreign cases
(`created_by != 3`) excluded from the gap (Rule 38) but PRESERVED (the union keeps every current
case_id).

Authorized by the QA lead ("update the test runs if that is also pending").

## Summary

| Run | Project | include_all | Before tests | Gap added | After tests | Result records (before→after) | Verify |
|---|---|---|---|---|---|---|---|
| 359 | Report Suite | False | 508 | 1 (C43984) | **509** | 535 → 535 | VERIFIED |
| 357 | Schedule | False | 195 | 0 | 195 | 553 (untouched) | already complete |
| 352 | Filters | False | 129 | 0 | 129 | 648 (untouched) | already complete |

**Total new tests added across all three runs: 1** (run 359, C43984).

## Run 359 — Report Suite (group 4281)

- `include_all` = **False**, not completed. Owners: Nebojsa & Viktoria.
- Our active cases under group 4281 = **509**; foreign = 14. Before: **508** tests in the run.
- **GAP = 1: C43984** — "Long summary-figure and column labels wrap to a second row, no
  truncation" (section 4361 *WIP — Visual & Accessibility*, under 4286 → group 4281,
  `created_by=3`). This is the newly-created WIP case that had not been synced into the run.
- **UNION written:** `sorted(current 508 | {C43984}) = 509`. `update_run/359` → **HTTP 200**.
- **VERIFY (Rule 50, exhaustive + exact, by id):**
  - after test count = **509** (= expected union).
  - case-id set **equal both directions** (union == live run case_ids).
  - result records **535 → 535**; **0 prior results missing by id**; **0 graded `status_id`
    changed**.
  - C43984 now present as a test (gap-present 1/1).
- Snapshots: `snapshots/PRE-run-359.json`, `snapshots/POST-run-359.json`.

## Run 357 — Schedule (group 4254)

- `include_all` = **False**, not completed. Owner: Ayesha.
- Our active cases = **195**; foreign = 0. Run holds **195** tests. **GAP = 0.**
- **Already complete — no write.** 553 result records untouched.
- Snapshot: `snapshots/PRE-run-357.json`.

## Run 352 — Filters (group 4110)

- `include_all` = **False**, not completed. Owner: Ahtasham.
- Our active cases = **124**; foreign = 5. Run holds **129** tests
  (124 ours + 5 foreign C43576–C43580, which are kept). **GAP = 0.**
- **Already complete — no write.** 648 result records untouched.
- Snapshot: `snapshots/PRE-run-352.json`.

## Result-preservation proof

Only run 359 was written. Its 535 prior result records were all matched **by result id** in the
post-write snapshot (0 missing, both directions set-equal) with **0 graded `status_id` changes**;
the update was add-only (union of the full current selection plus C43984). Runs 357 and 352 had a
zero gap and received no write, so their 553 and 648 result records are untouched.

Machine record: `audit.json` (this directory).
