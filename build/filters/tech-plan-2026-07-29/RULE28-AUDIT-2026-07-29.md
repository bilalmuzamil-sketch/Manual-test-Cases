# Filters tech-plan pass — Rule-28 Three-Dimension Audit (2026-07-29)

Scope: 100% of the cases TOUCHED by the tech-plan reconciliation — 15 new cases
+ the edited cases (per Rule 28 the audit gates this authoring pass; the full-suite
audit re-runs at the next major reconciliation, i.e. the spec-v1.3 pass).

## Dimension 1 — USEFUL (verdict per new case)

| Case | Verdict | Why it earns its place (failure = real reportable bug) |
|---|---|---|
| FLT-TAB-06 | KEEP | Landing-tab default + last-used-tab memory; regression = wrong landing view for every user |
| FLT-STAT-07 | KEEP | Imported exclusivity; regression = broken/misleading combined filtering on a separate data source |
| FLT-ASSET-07 | KEEP | "No" filtering is a brand-new backend path engineering explicitly flags for verification |
| FLT-URL-05 | KEEP | Shared-link visit silently overwriting saved filters would destroy users' saved state |
| FLT-PERS-05 | KEEP | Per-view/per-tab scoping; regression = filters bleeding across Parts views/report tabs |
| FLT-PERS-06 | KEEP | Release-critical one-time migration; failure = every existing user loses saved filters |
| FLT-RPTS-23 | KEEP | New chip TYPE used by nearly every report; one case pins its whole contract |
| FLT-PSRCH-01 | KEEP | The new page-search component's states + as-you-type behavior (blur rules deliberately MERGED in — not authored as a separate case) |
| FLT-PSRCH-02 | KEEP | Search+filters AND-combination and independent clearing — the core interaction contract |
| FLT-PSRCH-03 | KEEP | Query persistence incl. the no-match-restore rule (S10-N2) — easy to regress |
| FLT-PSRCH-04 | KEEP | Search in the shareable URL + malformed handling (mirrors the filter URL set, distinct subject) |
| FLT-PSRCH-05 | KEEP | Mobile inline expansion + D21 toolbar restructure — visible layout change on named pages |
| FLT-PSRCH-06 | KEEP | The Story-14 safety sweep: no list silently loses text search (tech-plan risk 9) |
| FLT-PSRCH-07 | KEEP | The removal itself: nav search must stop filtering lists app-wide |
| FLT-API-06 | KEEP | New endpoint contract incl. per-user isolation (a cross-user leak is a security bug) |

Slop patterns hunted, none found in this batch: no per-column/sort explosions; no
tooltip/empty-state splits (blur rules merged into FLT-PSRCH-01); FLT-ASSET-07 is not a
Yes/No mirror-slop pair — "No" is a new code path with its own risk; FLT-PSRCH-04 vs
FLT-URL-01..04 test different subjects (search term vs filters) on the same surface.

**Tally: 15 KEEP / 0 MERGE / 0 WEAK-KEEP / 0 CUT.**

## Dimension 2 — MAKES SENSE (cold read, 6 fail conditions)

All 15 new cases: steps executable in order from stated preconditions; expected follows
from steps; no internal contradictions; no control referenced outside the tech-plan/design
sources (engineering-pinned strings are quoted as "engineering copy" with a confirm-live
flag, never asserted as build truth); no domain nonsense; every case states what to DO and
what PASS looks like. Two cases carry deliberate softness, flagged inside the case rather
than hidden: FLT-PSRCH-02 expected 3 (does "Clear filters" also clear the search — S8-R5
text unseen) and FLT-PERS-06 (needs a pre-update browser profile — preparation instruction
included). Edited case FLT-PERS-02 re-read after the appended step 6/expected 3: numbering
continuous, no contradiction with the existing browser-close legs.

**Tally: 15 SENSIBLE / 0 FIX-WORDING / 0 NONSENSE.** (Edited cases: tester-facing text
changed only in FLT-PERS-02 — SENSIBLE; all other edits are QA-notes/metadata only.)

## Dimension 3 — GENUINE + LAYMAN-RUNNABLE

Every new case carries Rule-20 refs: Jira ticket **TBD** (Epic key still unavailable —
stated, not invented) + the tech-plan §anchor + the spec v1.3 S#-R# anchor where the tech
plan cites it (export awaited — refs to be upgraded to the ratified spec anchors at the
v1.3 reconciliation). Tester-facing wording is plain layman language (no endpoint names,
HTTP codes or jargon outside the API-section cases, per Rule 4/7/9); titles all ≤ 80 chars
(longest checked: 79). **15/15 genuine + layman-runnable.**

## Honest "is the critic right?" answer for this batch

- Waste: **0% of the 15 new cases** are slop by the named patterns; each pins one distinct
  observable behavior with a real failure mode.
- Makes-no-sense: **0%** on the cold read; the two soft spots are explicitly flagged
  inside the cases as confirm-live items, not silent gaps.
- The honest waste-RISK in the suite is pre-existing, not from this pass: **FLT-SRCH-01..08**
  (8 spotlight cases) very likely belong to the Global Search project per engineering
  (deltas C6) — they are now flagged for transfer/retire pending Branko's ruling rather
  than left to double-cover another suite's component.

## Caveats (Rules 12/22)

Everything in this pass is DESIGN/TECH-PLAN-LEVEL: nothing was live-verified (no QA build
exists yet — tech plan is at Phase 0). All 15 new cases are `viu_status: VIU-Pending`;
engineering-pinned strings carry confirm-live flags. This audit gates the authoring
quality only, not build conformance.
