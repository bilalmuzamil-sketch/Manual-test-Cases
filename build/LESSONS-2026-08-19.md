# Lessons from the SV-8815 run (19–20 August 2026)

The QA lead's instruction: *"learn from your mistakes i dont want you to repeat your mistakes and your
learning should help you in future testing"*, and *"once you learned save it with you in your rules/
playbook/skills/recipe … I do not want you to search for the same process from scratch again and
again."*

The reusable **recipes** went into `build/APP-ACTIONS-PLAYBOOK.md` (§T for this branch's mechanics,
**§U for the unblock ladder**, **§V for evidence**, **§W for the navigation map**). **This file is the
other half: the mistakes.** **Eleven** of them, each with the rule that prevents a repeat. They are
written bluntly on purpose — a lesson I soften is a lesson I will make again.

> ⚠️ **Read lesson 1 together with lesson 10.** Lesson 1's *conclusion* about QuickBooks was itself
> wrong and is superseded; its **rule** (read the state, not the decoration) still stands and is what
> lesson 10 sharpens.

---

## 1. I reported a cause I had inferred, and it was wrong

**What I did.** I reported *"QuickBooks is not connected on this branch"* as the reason fees and
discounts could not be added. I had inferred it from the QuickBooks admin page showing a
"Connect to QuickBooks" button.

**What I said instead.** `GET /api/bookkeeping/adjustment-item-mapping-status` returns
**`quickBooksConnected: true`**, so I declared QuickBooks connected with only the Fee and Discount
**item mappings** missing.

**⚠️ AND THAT REPLACEMENT WAS ALSO WRONG — see lesson 10.** Reading the whole state later showed
`products-and-services` → 400 *"Bookkeeping is not configured"*, `integration` → an unused Intuit OAuth
URL, and the admin page offering only a Connect button. **No QuickBooks company is attached**, so my
original sentence was closer to the truth than my correction was. What was genuinely wrong in the
original was the *method* — inferring from a button — not, as it turned out, the conclusion.

**Why it matters.** That sentence was in a document heading for a Jira comment. A developer would have
corrected me in public, and every other number in the report would then have been doubted.

> **RULE — read the STATE, never the decoration.** A page's rendering is a *symptom*. Before writing
> any "X is not configured / not connected / does not exist", find the endpoint or field that
> *states* it, and quote that. This is Standing Rule 12 applied to causes as well as results:
> **a cause I inferred is not a finding, it is a guess with a confident voice.**

---

## 2. I stopped at an API refusal instead of finding the control

**What I did.** `POST /api/work-orders/adjustments/add` returned 409, so I concluded fees and
discounts "cannot be added at all on this branch".

**The truth.** The control was sitting in the UI the whole time: work order → **⋮** →
**Add Work Order Fee / Discount**. The QA lead knew it and had to tell me. Only *then* did I find the
real gate.

**Why it matters.** I skipped the two cheapest rungs of the ladder — read the screen, ask the DOM —
and went straight to the API. The user should not have to be my rung 1.

> **RULE — "cannot be done" requires having looked at the SCREEN.** Before that phrase is allowed:
> enumerate `data-test-id`s and grep them, open the relevant menus, and check whether the control is
> present-but-disabled. An API refusal tells me one path is closed, never that the feature is absent.
> **And check whether the control is `disabled`** — that distinguishes "my click missed" from "the
> product is refusing", which are completely different investigations.

---

## 3. I guessed payloads for an hour where reading the bundle took minutes

**What I did.** Probed `inventory/orders/accept` and `part/make-request` with invented field names,
reading 400s and 500s, for a long time.

**The truth.** The deployed JavaScript is first-party source and it is *right there*. Grepping the
chunks gave me, in minutes: the guard endpoint and its exact boolean, and the working payload
`{work_order, line, description, quantity, part_source_type, part_category_id}` — whose field names are
**`work_order` and `line`, not `work_order_id`/`line_id`**. That single mismatch is what had made the
task look impossible.

**Also learned the hard way:** this API replies in **snake_case** about a body that actually wants
**camelCase**, so reverse-engineering the shape from its own error messages sends you the wrong way.

> **RULE — read the deployed bundle BEFORE probing.** Collect chunk URLs via `page.on('response')`,
> fetch with `credentials:'include'`, grep for the message / test-id / field name. **A lazily-loaded
> dialog's chunk only appears after the dialog is opened**, so open it first. Probing is rung 5, not
> rung 1.

---

## 4. Ordering bug in my own harness: I set up the labour type before pinning the location

**What I did.** Called `ensureLT()` (find/create the $1/hour labour type) before `ensureHD()` (pin the
active location). The session happened to be sitting on Lethbridge, so the labour type was created
*there*, and every `lines/change` against Heavy Duty then failed with *"Labor type not found for this
workplace"*.

**The cost.** Five exhibit work orders built and **invoiced** with wrong amounts before I noticed.
Invoiced work orders cannot be tidied away.

> **RULE — pin the environment FIRST, then create anything in it.** Location before labour types,
> canned lines, or work orders. And **assert the setup produced what was asked for** (`subOK`/`taxOK`
> checks in the same log line) so a bad batch stops at item one instead of item seven.

---

## 5. I captured a whole batch of screenshots against the wrong active location

**What I did.** Ran the capture pass while the session was still on Lethbridge. Eleven of twelve
captures came back with an **empty** Financial Info panel, because a work order only renders while its
own location is active.

**The tell.** One capture (the Lethbridge one) worked and eleven did not. A single working case in a
failing batch is a *configuration* signal, not a flaky-page signal.

> **RULE — same as 4, applied to reading: re-assert the location per target, not once per script.**
> The capture script now switches when the target's location differs, and logs the switch.

---

## 6. I nearly used non-invoiced work orders as evidence

**What I nearly did.** Planned to screenshot the original case-2 / case-3 / case-5b work orders as
proof of their tax figures.

**Why that would have blown up.** A work order that has **not** been invoiced **re-prices live**
against its location's *current* settings. Those work orders were built under tax models I later
changed. A developer clicking the link would have seen different numbers and concluded the report was
fabricated.

**What I did instead.** Rebuilt every exhibit and **invoiced** it, so each figure is frozen and will
still read the same tomorrow.

> **RULE — evidence must be FROZEN evidence.** Screenshot a state that cannot drift: an issued
> invoice, not a live estimate. And where a surface recomputes, say so on the exhibit.

---

## 7. My annotations covered the very numbers they pointed at — twice

**What I did.** First pass: labels placed over the Financial Info rows, hiding Subtotal and Tax. Second
pass on the stacked-rates exhibit: three labels 19px apart, overlapping each other, and boxes merged
into one blob. Both were caught only because I opened the rendered images and looked.

**A related miss:** the first two annotated images used coordinates I had **eyeballed off a scaled
screenshot**. They happened to land correctly. That was luck, and I rebuilt them from captured
geometry.

**And the one that would have cost the most:** the 2025-invoice exhibit. Its boxes were drawn at
`y≈2874`, inside an **inner scroll container** that a "fullPage" screenshot never reaches — so they
landed off-image entirely. Worse, the visible panel read **2,833.11 / 2,974.77** while my caption
claimed **2,833.13 / 2,974.79**, because that panel re-prices old invoices. Published as-is, that is
precisely the two-cent discrepancy a developer would have opened with.

> **RULES — four, all cheap:**
> 1. **Take real bounding boxes** (`getBoundingClientRect`) and drive the annotation from them. Never
>    eyeball coordinates from a scaled image.
> 2. **Scroll the target into view and re-read its rect** before shooting. A `fullPage` screenshot does
>    **not** capture content inside an inner scroll container.
> 3. **Place labels in empty space, spread by index** (a fixed x column, ~52–64px vertical steps) so
>    they can never cover the values or each other. Tight padding when rows are close together.
> 4. **OPEN EVERY RENDERED EXHIBIT AND LOOK AT IT.** Every single annotation defect above was invisible
>    in the code and obvious in the image. And **pre-empt the discrepancy**: if a screenshot contains a
>    number that appears to contradict the claim, explain it in the caption rather than leaving it for
>    a reviewer to "discover".

---

## 8. THE WORST ONE — I repeated a mistake that was already written down, and published it

**What I did:** reported in a public Jira comment that *"receiving a part returns HTTP 500 on this
branch"*, with a table of **six causes ruled out** and request ids, and wrote a repro doc offering to
file a ticket. **The QA lead then received a part successfully on S-15998 and sent the screenshots.**

**The actual fault:** there are **two Receive surfaces**. The live one is the **part row's Receive
button** → `POST /api/orders/receive-requested-parts` → **200**. I used **`/accept-delivery`**, which
still renders, still enables its button, and answers **500** because the product no longer routes a
work-order part request through it.

**Why this one is the worst:**

- **It is the same mistake I was corrected for on SV-8779**, and that correction was *already in the
  playbook*. Written down is worth nothing if it is only read **after** the mistake.
- **The "ruled out" table made it worse, not better.** Six confidently eliminated causes tell the next
  reader to stop looking. Every one of them was tested against the wrong screen, so the table was six
  rows of noise wearing the costume of rigour.
- **It went into a public comment** on a ticket that engineering and the PO read. A false claim about
  the product is exactly the thing the QA lead asked me to make unchallengeable.
- **I diagnosed the wrong variable twice.** From his screenshots I guessed the difference was ticking
  *Line Approved* or setting the **vendor in the New Part Request modal**; I tested both — against the
  dead screen — and both "failed", which felt like evidence and was noise.

**And the premise underneath was wrong too.** I had called this a *blocker* for the handoff's
credit-memo tax item. A **credit memo is not a part return**: `POST /api/credit-memos` takes
**`customer_account_id` and `amount`, nothing else** — no tax, no rate, no lines. One 30-second probe
would have shown there was **no tax in a credit memo to pro-rate**, which is a better answer than
"blocked" and was available on day one.

> **RULES:**
> 1. **BEFORE any app action, run the pre-action check — playbook §U.0.** Five questions, thirty
>    seconds. Question 2 is *"is there more than one surface for this action, and am I on the one the
>    product uses?"*, and this trap is **named in it** with both instances.
> 2. **Reach the action from the object's own row/page** and follow the button a user would press. A
>    route that renders is not proof it is the live route.
> 3. **Never publish a "ruled out" table without confirming the surface first.** Eliminating six causes
>    on the wrong screen is not rigour, it is six wrong answers.
> 4. **When a recipe in the playbook turns out to be wrong, CORRECT it — do not append.** T.8 spent a
>    day teaching that receiving was broken. It is rewritten, not annotated.
> 5. **Test the premise before testing the thing.** "I need X to test Y" deserves one probe of Y first.

---

## 9. The exhibit guard passed while every label was struck through

Five annotated exhibits, the collision guard green on all five — and every label had its own leader
line drawn straight through its text, because `annotate.py` always drew from the label's **right**
edge and these labels sat to the **right** of their values. The guard compares label and box
*rectangles*; it has no idea where the *line* goes.

> **RULE:** the guard is a **filter, not a sign-off** (playbook §V.5). **Open every image before it is
> published** — this is the second consecutive run where the defect was invisible in the code and
> obvious in the picture. `annotate.py` now picks the leader's origin edge from the label's position.

---

## 10. I corrected an inference with another inference, and got it wrong twice

**What I did:** reported *"QuickBooks is not connected"* (inferred from a Connect button), was told that
was wrong, and **corrected it to "connected, only the item mappings are missing" — on the strength of one
boolean**, `quickBooksConnected: true`. When I finally read the whole state, three other signals said the
opposite: `products-and-services` → 400 *"Bookkeeping is not configured"*, `integration` → an Intuit
OAuth URL still waiting to be used, and the admin page offering only a Connect button. **My first
statement was closer to right and my correction was the bigger error**, because it came with more
confidence and it changed a real conclusion: it turned an external dependency into a "one-step unblock"
that nobody could actually perform.

> **RULES:**
> 1. **A single field is not "reading the state".** Before asserting a system's condition, read every
>    signal that speaks to it and say what they *collectively* show — and when they disagree, **say which
>    one is the odd one out and why** (that disagreement was itself a reportable finding).
> 2. **A correction deserves MORE evidence than the claim it replaces**, not less. It arrives with
>    authority and it stops anyone looking again.
> 3. Playbook §T.7 now carries the four-signal table so nobody trusts that flag again.

---

## 11. A chain of corrections is the wrong shape for a Jira comment

I posted a QA result, then a correction comment under it, and offered to patch the original in place. The
QA lead's answer: *"Just post one new complete comment and I will delete the older comments"*, and
*"do not post it as you were wrong before and right now etc. Just post a comment which with all the
evidences proves that why this ticket is QA passed."*

**He is right, and the reason is about the reader.** A PO or developer opening a ticket needs the result:
what was tested, what it showed, what remains. A narrative of the tester's own revisions makes them
discount everything around it and buries the verdict — and a "correction below the original" arrangement
leaves the wrong version still readable on top.

> **RULES:**
> 1. **When findings change, rebuild ONE standalone comment** carrying the whole result, so the old ones
>    can be deleted and a single clean record remains. Playbook **§V.10**.
> 2. **A QA comment states the result, not the journey.** Self-review belongs in these lessons and in the
>    playbook, where it prevents recurrence; Jira gets the finding. This is not licence to soften a
>    verdict — the two real gaps and the open developer question were all stated plainly in that comment.
> 3. **Generate it with a script and lift reusable content out of the previous comment's ADF** rather than
>    retyping 30 rows and 14 captions; then **tone-gate the flattened text** and **read the posted comment
>    back** to check media count, order and tables. Playbook **§V.9**.

---

## The one-line version

**Rungs 1–4 of the ladder are nearly free; rungs 5+ are expensive — and rung 0 is free.** Every mistake
here was skipping a free rung (1, 2, 3), failing to assert my own setup (4, 5, 6), failing to look at my
own output (7, 9), **failing to read what I had already written down (8)**, **correcting an inference with
another inference (10)**, or **choosing the wrong shape for the deliverable (11)**. Number 8 is why the
playbook now opens §U with a **pre-action check** instead of only a post-mortem.

Cross-references: `build/LESSONS-2026-08-10.md` (the previous set), playbook **§U.0** (the pre-action
check), **§T.8** (the two Receive surfaces — corrected), **§U** (the ladder), **§V.4/V.5** (leader
direction and the guard's blind spot), **§S.11** (never conclude absence from a truncated read — the
same family as lesson 1).
