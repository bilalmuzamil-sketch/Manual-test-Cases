# SV-8182 — Bug re-test on staging

**Ticket:** https://shopview.atlassian.net/browse/SV-8182
**Type:** Bug (to re-test / confirm fixed on staging)
**Requested:** 2026-07-09
**Status: FETCH BLOCKED — ticket content NOT obtained. Awaiting user paste.**

---

## Fetch attempt log (2026-07-09)

The ticket could not be read. Every available method was tried once (no looping):

| # | Method | Result |
|---|--------|--------|
| 1 | Atlassian Jira MCP tools (via ToolSearch: `getJiraIssue`, `searchJiraIssuesUsingJql`) | **Absent.** "No matching deferred tools found." The Atlassian connector is not exposed to this Claude Code environment (it is only available in the user's chat surface). |
| 2 | WebFetch on the browse URL | **HTTP 403 Forbidden** (login-walled). |
| 3 | `curl -L` on the browse URL (agent proxy) | **HTTP 200 but useless** — returns only the Jira SPA JavaScript shell (`<title>Jira</title>` + bootstrap script), no ticket content. Login wall renders client-side. |
| 4 | `curl` Jira REST API `GET /rest/api/3/issue/SV-8182` (unauthenticated) | **HTTP 404** — `{"errorMessages":["Issue does not exist or you do not have permission to see it."]}`. This is the standard Atlassian response for an unauthenticated request; we hold no Atlassian SSO credentials (stored creds are TestRail-only). |
| 5 | Local export check — `/root/.claude/uploads`, `/tmp`, cached MCP tool-results | **None found.** No SV-8182 export attached; cached `mcp-Atlassian-*` tool-results are prior-session JQL searches and contain no SV-8182 issue detail. |

**Conclusion:** SV-8182 is login-walled / connector-absent in this environment.
We have **no ticket content** — no title, description, repro steps, or
expected-when-fixed behavior.

---

## What we need from the user to proceed

1. **Paste the SV-8182 ticket**, specifically:
   - **Title / summary**
   - **Description** (what the bug is)
   - **Steps to reproduce**
   - **Expected behavior when fixed** (so we know how to judge "fixed")
   - **Affected area / component** + any **environment/build** hints
2. **Fresh `app.staging.shopview.com` cookies** to test:
   `sv_sso_session` / `PHPSESSID` / `cf_clearance` (domain `.staging.shopview.com`).
   The prior cookies are long expired. Secrets go in `/tmp` only — never committed.

---

## Test plan (to be filled once ticket + cookies are supplied)

- **Env:** `app.staging.shopview.com` (SPA) / `api.staging.shopview.com` (backend).
  Auth via `POST /api/quick-login {key:'admin'|'tech'}`; see `build/TESTING-RUNBOOK.md`
  and `build/APP-ACTIONS-PLAYBOOK.md` for the harness + action recipes.
- **Approach:** reproduce the original bug's steps on current staging; compare
  observed behavior against the ticket's expected-when-fixed; record fixed /
  not-fixed with evidence.

## Findings

_Pending — not yet tested (no ticket content, no env access this task)._

## Verdict

_Pending._
