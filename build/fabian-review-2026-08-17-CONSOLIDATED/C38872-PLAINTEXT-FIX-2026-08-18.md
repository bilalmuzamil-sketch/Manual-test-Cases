# C38872 (SCH-API-01) — plain-text restoration execution log

**2026-08-18 · one authorized `update_case` · TestRail write · NO Jira · NO run writes · app NOT opened**

## Why
During the 2026-08-18 marker revert, C38872's three text fields were inadvertently re-encoded
plain→HTML (`<ol>/<li>`, `<hr/>`, `<a href>`, `&`→`&amp;`) — a rendering reformat with ZERO meaning
change (documented in `MARKER-REVERT-INDEPENDENT-VERIFY.md`). This project renders that markup as RAW
MARKUP to the tester, so the fields were restored to plain text. The marker was already correctly
restored by the revert and was KEPT unchanged.

## Baseline / target
- Pre-revert PLAIN baseline extracted from `git show 0d101722:build/schedule/cases/cases-H-tech-plan.json`.
- TARGET = baseline plain `preconditions`/`steps` + baseline plain `expected` BODY + baseline plain
  Rule-54 provenance line, with the marker line = the CURRENT LIVE (correct, already-restored) marker
  `AUTOMATION: HOLD - needs three separate sign-ins, one per permission level` (NOT the old deferred marker).
- TARGET proven byte-equal to the current HEAD local source (`cases-H-tech-plan.json`) for all three fields.
- Byte-semantic check: TARGET(minus marker line) == baseline(minus marker line) — 0 meaning change.

## Operation
| op | target | HTTP | byte-verification |
|---|---|---|---|
| `update_case` (custom_preconds + custom_steps + custom_expected, plain) | C38872 | 200 | PASS |

Pre-write `custom_atmstatus` = 1 (Not Automated) — confirmed non-Automated before writing.
`refs`, `custom_atmstatus`, title, section_id (5409), type_id (5), template_id (1), priority_id (3)
NOT sent; re-GET confirms all byte-identical to the pre-write snapshot.

## Post-write verification (re-GET, byte-compare)
- `custom_preconds` / `custom_steps` / `custom_expected` == TARGET (byte-identical): PASS
- raw HTML (`<ol|<li|<hr|<a `) in the three fields: 0 — PASS
- automation marker: exactly one, == `AUTOMATION: HOLD - needs three separate sign-ins, one per permission level`: PASS
- provenance line: exactly one: PASS
- title / refs / custom_atmstatus / section_id / type_id / template_id / priority_id: byte-identical to pre-write: PASS

## Deliverables
- Local case source (`cases-H-tech-plan.json`): already correct plain form — unchanged.
- `build/schedule/testrail-id-map.csv`: 0 blanks, refs preserved (generator blanks C-ids on rerun; re-merged from committed).
- `testrail-import/schedule-v1-testrail-import.csv`: byte-identical (unchanged); shredding guard PASSED (0/195); header sha256 == all 6 peers.

## Run / Jira
- Run 357: NOT touched. Jira: NOTHING created (Standing Rule 62 / active creation hold — no ticket work).
