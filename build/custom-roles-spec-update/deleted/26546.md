# DELETED CASE — full content for possible recreation

TestRail case **26546** was deleted on 2026-07-06 per explicit user request.
This record captures every field verbatim so the case can be recreated if needed.

> To recreate: use `add_case/<section_id>` with the fields below.
> Remember `add_case` REQUIRES `custom_atmstatus` + `custom_automation_type`.

## Identity / metadata
| Field | Value |
|---|---|
| id | 26546 |
| title | Parts Department parent OFF overrides children ON — children inaccessible |
| section_id | 3553 |
| suite_id | 1 |
| template_id | 1 |
| type_id | 6 |
| priority_id | 3 |
| milestone_id | null |
| refs | SV-7520 |
| estimate | null |
| estimate_forecast | null |
| display_order | 7 |
| is_deleted | 0 (at time of capture) |
| created_by | 3 |
| created_on | 1781282819 |
| updated_by | 3 |
| updated_on | 1781282819 |

## Custom fields
| Field | Value |
|---|---|
| custom_atmstatus | 1 |
| custom_automation_type | 0 |
| custom_ai_type | null |
| custom_ai_model | null |
| custom_testrail_bdd_scenario | null |
| custom_steps_separated | null |
| custom_mission | null |
| custom_goals | null |
| ai_automated_test | null |
| labels | [] |

## custom_preconds (verbatim HTML)
```html
<p>Custom role 'Parts Parent Off' has Parts Department parent toggle OFF, but each child (Part sales, Catalog and Inventory, Vendor and order management) has every CRUD box checked. Test user assigned.</p>
```

## custom_steps (verbatim HTML)
```html
<ol>
<li>Open the role for edit. Confirm the Parts Department parent toggle is OFF but all child CRUD boxes show as checked (may be visually disabled if UI cascades, otherwise literally checked).</li>
<li>Save.</li>
<li>Log in as test user.</li>
<li>Look for top nav > Parts.</li>
<li>Look for left sidebar PARTS group.</li>
<li>Attempt direct URL navigation to part sales, catalog, vendor pages.</li>
</ol>
```

## custom_expected (verbatim HTML)
```html
<p>Parts top nav and PARTS sidebar group are hidden. Direct URLs denied. Parent-OFF wins over children-ON — confirms parent gate is enforced server-side, not just visually.</p>
```

## Raw JSON (full get_case response)
```json
{"id":26546,"title":"Parts Department parent OFF overrides children ON — children inaccessible","section_id":3553,"template_id":1,"type_id":6,"priority_id":3,"milestone_id":null,"refs":"SV-7520","created_by":3,"created_on":1781282819,"updated_by":3,"updated_on":1781282819,"estimate":null,"estimate_forecast":null,"suite_id":1,"display_order":7,"is_deleted":0,"custom_ai_type":null,"custom_ai_model":null,"custom_automation_type":0,"custom_atmstatus":1,"custom_preconds":"<p>Custom role 'Parts Parent Off' has Parts Department parent toggle OFF, but each child (Part sales, Catalog and Inventory, Vendor and order management) has every CRUD box checked. Test user assigned.</p>\n","custom_steps":"<ol>\n<li>Open the role for edit. Confirm the Parts Department parent toggle is OFF but all child CRUD boxes show as checked (may be visually disabled if UI cascades, otherwise literally checked).</li>\n<li>Save.</li>\n<li>Log in as test user.</li>\n<li>Look for top nav > Parts.</li>\n<li>Look for left sidebar PARTS group.</li>\n<li>Attempt direct URL navigation to part sales, catalog, vendor pages.</li>\n</ol>\n","custom_testrail_bdd_scenario":null,"custom_expected":"<p>Parts top nav and PARTS sidebar group are hidden. Direct URLs denied. Parent-OFF wins over children-ON — confirms parent gate is enforced server-side, not just visually.</p>\n","custom_steps_separated":null,"custom_mission":null,"custom_goals":null,"ai_automated_test":null,"labels":[]}
```
