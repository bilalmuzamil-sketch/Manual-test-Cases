# Custom Roles & Permissions — Spec Diff (OLD 2026-07-09 export vs NEW 2026-07-15 export)

**OLD:** `build/custom-roles-spec-update/updated-spec-source.md` (exported 09 Jul 2026; last change-log row = 09 Jul 2026)
**NEW:** `build/custom-roles-spec-update/current-spec-2026-07-15.md` (exported 15 Jul 2026 from Confluence pageId 565116952, lastModified ~11h before export; last change-log row = 7/14/2026 "Updated Office Role definition") — **ground truth**.

Both files were read in full and compared section-by-section and (for all four matrix tables) cell-by-cell. Formatting-only differences (Confluence `<custom>` markup, bold markers, date format `09 Jul 2026` → `7/9/2026`, hyperlinked references, horizontal rules, list numbering) are **ignored** below unless the words changed. Everything not listed as ADDED/REMOVED/CHANGED is **verbatim-identical**.

---

## 1. Change-log delta

### 1.1 Rows present in NEW but not OLD

| Date | Reporter | Change (exact text) |
|---|---|---|
| **7/14/2026** | @Sasha Grosman | "Updated Office Role definition" |

This is the **only new change-log row**. It corresponds to the Office-column matrix changes in §2.9 below.

### 1.2 Existing rows whose WORDING changed

- **6/28/2026 row — CHANGED (retroactively edited).** The reverse-invoice re-gating entry was expanded:
  - OLD: "Reversing an Invoice has been moved (for Work Orders and Part Sales) Previously: required Invoice & Payments → Delete **Now: requires Work Order → Delete**"
  - NEW: "Reversing an Invoice has been moved (for Work Orders and Part Sales) Previously: required Invoice & Payments → Delete **Now: For WO requires Work Order → Delete For PS requires Part Sale → Delete**"
  - Net: the Part-Sales branch ("For PS requires Part Sale → Delete") was added to the changelog row. The spec **body** already said this in BOTH versions (§1e Delete: "Delete part sales and reverse part sales invoices."), so this is a changelog correction, not a new behavior.

### 1.3 Rows the task asked to verify — 7/8 and 7/9

- **7/8/2026 row: IDENTICAL wording** in OLD and NEW ("Added Manage AP / AR required See Financial Data on (not the other way around) Clarified Notes Tab vs Notes field behavior on WO, Customer, and Asset pages Departments belongs under App Settings … Settings → Integrations: clarified which system roles should have this default on."). OLD's row has an empty Reporter cell; NEW's also has no reporter mention. No difference.
- **7/9/2026 row: IDENTICAL wording** ("Clarified See Financial Data applies to Core app, not Customer Portal, Billing Portal, and Settings pages."). Reporter "Sasha Grosman" in both (plain text vs @mention markup only).
- All other rows 5/29–7/7: **identical wording** (7/7 row verified word-for-word identical, including "Story history (WOL level) requires - WOL - View" and "Marking Cores OK/Not Ok is gated by WO->View").

---

## 2. Section-by-section body delta

### 2.0 Header / TOC / Business Case / Research & References / Scope
- Header/Owner/Epic/Figma: identical content. OLD had a flattened TOC paragraph (export artifact); NEW has none. **No substantive change.**
- Business Case: **no change.**
- Research and References: same two entries; NEW adds the actual Confluence URLs (links only). **No substantive change.**
- Scope: **no change** (still "12 system role templates", "Migration plan from legacy 15 roles to new 12 system roles").

### 2.1 Concepts → Roles (incl. the Office/Time-Clock edit lock)
**No change.** The rule the task asked about — "System roles can be edited, with exceptions: **Office** and **Time Clock** roles cannot be edited. System roles cannot be deleted." — is present **verbatim in BOTH** OLD (line 40) and NEW (line 52). Likewise the Roles-list-page behavior bullet "Most system roles can be edited. Exceptions: Office and Time Clock cannot be edited — clicking them opens a read-only permission summary." is in BOTH. So this is NOT a new rule; it predates the 7/9 export.

### 2.2 Permission Settings groups / §1 CRUD level table / dependency rule
**No change.**

### 2.3 §1a Work Orders
**No change** in any of the three permission rows, the sub-settings table, or the notes:
- View row (incl. Notes wording and cores) identical in both: "Users can also see the Notes tab, create notes, and edit any note (including notes created by others) so people can collaborate within a single note and can delete the notes they created. **Users can mark cores Ok/Not Ok.**"
- Edit row identical: "…Users can view the work order and work order line level audit logs." (audit-log gating = WO Create&Edit, in BOTH).
- Delete row identical: "Delete work orders, Reverse Invoices as long as validation criteria is met (e.g. no payments made). Delete any note, including notes created by other users. Note: this permission is not enough to delete payments."
- Sub-settings (Review Work Orders / Pick Parts / Order Parts incl. the Order Parts Financial Gate), the "Returning a part from a WOL does not require a permission" note, and the "Important: These sub-settings only require Work Orders: View" note: identical.

### 2.4 §1b Work Order Lines (+ unnumbered Schedule block)
**No change.**
- Edit row identical in both, still ending: "**Mark a core OK or Not OK on a line. View a line’s story history.**" → the cores/story-history contradiction with §1a View and the 7/7 changelog exists in BOTH versions (see §3).
- Delete row identical ("Remove lines from work orders. Digital Inspections: Reopen inspections that are complete… Delete complete inspections").
- Schedule (key `schedule`, still living headerless inside §1b in both): all three rows identical.

### 2.5 §1d Customer Management
**No change.** View row ("Allows viewing the Notes tab, creating notes, editing notes that anyone created, and deleting your own Notes."), Edit row, Delete row ("Does NOT control deleting customer payments — that requires Invoicing: Delete. Allows deleting notes other people created."), and the 7-item Sensitive Customer Fields list: all identical.

### 2.6 §1e Part Sales / §1f Catalog and Inventory / §1g Vendor and Order Management
**No change** in any row (incl. §1e Delete "Delete part sales and reverse part sales invoices." — the PS reverse-invoice gate was already in OLD).

### 2.7 §1h View Part History — CHANGED (label field only)
- Section heading "1h. View Part History": same in both.
- **Label CHANGED:** OLD "Label: **View History Logs**" → NEW "**Label:** **View Part History**".
  (This applies the 7/7 changelog instruction 'Changed "View History Logs" Relabel "View Part History"', which the 7/9 OLD export had not yet applied to this field.)
- Description ("Controls visibility of part sale history."), Parent gate, and ON/OFF rows: identical.
- NOTE: the 7/7 changelog also says the setting "lives under Part Sales as last in the list", but in BOTH versions §1h remains its own section and the permission-matrix row is still named "History Logs" in the Cross-Cutting table (see §3.7).

### 2.8 §1i Invoicing
**No change** in substance. The two financial gates are in BOTH versions:
- Gate 1 (See Financial Data) — identical.
- Gate 2 — **already present in OLD**: "“Delete” additionally requires Manage Accounts Payable and Receivables (“Manage AP/AR”). If Manage AP/AR is OFF and a user enables Delete checkbox, a confirmation modal prompts them to enable Manage AP/AR." (OLD line 166 = NEW line 228, word-identical; OLD only had the OCR-ish "Fi nancial gat es:" heading typo, fixed to "Financial gates:" in NEW.) **Answer to the task question: yes, the Invoicing-Delete-requires-Manage-AP/AR gate was in OLD.**
- View / Edit (incl. "Send to terminal: To send to terminal use must have this and “Customer Portal: ON” enabled.") / Delete rows and the customer-payments note: identical.

### 2.9 §1j Timesheets · §2a Reports · §2b Customer Portal · §2c Billing Portal · §2d Parts Department
**No change** in any of these (Reports all-or-nothing note incl. AR/AP aging: identical).

### 2.10 §3 Settings Access + Sub-settings
**No change.** Both say "A parent toggle with **6 sub-settings**" and both list **7 rows** (App Settings, Service, Parts, **Integrations**, Finance, Data Import, View/Manage Wages) — the Integrations row and the 6-vs-7 count mismatch exist in BOTH (see §3.4). App Settings row (covers Roles and Permissions, Staff/Workplaces, departments): identical.

### 2.11 §4 View Mode
**No change.** Full View row identical (incl. Review exception + "Has access to “Send to Portal” button"). Tech View restrictions table: **all 9 rows identical in both**, including the two the task flagged — "Cannot edit existing WO lines | Can only create new work order lines. Cannot edit existing work order lines — they are read-only." and "WO lines read-only after approval | Can only edit a work order line while its authorization is pending — once a WOL has been approved, it becomes read-only for that user." **These were already in OLD; they are NOT new in NEW.** Closing "UX simplification, not a security boundary" paragraph: identical.

### 2.12 §5a See Financial Data
**No change.** The Creating-an-Inventory-Item behavior (Cost hidden; user can Save; system stamps Cost/Sell Price/Core Charge with 0) and the core-app-only scoping ("These requirements apply to the core app. They do not apply to Customer Portal, Billing Portal, and Settings pages." — in both ON and OFF rows, and in the Description) are **identical in OLD and NEW** (OLD already contained the 7/9 clarification). Dependencies prompt + 4-item list (Invoicing full CRUD, Part Sales full CRUD, Order Parts, Manage AP/AR) + design note: identical.

### 2.13 §5b Manage Accounts Payable and Receivable
**No change**, including the defect text: the OFF row in BOTH versions contains the stray duplicated fragment "…hidden on both Customer and Vendor detail pages. **Order Parts (Work Orders sub-setting) Order Parts (Work Orders sub-setting)**Sensitive customer fields are hidden…". Financial gate, ON row (sensitive customer + vendor fields), independence note, "Does NOT gate CRUD areas" note: identical.

### 2.14 Admin Pages
- Roles and Permissions List Page (columns + behaviors incl. Office/Time-Clock read-only): **no change.**
- **Create / Edit Custom Role — CHANGED (one bullet).** Permission Editor cross-cutting bullet:
  - OLD: "Cross-Cutting Toggles (See Financial Data, See AP/AR Data, **View History Logs**)"
  - NEW: "Cross-Cutting Toggles (See Financial Data, See AP/AR Data)"
  - Net: **"View History Logs" was REMOVED from the role-editor Cross-Cutting Toggles list** (consistent with the 7/7 relabel/move of View Part History; the NEW spec never states where it now appears in the editor — presumably under Part Sales per the 7/7 changelog, but the body doesn't add it there).
  - All other flow steps, dependency-enforcement bullets: identical.
- Financial Data Confirmation Modal / Delete Role / Permission Summary: **no change.**

### 2.15 Staff Page Updates
**No change** (System Roles "the 12 built-in templates"; forced logout on role change — identical).

### 2.16 System Role Templates — Permission Matrix (cell-by-cell diff)

Intro prose: identical ("The following 12 system roles ship with ShopView…" — still says 12; 11 columns/11 descriptions in both, see §3.3).

**CRUD Areas table — CHANGED cells (Role/Area: old → new):**

Admin column (was rendered EMPTY in OLD for 8 of 9 rows; now explicitly filled):
- **Admin/Work Orders:** (blank) → **V/E/D**
- **Admin/WO Lines:** (blank) → **V/E/D**
- **Admin/Schedule:** (blank) → **V/E/D**
- **Admin/Customers:** (blank) → **V/E/D**
- **Admin/Part Sales:** (blank) → **V/E/D**
- **Admin/Catalog and Inv:** (blank) → **V/E/D**
- **Admin/Vendor and Order:** (blank) → **V/E/D**
- **Admin/Invoicing:** (blank) → **V/E/D**
- (Admin/Timesheets was already V/E in OLD — unchanged.)

Office column (the 7/14 "Updated Office Role definition"):
- **Office/Work Orders: V → —** (Office loses Work Orders entirely)
- **Office/Part Sales: V → —** (Office loses Part Sales)
- **Office/Invoicing: V → V/E/D** (Office gains Invoicing Create and Edit + Delete)

Every other cell in the CRUD table is **unchanged**, explicitly including the ones the task flagged:
- **Time Clock column: no change** (WO V, WO Lines —, Schedule V, Customers —, Part Sales —, Catalog —, Vendor —, Invoicing —, Timesheets V).
- **Sales Rep column: no change** (WO V, WO Lines V, Schedule —, Customers V/E, Part Sales V, Catalog —, Vendor —, Invoicing —, Timesheets —).
- **Svc Mgr/Work Orders: V/E/D in both — no change** (and Svc Mgr/Invoicing stays V/E, no Delete, in both).
- Office/WO Lines stays **V**, Office/Schedule **V**, Office/Customers **V/E/D**, Office/Catalog **V**, Office/Vendor **V**, Office/Timesheets **V/E** — unchanged (which creates a NEW-only contradiction, see §3.6).
- Matrix note "WO Lines View is not independently configurable — it is inherited from Work Orders View.": identical.

**Toggles table: no change** — all 55 cells identical (Reports ON for Admin/SvcMgr/SrSA/PartsMgr/Office/SalesRep; Customer Portal ON for Admin/SvcMgr/SrSA/SvcAdvisor/PartsMgr; Parts Dept ON for all but Tech/SalesRep/TimeClock — **incl. Office still ON**; Billing Portal ON Admin/SvcMgr/Office; Settings ON Admin/SvcMgr/PartsMgr/Office).

**Settings Sub-Toggles table: no change** — all cells identical (App Settings ON/ON/—/ON; Service ON/—/—/ON; Parts ON/—/ON/—; Integrations ON/—/—/ON; Finance ON/—/ON/ON; Data Import ON/—/ON/ON; Wages ON/ON/—/ON for Admin/SvcMgr/PartsMgr/Office).

**WO Sub-Settings, View Mode, Cross-Cutting Toggles table: no change** — all cells identical (Review WOs, Pick Parts, Order Parts rows; View Mode row Full/…/Tech/…/— ; See Financial ON for all but Tech/TimeClock; See AP/AR ON Admin/SvcMgr/SrSA/PartsMgr/Office/SalesRep; **History Logs row unchanged incl. Office=ON** and the row is still titled "History Logs" despite the §1h relabel).

### 2.17 Role Descriptions
**No change** — same 11 rows (Admin … Time Clock), same descriptions verbatim (Office still "Office administration, reporting, and back-office operations" — NOT updated for 7/14).

### 2.18 Migration Plan
- Legacy Role → New System Role mapping (15 rows): **no change.**
- Behavior Changes for Migrating Users (9 rows): **no change** — Office row still reads "Catalog reduced to V only. Customer Mgmt expanded to FULL (gains Delete)." with **no mention of the 7/14 WO/Part-Sales loss or Invoicing V/E/D gain** (stale vs the new matrix; see §3.6/§4).
- Staff Record Settings: **no change.**

### 2.19 Office Users Cannot Create Invoices
**No change** — identical text in both: "Office users are expected to be able to make payments but not create invoices. A hard coded rule for Office users will disable the Create Invoice button on Work Orders and Part Sales. This overrides the Invoicing & Payments CRUD." (Section moved under Migration Plan in NEW's structure; wording identical. Now interacts with the NEW Office Invoicing V/E/D cell — see §3.6.)

### 2.20 Future Consideration: Rebrand Role to Profile
**No change.**

### 2.21 Key Decisions
**No change** — all 25 rows verbatim-identical, including "Marking Cores OK/Not Ok | Discussed with Cody and agreed everyone should have access to this. Therefore gate is WO->View (which implies WOL-> View)" (in BOTH) and "Digital Inspections use existing atoms…" and "Office Users Cannot Create Invoices".

### 2.22 Open Questions
**No change** — all 11 rows identical, questions AND answers:
- Q1 (dashboard) answer unchanged; Q2 (Office create invoices) answer **still BLANK in both** (and its Context "Currently Invoicing is V only for Office" is now stale vs the NEW matrix — see §3.6); Q3 (return to inventory = Yes) unchanged; Q4 (Send to Terminal/Deposits = Invoicing C&E) unchanged; Q5 (keep AP/AR, renamed) unchanged; Q6 (Customer Portal roles + Send-to-Portal = anyone who can approve a WOL) unchanged; Q7/Q9 (Owner dropped/merged into Admin) unchanged; Q8 (DI migration Done) unchanged; Q10 (Customer detail related tabs visible, links not clickable) unchanged; Q11 (Reset to Template requirements missing) **still unanswered in both**.
- **No answers were filled or changed between OLD and NEW.**

---

## 3. Spec-internal contradictions in NEW (rule: last-update-wins by change-log dating)

1. **Cores OK/Not-OK gate — WO View vs WOL Create&Edit (in BOTH OLD and NEW).**
   - §1a Work Orders View: "Users can mark cores Ok/Not Ok." + Key Decisions: "Therefore gate is WO->View (which implies WOL-> View)" + 7/7 changelog: "Marking Cores OK/Not Ok is gated by WO->View".
   - vs §1b WO Lines Edit: "Mark a core OK or Not OK on a line."
   - Dating: Jul 3 changelog tied cores to "Work Order Lines → Create & Edit"; **7/7 changelog re-gated it to WO→View. 7/7 is later → WO View wins**; the §1b Edit sentence is stale residue. Same contradiction existed in OLD.
2. **Line story history — WOL View vs WOL Create&Edit (in BOTH).**
   - 7/7 changelog: "Story history (WOL level) requires - WOL - View" vs §1b Edit still listing "View a line’s story history."
   - Dating: Jul 3 said WOL C&E; **7/7 is later → WOL View wins** (and WOL View = inherited from WO View). §1b Edit sentence is stale residue. Same in OLD.
3. **"12 system roles" vs 11 listed (in BOTH).** Scope ("12 system role templates"), Concepts ("12 pre-defined role templates"), Staff Page ("the 12 built-in templates"), System Role Templates intro ("The following 12 system roles ship with ShopView") — but the matrix has **11 role columns** and Role Descriptions lists **11 roles** (Owner was dropped per Q7/Q9 and the 6/10 changelog). The prose count was never corrected.
4. **"6 sub-settings" vs 7 rows (in BOTH).** §3: "A parent toggle with 6 sub-settings" and role-editor note "hides… all 6 sub-settings", but the Sub-settings table has **7 rows** (Integrations was added 6/28; count not updated).
5. **§5b OFF row stray duplicated text (in BOTH).** "…hidden on both Customer and Vendor detail pages. **Order Parts (Work Orders sub-setting) Order Parts (Work Orders sub-setting)**Sensitive customer fields are hidden…" — copy/paste residue, meaningless in context (Order Parts' AP/AR relationship is not otherwise specified; its gate is See Financial Data per §1a).
6. **NEW-ONLY — Office column self-contradictions introduced by the 7/14 edit:**
   - **Office/Work Orders = — but Office/WO Lines = V**, while the matrix's own note says "WO Lines View is not independently configurable — it is inherited from Work Orders View." An Office user with WO=— cannot have WOL View. One of the two cells is wrong; per last-update-wins the 7/14 Office edit is authoritative but it is internally incoherent — needs PO confirmation.
   - **Office/Invoicing = V/E/D vs "Office Users Cannot Create Invoices"** ("A hard coded rule… will disable the Create Invoice button… This overrides the Invoicing & Payments CRUD"). Reconcilable by design (the hard-coded rule overrides), but the net Office capability = pay/delete payments yet never create invoices — test both sides.
   - **Office/Invoicing = V/E/D vs Open Question 2 context** "Currently Invoicing is V only for Office" (stale) — and Q2's answer cell is still blank even though the matrix now de-facto answers it (with the hard-coded-rule carve-out).
   - **Office/Invoicing V/E/D + Office/Work Orders — + Office/Part Sales —**: §1i View says invoice entry points are Work Orders, Part Sales, or Customers; Office retains only Customers (V/E/D), so Office invoicing is reachable solely via Customers.
   - **Behavior Changes for Migrating Users (Office row) and Role Descriptions (Office) not updated** to reflect the 7/14 changes — stale vs the matrix.
   - **Office/Parts Dept toggle still ON** while Office now has Part Sales — (and Catalog V / Vendor V) — consistent-but-odd; and **Office "History Logs" = ON** while §1h (View Part History) is Parts-gated — still satisfiable since Parts Dept is ON.
7. **NEW-ONLY (minor) — View Part History placement residue:** §1h Label is now "View Part History" and it was removed from the role-editor Cross-Cutting bullet, but the matrix row is still called "**History Logs**" and still sits in the "WO Sub-Settings, View Mode, Cross-Cutting Toggles" table (7/7 said it "lives under Part Sales as last in the list"). The body never re-lists it under Part Sales.

---

## 4. QA-impact summary (behavioral deltas that can change test steps/expected results)

- **[Matrix/CRUD — Office] Office loses Work Orders entirely (V → —).** Any Office-role case that opens the WO list/detail, Notes tab on WO, or marks cores must now expect the **Work Orders nav item hidden** and no WO access. (7/14 "Updated Office Role definition".)
- **[Matrix/CRUD — Office] Office loses Part Sales (V → —).** Office cases expecting read-only Part Sales visibility must flip to **no Part Sales access** (nav/section hidden), even though Parts Dept toggle stays ON for Office.
- **[Matrix/CRUD — Office] Office gains Invoicing V/E/D (was V).** Office can now create/edit invoices per CRUD, process payments, collect deposits, and **Delete payments/void/reverse-return** (Office has See Financial Data ON + Manage AP/AR ON, satisfying both §1i gates) — BUT the hard-coded "Office Users Cannot Create Invoices" rule still disables the **Create Invoice button** on Work Orders and Part Sales. Expected: payments/deletes allowed, invoice creation blocked by the hard-coded rule; only entry point left is Customers (Office has no WO/PS access).
- **[Matrix/CRUD — Office] Contradiction to resolve before authoring: Office WO Lines = V with Work Orders = —** violates the WOL-View-inherits-WO-View rule; flag to Sasha/PO rather than encode either behavior. Also Review-WOs/Pick-Parts/Order-Parts stay — for Office (unchanged), consistent with no WO access.
- **[Matrix/CRUD — Admin] Admin column now explicitly V/E/D across all 8 previously-blank CRUD rows.** If any case treated the blank Admin cells as undefined/assumed, they can now assert full CRUD for Admin verbatim from spec. No behavior change expected in build (Admin was always full access) — documentation-only.
- **[§1h] Label change "View History Logs" → "View Part History".** Case wording (Standing Rule 9) must use the NEW label; also verify in the build which label the role editor actually shows and where the toggle sits (Cross-Cutting vs under Part Sales — spec is still ambiguous, matrix row still "History Logs").
- **[Admin Pages / role editor] Cross-Cutting Toggles bullet no longer lists View History Logs.** Role-editor UI cases enumerating the Cross-Cutting group (See Financial Data, See AP/AR Data) should drop View History Logs from that group's expected contents.
- **[Change log 6/28 row] Reverse-invoice gating clarified: WO reverse = Work Order → Delete; PS reverse = Part Sale → Delete** (changelog now says both branches explicitly; body already did). Confirm no case still gates PS invoice-reversal on Invoicing → Delete. (Invoicing → Delete remains: delete payments, void transactions, delete a Part Sale return — and still requires Manage AP/AR, unchanged from OLD.)
- **[Stale docs to NOT act on] Migration "Behavior Changes" Office row, Office Role Description, and Open Question 2 context were NOT updated for 7/14** — do not derive expected results from them; the Permission Matrix + "Office Users Cannot Create Invoices" section are the authoritative (latest) statements.
- **No other behavioral deltas.** Everything else — 1a/1b/1d/1e/1f/1g/1i/1j bodies, Schedule, all §2 toggles, §3 settings + sub-toggles, §4 View Mode (incl. both "Cannot edit existing WO lines" and "WO lines read-only after approval" rows, already in OLD), §5a SFD (incl. inventory-item creation + core-app-only scoping, already in OLD), §5b, Toggles/Sub-Toggles/WO-Sub-Settings matrix tables, Time Clock and Sales Rep columns, Svc Mgr WO Delete, Role Descriptions, Migration mapping, Key Decisions, all 11 Open Questions — is **verbatim-identical** between the 7/9 and 7/15 exports.
