# TestRail Case 26505 — Amendment Audit Log

**Case URL:** https://shopview.testrail.io/index.php?/cases/view/26505
**Section:** Custom Roles - (Revised) > Per-Role Verification (section 3548)
**Amended:** 2026-07-06
**Correction:** The Time Clock system role's ACTUAL/CORRECT permission set is three
read-only View permissions ON — Work Orders: View, Schedule: View, Timesheets: View —
with EVERYTHING ELSE OFF (no Create/Edit, no Delete anywhere; no Customers, Parts,
Invoicing/Payments, Reports, Settings, See Financial Data, Manage AP/AR, view-mode).
The case previously expected "clock in/out only / everything else OFF / all CRUD OFF /
no view access," which was wrong. Updated so the expected result asserts the three
read-only Views ON + all else OFF.

Authorized single write: POST /api/v2/update_case/26505, HTTP 200. Only fields title,
custom_preconds, custom_steps, custom_expected were updated. No other case was touched.

---

## BEFORE (verbatim)

**Title:**
```
Time Clock User: clock in/out only; everything else OFF; non-editable role
```

**Preconditions (custom_preconds):**
```
<p>User assigned to system-timeclock role.</p>
```

**Steps to Reproduce (custom_steps):**
```
<ol>
<li>Verify all CRUD OFF, all toggles OFF, all sub-settings OFF.</li>
<li>Verify See Financial OFF, See AP/AR OFF, View History Logs OFF.</li>
<li>In app: confirm only the basic clock in/out control is accessible.</li>
<li>On Roles list, confirm Time Clock User row has lock icon and only eye icon.</li>
</ol>
```

**Expected Results (custom_expected):**
```
<p>User can clock in/out and nothing else. Role is non-editable. Per spec change 6/9: View Mode permission for Time Clock User is EMPTY (no value — not Full View, not Tech View). Confirm in Permission Summary that the View Mode field shows no selected value.</p>
```

---

## AFTER (as stored)

**Title:**
```
Time Clock role grants read-only Work Orders / Schedule / Timesheets view; all other permissions OFF
```

**Preconditions (custom_preconds):**
```
1. Log in as an Admin.
2. The Time Clock system role exists (default role).
```

**Steps to Reproduce (custom_steps):**
```
1. Go to Administration > Roles & Permissions.
2. Open the Time Clock role’s permissions (View Permissions / eye icon).
3. Review which permissions are ON vs OFF.
4. Optionally assign the Time Clock role to a test user, log in, and confirm the navigation.
```

**Expected Results (custom_expected):**
```
1. The Time Clock role has exactly three permissions ON, all read-only (View): Work Orders: View, Schedule: View, and Timesheets: View.
2. Every other permission is OFF — no Create & Edit and no Delete in any area, and Customers, Parts Department, Invoicing & Payments, Reports, Settings, See Financial Data, and Manage AP/AR are all OFF.
3. View Mode is empty/none for this role.
4. A user assigned the Time Clock role can view Work Orders, Schedule, and Timesheets (read-only) and nothing else.
```

---

## Revert reference

To revert, POST /api/v2/update_case/26505 with the BEFORE values above.
