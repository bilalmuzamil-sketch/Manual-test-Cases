# TestRail Case 27790 — Amendment Audit Log

**Case URL:** https://shopview.testrail.io/index.php?/cases/view/27790
**Amended:** 2026-07-05
**Spec change:** #1 (Work Orders > View grants create + edit of ANY note — team collaboration) and #2 (Work Orders > Delete grants delete of ANY note). Only title (where changed), custom_preconds, custom_steps, custom_expected were updated. No other case was touched.

---

## BEFORE (verbatim)

**Title:**
```
Verify Senior Service Advisor can edit and delete another user's work order note
```

**Preconditions (custom_preconds):**
```
Log in to ShopView as a user assigned the Senior Service Advisor role. (edit/delete ALL notes requires Work Orders: Delete per spec v33; this role has it).
A work order exists with a note created by a DIFFERENT user.
```

**Steps to Reproduce (custom_steps):**
```
1. Open the work order's Notes tab.
2. Edit another user's note and save.
3. Delete that note and confirm.
```

**Expected Results (custom_expected):**
```
Both the edit and the delete succeed with no permission error.
```

---

## AFTER (as stored)

**Title:**
```
Verify Senior Service Advisor can edit and delete another user's work order note
```

**Preconditions (custom_preconds):**
```
1. Log in as a user with the Senior Service Advisor role.
2. The role has Work Orders > View turned ON.
3. The role has Work Orders > Delete turned ON.
4. A work order exists that already has a note written by a DIFFERENT user (e.g., an Admin).
```

**Steps to Reproduce (custom_steps):**
```
1. Open the work order.
2. Click the Notes tab.
3. Find the note written by the other user.
4. Click the note's menu and choose Edit.
5. Change the text and click Save.
6. Open the note's menu again and choose Delete.
7. Confirm the deletion.
```

**Expected Results (custom_expected):**
```
1. The Edit option is available on the other user's note (Work Orders > View allows editing ANY note for team collaboration).
2. The edited text saves successfully with no permission error.
3. The Delete option is also available on the other user's note (Work Orders > Delete allows deleting ANY note, including other users').
4. The other user's note is deleted with no permission error.
```

---

## Revert reference

To revert, POST /api/v2/update_case/27790 with the BEFORE values above.
