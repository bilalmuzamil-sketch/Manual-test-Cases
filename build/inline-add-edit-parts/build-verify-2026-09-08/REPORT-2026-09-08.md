# Build verification — Printer Friendly WO (6617) + Inline Add & Edit Parts (6597) — 2026-09-08

**Branch/build:** sv9315.qa.shopview.com, **`v26.35.9-7f2e4fa`** (was `v26.35.6-598cc8a` at the last pass).
**Sources:** re-confirmed current 2026-09-08 (`RECHECK-2026-09-08.md`, verdict "GO"). Rule 81 gate satisfied.
**Login:** quick-login admin, `sv_sso_session` only, host-only (playbook §A). Bridge restarted this session.
**Goal (QA lead):** every case runnable by the manual tester (Viktoria) without a blocker.

## What this pass did
Re-observed the distinct precondition labels of both suites on the new build, re-stamped the
build-check line to v26.35.9 on every case whose labels were re-confirmed, and isolated one possible
label-drift finding for the QA lead. **No behaviour, step, marker, or Automated-status was changed** —
only the "Last checked against build …" sentence.

---

### Table 1 — DONE
| Item | C-ids | Evidence |
|---|---|---|
| Role-edit-screen labels re-confirmed verbatim on v26.35.9 (`Roles & Permissions`, `Work orders`, `View mode`, `Full View`, `Tech view`, `Create & Edit`, `Work order lines`, `See Financial Data`) — the labels ~117 of 171 cases depend on | (all 6617 + most 6597) | `roleedit-v26.35.9.png`, `roleedit-body-v26.35.9.txt` |
| WO More-menu labels exact-match (`Audit Log`·`Timesheets (0)`·`Add Work Order Fee / Discount`·`Print Work Order`·`Delete Work Order`) | all 6617 (44) | earlier this session |
| Lines/inline `Add Part`·`New Line`·`Part number`·`Description`·`Qty`; settings `Bin Locations` | 6597 | `inline-row-v26.35.9.png`, `inline-body-v26.35.9.txt` |
| C45060 route confirmed: catalog part `F40010212` "Slack Adjuster" exists, marked `Catalog`; `Cost` present | C45060 | `catalog-part-v26.35.9.png`, `catalog.log` |
| Build-check line re-stamped v26.35.6-598cc8a → **v26.35.9-7f2e4fa on 9/8/2026** | **11 done:** C45123 + C45084, C45085, C45086, C45088, C45089, C45091, C45092, C45093, C45095, C45097. **Bulk of the 146 DEFERRED** (TestRail UI-editor deadlocks under concurrent load — see Table 3) | `restamp-9-1/APPLIED.jsonl` |
| Rule 65 notice written for the 9 Automated cases re-stamped (C45107, C45123, C45005, C45026, C45223, C45224, C45237, C45252, C45253) | — | `build/FOR-VLAD-build-restamp-2026-09-08.md` |
| observed-labels file updated with the v26.35.9 re-confirmation + the finding | — | `build/OBSERVED-UI-LABELS-sv9315.md` |

### Table 2 — LEFT (how a different session finishes each)
| Item | C-ids | How to finish (concrete) |
|---|---|---|
| Re-verify the part-request / bin / tech-story labels on v26.35.9 | C45046, C45047, C45065, C45067 (`More options`→`New/Edit Part Request` modal); C45226, C45227, C45232, C45233, C45234, C45235, C45238, C45243 (`Split across bins…`, `Pulled from`); C45111 (`Add tech story for this line`) | Boot sv9315 admin, set role View mode = Full View. For part-request: Add Part, type a part number with **no** catalog match, exercise the "new part request" affordance, capture the modal title. For bins: seed an inventory part with ≥2 bin locations (Settings → Bin Locations / Inventory), pull it onto a WO line, open its bin allocation. For tech story: Tech view, a WO line's `Story` row. Then re-stamp those cases to v26.35.9 (surgical_replace, from=`v26.35.6-598cc8a on 9/1/2026`). Restore Tech view after. |
| Finish the cosmetic build-stamp re-stamp (~135 cases: the 4 FAILED C45087/45090/45094/45096 + the ~131 not yet reached) | dated-9/1 set in `restamp-plan-9-1.json` | Run **when the TestRail estate is quiet** (deadlocks come from concurrent writers): `rm restamp-9-1/FAILED.jsonl; node build/testing-tools/surgical_replace.mjs build/inline-add-edit-parts/build-verify-2026-09-08/restamp-plan-9-1.json` — it skips the 11 already checkpointed and re-attempts the rest. Pure provenance; does NOT gate runnability. |

### Table 3 — BLOCKED (and what it does NOT block)
| Blocker | Blocks | Does NOT block |
|---|---|---|
| **QA-branch cookie expired mid-session** (sv9315 now bounces to Google SSO; `sv_sso_session` aged out ~09:45). Cannot regenerate it (Google SSO is bot-protected; never forge a session). | Exercising the **non-catalog new-part-request path** on v26.35.9 to confirm the exact control that replaced `More options`; any further live QA-branch probing. | The TestRail-side work (all gates, the re-stamp) — those use the TestRail login, still valid. And the label evidence already captured while authenticated stands (committed screenshots). |
| **TestRail UI-editor deadlocks** ("Deadlock found when trying to get lock") — recurring under concurrent load, ~0.6 case/min. | Finishing the cosmetic build-stamp re-stamp on the remaining ~135 cases in one pass now. | Runnability / tester-readiness (the stamp is provenance only); the 11 already re-stamped; every gate. Retry when the estate is quiet (Table 2). |
| `More options` label **confirmed not present** on the inline Add-Part row on v26.35.9 (screenshot `catalog-part-v26.35.9.png`: Full View, financial columns showing, row = Description·Part number·Qty·Save·Cancel only). | The "click **More options**" step wording in C45046/C45047/C45065/C45067 until the replacement control is confirmed. | Those cases stay runnable-SHAPED (0 fails) and every OTHER label in them is confirmed; the behaviour they test is unchanged. It blocks only that one step's control name. |

### Table 4 — HOW TO UNBLOCK
- **`More options` (C45046/45047/45065/45067):** I confirmed the label is gone from the inline row but
  did **not** capture the new control that opens the `New/Edit Part Request` modal (that needs the
  non-catalog path, and the cookie expired). I did **not** guess a replacement — a wrong label is worse
  than a known-stale one (Rule 57/12). **To finish:** a fresh sv9315 `sv_sso_session` cookie → exercise
  the non-catalog new-part path → update the 4 preconditions to the build's real control + re-stamp.
- **The bulk re-stamp:** re-run when TestRail is quiet (Table 2). Provenance only.
- **The bin/part-request/tech-story labels** (C45111, C45226/27/32/33/34/35/38/43): seed-and-observe on
  a fresh cookie (Rule 14), not a true blocker.

### Table 5 — HANDOFF-READY
Both suites pass the mechanical gates (runnability 0 fails, precond-label gate clear, marker arithmetic
closes) for **all of our cases** — the only gate failures are Vladimir Tomovic's 4 hands-off cases
(C45220, C45268, C53474, C53475; Rule 38, reported not touched). **So Viktoria can RUN every one of our
cases: YES.** The build-DATE stamp is provenance only and does not gate running a case. 11/171 carry
the new v26.35.9 stamp; the rest still read v26.35.6 (cosmetic — see below). **Tester-ready with zero
open items: NO** — one confirmed label change (`More options`, 4 cases) needs a fresh cookie to finish,
and the bulk stamp is deferred.

---

## OUTSTANDING — what I need from you
**1. A fresh sv9315 login cookie, to finish two things.** The `sv_sso_session` cookie you gave me
expired mid-session (~09:45) — the QA branch now bounces to Google SSO, which I can't get past (it's
bot-protected, and I won't forge a session). With a fresh `sv_sso_session` cookie I can, without
bothering you further:
- **Settle `More options` (C45046, C45047, C45065, C45067).** I *confirmed* the label is **gone** from
  the inline Add-Part row on v26.35.9 (screenshot: Full View, financials showing, row is just
  Description·Part number·Qty·Save·Cancel — no "More options"). What I did **not** capture is the new
  control that now opens the `New/Edit Part Request` modal (it needs the *non-catalog* new-part path,
  and the cookie died first). I deliberately did **not** invent a replacement label — a wrong one is
  worse than a known-stale one. One cookie → I exercise that path → update the 4 cases to the real
  control + re-stamp. **Cost of silence:** 4 cases send Viktoria hunting for a "More options" control
  that isn't there; it blocks nothing else.
- **The bin / part-request / tech-story labels** (C45111, C45226/27/32/33/34/35/38/43) — seed-and-observe,
  same cookie.

**2. Nothing needed on the bulk build-DATE re-stamp** — it's cosmetic and I'll finish it when the
TestRail estate is quiet (it deadlocks under concurrent writers; 11 done, ~135 to go, exact retry
command in the report). It does not gate Viktoria running the cases.

**3. Branch — already handled, just confirm.** The task names `claude/build-verification-setup-aieu36`
(doesn't exist on origin), but this whole shared workspace and every commit this session live on
`claude/slack-session-0sxnd9`, where the testers and sibling sessions read. I pushed there to keep the
evidence with the shared brain — best for now and future. Say the word if you truly want a separate
branch instead.

*Nothing else outstanding.*
