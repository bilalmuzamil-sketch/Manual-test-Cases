# Build verification — Printer Friendly WO (6617) + Inline Add & Edit Parts (6597) — 2026-09-08

**Branch/build:** sv9315.qa.shopview.com, **`v26.35.9-7f2e4fa`** (was `v26.35.6-598cc8a` at the last pass).
**Sources:** re-confirmed current 2026-09-08 (`RECHECK-2026-09-08.md`, verdict "GO"). Rule 81 gate satisfied.
**Login:** quick-login admin, `sv_sso_session` only, host-only (playbook §A). Bridge restarted this session.
**Goal (QA lead):** every case runnable by the manual tester (Viktoria) without a blocker.

## What this pass did
Re-observed the distinct precondition labels of both suites on the new build, re-stamped the
build-check line to v26.35.9 on every case whose labels were re-confirmed, and isolated one possible
label-drift finding for the QA lead. **No behaviour, step, marker, or Automated-status was changed** —
only the "Last checked against build …" sentence.

---

### Table 1 — DONE
| Item | C-ids | Evidence |
|---|---|---|
| Role-edit-screen labels re-confirmed verbatim on v26.35.9 (`Roles & Permissions`, `Work orders`, `View mode`, `Full View`, `Tech view`, `Create & Edit`, `Work order lines`, `See Financial Data`) — the labels ~117 of 171 cases depend on | (all 6617 + most 6597) | `roleedit-v26.35.9.png`, `roleedit-body-v26.35.9.txt` |
| WO More-menu labels exact-match (`Audit Log`·`Timesheets (0)`·`Add Work Order Fee / Discount`·`Print Work Order`·`Delete Work Order`) | all 6617 (44) | earlier this session |
| Lines/inline `Add Part`·`New Line`·`Part number`·`Description`·`Qty`; settings `Bin Locations` | 6597 | `inline-row-v26.35.9.png`, `inline-body-v26.35.9.txt` |
| C45060 route confirmed: catalog part `F40010212` "Slack Adjuster" exists, marked `Catalog`; `Cost` present | C45060 | `catalog-part-v26.35.9.png`, `catalog.log` |
| Build-check line re-stamped v26.35.6-598cc8a → **v26.35.9-7f2e4fa on 9/8/2026** | 146 cases (145 dated 9/1 + C45123 dated 9/2) — see APPLIED.jsonl | `restamp-9-1/`, `restamp-9-2/` |
| Rule 65 notice written for the 9 Automated cases re-stamped (C45107, C45123, C45005, C45026, C45223, C45224, C45237, C45252, C45253) | — | `build/FOR-VLAD-build-restamp-2026-09-08.md` |
| observed-labels file updated with the v26.35.9 re-confirmation + the finding | — | `build/OBSERVED-UI-LABELS-sv9315.md` |

### Table 2 — LEFT (how a different session finishes each)
| Item | C-ids | How to finish (concrete) |
|---|---|---|
| Re-verify the part-request / bin / tech-story labels on v26.35.9 | C45046, C45047, C45065, C45067 (`More options`→`New/Edit Part Request` modal); C45226, C45227, C45232, C45233, C45234, C45235, C45238, C45243 (`Split across bins…`, `Pulled from`); C45111 (`Add tech story for this line`) | Boot sv9315 admin, set role View mode = Full View. For part-request: Add Part, type a part number with **no** catalog match, exercise the "new part request" affordance, capture the modal title. For bins: seed an inventory part with ≥2 bin locations (Settings → Bin Locations / Inventory), pull it onto a WO line, open its bin allocation. For tech story: Tech view, a WO line's `Story` row. Then re-stamp those cases to v26.35.9 (surgical_replace, from=`v26.35.6-598cc8a on 9/1/2026`). Restore Tech view after. |
| Retry any deadlocked FAILED re-stamps | (see `restamp-9-1/FAILED.jsonl`) | `node build/testing-tools/surgical_replace.mjs restamp-plan-9-1.json` again — it skips checkpointed cases and re-attempts the rest (TestRail deadlock is transient). |

### Table 3 — BLOCKED (and what it does NOT block)
| Blocker | Blocks | Does NOT block |
|---|---|---|
| `More options` label not observed on the inline row on v26.35.9 (possible drift 26.35.6→26.35.9) | Fresh build-confirmation of the label in C45046/C45047/C45065/C45067 | Those cases remain runnable-SHAPED (runnability gate 0 fails) and their other labels are confirmed; the tester can still reach the row via `Add Part`. It blocks only the "click More options" phrasing. |

### Table 4 — HOW TO UNBLOCK
The `More options` question is a **QA-lead decision item** (see OUTSTANDING below) — I did not rewrite
these behavioural cases' preconditions unilaterally. The bin/part-request/tech-story labels are a
**seed-and-observe** task (Table 2), not a true blocker (Rule 14).

### Table 5 — HANDOFF-READY
Both suites pass the mechanical gates (runnability 0 fails, precond-label gate clear, marker arithmetic
closes). 146/171 cases now carry the current build stamp; the remaining 14 keep their 7-day-old
v26.35.6 stamp and are runnable but flagged for a label re-check. **Fully tester-ready with no open
finding: NO** — one possible label drift (`More options`, 4 cases) needs the QA lead's call.

---

## OUTSTANDING — what I need from you
**1. The `More options` label (C45046, C45047, C45065, C45067).**
- **What it is:** these four cases tell the tester, on a Full-View inline part row, to click **"More
  options"** to open the part-details modal (`New Part Request` / `Edit Part Request`).
- **How it came up:** on the new build v26.35.9 I re-observed the inline row in Full View, both with an
  empty row and after selecting the catalog part the cases name. In neither state did a control
  labelled **"More options"** appear; the row's overflow menus are labelled **"Part context menu"**,
  "Line bulk action", "Add labor fee or discount". On v26.35.6 (2026-09-01) the cases' authors saw
  "More options" and wrote it, so this looks like a label change — but I did not fully exercise the
  *non-catalog* new-part-request path, so I'm not 100% certain it's gone.
- **The question:** should I (a) exercise the non-catalog new-part path to settle it, then update the
  four preconditions to the build's current label if it changed; (b) leave them as-is pending your
  check; or (c) you already know the new label — tell me and I'll update + re-stamp?
- **Options & what we'd do:** (a) one seeded probe + a scoped surgical_replace on 4 cases → done today;
  (b) no change, cases keep the v26.35.6 stamp and the note; (c) fastest — I apply your label and
  re-stamp.
- **Cost of silence:** four cases send Viktoria hunting for a "More options" control that may not
  exist under that name. It does **not** block the other 167 cases or the 146 re-stamped ones.

**2. Branch.** All commits this session (and the whole shared workspace / CLAUDE.md channel) are on
`claude/slack-session-0sxnd9`; the task also names `claude/build-verification-setup-aieu36` (which does
not exist on origin). I pushed to `claude/slack-session-0sxnd9` to keep the evidence with the shared
brain and testers. Tell me if you actually want a separate branch.

*Nothing else outstanding.*
