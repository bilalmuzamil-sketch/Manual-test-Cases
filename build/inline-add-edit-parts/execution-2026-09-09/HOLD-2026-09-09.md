# PAUSED at the QA lead's request — 2026-09-09, 14:19 UTC

He is source-verifying and build-verifying this suite himself. **No further execution, no results
written, no defects filed** until he says to resume.

## State at the moment of the hold

**Run R418 is untouched by this session.** `untested 113 · passed 1 · failed 0 · blocked 10`.
All 11 result rows belong to **Viktoria Videnovic (TestRail user 4)**; the newest is hers, 12:00 UTC.
Our account (user 3) has written **no result row** to this run in this pass.

## What WAS changed — 9 case texts, all through the UI editor, all verified

Done under his instruction "Do what is correct" after he agreed Viktoria's corrections were right.
Applied with `build/testing-tools/surgical_replace.mjs`, which refuses unless the search string
appears exactly once, re-reads through the API afterwards and asserts
`after === before.replace(from, to)`, and confirms the served page still renders in `markdown fr-view`.
Audit trail: `APPLIED.jsonl` in this folder.

### (a) Precondition status list — 7 cases

The two lists were each missing a different real status. Both now read the same complete, verified set.

| Case | Was | Now |
|---|---|---|
| C44988 | "Estimate, Approved, In Progress, or **Declined**" | "Estimate, Approved, In Progress, **Review, or Declined**" |
| C44989, C44990, C44991, C44992, C44995, C44997 | "Estimate, Approved, In Progress, or **Review**" | "Estimate, Approved, In Progress, **Review, or Declined**" |

Evidence for including Declined: on Declined work order **S9315-15897** the Add Part control is
present and visible (1) and the Edit controls are present (9) — so it is an editable status.

### (b) The stale tester note — 2 cases (C44993, C44994)

Was: *"of those three statuses only Paid exists in the data on this test system, so check it on a Paid
work order and mark the case Blocked saying so … (Two statuses this case used to name, "Declined" and
"Imported", are not statuses this product has at all …)"*

Now: *"all three of these statuses exist on the test system, so check all three. The full list of work
order statuses is Estimate, Approved, In progress, Review, Declined, Complete, Invoiced and Paid —
"Declined" is a real status and the button is shown on it, while "Imported" is not a status this
product has."*

Both halves of the old note were false on this branch:

| Claim | Reality on `v26.36.0-f43b2fd` |
|---|---|
| "only Paid exists in the data" | Complete (a whole tab), Invoiced **S2-15828**, Paid **S9315-15894** — all present |
| "Declined … not a status this product has at all" | Declined **S9315-15897** exists |

## What is READY but NOT written — 8 Passed results

Sitting in `RESULTS.json`, dry-run clean, waiting on his go-ahead:
C44988 · C44989 · C44990 · C44991 · C44992 · C44993 · C44994 · C44997.
Each carries the standing footer he asked for: *"Tested on the QA branch. This will be re-tested on
Staging once the change reaches it, and the result confirmed there."*

To write them when he resumes:

```
python3 build/testing-tools/push_results_to_run.py --run 418 \
  --results build/inline-add-edit-parts/execution-2026-09-09/RESULTS.json \
  --build-marker v26.36.0-f43b2fd --date "9 September 2026" --env "the sv9315 QA branch" \
  --footer "Tested on the QA branch. This will be re-tested on Staging once the change reaches it, and the result confirmed there."
```

## Environment left as found, except the two authorised changes

- **Admin staff → Admin role** (was the custom "Tech View See financial ON"), reset before assigning,
  all permissions on bar one. `view_mode: full`. This is now his standing rule, so it stays.
- **"Move labor" will not persist** in the Admin role — Save fires only `POST /api/check-existing-roles`
  and no role-update call. He directed this be ignored as a separate, unrelated bug.
- No test data was seeded. No work order was modified. The one inline row opened during C44997 was
  abandoned unsaved.

## Still open, for whenever he resumes

| Item | Note |
|---|---|
| 115 cases unexecuted | Tech View (38) needs a Tech View session; Full View (36) now reachable; Unsaved Data (15); Bin Allocation (22) |
| C44995 | Needs the Create-and-Edit permission off on a non-admin role — not yet run by us |
| C45250 / C45251 | Need a completed line whose part has been **picked**, not merely in stock |
| C45250 redesign | Viktoria wants it renamed and rewritten as a Work Order test. **Held — I think she is wrong**: the case is deliberately about a completed LINE, C45220 confirms completed lines are a real concept, and C44993 already covers the work-order status. Needs his ruling |
| C45268, C53474, C53475 | Vladimir Tomovic's, outside R418, hands-off |
