# OUTSTANDING ITEMS REGISTER — everything we are waiting on, across every project

## 🆕🆕 2026-08-19 (2nd message) — REPORT SUITE / WIP: Chris Ward's SECOND WIP message. **AGING IS PER JOB AND FINAL → THE PER-LINE-AGING HOLD (RS-WIP-4) IS CLEARED; QUOTE AGE = FUTURE SV-9372; S11-R7 CORRECTED → WIP SNAPSHOT/STORY-11 CASES NEED A LIVE RE-CHECK; WIP SPEC IS NOW v24.**
Source: `build/report-suite/chris-answers-2026-08-19/WIP-CHRIS-RULINGS-2026-08-19.md` (2nd-message section, verbatim + interpreted; DOCS-ONLY, **no TestRail / Jira / staging write**). This is Chris's SECOND WIP message of 2026-08-19 and it **SUPERSEDES / FINALISES** the earlier "confirming intent, will come back to you" note on per-line aging. Authoritative source (c) under Rule 57, newest product source (Rule 32).
- **✅ RS-WIP-4 CLEARED (per-line aging HOLD LIFTED)** — Chris [1], verbatim: *"Aging is per job, and that's final. Days Open is whole days since the work order's date. On a job that produces two rows — its status tab plus Estimates — both rows show the same number. That's correct behavior, not a bug: the money splits by line state, the clock doesn't. The spec's stray line about unapproved lines ageing from the line's creation date is gone as of page v24, so nothing conflicts now."* → **WIP-COL-07 = [C30472](https://shopview.testrail.io/index.php?/cases/view/30472)** keeps whole-job Days Open (S4-R12) + gains "both rows of a two-row job show the same number — correct, not a defect"; **NO per-line aging** asserted; **the shared Days Open value must NOT be filed as a defect.** Moved to §7 Recently cleared.
- **🆕 RS-WIP-7 — QUOTE AGE column = FUTURE / NOT STARTED ([SV-9372](https://shopview.atlassian.net/browse/SV-9372), Parth).** Chris [2], verbatim: *"A separate Quote Age column will come later — SV-9372, Parth's build, and not started yet. Nothing to test there for now; just don't file the shared Days Open value as a defect in the meantime."* *What it blocks:* nothing to test now; any WIP case that touches a Quote Age column is **feature-not-built / deferred (Rule 69)** — do NOT file, and do NOT file the shared Days Open value as a defect meanwhile. No such case exists in the WIP suite today. *Re-check trigger:* SV-9372 shipping, NOT a redeploy (Rule 49/61). Owner: engineering (feature ship). Since 2026-08-19.
- **↺ RS-WIP-6 UPDATED — the WIP page is now pinned at Confluence v24 (was "ahead of v22"), AND S11-R7 was rewritten.** Chris [3], verbatim: *"S11-R7 was wrong and is now fixed. It said no screen reads the snapshot. It does — pick any earlier 'as of' date and the report reconstructs that day from the nightly snapshot (S7-R8a). Only the trend view is unbuilt. If you tested against the old wording, that's a real behavior to re-check."* → the queued WIP reconciliation must pull **v24** (local baseline v22) and additionally **RE-CHECK the S11-R7 snapshot-read behaviour LIVE**: an "as of" date reconstructs the day via **S7-R8a**; only the **trend view** is unbuilt. Candidate WIP snapshot / "as of" / Story-11 cases (read-only): **WIP-TAB-05 [C30455](https://shopview.testrail.io/index.php?/cases/view/30455)** (cites S11-R7 — no-trend confirmed, re-check it does not carry the old "no screen reads snapshot" wording), **WIP-FLT-05 [C30502](https://shopview.testrail.io/index.php?/cases/view/30502)** (cites S7-R8a), **WIP-FLT-04 [C30501](https://shopview.testrail.io/index.php?/cases/view/30501)**, and the History family **WIP-API-01 [C30528](https://shopview.testrail.io/index.php?/cases/view/30528) / [C30530](https://shopview.testrail.io/index.php?/cases/view/30530) / [C30531](https://shopview.testrail.io/index.php?/cases/view/30531) / [C30533](https://shopview.testrail.io/index.php?/cases/view/30533)**, **WIP-SCOPE-05 [C30460](https://shopview.testrail.io/index.php?/cases/view/30460)**. Owner: us (queued pass). Since 2026-08-19.

---

## 🆕 2026-08-19 — REPORT SUITE / WIP: Chris Ward's THREE WIP rulings recorded (History grain · Aging · spec hygiene). **THE TAB-PLACEMENT SELF-CONTRADICTION IS RESOLVED AT SOURCE; ONE NEW HOLD (per-line aging); ONE NEW SPEC-CURRENCY ASK.**
Source: `build/report-suite/chris-answers-2026-08-19/WIP-CHRIS-RULINGS-2026-08-19.md` (rulings captured verbatim + interpreted; DOCS-ONLY, **no TestRail / Jira / staging write**). Chris Ward is the Report Suite/WIP PO — his answers are authoritative source (c) under Rule 57 and the newest product source (Rule 32). A **WIP TestRail content-reconciliation pass applying these rulings (interim `<br>` format) is QUEUED after the current TU + WIP + IV sweep** — nothing executed here.
- **✅ RS-WIP-5 + RS-BV-4(a) CLEARED** — the WIP tab-placement spec self-contradiction (S2-R4 / Story 3 status model vs the §3 SV-9027 line-state Key Decision) is **RESOLVED by Chris.** Ruling 3, verbatim: *"already tidied on the live Confluence page — Story 3 and S2-R4 now describe the line-state model (new S3-R5, S3-R6). Pull the live page; local copies are behind."* Moved to §7 Recently cleared. Consequence: our cases follow the line-state model and now AGREE with the tidied live spec — the Rule-56 divergence note on those cases can be retired in the reconciliation pass.
- **✅ RS-WIP-3 ANSWERED / CONFIRMED** — nightly-snapshot granularity. Ruling 1, verbatim: *"one row per job per tab, keyed by work order + tab + date (S11-R1). Already built that way … Note a job writes at most two rows (its status tab, plus Estimates if it has unapproved lines), and Adjustments belong only on the status-tab row (S3-R6)."* → **WIP-API-01 = [C30528](https://shopview.testrail.io/index.php?/cases/view/30528) is correct as written** (grain = one row per job per tab per date). Moved to §7 Recently cleared; the reconciliation pass adds the max-two-rows + Adjustments-only-on-status-tab-row detail.
- **✅ RS-WIP-4 — CLEARED 2026-08-19 by Chris's 2nd message (aging is per job and *"that's final"*; the stray per-line line is gone as of page v24). See the 2026-08-19 (2nd message) section at the top + §7. The HOLD text below is retained as the record of the 1st-message state.** ~~🔴 RS-WIP-4 — WIP PER-LINE AGING = HOLD (updated with Chris's verbatim ruling; supersedes the earlier "author only once pinned" framing).~~ *What was held:* whether an unapproved line ages from the LINE's own date (per-line aging) rather than the whole job. *The ruling, verbatim (Rule 48):* Ruling 2 — *"per job — Days Open is whole days since the work order's date (S4-R12), and both rows of a two-row job show the same number. There's a stray line in the spec suggesting per-line aging for unapproved lines; it isn't built. Please don't build it yet — I'm confirming the intent and will come back to you."* *What it blocks:* no per-line-aging case may be authored; **WIP-COL-07 = [C30472](https://shopview.testrail.io/index.php?/cases/view/30472)** keeps whole-job Days Open (S4-R12) and gains the "both rows of a two-row job show the same number" detail, asserting NO per-line aging. *Why reasonable:* the source is a stray/unconfirmed spec line and the build does not implement it (Rules 57/58). *What unblocks it:* Chris's follow-up confirming the intent. Owner: Chris Ward (via QA lead). Since 2026-08-19.
- **🆕 RS-WIP-6 — SPEC CURRENCY (Rule 31/59): local WIP baseline is v22; live Confluence page 703660034 is AHEAD** (new S3-R5/S3-R6 line-state + S11-R1 history grain, per Ruling 3). *What it blocks:* the reconciliation of WIP cases to the tidied line-state wording. *What is needed:* pull the LIVE WIP Confluence page (pageId **703660034**) and reconcile — the **WIP reconciliation pass is QUEUED** after the TU + WIP + IV sweep. Owner: us (queued pass). Since 2026-08-19.

---

## 🆕🆕🆕🆕🆕 NEWEST — 2026-08-19, SCHEDULE BUILD-VERIFICATION BLOCKED ON FRESH STAGING COOKIES. **134 CASES (BATCHES B + C) WAITING; BATCH A (57) DONE. THE #1 CROSS-PROJECT ASK.**

Source: `build/schedule/build-verify-2026-08-18/{SCHEDULE-PLAN.md, A-EXECUTION.md, A-FINDINGS.md, B-EXECUTION.md, DEFERRED-RUN.md}` + the consolidated coverage table `build/BUILD-VERIFICATION-COVERAGE-2026-08-19.md`. Schedule = epic **SV-8685**, PO **Branko Cicovic**, 195 cases in TestRail group 4254. **Batch A (61 cases) is build-verified on `v3.8-bd246fd`** — 57 READY (byte-verified `update_case`) + 4 feature-absent deferred (Rule 69). **Batches B (66) + C (68) = 134 cases are NOT verified: a container reset (~06:30 UTC 2026-08-19) wiped the live staging session from ephemeral `/tmp`, and no session can be minted here** (`quick-login` is itself SSO-gated → HTTP 401 `sso_required`). The build is reachable and unchanged (`v3.8-bd246fd`); **the blocker is authentication, not the build.** 0 TestRail writes, 0 Jira writes (Rule 62 hold H1 stands). Tester-facing defect file (Batch A's one deviation + all Report Suite defects): `build/report-suite/build-verify-2026-08-18/Defects-for-Testers_ReportSuite-and-Schedule_2026-08-19.{xlsx,md}`.

| Row | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **SCH-BV-1** | **Fresh `.staging.shopview.com` session cookies — `sv_sso_session` + `PHPSESSID` + `cf_clearance`** — dropped into **`/tmp/staging-cookie.txt`** (header form) or **`/tmp/cln/cookies.json`** (json form). Mintable only by the user via `auth.staging.shopview.com/login` (Rule 22). **Ruling context (Rule 48):** no ruling blocks this — it is a plain access ask; the prior session simply expired with the container. **Why it was reasonable:** `/tmp` is ephemeral by design; cookies always die with a container reset. **The one thing that unblocks it:** the three cookie values, from the QA lead. | **QA lead (user)** | **Schedule batches B + C = 134 cases** cannot be build-verified. Batch B (drag/scope/spread/shift/reassignment, 66) resumes verbatim the moment cookies land — batch-A boot2 recipe applies unchanged, build unmoved. Batch C (events/conflicts/capacity/deletion/settings/permissions/API, 68) starts after. **Nothing is written to TestRail without live observation (Rule 69) — a metadata-only marker write would disarm the cases.** | 2026-08-19 (~06:30 UTC container reset) |
| **SCH-BV-2** | **A 2nd non-admin (single-location) test sign-in** for the Schedule branch — for the 13 Permissions cases (§4279) that need per-role UI gating. | QA lead (user) | The 13 Schedule Permissions cases (in batch C) — same recurring blocker as all three projects. Confirm a 2nd login is available, or rule to skip the negative-permission checks. | 2026-08-19 |
| **SCH-BV-3** | **~~4~~ 3 Schedule features not found in the build (Rule 69 deferred)** — [C30005](https://shopview.testrail.io/index.php?/cases/view/30005) shift edge-resize, [C43812](https://shopview.testrail.io/index.php?/cases/view/43812) day-view zoom, [C43813](https://shopview.testrail.io/index.php?/cases/view/43813) day-view clipped-block chevron. **✅ [C29945](https://shopview.testrail.io/index.php?/cases/view/29945) Priority filter REMOVED from this list — RESOLVED by Branko's ruling 2026-08-19** (see SCH-BV-4). | Engineering (feature ship) | Whether these 3 cases lift from `Not available on Build` to `READY`. **Re-check trigger = the feature shipping, NOT a redeploy** (Rule 49/61). | 2026-08-18 (Batch A) |
| **SCH-BV-4** | **✅ RESOLVED (proposal owed to QA lead) — batch-A D1 Priority-filter flag CLOSED by Branko's ruling 2026-08-19**, verbatim *"Proceed without it, I'll remove that part from the PRD."* The sidebar filter **Priority** group (spec v30 §5.1) is **intentionally removed**, not pending dev — so [C29945](https://shopview.testrail.io/index.php?/cases/view/29945) (SCH-FILT-04) was wrongly DEFERRED as "not built". Proposal (RE-SCOPE C29945 → negative "popover offers only Assignment + Status; AUTOMATION: READY"; TWEAK [C29942](https://shopview.testrail.io/index.php?/cases/view/29942) SCH-FILT-01 to drop Priority): `build/schedule/build-verify-2026-08-18/PRIORITY-FILTER-BRANKO-RULING-2026-08-19.md`. **⚠️ Spec-currency (Rule 59): PRD v30 §5.1 STILL lists a Priority group — Branko to update it to drop Priority; until then §5.1 is STALE on this point and cases follow Branko's later decision (Rule 32/56).** | **QA lead** to authorise the re-scope/tweak writes (hold H1 / Rule 62) · **Branko** to publish the PRD update dropping §5.1 Priority | 2 case edits (C29945 re-scope + C29942 tweak) stay unexecuted; the deliberate-removal coverage is not yet in TestRail. | 2026-08-19 |

| **SCH-BV-5** | **Schedule redeployed 2026-08-19 (Stefan V, `v3.8-b7d80dc`) with broad UI changes + SV-9361/SV-9357 fixes + a behavior change → all 195 Schedule build-verify verdicts are PROVISIONAL vs the new build (Rule 60 layers 1-2); a Schedule re-check against `v3.8-b7d80dc` is QUEUED (fold into the Schedule permission-residual + Priority-fix step, before Filters). Source-currency note: Branko's "Schedule V2 canvas" design is the conformance source Stefan cited — confirm our design baseline is current (Rule 31/57).** | QA lead (re-check go-ahead) / Engineering (deploy already done) | The build move (`v3.8-da72171`/`bd246fd` → `v3.8-b7d80dc`) invalidates on-screen labels + pass/fail verdicts on the 195 cases (Rule 60 layers 1-2); some already-recorded defects (esp. C30029 conflict styling, the B3 spread-modal hours error, SV-8870 Month-view drag) may now be FIXED — tester defect sheet banner-flagged so testers don't file already-fixed defects. | 2026-08-19 |

**⭐ THE ONE THING I MOST NEED FROM YOU: fresh staging cookies (SCH-BV-1) — it is the sole blocker on finishing Schedule build-verification (134 cases).**

---

## 🆕 2026-08-19 — REPORT SUITE / SBC RE-VERIFY SWEEP COMPLETE: all 36 manual SBC cases WRITTEN in interim `<br>` format
Source: `build/report-suite/build-verify-2026-08-18/SBC-SWEEP-{EXECUTION,FINDINGS,HELD-AUTOMATED}.md` +
`sbc-sweep-oplog.jsonl`. The QA lead **accepted the interim `<br>` line-break format** (TestRail API
still wraps markdown → `<ol>/<li>` cannot be stored; `<br>` renders correctly, verified on C30133). All
**36 atm=1 SBC cases written + render-verified** (29 READY / 7 HOLD); **10 Automated (atm=3) HELD, 0
writes**. Four cases newly driven live this run (C30132 reverse-invoice, C30137 dup-label suffix, C30101
location enforcement, C43550 column-selector). **Run 359 untouched · 0 Jira · 0 foreign.** Build
redeployed mid-pass `v3.8-da72171` → `v3.8-b7d80dc`; stamps name the observed build; verdicts PROVISIONAL.
- **SBC-SWEEP-1 — CLEANUP DEBT: 36 SBC cases are written in interim `<br>` format; demark to plain
  numbered text once the TestRail `update_case` markdown-wrap API regression is fixed** (re-test cheaply
  by writing a throwaway string to C30133 and re-GET; if no `<p>`, the block has lifted → run
  `build/markup-regression-2026-08-10/demark.py` + census). Owner: US (on API fix). Since 2026-08-19.
- **SBC-SWEEP-2 — 5 invoice-link HOLD cases (C30100/C30139/C30140/C30141/C43558)** wait on Chris Ward's
  answer to the invoice-number link-vs-plain-text question (same as RS-BV-4b). Owner: Chris Ward (via QA
  lead). Since 2026-08-18.
- **SBC-SWEEP-3 — 2 genuine-dependency HOLDs:** C30131 (build blocks a no-vehicle service WO → 500;
  trigger = a build that allows it) · C43553 (broken-logo-file needs storage-layer orphaning, dev/infra;
  case sanctions Blocked). Owner: dev/build. Since 2026-08-19.
- **SBC-SWEEP-4 — C43550 single-location live LOGIN not performed** (fresh single-workplace reports staff
  created but unconfirmed → switch-user 401; yopmail unreadable via proxy). Verdict READY on structural +
  accessible-locations-mechanism evidence. A confirmed single-location sign-in would close it directly.
  Owner: QA lead (2nd sign-in, overlaps RS-BV-5). Since 2026-08-19.
- **Env residue:** 3 ZZAUTOTEST seed WOs left Complete-but-un-invoiced (reversed, so absent from the SBC
  report) — not fully deletable (Complete-status transition blocked); 1 unconfirmed ZZAUTOTEST staff
  invite (inert, cannot authenticate). Disposable-env clutter, noted.

## 🆕 2026-08-19 — REPORT SUITE / SBR RE-VERIFY SWEEP COMPLETE: 57 manual SBR cases WRITTEN in interim `<br>` format
Source: `build/report-suite/build-verify-2026-08-18/SBR-SWEEP-{EXECUTION,FINDINGS,HELD-AUTOMATED}.md` +
`sbr-sweep-oplog.jsonl`. Build **`v3.8-b7d80dc`** (read live start+end; supersedes 8/18's `v3.8-bd246fd`,
same-minor bug-fix, Rule 60). The 57 SBR cases (`created_by=3`, `atm=1`) that carried no fresh build-check
were re-stamped in the accepted interim `<br>` format + normalization-aware re-verified; **0 anomalies,
0 content changes, 0 marker changes.** Live atm re-read found **14 Automated (atm=3), not 4** as the 8/18
doc said — **all 14 HELD, 0 writes** (Rule 71). **Run 359 untouched (508 tests) · 0 Jira · 2 foreign
untouched (C38923/C43981).**
- **SBR-SWEEP-1 — CLEANUP DEBT: 57 SBR cases (+36 SBC) are written in interim `<br>` format; demark to
  plain once the TestRail `update_case` markdown-wrap API regression is fixed** (re-test via C30133
  canary; when no `<p>`, run `demark.py` + census). Owner: US (on API fix). Since 2026-08-19.
- **SBR-SWEEP-2 — Ratify the 14 SBR Automated (atm=3) cases** (verified live, HELD). C30221 wants a
  marker lift (expand-tree built); other 13 already READY. Owner: QA lead (Rule 71). Since 2026-08-19.
- **SBR-SWEEP-3 — 3 defect reopens/closes flagged (Jira hold H1):** SV-8973 (empty-state wording, C30298)
  + SV-8975 (icon aria-labels, C30307) still reproduce → reopen/refile; SV-8823 (CSV money, C30287)
  looks fixed → confirm & close. Owner: QA lead. Since 2026-08-18.
- **SBR-SWEEP-4 — 1 genuinely-absent-feature deferred (Rule 69):** C30311 (WO-rep-assignment path; no
  rep-assigned invoices exist). Trigger = the UI shipping / a rep-assigned invoice, NOT a redeploy.
  FLAGGED: confirm truly absent vs unseeded. Also C30202 HOLD is a 366-day harness limit (runnable
  manually) — review whether it should be READY. Owner: QA lead. Since 2026-08-19.
- **SBR-SWEEP-5 — PO-dependency HOLDs (C30310/C30315/C43559)** wait on Chris Ward's invoice-link
  plain-text-vs-link answer (same as SBC-SWEEP-2 / RS-BV-4b); C43559 also needs the 2nd non-admin
  sign-in (RS-BV-5). Owner: Chris Ward / QA lead. Since 2026-08-19.
- **SBR-SWEEP-6 — 2 EXPECT-FAIL kept (C30290/C30320, SV-8818):** over-cap Expanded-PDF / API row-cap not
  reachable at 88 invoices. Trigger = a > row-cap data size. Owner: seed on a future pass. Since 2026-08-18.

---

## 🆕 2026-08-19 — REPORT SUITE / PV RE-VERIFY SWEEP COMPLETE: 36 manual PV cases WRITTEN in interim `<br>` format
Source: `build/report-suite/build-verify-2026-08-18/PV-SWEEP-{EXECUTION,FINDINGS,HELD-AUTOMATED}.md` +
`pv-sweep-oplog.jsonl`. Build **`v3.8-d0e135e`** (read live start 13:46Z + write-start 14:04Z + end
14:12Z, byte-stable; supersedes 8/18's `v3.8-bd246fd`, same-minor bug-fix, Rule 60). The 36 PV cases
(`created_by=3`, `atm=1`) that carried no fresh build-check were re-stamped in the accepted interim `<br>`
format + normalization-aware re-verified; **0 anomalies, 0 content changes, 0 marker changes.** Live atm
re-read found **13 Automated (atm=3), not 8** as the 8/18 doc said — **all 13 HELD, 0 writes** (Rule 71).
The report was driven live (nav, defaults, all filters + 366-day validation, two-location/special-order
row model, 20 columns + picker, calc contract Margin%=0-mismatch + movement-vs-billed, CSV export, PDF-500,
localStorage view, reports gate, dark mode). **Run 359 untouched (508 tests) · 0 Jira (one SV-8818 GET) ·
3 foreign untouched (C38920/C43567/C43568).**
- **PV-SWEEP-1 — CLEANUP DEBT: 36 PV cases (+57 SBR +36 SBC) are written in interim `<br>` format; demark
  to plain once the TestRail `update_case` markdown-wrap API regression is fixed** (re-test via C30133
  canary; when no `<p>`, run `demark.py` + census). Owner: US (on API fix). Since 2026-08-19.
- **PV-SWEEP-2 — Ratify the 13 PV Automated (atm=3) cases** (verified live, HELD). Marker changes wanted:
  C30346 + C30353 lift `Not available`→`READY` (built); C30352 strip stale SV-8938 expect-fail (confirm PO
  position first); **C30328 needs review** ("All types" vs "Both" + multi-vs-single-select — possible
  EXPECT-FAIL). Owner: QA lead (Rule 71). Since 2026-08-19.
- **PV-SWEEP-3 — C30372 (core exclusion) HOLD kept: org has 0 `is_core=true` parts, no part-create
  endpoint found** (`POST /api/inventory/parts`=405, `/api/catalog/parts`=404). Trigger = a seeded core
  SKU + movement, NOT a redeploy. Owner: QA lead / seed pass. Since 2026-08-19.
- **PV-SWEEP-4 — 3 characterized data/integration limits (kept READY, flagged not skipped, Rule 74 §8.5):**
  C38924 (no fractional-unit sale in data — seed one), C38925 (QuickBooks not confirmably connected —
  connect QB + fractional sale), C30340 negative branch (no single-location user — admin=8/tech=5; supply
  one). Owner: QA lead / seed pass. Since 2026-08-19.
- **PV-SWEEP-5 — 2 EXPECT-FAIL kept (C38885/C43547, SV-8818):** PDF export 500 (CSV works) re-confirmed
  live; ticket OPEN "TESTING QA" (Jira GET). PDF-content cases (C30382 + PDF sides) blocked by the SV-8818
  build defect — CSV sides verified. Owner: engineering (SV-8818 fix). Since 2026-08-18.

---

## 🆕🆕🆕🆕 2026-08-18, THE SCHEDULE AUG-5 DESIGN-REVIEW RECONCILIATION. **5 CARRYOVER / WHOLE-WO ITEMS THE REVIEW MARKED V1 BUT SPEC v30 IS SILENT ON — NOW ON BRANKO'S FOLLOW-UP SHEET (Qs 4-8), NOT AUTHORED FROM SILENCE.**

Source: `build/schedule/aug5-review-2026-08-17/RECONCILIATION.md`. The 5 August design review
(Fabian / Sasha; 3 bugs + 16 enhancements) was reconciled against the live Schedule suite (195
cases) and Schedule spec Confluence **v30** (13 Aug 2026, read live). **Result: 0 gaps, 0 new cases,
0 case edits** — the suite already reflects v30, which subsumes the review's V1 scope (every V1 item
covered; every out-of-scope item confirmed absent). **The ONLY review "Yes"/TBC items not in the
suite are 5 carryover / whole-WO items that spec v30 is entirely SILENT on** — so under Rule 32
(newest authoritative source wins) they are not currently V1, and under Rule 58 we do not author from
a silent source. They go to Branko as **follow-up questions 4-8** on the existing sheet
(`build/filters/questions-2026-08-17/Filters-and-Schedule_Questions-for-Branko_2026-08-17.{xlsx,md,docx}`;
follow-up-only copy `…_FOLLOW-UP-5_2026-08-17.docx`).

**Status confirmations (Standing Rule 36):**
- **PO question sheets SENT:** the **Chris Ward** sheet (2 Report Suite questions) and the **Branko**
  Filters/Schedule sheet (**questions 1-3**) were **SENT on 2026-08-17** (per the QA lead). Branko
  follow-up **questions 4-8** (the rows below) are **HELD pending the QA lead sending them**
  (Standing Rule 66 — the question sheet is the last thing sent). Nothing written to TestRail or Jira.
- **Jira ticket-creation hold ACTIVE:** row **H1** stands — *"Do not create anything until my next
  order."* No Jira ticket was created this pass; the 5 items are **product decisions for the PO**, not
  defects.
- **Build-verify pending:** all **195** Schedule cases carry `Not available on Build to test Yet`
  (Standing Rule 69). A fresh `.qa.shopview.com` sign-in is still owed (the estate-wide
  `sv_sso_session` ask at the top of this file; Schedule row **SCH-FR-3**).

| Row | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **SCH-A5-1** | **Branko decision (Q4)** — a user-level "always schedule the whole work order" preference (remember the choice so the scope picker stops asking whole-WO vs by-line each time). Aug-5 review E6 marks it V1 **and** an open question; spec v30 SILENT (0 hits). | Branko (PO) | Whether one preference case is authored — unauthored until answered (Rule 32 v30 wins / Rule 58 no inventing from silence). | 2026-08-05 (review) / raised 2026-08-18 |
| **SCH-A5-2** | **Branko decision (Q5)** — restore the one-click "carryover" button (duplicates a scheduled block to the next day at the same time; present in the old scheduler, absent now). Aug-5 review E15; spec v30 SILENT on carryover entirely. | Branko (PO) | Whether carryover-button case(s) are authored. | 2026-08-05 / raised 2026-08-18 |
| **SCH-A5-3** | **Branko decision (Q6)** — rename "Carryover" to "Add a Day" / "Extend a Day" (final wording TBC). Aug-5 review E7; spec v30 SILENT. Depends on SCH-A5-2. | Branko (PO) | The label used when the carryover case is authored. | 2026-08-05 / raised 2026-08-18 |
| **SCH-A5-4** | **Branko decision (Q7)** — for a multi-day shift, carryover should add ONE more day, not duplicate the whole span. Aug-5 review E8; spec v30 SILENT. Depends on SCH-A5-2. | Branko (PO) | The behaviour asserted by the carryover case. | 2026-08-05 / raised 2026-08-18 |
| **SCH-A5-5** | **Branko decision (Q8)** — allow dragging a shift onto the next day directly in week view (alternative to the carryover button). Aug-5 review E9; spec v30 SILENT (reassignment across tech rows is covered separately by SCH-REAS-01 / [C30052](https://shopview.testrail.io/index.php?/cases/view/30052)). | Branko (PO) | Whether a week-view drag-to-next-day case is authored. | 2026-08-05 / raised 2026-08-18 |

*(The 3 review bugs are not outstanding: B1/SV-8915 closed OBSOLETE, B4/SV-8916 superseded-and-covered
by SV-9242, B5/SV-8917 live and covered — recorded for the build-verify sync in
`build/schedule/aug5-review-2026-08-17/KNOWN-FAILURES-FOR-SYNC-aug5review.md`.)*

---

## 2026-08-18 — REPORT SUITE: BUILD-VERIFICATION COMPLETE (all 6 reports, build v3.8-bd246fd)
Source: `build/report-suite/build-verify-2026-08-18/` — `COMPLETION-TABLE.md`, `AUTOMATION-RATIFICATION-LIST.md`, `FLAGGED-DEFECTS-FOR-JIRA.md`, plus the per-report EXECUTION/FINDINGS/HELD-AUTOMATED/DEFERRED-RUN files. **All 6 reports build-verified on `v3.8-bd246fd`** (SBC on `v3.8-2bf8d14`, an earlier same-minor bug-fix build — Rule 60, not stale). Census: ours **508** (= run 359's 508 tests) / live **518** / foreign **10** (Vladimir Tomovic id 1, 0 touched). **298 writes byte-verified; 0 run/result writes (run 359 untouched); 0 Jira writes.** Marker split live: READY 442 / EXPECT-FAIL 13 / HOLD 40 / NOT-AVAILABLE 13; ready-to-automate 455 (gate closes both ways). Every report is fully built; the only genuinely absent feature is the TU Total Hours link (4 deferred cases).
- **RS-BV-1 — Ratify the 39 Automated cases (GO-AHEAD needed, Rule 71):** (A) 4 SBC Automated cases already edited need **retrospective ratification** (C30107, C30114, C30121, C30123); (B) 35 held Automated cases (SBR 4 / PV 8 / TU 8 / WIP 10 / IV 5) need **go-ahead before editing** — 16 of the 39 change what an automated run concludes. Full table + your-decision column: `AUTOMATION-RATIFICATION-LIST.md`. *Reply "ratify the Automated list" (or edit-differently).* On approval each edit is coupled with build verification then handed to Vlad (register `build/fabian-review-2026-08-17-CONSOLIDATED/AUTOMATED-CASES-REGISTER.md`). Owner: QA lead. Since 2026-08-18.
- **RS-BV-2 — 26 flagged defects awaiting the Jira-creation hold to lift (Rule 62/73):** 25 closed-OBSOLETE tickets that STILL reproduce on v3.8 + 1 NEW deviation (TU-EXP-07, no ticket). Recommend REOPEN/REFILE, one at a time against the Rule-73 checklist. **Ruling that blocks it (verbatim, Rule 48):** *"Do not create anything until my next order"* (2026-08-10, row H1) + *"Even then we will keep a hold on creating tickets until I allow you to create the tickets"* (2026-08-17). *Blocks:* the tester will fail these closed-but-reproducing defects with no live ticket to point at. Full table: `FLAGGED-DEFECTS-FOR-JIRA.md` Cluster 1. **The one that would unblock it:** your "you may create the tickets" order. Owner: QA lead. Since 2026-08-18.
- **RS-BV-3 — Close-as-fixed: SV-8823 (still OPEN — TESTING QA) appears FIXED** (SBR C30287 + IV C30589 CSV money now plain numbers). ⚠️ Verify the CSV **honours column-selection-in-export** (IV sub-claim NOT tested; C30588 keeps an SV-8823 note) BEFORE closing. Owner: QA lead. Since 2026-08-18.
- **RS-BV-4 — 3 PO questions for Chris Ward (document conflicts, Rules 32/57/58):** (a) **WIP tab-placement spec self-contradiction** — S2-R4 vs the SV-9027 line-state Key Decision (also RS-WIP-5) — **✅ ANSWERED/RESOLVED by Chris 2026-08-19 (he tidied the live spec to line-state, new S3-R5/S3-R6); see the 2026-08-19 pass above + §7**; (b) **invoice-number link-vs-plain-text** (SBC/SBR — spec states both) — **still open**; (c) **PV Location-column position** (SV-8938 contested — get the intended position before treating 6th as a defect) — **still open**. (b) and (c) HELD pending the QA lead sending (Rule 66). Owner: Chris Ward (via QA lead). Since 2026-08-18.
- **RS-BV-5 — Second, non-admin (single-location) test sign-in for the Report Suite branch.** ~20 permission-negative cases across all 6 reports (SBC 6, SBR-PERM, PV-PERM-02, TU-LOC-05/TU-NAV-07, IV/WIP entitlement checks) can't be driven with one admin cookie without rotating the shared session (quick-login/switch-user avoided — shared-session safety). *Supply a 2nd non-admin session, or say to skip the negative-permission checks.* Owner: QA lead. Since 2026-08-18.
- **RS-BV-6 — TU Total Hours LINK feature is ABSENT from the build** → 4 TU-LINK cases deferred (C30428, C30430, C30432, C30433) + 1 Automated case flagged for review (C30429). Re-check trigger = the link shipping, NOT a redeploy (Rule 49/61). Owner: engineering (feature ship). Since 2026-08-18.
- **RS-BV-7 — Location-column defects need a ticket once the Jira hold lifts:** SBC C38912 (build does not follow the ratified Location rule) and WIP C30467/C43551 (Location absent from the Column Selection control → one edit from EXPECT-FAIL once a ticket exists). Owner: QA lead (Jira hold). Since 2026-08-18.

---

## 2026-08-18 — REPORT SUITE / WIP: Chris Ward's WIP ruling applied (Q1=A, Q2=B) + 1 new case
Source: `build/report-suite/chris-answers-2026-08-18/`. WIP spec re-confirmed live **v21** (Rule 59). Applied Chris's **Q1=A** (keep longer Estimates tooltip — confirmation) + **Q2=B** (WIP = sum of lines → a work order appears in EVERY matching tab by line state). **4 manual cases updated + byte-verified** (C30456, C30458, C30464, C30493); **1 new case C43979** (WIP-PLACE-05); **2 Automated cases HELD** (C30462, C30452 — Rule 71). Census: ours **508** / live **517** / foreign **9** (Vladimir id 1, 0 touched).
- **RS-WIP-1 — Build-verify + reword the 2 held Automated WIP cases (C30462 WIP-PLACE-01, C30452 WIP-TAB-02):** both `custom_atmstatus=3`; Rule 71 bars editing them without a coupled build-verify pass. Exact rewords staged in `HELD-AUTOMATED.md` (incl. a wrong-story-ref fix on C30462: SV-8656→SV-8659). *Blocks:* their live text still asserts the old status model. Owner: build-verify pass (needs WIP on a build). Since 2026-08-18.
- **RS-WIP-2 — Run-359 union sync for C43979 (GO-AHEAD needed):** `include_all` false, so the new case won't appear. Staged in `STAGED-RUN-359-SYNC.md`. *Reply "sync run 359".* Owner: QA lead. Since 2026-08-18.
- **RS-WIP-3 — ✅ ANSWERED/CONFIRMED by Chris 2026-08-19** (was: nightly snapshot granularity under "sum of lines" — does WIP-API-01 (C30528) record per-line-state / per-bucket rows or per work order?). Chris's History ruling: **one row per job per tab per date** (WO+tab+date key, S11-R1), **max two rows per job** (status tab + Estimates if unapproved lines), **Adjustments only on the status-tab row (S3-R6)**. C30528 correct as written. See the 2026-08-19 pass above + §7. Cleared 2026-08-19.
- **RS-WIP-4 — ✅ RESOLVED by Chris's 2nd message 2026-08-19** (was: WIP per-line aging HOLD). Chris [1]: *"Aging is per job, and that's final … both rows show the same number. That's correct behavior, not a bug … The spec's stray line about unapproved lines ageing from the line's creation date is gone as of page v24."* → **WIP-COL-07 (C30472)** keeps whole-job Days Open (S4-R12) + "both rows same number, correct not a defect"; **do NOT file the shared Days Open value as a defect.** See the 2026-08-19 (2nd message) section at the top + §7. Cleared 2026-08-19.
- **RS-WIP-5 — ✅ RESOLVED by Chris 2026-08-19** (was: spec still states BOTH tab-placement rules — ask Chris to reconcile S2-R4 / Story 3 to line-state). Chris (Wording ruling): the live Confluence page is **already tidied** — Story 3 and S2-R4 now describe the line-state model (new S3-R5, S3-R6); local copies are behind. Pull the live page (see RS-WIP-6). Moved to §7. Cleared 2026-08-19.
- **RS-WIP-6 — ↺ UPDATED 2026-08-19 (2nd message): WIP live page pinned at Confluence v24 (local baseline v22), AND S11-R7 was rewritten.** The queued WIP reconciliation must pull **v24** and additionally **RE-CHECK the S11-R7 snapshot-read behaviour LIVE** — an "as of" date reconstructs the day (S7-R8a); only the trend view is unbuilt. Candidate cases (read-only): WIP-TAB-05 (C30455), WIP-FLT-05 (C30502), WIP-FLT-04 (C30501), WIP-API-01 (C30528)/C30530/C30531/C30533, WIP-SCOPE-05 (C30460). Owner: us (queued pass). Since 2026-08-19.
- **RS-WIP-7 — 🆕 QUOTE AGE column = FUTURE ([SV-9372](https://shopview.atlassian.net/browse/SV-9372), Parth, not started).** Chris [2]: nothing to test now; any WIP case touching a Quote Age column is feature-not-built / deferred (Rule 69); **do NOT file the shared Days Open value as a defect meanwhile.** No such case exists today. Re-check trigger = SV-9372 shipping (Rule 49/61). Owner: engineering. Since 2026-08-19.

## 2026-08-17 — REPORT SUITE: Fabian design-review reconciliation (Adjustments + Estimates tooltip DONE; 7 Loom items STAGED)
Source: `build/report-suite/fabian-review-2026-08-17/`. Specs re-read live (SBC v20 / SBR v22 / PV v10 / TU v9 / WIP v21 / IV v10). Authored **18 new Adjustments cases** (C43814–C43831, WIP+SBC+SBR, byte-verified) + updated **WIP-SUM-07 (C30493)** to the locked Estimates tooltip. Live group 4281 = ours **498** / foreign **12** (0 touched).
- **RS-FB1 — Run-359 sync (GO-AHEAD needed):** the 18 new cases are not in run 359 (`include_all` false). Union-only sync needs explicit per-ask authorisation (Rule 34). *Blocks:* run under-reports coverage. Owner: QA lead. Since 2026-08-17.
- **RS-FB2 — ✅ ANSWERED by Chris 2026-08-18 (Q1=A, *"you did the right thing!"*):** keep the longer S5a-R2 Estimates tooltip (already asserted by C30493), drop the short S5-R12. C30493's resolved-ambiguity note removed + provenance re-stamped as a confirmation (see 2026-08-18 pass below). **Spec hygiene STILL owed from Chris:** delete the short S5-R12 wording from the spec so it states it once. Since 2026-08-17, answered 2026-08-18.
- **RS-FB3 — Fabian Claude design artifact unfetchable** (undated share link, empty shell). *Blocks:* authoring the math-strip, amber-glow and two-row-header-wrap cases without inventing labels/colours (Rule 12). Owner: QA lead — dated export/screenshots. Since 2026-08-17.
- **RS-FB4 — STAGED authoring/updates (budget/auth):** Labor Delta rename (40 cases, 2 delicate flagged: C30151/C30229) · WIP "as of" date (~19 cases) · CSV filter-metadata SV-9283 (6 reports) · VIN "(no unit #)" suppression (SBC) · math strip / amber glow / two-row wrap (need design). Specified in `COVERAGE-REDERIVATION.md`. Since 2026-08-17.
- **RS-FB5 — Tech plan not provided** (Rule 30). Since 2026-08-17.

> **PAUSED 2026-08-10 — the Filters + Schedule source-accuracy pass was stopped mid-build with no TestRail writes; resume from [`build/RESUME-SOURCE-ACCURACY-2026-08-10.md`](RESUME-SOURCE-ACCURACY-2026-08-10.md).**

> **This is the single cross-project list of what is missing.** Governed by **Standing Rule 36**
> ("Always remind the user of everything OUTSTANDING for each project").
>
> **READ THIS** before writing any project status report, management deliverable or progress
> update — every one of those must END with an **"OUTSTANDING — what I need from you"** section
> for that project (and must say *"nothing outstanding"* explicitly if that is true, never omit
> the section).
>
> **UPDATE THIS** the moment an item is raised or cleared — same turn, no exceptions. Items are
> removed **only when genuinely satisfied**; a satisfied item moves to **§7 Recently cleared** with
> the date and how it was satisfied, so nothing quietly disappears and nothing gets re-asked.
>
> **VERIFICATION CONVENTION FOR THIS REGISTER (Standing Rule 50, 2026-08-04 — the QA lead's gloss on
> "byte-level" is *"not to miss anything"*):** every verification claim made in this register — an
> item recorded as **cleared**, a count, a "pushed and verified" row, a "run is in sync" row, a
> "their cases untouched" row — is **EXHAUSTIVE FIRST** (the WHOLE population, every case/field/
> requirement/role/surface — **no sampling**, and a sample is never described in words that imply the
> whole) and **THEN EXACT**, established by **BYTE-LEVEL comparison**, never by eye and never by a
> matching total. A write is re-GET and byte-compared
> against the intended payload (with untouched fields proven byte-identical to their pre-write
> snapshot); a count is proven as **set equality in both directions**; a run sync verifies **each
> prior result present by id**. An item may only move to **§7 Recently cleared** on that basis —
> anything short of it stays OPEN and is labelled **NOT VERIFIED** (Rule 12).
>
> **Why this exists:** the end goal is **100% authentic test cases**. Most of our remaining
> authenticity gaps are not our own work — they are inputs we are waiting on. A missing epic means
> Rule-20 ticket traceability cannot be satisfied at all. An unanswered PO question means a case
> stays hedged instead of asserted. A missing QA branch means **nothing is live-verified** and a
> whole suite sits VIU-Pending.

> # 🟢 THE ONE THING MOST NEEDED FROM THE QA LEAD, RIGHT NOW
>
> ## A fresh **`sv_sso_session`** for **`.qa.shopview.com`**.
>
> **⚠️ CORRECTED 2026-08-11 (later) — THIS ASK IS FAR BIGGER THAN IT WAS FRAMED. THE SUPERSEDED
> WORDING IS KEPT VISIBLE IMMEDIATELY BELOW rather than overwritten, because the old framing
> UNDERSTATES IT BADLY and a silently-corrected figure is how a stale number survives.**
>
> **THE REAL FIGURE: a working sign-in is the single largest lever on 433 CASES ACROSS THREE FINAL
> BRANCHES, with the release on THURSDAY** — **Schedule 174** (build verification in progress; the
> last pass observed **0 of 174**, the session died 14 minutes in) · **Filters 8** (needs the
> **second, non-administrator** sign-in — a different ask, outstanding since 5 August) · **Report
> Suite 251** (Sales By Representative 112 · Parts Velocity 71 · Inventory Value 68 — source-accurate,
> **never build-verified**). **All three branches are now FINAL** (*"The Branches are Final now."*),
> so a deviation on any of these is a **real defect in a finished feature**, not a maybe-unfinished
> one. **331 cases ARE build-verified** (Report Suite 225 · Filters 106); **433 + 331 = 764**, the
> three suites in full.
>
> *(Superseded wording, kept for the record — its "47 of the 73" predates today and describes the
> Report Suite alone. Reproduced below exactly as it stood:)*
>
> **That single value unblocks 47 of the 73 remaining Report Suite cases** — nothing else is in the way
> of those 47 — **and it is also what lets us re-check the on-screen labels and verdicts on all three
> suites against builds that have moved SEVEN TIMES TODAY.** Under Rule 60 the labels and the
> pass/fail verdict are exactly the two layers a redeploy really does invalidate, so this is not a
> nice-to-have.
>
> **Ask for the `sv_sso_session` specifically, not "new cookies".** The refusal is estate-wide: all
> three branches return **HTTP 401 `sso_required`** together on a **byte-identical shared
> `sv_sso_session`**, and the refusal arrives from nginx as **`application/json`** — so the request is
> reaching the app and **`cf_clearance` is fine** (playbook §A trap 1 does **not** apply here).
> **`quick-login` is not a way round it — it is itself SSO-gated and answers 401.**

**Last updated:** 2026-08-12 — **THREE RULINGS: DEFECT TICKETS MUST BE UNCHALLENGEABLE · QUESTION SHEETS GO OUT LAST · THE TECHNICAL-DESIGN AUTHORITY QUESTION IS ANSWERED.** **(1)** Standing Rule 52 gains an **eight-item EVIDENCE BAR** — *"the defects you create can not be challenged and should not bite me, **they did badly bite me and my job is on threat due to that. However for now the Jira ticket creation is still on hold.**"* **The hold is STILL ACTIVE; the bar is for the future.** **(2)** New **Standing Rule 66** — a PO/dev question sheet is **the last thing sent**, once everything we can do ourselves is finished; **rows C2 and C4 stay open with that send condition**, and a genuine blocker is still raised immediately. **(3)** **Row D1 is CLOSED after six days** — the technical design **sources a case alone where nothing contradicts it (RELEASING ELEVEN HELD CASES)**, and **where it contradicts, the specs/tickets/answer sheets/Claude design/Figma win**, with **every contradiction reported to him**. List delivered: [`build/rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md`](rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md) — **6 live contradictions, 6 resolved.** **The SECURITY items are CLOSED by his ruling** (*"Do not change anything if that is not blocking you from testing"*; public repos stay public) — **Rule 6's secrets bar untouched.** **Three new rows: T1, T2, T3.**
*(Prior header, kept for the record:)* **2026-08-11 (later) — ALL THREE BRANCHES ARE FINAL, NOT JUST THE REPORT SUITE.** QA lead, verbatim: *"The Branches are Final now."* — plural, immediately after confirming all six reports were handed off, so it **extends finality to Schedule (`sv8685`) and Filters (`sv8785`)**. **Findings on all three suites are no longer provisional pending development — a deviation is a real defect in a finished feature — and Rule-49 queue rows may close on all three, on the ORDINARY close condition (the bar is not lowered).** **An OPEN queue is no longer "the normal steady state"** — that framing was a consequence of branches never declared final, and the premise is gone. *"Final"* still means **handed off / feature-complete**, **not** *"the code will never change"* — **a redeploy still invalidates the labels and the pass/fail verdict (Rule 60, layers 1–2) on every one of them.** **⚠️ THIS RAISES THE STAKES: 433 cases across the three projects are FINAL BUT NOT BUILD-VERIFIED (Schedule 174 · Filters 8 · Report Suite 251) against 331 that are, and the release is THURSDAY — new row RS-F2, and the headline ask at the top of this file is corrected.**
*(Prior header, kept for the record:)* **2026-08-11 — THE REPORT SUITE BRANCH IS FINAL: ALL 6 REPORTS ARE NOW HANDED OFF.** QA lead, verbatim: *"note that ALL 6 reports have been handed off now."* — satisfying the condition he set on 2026-08-10. **Findings on all 476 Report Suite cases are no longer provisional pending development, and its Rule-49 queue rows may close on the ORDINARY close condition (the bar is not lowered).** *"Final"* means **handed off / feature-complete**, **not** *"the code will never change"* — **a redeploy still invalidates the labels and the pass/fail verdict (Rule 60, layers 1–2).** **⚠️ This RAISES the outstanding work: only 225 of 476 are build-verified; the other 251 (SBR 112 · PV 71 · IV 68) are FINAL-BUT-NOT-BUILD-VERIFIED — new row RS-F1, queued behind Schedule and Filters on his own sequencing.** **Schedule and Filters are unchanged.**
*(Prior header, kept for the record:)* **2026-08-11 — A NEW RULING: EVERY CASE MUST HAVE A SOURCE.** A case whose expected behaviour rests on **no document** should not exist and is a **deletion candidate** — but **the automation marker is checked first**, and **the candidate list comes to the QA lead before anything is deleted** (Rule 6 — `delete_case` is irreversible). Recorded as **Standing Rule 64**, which **escalates Standing Rule 20** from *flag* to *flag → search → delete-if-truly-unsourceable*. **Two new rows, S1 and S2.** **⚠️ The sweep must separate a genuinely unsourceable case from a mere TRACEABILITY GAP (a source exists, it was never recorded — find and record it) and from an OPEN PO QUESTION (held, because the answer may source it).**
*(Prior header, kept for the record:)* **2026-08-11 — SCOPE CORRECTION: THE CREATION HOLD IS *JIRA TICKETS ONLY*.** The QA lead clarified that **creating and updating TEST CASES is our core work and was never in scope** — *"we are supposed to create the test cases"* — so **`add_case` and `update_case` are PERMITTED AND EXPECTED**, and only **Jira ticket creation** stays barred until his next order. **Row H1 amended; new standing duty H1a added** (*anything that stops us creating/updating a test case is escalated to him immediately*). **Schedule §5.3 "Panel collapse" is NOT blocked — it is outstanding authoring work.**
*(Prior header, kept for the record — note its "nothing may be created anywhere" is the wording corrected above:)* **2026-08-10 — RULINGS PASS: three QA-lead rulings recorded. A CREATION HOLD is ACTIVE ("Do not create anything until my next order") — nothing may be created anywhere until he lifts it, though `update_case` on existing cases continues. The branch-finality question is ANSWERED PER REPORT (3 of 6 final). Four items moved to §7 Recently cleared, and one DATED reminder (SV-8954, due 2026-08-12) is now findable.** Read the section immediately below first. *(Prior header, kept for the record: "DOCUMENTATION-UPKEEP PASS: three owed rows added, four rows refreshed, four items cleared.")*
**Superseded header (2026-08-06), kept for the record:** **DOCUMENTATION-UPKEEP PASS: three owed rows added (the technical-design authority question · the Schedule design baseline as a PARTIAL source · the two unreconciled design-ruling points), four rows refreshed against today's live figures, and four items moved to §7 Recently cleared.** Read the section immediately below first. *(Prior header, kept for the record: "CONSOLIDATION PASS: four QA-lead rulings folded in, THREE of them move work off him and onto US; one item cleared; the full six-category sweep re-done.")*
**Active projects:** Report Suite · Schedule · Filters (user ruling 2026-07-27)

> **🆕 2026-08-17 — FILTERS RECONCILED TO THE FABIAN REDESIGN (spec v21). New OUTSTANDING items:**
> **(FAB-1)** engineering owes the **per-view filter list** (spec S1-R8/S13-R23) — until it lands,
> Parts/Reports chip coverage stays behavioural + "confirm live". **(FAB-2)** QA-lead ruling needed on
> the **greyed-vs-hidden Status chip** on Estimates/Completed — v21 S9-R5 says *hidden*; the recorded
> QA-lead ruling of **2026-07-30** said *greyed-out/pre-filled*; verdict **HELD** on
> **[C29609](https://shopview.testrail.io/index.php?/cases/view/29609) /
> [C29610](https://shopview.testrail.io/index.php?/cases/view/29610)** (Rule 33 — not silently
> reversed). **(FAB-3)** Ahtasham's authorization to **sync run 352** — the 9 new cases (C43841–C43849)
> are frozen out (`include_all:False`); union staged in
> `build/filters/fabian-review-2026-08-17/STAGED-RUN-352-SYNC.md`. **(FAB-4)** a **QA-branch sign-in**
> for `sv8785.qa.shopview.com` to build-verify the redesign and lift the 69 Rule-69
> (`Not available on Build to test Yet`) markers to READY. **(FAB-5) ✅ CLEARED 2026-08-17** — the **v19→v21 provenance re-stamp of the 55 unchanged Filters
> cases is DONE**: 55 `update_case`, byte-verified (refs `[spec v21 2026-08-14]` + provenance
> Confluence v21, spec+epic read-dates → 17 Aug, stale build sentence dropped, plain READY → Rule-69
> marker, HOLD/EXPECT-FAIL kept). **All 124 our Filters cases now cite Confluence v21 (0 cite v19).**
> Spec v21 + epic (33 children) re-confirmed CURRENT live. 0 content-stale, 0 label-finalize (all 26
> "confirm live" are the per-view rollout owed by engineering, not design-pinnable labels), 0 live
> contradictions; four counts live/local/id-map/import = 124 set-equal. Pass:
> `build/filters/currency-2026-08-17/FILTERS-CURRENCY-REPORT.md`.
> Full fabian report: `build/filters/fabian-review-2026-08-17/FILTERS-COMPLETION-REPORT.md`.
**Predecessor snapshot (kept for the record):** `build/PROJECTS-NEEDS-2026-07-27.md`
**Companion defensibility register (read before answering any QA challenge in a public channel):** `build/qa-preemptive-answers-2026-07-31/ANTICIPATED-QUESTIONS-AND-ANSWERS.md` / `.xlsx` — 65 rows of *what someone could say* → *the paste-ready answer* → *the evidence*, across the three active projects, with a TOP 10 and an open list of what we would genuinely have to concede.
**Companion retrospective:** `build/LESSONS-2026-07-31.md` — what went wrong on 2026-07-31, what it
cost, and the five standing rules (**40–44**) that now prevent each. Several items in this register
are the *asks* those lessons generated (a spec correction from Chris, one live WIP observation).

> **IF AN ITEM IS BLOCKED ON THE QA LEAD HIMSELF — Standing Rule 48 applies, and it is not
> optional.** Any row that says *awaiting your authorisation*, *frozen by your ruling* or *held by
> your decision* must carry **five extra things**: **(1)** the ruling **quoted verbatim**, **(2)**
> **when** he gave it and **what question it answered**, **(3)** the **named cases** it blocks
> (internal ID + C-id + link, Rule 8), **(4)** **why the ruling was reasonable** — or honestly what
> has **changed since** that makes it worth revisiting, and **(5)** the **one specific thing** that
> would unblock it, and **from whom**. A bare *"awaiting your decision"* is non-compliant.
> **A ruling is a source, and sources get cited** — the same standard we already hold specs, PO
> answers and tickets to. A blocked row with no cited ruling is indistinguishable from us having
> forgotten to do the work.

**Categories used in the tables:** `SOURCE` (a spec, epic, design, tech plan or promised artefact
we do not have) · `QUESTION` (asked, not answered) · `GO-AHEAD` (your authorization needed before
we may write anything) · `ACCESS` (a login, branch, token or connected account) · `DECISION`
(something you or the QA lead held or deferred) · `OTHER TEAM` (a PO, dev or Jira artefact someone
else owes).

---

## 🆕🆕🆕 NEWEST — 2026-08-17, THE SCHEDULE FABIAN-REVIEW PASS **NOW COMPLETE**. **SPEC RE-INGESTED v25→v30; 19 NEW CASES + 28 EXISTING CASES ALIGNED TO v30; 0 LIVE CONTRADICTIONS; BUILD VERIFICATION DEFERRED (RULE 69).**

Full report: `build/schedule/fabian-review-2026-08-17/SCHEDULE-COMPLETION-REPORT.md`. Two passes: the
authoring pass added **19 new cases** C43795–C43813 (Rule-50 byte-verified, `custom_atmstatus=1`); the
**completion pass (this one) updated 28 existing cases to spec v30** — all byte-verified — and
**re-ingested the spec (`requirements.md` → v30 baseline, `SPEC-REINGEST-v25-v30.md`)**. Suite **195,
ours == live == id-map == import, 0 foreign**. Every touched case carries
`AUTOMATION: Not available on Build to test Yet - Last checked 8/17/2026` (Standing Rule 69). **0
build-verified / 0 steps walked** — the app was not opened. **Finding: spec v30 RESOLVES the oldest
Schedule question (shop closures, S1/X1/NQ-1/P9) — and contradicts the 2026-07-29 tech plan, reported
per the 2026-08-12 tech-design ruling.** Still open: the run-357 sync go-ahead (SCH-FR-1), the
build-verify sync (SCH-FR-3), the tech plan + closures ruling (SCH-FR-4).

| Row | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **SCH-FR-1** | **Run 357 sync go-ahead** — the 19 new cases are absent from Ayesha's run 357 (`include_all` false, frozen at 176; union to 195 staged in `STAGED-RUN-357-SYNC.md`). A run write needs your explicit per-ask permission (Rules 6/34) | QA lead | The new cases appearing in the execution run | 2026-08-17 |
| ~~**SCH-FR-2**~~ ✅ **DONE 2026-08-17 (completion pass)** — **28 existing cases updated to v30 and byte-verified** (the ~22 plus SCH-EDGE-05 and the 6 PANEL re-anchors and 2 title fixes). **0 live contradictions across all 195 cases** (`POST-WRITE-AUDIT.md`, `QUALITY-GATE.md` re-run). The 5 prior in-direction contradictions are now resolved IN CONTENT. | — | *cleared* | 2026-08-17 |
| **SCH-FR-3** | **The build-verification sync** — to lift the Rule-69 markers on the 19 new + 28 updated cases to `READY` (or `READY - EXPECT FAIL` where a live ticket backs it — see `KNOWN-FAILURES-FOR-SYNC.md`) | QA lead / build | Any "ready to automate" figure for the Schedule new scope | 2026-08-17 |
| **SCH-FR-4** | **Technical design / tech plan for the Fabian scope** (Rule 30) — none supplied; design source PARTIAL. **AND a tech-design contradiction to rule on: v30 says the spread skips weekends only and closures receive shifts; the 2026-07-29 tech plan built real closure-skipping.** Per your 2026-08-12 ruling the spec wins and it is reported here. | engineering / QA lead | Edge-case strengthening; a ruling on the closures contradiction | 2026-08-17 |
| ~~**SCH-FR-5**~~ ✅ **DONE 2026-08-17 (completion pass)** — **`requirements.md` promoted to the v30 baseline**; exhaustive version-attributed delta in `build/schedule/fabian-review-2026-08-17/SPEC-REINGEST-v25-v30.md`; **0 orphaned anchors**. | — | *cleared* | 2026-08-17 |
| **SCH-FR-6** | *(new)* **C30075 1-line alignment** (view-only case should also name "no 'Assign work order'") + **C30015 open PO question** (modal Reassign vs §7 stacking). Neither is a coverage gap; both out of the alignment scope. | QA lead | nothing critical | 2026-08-17 |
| **🟢 CLEARED BY SPEC v30 (2026-08-17): the shop-closures contradiction (rows S1 / X1 / NQ-1 / P9 below, and the shop-closures half of C4)** | — | v30 §4.5 and §12 now AGREE: spread skips weekends only; shop closures, public holidays and booked days ALL receive shifts. **The PO question is moot** — no answer needed. SCH-EDGE-05 (C30089) and SCH-SPREAD-07 (C29983) are aligned to v30 (their "waiting on PO" HOLD removed). **⚠️ It contradicts the 2026-07-29 tech plan → reported as a tech-design contradiction (SCH-FR-4).** The S1/X1/NQ-1/P9 rows below are kept for the record and marked resolved-by-v30. | 2026-08-17 |

---

## 🆕🆕 NEWEST — 2026-08-12, THE RULINGS PASS. **THREE RULINGS. ONE LOAD-BEARING QUESTION CLOSED AFTER SIX DAYS, ELEVEN CASES RELEASED, THE SECURITY DECISIONS CLOSED — AND A NEW, HIGHER BAR ON EVERY DEFECT TICKET WE WILL EVER FILE.**

**DOCUMENTATION ONLY this pass. 0 TestRail calls · 0 Jira calls · 0 Confluence calls · 0 application
access · 0 test cases changed · 0 tickets created.** Two siblings were writing to TestRail throughout.

### 🔴 RULING 1 — DEFECT TICKETS MUST BE UNCHALLENGEABLE (Standing Rule 52, amended)

**Verbatim:** *"The Engineering manager had raised a concern over creating tickets which does not make
sense, so we have to make sure that the defects or tickets which we create do NOT bite us like it did,
and must have solid references for the expected behavior, and should have the annotated screenshots in
them, but this is for the future but you have to amend your rule to make sure that the defects you
create can not be challenged and should not bite me, **they did badly bite me and my job is on threat
due to that. However for now the Jira ticket creation is still on hold.**"*

**An eight-item EVIDENCE BAR is now written into Rule 52:** the expectation **quoted verbatim from a
named source with its version and date** — **no quotable document, no ticket** · **annotated**
screenshots · **exact named test data** plus what was ruled out · the build marker and environment ·
a duplicate search **with its queries recorded** · the five-part reader shape · a **written pre-filing
self-challenge** · and a check that it is not a **Rule-24 PASS**. **The bar decides FITNESS, never
authorisation.**

**🛑 THE HOLD IS STILL ACTIVE — HE RE-STATED IT IN THE SAME BREATH.** This is a rule **for the future**.
**Nothing is filed now.**

### 🔴 RULING 2 — PO QUESTION SHEETS ARE THE LAST THING SENT (new Standing Rule 66)

**Verbatim:** *"This should be the last thing once you give me the report that everything else has been
done only this part is left and save it as a rule for now and for the future projects too."*

Write them any time; **send only once everything we can do ourselves is finished**, reported to him as
exactly that. **Rows C2 and C4 stay OPEN and gain this send condition.** **It does NOT license sitting
on a blocker** — anything that actually stops work is still raised immediately.

### 🔴 RULING 3 — THE TECHNICAL-DESIGN AUTHORITY QUESTION IS ANSWERED (row D1 CLOSED after six days)

**Verbatim:** *"Technical design is the authority but if that contradicts with specs/tickets/answer
sheet/claude design/figma (because they are also the authority with the rule that the latest entry for
that question wins) I would suggest to consider the specs/tickets/answer sheet/claude design/figma
(with the rule that the latest entry for that question wins) as the authority for the test cases **but
let me know where it contradicts with the tech design**."*

**Both halves matter.** The technical design **sources a case on its own where nothing contradicts it**
— which **RELEASES ELEVEN HELD CASES** — and **where it contradicts, the other five sources win**, with
latest-wins among them. **And every contradiction is REPORTED TO HIM: applying the order silently is
not compliance.** Recorded at Rule 57 follow-up (ii) · Rule 30 · Rule 33 · the deliverable conventions.

**THE LIST HE ASKED FOR IS DELIVERED: [`build/rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md`](rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md)**
— **6 live contradictions · 6 already resolved · the 11 released cases with what each now needs · and
the near-misses that are deliberately not on the list.**

### The rows this pass opens, closes and changes

| Row | Item | Category | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|---|
| **T1** | **The 6 LIVE tech-design contradictions — do you want them raised with engineering?** Our cases are already on the winning side of all six, so **no test-case change is needed**; what is at stake is that **engineering may be building to a description the PRD contradicts.** **The one to raise first is L2 — Schedule split shifts:** the plan's `staff_working_hours` carries a **unique key on `(staff_id, workplace_id, day_of_week)`**, so a second range is **impossible to store**, while spec v25 requires *"'Add hours' appends more to support split shifts, each removable"* with overlap validation. Also **L1** closures · **L3** double-booking not counted in the conflict pill · **L4** business hours on an admin page the PRD does not contain · **L5** the phone Apply button · **L6** the Location filter | **DECISION** | **the QA lead** | **Nothing in QA today** — every affected case already asserts the winning source. What it blocks is a conversation with engineering that only you can start | **2026-08-12** |
| **T2** | **A LIVE re-read of the Location-FILTER requirement on the current PV / SBR / TU / IV specs.** Our mirror of that requirement is **five versions stale** and **agrees with the technical design and AGAINST the walkthrough video** — PV `S2-E4`: *"A user with access to only one location **still sees** the Location filter…"* Chris has since reworded the Location rules across three specs, so it may be superseded; **we did not re-fetch it, so we do not know** | **ACCESS** / SOURCE | us, once a Confluence read is possible | **4 cases turn on which is right** — SBR-LOC-04 = [C30216](https://shopview.testrail.io/index.php?/cases/view/30216) · TU-LOC-05 = [C30446](https://shopview.testrail.io/index.php?/cases/view/30446) · IV-LOC-04 = [C30577](https://shopview.testrail.io/index.php?/cases/view/30577) · PV-FILT-13 = [C30340](https://shopview.testrail.io/index.php?/cases/view/30340). **[SV-8879](https://shopview.atlassian.net/browse/SV-8879) rests on the same point** | **2026-08-12** |
| **T3** | **Go-ahead for three recording fixes surfaced by the ruling — references and provenance only, NO assertion touched.** **C29600** ([link](https://shopview.testrail.io/index.php?/cases/view/29600)) needs its `refs` corrected to cite the tech plan rather than §2/S8-R3 **and has NEITHER a Rule-54 provenance line NOR an `AUTOMATION:` marker — the only Filters case with neither** · **C29632** ([link](https://shopview.testrail.io/index.php?/cases/view/29632)) the same `refs` correction · **C29624** ([link](https://shopview.testrail.io/index.php?/cases/view/29624)) still records the superseded tech-plan real-time reading while its body correctly asserts the spec's Apply-button behaviour | **GO-AHEAD** | **the QA lead** | Three cases carry references that point at documents which do not support them — the false-authority failure Rules 46/54 exist to prevent. **⚠️ C29600 is flagged `custom_atmstatus = 3` Automated, so Rule 65 applies: Vlad must be told** | **2026-08-12** |
| **D1** | **✅ CLOSED 2026-08-12 — see Ruling 3 above.** The technical-design authority question is **ANSWERED**. Moved to §7 Recently cleared with the ruling quoted in full | — | — | — | raised 2026-08-06 · **closed 2026-08-12** |
| **L2** | **✅ CLOSED 2026-08-12 by the QA lead — see the security block below.** | — | — | — | raised 2026-08-11 · **closed 2026-08-12** |
| **H1** | **✅ THE RE-CHECK HALF RAN 2026-08-13 — AND THE POPULATION WAS SIX DEFECTS, NOT FIVE** ("Defect 6", the WIP silent empty export, sat uncounted in the same file, `build/report-suite/full-viu-2026-08-06/DEFECTS-FOR-PERMISSION.md`). **Scorecard: `build/report-suite/defect-recheck-2026-08-13/SCORECARD.md`.** Every quoted requirement was diffed against the CURRENT specs (SBC **v20** · TU **v9** · WIP **v15**, all edited 2026-08-12) and **survives verbatim — nothing was overtaken by a spec change**; SV-8954/8943/8967 re-read live, all still OBSOLETE/Done. **Verdicts: D1 HOLDS** (offer as reopen + broaden of SV-8954) · **D2 + Defect 6 HOLD** (new tickets) · **D3/D4 NOT filable as new tickets** — closed SV-8943/SV-8967 still reproduce, so they are REOPEN ASKS (skill `06` item 8) · **D5 WITHDRAWN as a ticket** (cosmetic, plausibly the document's error → a PO question for Chris). **Owed before any is offered:** written self-challenges (0 existed) · recorded JQL (0 existed; the 2026-08-13 sweep is in the scorecard — 13 neighbours read, none covers these) · C-ids stripped from paste bodies · version re-stamps · **annotated screenshots re-captured AFTER next week's Reports build lands** (every capture is bare; D4's cited `wip-checks.png` does not exist). **The FILING still waits on him — the hold stands, re-confirmed, and nothing was filed.** **⚠️ Superseded wording, kept and dated:** until 2026-08-13 this row read *"the five prepared defects… must now be re-checked… one half is not blocked at all and has simply not been done."* Done now. | **DECISION** (filing + the three reopen calls + the D5 question) · **WORK** (the owed repairs, ours) | **the QA lead** for the lift and the reopen decisions · **US** for the repairs | 6 prepared defects (3 offerable after repairs, 2 reopen asks, 1 withdrawn-to-question); the cases on `AUTOMATION: HOLD` only because an expect-fail marker needs a ticket number — **each is one edit from `READY - EXPECT FAIL` once a ticket exists** (core §15.1a) | 2026-08-10 · re-confirmed 2026-08-12 · **re-check DONE 2026-08-13** |

### 🔒 THE SECURITY DECISIONS ARE CLOSED — his ruling, dated, so nobody re-raises them

**Verbatim:** ***"Do not change anything if that is not blocking you from testing"***.

**And on repository visibility: PUBLIC REPOSITORIES STAY PUBLIC — the branches are temporary and
non-sensitive.** **This is HIS DECISION, recorded on 2026-08-12.**

**What that closes, item by item:**

- **The JWTs in git history (row L2) — CLOSED. Nothing is rotated, nothing is rewritten.** The tracked
  tree is clean at HEAD across **11,696 files**; the historic copies stay where they are. **All twelve
  tokens are already expired and none of them ever granted more than "listen to one user's notification
  feed".** Rewriting history would have rewritten **~491 commits** and force-pushed a branch several
  live sessions share — which the standing rules forbid anyway.
- **"Should this repository be public at all?" — CLOSED.** He has answered: **it stays public.**
- **No repository setting was, or will be, changed by a worker.**

**⚠️ WHAT THIS RULING DOES *NOT* RELAX, said explicitly so it cannot be stretched: STANDING RULE 6's
SECRETS BAR IS UNTOUCHED.** **Secrets still live in `/tmp` only and are NEVER committed**, and
`build/testing-tools/scan_secrets.py` still runs over every staged diff. **His ruling is about not
disturbing what is already published and settled; it is not permission to publish anything new.** **A
public repository makes that bar MORE important, not less** — anything pushed to one must be assumed
already cloned, forked and mirrored.

---

## 🆕 2026-08-12 (earlier), THE LOSS AUDIT. **NO QA WORK WAS LOST IN THE LIMIT KILL. ONE PIECE OF EVIDENCE WAS, AND IT IS NAMED. TWO ITEMS ARE OPEN, ONE OF THEM FOUND BY THE AUDIT ITSELF.**

**The QA lead asked whether the limit kill cost us anything.** It was re-proved independently from
live TestRail, live git and each pass's own committed artefacts — **not** by re-reading the recovery
note. **Verdict: every intended operation landed or was deliberately dropped for a documented better
reason; all three runs intact with 0 graded fields changed; nothing in the "never landed and never
redone" category.** Full paper: `build/loss-audit-2026-08-11/VERDICT.md`.

**What was lost, honestly:** the follow-up push's **contemporaneous byte-comparison output**, which
lived in `/tmp`. Its two writes landed and are provably correct, but the original proof is gone and
its execution log is an **after-the-fact reconstruction that says so on its first line.** No ask
attached — recorded because it is the reason Standing Rule 29 now requires verification evidence to
be committed to the repository.

| Row | Item | Category | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|---|
| **L1** | **Re-sync the Schedule local case source FROM LIVE and regenerate its deliverables — 6 cases are stale.** The labels-final pass pushed 12 build-accurate label corrections to TestRail and did not re-sync local. **[C29930](https://shopview.testrail.io/index.php?/cases/view/29930) · [C30015](https://shopview.testrail.io/index.php?/cases/view/30015) · [C30025](https://shopview.testrail.io/index.php?/cases/view/30025) · [C30042](https://shopview.testrail.io/index.php?/cases/view/30042) · [C30046](https://shopview.testrail.io/index.php?/cases/view/30046) · [C30051](https://shopview.testrail.io/index.php?/cases/view/30051)** still carry the OLD wording locally (`Saturday` for `'Show Saturday'`, `'VIN'` for `'VIN Number'`, `working hours` for `business hours`), and `testrail-import/schedule-v1-testrail-import.csv` was last written **before** the push | GO-AHEAD | QA lead | **Nothing today — live TestRail is already correct, so no TestRail write is needed.** The risk is forward-looking and it is the same one that nearly bit Filters: anyone regenerating Schedule deliverables from local, or driving a future pass off it, would **revert live to wording the build does not use**. Filters (115) and Report Suite (480) are clean | 2026-08-12 |
| **L2** | **✅ CLOSED 2026-08-12 BY THE QA LEAD — verbatim: *"Do not change anything if that is not blocking you from testing"*, and public repositories STAY PUBLIC because the branches are temporary and non-sensitive. Nothing is rotated, nothing is rewritten, no repository setting is changed. All twelve tokens are already expired and none ever granted more than "listen to one user's notification feed". ⚠️ This does NOT relax Standing Rule 6's secrets bar — secrets stay in `/tmp`, never committed, and the scanner still runs on every staged diff.** *(The original row, preserved:)* **Decide what to do about the JWTs still in git history.** The redaction cleaned HEAD — the tracked tree is clean across **11,696 files** — but `git show 5b1b573f:…/diag-roles.json` **still returns a live-shaped token**. Options are laid out in `build/secret-redaction-2026-08-11/REPORT.md` | DECISION | QA lead | **Nothing in QA.** This is the only item in the audit with a blast radius outside the team. Rotating the signing secret makes the historic copies inert; rewriting history would rewrite ~491 commits and force-push a branch several live sessions share, which the standing rules forbid | 2026-08-11 |

**Honesty note — what the audit could NOT determine.** Whether the killed Filters pass issued any
TestRail call before dying (no oplog; that **no write landed** is proven, attempts are not
reconstructable) · which phase last wrote C38866 (TestRail exposes only the latest write) · **whether
any killed pass had `/tmp`-only output nobody has noticed is missing** — unknowable by construction,
and the strongest argument for the new write-the-log-first requirement · how many historic secret
exposures exist across all ~491 commits (at least one confirmed; not swept exhaustively).

---

## 🆕🆕🆕🆕 2026-08-12 (NEWEST OF ALL), THE FILTERS RUNNABILITY FINISH — SECOND PASS. **A CASE THE TESTER MARKED FAILED ACTUALLY PASSES AS WRITTEN, AND THE GAP BEHIND IT IS UNCOVERED. ALSO: WE CORRECTED ONE OF OUR OWN READINGS BEFORE IT BECAME A FALSE FINDING.**

Deliverables: **`build/filters/finish2-2026-08-12/`** — read `COMPLETION-REPORT.md` (the Rule-67
table) first, then `FINDINGS.md` §1, then `RUNNABILITY.md` and `DIVERGENCES.md`.

**Build `v3.6-3e9dd6d`, unmoved. 115 ours / 120 live. 10 `update_case`, every one byte-verified,
30 fields each, 0 mismatches, 0 collateral. 0 add · 0 delete · 0 run writes · 0 results · 0 Jira.
Markers unchanged: 90 READY + 7 EXPECT-FAIL + 18 HOLD, gate closing both ways at 97.**

**THE HONEST HEADLINE: 12 cases had EVERY step walked this pass — 22 across all Filters passes, of
115.** Three more were part-walked and say which steps were not driven; **two produced nothing
because the check itself failed, and are reported as failed checks, not findings.**

**THE PRIORITY HAD MOVED, AND IT IS WORTH KNOWING WHY:** re-derived live, **zero** never-examined
`READY` cases were still Untested — the tester had already Passed 74 and Failed 7. So the value lay
in **the cases he FAILED**: five were plain `READY` and each carried a **fresh Jira ticket**, and if
any of those cases were wrong we would have caused a bogus ticket the day before release. **Four of
the five are squarely evidenced. The fifth is row F-C29603 below.**

**WE ALSO CORRECTED OURSELVES INSIDE THE PASS.** An early probe applied a filter **by URL**, saw it
not restored, and read that as corroborating the persistence defect behind SV-8828. Driven the way a
tester drives it — **clicking the chip** — the filter **is** saved and **is** restored. The build
distinguishes the two; the first reading was our own artefact and is corrected in `DIVERGENCES.md`
§2 rather than left in an evidence file to be quoted later.

| id | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **F-C29603** | **A ruling on [C29603](https://shopview.testrail.io/index.php?/cases/view/29603) and SV-8905.** The case **PASSES as written** — driven end to end today: collapsed survives a return, expanded survives a return, and the saved preference carries `"collapsed": false`. **The tester failed it and filed SV-8905 with the comment *"Collapsed state is remembered on Work Orders page, but not on Parts/Reports page"* — ground this case never covers, because its steps only ever exercise Work Orders.** Two things follow, pointing opposite ways: the Failed result is **not supported by this case**, and the behaviour he found is **probably real and covered by NO Filters case at all**. | **the QA lead** — either the result moves, the ticket's evidence is re-pointed, or a new case is authored (authoring is **not** barred by the creation hold; only Jira is) | Tomorrow's run carries a Failed result whose case passes, and a genuine coverage gap on Parts/Reports collapse persistence stays uncovered. | **2026-08-12** |
| **F-TICKET-1** *(unchanged, restated)* | **A defect ticket for [C38897](https://shopview.testrail.io/index.php?/cases/view/38897)** — still the only unticketed real deviation on this project. Not re-driven this pass; the previous pass established it with a proper rule-out. | **the QA lead** — the creation hold, re-stated 2026-08-12: *"However for now the Jira ticket creation is still on hold."* | It will read as an unexplained failure in tomorrow's run. | **2026-08-12** |
| **F-CTRL-1** *(HALF CLOSED)* | **✅ C38877 step 3 is RESOLVED** — the cause was never the product: the filter options are **`DIV[data-test-id^="filter_option_"]`**, not `label` and not `.q-item`, so two passes' readers matched nothing and **could not fail**. Found by dumping the menu DOM. Ticking Imported disables all four other chips; picking a second status deselects Imported. **❌ C43560 and C38876 STILL produce nothing** — neither status pick registered, `updatedAt` never moved, and the guard reported `check_could_fail: false`. | another pass | Nothing is known to be wrong in the product; **the point is that we do not know**, and a vacuous check must never be reported as a pass. | **2026-08-12** |
| **F-WALK-1** *(advanced)* | **22 of 115 walked, not 10.** 93 remain — **but most have been executed successfully by the tester himself**, which is direct evidence of runnability. Still owed: C29614 steps 3–6, C29625 steps 1/2/4, C43563 steps 6–7, and **C29581 / C29588, which need a staff record deactivated — barred for us, ordinary work for a tester.** | another pass; **C29581/C29588 are tester work** | The runnability claim is **22 of 115**, stated plainly so nobody reads the walk as complete. | **2026-08-12** |
| **F-PLAYBOOK-1** | **A playbook note is owed** — the `filter_option_` selector, plus four traps this pass paid for: a URL-applied filter is not persisted (a chip-applied one is); `.q-dialog` is the full-screen wrapper not the sheet card; collapsed accordions stay mounted so a card-wide option sweep reads the wrong section; and the customer list marks selection with a **check glyph**, not a Quasar checkbox. | **`build/APP-ACTIONS-PLAYBOOK.md` §J** — not edited from this worker | Every one of these has already cost a pass real time, twice in two cases. | **2026-08-12** |

## 🆕🆕🆕 2026-08-12 (NEWEST), THE FILTERS RUNNABILITY FINISH. **10 OF THE 29 PRIORITY CASES DRIVEN END TO END, ONE PRECONDITION CORRECTED THAT WOULD HAVE COST A TEST TOMORROW, AND ONE REAL BUILD DEVIATION THAT NEEDS A TICKET THE MOMENT THE HOLD LIFTS.**

Deliverables: **`build/filters/finish-2026-08-12/`** — read `COMPLETION-REPORT.md` (the Rule-67 table)
first, then `RUNNABILITY.md` and `DIVERGENCES.md`.

**Build `v3.6-3e9dd6d`, unmoved. 115 ours / 120 live. 1 `update_case`, byte-verified. 0 add · 0 delete ·
0 run writes · 0 results · 0 Jira. Markers 90 READY + 7 EXPECT-FAIL + 18 HOLD; the gate closes both
ways at 97.**

**THE HONEST HEADLINE:** **10 of 29** priority cases had **every step** verified against the shipping
build; **13 more** had their navigation path and every named control verified but not every step
driven; **6** could not be walked. **The 13 are not folded into the 10** — an unverified step is an
unverified case. Against the whole suite that is **10 of 115**, and **92 cases still name the 5 August
build** because **no re-stamp campaign was run**: asserting a check nobody made is worth less than an
honest stale stamp.

**WHAT WAS FIXED:** [C43590](https://shopview.testrail.io/index.php?/cases/view/43590) sent the tester
to **Parts → Part Sales** as the example of a page with exactly one filter button. That page now shows
**no filter bar at all**, while **Reports → Technician Efficiency** shows exactly one and no collapse
control. Corrected as cosmetic, and the escape hatch widened to cover zero-filter pages, which the old
wording did not anticipate. **Without it a runnable case would have been marked BLOCKED tomorrow.**

**ALSO WORTH KNOWING: run 352 was re-scoped and fully assigned to user 7 under our shared account
between 09:09 and 11:07 UTC** — 115 → 120 tests, 473 → 635 results, of which 154 are assignment
records. Somebody is preparing the run for the tester. Nothing of ours; recorded so a later reader
does not attribute it to this pass.

| id | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **F-TICKET-1** | **A defect ticket for [C38897](https://shopview.testrail.io/index.php?/cases/view/38897).** Spec v19 `S8-R4`/`S8-R5` require the no-results state to offer a way to clear the filters **and, when a search is active, a separate way to clear the search**. The build shows **`No work orders match your filters`** — filters only, even when only a search is active — and offers **`Clear Filters`** alone. Proven as an absence properly: the scanner was first run in a state where the search-clear control **is** present and saw it (`evidence/empty-state.json`). **The case was deliberately NOT edited** — under Rule 57 it keeps its documented expectation and the tester will fail it, which is correct; adding a hold would disarm a case that is working. **No expect-fail marker was added either, because Rule 61 as amended requires live backing and no ticket exists.** | **the QA lead** — the creation hold, re-stated 2026-08-12: *"However for now the Jira ticket creation is still on hold."* | Nothing today: the tester will fail it and be right to. But it is **the only unticketed real deviation this pass found**, and it will read as an unexplained failure in tomorrow's run. | **2026-08-12** |
| **F-WALK-1** | **6 of the 29 priority cases still unwalked, and 86 of the 115 never examined at all.** The 6 are C29619, C38876, C38879, C43560 (ordinary work) plus **C29581 and C29588, whose preconditions need a staff record deactivated — barred on this branch, because such an edit destroys the session of every holder.** | another pass; **C29581/C29588 are ordinary work for a tester** | Coverage of the runnability check is **10 of 115**, not the suite. Stated plainly so nobody reads the walk as complete. | **2026-08-12** |
| **F-CTRL-1** | **Two checks produced nothing because their own control failed** — C38877 step 3 and C43560. The Status-menu reader returned **0 options on a clean page** where it had earlier returned 10, so neither result can be believed in either direction. Recorded rather than reported, and the fragility is written into `RESUME.md`. | another pass | Nothing is wrong in the product as far as anyone knows; **the point is that we do not know**, and a check that cannot fail must never be reported as a finding. | **2026-08-12** |

## 🆕🆕 2026-08-12 (NEWEST), THE FILTERS BUILD-VIU PASS. **✅ F-BLOCK-1 IS CLEARED — THE QA LEAD SENT WORKING `sv8785` COOKIES AND THE 8-DAY BLOCKER IS GONE. C29615 AND C38895 BOTH PASS.**

**✅ CLEARED SAME DAY — F-BLOCK-1.** He supplied two fresh `sv8785` `PHPSESSID` values; **both
returned HTTP 200**. The two identities were **proven different three independent ways** before any
observation was recorded (42 permissions / `view_mode: full` against **6** / `tech`; the staff
directory showing role **Technician**; and different per-user preference stores). **C29615 and
C38895 were driven live as the Technician and BOTH PASS**, closing the item that had been open since
5 August. **C43590 and C43561 were also verified live.** `quick-login` and `switch-user` were never
called, so both sibling workers kept their session. **The row is left below, struck through in
substance rather than deleted**, because the diagnosis in it is what produced the narrow, correct
ask — *the two `PHPSESSID` values by name* — rather than a scattergun request for "new cookies".

**⚠️ C1 IS NOT CLEARED BY THIS.** A second non-administrator sign-in now EXISTS for **Filters**, but
C1 is cross-project: the Schedule and Report Suite permission cases still need one on **their**
branches, and `PHPSESSID` is per-branch.

**Still open from this pass: F-STAMP-1 and F-PASS-1 below** — though **F-PASS-1 has softened**: we can
now say the Passed verdict recorded on C29615 was the **correct** one, since the case genuinely
passes. What remains unknowable is whether the step was driven at the time.

### ~~The original row, kept for the diagnosis it carries~~

| id | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **F-BLOCK-1** | **A live `PHPSESSID` for branch `sv8785` — one for the administrator, and one for `bilal.muzamil+filters@shopview.com` (Technician).** Both values supplied on 2026-08-12 return **HTTP 409 `Session has expired.`** on `sv8785api`, and on the Schedule and Report Suite API hosts too. **This is NOT the estate-wide `sv_sso_session` failure of row A1/A2:** the shared `sv_sso_session` and `cf_clearance` supplied are **PROVEN ALIVE** — a sibling worker's byte-identical shared pair returns **HTTP 200 with 42 permissions** on its own branch. **`PHPSESSID` is per-branch, proven both ways.** So **ask for the two `sv8785` `PHPSESSID` values by name** — not for "new cookies", not for a `cf_clearance`. Evidence, every probe with its HTTP code: `build/filters/build-viu-2026-08-12/evidence/session-probe-2026-08-12.txt` | **the QA lead** | **Everything that needs the build.** **0 of 115 Filters cases were observed on 2026-08-12.** The **8** cases blocked since 5 August stay blocked; the other **107** keep verdicts from an earlier build. **⚠️ AND IT BLOCKS ROW C1's OWN REMEDY:** his ruling *"You should unblock yourself"* is right and the method is real (`POST /api/iam/create`, or `switch-user`) — **but both require a working admin session to execute**, so "unblock yourself" cannot start from here. **`quick-login` was deliberately NOT called** (playbook §A trap 5: it rotates the shared token and would sign out the two siblings live on Schedule and Reports, and possibly his own browser). | **2026-08-12** |
| **F-STAMP-1** | **Authorisation to re-stamp ~89 Filters cases whose build line understates when they were really checked.** **95 of 115 say `Last checked against build v3.4.2-d00239b on 8/5/2026`** while the branch runs **`v3.6-3e9dd6d`** — but yesterday's pass checked **106 of 114 against that exact running build** and wrote to only the 8 it had to correct. So the stamps are wrong **in the cases' favour**. | **the QA lead** (a go-ahead) | Nothing operationally — but it makes the suite look a week staler than it is, and it is the one number a reviewer would seize on. **This session deliberately did NOT do it:** the brief said re-stamp only what you observed, and this session observed nothing. | **2026-08-12** |
| **F-PASS-1** | **A ruling on five HELD cases that already carry PASSED results**, all graded by user 7 (Ahtasham Amjad), all with empty comments: C29559, C29609, C29610, C29612 and — the one that matters — **[C29615](https://shopview.testrail.io/index.php?/cases/view/29615)**, whose whole assertion is that **one user's saved filters do not reach another user**, marked Passed on 6 Aug 09:49Z while its own marker still reads `HOLD - needs a second test login`. **Either the second login existed a week ago, or the per-user step was never driven.** | **the QA lead** (it is another author's result on our case — reported, never touched, Rule 38) | Confidence in the per-user isolation coverage the suite currently appears to have. **It also predicts tomorrow:** the `AUTOMATION: HOLD` marker is not stopping testers running held cases, and **15 held cases are still Untested** in run 352. | **2026-08-12** |

**What did NOT need you:** the suite was audited whole and is **structurally clean** — 0 raw markup,
0 marker defects, 0 provenance defects, 0 dead links, 0 over-long titles, gate passes both ways
(88 + 7 = 95 = 115 − 20). **5 build-independent corrections were pushed and byte-verified**, the
most useful being **[C43561](https://shopview.testrail.io/index.php?/cases/view/43561)**, which is
`READY` and **Untested** and was sending testers to a *"Sales Tax"* report with a *"Collected"* tab
that `S13-R19` does not describe. **And the workload for the morning is 28 runnable cases, not 43** —
`build/filters/build-viu-2026-08-12/FINDINGS.md` §1 names both lists.

---

## 🆕 2026-08-11 (latest), THE FILTERS RE-SYNC + SV-9041 PASS. **THE LOCAL FILTERS SOURCE WAS STALE ON ALL 114 CASES AND IS NOW FIXED. THE EPIC GAINED A CHILD CARRYING A PO RULING — AND FOR ONCE IT CONFIRMS OUR WORDING RATHER THAN BREAKING IT.**

**What cleared, without needing you:** the Filters local case source was stale on **114 of 114**
bodies (spec v18 provenance + missing read-dates + seven build-accurate label corrections).
Regenerating any Filters deliverable would have reverted two completed live passes. **Re-synced from
live, verified 0 of 114 differing, four counts set-equal both ways.** No ask attached — recorded so
nobody re-opens it.

**SV-9041 is applied.** Two cases repaired and byte-verified — **C29601** and **C43562**. Scope was
re-derived exhaustively across all 114 cases: **two**, not the eight in the brief nor the three the
killed pass planned.

| Row | Item | Category | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|---|
| **F12** | **Send the Branko sheet with the new SV-9041 addendum** (`build/filters/questions-2026-08-06/ADDENDUM-2026-08-11_SV-9041-collapse-toggle.md`) — does the "toggle only when more than one filter" rule belong in the PRD, and does it cover **Parts and Reports** as well as Work Orders? | ANSWER | QA lead → Branko | **Nothing is blocked.** Both cases already follow the ticket, which is ~3 months newer than the spec requirement it qualifies. The ask settles whether the rule is **written down** and **how far it reaches** — the ticket says only "the page", while its own QA evidence is a Parts page | 2026-08-11 |
| ~~**F13**~~ | ~~A real coverage gap left UNFILLED: no case drives SV-9041's negative limb~~ **✅ CLEARED THE SAME DAY — [C43590](https://shopview.testrail.io/index.php?/cases/view/43590) is created and byte-verified.** The row was raised on a **misreading of your creation hold**, corrected by you within the hour | — **closed** | — | **nothing** | raised and cleared 2026-08-11 |
| **F15** | **Authorise the run 352 sync.** Creating C43590 left the run one case short — `include_all` is **false**, so it holds a frozen 114-case selection. **`update_run` was NOT called**; the full **115-id union** is staged in `build/filters/sv9041-2026-08-11/STAGED-RUN-352-SYNC.md` | GO-AHEAD | QA lead | **C43590 has no test in Ahtasham's run**, so it cannot be executed there. The run holds **473 graded results** and `update_run` REPLACES the selection, so this is not ours to run unasked | 2026-08-11 |
| **F14** | **SV-8906** — *"Clarification Required: empty-state inconsistency across…"*, Task, **Board Backlog**, unanswered since 5 August. New to our written records | OTHER TEAM | Branko | Unknown until read — not analysed in this pass | 2026-08-05 |

### 🔴 A CORRECTION WE OWE — WE READ YOUR CREATION HOLD TOO WIDELY, AND IT COST A CASE MOST OF A DAY

**Row F13 above should never have existed.** This pass declined to author the SV-9041 case, recording
it as blocked by your creation hold. **That was wrong.** Your ruling is verbatim:

> ***"Keep up the Jira ticket creation hold, but do not hold creating the test cases in Testrail."***

**The hold covers Jira tickets ONLY.** Creating TestRail cases is not merely permitted, it is the
substance of what you asked for — *"We are supposed to create test cases and accurate ones… And
anything that stops you from creating/updating a test case You MUST let me know."*

**Recorded, not quietly deleted**, because a wrong reading that is silently fixed is a wrong reading
that comes back. The case was authored on your correction the same day: **C43590**, section 4118,
`custom_atmstatus = 1`, `AUTOMATION: READY`, byte-verified.

**The generalisable lesson: when a hold's wording is broader than the question it answered, ASK —
do not resolve the ambiguity by stopping.** Stopping felt like the cautious reading; it was simply a
different way of getting it wrong, and it withheld coverage on a **final** branch with the release on
**Thursday**.

### 🔴 The epic moved, and one new child carries a Branko ruling — **it CONFIRMS our wording**

**Epic SV-8785 now has 21 children, not 20** (verified two independent ways, sets equal, no paging
remainder). **[SV-9076](https://shopview.atlassian.net/browse/SV-9076)**, closed **Done** on
2026-08-10, carries Branko verbatim:

> *"Let's not change header, it's not part of this feature and **create work order label can stay**"*

**That supersedes S13-R18**, which calls the button *"New Work Order"*. **Our suite needs no change:**
all three cases naming it — C29601, [C29629](https://shopview.testrail.io/index.php?/cases/view/29629),
[C38898](https://shopview.testrail.io/index.php?/cases/view/38898) — already say *"Create Work
Order"*, and **zero** say *"New Work Order"*. **It is deliberately NOT on the question sheet** — he has
already answered it, and re-asking is what this register exists to prevent.

### One thing this pass could NOT establish (Rule 12)

**The designs were not re-fetched**, so if a design shows the toggle on a single-filter page, that
PRD-vs-design mismatch would not have been seen. Since 2026-08-06 the design and Figma are
**authoritative sources** (Rule 57 as amended), which is why **row F11's "confirm the canonical design
artefact" ask now matters to Filters too**, not only Schedule.

---

## 🆕 NEWEST — 2026-08-11 (later), THE SECOND FINALITY RULING. **ALL THREE BRANCHES ARE FINAL. AND WITH THE RELEASE ON THURSDAY, 433 CASES ARE FINAL BUT NOT BUILD-VERIFIED.**

**DOCUMENTATION ONLY this pass. No TestRail call, no Jira call, no app access, no test-case change.**

QA lead, verbatim: ***"The Branches are Final now."***

**The word is PLURAL, and it came immediately after he confirmed all six reports were handed off** — which had already made the Report Suite branch final on its own (the section below). **So this ruling EXTENDS finality to SCHEDULE (`sv8685`) and FILTERS (`sv8785`) as well.**

**WHAT IT CHANGES, on all three:** findings are **no longer provisional pending development** — a deviation is a **real defect in a finished feature** — and **Rule-49 queue rows may close on every one of them**, on the **ordinary close condition** (the row re-verified with fresh evidence; **the bar is not lowered**, only the *"wait for the build to settle"* blocker is removed). **And the "OPEN queue is the normal steady state" framing is RETIRED** — it was a consequence of branches that were never declared final, and that premise is gone. Recorded in **Standing Rule 49** (the later 2026-08-11 amendment), with **Standing Rule 60's headline now marked FULLY SUPERSEDED** — kept verbatim and dated, because **its strategy is untouched**: the layer split and practices (a)–(f) still govern redeploys.

**⚠️ WHAT IT DOES *NOT* CHANGE, and it will be misread otherwise: *"final"* means HANDED OFF / FEATURE-COMPLETE, NOT *"the code will never change."*** All three can still redeploy — not least to fix the very defects we are reporting — so **a redeploy still invalidates the on-screen labels and the pass/fail verdict (Rule 60, layers 1–2) on every one of them.** The Schedule branch redeployed to `v3.5-65d6500` on the morning of 2026-08-11, which is exactly why this matters.

### The row — four fields (Standing Rule 36)

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **RS-F2** | **BUILD VERIFICATION OF 433 CASES THAT ARE NOW FINAL, ACROSS ALL THREE PROJECTS — and the release is THURSDAY.** **SCHEDULE 174** — build verification **in progress right now**; the last pass observed **0 of 174**, the session having died 14 minutes in (`build/schedule/build-verify-2026-08-11/BUILD-VERIFICATION.md`). **FILTERS 8** — blocked on the **second, non-administrator sign-in**, outstanding **since 5 August**; the other 106 were checked against the running build (`build/filters/build-verify-2026-08-11/RESUME.md`). **REPORT SUITE 251** — SBR 112 · PV 71 · IV 68, **source-accurate and never build-verified** (`build/report-suite/source-accuracy-remaining-2026-08-11/RESUME.md`); this is row **RS-F1** widened, not a second copy of it. **331 cases ARE build-verified** (Report Suite 225 · Filters 106), and **433 + 331 = 764 = Schedule 174 + Filters 114 + Report Suite 476.** **⚠️ Like RS-F1, this is a CONSEQUENCE OF THE GOOD NEWS: finality did not create the shortfall, it made the shortfall COUNT.** | **us** (the verification) · **the QA lead** for a fresh **`sv_sso_session`** for `.qa.shopview.com`, **and separately the second non-administrator sign-in** for the Filters 8 — without which nothing can be observed at all | **Any claim that any of the three suites is build-verified.** 433 of 764 cases carry verdicts and automation markers **never checked against any build** — and they are now final, so their findings count for real: an `AUTOMATION: READY - EXPECT FAIL` on one of them asserts a build fact nobody has seen (Rule 12), and a genuine defect on one of them is currently **unreported, four days before release**. **NOT idle: the QA lead sequenced the work Schedule → Filters → Report Suite** (Rule 48 — his call, and the right one). | **2026-08-11** |

### Honesty note — including an arithmetic correction to this ruling's own first framing

**Nothing was verified live this pass** — no application, no TestRail, no Jira. The ruling is recorded from the QA lead's own words; every component figure was re-derived from the committed evidence named in the row.

**⚠️ THE TOTALS WERE FIRST STATED AS "425 final but not build-verified / 339 build-verified" AND BOTH WERE WRONG — they DOUBLE-COUNT THE 8 FILTERS CASES**, which appear as unverified in the first figure and as verified in the second (**433 − 8 = 425; 331 + 8 = 339**). **The per-project components — Schedule 174, Filters 8 of 114, Report Suite 251 of 476 — were RIGHT; only the sums were not.** Recorded rather than quietly fixed, because a figure that fails its own gate is a finding (Rule 50), and the corrected totals now gate both ways against the three suites in full.

**Whether any of the 433 actually passes on the build is UNKNOWN, and that is exactly what this row is for.**

---

## 2026-08-11 (earlier), THE FIRST FINALITY RULING. **THE REPORT SUITE BRANCH IS FINAL — ALL SIX REPORTS, NOT THREE. AND IT RAISES THE OUTSTANDING WORK RATHER THAN LOWERING IT.** *(Superseded in scope by the section above — all three branches are now final; kept verbatim and dated.)*

**DOCUMENTATION ONLY this pass. No TestRail call, no Jira call, no app access, no test-case change.**

QA lead, verbatim: ***"note that ALL 6 reports have been handed off now."*** That **satisfies the condition he set on 2026-08-10** in his own words — *"Once all 6 reports are handded of to the QA only then we can consider the branch as final."*

**WHAT IT CHANGES:** findings on **all 476** Report Suite cases are **no longer provisional pending development** — a deviation is a **real defect in a finished feature**, on any of the six — and **Rule-49 queue rows for the whole suite may close** as each case is re-checked, on the **ordinary close condition** (**the bar is not lowered**; only the *"wait for the build to settle"* blocker is removed). Recorded in **Standing Rule 49** (2026-08-11 amendment) with a dated cross-reference at **Rule 60**. The **three-and-three split is kept visible and dated**, so the record shows *when* each half became final.

**⚠️ WHAT IT DOES *NOT* CHANGE, and it will be misread otherwise: *"final"* means HANDED OFF / FEATURE-COMPLETE, NOT *"the code will never change."*** The branch can still redeploy — not least to fix the very defects we are reporting — so **a redeploy still invalidates the on-screen labels and the pass/fail verdict (Rule 60, layers 1–2) on every one of the six.** What finality removes is a different doubt: whether a gap is an **unfinished feature** or a **defect**.

**Schedule (`sv8685`) and Filters (`sv8785`) are UNCHANGED — neither declared final, verdicts still PROVISIONAL.**

### The row — four fields (Standing Rule 36)

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **RS-F1** | **BUILD VERIFICATION OF 251 REPORT SUITE CASES THAT ARE NOW FINAL — SALES BY REPRESENTATIVE 112 · PARTS VELOCITY 71 · INVENTORY VALUE 68.** These three have had **source accuracy done and NO BUILD VERIFICATION AT ALL** (scope evidence: `build/report-suite/source-accuracy-remaining-2026-08-11/RESUME.md`). **Only the three previously-final reports are build-verified — 225 of 476.** **⚠️ This is a CONSEQUENCE OF THE GOOD NEWS, not a new failure: finality did not create the shortfall, it made the shortfall COUNT** — until 2026-08-11 a gap on these three could honestly be an unfinished feature, and now it is a defect. **It therefore RAISES the outstanding work rather than lowering it.** | **us** (the verification) · **the QA lead** for a fresh **`sv_sso_session`** for `.qa.shopview.com`, without which no case can be observed at all | **Any claim that the Report Suite is build-verified.** 251 of 476 cases carry verdicts and markers that have **never been checked against any build**, and they are now final, so their findings count for real — an `AUTOMATION: READY - EXPECT FAIL` on one of them asserts a build fact nobody has seen (Rule 12), and a genuine defect on one of them is currently **unreported**. **NOT forgotten and NOT idle: the QA lead has sequenced the work Schedule → Filters → Report Suite, so these 251 are QUEUED BEHIND the other two by his own ordering** (Rule 48 — it is his call and it is the right one; the two ahead have open coverage work that would otherwise stall). | **2026-08-11** |

### Honesty note

**Nothing was verified live this pass** — no application, no TestRail, no Jira. The ruling is recorded from the QA lead's own words; the counts are taken from committed evidence (`source-accuracy-remaining-2026-08-11/RESUME.md` = 251, SBR 112 · PV 71 · IV 68) and the 225 is the arithmetic remainder of the 476 census. **Whether any of the 251 actually passes on the build is UNKNOWN and is exactly what row RS-F1 is for.**

---

## 🆕 2026-08-11, THE AUTOMATED-MARKER RULING. **A NEW STANDING DUTY: WHEN WE CHANGE A CASE TESTRAIL FLAGS AS AUTOMATED, VLAD HAS TO BE TOLD — AND WE OWE HIM A WEEK OF BACKLOG, WHICH IS NOW WRITTEN.**

**The ruling, verbatim (2026-08-11):** *"I was referring to testrail OWN AUTOMATED marker, because when we change any test case which has the testrail OWN automated marker we have to update Vlad who does the automation so that he can adjust accordingly his automation with our updates/delete of test cases."*

**This did two things.** It **settled row S2** (the marker is TestRail's own `custom_atmstatus`, not our `AUTOMATION:` text marker — Rule 64 updated), and it **created a duty broader than deletion**: *"our **updates**/delete of test cases"*. Recorded as **Standing Rule 65** — every pass that writes to cases now ends with an **"Automated cases changed — for Vlad"** section, saying *"none"* where none.

**READ-ONLY pass. 0 TestRail writes · 0 Jira calls · no build opened.** The backlog list is written and ready to forward: **`build/automated-cases-changed-2026-08-11/FOR-VLAD.md`** (the list) and **`METHOD.md`** (how it was derived and what would make it incomplete).

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **V1** | **FORWARD THE BACKLOG LIST TO VLAD.** We changed **73 of the 75** Automated cases since 6 August without tracking it, because the duty did not exist until today. **27 of those changes affect what an automated check should conclude; 46 do not** — and **only 8 of the 27 sit on cases Vlad himself marked Automated.** The sharpest two are **[C30510](https://shopview.testrail.io/index.php?/cases/view/30510)** and **[C30515](https://shopview.testrail.io/index.php?/cases/view/30515)**, which went from *expected to fail* to *expected to pass* when SV-8907 was fixed — **an un-updated suite would report a false alarm on a working build.** The list is banded so the 8 are at the top. | **the QA lead** — he is the channel to Vlad | **Nothing of ours.** What it risks is **Vlad's time**: until he sees it, his automation may assert things four Report Suite cases no longer expect. | **2026-08-11** |
| **V3** ⏰ | **⏰ DUE THE WEEK BEGINNING 2026-08-17 — REMIND HIM. REMOVE TESTRAIL'S OWN "AUTOMATED" FLAG (`custom_atmstatus = 3`) FROM THE ~790 CASES IN THE PROJECTS OUTSIDE THE THREE ACTIVE ONES** — **Custom Roles ≈354 · Test Cases ≈312 · Fees & Discounts ≈118 · ShopCoach ≈6.** **(1) THE RULING, VERBATIM (2026-08-11):** *"We will have to remove them for sure but next week, and you have to remind me about that next week."* **(2) WHEN AND IN WHAT CONTEXT:** given 2026-08-11, after a sweep found the flags while clearing them from the three active projects; **deferred deliberately because the release is the next day.** **(3) WHAT IT BLOCKS: nothing today** — these are completed or inactive projects; the cost is that **Vlad's automation view is polluted by cases nobody automated.** **(4) WHY THE DEFERRAL IS RIGHT:** the release is tomorrow and **these projects are not in it.** **(5) WHAT IT NEEDS TO PROCEED:** the QA lead's **go-ahead**, then a **per-case `get_history_for_case` check.** **🔴 THE QUALIFIER THAT MATTERS MOST, BECAUSE GETTING IT WRONG IS THE REAL RISK: a 20-case SAMPLE suggested roughly 19 in 20 of these flags were set by Vladimir Tomovic himself, so only ~40 of the 790 are likely ours. HIS FLAGS MUST NEVER BE CLEARED** — his instruction was explicit: *"if Vlad has added that 'Automated' marker, then dont touch that."* **⚠️ THAT FIGURE IS A SAMPLE, NOT A CENSUS.** The pass must establish ownership **PER CASE from `get_history_for_case`, never in bulk**, and **anything ambiguous is reported rather than written.** **The tooling can no longer create the problem:** the canonical builder defaults to Not Automated and refuses the Automated value on both spellings, and the repo guard passes at **zero across 947 files** | **the QA lead** — the go-ahead, and the reminder is ours to give | **Nothing today.** It is queued behind the release by his own ruling (Rule 48) | **2026-08-11**, **due w/c 2026-08-17** |
| **V2** | **A QUESTION FOR VLAD, AND IT AFFECTS WHETHER THIS DUTY IS EVEN MEASURABLE: are the 31 SCHEDULE cases really automated?** TestRail's history shows **nobody ever set** `custom_atmstatus` on any of them — **our own `add_case` tooling hardcodes `3` at creation** (verified in `build/schedule/panel-collapse-2026-08-11/tools/push.py`, `exec_sync_epic_2026-07-27.py`, `exec_sync_2026-07-22.py`; every Schedule case above id 30090 is `3`, every imported one is `1`). By contrast **all 44 Report Suite and Filters flags were set by hand by Vlad**. **So on Schedule the flag asserts nothing**, and 19 of the 27 "changes that matter" are probably noise. **Correcting the 31 flags is a TestRail write and is NOT authorised** (Rule 6) — it is put to him, not done. | **the QA lead + Vlad** | **Nothing is blocked.** The cost is that **this duty's headline number is inflated on Schedule**, so the list has to carry the caveat every time until the flags are corrected or confirmed. | **2026-08-11** |

### Report Suite — spec-delta round, raised 2026-08-11

**Source:** `build/report-suite/spec-delta-2026-08-11/`. Sheet ready to send:
`build/report-suite/questions-2026-08-11/Questions-for-Chris-Ward_Report-Suite_2026-08-11.xlsx`.

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **RS-Q1** | **WHICH TAB-PLACEMENT RULE GOVERNS THE WORK IN PROGRESS REPORT.** Its specification now says two incompatible things **in the same live document**: the v11 §3 Key Decision (added 2026-08-10, SV-9027) says *"Buckets are keyed on line state, not work-order status … a work order carrying lines in more than one state **appears in each matching tab**"*, while the unchanged `S2-R4` says *"exactly once, in **exactly one tab**"* and `S3-R1`–`S3-R4` place a work order by **its status**. **No side was picked** (Rules 15/57/58) — the cases keep the requirement they cite, word for word. | **Chris Ward** (question 1 on the 2026-08-11 sheet) | **3 cases on `AUTOMATION: HOLD`** — [C30458](https://shopview.testrail.io/index.php?/cases/view/30458), [C30462](https://shopview.testrail.io/index.php?/cases/view/30462) (**Automated**), [C30464](https://shopview.testrail.io/index.php?/cases/view/30464). **And it blocks AUTHORING for two surfaces with no coverage at all**: the per-tab money slice, and line-level vs work-order-level ageing. **[C30528](https://shopview.testrail.io/index.php?/cases/view/30528)** (the nightly snapshot's *"one row per then-open job per calendar day"*) may also be wrong under the new reading — flagged, not edited, and **recorded as HIGH risk** because the snapshot feeds trend history and a wrong shape there is invisible on screen. | **2026-08-11** |
| **RS-Q2** | **WHETHER PARTS VELOCITY'S LOCATION COLUMN BEHAVES LIKE THE OTHER FIVE REPORTS.** v6 rewrote `S3-R10` to the access-gated, picker-toggleable rule but **left `S2-R12` scope-driven** (*"when a single location is in scope the column is hidden"*) and left Location out of the **twenty** picker columns `S4-R1`/`S4-R2`/`S4-R3` enumerate. **The other five reports are settled.** | **Chris Ward** (question 2) | **Nothing is held.** [C38914](https://shopview.testrail.io/index.php?/cases/view/38914) and [C30352](https://shopview.testrail.io/index.php?/cases/view/30352) (**Automated**) assert only the uncontested half and tell the tester not to fail on the contested points — so the cost is a hedge on one report that the other five do not carry. | **2026-08-11** |
| **RS-Q3** | **CONFIRM THE EXPORT ROW LIMIT.** All six specifications now state a 10,000-row cap with the verbatim message, **and all six reports already have a case for it** — but **Inventory Value v5 carries his own bracketed note**: *"[Cap value 10,000 is a proposed default — confirm the exact suite-standard value with the owner before dev.]"* | **Chris Ward** (question 3) | **Nothing is held.** If the number changes, **six cases need one edit each** (C30172, C30290, C38885, C38887, C38918, C30593). | **2026-08-11** |
| **RS-Q4** | **AUTHORISATION TO SYNC RUN 359.** `include_all` is **false**, so the run is frozen at **476** tests and does **not** contain the 4 cases created on 2026-08-11. The full **480-id union** is staged and left unexecuted. | **the QA lead** — the run belongs to Nebojsa / Viktoria (Rules 6/34/47) | **A reviewer reading run 359 will not see the new coverage** — the false-coverage-gap problem Rule 47 exists to prevent. `update_run` **replaces** the selection, so a partial list would delete the omitted tests **and their 535 results**; the union is in `STAGED-RUN-359-SYNC.md`. | **2026-08-11** |
| **RS-Q5** | **A MARKER AND A SPEC VERSION FOR [C30288](https://shopview.testrail.io/index.php?/cases/view/30288).** A live census of all 476 found **474 marked, not 476** — the recorded *"476/476"* is stale. C30169 was being touched anyway and was repaired; **C30288 was outside this pass's charter** and is recorded rather than quietly fixed (Rule 41). **[C38925](https://shopview.testrail.io/index.php?/cases/view/38925)** likewise names no spec version. | **the QA lead** — authorising a small marker pass | **The arithmetic gate.** 337 READY + 100 EXPECT-FAIL = **437**, while 480 − 42 HOLD = **438**. **It is out by exactly 1, and that 1 is C30288** — so the gate is not broken, the quoted figure was. | **2026-08-11** |

### Cleared on 2026-08-11 — a question Chris had already answered

**The 2026-08-06 sheet's question 1** — *is the Location column access-gated and toggleable?* — **was
ANSWERED by Chris's own specification edits of 7 August**, whose version message reads *"reworded the
Location-column visibility to the access-gated, column-selector-toggleable rule, matching the decision
note already in this spec."* **Four `AUTOMATION: HOLD` markers raised for it are lifted**:
[C38917](https://shopview.testrail.io/index.php?/cases/view/38917),
[C30551](https://shopview.testrail.io/index.php?/cases/view/30551),
[C30554](https://shopview.testrail.io/index.php?/cases/view/30554),
[C30588](https://shopview.testrail.io/index.php?/cases/view/30588). **Seven cases were still telling
testers the question was open; they no longer do.** Recorded here rather than deleted, because
re-asking a question a source has already answered is an embarrassment this workspace has had once.

---

### Honesty note on V1 — the duty was discovered a week late, and the list is a reconstruction

**Nobody asked us to track this before today, so the backlog is not a process failure — but the list IS a reconstruction, and reconstructions have edges.** They are all written out in `METHOD.md` §6; the two that matter most: it covers **only changes made under user id 3**, and **the QA lead works under that same account**, so any edit he made by hand appears in the list as ours. And the window starts **6 August** — **[C38877](https://shopview.testrail.io/index.php?/cases/view/38877) was already flagged Automated before that date**, so a change we made to it on 5 August would be missed.

---

## 2026-08-11, THE UNSOURCED-CASE RULING. **A CASE WITH NO SOURCE SHOULD NOT EXIST — SO A SWEEP IS NOW RUNNING ACROSS ALL THREE PROJECTS, AND ITS CANDIDATE LIST COMES TO THE QA LEAD BEFORE ANYTHING IS DELETED.**

**The ruling, verbatim (2026-08-11):** *"And there should not be a case for which we do not have a source. A case should only exists IF there is a source for that. Otherwise the case should be deleted, but before deleting the case check if that case has 'Automated' marker"*. Recorded as **Standing Rule 64**, which also **escalates Standing Rule 20** (its remedy for an unsourced case was *flag*; it is now *flag → search → and, only where the case genuinely cannot be sourced, delete*).

**DOCUMENTATION ONLY in this row's own pass — no TestRail call, no Jira call, nothing deleted.**

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **S1** | **THE UNSOURCED-CASE SWEEP'S CANDIDATE LIST — a GO-AHEAD, and it has not been asked for yet because the sweep is still running.** The sweep is under way across **Report Suite, Schedule and Filters**. **Nothing may be deleted until the candidate list is presented and he approves it** — `delete_case` is **irreversible**, and **Rule 6 means nothing is written to TestRail without his explicit permission**; this ruling is **not** a standing licence to delete. Each candidate will carry its **internal ID + C-id + link** (Rule 8), **what it asserts**, **which documents were searched and at which versions**, **its automation status**, and our **recommendation**. **⚠️ THE SWEEP MUST SEPARATE THREE STATES, NOT TWO** — **(a)** genuinely unsourceable (a candidate) · **(b)** a **traceability gap**, where a source exists and was simply never recorded (**the remedy is to FIND AND RECORD it** per `build/MISSING-TRACEABILITY-PROCESS.md` — **deleting one of these throws away real coverage**) · **(c)** the source question is **open with the PO**, e.g. **[C29600](https://shopview.testrail.io/index.php?/cases/view/29600)**, which asserts how two filters combine when no requirement says (**HELD pending the answer, because the answer may source it**). | **the QA lead** — the deletions are his to authorise | **No case is deleted, and none should be, until he sees the list.** What is blocked meanwhile is only the *closing* of the sweep — the flagging, searching and recording all continue, and **(b)-class cases are repaired rather than waiting on him at all.** | **2026-08-11** |
| ~~**S2**~~ | ✅ **CLEARED 2026-08-11 — HE ANSWERED IT.** The ask was: which "Automated" marker does the pre-deletion check mean, TestRail's own `custom_atmstatus` field or our `AUTOMATION:` text marker? **His answer, verbatim: *"I was referring to testrail OWN AUTOMATED marker, because when we change any test case which has the testrail OWN automated marker we have to update Vlad who does the automation so that he can adjust accordingly his automation with our updates/delete of test cases."*** So it is **`custom_atmstatus` (3 = Automated)**, and the both-readings safe interpretation is retired — **Rule 64 updated, superseded wording kept and dated.** **The answer also created a NEW STANDING DUTY that is broader than deletion — see Standing Rule 65 and row V1 below.** | — | — | cleared **2026-08-11** |

### Honesty note on S2 — the safe reading was taken, AND the ask goes with it

**This is deliberately the same shape that went wrong on 2026-08-10 and was corrected on 2026-08-11** (row **H1**, where the creation hold was read too broadly and the broader branch was **acted on instead of being put to him**, leaving a real Schedule coverage gap unauthored for a day). **The lesson written down then is applied here: the safe reading is only safe when the ask goes with it** (Rule 63). So the cautious reading is in force **and** S2 is on his desk — neither silently.

---

## 2026-08-11, THE DATED-PROVENANCE PASS. **A NEW RULING CREATES A SWEEP WE OWE ACROSS EVERY PROJECT, AND IT IS NOT DONE.**

**Standing Rule 54 was amended today: every source a case cites must also carry THE DATE WE READ IT.** His purpose, verbatim: *"…so that in future if someone changes the source of truth I can guard myself telling that the refrence taken from the source of truth was from the state of that source which was at this certain date."*

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **D1** | **THE READ-DATE SWEEP — every case stamped before 2026-08-11 names its sources WITHOUT a read-date.** The amended Rule 54 requires **one date per cited source** in the provenance line's sentence 1. **Only 2 cases carry it today — C30452 and C30434**, both written by this pass; **every other case across Report Suite, Schedule and Filters does not.** **The sweep is NOT started.** ⚠️ **It cannot be done by stamping today's date on everything** — Rule 54 as amended bars exactly that: the date must be the date that source was **actually re-read**, so the sweep has to **re-read the sources per project**, which makes it a real pass and not a text substitution. | **the QA lead** — it needs his go-ahead as a `update_case` pass over ~750 cases | **No suite may be described as compliant with the amended Rule 54 until it runs.** Until then his stated protection — being able to show a reference was taken from a source *as it stood on a given date* — **exists on 2 cases and nowhere else.** **⚠️ RE-CONFIRMED STILL NOT STARTED, 2026-08-13** (the Skill-set adversarial audit) — **this is the single largest piece of owed work in the register**, and the skills now carry the amendment in three places (core §14.1, `01` step 5, `02` step 8) **while no suite complies with it**, which is a gap the skills cannot close by themselves. | **2026-08-11** · **re-confirmed 2026-08-13** |
| **D2** | **A PO QUESTION FOR CHRIS WARD — does the Technician Utilization download menu have to list its four options in the SAME ORDER as Sales By Customer?** The TU specification (v7, read 2026-08-11) requires each of the four options to **exist** with an exact label (S7-R2, S7-R3, S7-R4, S7-R4a) and **pins no order at all** — it pins column order (S2-R1) but never menu order. The build lists them Summary (PDF), Summary (CSV), Expanded View (PDF), Expanded View (CSV); Sales By Customer runs Summary (PDF), Expanded View (PDF), Summary (CSV), Expanded View (CSV). **No source says which is right, so C30434 asserts neither** (Rule 57 — where no source speaks, the gap is a question, not an invented requirement). | **Chris Ward** | Nothing is blocked — **C30434 passes on the four labels**. What is blocked is any claim that the menu **order** is verified either way. | **2026-08-11** |

---

## 🛑 2026-08-10, THE RULINGS PASS. **THREE QA-LEAD RULINGS. ONE OF THEM STOPS ALL CREATION UNTIL HE SAYS OTHERWISE, AND ONE GIVES US THE FIRST FINALITY ANSWER THIS PROJECT HAS EVER HAD — a PARTIAL one.**

**DOCUMENTATION ONLY this pass. No TestRail call, no Jira call, no app access, no test-case change, and — per the first ruling below — nothing created anywhere.**

### The rows — four fields each (Standing Rule 36)

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **H1** | **THE LIFT OF THE CREATION HOLD — 🔴 SCOPE CORRECTED 2026-08-11: THE HOLD IS *JIRA TICKETS ONLY*.** The QA lead ruled, verbatim, answering a request for permission to file five prepared defects: ***"Do not create anything until my next order."*** **This is a TEMPORARY HOLD layered on Standing Rule 62, not a new standing rule** — Rule 62 says *ask first*; **this ruling says the answer is NO for now, so there is nothing to ask about until he lifts it.** **He CLARIFIED the scope on 2026-08-11 — verbatim: *"We are supposed to crfeate test cases and accurate ones … And anything that stops you from creating/updating a test case You MUST let me know, we are supposed to create the test cases."*** So: **`add_case` is PERMITTED AND EXPECTED, and so is `update_case` — TEST-CASE AUTHORING IS NOT BLOCKED AND NEVER WAS.** **Only JIRA TICKET CREATION remains barred** until his next order. **⚠️ Superseded wording, kept visible and dated (Rules 31/52/53 pattern):** the row previously read *"Scope encoded on the safe reading … no Jira ticket · **no new TestRail case (`add_case`)** · no new artefact in any external system of record."* **The `add_case` bar was OUR over-broad reading of his words, not his instruction — he clarified, we had read it too broadly; this is a correction to how we RECORDED his ruling, not a reversal by him.** | **the QA lead** — the hold is his to lift | **The five prepared Report Suite defects stay prepared and UNFILED — that is now the SOLE cost of the hold.** Also: every case sitting on `AUTOMATION: HOLD` **solely because an expect-fail marker needs a ticket number that does not yet exist** stays on `HOLD` — each becomes `READY - EXPECT FAIL` with one edit once a ticket exists. **NOT blocked, contrary to what this row said until 2026-08-11: authoring or correcting any test case** — including Schedule §5.3 "Panel collapse", which is **outstanding authoring work, not held**. **Nothing is lost** — findings are written up in full and wait (Rule 62: we do the whole job and stop at the button). | **2026-08-10** *(scope corrected **2026-08-11**)* |
| **H1a** | **⇒ A STANDING DUTY THIS CORRECTION CREATED, in his own words: *"anything that stops you from creating/updating a test case You MUST let me know."*** **A blocker on authoring or correcting a test case is ESCALATED TO HIM IMMEDIATELY** — an access blocker, a missing source, a hold someone believes is in force, an unanswered authorisation, a tooling failure. **It is NOT parked in a gaps list, a coverage matrix or a findings file to be discovered later.** Log it here **and raise it with him in the same breath**; where the blocker is a rule or ruling we believe is in force, **surface it BEFORE the work, not in the closing summary** (Rule 63). | **us** — this duty is ours, not his | Nothing today. **This row is a STANDING DUTY, not an ask** — it stays as a permanent reminder. **The evidence for why it exists is H1 itself:** a Schedule coverage gap sat unauthored for a day because a hold was read too widely and the blockage was **recorded rather than raised**. | **2026-08-11** |
| **H2** | **⏰ A DATED REMINDER — [SV-8954](https://shopview.atlassian.net/browse/SV-8954), DUE 2026-08-12.** His instruction, verbatim: ***"Need not to reopen anything, but you can remind me in next 2 days."*** **The ticket STILL REPRODUCES and is UNDER-SCOPED: the fault is on ALL THREE handed-off reports, not the one report it names.** He has explicitly **not** asked for a reopen — the ask is only that we **remind him on 2026-08-12**. | **us** (to raise it) → then **the QA lead** (to decide) | Until he decides, the ticket **understates the defect's reach** — anyone reading it will fix one report and believe the issue closed, leaving the same fault live on the other two. | **2026-08-10** |

### Honesty note on H2 — there is NO scheduler

**Nothing fires this reminder automatically.** The mechanism is **this register plus the session-start check**, exactly as with the **Rule-35** design-fetch queues and the **Rule-49** re-check queues — a **committed, dated, findable item**, and nothing more. **A session working on or after 2026-08-12 that reads this register is the trigger.** Saying otherwise would imply a capability we do not have (Rule 12).

### The third ruling — recorded in `CLAUDE.md`, no register row needed

He also restated the currency rule and **widened the source list explicitly** — *"Specs/Stories/Answer sheets/New design/new .md files/new claude designs and anything which is provided to you and is latest"*. That is **folded into Standing Rule 57 as the 2026-08-10 amendment** (sources now **(a)–(g)**, and the list is **open-ended by his instruction** — a new document type does not need a rule amendment before it counts). **It raises no new ask**, so it gets no row here; the duty it creates is ours: **a new document goes through the Rule-31 currency check and a Rule-43 per-requirement reconciliation, not a skim.**

### Honesty notes

- **Nothing was verified live this pass** — no application, no TestRail, no Jira. The rulings are recorded from the QA lead's own words; the supporting figures are taken from committed evidence and cited to it.
- **The creation hold is recorded WITH ITS LIFT CONDITION and marked as temporary**, so a session weeks from now does not mistake it for standing law. **Where a worker cannot tell which side of the creation line an action sits, it stops and asks** rather than guessing.
- **⚠️ AND THAT IS EXACTLY WHAT WENT WRONG HERE, RECORDED RATHER THAN TIDIED AWAY (2026-08-11):** the ambiguity WAS flagged, and the **broader branch was taken and acted on** instead of being put to him. **The safe reading is only safe when the ask goes with it.** He corrected it the next day — *"WHY? We are supposed to crfeate test cases"* — and the cost in the meantime was a real Schedule coverage gap left unauthored. **The lesson is Rule 63's: surface the conflict BEFORE the work, not in the write-up afterwards.**
- **The finality answer is PARTIAL and is written down as partial** — see §7 for what it cleared, and Standing Rules 49 and 60 for the full text. *"Final"* means **handed off / feature-complete**, **not** *"the code will never change"*: **a redeploy still invalidates the labels and the pass/fail verdict even on a final report.**

---

## ⚠️⚠️⚠️⚠️⚠️⚠️⚠️ 2026-08-06, THE DOCUMENTATION-UPKEEP PASS. **THREE ROWS THAT WERE OWED AND HAD NEVER BEEN WRITTEN — and one of them decides whether a technical design can move an expectation the PRD states differently.**

**DOCUMENTATION ONLY this pass. No TestRail call, no Jira call, no app access, no test-case change.**

These three were described in Standing Rule 57's text and in the Schedule project notes but **had no
register row**, because the workers who found them were scoped out of adding one. An item that exists only
inside a rule's rationale is **not tracked** — which is the whole failure mode Rule 36 exists to prevent.

### The three rows — four fields each (Standing Rule 36)

| # | What is missing | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|
| **D1** | **✅ ANSWERED AND CLOSED 2026-08-12.** QA lead, verbatim: *"Technical design is the authority but if that contradicts with specs/tickets/answer sheet/claude design/figma (because they are also the authority with the rule that the latest entry for that question wins) I would suggest to consider the specs/tickets/answer sheet/claude design/figma (with the rule that the latest entry for that question wins) as the authority for the test cases but let me know where it contradicts with the tech design."* **So: the technical design SOURCES a case on its own where nothing contradicts it — which RELEASES THE ELEVEN HELD CASES — and where it CONTRADICTS, the other five win, latest-wins among them; and EVERY contradiction is REPORTED TO HIM.** Recorded at Rule 57 follow-up (ii), Rule 30, Rule 33 and the deliverable conventions, with the superseded "open question" wording kept visible and dated. **The list he asked for is delivered: [`build/rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md`](rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md)** — 6 live contradictions, 6 resolved, the 11 released cases with what each now needs. **Successor rows: T1** (raise the 6 with engineering?), **T2** (the Location-filter live re-read), **T3** (three recording fixes). *(The original row, preserved:)* **A ruling on whether a TECHNICAL DESIGN carries the same authority as the PRD.** **Standing Rule 57's amendment names the technical design as an authoritative design artefact**, while **Standing Rule 30** says an engineering document *"informs but never overrules product truth"* from the spec/PO. **Those two cannot both be applied literally to the same artefact.** **Rule 30's clause is currently preserved as OUR READING, pending his confirmation — he has NOT ruled on it.** **The question, verbatim as it must be put to him:** *"does a technical design carry the same authority as the PRD on what the product should do, or does 'informs but never overrules' still hold for it?"* | **the QA lead** | **This is LOAD-BEARING, not housekeeping: it decides whether a technical design can MOVE AN EXPECTATION THE PRD STATES DIFFERENTLY.** Until it is answered, any case where the two disagree has no defensible resolution order — and under Rule 57 an expectation with no settled source is exactly what we may not assert. It also governs the **6 Schedule items on Branko's sheet that only the engineering plan describes**. | 2026-08-06 |
| **D2** | **Confirmation of WHICH Schedule design artefact is canonical — our baseline is a PARTIAL source and must be recorded as one (Rule 31).** Our baseline is **`build/schedule/design-2026-07-27/`** (the Claude prototype Branko ruled authoritative at **Q0**), and **~48 Schedule labels were pinned from it**. **Sasha Grosman's three tickets — SV-8915, SV-8916, SV-8917 — all close with a different source:** `Design: https://claude.ai/design/p/d3cdcf5c-83df-45ea-ba75-7ddedb5124b5?file=Schedule.dc.html&via=share`, each *"Raised in the Schedule design review with Fabian on 5 Aug 2026."* **That is a share URL to a live, editable page carrying NO version and NO date.** **⚠️ AN UNDATED ARTEFACT CANNOT BE DATED FOR RECENCY PURPOSES, so Rule 32's latest-wins CANNOT BE APPLIED TO IT AT ALL — it goes to the escalation limb, not the tiebreak.** Re-ingestion is authorised **only conditionally** — his ruling, verbatim: ***"Yes if Sasha's design is final."*** **That precondition is NOT established.** | **Sasha Grosman / Fabian** (is it final?) → then **us** (fetch + diff) | **Designs are now authoritative (Rule 57 as amended), so if his link is newer our Schedule design baseline is STALE and ~48 pinned labels rest on a superseded artefact.** Concretely today: we **cannot verify any of his three design-sourced claims against a design we hold**, and **SV-8916 could not be verified at all** — its button is in *his* design and in **no requirement of the specification**. *(Honest scope of that check: it was made against **v23**, the baseline at the time; the baseline has since moved to **v25**, and the check has NOT been re-run against v25 — so treat it as established for v23 only.)* | 2026-08-06 |
| **D3** | **Two points in the design rulings that were FLAGGED and NEVER SETTLED.** **(a) Does *"does not make sense"* have any THRESHOLD?** He authorised the judgement but **named no bar**, so the test for rejecting a design-sourced expectation is currently ours to invent — and an unstated bar is indistinguishable from no bar. **(b) Does a per-project *"single source of truth"* designation by the PO OUTRANK RECENCY when both artefacts ARE dated?** Rule 32 says the newest authoritative source wins; a PO naming one artefact canonical would override that, and nobody has said whether it does. | **the QA lead** | **Both bite the moment two dated design artefacts disagree** — (b) decides which one wins, and (a) decides whether we may reject either on judgement. Neither can be settled by reading a document, so neither may be resolved by us (Rule 58: an ambiguous source is never resolved by looking at the build, and this one cannot be resolved by looking at anything). | 2026-08-06 |

### Honesty notes

- **Nothing was verified live this pass** — no application, no TestRail, no Jira. Every figure was taken
  from the projects' own committed evidence (`full-viu-2026-08-06/{RESUME,RECHECK-QUEUE}.md`,
  `REMAINING.txt`, `build/schedule/requirements.md`, the two question-sheet folders) and is cited to it.
- **D1 is recorded as an OPEN QUESTION, not as a resolved precedence order.** Our reading (Rule 30's
  clause still holds) is stated as a reading. Writing it down as settled would be exactly the
  false-authority failure Rules 46 and 54 exist to prevent.
- **The four refreshed rows below keep their superseded figures visible and dated** rather than being
  overwritten, the pattern Rules 31/52/53 already use.

---

## ⚠️⚠️⚠️⚠️⚠️⚠️ 2026-08-06, THE ZEROS-ROW PASS. **ONE GO-AHEAD NEEDED, AND IT IS SMALL: the SCREEN half of a filed defect still cannot fail.**

**One `update_case` (C30173) and one Jira create (SV-8991) this pass. No `add_case`, no `delete_case`,
no section op, no run write, no result logged. Run 359 read only.**

| Ref | What is needed | Category | Who owes it | **What it BLOCKS** | Since |
|---|---|---|---|---|---|
| **Z1** | **Authorisation for ONE `update_case` on [C30114](https://shopview.testrail.io/index.php?/cases/view/30114).** It is the **screen** half of the defect now filed as **[SV-8991](https://shopview.atlassian.net/browse/SV-8991)**, and it still carries a tester note claiming *"the written description does not say what the totals row should do when nothing matches"* — while its **own `refs` cite `S18-N1`**, which says *"the report shows the empty state (Story 17) and the totals row shows zeros"*. Needed in one write: restore the zeros assertion, replace the false note with the Rule-61 symptom and its three outcomes naming SV-8991, set `AUTOMATION: READY - EXPECT FAIL (SV-8991)`, and pin `refs` from the stale `v13` to `v15`. | **GO-AHEAD** | **the QA lead** | **The screen half of a filed defect cannot fail.** C30173 (the download half) was repaired under this pass's authorisation; C30114 was outside it. A case that cannot fail is not a test (Rule 57), so SV-8991 currently has a ticket and only half a test guarding it. | 2026-08-06 |
| **Z2** | **A live re-check of one contradiction in our OWN evidence** — `full-viu-2026-08-06/evidence/2026-08-06-session2/sbc7.json` holds an empty table body beside a **fully populated, non-zero** totals row, which cannot be the same state as `sbc9.json`'s `afterClear: {"totals": null}`. Likeliest reading: two different empty states (empty **date range** vs empty **customer selection**), which would make a **stale non-zero totals row a SECOND, separate defect**. | **ACCESS** (needs the app) | us, once a sign-in exists | Nothing yet — it is **asserted nowhere** (Rule 12). It is a candidate defect we cannot confirm or dismiss: the harness script that produced the capture is not in the repository and the branch is unreachable. | 2026-08-06 |

**Two things this pass CLEARED rather than raised:**

- **The zeros-row question for Chris Ward is CLEARED and was never his to answer.** It was kept off the
  2026-08-06 sheet as Q2 because its premise was false, and the live document proves it: SBC **v15**
  states the requirement **three times** (`S18-R10`, `S18-N1`, and the Story 16 placeholder note). Filed as
  **SV-8991** instead — Rule 7 forbids putting a bug in front of a product owner.
- **`NO-SOURCE-DEFECTS.md` item 2 is CORRECTED** — it claimed no requirement covered this. Struck through
  and dated rather than overwritten. Working: `build/report-suite/zeros-row-2026-08-06/SOURCE-VERIFICATION.md`.

---

## ⚠️⚠️⚠️⚠️⚠️ NEWEST — 2026-08-06, THE CONSOLIDATION PASS. **FOUR QA-LEAD RULINGS ARRIVED. THREE OF THEM TAKE THE ITEM OFF HIS DESK AND PUT IT ON OURS — and all four are QUEUED BEHIND Report Suite's remaining 276 cases and Vlad's Filters gap table, on his own instruction.**

**DOCUMENTATION ONLY this pass. No TestRail write, no Jira write, no app access, no case edit.**

**His closing instruction on all four, verbatim:** *"BUt do that too once you are done with everything
else in the last."* So **none of the work below has been started**, and none of it may be started ahead
of **RS1** (Report Suite's 276) and **F9** (Vlad's eleven rows).

### The four rulings, and exactly what each one changes

| # | The item he was answering | **His ruling, verbatim** | What it changes |
|---|---|---|---|
| **1** | A second test login without administrator rights | **_"You should unblock yourself."_** | **NO LONGER blocked on him. It becomes an OPEN WORK ITEM OWNED BY US** — see **C1** below. |
| **2** | [SV-8821](https://shopview.atlassian.net/browse/SV-8821) — keep it with the spec-is-silent note, or withdraw it? | **_"Marked it as AObsolete - ignore it for now."_** | **DECIDED AND DONE — he transitioned it himself.** Moved to **§7 Recently cleared**. |
| **3** | Chris Ward's unanswered items (the Location ruling · four unfinished spec contradictions · the export cap missing from three specs) | **_"If there are more questions for Chris make sheet for him."_** | **AUTHORISED work on us: produce a Rule-55 follow-up sheet** — see **C2**. Not a blocked row any more. |
| **4** | Whether to re-ingest the designs, given Sasha Grosman's live share link | **_"Yes if Sasha's design is final."_** | **CONDITIONAL authorisation.** The precondition — *is his design final?* — **has NOT been established by us**, so it stays OPEN with the precondition named. See **C3**. |

### The open items after those rulings — four fields each (Standing Rule 36)

| # | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **C1** | **A second test login WITHOUT administrator rights — and it is now OURS TO CREATE, not his to supply.** His ruling, verbatim: *"You should unblock yourself."* **The method is Standing Rules 5 and 14:** create a fresh non-administrator staff member ourselves (`POST /api/iam/create {email, firstName, lastName, roleId, departments, workplaceId}`) **or** impersonate an existing non-administrator holder (`POST /api/switch-user {user_id}`, with `GET /api/staff?limit=200` giving `role_label` per staff). **⚠️ THE CAUTION THAT MUST TRAVEL WITH IT: `switch-user` and `quick-login` BOTH ROTATE the shared `sv_sso_session` and will sign concurrent workers OUT of the other QA branches** (playbook §A trap 5) — **so only ONE live-build worker may hold that right at a time**, and it must be arranged, not assumed. | **us** (to do it) | **THE LARGEST SINGLE BLOCKER WE HAVE.** **12 Schedule cases** have never been observed at all (the whole Permissions area plus C30044, C38872, C38874, C38926) · **every Report Suite permission case, on all six reports**, cannot be driven · and **the negative half of Chris's link-permission rule** — a user with reports access but *without* work-order access — has **no verdict on any report** (WIP-COL-09 = [C43557](https://shopview.testrail.io/index.php?/cases/view/43557), SBC-LINK-05 = [C43558](https://shopview.testrail.io/index.php?/cases/view/43558), SBR-LINK-06 = [C43559](https://shopview.testrail.io/index.php?/cases/view/43559), SBC-PERM-04 = [C30100](https://shopview.testrail.io/index.php?/cases/view/30100), all `AUTOMATION: HOLD`). | 2026-08-04 · **ruling received 2026-08-06** |
| **C2** | **🔔 STAYS OPEN, AND ITS SEND CONDITION IS NOW A STANDING RULE (2026-08-12, new Standing Rule 66).** QA lead, verbatim: *"This should be the last thing once you give me the report that everything else has been done only this part is left and save it as a rule for now and for the future projects too."* **So the sheet is written and HELD, and it goes out ONLY once everything we can do ourselves on the Report Suite is finished — reported to him as exactly that.** **⚠️ This is NOT permission to sit on a blocker:** anything that actually stops work is still raised immediately, and anything that stops us creating or correcting a case is escalated in the same breath (Rule 62). **The 12 Location cases named below remain HELD, and they must say in their own words that the question has not been sent yet — never wording that implies Chris is sitting on it.** *(The original row, preserved:)* **🔄 REFRESHED 2026-08-06 (late) — THE SHEET IS NOW WRITTEN. It has NOT been sent.** `build/report-suite/questions-2026-08-06/Questions-for-Chris-Ward_Report-Suite_2026-08-06.xlsx` / `.md` — **13 items: one confirmation that releases seven held tests, five decisions, and seven wording edits needing no decision at all.** The project and the report are named on **every row** (Rule 55), all six spec versions were confirmed live immediately before writing (SBC 15 · SBR 17 · PV 5 · TU 6 · WIP 9 · IV 4), and **its 4th tab is QA-only and must not be forwarded**. **So what is outstanding is no longer the writing — it is SENDING it, and then Chris's answers.** **⚠️ SUPERSEDED WORDING, kept visible: this row read "authorised, not yet written".** The original requirement it was written against is preserved below.<br><br>It must carry **the project AND the feature/report on EVERY row**, because **he owns BOTH the Report Suite AND Fees & Discounts**. The questions to carry over, none of them lost: **(a) THE LOCATION-COLUMN RULING** — the build implements it **three ways out of six**, **all six specifications state it BOTH ways**, so **three reports will be wrong whichever way he rules**; **12 cases are held on it** (C30156, C30352, C30467, C30511, C30551, C30554, C30588, C38912, C38913, C38914, C38916, C38917). **(b) THE FOUR UNFINISHED SPEC CONTRADICTIONS** he has already *decided* but not finished editing — SBR **S21-R7** · WIP **S7-R13** (contradicting **S4-R3** in the same version) · IV **S7-R6** · SBC **S13-R4** — plus **PV**, the one report he has never touched on this point (**PV v5 S3-R10** still says the column is *"not user-toggleable"*). **(c) THE ~10,000-ROW EXPORT CAP.** It is real and deliberate — epic story [SV-8591](https://shopview.atlassian.net/browse/SV-8591) *"Export contract + 10k row-cap guard"* — and it means the Parts Velocity first-visit view (10,064 rows) **cannot be exported at all**. **✅ CORRECTED THIS PASS AGAINST THE LIVE SPEC MIRRORS: the cap IS documented in THREE of the six** — SBC v15 **S14-R16** + **S15-R25**, SBR v17 **S14-E2**, IV v4 **S10-R12** — **and is absent from PV v5, TU v6 and WIP v9.** ⚠️ **`build/report-suite/full-viu-2026-08-06/{FINDINGS,FILED}.md` both say *"none of the six specifications mentions the cap"* — THAT IS WRONG**, and the ask must be the narrow, correct one: *please add it to the three that are missing it*, not *please document it*. | **us** (write the sheet) → then **Chris Ward** (answer) | The **12** Location cases stay `HOLD`; PV-COL-02 = [C30352](https://shopview.testrail.io/index.php?/cases/view/30352) and PV-FILT-14 = [C38914](https://shopview.testrail.io/index.php?/cases/view/38914) stay deliberately unchanged; and three specifications keep a real behaviour undocumented, which is exactly how a correct build gets failed by a tester. | 2026-08-05 · **ruling received 2026-08-06** |
| **C3** | **A decision on whether to RE-INGEST the Schedule designs — CONDITIONALLY authorised, and the condition is NOT met yet.** His ruling, verbatim: *"Yes if Sasha's design is final."* **We have NOT established that it is.** **Sasha Grosman's three tickets — SV-8915, SV-8916, SV-8917 — all close with the same source:** `Design: https://claude.ai/design/p/d3cdcf5c-83df-45ea-ba75-7ddedb5124b5?file=Schedule.dc.html&via=share`, each *"Raised in the Schedule design review with Fabian on 5 Aug 2026."* That is a **share URL to a live, editable design page carrying NO version and NO date** — **not** the artefact we ingested (`build/schedule/design-2026-07-27/`, the Claude prototype Branko ruled authoritative at **Q0**) — and **~48 of our Schedule labels were pinned from that prototype**. | **Sasha Grosman / Fabian** (to say whether it is final) · then **us** (fetch + diff) | Under **Rule 31** a design whose currency cannot be established is a **PARTIAL source**, and under **Rule 32** the newest authoritative source wins — **so if his link is newer, our Schedule design baseline is STALE**. Today it means: we **cannot verify any of his three design-sourced claims against a design we hold**, and **SV-8916 could not be verified at all** — its button is in *his* design and in **no requirement of spec v23**. | 2026-08-06 |
| **C4** | **🔔 STAYS OPEN, AND ITS SEND CONDITION IS NOW A STANDING RULE (2026-08-12, new Standing Rule 66)** — same text as C2 above: the sheet is written and **HELD**, and it goes out **only once everything we can do ourselves on Filters and Schedule is finished**, reported to him as exactly that. **⚠️ AND THIS ROW IS THE REASON THE RULE NEEDED ITS COUNTER-LIMIT WRITTEN IN: the shop-closures question was drafted on 22 JULY and has never been sent — "the blocker is US, not him" — so a deferral must never become a drift.** Its 3 `AUTOMATION: HOLD` cases must say plainly that the question has not been sent. **The shop-closures item is also the product half of live tech-design contradiction L1** in [`build/rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md`](rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md). *(The original row, preserved:)* **🔄 REFRESHED 2026-08-06 (late) — THE SHEET TO SEND IS THE NEWER ONE, AND IT NOW CARRIES 21 ITEMS.** **Send `build/filters/questions-2026-08-06/Questions-for-Branko-Cicovic_Filters-and-Schedule_2026-08-06.xlsx`** (its plain-language twin and generator sit beside it; **the 4th tab is QA-only and must not be forwarded**). **⚠️ SUPERSEDED PATH AND COUNT, kept visible: this row pointed at `build/branko-questions-2026-08-05/…_Schedule-and-Filters_2026-08-05.xlsx` and said "13 questions".** That folder is now **banner-marked SUPERSEDED and is not a deliverable** — nothing in it was deleted or edited; **all 13 of its items were carried forward, imported from its own generator so the wording could not drift**, then 4 new items were added (17), and 4 Schedule items were merged in to reach **21**. **The reason for one file rather than two is Rule 55 itself: two unsent sheets to the same person is the "drip of separate asks" the rule forbids.** **Still outstanding: SENDING it, and then Branko's answers.**<br><br>*(The original row, preserved:)* **13 questions, one combined file, a tab per project, the project named on every row** (Rule 55). **It includes the SHOP-CLOSURES question drafted on 22 JULY and never sent — the blocker is US, not him**, and the sheet says so to his face. **Two further items belong on it or beside it: (a) SV-8915's TWO FALLBACKS are not in the specification** — a 7:00am default when no business hours are set, and deferring to an earlier shift's start; the second **genuinely conflicts with v23's own buffer wording**, so it is a **PO question before it is a test case** (Rule 57). **(b) SV-8916 has NO counterpart among our 168 and only a DESIGN as its source** — a candidate coverage gap that cannot be authored until C3 settles which design is canonical. | **the QA lead** (send it) → **Branko** (answer) | **3 Schedule cases stay `AUTOMATION: HOLD` waiting on a product-owner answer that has never been sent** — SCH-SPREAD-07 = [C29983](https://shopview.testrail.io/index.php?/cases/view/29983), SCH-EDGE-05 = [C30089](https://shopview.testrail.io/index.php?/cases/view/30089), SCH-DND-09 = [C43555](https://shopview.testrail.io/index.php?/cases/view/43555). **Honest note: for two of them his answer alone will not make the case runnable — the shop-closure setting does not exist anywhere in the product yet.** | **shop closures 2026-07-22**; sheet written 2026-08-05 |
| **RS1** | **🔄 REFRESHED 2026-08-06 (late) — Report Suite's remaining 73 cases.** **403 of 476 carry a verdict; 73 do not** (403 + 73 = 476), re-derived from live TestRail and **set-equal in BOTH directions** to the handover list. **⚠️ SUPERSEDED FIGURE, kept visible: this row read "the remaining 276 cases … 200 of 476 carry a verdict established on `v3.5-16cf83f`; 276 do not (SBR 109 · WIP 67 · SBC 58, plus 42 already NOT OBSERVED)".** **47 of the 73 need ONLY a working Admin sign-in** — see the green block at the top of this file. **Inventory Value, Parts Velocity and Technician Utilization are FINISHED.** **The branch has since moved to `v3.5-f77875c`, so only 51 of the 476 verdicts rest on the build now running.** | **us** (26 of the 73) · **the QA lead** (a sign-in, for the other 47) | **The arithmetic gate for Report Suite is NOT claimed to pass and must not be**, and the project may not be described as VIU-complete. **⚠️ TWO SUB-CLAIMS OF THIS ROW WERE WRONG AND ARE CORRECTED: (a) the "12 raw-markup cases" are REPAIRED — a full live census of all 476 found 0 raw markup and 476/476 carrying exactly one marker and one provenance line;** **(b) the "8 cases with no build line" is 5** (C30278, C43550, C43551, C43558, C43559) **and is NOT a defect** — each says in its own text that it has not been checked against a build, which is exactly what Rule 60 requires. | 2026-08-06 |
| ~~**RS2** — a Rule-49 re-check queue for the 2026-08-06 Report Suite pass.~~ | Report Suite | **✅ CLEARED 2026-08-06 — the queue EXISTS and is OPEN:** `build/report-suite/full-viu-2026-08-06/RECHECK-QUEUE.md`, and it covers **both** the earlier verdicts and the later session's own. **So the Report Suite queue family is FIVE files, all OPEN**, not four. Moved to **§7**. *(This row read: "That pass opened NO queue file, so the 200 verdicts it established are queued nowhere." True when written.)* | Nothing. Kept visible rather than deleted so the ask is provably discharged. | Raised **2026-08-06** · **cleared 2026-08-06** |
| **F9** | **Vlad's eleven-row Filters coverage-gap table** — unchanged, still queued. Full detail in the section immediately below. | **us** to work it · **the QA lead** to release it · **Branko** for row 7 | **Any claim that the Filters suite is coverage-complete.** | 2026-08-06 |
| **A1** | **Fresh `.qa.shopview.com` cookies whenever a live pass resumes** — and **read playbook §A's five traps FIRST**, because three of them have each produced a **false "dead session"**: a 401 is usually an expired **`cf_clearance`** rather than a dead sign-in (`sv_sso_session` and `PHPSESSID` were byte-identical across the failure); the **app host answers HTTP 200 on any path** because it serves the SPA shell; and **`paste -sd'; '` corrupts the cookie header** and cost a whole pass. **So ask for a fresh `cf_clearance`, not a new sign-in.** | **the QA lead** (on request) | Any live observation. Not currently blocking — the sessions worked on 2026-08-06. | standing |
| **B1** | **Engineering to declare any of the three branches final — and they have said they will NOT before release.** **⚠️ PARTLY ANSWERED 2026-08-11 — THE REPORT SUITE BRANCH *IS* FINAL** (all six reports handed off; QA lead, verbatim: *"note that ALL 6 reports have been handed off now."*). **This row now covers SCHEDULE (`sv8685`) and FILTERS (`sv8785`) ONLY.** | **engineering** | *(Superseded wording, kept verbatim and dated 2026-08-11:)* ~~**All 754 verdicts across the three projects stay PROVISIONAL** and all Rule-49 queues stay OPEN.~~ **CORRECTED: the 476 Report Suite verdicts are NO LONGER provisional pending development** — a deviation there is a real defect in a finished feature, and its Rule-49 rows **may close** on the ordinary close condition. **The 278 Schedule + Filters verdicts stay PROVISIONAL** and those queues stay OPEN. **Under Rule 60 that is the NORMAL STEADY STATE, not a failure** — but neither may be described as VIU-complete. **And note what finality did NOT buy on Report Suite: 251 of its 476 have still never been checked against any build (row RS-F1).** | 2026-08-03 *(Report Suite half answered **2026-08-11**)* |

### 🔴 BLOCKED ON THE QA LEAD — Standing Rule 48, all five fields

Only **one** row remains after today's rulings, and it is the release of the queue.

| Field | **Q1 — the release of RS1 and F9, and with them all four rulings above** |
|---|---|
| **(1) The ruling, verbatim** | *"BUt do that too once you are done with everything else in the last."* — and, on Vlad's table, *"But do that after everything else has ben done."* |
| **(2) When, and what it answered** | **2026-08-06**, answering *"when should these be picked up?"* for the four rulings and for the eleven-row gap table. |
| **(3) What it blocks, named** | **C1, C2, C3, C4 and F9 — all queued behind RS1.** Concretely: the 12 unobserved Schedule cases and every Report Suite permission case (C1), the 12 held Location cases (C2), the Schedule design diff (C3), 3 held Schedule cases (C4), and any claim that Filters is coverage-complete (F9). |
| **(4) Why it was reasonable** | **Entirely reasonable, and it is the right sequencing.** Report Suite has **276 cases with no verdict on the current build** — that is the largest honest gap in the estate, and interleaving five other workstreams into it (across three OPEN Rule-49 queues) would leave everything half-done. **An eleven-row requirement-side re-derivation is a whole pass, not a patch.** Nothing has changed that argues for reordering it. |
| **(5) The one thing that would unblock it** | **His word that RS1 is done** — or an explicit instruction to take one of C1–C4 out of order. From **the QA lead**. |

### ✅ What this pass CLEARED

- **`SV-8821` — keep or withdraw?** **DECIDED BY HIM: *"Marked it as AObsolete - ignore it for now."*** He
  transitioned it himself. **Our recommendation on record had been KEEP** (a server error on an ordinary
  user action reachable from the product's own screen is self-evidently wrong regardless of
  documentation — which is why it was kept when its API-only sibling SV-8822 was withdrawn), and **his
  ruling supersedes it (Rule 33)**. Moved to **§7**. **⚠️ STANDING CAUTION, Rule 53's corollary: that
  transition was HIS action under OUR SHARED ACCOUNT, so the changelog will read our own name. NOBODY
  MAY "RESTORE" OR RE-OPEN IT.** We have already made that exact mistake once — the
  `High → Low → High → Low` round trip of 2026-08-04.
- **"A second test login is blocked on the QA lead"** — **no longer true, and it was the largest row in
  this register.** It is now **C1, owned by us**, with the method and the concurrency caution recorded.
- **"Should we ask Chris anything else?"** — answered **yes**; it is now **C2**, authorised work rather
  than a blocked row.
- **A factual error in our own 2026-08-06 Report Suite records** — *"none of the six specifications
  mentions the export cap"*. **Three of six DO** (SBC v15 S14-R16 + S15-R25, SBR v17 S14-E2, IV v4
  S10-R12), verified against the spec bodies fetched live that day; **PV v5, TU v6 and WIP v9 are the
  three that are missing it.** Corrected here so the ask to Chris is the narrow, correct one. **The
  source files themselves were NOT edited — they belong to another worker's pass** (Rule 38's spirit);
  this register is the correction of record.

### Honesty notes

- **Nothing was verified live this pass** — no application, no TestRail, no Jira. Every figure above was
  taken from the projects' own committed evidence (`READINESS-*`, `full-viu-*/FINDINGS.md`,
  `REMAINING.txt`, `build/ticket-source-blocks-2026-08-06/`) and is cited to it.
- **The four rulings are relayed instructions, quoted verbatim.** Where one contradicts a recommendation
  of ours, the ruling wins and the recommendation stays on the record next to it (Rules 33 and 48).
- **All three Rule-49 queue families are OPEN**, and one project's newest pass has no queue at all (RS2).
  **Every pass/fail verdict on all three projects is PROVISIONAL** — that weakens none of the questions
  above, because all of them are decided by documents rather than by a build.

---

## ⚠️⚠️⚠️⚠️ 2026-08-06. **FILTERS: Vlad has raised an ELEVEN-ROW COVERAGE-GAP TABLE. It is QUEUED behind all other work on the QA lead's instruction, and until it is worked we CANNOT claim the Filters suite is coverage-complete.**

**RECORD-ONLY so far. Nothing analysed, nothing verified, no TestRail or Jira call, no case touched.**
Full transcription of the table, the four deliverables he asked for, and the starting hypotheses:
**`build/filters/vlad-gap-review-QUEUED.md`**.

| # | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **F9** | **The work on Vlad's eleven-row Filters coverage-gap table** — four deliverables, none started: **(1)** a **root cause in simple words** for how these were missed; **(2)** a **per-row verification** of whether the coverage is genuinely **absent**, **partial**, or **actually present**; **(3)** for anything that **does** exist, its internal ID + C-id + `https://shopview.testrail.io/index.php?/cases/view/<id>` link (Rule 8); **(4)** for anything genuinely missing, **the case authored** — a TestRail write needing a go-ahead (Rule 6). The rows span `S9-R2/S9-R3`, `S11-R7`, `S10-R2`, `S13-R19`, `S13-N4`, `S14-R6`, Parts views, Reports date-range URL, `R3 Q5` parity, `R3 Q5` single-range, and mobile imported-exclusivity. **Four rows name cases that already exist — C38896, C38908, C38882, C38877 — so those are PARTIAL-coverage claims, not absence, and each needs both texts quoted side by side (Rule 45(e)) before we agree or disagree.** | **us** to work it · **the QA lead** to release it from the queue and to authorise any authoring · **Branko** for row 7 (the Parts/Reports PRD, already tracked as **F8**) | **Any claim that the Filters suite is coverage-complete.** The 2026-08-05 final pass verified all **110** cases we **have**, each driven live — it did **not** re-derive whether **110 is the right set**, which is the requirement→case direction Rule 43 requires and Rule 45 exists to force. **Row 1 is the sharpest: it alleges our cases assert the REJECTED Status-chip behaviour rather than the DECIDED one, which would be a Rule-57-class defect and not merely a gap.** | 2026-08-06 |

**Why it is queued and not being worked now — Standing Rule 48, the QA lead's own instruction,
verbatim:** *"But do that after everything else has ben done."* It answers *"when should this be
picked up?"*, it is **reasonable** (an eleven-row requirement-side re-derivation is a whole pass, not
a patch, and starting it would interleave with three OPEN Rule-49 queues), and **the one thing that
would unblock it is his word that everything else is done.**

**The honest part, recorded now rather than discovered later:** this is the **fourth time this week**
an outsider saw something our own checks did not — **Vlad in July on the SBR export columns**,
**Ahtasham twice on Filters**, and now this. **The root-cause deliverable must address that pattern,
not just these eleven rows.**

---

## ⚠️⚠️⚠️ 2026-08-05, ~17:05 UTC, the PO QUESTION-SHEET pass. **THE TWO SHEETS ARE WRITTEN AND READY TO SEND. NOTHING NEW IS BLOCKED — WHAT CHANGES IS THAT 26 QUESTIONS NOW EXIST IN SENDABLE FORM, AND THREE PREVIOUS ROWS WERE WRONG.**

**DOCUMENTATION ONLY. No TestRail write, no Jira write, no case edit.** Both sheets are drafts for
the QA lead to send, mirroring the established format 1:1 (Rule 16) with a QA-only tab that must not
be forwarded, and every reader-facing row naming its **project and feature** with no case IDs, no
requirement anchors and no jargon (Rule 55).

| Deliverable | Questions | Path |
|---|---|---|
| **Chris Ward — Report Suite, round 3** | **13** | `build/report-suite/rulings-2026-08-05/Questions-for-Chris-Ward_Report-Suite_Round-3_2026-08-05.xlsx` / `.md` |
| **Branko Cicovic — Schedule AND Filters, one combined file** | **13** | `build/branko-questions-2026-08-05/Questions-for-Branko-Cicovic_Schedule-and-Filters_2026-08-05.xlsx` / `.md` |

**All eight specifications were re-fetched LIVE immediately before drafting** (Rule 31 — and it paid
for itself twice, see the corrections below). Report Suite at **2026-08-05T16:56:26Z**: SBC **v14** ·
SBR **v16** · PV **v5** · TU **v6** · WIP **v7** · IV **v4**, all HTTP 200. Schedule and Filters at
**17:00:12Z**: Schedule **v23** · Filters **v18**, both HTTP 200.

### 🔴 THREE ROWS IN THIS REGISTER WERE WRONG, AND THE LIVE RE-CHECK IS WHAT CAUGHT THEM

| Row | What it said | What is actually true | Consequence |
|---|---|---|---|
| **F5** (above) | *"Branko owes an answer on [SV-8876](https://shopview.atlassian.net/browse/SV-8876) … status **Ready**"* | **SV-8876 is CLOSED — `Done`.** Ahtasham closed it **himself** at **2026-08-05T08:38:16 −0500**, 2 h 21 min after raising it, commenting *"closing this as it was a gap with test case, I've updated the test case here >> C29557 And created a story defect >> as the build is not behaving as per PRD"*. He raised **[SV-8883](https://shopview.atlassian.net/browse/SV-8883)** (Story Defect, Open, parent SV-8786) instead. **Branko never answered it.** | **F5 is superseded, not cleared** — his *second* question (*"please update S1-R1 if the same-row layout is approved"*) died with the ticket and is still genuinely open. It is now **Filters question 1** on the Branko sheet, narrowed to the half that is his: *did you want them on one row, in which case SV-8883 should be cancelled?* Asking him to answer a closed ticket would have been wrong. **For the record, Ahtasham's resolution was CORRECT under Rule 57** — a closed ticket is not a spec amendment, so the build failing S1-R1 is a defect, not a spec question. |
| **The Report Suite "4 specs still state it both ways" framing** | four contradictions, TU among them | **TU v6 is now internally consistent** — S9-R9 **and** S10-R4 both state the access-gate + toggleable model. Only three stray sentences elsewhere in TU still read the old way. | **TU is deliberately NOT asked as a fifth contradiction.** It appears on Chris's sheet as a no-answer-needed tidy-up note. Asking it would have been asking about something he had already fixed. |
| **3 Report Suite cases marked "waiting on the product owner"** | implied Chris owes an answer | **He does not — this is OUR defect.** **SBC-VIS-02 = [C30186](https://shopview.testrail.io/index.php?/cases/view/30186)**: all five assertions are SBC v14 **S20-R8/R9/R10/R11/R14** almost verbatim, checked live — there is no product question behind it. **WIP-FLT-05 = [C30502](https://shopview.testrail.io/index.php?/cases/view/30502)**: WIP v7 **S7-R8** says *"A Custom range is capped at a 366-day maximum span"* and the build accepts 367 — per Rule 57 that is a **developer defect** with the documented expectation kept, not a PO question. **SBC-COL-04 = [C43550](https://shopview.testrail.io/index.php?/cases/view/43550)**: SBC v14 **S4-R12** answers it outright. | **All three were deliberately NOT put in front of Chris.** They need an authorised repair pass, not an answer — **+3 to ready-to-automate for free**, and one defect ticket to write. |

### The asks — what is outstanding after this pass

| # | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **P1** | **Send the two sheets.** Both are finished drafts; neither has been sent. The QA lead sends them. | **the QA lead** | **26 questions cannot be answered until they are sent.** Chris's Tab 1 alone releases the **16** Location cases (Report Suite ready-to-automate **440 → 456**); Branko's Schedule question 1 releases **2** (Schedule **158 → 160**). | 2026-08-05 |
| **P2** | **Chris Ward to finish the location-column edit in four documents.** He has **DECIDED** it — his own version messages on SBR/TU/WIP/IV say *"Location column changed to an access gate and made toggleable in the column selector"* — but four documents still carry the older paragraph, quoted verbatim on his sheet: **SBR v16 S21-R7** · **WIP v7 S7-R13** (contradicting **S4-R3** in the *same version*) · **IV v4 S7-R6** · **SBC v14 S13-R4** (a closed list of nine without Location). | **Chris Ward** | The **16** held cases. This is *"please finish"*, not *"please decide"* — a much smaller ask than it was this morning. | 2026-08-05 |
| **P3** | **Chris Ward to RULE on Parts Velocity — the one report he has not touched on this point.** PV **v5 S3-R10** still reads *"…it is not one of the 20 columns in the picker, S4-R1, and is not user-toggleable"*, and **he republished PV v5 today at 13:21:40Z**, so latest-wins (Rule 32) currently points **at the specification** for that report. It is now the odd one out of six. | **Chris Ward** | PV-COL-02 = [C30352](https://shopview.testrail.io/index.php?/cases/view/30352), PV-FILT-14 = [C38914](https://shopview.testrail.io/index.php?/cases/view/38914). **Deliberately not changed on the strength of what he decided for the other reports** — that is exactly the mistake that produced the boilerplate we spent today removing. | 2026-08-05 |
| **P4** | **Chris Ward — the 5 round-2 questions his updates did not touch**, carried forward: where the branch line sits in a Parts Velocity / Technician Utilization download · whether an on-screen indicator naming the branches should exist at all (**we removed it — 0 mentions in all six specs — and the sheet tells him so plainly**) · the export-filename leftover still mapping *Today* and *Yesterday* · the screen-reader name for the column button · whether Technician Utilization is meant to offer two spreadsheet downloads. | **Chris Ward** | C30160, C38859, C43552 and the 7 repaired-by-removal cases. Only the two-spreadsheet answer moves the count (**+1**). | 2026-08-04 |
| **P5** | **Chris Ward — the 2 questions now asked for the THIRD time**, each holding a live test: which menu group Sales By Customer belongs in (our build notes say SALES, his description says Performance) and whether the full word *"Representative"* applies on screen and on the customer record as well as in the files. Left blank on both previous sheets. | **Chris Ward** | SBC-NAV-01 = [C30096](https://shopview.testrail.io/index.php?/cases/view/30096) · SBR-WO-01 = [C30310](https://shopview.testrail.io/index.php?/cases/view/30310) · SBR-WO-06 = [C30315](https://shopview.testrail.io/index.php?/cases/view/30315). **+3** when answered either way. | 2026-08-04 |
| **P6** | **Chris Ward to confirm the new nine-preset date list is what he intends.** SBC v14 **S2-R2** puts *"Last 12 Months"* first and states *"There is no Today, no Yesterday, and no option labeled 'Custom'"*. Observed live: the two he deleted still work and **the new first one is refused**. **NOTHING HAS BEEN FILED** — the requirement was hours old, the picker was never driven on screen, and per Rule 51 an interface-only difference is asked about, never filed unasked. Reader-facing wording carries **no technical detail at all**. | **Chris Ward** first, then **the QA lead** for the Rule-51 filing call | Six date cases across all six reports, all currently READY. No count moves; what moves is whether they are written to a list the product actually has. | 2026-08-05 |
| **P7** | **Branko to answer whether the filter bar was meant to sit on the tabs' row — because a developer job has ALREADY been raised on the other reading.** Filters **v18 S1-R1**: *"The filter bar is displayed below the tab navigation row (All, Estimates, Completed, My Work Orders) by default."* The build puts the chips beside the tabs. **[SV-8883](https://shopview.atlassian.net/browse/SV-8883) is Open and will move the product.** | **Branko** | FLT-BAR-01 = [C29557](https://shopview.testrail.io/index.php?/cases/view/29557), now `READY - EXPECT FAIL`. **If he wanted the same row, SV-8883 should be cancelled and S1-R1 updated** — better asked before the change is built than after. **Note it would NOT restore our old waiver note either way** (Rule 57). | 2026-08-05 |
| **P8** | **Branko to ratify or correct 6 expectations that rest on the ENGINEERING PLAN only** — the specification is entirely **silent** on five of them: pre-existing shifts and events surviving the release · the Dashboard showing a multi-day job as one row · a work-order appointment reaching the board · a shift being scoped to the **job's** branch and never the technician's · the work-order priority choices and their default · whether an 8-week / 120-shift spread cap exists at all (**neither number appears anywhere in spec v23**). Covers **8 cases**: [C38867](https://shopview.testrail.io/index.php?/cases/view/38867)–[C38871](https://shopview.testrail.io/index.php?/cases/view/38871), [C38875](https://shopview.testrail.io/index.php?/cases/view/38875), [C38863](https://shopview.testrail.io/index.php?/cases/view/38863), [C38873](https://shopview.testrail.io/index.php?/cases/view/38873). **This is S4 above, now in sendable form as 6 plain questions.** | **Branko** | **Rule 20 AUTHENTICITY, not readiness** — and the distinction is stated rather than blurred: 6 of the 8 are `READY` (a case can be automatable while its basis is un-ratified) and 2 are `HOLD` on the not-built ground. **The sheet tells him to his face that we will not call something a requirement when no product document says it, and that those tests stay parked until he answers.** | 2026-08-05 |
| **P9** | **Branko to settle a Schedule gap that is a gap rather than a contradiction:** for a technician with **no working hours of their own**, is a weekend an ordinary working day or a day to warn about? Three parts of v23 do not add up — §4.2's default *"7:00 AM to 7:00 PM"* carries no weekday restriction, §4.5 says the spread *"automatically skips weekends"*, §4.11 counts a weekend shift as a conflict. | **Branko** | SCH-CONF-02 = [C30024](https://shopview.testrail.io/index.php?/cases/view/30024), SCH-CAP-01 = [C30030](https://shopview.testrail.io/index.php?/cases/view/30030) — **both READY, neither held.** No count moves. Included because we would otherwise be relying on our own reading of three sentences that disagree. | 2026-08-05 |
| **P10** | **Branko — two small Filters items.** (a) The phone button reads **"Apply Filters"** with a capital F while v18 writes *"Apply filters"* in both places — our cases must quote the screen (Rule 9), so we would rather the document matched. (b) **NO DECISION NEEDED:** **S12-R2** says the chips behave like desktop *"with one exception (see S12-R5)"* but **S12-R5 is the page-search paragraph**; the real exception is **S12-R6**. **Cause proven from his own v17 version message, read live:** *"Fix Story 12 numbering: deferred-apply requirement renumbered to S12-R6, placed after the page-search S12-R5."* He moved the target and the pointer stayed. | **Branko** | Nothing blocked. The stale pointer already cost one round of confusion in the 12:30 UTC cleanup pass. | 2026-08-05 |

### 🔴 BLOCKED ON THE QA LEAD — Standing Rule 48, all five fields

| Field | **P11 — the Report Suite repair pass for the 3 mis-held cases** | **P12 — the Parts/Reports PRD freeze (restates F8)** |
|---|---|---|
| **(1) The ruling, verbatim** | *"The ONLY real/production system is TestRail — NEVER write to TestRail (create/update/delete cases, runs, or results) without explicit user permission."* (Standing Rule 6) | *"lets wait for Brankos PRD"* |
| **(2) When, and what it answered** | Standing, and reaffirmed on every push this week. It answers *"may a worker fix a case it can see is wrong?"* — no, not without a go-ahead. | Given when he was asked what it would take to apply the staged Parts/Reports groups. |
| **(3) What it blocks, named** | **SBC-VIS-02 = [C30186](https://shopview.testrail.io/index.php?/cases/view/30186)** (spec-backed, wrongly held) · **WIP-FLT-05 = [C30502](https://shopview.testrail.io/index.php?/cases/view/30502)** (a developer defect, not a PO question) · **SBC-COL-04 = [C43550](https://shopview.testrail.io/index.php?/cases/view/43550)** (answered by S4-R12). **+3 ready-to-automate**, and one defect ticket to write for the 366-vs-367-day cap. | The **8** Filters cases on `HOLD - the feature is not in the product yet`: [C38904](https://shopview.testrail.io/index.php?/cases/view/38904)–[C38911](https://shopview.testrail.io/index.php?/cases/view/38911), plus [C38882](https://shopview.testrail.io/index.php?/cases/view/38882). |
| **(4) Why it was reasonable** | **Entirely reasonable, and this pass obeyed it** — the three were found, written up and left alone. Today's whole lesson is what happens when a pass edits on its own initiative. | **Correct, and today's audit proves it.** Applying them means asserting behaviour no written product source supports — precisely what Rule 57 forbids. Nothing has changed to reopen it. |
| **(5) The one thing that would unblock it** | A go-ahead for **3 `update_case` operations** (plus a Rule-51-free defect ticket for the day-cap). From **the QA lead**. | Branko's write-up — and **honestly, that alone does not move the count**: the feature must still ship before anyone can run these. The sheet says exactly that to Branko rather than implying his answer unblocks work it does not. |

### ✅ What this pass CLEARED

- **`S1` (Schedule shop closures — "never sent, the blocker is US")** — **no longer un-drafted.** It is
  **question 1 on Branko's sheet**, with both v23 sentences quoted, and it carries an **explicit apology
  in the reader-facing text**: drafted 22 July, never sent, two cases parked for two weeks waiting on an
  answer nobody asked him for. **It is not cleared until it is SENT** (that is P1).
- **`S4` and `S5` (Schedule)** — both are now sendable plain-English questions rather than internal notes
  (P8 and Branko Tab 1 question 2).
- **`F8` (Parts/Reports PRD)** — restated as a **gentle status ask** in Branko's own reading, per the QA
  lead's ruling, not as a new question.
- **The Report Suite round-2 sheet's Q1** — superseded. Chris **decided** the location column today; the
  question is no longer *what should it be* but *which leftover sentence to drop*.

### Honesty notes

- **Nothing was written anywhere.** Read-only Confluence GETs and read-only Jira GETs only. **SV-8876 and
  SV-8883 were read and NOT touched** (Rule 38 — and SV-8883 is Ahtasham's).
- **Both QA branches are still PARTIAL sources** — neither declared final, so the Rule-49 queues for
  Schedule, Filters and Report Suite all stay **OPEN** and every pass/fail verdict on all three is
  **PROVISIONAL**. That weakens none of the 26 questions: **all of them are decided by documents, not by
  a build.**
- **One combined Branko file, not two — deliberately.** Rule 55 says to sweep every open ambiguity onto
  **one** sheet so a PO answers in a single sitting *"rather than a drip of separate asks"*, and two files
  to the same person on the same day **is** that drip. Rule 55's real worry — a PO who owns three projects
  answering the wrong one — is handled by naming the project on **every row** and giving each project its
  **own tab**. Pointer stubs sit in `build/filters/branko-questions-2026-08-05/` and
  `build/schedule/branko-questions-2026-08-05/`.
- **The Rule-31 lesson landed again, and cheaply this time.** Re-fetching immediately before drafting
  turned one question into a different question (SV-8876), removed a question that no longer existed (TU),
  and stopped three cases being blamed on Chris that were our own defect. **A question sheet written off a
  40-minute-old read would have been wrong in three places.**

---

## ⚠️⚠️ NEWEST — 2026-08-05, ~14:15 UTC, the SCHEDULE final VIU pass. **THE COOKIE ASK (A2) IS CLEARED FOR SCHEDULE. SIX SCHEDULE ASKS, ALL NEW OR RESTATED.**

**Cookies arrived and worked. `A2` above is CLEARED for Schedule:** all 165 cases now carry an
automation marker (137 READY · 21 READY-EXPECT-FAIL · 7 HOLD) and **no provenance line names the dead
build any more.** Vlad can start. Sources: `build/schedule/expected-behaviour-audit-2026-08-05.md`,
`build/schedule/final-viu-2026-08-05/`, `build/schedule/READINESS-2026-08-05.md`.

**Ready to automate: 158 of 165** (up from 157 — click-to-arm turned out to be built).

| # | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **S1** | **Send Branko the shop-closures question. IT HAS NEVER BEEN SENT — the blocker is US, not him.** The specification answers the point two opposite ways and both sentences are still in live v23: §4.5 *"Shop closures and public holidays are not skipped in V1."* vs §12 *"Shop closures … block the spread step from placing shifts on those days."* The sheet was drafted on 22 July and sits ready at `build/schedule/PO-Questions-Branko-Schedule-2026-07-27.md`. | **us first**, then Branko | **SCH-EDGE-05 = [C30089](https://shopview.testrail.io/index.php?/cases/view/30089) and SCH-SPREAD-07 = [C29983](https://shopview.testrail.io/index.php?/cases/view/29983) stay `AUTOMATION: HOLD`** and cannot be given a verdict. Their markers now say honestly that the question has not been sent, rather than implying he is sitting on it. | **2026-07-22** |
| **S2** | **A yes or no on the API-only finding.** Standing Rule 51: an API-only defect is never filed on our own initiative, **and a go-ahead for a batch of ordinary defects does not cover it.** Honest complication: the 8-week / 120-shift limits appear **only in the engineering plan** and **nowhere in spec v23**, so there are three possible answers and we are not guessing. Write-up: `build/schedule/final-viu-2026-08-05/API-ASK.md`. | **the QA lead** | **SCH-API-02 = [C38873](https://shopview.testrail.io/index.php?/cases/view/38873) and SCH-SPREAD-11 = [C38863](https://shopview.testrail.io/index.php?/cases/view/38863)** sit on HOLD, and we cannot say whether they are defects, dropped scope, or future work. | 2026-08-04 |
| **S3** | **Authorisation to author 3 new cases for coverage another QA can see and we cannot claim.** Found by reverse-coverage diff over all 22 story defects, **no counterpart among our 165**: **[SV-8863](https://shopview.atlassian.net/browse/SV-8863)** (which view the module opens on — already **Ready to Fix**, so its case would be born EXPECT FAIL), **[SV-8870](https://shopview.atlassian.net/browse/SV-8870)** (creating a shift by dragging onto a day in **Month** view), **[SV-8867](https://shopview.atlassian.net/browse/SV-8867)** (reassigning a **series** member, per view). Internal IDs reserved and never used: `SCH-NAV-08`, `SCH-DND-09`, `SCH-REAS-07`. | **the QA lead** (authorisation) **+ a live run** | **Three real coverage gaps stay open.** They were deliberately **not** authored, because authoring a case without observing the behaviour is exactly the failure this pass was correcting. | 2026-08-05 |
| **S4** | **Branko to ratify 8 expectations that currently rest on the ENGINEERING PLAN, not on product truth — and the specification is silent on 6 of them.** SCH-REG-01…05 = [C38867](https://shopview.testrail.io/index.php?/cases/view/38867)–[C38871](https://shopview.testrail.io/index.php?/cases/view/38871) (data migration · the Dashboard collapsing many shifts into one row · work-order appointments reaching the board · cross-location leakage · work-order priority), plus SCH-API-04 = [C38875](https://shopview.testrail.io/index.php?/cases/view/38875), SCH-SPREAD-11, SCH-API-02. | **Branko** | Standing Rule 30: engineering intent never overrules product truth. **These are un-ratified expectations presented as tests** — the honest concession if challenged. They were **kept, not cut**, because they cover real release risks nothing else covers. | 2026-08-05 |
| **S5** | **Branko to fix a specification self-contradiction nobody had noticed.** §7 says the grid cell menu opens on **left-click**; **§14.1 and §14.2 twice describe a right-click context menu** as an editing affordance a permission tier unlocks. | **Branko** | **No case is wrong today** — ours follow §7 and the build agrees — but **a reader of §14 alone would test the wrong gesture.** Affects SCH-REAS-03 = [C30054](https://shopview.testrail.io/index.php?/cases/view/30054), SCH-PERM-02 = [C30075](https://shopview.testrail.io/index.php?/cases/view/30075), SCH-PERM-04 = [C30077](https://shopview.testrail.io/index.php?/cases/view/30077). | 2026-08-05 |
| **S6** | **Engineering to declare branch `sv8685` final; and a re-check of the 158 owed rows.** **Only 7 of the 165 were re-observed live this pass**; the other 158 carry 4 August verdicts on the now-dead `v3.5-4873abe` **and each says so in its own text.** Queue: `build/schedule/final-viu-2026-08-05/RECHECK-QUEUE.md`. | **engineering** (final) · **us** (the re-check) | **All 165 verdicts are PROVISIONAL** and the Rule-49 queue stays OPEN. Priority for the next run: the **3 not-built** cases first (one of the original four **did** flip today), then the 19 deviations. | 2026-08-05 |

### ✅ CLEARED by this pass — so nothing is re-asked

- **`A2` (Schedule markers + cookies)** — cookies arrived; **all 165 markers written and byte-verified**,
  all 165 provenance lines re-pointed. **Arithmetic gate passes: 137 + 21 = 158.**
- **The "which marker to standardise on" doubt for Schedule** — all 165 use the three exact literals.
- **The 16 raw-markup Schedule cases** — repaired in the same single write per case, formatting only.
- **Two Schedule cases claiming their fault had no ticket** — [C30010](https://shopview.testrail.io/index.php?/cases/view/30010) now names **SV-8834**, [C30041](https://shopview.testrail.io/index.php?/cases/view/30041) now names **SV-8874**.
- **One NEW Schedule defect filed:** **[SV-8886](https://shopview.atlassian.net/browse/SV-8886)** — the scope picker's tick-box mode has no *Select all* and no *Cancel*. Bug · Low · parent SV-8685 · story SV-8689 linked *Relates* · 11 field checks read back, all PASS · duplicate-searched with 4 JQL queries first.
- **A defect in OUR OWN work, found and reported rather than buried:** the 4 August pass changed **37 expected results and ZERO steps or preconditions**, so the label-correcting half of VIU was never done — and **two expectations had been bent to match the build** (SCH-SCOPE-05 = [C29967](https://shopview.testrail.io/index.php?/cases/view/29967) had come to assert that *Select all* and *Cancel* **do not exist**). Both restored. Full evidence: `build/schedule/expected-behaviour-audit-2026-08-05.md`.
- **Two of our own verdicts corrected against other QAs' tickets:** **SV-8873 was right and we were wrong** (typing a technician's full name finds nothing), and **click-to-arm is BUILT**, so one of the four not-built features has shipped.

---

## ⚠️ NEWEST — 2026-08-05, 14:25 UTC, the Filters FINAL-CHECK pass. **F1 AND F2 ARE BOTH CLEARED. TWO NEW ASKS, AND ONE OF THEM IS A JUDGEMENT CALL ONLY YOU CAN MAKE.**

Source: `build/filters/final-viu-2026-08-05/` and `build/filters/expected-behaviour-audit-2026-08-05.md`.
**Cookies arrived, so all 110 Filters cases were rewritten and byte-verified, the 8 phone cases were
finally observed on the running app, and the QA lead's correction of principle was audited across all
110.** Run 352 proven undamaged — 438 results, 0 missing by ID, Ahtasham's 36 Passed / 2 Failed untouched.

**✅ F1 CLEARED** — the cookies worked. **All 8 phone cases observed at 390 × 844.** The combined
"All Filters" sheet defers correctly; a single filter's own sheet applies on tap with **no button at all**.
**The label is `Apply Filters` — capital F** (`data-test-id="apply_filters"`). **Ready-to-automate is now
100 of 110**, not the predicted 101, because C38882 correctly moved *on* to HOLD (its report date filter
is not in the product).

**✅ F2 CLEARED, all three parts.** (a) SV-8845 is qualified as *"reported, closed without a fix"* — and
note it **still reproduces**, see N1 below. (b) **All 110 provenance lines now name Confluence version 18**
instead of the in-body *"1.6"*. (c) The capital-F label is corrected wherever it appears.

**⚠️ AND THE ANSWER TO F2(a) TURNED OUT TO BE THE OPPOSITE OF WHAT WE PROPOSED.** We asked whether the
closed-ticket cases should now say *"known and accepted"*. **They should not, and five cases already did.**
Your ruling settled it: *"A closed ticket does not change the expected behaviour."*

| # | What I need from you | Who owes it | What it blocks | Since |
|---|---|---|---|---|
| **F3** | **A decision on whether to reopen [SV-8845](https://shopview.atlassian.net/browse/SV-8845), and our recommendation is YES.** It is closed **OBSOLETE / Done** (Ahtasham, 2026-08-05 04:41:58 −0500) and **it still reproduces, worse than it was reported.** On a phone, **every** shared filter link is ignored and the app requests `filters[0][value]=estimate` instead — proven on `?status=declined`, `?status=paid` and `?status=imported`, all three listing 30 Estimate work orders — while the chips read *"All Filters (1)"* and *"Status (1)"* as though the filter had taken. The **same link on desktop** correctly requested `declined` and returned 7 Declined rows. This is a data-correctness fault, not a layout preference: a user shares a filtered link and the recipient sees the wrong work orders with no sign anything is wrong. **Two more closed tickets also still reproduce — [SV-8843](https://shopview.atlassian.net/browse/SV-8843) and [SV-8847](https://shopview.atlassian.net/browse/SV-8847), both closed under your own account on 4 Aug — and those two are cosmetic-to-moderate, so we are not recommending reopening them.** Nothing was reopened; that is your call, not ours. | **the QA lead** | Nothing is blocked in the suite — the cases keep the documented expectation and their markers say `EXPECT FAIL (SV-88xx - reported, closed without a fix)` so no tester waits. But **a real functional defect currently has no open ticket**, so it will not reach a developer. | 2026-08-05 |
| **F4** | **A one-line note in `build/APP-ACTIONS-PLAYBOOK.md` §J, from a worker that owns that file.** We discovered a **new TestRail normalisation** the hard way: **`update_case` re-renders any TEXT field you OMIT from the payload through its HTML pipeline** — it wrapped `custom_preconds` and `custom_steps` in `<p>…</p>` and turned `\n` into `\r\n` on the very first of 110 writes. A field **sent explicitly** is stored **verbatim**. The byte-check caught it on case 1, the batch stopped as Rule 50 requires, the two fields were restored byte-exact, and every later payload carried all three text fields. **This matters on this project specifically, because TestRail shows this markup literally to the tester here** — it is the same class of damage as the raw `<ol>`/`<li>` we repaired this morning. **We did not edit the playbook because it is outside this task's ownership.** | **the QA lead, or whichever worker owns the playbook** | Any future partial `update_case` on any project silently corrupts the fields it did not send. Report Suite and Schedule are both doing partial updates. | 2026-08-05 |

### Still open on Filters, unchanged

| # | What | Who | Note |
|---|---|---|---|
| **F5** | ⚠️ **SUPERSEDED 2026-08-05 17:00 UTC — SEE ROW P7 IN THE NEWEST SECTION. This row was WRONG: [SV-8876](https://shopview.atlassian.net/browse/SV-8876) is NOT `Ready`, it is `Done`.** Ahtasham closed it **himself** at 08:38:16 −0500, 2 h 21 min after raising it, and raised **[SV-8883](https://shopview.atlassian.net/browse/SV-8883)** (Story Defect, Open, parent SV-8786) instead. **Branko never answered it**, so the half that is still genuinely his — *did you want the buttons on the tabs' row, in which case SV-8883 should be cancelled?* — is now Filters question 1 on his sheet. | **Branko** (narrowed), via our own sheet, **not** via Ahtasham's closed ticket | Our cases follow the PRD until he rules. Neither ticket touched (Rule 38). | 
| **F6** | **A second test login** to prove one person's saved filters do not reach another — FLT-PERS-03 [C29615](https://shopview.testrail.io/index.php?/cases/view/29615). Not a tool limit; a genuinely absent second account. | **the QA lead** | The only genuinely un-settable case of the 110. It is 1 of the 10 `HOLD`. |
| **F7** | **The `sv8785` branch declared final.** | engineering | Until then **every one of the 110 verdicts is PROVISIONAL** (Rule 49) and `final-viu-2026-08-05/RECHECK-QUEUE.md` stays OPEN. |
| **F8** | **Branko's Parts/Reports PRD.** Your ruling stands: *"lets wait for Brankos PRD"*, so **no new Parts/Reports coverage was authored.** | **Branko** | 8 cases sit on `HOLD - the feature is not in the product yet` (C38904–C38911) plus C38882. That is absent product, not a readiness shortfall. |

### The honest item — our own miss, on the record

**Five Filters cases were telling testers to ignore a real specification violation, and they said so in
writing.** Each carried *"Known and accepted: … The product behaves this way on purpose for now. **Do not
raise this as a new problem.**"* over a requirement the PRD states plainly. **Nothing supported "on
purpose"** — the tickets behind them had merely been *closed*. **You found it in C29557. Ahtasham had
independently filed [SV-8876](https://shopview.atlassian.net/browse/SV-8876) about it eight hours
earlier.** All five are repaired, the audit covers all 110 (**A=5 · B=0 · C=104 · D=1**), and it was
committed **before** the repairs so the evidence stands alone. **Class B being zero is the one piece of
good news: every affected case had a documented requirement to go back to, so none of them needs Branko.**

---

## ⚠️ NEWEST — 2026-08-05, 12:30 UTC, the Filters cleanup pass. **TWO OF THE THREE ASKS BELOW ARE NOW CLEARED. ONE NEW ASK, AND IT IS THE SAME COOKIE ASK AS A2.**

Source: `build/filters/cleanup-2026-08-05/`. **25 Filters cases were repaired, all byte-verified, run
352 proven untouched.**

**✅ CLEARED — defect 1 below, the dead GitHub links.** Fixed on the 10 cases where that file genuinely
is the source, and **removed** from 7 more where it was not what the expectation rested on. Fixing the
owner alone was **not enough**: `bilalmuzamil-sketch/…/blob/main/…` gives **404**, because **there is no
`main` branch on this repository** — only four `claude/*` session branches. `blob/HEAD/` was used and
**verified HTTP 200** before and after the writes.

**✅ CLEARED — defect 2 below, the raw markup.** All 10 converted to plain numbered text. Each was
verified broken from its live text first, and a sweep of all 110 found no eleventh. **Formatting only.**
It also turned up an extra fault: **C29613 carried TWO provenance lines**, an older one left inside
`<hr /><p>…</p>`. The stale copy was removed, so all 110 now carry it **exactly once**.

**◐ PARTLY CLEARED — A1, the 8 phone cases.** The false line is **gone from all 8**, and
[C29624](https://shopview.testrail.io/index.php?/cases/view/29624) — which asserted the exact opposite of
the ratified specification — has been **reversed**, with a plain divergence sentence so a tester who
remembers the old behaviour does not raise a false bug. **What could NOT be done, and why it matters:**

- **No defect was filed, and none should be.** **[SV-8875](https://shopview.atlassian.net/browse/SV-8875)
  already reports it** — a Story Defect under SV-8797, **Open**, raised by **Ahtasham Amjad at
  2026-08-05T05:50:12 −0500**, 32 minutes after Branko's closure, quoting the same reading of S12-R6 we
  reached independently and naming our own case numbers. **A1's "raise one Low defect" instruction is
  therefore superseded — filing would be a duplicate.** His ticket was not touched.
- **Nothing was observed on the app**, so all 8 carry **`AUTOMATION: HOLD - needs one live check…`** and
  **Rule-54 state-1 provenance with no build date**. Marking them READY would assert the build passes and
  EXPECT FAIL would assert it fails; we have seen neither. **So the Filters ready figure stays at 93 of
  110 — it does NOT reach 101 this pass.**
- **A1's "28 minutes" figure was wrong and is corrected below.**

| # | What I need | Who owes it | What it blocks | Since |
|---|---|---|---|---|
| **F1** | **Fresh QA-branch cookies — the same ask as A2 in the section below, and it blocks Filters too, not just Schedule.** Every cookie set on the machine returns `HTTP 401 {"error":"sso_required"}` against `sv8785api.qa.shopview.com`, and they all share the **same expired `sv_sso_session`**, so there is no fallback; `quick-login` is itself gated by a valid session and also 401s. **What I need: `sv_sso_session`, `PHPSESSID` and `cf_clearance` for `.qa.shopview.com`.** | **the QA lead** | **The 8 Filters phone cases cannot be given a pass-or-fail verdict, so the ready figure is stuck at 93 instead of 101**, and we cannot confirm whether the button on screen reads *"Apply filters"* or *"Apply Filters"*. Exactly what to observe, with named test data, is written out ready to run in `build/filters/cleanup-2026-08-05/PENDING-LIVE-CHECK.md`, and each case has a row in that folder's `RECHECK-QUEUE.md`. It is about ten minutes' work once the cookies land. | 2026-08-05 |
| **F2** | **Your word on three Filters tidy-ups we deliberately did NOT make**, each a decision rather than a repair. **(a)** [SV-8845](https://shopview.atlassian.net/browse/SV-8845) is now **OBSOLETE / Done** — Ahtasham closed it at 2026-08-05T04:41:58 −0500 — yet **two of our cases still describe it as an open reported problem** (C29630 and the shared-link case). Should they now say *"known and accepted"*? **(b)** Every provenance line in the suite says *"specification version 1.6"* while the live Confluence version is **18** — the exact staleness trap Standing Rule 31 warns about. Changing it on a few cases while the rest keep the old wording would make the suite inconsistent, so it wants **one authorised pass over all 110**. **(c)** The **"Apply Filters"** capital-F label correction, once someone has looked. | **the QA lead** | Nothing is broken today, but (a) points a tester at a closed ticket and (b) means no reader can tell from a case which version of the specification it was written against. | 2026-08-05 |
| **F3** | **Something to tell Ahtasham, not a task for us.** He raised **three new Story Defects today** — **SV-8872**, **SV-8875**, **SV-8878** — plus **SV-8876**, a clarification asking why the filter bar shares the tab row. **That last one is ground you already closed as accepted in [SV-8843](https://shopview.atlassian.net/browse/SV-8843)**, so it is heading for a reopen by the back door unless someone tells him. He is also **actively grading run 352 right now** — it moved from 27 Passed / 5 Failed to 36 Passed / 2 Failed during our 30-minute window. | **the QA lead** | Nothing of ours. Raised so a closed decision is not quietly reversed (Standing Rule 33). | 2026-08-05 |
| **F4** | **Branko still owes one small specification fix.** **S12-R2 says *"see S12-R5"* where it means **S12-R6*** — a leftover from his own version-17 renumbering (his note: *"deferred-apply requirement renumbered to S12-R6"*). It matters more than it looks: that cross-reference is what tells a reader the deferred-apply rule covers a **single** filter's sheet and not just the combined one, which is the whole basis of SV-8875 and of our reversed C29624. | **Branko (PO)** | No test case is blocked — we read it with the renumbering in hand — but the sentence as written points at the wrong rule. | 2026-08-05 |

## 2026-08-05, the automation-marker pass (superseded in part by the section above)

**Filters and Schedule were both authorised for the automation marker. Filters is DONE on 102 of 110
cases; Schedule is at 0 of 165 by design.** Sources: `build/automation-markers-2026-08-05/`.

| # | What I need | Who owes it | What it blocks | Since |
|---|---|---|---|---|
| **A1** | **◐ PARTLY CLEARED 2026-08-05 12:30 — see the section above. The false line is gone from all 8 and C29624 is reversed; the defect must NOT be raised because [SV-8875](https://shopview.atlassian.net/browse/SV-8875) already reports it; and the markers are HOLD, not `READY - EXPECT FAIL`, because nothing was observed on the app — so the ready figure stays at 93, not 101. ALSO: the "28 minutes" below is WRONG — the report was finished about 04:50 UTC and his comment was at 05:18 −0500, which is 10:18 UTC, so the gap was FIVE AND A HALF HOURS. A −0500 timestamp was read as if it were UTC.** ~~One word on the 8 Filters phone cases.~~ **Branko ANSWERED and CLOSED [SV-8825](https://shopview.atlassian.net/browse/SV-8825) at 2026-08-05 05:18 UTC** — *"This is updated in the filters prd, I'm closing it."* — **28 minutes after our readiness report was finished saying it was still open with no comments.** Spec **v18 S12-R6** now rules it: a phone applies **only when you tap "Apply filters"**. The build applies **as you tap**. So those 8 cases are **no longer waiting on him**, the line inside each of them saying they are is **now false**, and their real verdict is *"the product is wrong"* — with **no defect ticket**. **Say go and one pass fixes all of it:** correct the 8, raise **one Low defect** on epic SV-8785 with story SV-8797 linked, and mark them `AUTOMATION: READY - EXPECT FAIL`. | **the QA lead** | **8 cases carry a statement that is untrue, and 8 cases the automation engineer is being told to skip that he could be automating.** Fixing it takes the Filters ready figure **from 93 to 101 of 110**. | 2026-08-05 |
| **A2** | **~~A choice on Schedule's markers.~~ NOW A SINGLE, SIMPLER ASK: fresh QA-branch cookies.** A full Rule-49 re-check of all 165 rows was attempted on 5 August and **could not start — 0 of 165 rows were re-observed.** The Schedule cookies (dated 2026-08-04 11:31 UTC, ~24.5 h old) return `HTTP 401 {"error":"sso_required"}`; the Filters and Report Suite sets are dead too, so it is the ordinary ~24-hour expiry across the whole `.qa.shopview.com` estate plus this morning's deploy, and it **cannot be worked around from here**. **What I need: fresh `sv_sso_session`, `PHPSESSID` and `cf_clearance` for `.qa.shopview.com`.** Everything else is ready and staged as a **single** write per case — `build/schedule/recheck-2026-08-05/WRITE-PLAN.md`. **Option (ii) — writing the 142 build-independent markers now — is no longer worth taking:** two of the 19 "expect fail" cases have changed since (they now have tickets), which is exactly the kind of drift that makes a half-pass need re-writing. | **the QA lead** | **All 165 Schedule cases still carry NO automation marker, and all 165 provenance lines still name the dead build `v3.5-4873abe`.** Vlad cannot tell which to start on. | 2026-08-05 |

**Two defects in our OWN data, found by this pass, reported and NOT silently fixed:**

1. **✅ FIXED 2026-08-05 12:30 (and the owner-only fix would NOT have worked — there is no `main` branch on the repository, so `blob/HEAD/` was used, verified HTTP 200).** ~~The GitHub links inside the Filters case provenance lines do not work.~~ They point at
   **`bmuzamil-shopview/Manual-test-Cases`** (HTTP **403**); the repository is
   **`bilalmuzamil-sketch/Manual-test-Cases`** (HTTP 200). A tester following the link in a case gets
   nothing. Fixing it means editing the provenance line on the affected cases — **needs your go-ahead**.
2. **✅ FIXED 2026-08-05 12:30 — all 10 converted, formatting only; a sweep of all 110 found no eleventh; and C29613 also had a doubled provenance line, now single.** ~~10 Filters cases show raw `<ol>` / `<li>` markup to the tester~~ — C29557, C29560, C29566, C29568,
   C29573, C29575, C29582, C29613, C29625, C38911. **This predates this pass** (the same 10 are in the
   pre-write snapshot), and it is the same defect already recorded for 16 Schedule cases. A repair is
   10 writes and **needs your go-ahead**.

**2026-08-05, the Schedule re-check attempt — TWO OLD ASKS ARE NOW ANSWERED, and neither needed us:**

1. **✅ CLEARED — the "eleventh Schedule ticket" ask is answered: it is NOT needed.** The 4 August
   readiness report asked whether to raise a ticket for SCH-MODAL-03 =
   [C30010](https://shopview.testrail.io/index.php?/cases/view/30010) (the shift window's time-logged bar
   reading fully worked when nothing had been clocked). A duplicate search across all 22 story defects on
   the epic found **[SV-8834](https://shopview.atlassian.net/browse/SV-8834)** — raised by **Mudassir
   Qamar on 04 Aug 08:39**, Open — describing the identical fault with the identical `1h / 1h` figure.
   **Filing ours would be a duplicate of another tester's ticket.** The case's own text still says the
   fault *"has no developer ticket yet"*, which is now false and is queued for repair in the single write.
2. **✅ CLEARED — SCH-TOOL-03 = [C30041](https://shopview.testrail.io/index.php?/cases/view/30041) is no
   longer an unticketed deliberate omission.** **[SV-8874](https://shopview.atlassian.net/browse/SV-8874)**
   (Mudassir Qamar, **05 Aug 05:26**) files the toolbar-search behaviour as a defect. Our reasoning for not
   filing it stands as reasoning, but the decisions-register entry must stop calling it unticketed.

**Two NEW items from the same pass, both needing the build to settle — recorded so they are not lost:**

| # | What it is | Who owes it | What it blocks | Since |
|---|---|---|---|---|
| **A3** | **Two of our PASS verdicts are contradicted by other testers' accepted defects, and they are probably right.** [SV-8873](https://shopview.atlassian.net/browse/SV-8873) (**Ready to Fix**) says sidebar search returns nothing for a technician's **full** name — our [C29939](https://shopview.testrail.io/index.php?/cases/view/29939) passed, and our evidence never records which form of the name we typed. [SV-8868](https://shopview.atlassian.net/browse/SV-8868) (**Ready to Fix**) says the Status filter returns nothing for **most** statuses — our [C29944](https://shopview.testrail.io/index.php?/cases/view/29944) passed on **Approved alone**, which is a sample, not the filter. Both look like **our verdict being wrong, not their ticket**. Needs the build to settle; their tickets were not touched. | **needs build access** (then us) | Two cases would tell an automation engineer to expect a pass where the product is accepted as broken. | 2026-08-05 |
| **A4** | **Three of their new defects look like coverage gaps we have no case for.** [SV-8863](https://shopview.atlassian.net/browse/SV-8863) (**Ready to Fix**) — which view the Schedule **opens on**; [SV-8870](https://shopview.atlassian.net/browse/SV-8870) — creating a shift by dragging onto a day in **Month view**; [SV-8867](https://shopview.atlassian.net/browse/SV-8867) — reassigning a **series member** by drag. A reverse-coverage diff against all 165 found no plausible counterpart for any of the three. **Authorising three new cases would close it** — not authored, because new cases need your go-ahead and live observation first. | **the QA lead** | Three real, already-accepted product faults that our suite would not catch on a re-run. | 2026-08-05 |

**Also worth knowing, and nothing is needed from you:** **Ahtasham has logged two more Passed results**
on Filters run 352 since this morning — he now stands at **25 Passed / 7 Failed** across **427** result
records, not the 23/7 and 425 our own morning report recorded. **Every one of his results was proven
present by ID and byte-identical before and after our writes.** And the standing note that the Filters
import file was still character-shredded is **out of date — it was already repaired**; the guard passed
and the file is clean.

---

## THE SHORT VERSION — one line per project

> **⚠️ REWRITTEN 2026-08-06.** The table that stood here was written on 2026-08-04 and every row of it
> had been overtaken by events (it still asked for the Filters VIU go-ahead, which has since run twice,
> and for a Schedule sheet that has been superseded). The 2026-08-04 table is kept immediately below,
> banner-marked SUPERSEDED, so nothing is deleted.

| Project | The single thing I most need from you |
|---|---|
| **Report Suite** | **Your word that RS1 is finished, or an instruction to take something out of order.** **276 of 476 cases carry no verdict on the current build** (SBR 109 not started · WIP 67 · SBC 58, plus 42 recorded not-observed with written reasons). Everything else in the estate is queued behind it on your own instruction: *"BUt do that too once you are done with everything else in the last."* Second, whenever a live pass resumes, **one non-administrator sign-in window** (C1) — it is the only thing blocking every permission case on all six reports. |
| **Schedule** | **Send Branko the combined sheet** — `build/branko-questions-2026-08-05/Questions-for-Branko-Cicovic_Schedule-and-Filters_2026-08-05.xlsx`, 13 questions, written and ready, including **the shop-closures question drafted on 22 July and never sent — that blocker is US, not him.** Then **say whether Sasha Grosman's design is final** (C3), because if it is, our design baseline is stale and ~48 pinned labels rest on the older prototype. |
| **Filters** | **Release F9 — Vlad's eleven-row coverage-gap table.** Until it is worked, **Filters may NOT be described as coverage-complete**: the 2026-08-05 pass verified all **110 cases we have** and every one was driven live, but it did not re-derive whether **110 is the right set**. **Row 1 is the sharpest — it alleges our cases assert the REJECTED Status-chip behaviour rather than the DECIDED one**, which would be a Rule-57-class defect rather than a gap. |
| **Cross-project** | **A ruling on "Simple Flow V2" (SV-8683)** — it is Open with 7 children against a project we have marked COMPLETED. |

### The 2026-08-04 short version — SUPERSEDED, kept for the record


| Project | The single thing I most need from you |
|---|---|
| **Report Suite** | **Two yes/no answers and one email.** (a) **Say yes to 7 leftover `refs` spec-version pins** — C30519, C30536, C30565, C30574, C30589, C30596, C30597 still cite their spec as a bare file path with no version. They were the only 7 of 367 left undone, because your instruction for the defect-finding and Location families was that the build-date line is the *only* permitted change, so I obeyed it literally rather than quietly doing it anyway. One word completes it. (b) **Rule on the 9 MERGE groups + 1 CUT** staged in `build/report-suite/audit-exhaustive-2026-08-04/AUDIT.md` — irreversible and structural, so they wait for you; and note the automation engineers start from the live suite today, so changing its shape underneath them may be worth deferring anyway. (c) **Send Chris ONE workbook — `build/report-suite/chris-consolidated-2026-08-04/Report-Suite_Questions-and-Decisions-for-Chris-Ward_2026-08-04.xlsx`** (+ `.md` twin). **UPDATED 2026-08-04 on your instruction, verbatim *"make one sheet and share with me with three tabs"*:** the three earlier sheets are consolidated into this single workbook — **Tab 1 "Urgent - Location column"** (1 question, needed today, unblocks the 8 Location cases), **Tab 2 "The product vs your write-up"** (9 items), **Tab 3 "Questions and things to note"** (14 items), plus a QA-only tab that is not for Chris. **24 items in front of him, from 28 — 4 overlaps removed so nothing is asked twice.** The three source sheets (`PO-Questions-Chris-ReportSuite-2026-08-03`, `chris-sheet-2026-08-04/…`, `chris-location-question-2026-08-04/…`) are **banner-marked SUPERSEDED and kept, not deleted — do not send any of them**; **24 held cases cannot be finished without his rulings** (**8** Location — the true count, re-verified live 2026-08-04 from all 8 case bodies, not the 7 the audit header says — 6 date-picker presets, 3 asset-identifier, 2 rep label, 2 single-location filter, 1 Estimates value, 2 PO-first). **CLEARED 2026-08-04:** the QA branch arrived and you ruled *"consider the branch verification as final for now"*, so all 478 cases now carry a plain build-date + spec-version line and every accumulated wording correction is live (`build/report-suite/final-push-2026-08-04/`). ~~all 475 cases VIU-Pending, none checked against the real build~~ — no longer true. **Still open regardless: the Rule-49 re-check queue stays OPEN** — engineering never withdrew "not final", so nothing here may be called VIU-complete. |
| **Schedule** | **Send Branko the Round-3 sheet** — 7 questions, written and ready (`PO-Questions-Branko-Schedule-2026-07-31-Round-3`); **send only Round 3**, it replaces the never-sent Round 2. **The QA branch has ARRIVED (`sv8685`, 2026-08-04) — so the second ask is now your GO-AHEAD to start the VIU on it**, which you reserved until Report Suite is finished. |
| **Filters** | ~~**Branko's returned sheet came back BLANK — 0 of 8 answered**~~ — **✅ ANSWERED 2026-08-04: 9 of 9, the fourth pull of the link and the first with anything in it.** **The sorting question is settled: Q4 = "C", sorting is NOT part of this project** — so the ~6–8 case proposal is **cancelled, not deferred**, and *"how many sorting cases do we have?"* is now permanently **zero, by product decision**. **Q1 = "A - no apply button"** retired **both** HIGH risks in the defence register. (~~Whether a Jira epic exists~~ — **ANSWERED 2026-08-04: SV-8785, all 110 cases cite a real ticket.**) **~~(1) the GO-AHEAD for the staged `update_case` operations~~ — ✅ GIVEN AND EXECUTED 2026-08-04: 12 × `update_case`, all HTTP 200 + byte-verified MATCH, run 352 untouched. *"a product owner decision is still awaited"* now appears on 0 of 110 cases, and per your citation ruling 12 cases now name Branko's answer file and give its link.** **What is owed by you now: (1) the GO-AHEAD to start the VIU** on `sv8785` (you reserved it until Report Suite is finished); **(3) arrange the Engineering + PO session** Branko proposed for the page-by-page Parts/Reports list — that ask is no longer his alone. |
| **Cross-project** | **A ruling on "Simple Flow V2" (SV-8683)** — it is Open with 7 children against a project we have marked COMPLETED. |

---

## 1. REPORT SUITE — PO: Chris Ward · Epic **SV-8582** · **ours: 469 active cases** (live folder total 474 — the other 5 are Vladimir Tomovic's automation cases, hands-off per Standing Rule 38) · **324 verified against the build · 392 of 469 automatable today**

> **UPDATED 2026-08-04 — the exhaustive audit changes the counts and adds 6 asks.**
> **Population is 478, not 475** (the three cases authored 2026-08-04 — SBC-API-06 = C43546,
> PV-EXP-12 = C43547, IV-EXP-10 = C43548); **live folder total is 483**, not 480. All 478 have now
> been **cold-read on all three dimensions** — the first genuinely exhaustive pass; the previous
> certification read 24 of 475. Single authoritative status ledger, replacing every other total in
> the repo: **`build/report-suite/audit-exhaustive-2026-08-04/VERDICT-LEDGER.md`**
> (**327 VIU-Observed-PASS · 109 DEVIATION · 13 NOT-BUILT · 20 EXTERNAL-DEPENDENCY ·
> 9 BLOCKED-BY-DEFECT = 478**, with the 9-case `invoices/create` HTTP 500 / SV-8821 reclassification
> applied and independently re-derived).
> **Six NEW asks, all on the QA lead, all raised 2026-08-04** — full five-field detail in
> `build/report-suite/audit-exhaustive-2026-08-04/AUDIT.md` §OUTSTANDING:
> 1. **A ruling on the Location-column contradiction** — the IV and WIP specs say the column appears
>    automatically; **7 of our cases say the user switches it on**, matching the build. As written
>    they **cannot fail a build that breaks the current spec**, and one says so in its own text
>    (*"That is what the build does today."*). Aligning them reverses a prior authorised choice, so it
>    is your call. Vladimir's C38920 already asserts the spec model, so it is visible from outside.
> 2. **Authorisation for the 25-case FIX-WORDING list** — all wording, none wrong about the product.
> 3. **Authorisation for the 9-group merge plan + 1 CUT** (478 → 468).
> 4. **A decision on the shared-shell family** — 114 cases assert behaviour from one shared component
>    across six reports; consolidating is a defensible ~90-case reduction, keeping it is defensible
>    too. Not our choice to make.
> 5. **A decision on re-verdicting 3 cases from PASS to DEVIATION** (SBR-EXP-06 = C30281,
>    SBR-VIS-03 = C30307, SBC-EXP-09 = C30167) — each recorded as passing while the observation
>    written underneath it contradicts the case. Ledger would become 324 PASS / 112 DEVIATION.
> 6. **Authorisation to extend 2 cases for two small surface gaps** (PV-EXP-04 needs a PDF leg;
>    SBR-EXP-11 needs the per-invoice order) — same defect class as the 2026-07-31 export miss.
>
> **Also updated:** the master re-check queue now represents **478 of 478** cases (was 341) and stays
> **OPEN**. **Still needed from others:** the **Jira ticket key for TU Story 10** (the only
> traceability shortfall in 478 cases), a **decision on re-reading epic SV-8582** (6 stories reopened
> 2026-07-31, source is PARTIAL), **Chris Ward's 5 pending spec edits**, **confirmation of when branch
> `sv8582` becomes final**, and **a technical tester for the 56 tool-dependent cases** (30 network
> panel, 25 screen reader/inspector, 1 QuickBooks — 422 of 478 are runnable unaided).

| Item | Category | Who owes it | What it blocks | Outstanding since |
|---|---|---|---|---|
| **NEW 2026-08-04 — the location column: one question, sheet WRITTEN and ready to send: `build/report-suite/chris-location-question-2026-08-04/PO-Question-Chris-Ward-Location-Column-2026-08-04.md`/`.xlsx`.** Deliberately single-issue and one page. Established live the same day: **Work In Progress** offers the column as a manual switch that is off by default and never appears on its own, **Inventory Value** shows it from the start and **does not hide it when you narrow to one location** (screenshot: filter reads one branch, column repeats that branch on every row) — while **both specs say it is automatic and not user-toggled** (WIP `S4-R3` + `S7-R13`, IV `S7-R6`, quoted verbatim on the sheet). The other four reports follow the automatic model. | QUESTION | **Chris Ward** — **you only need to send it** | **8 cases** (the true count, all 8 re-read live 2026-08-04): IV-COL-01 = [C30551](https://shopview.testrail.io/index.php?/cases/view/30551) · IV-COL-04 = [C30554](https://shopview.testrail.io/index.php?/cases/view/30554) · IV-PERS-02 = [C30580](https://shopview.testrail.io/index.php?/cases/view/30580) · IV-EXP-02 = [C30588](https://shopview.testrail.io/index.php?/cases/view/30588) · IV-LOC-06 = [C38917](https://shopview.testrail.io/index.php?/cases/view/38917) · WIP-COL-01 = [C30466](https://shopview.testrail.io/index.php?/cases/view/30466) · WIP-COL-02 = [C30467](https://shopview.testrail.io/index.php?/cases/view/30467) · WIP-FLT-09 = [C38916](https://shopview.testrail.io/index.php?/cases/view/38916). **All 8 currently describe the build rather than the spec, so they cannot fail a build that is wrong** — and the automation engineers start from them today. | 2026-08-04 |
| **NEW 2026-08-04 — a decision on the 6 Work In Progress nightly-snapshot cases, and 3 developer asks behind the other 6.** You asked *"The 12 cases which can not be verified -> WHich are those cases? Can a manual tester run them?"* — answered in full in `build/report-suite/nightly-cases-2026-08-04/TWELVE-CASES-EXPLAINED.md`. **The "none of them can be verified" claim was too pessimistic and is corrected: 6 of the 12 are wholly or partly runnable today** through the Inventory Value "As of" date control (proved live by mouse clicks — picking a past date moved the totals to a different, stored figure). **The 6 Work In Progress ones are not runnable by anyone, BY DESIGN** — WIP spec `S11-R7` verbatim: *"No screen in this version reads the snapshot; there is no Trend tab."* | DECISION + OTHER TEAM | **(a) you** — choose: ask dev for a QA read route, or defer the 6 until the Trend view ships (**I recommend defer**); **(b) a developer on SV-8667/SV-8678** — a capture trigger + 13-months-deep history; **(c) a developer or Chris** — is part `R134A-CORE` flagged as a core part? | **WIP-API-01…06 = [C30528](https://shopview.testrail.io/index.php?/cases/view/30528)–[C30533](https://shopview.testrail.io/index.php?/cases/view/30533)** are permanently unanswerable without a ruling. **IV-API-05 = [C30609](https://shopview.testrail.io/index.php?/cases/view/30609)** and **IV-API-06 = [C30610](https://shopview.testrail.io/index.php?/cases/view/30610)** need history 13+ months deep (this org holds ~5 days; earliest served day **2026-07-31**) — otherwise unrunnable until ~Sept 2027. Wording fixes for 5 IV cases are **staged, not applied**, awaiting your go-ahead. | 2026-08-04 |
| ~~**QA branch / environment + feature-flag state.**~~ **✅ CLEARED 2026-08-03 — the QA branch arrived and access is PROVEN.** App `https://sv8582.qa.shopview.com`, API `https://sv8582api.qa.shopview.com`, build marker **`v3.4.1-0ed4433`**. `quick-login` 200, `reportsPageAccess` held, all six reports reachable, exact routes captured. **Flag state answered: there is NO Report Suite feature flag** — the 13-flag catalogue contains no such entry and all six reports render unflagged (9 flags enabled for the org). Evidence `build/report-suite/viu-2026-08-03/ACCESS-PROOF-2026-08-03.md`. **⚠️ BUT engineering says the branch is NOT FINAL**, so every finding is provisional under new **Standing Rule 49** and queued in `build/report-suite/viu-2026-08-03/RECHECK-QUEUE.md` (**OPEN, 35 rows, 0 re-checked**). | ACCESS | — **closed**, but see the new re-check row below | **The suite is no longer theoretical: 86 cases have a live-matched assertion, 9 wordings are proven wrong, 13 are right-but-the-build-is-behind, 124 are still untouched.** No case may be called VIU-complete while the build is non-final. | Raised 2026-07-22 · **cleared 2026-08-03** |
| **8 spec-text corrections Chris still owes**, headed by the **WIP asset identifier**. He told us on 2026-07-29 he had already edited it; the live spec re-pull on 2026-07-31 shows §4 / S4-R7 / S4-R8 / S4-R9 / S7-R4 **still unit-number-first**. Our WIP cases follow his later answer, so spec and cases are out of step. The other open items are SPEC-WATCH **4, 6, 8, 9, 10, 11**. | OTHER TEAM | **Chris Ward** (PO) | Our cases are currently *more* current than his spec. Anyone auditing us against the written spec will read a mismatch as our error. Two of the items (**4** = location filter hidden, **9** = "Sales Representative" labels) now **actively contradict** rulings he gave us afterwards. | 2026-07-28 (SPEC-WATCH opened); deadline 2026-08-04 **partly met** — the changelog landed, these did not |
| **The sheet is now WRITTEN — 5 questions, ready to send: `build/report-suite/PO-Questions-Chris-ReportSuite-2026-07-31.md`/`.xlsx`.** Q1 his own Sales-By-Rep description contradicts itself on the download columns · Q2 will the seven outstanding description corrections land (incl. the WIP identifier) · Q3 where the location column sits in the two shorter Summary downloads (written down nowhere) · Q4 "the same logo treatment" — three reports describe three different rules · ~~Q5 does "normal reports access" mean ONE reports permission or the existing per-area ones~~ — **Q5 is now ANSWERED = A (2026-08-03): "Collapse all report access into a single Reports permission." DO NOT RE-ASK IT.** It came back as Q2 of the separate SBC permissions Google Doc; the surviving follow-on (how far the collapse reaches) is its own row below. **Q1, Q2, Q3 and Q4 of this sheet remain UNANSWERED.** **Two of the three items previously listed here were WITHDRAWN as already answered** — the "Rep is active?" header and the Assignments file name are both covered by his 2026-07-31 Q5 = A ("slang, let's do representative everywhere"), quoted in the sheet's QA-only withdrawn appendix. | QUESTION | **Chris Ward** — **you only need to send it** | Q1/Q4 are live self-contradictions in his own descriptions that will keep confusing every reader; Q3 leaves 5 export cases hedged; Q5 leaves the two parts reports' permission wording approximate. Nothing is guessed (Rule 32). | 2026-07-31 |
| **PO ACTION LIST written 2026-07-31 (companion to the sheet above): `build/report-suite/What-We-Need-From-Chris-Ward-2026-07-31.md`/`.docx`** — the same asks as an action list rather than a questionnaire: **5 decisions** (the sheet's Q1/Q3/Q4/Q5 + one new: does the on-screen label stay "VIN" for non-vehicle assets, a concern **he raised himself** 2026-07-29 and never settled) + **7 write-it-down items** (WIP identifier · location-filter notes · "Representative" labels · export cap + message · **the Escape spec line** · nav-group/"only report"/mojibake · the TU Column Selection ticket). Reader-facing part is machine-checked jargon-free (0 case IDs, 0 anchors, 0 versions); QA-only appendix carries the item→C-id map. | QUESTION | **You to send · Chris to act** | **Decide whether to send this INSTEAD OF or ALONGSIDE the question sheet** — they overlap by design and sending both risks him answering only one. Suggested: send the action list, attach the sheet for the four A/B answers. | 2026-07-31 |
| ~~**The SBR staff-dialog Escape-key question.**~~ **CORRECTED 2026-07-31 — this row was WRONG: Chris ANSWERED it on 2026-07-28, verbatim "B."** (= Esc must NOT dismiss; `chris-answers-2026-07-28/answers-ingested.md` Q1), and **SBR-DEACT-04 = C30255** ([link](https://shopview.testrail.io/index.php?/cases/view/30255)) **already matches his ruling**. **DO NOT RE-ASK IT.** What genuinely remains is only his SPEC EDIT — the live SBR capture (2026-07-31) still has `S13-R8` dismissing *"on Cancel, X, or Escape"*, and SV-8599 still carries *"surface as decision"*. It is now a write-it-down item (**item 10** of `What-We-Need-From-Chris-Ward-2026-07-31`), not a question. The same stale claim also appears in `PO-Questions-Chris-ReportSuite-2026-07-31.md` ("open 4 days") and `DELIBERATE-DECISIONS.md` D6 — both were written earlier the same day and are flagged there. | OTHER TEAM (was QUESTION) | **Chris Ward** — spec text only | Nothing in our suite: the case is aligned to his ruling. The spec line will keep re-generating this confusion for every reader, and dev is still treating it as open. | Question raised 2026-07-27 · **answered 2026-07-28** · spec edit outstanding since 2026-07-28 |
| **The permission dev-change ticket — ✅ FILED 2026-07-30 as [SV-8780](https://shopview.atlassian.net/browse/SV-8780)** (Story Defect under story **SV-8598**, epic **SV-8582**, priority Medium, HTTP 201, verified by reading the issue back; duplicate search run first). Chris ruled every report is gated by the ordinary reports permission, but the build ships a dedicated `ROLE_SALES_BY_CUSTOMER_REPORT::VIEW` atom. **Rule 24 checked before filing — NOT a front-end-blocks/back-end-allows pass**, so filing was correct. **Rule 12 honesty: SPEC-DERIVED, never live-observed** (no Report Suite QA branch) — stated in the ticket body. Full draft: `build/dev-tickets-2026-07-31/DEV-TICKET-SBC-permissions.md`. **Now waiting on dev to action it.** | OTHER TEAM | **Dev** (SV-8780) — ~~**Chris Ward** (fix spec `S1-R2`)~~ **that half is CLEARED, see below** | **SBC-PERM-01 = [C30098](https://shopview.testrail.io/index.php?/cases/view/30098)** and **SBC-NAV-01 = [C30096](https://shopview.testrail.io/index.php?/cases/view/30096)** fail on purpose until the change lands; **SBC-PERM-02 = [C30099](https://shopview.testrail.io/index.php?/cases/view/30099)** probably still passes behaviourally (only its wording is affected). A tester now has a ticket to point at instead of reporting them as our bug. | 2026-07-31 |
| **UPDATE 2026-08-03 on SV-8780 — two things changed, both verified live.** (1) **Chris's `S1-R2` spec correction is DONE** — the live SBC page (Confluence 577634305, change-log row 2026-07-31) now reads *"The report is gated by ordinary reports access, not by a report-specific permission."* Checked in the spec body, **not** taken on his word. He also commented on the ticket 2026-07-31: *"Surfaced in stand-up . the answer is: Yes. Spec updated accordingly."* (2) **The ticket is now at `Ready to Fix`** (not "Open" as our draft records), updated 2026-08-02 — **and Chris has changed the accepted FIX ROUTE**: *"if it's already built, we just hide the new permissions from FE (they can exist and not do anything for now -- no wasted time)"*. So the dedicated atom does **not** have to be removed; it may stay inert and hidden. **A comment recording this is DRAFTED BUT NOT POSTED**: `build/dev-tickets-2026-07-31/SV-8780-followup-draft.md`. Analysis: `build/report-suite/chris-answers-2026-08-01/answers-ingested.md` §5. | GO-AHEAD | **You** — to release the drafted comment | **Time-sensitive:** the ticket is `Ready to Fix`, so engineering could pick it up and do the expensive atom removal Chris explicitly said he does not want. **Rule 48 five fields:** the ruling that froze it is the QA lead's own — *"DRAFT a comment but DO NOT POST IT … posting to Jira is outward-facing and needs the QA lead's explicit go-ahead"* (this pass, 2026-08-03, answering what to do about SV-8780 after Chris's ruling). It blocks only the posting; **it was the right call** — a Jira comment is visible to the whole squad and the PO — but it is one of the few held items with a real cost to waiting. **One word from you unblocks it**; the text is final. | 2026-08-03 |
| **Q2 = A — "collapse all report access into a single Reports permission" — HOW FAR DOES IT REACH?** Chris answered **A** to both questions on the SBC permissions sheet (Google Doc, verbatim export committed at `build/report-suite/chris-answers-2026-08-01/SOURCE-google-doc-export-2026-08-03.txt`). **Q1 = A confirms what we already hold** (third restatement). **Q2 = A is NEW and contrary** — it reverses the per-area model **8 live cases** assert. But his words fit **two readings** and we will not pick one (Rule 32(iii)): do the six reports simply **read one Reports permission**, or are the pre-existing per-area reports permissions (**Inventory Reports → View**, the timesheet one, the WIP one) actually **merged/retired in Custom Roles** — which is a permission-matrix change affecting consumers **outside** this project? | QUESTION | **Chris Ward** | **10 case edits + 2 retire-or-rescope decisions**, all staged and unexecuted in `build/report-suite/chris-answers-2026-08-01/staged-case-plan.md`: name edits on PV-PERM-01 = [C30325](https://shopview.testrail.io/index.php?/cases/view/30325), IV-PERM-01 = [C30603](https://shopview.testrail.io/index.php?/cases/view/30603), IV-PERM-02 = [C30604](https://shopview.testrail.io/index.php?/cases/view/30604), TU-NAV-07 = [C30398](https://shopview.testrail.io/index.php?/cases/view/30398), WIP-PERM-01 = [C30526](https://shopview.testrail.io/index.php?/cases/view/30526), WIP-PERM-02 = [C30527](https://shopview.testrail.io/index.php?/cases/view/30527); precondition-only edits on PV-NAV-01 = [C30322](https://shopview.testrail.io/index.php?/cases/view/30322), IV-NAV-01 = [C30534](https://shopview.testrail.io/index.php?/cases/view/30534), TU-NAV-01 = [C30392](https://shopview.testrail.io/index.php?/cases/view/30392), WIP-TAB-01 = [C30451](https://shopview.testrail.io/index.php?/cases/view/30451); and **PV-PERM-03 = [C30327](https://shopview.testrail.io/index.php?/cases/view/30327)** + **PV-API-04 = [C30391](https://shopview.testrail.io/index.php?/cases/view/30391)** whose premise (*"has Reports access but NOT Inventory Reports View"*) Q2=A **abolishes** — retire-or-rescope, **recommendation HOLD**. | 2026-08-03 |
| **The spec edit Q2 = A creates — Chris owes it.** If report access collapses into one permission, **PV `S1-R4` / `S1-N2`**, **IV `S1-R4`** and the TU/WIP permission prerequisites now say the opposite of his newest ruling. ~~**Honest limit (Rule 31):** we have **not** re-read those four spec pages since the 2026-07-31 capture~~ — **that limit is now CLEARED (2026-08-04):** the four pages were re-read at their live versions (PV 4 · TU 5 · WIP 6 · IV 3, all still their 2026-07-29 versions) and **all four DO still contradict him**, quoted verbatim as item 9 of the new spec-vs-build sheet below. **Recommend adding it as a new SPEC-WATCH row** (SPEC-WATCH deliberately left unedited this pass). | OTHER TEAM | **Chris Ward** | Otherwise the next reader rebuilds the per-area model — exactly how the SBC dedicated permission got specced in the first place. | 2026-08-03 |
| **NEW 2026-08-04 — the SPEC-versus-BUILD decision sheet for Chris is WRITTEN and READY TO SEND: `build/report-suite/chris-sheet-2026-08-04/Report-Suite_Spec-vs-Build_Decisions-for-Chris-Ward_2026-08-04.md`/`.xlsx`** (generator `gen_spec_vs_build_sheet.py` alongside). Built on your instruction, verbatim: *"Anything which is blocked on Chris, make a SHeet for him to confirm which behavior he would prefer to use, give him reference from specs what specs says vs what is in the build."* **10 items — 8 decisions + 1 write-down + 1 awareness-only** — each row quoting his own requirement VERBATIM beside the live 2026-08-03/04 observation on build `v3.4.1-0ed4433`: **(1)** the location chooser still shows for a one-location person on **all six** reports, against his 2026-07-31 ruling · **(2)** the extra location column is a manual switch on Work In Progress (off by default) and on Inventory Value (on by default, and it does not hide when narrowed) — two models, neither matching either spec · **(3)** the asset still leads with unit number, not the VIN chain he ruled on 2026-07-29 · **(4)** the exports say a **third** spelling, `Representative` · **(5)** four columns missing from the Sales By Representative summary export · **(6)** the shared date picker offers nine presets and no Custom (one of our cases is **unrunnable** today) · **(7)** the Technician Utilization export menu has four differently-worded items, not three · **(8)** the Inventory Value CSV carries an `As of` line no requirement asks for · **(9)** the four stale per-report permission descriptions (**no decision — already ruled**) · **(10)** Print is gone from the build everywhere (**awareness only**). **8 candidate items were WITHDRAWN as already answered**, each with his answering words quoted in the QA-only appendix; **9 more are listed as not-asked with reasons**, including everything already on the 3 August sheet — **the two sheets deliberately do not overlap**. Reader-facing text is machine-gated: 0 case IDs, 0 spec anchors, 0 version numbers, 0 build markers, 0 ticket keys, 0 jargon outside a direct quote; every C-id on the QA-only tab is verified against `testrail-id-map.csv` and the generator **aborts** on a mismatch (proven by a deliberate-break test). | QUESTION | **You to send · Chris to answer** | **Send it WITH the 3 August sheet** — that one asks him to fix wording, this one asks him to choose between the wording and the product; sending only one leaves half the suite frozen. While the 8 decisions are open, **28 named cases stay exactly as they are** — 26 whose wording turns on his answer, plus 2 named only as cross-references (SBC-LOC-01 = [C30109](https://shopview.testrail.io/index.php?/cases/view/30109) and SBC-LBL-01 = [C30134](https://shopview.testrail.io/index.php?/cases/view/30134)) — and cannot be corrected in either direction (they are HELD, not overlooked — editing them would assert behaviour no written source supports). **Honest caveat (Standing Rule 49):** the QA branch is **NOT FINAL** by engineering's own statement, so every observation on the sheet is **PROVISIONAL** and queued in `viu-2026-08-03/RECHECK-QUEUE.md` — the sheet says so on its face. | 2026-08-04 |
| **Which SBC features "dropped almost right before the squad assembled"?** Chris says *"the requirements should have dropped with the additional features dropping, I own that"* — i.e. his own spec may still describe cut features. **We checked rather than asked blind, and the honest finding is ZERO:** the live change log records four descoping rounds (customer comparison + "Show only comparing" and side-by-side asset comparison 2026-07-12 · global-search narrowing 2026-07-15 · "All Time" 2026-07-16 · Print 2026-07-29); Stories 5 and 19 are **clean placeholders with no requirements**; and **no case of ours tests a descoped feature** — the one that did (SBC-EXP-13, Print) was retired 2026-07-28, and SBC-DATE-01 correctly asserts *"There is no 'All Time' option."* **No retire list has been manufactured to fill the gap** (Rule 12). Evidence: `chris-answers-2026-08-01/answers-ingested.md` §3. | QUESTION | **Chris Ward** | **Nothing today.** It stays open only so a descoped requirement cannot be sitting somewhere we have not looked. If he means the four above, he can simply be told it is already closed. **Note for later:** he says those features *"SHOULD be gated behind an additional permission set"* — so if any returns, the dedicated permission becomes justified again. | 2026-08-03 |
| **Authorisation for the staged case plan from Chris's 2026-08-01-round answers.** `build/report-suite/chris-answers-2026-08-01/staged-case-plan.md` — **16 cases touched: 13 `update_case` + 1 `add_case` + 2 pending-decision. NOTHING EXECUTED.** Bucket A (3 SBC cases — C30096/C30098/C30099) is **metadata + tester-note only, and has NO dependency on Chris**, so it can be released on its own: two `refs` fields still claim the spec contradicts the case when it now agrees, and the tester note omits SV-8780 and the new "the permission should be hidden from the screen" fact. Bucket B is **1 new case** (SBC-PERM-03, no C-id yet) covering the only testable half of the FE-hide ruling. | GO-AHEAD | **You** | **Rule 48 five fields:** frozen by the QA lead's own instruction — *"EXECUTE NOTHING: no TestRail writes, no case-source edits, no Jira posts."* (this pass, 2026-08-03, answering how far to take Chris's answers). It blocks all 16 rows of the plan. **It was right, and more so than usual** — the Q2 blast radius is genuinely unresolved, so executing now would bake in a reading Chris has not confirmed. **What unblocks it:** your go-ahead on bucket A today; buckets C/D/E after Chris answers the Q2 reach question. | 2026-08-03 |
| **⚠️ RUN-359 CONSEQUENCE of the one `add_case` (Rules 34/47).** Verified live 2026-08-03: run **359** *"Reports Suite - Nebojsa/Viktoria (VIU Pending)"* has **`include_all = false`**, **474 tests**, **0 results recorded** (474 Untested). A fixed-selection run **never** auto-picks up new cases, so SBC-PERM-03 would be invisible to Nebojsa and Viktoria — the same false coverage gap that produced Rule 34 on Filters run 352. The sync must be a **UNION** (`sorted(set(current) \| set(new))`, 474 → **475**), with `get_tests` + `get_results_for_run` snapshotted **before** the write. **A partial `case_ids` list DELETES the omitted tests AND their results.** | GO-AHEAD | **You** — a **separate** authorisation from the case write (run 359 belongs to other testers, Rule 6) | The new case's visibility to the actual testers. | 2026-08-03 |
| **A stale C-id in a SENT question sheet — our error, flagged not silently fixed.** `build/report-suite/PO-Questions-Chris-ReportSuite-2026-07-31.md`, QA Internal Mapping row for Question 5, lists **"PV-API-04 (C30388)"**. **Wrong.** Verified live + against `testrail-id-map.csv` rows 262/265: **PV-API-04 = [C30391](https://shopview.testrail.io/index.php?/cases/view/30391)** (the permission case); **C30388 = PV-API-01**, *"The report is server-paginated"* — nothing to do with permissions. Anyone acting on that row would have edited the wrong case. The row is **left unedited** (it is a sent document; correcting it is a separate authorised change). | GO-AHEAD | **You** | Nothing today, but the wrong id is in a document that has been sent. | 2026-08-03 |
| **The TU spec's new Story 10 (Column Selection) has no Jira ticket.** | OTHER TEAM | **Chris Ward / dev** | **TU-COL-01 = C38859** and **TU-LOC-06 = C38915** cannot cite a story key — Rule 20 traceability is incomplete for them. | 2026-07-31 |
| ~~**Go-ahead to push 2 new cases** (`PV-PREC-01` + `PV-PREC-02`, the SV-8589 QuickBooks / part-of-a-unit precision gap).~~ **CLEARED 2026-07-31 — this row was STALE: both cases are LIVE in TestRail** — **PV-PREC-01 = [C38924](https://shopview.testrail.io/index.php?/cases/view/38924)** and **PV-PREC-02 = [C38925](https://shopview.testrail.io/index.php?/cases/view/38925)**, verified in `build/report-suite/testrail-id-map.csv` and cited as covering cases by `coverage-rederivation-2026-07-31/COVERAGE-REDERIVATION.md` §7. Run **359** was verified at **474/474** tests in the same pass, so the Rule-34 resync is a no-op. **Do not chase this.** | GO-AHEAD | — **closed** | Nothing. Kept visible rather than deleted so the ask is provably discharged. | Raised 2026-07-31 · **cleared 2026-07-31** |
| ~~**Go-ahead for a title-trim pass** (288 of 474 titles over 80 characters).~~ **CLEARED 2026-07-31 — this row was STALE.** Re-measured from the live case bodies on 2026-07-31: **0 of 474 titles exceed 80 characters** (longest is exactly 80). The trim was completed in the closing authenticity pass earlier the same day; the row had not been retired. **Do not chase this.** | GO-AHEAD | — **closed** | Nothing. Kept visible rather than deleted so the ask is provably discharged, not silently dropped. | Raised 2026-07-28 · **cleared 2026-07-31** |
| ~~**Designs — confirm none exist.**~~ **CLEARED 2026-07-31 — already answered by Chris on 2026-07-28**, verbatim: *"B -- currently the best is my kickoff video that's pinned in the chat … I'm going to film a much more condensed click-through tonight"* (`chris-answers-2026-07-28/answers-ingested.md` Q3) — i.e. **no pictures or mock-ups exist; the two videos ARE the visual reference**, and we have both (kickoff + the companion delivered 2026-07-30). **Do not re-ask.** Confirmed absent independently: 0 attachments on the epic and all 97 stories, no Figma. | SOURCE | — **closed** | Nothing. Visual detail the videos do not pin stays "confirm in the build", which the QA-branch row above covers. | Raised 2026-07-22 · **answered 2026-07-28 · row cleared 2026-07-31** |
| **Suite-wide logo inconsistency** found by our own sweep: TU says the bundled ShopView default always, SBC has a three-step chain ending in *no logo*, PV has no logo requirement at all — yet Chris promised "same logo treatment all reports". | OTHER TEAM | **Chris Ward** | Flagged, no case changed. Export-header cases across three reports cannot all be right. | 2026-07-31 |
| **5 foreign cases sit in our Report Suite folder — author Vladimir Tomovic** (C38919–C38923, created 2026-07-30, all **Automated**, all with **no References**), confirmed automation coverage. Live folder total is **479 = ours 474 + his 5**; they are in **no run**. **YOUR RULING 2026-07-31: do NOT message Vladimir** — no note, no question. The file is retained marked **NOT SENT** (`build/qa-team-responses-2026-07-31/Note-to-Vladimir-automation-overlap.md`). **What his cases established, for the record:** his **C38923 was CORRECT** and exposed a real defect on OUR side (see the next row); his **C38922** raises a build-conformance question (the row after that). Overlap read otherwise unchanged: C38920 duplicates **PV-FILT-14 = C38914**; C38922 duplicates **WIP-EXP-02 = C30511** + **WIP-EXP-07 = C30516**; C38919 bundles **TU-COL-01 = C38859** + **TU-EXP-04 = C30437**; C38921 is genuine new coverage. | DECISION (settled) | — **closed by your ruling** | **Nothing outstanding on Vlad.** His cases stay untouched forever (Standing Rule 38). Listed here so the ask is visibly discharged, not forgotten. | Raised 2026-07-31 · **ruled 2026-07-31** |
| **ONE SPEC CORRECTION FROM CHRIS — SBR v15 contradicts itself on the CSV headers.** His own new **`S14-R20`** (2026-07-29) says the Location column is *"included in all four exports in the same position it occupies on screen"*, but **`S14-R15`** (Summary CSV) and **`S14-R16`** (Expanded CSV) still enumerate their header lists *"in order"* **without it**. **OUR SIDE IS NOW DONE (2026-07-31, user-authorized):** the export cases follow the newer `S14-R20` — SBR-EXP-10 = [C30285](https://shopview.testrail.io/index.php?/cases/view/30285), SBR-EXP-11 = [C30286](https://shopview.testrail.io/index.php?/cases/view/30286), SBR-EXP-03 = [C30278](https://shopview.testrail.io/index.php?/cases/view/30278), SBR-EXP-04 = [C30279](https://shopview.testrail.io/index.php?/cases/view/30279) pushed and verified. It is now **Q1 of the 2026-07-31 sheet**. | OTHER TEAM | **Chris Ward** (PO) | **Blocks nothing** — `S14-R20` is newer and wins (Rule 32) and our cases match it. But the contradiction sits live in the spec and will keep re-generating this confusion for every reader. | 2026-07-31 |
| ~~**ONE LIVE OBSERVATION OWED — is `Location` listed in the WIP Column Selection menu?**~~ **✅ ANSWERED LIVE 2026-08-03: YES, IT IS.** The Column Selection panel lists `Location` between `VIN` and `Advisor`, **off by default**, and it did **not** appear automatically at two-location scope. **So Vladimir's C38922 step 3 was right and our case is wrong** — and it was already contradicting two of our OWN cases, **WIP-COL-01 = [C30466](https://shopview.testrail.io/index.php?/cases/view/30466)** and **WIP-PERS-02 = [C30507](https://shopview.testrail.io/index.php?/cases/view/30507)**, which both list Location among the toggleable columns. Our July usefulness audit rated all three sensible; it took a running build to expose it. Evidence `build/report-suite/viu-2026-08-03/evidence/work-in-progress/menus.json` + `LABEL-DIFF.md` §A1. | DECISION → **GO-AHEAD needed** | **You** — approve the edit | **Edit staged, not pushed:** **WIP-COL-02 = [C30467](https://shopview.testrail.io/index.php?/cases/view/30467)** expected item 3 must be rewritten (and item 2 must add Location to the off-by-default list). **My recommendation: YES, fix it** — a tester following it today looks for a column that only appears when switched on, and would file a bug that is ours. Note **WIP-FLT-09 = [C38916](https://shopview.testrail.io/index.php?/cases/view/38916)** rests on the same three v6 spec sentences and needs the same review. | Raised 2026-07-31 · **answered 2026-08-03** |
| **NOTHING outstanding on requirement coverage** — recorded so it is not re-asked. A full **coverage re-derivation from the six CURRENT descriptions** (2026-07-31) enumerated **895 requirements** and found **888 covered · 6 genuine gaps, all closed · 0 stale or invented references**. The 6 gaps were all one thing: the *export* half of the new per-row Location column. Closed as **extensions, 0 new cases**; 33 `update_case` pushed and verified; run 359 untouched (474/474 tests, 539/539 results). The 7 requirements with no case are deliberate and each has a written reason. Evidence: `build/report-suite/coverage-rederivation-2026-07-31/`. | DECISION (settled) | — **closed** | **Nothing.** Listed so the completeness question has a citable answer if a QA challenges it. | Raised + closed 2026-07-31 |

| **NEW 2026-08-03 — GO-AHEAD needed on 9 wording fixes + 1 new case, all staged, none pushed.** The first live check found 9 tester-facing wordings that are wrong against the build (nav heading, the WIP Location toggle, the empty-download message, the "Locations:" line position, "All locations", "Qty", "Turns/Yr", and two notes about a column that cannot be downloaded), plus **SBC-API-06**, the one genuinely missing case — SBC has 5 back-end cases and none covers permissions. Full list with current-vs-proposed text: `build/report-suite/viu-2026-08-03/CHANGE-LEDGER.md`. | GO-AHEAD | **You** | **My recommendation: YES to all ten.** Three of the nine would make a tester file a bug that is ours; SBC-API-06 can land **VIU-Verified** rather than Pending because the evidence already exists. **Run-sync consequence: adding SBC-API-06 needs a UNION `update_run` on run 359** — snapshot `get_tests` + `get_results_for_run` first and send the FULL union; a partial list would delete tests **and their 539 recorded results**. | 2026-08-03 |
| **NEW 2026-08-03 — 7 questions for Chris, every one now backed by a live observation** (previously they were spec-vs-ruling arguments; now the build has taken a side). Highest risk: **the Location filter is still SHOWN to a one-location user on all six reports**, i.e. the build follows his un-updated spec text, not his own "hidden" ruling — 4 cases will fail a correctly-built version. Then: the asset identifier still leads with the unit number (his ruling owed a **third** time); the export column is headed **"Representative"**, a third spelling; four columns are missing from the SBR Summary download; the date picker offers nine choices not eleven and has no "Custom" button (which makes **[C30104](https://shopview.testrail.io/index.php?/cases/view/30104)** un-runnable as written); the TU download menu has four differently-worded items; and the one-permission paragraph is still owed on four specs. | UNANSWERED QUESTION | **Chris Ward** | Until he answers, **13 cases are knowingly ahead of the product** and a tester would file 7 bugs that are not bugs. | 2026-08-03 |
| ~~**NEW 2026-08-03 — the PDF download fails on two reports and is NOT filed.**~~ **UPDATED 2026-08-04 — you AUTHORISED filing ("For anything which is a Bug/Defect create a ticket and share with me its link"), it is now characterised on FIVE reports (PV · TU · IV · SBC · SBR), and the ticket is written — but it COULD NOT BE FILED: the Atlassian MCP is not present in the filing session** (GitHub MCP only; no `jira_*` tool, no Atlassian MCP entry, no credential in `/tmp` — searched four ways). **Re-tested before filing as you asked, and it still reproduces.** The two batches' contradictory causes are now resolved into what is established vs not: it fails far below the 10,000-row guard, the CSV of the identical scope always succeeds in ~1–2 s, and **the same 449 rows fail with 20 columns but SUCCEED with 2** — so the mechanism is **not** established and is deliberately not asserted. Paste-ready: `build/report-suite/defect-pack-2026-08-04/TICKET-1-pdf-export-500-at-scale.md`. | ACCESS / OTHER TEAM | **You or the coordinator** (enable the Atlassian MCP for this worker, or hand the 6 ticket files to the sibling worker that has it), then **dev** | **Six tickets are written and none is filed**, so the QA lead has no links to share. **I also could not run the duplicate search**, so I cannot promise none of these is already filed. | Raised 2026-08-03 · **filing authorised + blocked 2026-08-04** · **✅ CLEARED 2026-08-04 — all six FILED as [SV-8818](https://shopview.atlassian.net/browse/SV-8818)…[SV-8823](https://shopview.atlassian.net/browse/SV-8823) via a live Atlassian login (no MCP needed); duplicate search run; then brought into line with your three new instructions — all six at priority `Low`, [SV-8822](https://shopview.atlassian.net/browse/SV-8822) withdrawn as API-only, types/parents unchanged. Record: `build/report-suite/defect-pack-2026-08-04/AUDIT-LOG-2026-08-04-standing-instructions.md`** |
| **NEW 2026-08-04 — WHICH LINK TYPE MEANS "STORY DEFECT"? Your instruction cannot be carried out as worded, because no such link type exists in this Jira.** Your ruling was *"attach the tickets to the Epic as Parent but when you liunk th etickets to the stories they should be linked as their story defects. You did it correctly before."* — so the **epic parent + story link shape is confirmed and unchanged**. But `GET /rest/api/3/issueLinkType` (read live) offers only these **eight**: **Blocks** · **Cause** (`caused by`/`causes`) · **Cloners** · **Duplicate** · **Fixes** · **Polaris work item link** · **Relates** · **Split**. **None of them means "is a defect of".** `Cause` is the nearest in spirit but expresses causation, not defect-of. **I changed nothing rather than guess.** | DEFERRED DECISION | **You** — pick one of the eight, or have a proper link type created in Jira | The four story links are still **`Relates`**: [SV-8818](https://shopview.atlassian.net/browse/SV-8818)→SV-8591, [SV-8819](https://shopview.atlassian.net/browse/SV-8819)→SV-8645, [SV-8820](https://shopview.atlassian.net/browse/SV-8820)→SV-8672, [SV-8823](https://shopview.atlassian.net/browse/SV-8823)→SV-8677. Until you pick, the tickets read as generally *related* to their stories rather than as **defects of** them — a reporting nuance, nothing is broken. | 2026-08-04 |
| **NEW 2026-08-04 — FOR AWARENESS, nothing needed from you: [SV-8822](https://shopview.atlassian.net/browse/SV-8822) is WITHDRAWN and its finding is retained.** Your ruling: *"Yes Tickets related to API which you have already created can be withdrawn"*. It was **closed by workflow transition to `OBSOLETE` / resolution `Done` with a plain-language comment — NOT deleted**, priority set `Low` first. ~~**[SV-8821](https://shopview.atlassian.net/browse/SV-8821) stays OPEN** because that failure also occurs through the product's own screen, so it is user-facing despite its technical evidence.~~ **⚠️ CORRECTED 2026-08-04 — see the new row below; that claim was wrong and SV-8821 is API-RELATED.** Classification of all six: `build/report-suite/defect-pack-2026-08-04/API-SPLIT.md`. | DEFERRED DECISION | **Nobody — closed** | Nothing blocked. Noted so the finding is not re-filed: the customer-save write-up stays in the pack and **must not be raised as a ticket again without asking you** (Standing Rule 51). | 2026-08-04 |
| **NEW 2026-08-04 — ONE DECISION FOR YOU: should the back-end-only half of [SV-8821](https://shopview.atlassian.net/browse/SV-8821) be raised at all?** You could not reproduce it and ruled *"This is not reproducible with the canned line I used … You should always name the canned line you used … unblock yourself by using different canned lines."* **You were right, and our report was wrong.** Re-tested on the same build `v3.4.1-0ed4433` (marker byte-identical at start and end) across **11 canned lines covering every pricing shape**: the canned line makes **no difference**. The real condition is that the **work order has no contact person** — and in that state the product's **Finance tab is disabled** with its own message *"Please select a contact for the asset"* and **no Create Invoice button exists**, so **no user and no manual tester can reach it**; with a contact set, pressing Create Invoice returns **201**. The only residual is that the back end answers a generic **500** instead of a clear validation message, reachable **only** by calling the endpoint directly — which makes it **API-RELATED under Standing Rule 51**, so nothing new has been filed. **SV-8821 was already `OBSOLETE`/`Done` when this pass started (closed 2026-08-04T01:14:57−0500, not by us) and was left exactly that way** — only its description was corrected and a comment added, byte-verified with status/resolution/priority/parent/links unchanged. Evidence + the 22-attempt table: `build/report-suite/defect-pack-2026-08-04/repro-sv8821/`. | GO-AHEAD | **you** | **Nothing is blocked** and no test case is affected. Two things need a word from you: (a) confirm **leave SV-8821 closed** — my recommendation, since there is no user-facing fault; and (b) say whether you want the back-end-only item raised as its own small ticket (*"return a clear message instead of a server error when a work order has no contact"*). Without (b) it stays a recorded finding only. | 2026-08-04 |
| **NEW 2026-08-04 — GO-AHEAD to merge 9 reclassifications into the master ledger.** The SBC/SBR batch labelled 9 cases **EXTERNAL-DEPENDENCY** when the actual blocker is the **`invoices/create` HTTP 500 inside this same branch** — a defect, not a third-party dependency. Corrected list + arithmetic: `build/report-suite/defect-pack-2026-08-04/RECLASSIFIED.md`. Corrected totals **326 PASS / 107 DEVIATION / 13 NOT-BUILT / 20 EXTERNAL / 9 BLOCKED-BY-DEFECT = 475**. The 9 are SBR-API-06 = [C30321](https://shopview.testrail.io/index.php?/cases/view/30321) and SBR-DEACT-02…09 = [C30253](https://shopview.testrail.io/index.php?/cases/view/30253)–[C30260](https://shopview.testrail.io/index.php?/cases/view/30260). | GO-AHEAD | **You** — and say **which worker** edits `batch-sbc-sbr/verdicts.csv` + `VERDICTS.md`, since another pass owns them and I deliberately did not touch them | **Two different totals now exist in the repo** (the batch files say 29 EXTERNAL; the corrected list says 20 + 9) so a report could quote the wrong one. The label change matters: "external dependency" reads as nobody's fault, "blocked by defect" has an owner and a fix. **Also awaiting your ruling on 3 more that may belong under the same label** — TU-ELL-04 = [C30407](https://shopview.testrail.io/index.php?/cases/view/30407), TU-ELL-05 = [C30408](https://shopview.testrail.io/index.php?/cases/view/30408) (blocked by `workplaces/delete` returning 500), and PV-PREC-02 = [C38925](https://shopview.testrail.io/index.php?/cases/view/38925) (genuine QuickBooks — should stay). | 2026-08-04 |
| **NEW 2026-08-04 — a developer-provided way to READ the nightly snapshot rows.** 12 cases assert values a nightly job stores, and **no read route into those stored rows exists** (probes 404) — so they cannot be passed or failed, only left unanswered. Either **(A)** a read-only QA route returning the stored rows for a date + location, or **(B)** a documented way to trigger the nightly capture on demand plus where to read its output. Not production, not a UI, not a product feature. Plain-language ask + the 12 C-ids: `build/report-suite/defect-pack-2026-08-04/NIGHTLY-SNAPSHOT-EXPLAINED.md`. | OTHER TEAM | **Dev** — the owners of **SV-8667** (WIP nightly capture) and **SV-8678** (IV nightly capture) | **12 cases can never be graded**: WIP-API-01…06 = [C30528](https://shopview.testrail.io/index.php?/cases/view/30528)–[C30533](https://shopview.testrail.io/index.php?/cases/view/30533) and IV-API-01…06 = [C30605](https://shopview.testrail.io/index.php?/cases/view/30605)–[C30610](https://shopview.testrail.io/index.php?/cases/view/30610). It also **blocks a live investigation**: Inventory Value reports its as-of date one day late, and we cannot check whether the WIP nightly record carries the same shift. If the answer is "deliberately not readable", the 12 need retiring or rewriting — **your call, not ours**. | 2026-08-04 |
| **NEW 2026-08-03 — authorise a full re-read of epic SV-8582 (Rule 37 Tier 2).** Six of its stories were reopened as of 2026-07-31 and nobody has read what changed. | GO-AHEAD | **You** | **My recommendation: YES, and soon.** It is a long job, which is why it is user-gated — but reopened stories change what must be tested, and we are now testing against a real build, so a stale ticket list is the one gap I cannot close myself. | 2026-08-03 |
| **NEW 2026-08-03 — the re-check obligation itself (Standing Rule 49).** **⭐ RE-RUN AND UP TO DATE 2026-08-04: the build was redeployed at 10:41:58 UTC (`v3.4.1-0ed4433` → `v3.4.1-3d03023`), the whole queue fell due, and it has been re-run.** Outcome over **all 469 cases**: **451 CONFIRMED · 4 CHANGED · 14 RESIDUAL** (`build/report-suite/recheck-2026-08-04/per-case-recheck-verdicts.csv`); row-level cells **212 CONFIRMED · 5 CHANGED**. **The 4 CHANGED are real findings, not tidy-ups:** **SV-8819 is FIXED** so PV-CALC-09 = [C30367](https://shopview.testrail.io/index.php?/cases/view/30367) and PV-CALC-16 = [C30374](https://shopview.testrail.io/index.php?/cases/view/30374) now pass and their "known issue" line was removed; and the deploy added a `"Date Range:"` line to every export which made IV-EXP-04 = [C30590](https://shopview.testrail.io/index.php?/cases/view/30590) FALSE — corrected. **Provenance re-stamped on all 469, now naming the build as well as the date** (two builds existed on 2026-08-04, so the date alone had gone ambiguous). **THE QUEUE STAYS OPEN** — engineering never withdrew "not final", and this deploy proved the point within hours. Re-read the marker with `curl -s https://sv8582.qa.shopview.com/ \| grep app-version` (**now: `v3.4.1-3d03023`**). | DECISION (standing duty) | **me**, at every session start and before/after any Report Suite work | **No Report Suite deliverable may claim VIU-complete while the queue is OPEN**, and none does. Two of the three open defects (SV-8818, SV-8820) are still reproducible and their cases still say so. | 2026-08-03 · **re-run 2026-08-04** |
| **NEW 2026-08-04 — DECISION NEEDED: the Inventory Value columns defect was NOT filed, because a duplicate exists.** You instructed *"**FILE IT** … **Search Jira for duplicates first** … **and do not file if one exists — report instead**."* **A duplicate exists and it is an exact one: [SV-8823](https://shopview.atlassian.net/browse/SV-8823)** — *"Inventory Value spreadsheet: money arrives as text, **and the file ignores the chosen columns and re-orders them**"* — whose title already covers **both** halves. Live status: **OBSOLETE / Done / Low / parent SV-8582**. **You closed it on the money half only**, on the condition *"if that still shows the amount in number and that amount is correct then its good to stay closed"* — a condition we re-verified this run and which **still holds** (0 wrong values in 55,584 cells). **The columns half was never ruled on**, and it is **re-confirmed live on the new build**: three exports asking for different columns come back **byte-identical**, SHA-256 `15a811e72ff5f52b687a49dd…`, with an invalid column name raising no error. It is **user-facing**, so Rule 51's API gate does not apply. | DECISION | **You** | Nothing is blocked today — the finding is written up either way. But the behaviour has **no open ticket**, so it can be lost. ~~**Re-open SV-8823 for the columns half, or tell me to raise a separate ticket.**~~ **✅ CLEARED 2026-08-04 — YOU RE-OPENED [SV-8823](https://shopview.atlassian.net/browse/SV-8823)** (changelog: `OBSOLETE → Blocked → Ready to Fix → Open`, resolution `Done → None`, at 09:07:33–09:07:41 −0500; live status re-read **Open / None / Low**). So the *"Known and accepted … Do not raise this as a new problem"* line on **IV-EXP-03 = [C30589](https://shopview.testrail.io/index.php?/cases/view/30589)** was false from 09:07 and is **gone**. **Four cases now carry the standard known-issue line pointing at the ticket** — [C30162](https://shopview.testrail.io/index.php?/cases/view/30162), [C30287](https://shopview.testrail.io/index.php?/cases/view/30287), [C30588](https://shopview.testrail.io/index.php?/cases/view/30588), [C30589](https://shopview.testrail.io/index.php?/cases/view/30589) — no assertion changed. Record: `build/report-suite/corrections-2026-08-04/CORRECTION-1-SV-8823.md`. **See the two NEW rows below — the behaviour is wider than the ticket's title says.** | 2026-08-04 · **cleared 2026-08-04** |
| **NEW 2026-08-04 — OTHER TEAM: [SV-8819](https://shopview.atlassian.net/browse/SV-8819) is FIXED in the build but still `Open` in Jira.** Proven fixed live this run: the `This Year` preset now uses the inclusive **216**-day window and agrees exactly with the same period picked by hand, measured over **500 rows per preset**. Its two test cases have had their "known issue" line removed on your instruction. **We did not touch the ticket** — status and priority are yours to move (Rules 6 and 53). | OTHER TEAM | **You / dev** | Nothing in the suite. But ~~**Jira and the build now disagree**, which misleads anyone reading the ticket.~~ **✅ CLEARED 2026-08-04 — the ticket has been moved to `Done` / resolution `Done`** (changelog 08:31:03–08:31:25 −0500, `Open → Ready to Fix → In Progress → Code Review → Merged to Staging → TESTING STAGE → Ready for Production → Done`). Re-read live: **Done / Done / Low / parent SV-8582**. Jira and the build now agree. **Nothing outstanding.** | 2026-08-04 · **cleared 2026-08-04** |
| **NEW 2026-08-04 — DECISION: SV-8823's title says "Inventory Value spreadsheet", but BOTH halves are wider than that.** Established live on `v3.4.1-3d03023` this run, per report, rather than assumed from the title. **Money arrives as text on FIVE of the six reports** — Inventory Value `$11,176.88`, Sales By Customer `$224.92`, Sales By Representative `$1,979.40`, Parts Velocity `$0.00`, Work In Progress `$314.92`; **only Technician Utilization writes plain numbers.** **The `columns=` instruction is ignored on THREE** — Inventory Value, Sales By Customer and Sales By Representative each return a **byte-identical file** for every column set including an invalid name, with no error; **Parts Velocity, Technician Utilization and Work In Progress honour it and reject a bad name with HTTP 400.** **Only Inventory Value has a case that fails on the columns half**, because SBC and SBR have no column picker — so the wider fault costs us no test today. Evidence: `build/report-suite/corrections-2026-08-04/evidence/columns-param-per-report.json` + `money-and-order-LIVE-2026-08-04.json`. **I did not touch the ticket.** | DECISION | **You** | Nothing today — all four affected cases point at SV-8823. It matters when someone fixes only the Inventory Value writer and believes the job is done. **Widen SV-8823's scope, or tell me to raise a companion ticket.** | 2026-08-04 |
| **NEW 2026-08-04 — DECISION: one case sits in two columns at once.** **IV-EXP-02 = [C30588](https://shopview.testrail.io/index.php?/cases/view/30588)** now carries **both** the known-issue line for SV-8823 **and** its existing *"DO NOT AUTOMATE YET: this behaviour is waiting on an answer from the product owner"* line. **(1) THE RULING, VERBATIM:** the brief said *"the cases now pointing at the re-opened SV-8823 move into the known-broken/expected-fail column"* — which reads as moving it out of the held column. **(2) WHY I DID NOT:** the held line says the behaviour awaits **Chris Ward's** answer, and **I cannot show Chris has answered** — re-opening the ticket settles that the build is wrong, not whether the specification's wording changes. Removing it would also have dropped the live DO-NOT-AUTOMATE count from **47 to 46**, and verifying that 47 was an explicit instruction in the same brief. **(3) WHAT IT AFFECTS:** one row of the readiness table reads slightly ambiguously; the case itself is correct and complete. **(4) WHAT WOULD UNBLOCK IT:** one word from you — it is a single `update_case`. | DECISION | **You** | Nothing is broken. The readiness table counts C30588 under *waiting on Chris Ward*, so it is **not** double-counted in the automatable figure. | 2026-08-04 |
| **NEW 2026-08-04 — DECIDE WHERE THE RULE-49 BUILD MARKER LIVES IN TESTRAIL. There is no Notes field on this project.** Verified read-only with `get_case_fields`: the whole field set is `refs` · `estimate` · `milestone_id` · `custom_expected` · `custom_goals` · `custom_mission` · `custom_preconds` · `custom_steps` · `custom_steps_separated` · `custom_testrail_bdd_scenario` · `custom_atmstatus` · `custom_automation_type` · `custom_ai_type` · `custom_ai_model`. All three VIU batches staged the marker "to the notes / metadata layer" and `batch-wip-iv` costed it as **149 `update_case`** — that plan assumed a field that does not exist here, which is why **475 planned TestRail writes became 0**. I refused all three alternatives rather than invent a convention: `refs` (the marker is ~250 chars, the longest live `refs` is already **245**, and `refs` splits on commas); `custom_mission`/`custom_goals` (**null on all 480 cases in the group and on every case in the workspace** — repurposing a field labelled "Mission" is a suite-wide convention change on 475 cases and every future pass); `custom_expected` (tester-facing, forbidden by Rules 9/20). **The markers ARE written** — to the local `notes` field of all 475 cases (committed to git, stripped from the import by design) and to the re-check queue. | GO-AHEAD | **You** — pick `custom_mission`, or a new TestRail custom field, or confirm local-only is enough | Rule 49 obligations 1/2/4 are fully met; obligation 3 ("stamp the provenance on the case itself") is met **locally** and is **PARTIAL on TestRail**. Someone reading a case in TestRail cannot currently tell that its wording came from a non-final build. | 2026-08-04 |
| **NEW 2026-08-04 — 45 of the 80 staged wording edits are HELD, in six named groups.** The authorised push applied **37 `update_case` + 3 `add_case`**, all byte-verified; the rest are held with a reason and an owner in `build/report-suite/viu-push-2026-08-04/MANIFEST.md` §3. Needing **YOU**: ~~the **30-case Rule-42 sweep** (§3i — the staged item offers *"either a version-pinned anchor **or** scope-conditional wording"* with no per-case text, **and the master ledger of the same pass says "No reference change is needed anywhere"** — two documents from one pass disagree)~~ **✅ SETTLED 2026-08-04 by the exhaustive coverage pass — see the dedicated row below; the 30-case backlog does not exist**, and the **2 empty-export cases** (§3f — the ledger says no file downloads, the batch says the file does download; both cannot be true). Needing **CHRIS WARD**: the **shared date-picker preset list** (§3d, 6 cases, already his open question), the **rep label** (§3e, 2 cases — three sources, three words), and **2 cases his own batch says to ask him about first** (§3g). **10 more are deliberately NOT held but deliberately NOT changed** (§3c) — the build deviates from a spec requirement, so the case stands and fails the build on purpose. | GO-AHEAD + UNANSWERED QUESTION | **You** (32 cases) · **Chris Ward** (8 cases) | Nothing is broken by the wait — every held case still tests the spec. But **3 staged edits were withdrawn as already-done** (C30285 · C30286 · C30218) and **applying them would have created contradictions**, so the held list should be re-derived, not re-applied blind. | 2026-08-04 |

| **NEW 2026-08-04 — the "30 Rule-42 cases with two contradicting source documents" is SETTLED, and it needs one ruling to close.** The exhaustive coverage pass (`build/report-suite/coverage-exhaustive-2026-08-04/RULE-42-CONTRADICTIONS.md`) established what that claim was: the "30" was a **keyword list**, and the "two contradicting documents" were **two internal QA documents of the same 2026-08-03 VIU pass** — `batch-sbc-sbr/STAGED-CHANGES.md` §3 (*"27 cases close a list with wording like 'exactly'. Each needs either a version-pinned anchor … or scope-conditional wording"*) versus `CHANGE-LEDGER.md` (*"Rule 42 compliance is good … **No reference change is needed anywhere**"*) — **not two product sources**. **Neither was substantiated:** the batch list was built by keyword over **SBC and SBR only** and **15 of its 27 entries are not enumerations at all** (*"access to exactly one location"*, *"sums exactly to that row total"*, *"restored exactly as set"*, and three cases that deliberately **refuse** to close the list); the ledger's conclusion rested on a **7-case sample**. Exhaustive result over all 478 cases: **76 keyword hits, 30 genuine closed enumerations, 30/30 version-pinned, 28/30 with a closing anchor.** Only **12** of the 30 are on the batch list; **15 of mine are in PV / TU / WIP, which nobody had ever swept for Rule 42.** | GO-AHEAD | **You** — 3 rulings | **(1) Close the 30-case remediation thread — it does not exist.** **(2) Accept the 2 anchorless cases as written** (TU-EXP-08 = [C30441](https://shopview.testrail.io/index.php?/cases/view/30441), WIP-EXP-10 = [C38918](https://shopview.testrail.io/index.php?/cases/view/38918)) — both state their own reason and pin the version; this is the *"needs the QA lead's call on the convention"* ask from that batch's §2, and my recommendation is that citing `§7` / `Story 9` **in prose with the reason stated** is the right convention when no `Sn-Rn` id exists. **(3) The REAL Rule-42 exposure is elsewhere** — see the next row. | 2026-08-04 |
| **NEW 2026-08-04 — go-ahead for a version-pin backfill: 358 of 478 cases cite the spec with NO version and NO date.** They cite it as a bare local path (`specs/sbr-sales-by-representative.md`); only 120 use the version-pinned form (`SBR spec v15 2026-07-29`). Per report: SBC 60 · SBR 86 · PV 50 · TU 43 · WIP 60 · IV 59. **Every anchor still resolves today**, so this is not a stale-anchor defect — it is the **mechanism Rule 42 depends on**: when a spec version bumps, a path-only `refs` gives nothing to re-check against. This is what let the SBR export defect survive on 2026-07-31. | GO-AHEAD | **You** | Nothing is failing now. But the next spec bump has no tripwire on 75% of the suite. **My recommendation: authorise it as a metadata-only `update_case` pass** (`refs` only, no tester-facing field touched, run untouched) — noting the 248-character `refs` limit means a few will need shortening rather than extending. | 2026-08-04 |
| **NEW 2026-08-04 — 1 CANDIDATE GAP and 1 CONTRADICTS-OURS from the outside-in check.** **(a) CANDIDATE GAP — does the SBR *Expanded* CSV carry a footer totals row?** SBR-EXP-10 = [C30285](https://shopview.testrail.io/index.php?/cases/view/30285) asserts the **Summary** CSV has **no** totals row; SBR-EXP-11 = [C30286](https://shopview.testrail.io/index.php?/cases/view/30286) says **nothing either way**; **the SBR spec is silent** (`S14-R16` has no totals-row line). It surfaced because Vladimir Tomovic's automated **C38923** has a step reading *"Inspect the Expanded footer totals row"* — an automation engineer working from the running build wrote a step for a detail our spec never wrote down. **(b) CONTRADICTS-OURS — the IV Location column.** His **C38921** asserts a *scope-conditional* column (which matches IV spec v3 `S7-R6`); our IV-LOC-06 = [C38917](https://shopview.testrail.io/index.php?/cases/view/38917) asserts a *toggle-driven* one (which matches the live build). **He wrote to the spec, we wrote to the build, and the two disagree.** Under Rule 39 both bases are on the table in `coverage-exhaustive-2026-08-04/OUTSIDE-IN.md` §6; **his source cannot be read off his case (no `refs`) and "unknown" is only acceptable after asking — he has not been asked.** | UNANSWERED QUESTION + DECISION | (a) **Chris Ward**, then **you** to authorise a case · (b) **you + Vladimir Tomovic** | (a) A tester who finds a totals row in the Expanded CSV has nothing to check it against, and one who reads the Summary rule by analogy would **fail a correct build**. (b) One of three things is true and I am not picking: the build is wrong and his case is right; the build is intended and Chris owes a spec edit; or IV and WIP were built inconsistently and it is a dev ticket. **We did not touch C38921 and will not** (Rule 38). | 2026-08-04 |
| **NEW 2026-08-04 — Chris Ward owes spec edits for 4 contradiction groups (12 requirements).** Our cases deliberately contradict their own spec text in **12 places, and in all 12 the case follows the newer authoritative source** — so what is owed is a **spec edit, not a case edit**. **Group A, single-location Location FILTER** (spec: *"still sees the filter"*; Chris 2026-07-31 Q1=A: hidden) — and **this pass found the group is SIX cases, not the four the deliberate-decisions register lists**: add **SBC-LOC-01 = [C30109](https://shopview.testrail.io/index.php?/cases/view/30109)** and **WIP-FLT-06 = [C30503](https://shopview.testrail.io/index.php?/cases/view/30503)** to SBR-LOC-04 = C30216 · PV-FILT-13 = C30340 · TU-LOC-05 = C30446 · IV-LOC-04 = C30577. **Group B, permission model** — PV `S1-R4`/`S1-N2` still say *"require the **Inventory Reports → View** permission"*; the SBC v13 correction never reached PV. **Group C, Location COLUMN auto-vs-toggle** — WIP `S7-R13`, IV `S7-R6`, IV `S3-R1`. **Group D, asset identifier** — WIP `S4-R7`/`S4-R8` still describe unit-number-first, against Chris's 2026-07-29 VIN→Unit #→plate ruling. | OTHER TEAM | **Chris Ward** (PO) | **Group A is the highest-risk item in the suite**: six cases will fail a build a developer built correctly from the spec. Until the specs are corrected, every one of the 12 is a legitimate-looking bug report waiting to be filed against us. | Groups A/B/D **2026-07-31** · Group C **2026-08-03** · the 2 extra Group-A cases found **2026-08-04** |

**In one line:** *Get us the QA branch so 474 cases stop being theoretical — release the drafted SV-8780 comment before engineering removes an atom Chris told us to leave alone — ask Chris how far his "single Reports permission" answer reaches (12 cases hang on it) — and send the action list: 4 remaining decisions and 7 description edits, led by the WIP identifier fix (due 2026-08-04). Q5 and the Escape-key item are NOT questions — both are answered.*

---

## 2. SCHEDULE — PO: Branko · Epic **SV-8685** (**28 children since 2026-08-04** — 15 stories all now `Ready for QA`, SV-8812 QA-env task **Done**, plus **12 Bug tickets SV-8826…SV-8841 raised 2026-08-04 by Mudassir Qamar**) · **165 active cases** · **LIVE VIU DONE 2026-08-04 against QA branch `sv8685`, build `v3.5-4873abe`** — 138 correct on the build · 19 broken against an open ticket · 4 not built yet · 2 held for Branko · 2 that cannot be set up on this estate · **161 of 165 ready to automate today**

| Item | Category | Who owes it | What it blocks | Outstanding since |
|---|---|---|---|---|
| **NEW 2026-08-04 — CLEARED: the VIU go-ahead and the whole VIU.** The Schedule live VIU is **DONE**: all 165 cases carry a definite verdict, every provenance line is at Rule-54 **state 2** (build date + marker `v3.5-4873abe`), 169 `update_case` operations all HTTP 200 and byte-verified, run 357 proven untouched with all 429 results present by id, and 10 defect tickets filed. Report: `build/schedule/READINESS-2026-08-04.md`. | — CLEARED | — | Nothing. | Raised 2026-08-04 · **cleared the same day** |
| **NEW 2026-08-04 — RULE ON THE TWO TICKETS THAT CONTRADICT BRANKO'S OWN RULINGS.** Another QA (Mudassir Qamar) raised **[SV-8835](https://shopview.atlassian.net/browse/SV-8835)** (the hover summary should hide the VIN when the switch is off) and **[SV-8829](https://shopview.atlassian.net/browse/SV-8829)** (the shift window should show labor/total figures). **Branko ruled the opposite on both** — 2026-07-31 Q6=A *"Vin is always visible on hover regardless of the toggle"*, and 2026-07-22 Q3 *"We do not show total \$ anywhere in the schedule."* Per Rule 33 the rulings stand and **nothing was changed on either side**; his tickets were not touched (Rule 38). | QUESTION | **You to rule · Branko to confirm** | **3 cases: SCH-TIP-01 = [C30034](https://shopview.testrail.io/index.php?/cases/view/30034) · SCH-VIEW-04 = [C30045](https://shopview.testrail.io/index.php?/cases/view/30045) · SCH-MODAL-04 = [C30011](https://shopview.testrail.io/index.php?/cases/view/30011)** all assert the build's behaviour on the strength of those rulings, and both are rated **HIGH** in the defence register because the spec text still supports the tickets. | 2026-08-04 |
| **NEW 2026-08-04 — GO-AHEAD NEEDED: author ONE new case for a real coverage gap another QA found first.** **[SV-8831](https://shopview.atlassian.net/browse/SV-8831)** — technician **Jose Young** renders as a Service/Parts row on the board and is lead tech on a work order, but a Staff search returns nothing out of 67 staff. **Reproduced live and confirmed.** Nothing in our 165 asserts that a technician row belongs to a real, findable staff record; SCH-PERM-10 = [C30083](https://shopview.testrail.io/index.php?/cases/view/30083) asserts only the *rule* (rows follow the department assignment). The case is **drafted, not authored** — authoring is a TestRail write (Rule 6). | GO-AHEAD | **You** | One genuine hole stays uncovered. It is recorded as a **MISS**, dated, in `build/schedule/viu-2026-08-04/DELIBERATE-DECISIONS.md` entry 5 — not dressed up as a deliberate choice. | 2026-08-04 |
| **NEW 2026-08-04 — API-ONLY ASK, NOT FILED (Rule 51).** A very long multi-day spread is neither capped nor confirmed: 76h 36m over 26 lines produced 7 shifts with no prompt and no limit, and nothing returns a confirm-or-refuse response. **It is invisible to a user and to a manual tester** — the spread window cannot generate a series long enough to hit the 8-week / 120-shift limit from one work order's estimate — so it is API-related and **was NOT filed even though ten non-API Schedule tickets were filed the same day**. Write-up: `build/schedule/viu-2026-08-04/API-ASK.md`. **Sharper sub-question: is that limit even a product requirement?** It appears only in the engineering plan; spec v23 does not mention it. | GO-AHEAD | **You** | **2 cases marked NOT-BUILT: SCH-SPREAD-11 = [C38863](https://shopview.testrail.io/index.php?/cases/view/38863) · SCH-API-02 = [C38873](https://shopview.testrail.io/index.php?/cases/view/38873)**. If the limit is not a requirement, our two cases should change rather than the code. | 2026-08-04 |
| **NEW 2026-08-04 (recovery) — GO-AHEAD NEEDED: an ELEVENTH ticket for a real fault we found and did not raise.** The shift window's **TIME LOGGED** bar read *"40h 19m / 40h 19m"* — **fully worked** — on a shift where **nothing had been clocked** (the work order reported 0 actual hours), and *"1h / 1h"* on another. It looks like the window is showing the estimate twice instead of time logged against it. §4.9 asks for *"Time logged vs estimate (progress)"*. **No ticket was raised** because ten had already been agreed for the pass and this was not among them — so it is now the **only broken case with no ticket and no stated reason**, which is precisely the gap the deliberate-decisions register exists to prevent (logged honestly as entry **19**). | GO-AHEAD | **You** | **1 case: SCH-MODAL-03 = [C30010](https://shopview.testrail.io/index.php?/cases/view/30010)**, which a tester will fail with nothing to point at. | 2026-08-04 |
| **NEW 2026-08-04 (recovery) — GO-AHEAD NEEDED: tidy 16 cases that show raw page markup to the tester.** Sixteen of the 165 have literal `<ol>` and `<li>` tags in their preconditions, steps and expected results. **It PREDATES this pass** — the identical 16 appear in the pre-write snapshot taken before any write today — so nothing today caused it and it is recorded as an accepted imperfection (entry **20**), not dressed up. It is readable, so nothing is blocked; it simply looks unprofessional. | GO-AHEAD | **You** | Nothing blocked. A repair is **16 `update_case` writes** and needs your word (Rule 6). Cases: C29928 · C29930 · C29931 · C29932 · C29936 · C29955 · C29969 · C29991 · C30008 · C30023 · C30034 · C30039 · C30052 · C30057 · C30071 · C30074. | 2026-08-04 |
| ~~**NEW 2026-08-04 (recovery) — CROSS-PROJECT: the FILTERS import file is corrupt from the same one-line bug.**~~ **✅ CLEARED 2026-08-05 by the Filters session.** `testrail-import/filters-v1-testrail-import.csv` had **a newline between every single character** of Preconditions, Steps and Expected Results in **all 110 rows** — **330 corrupted fields**. Cause confirmed: `joinlines()` did `"\n".join(x)` over its argument, and the 2026-08-04 re-sync from live writes those fields as **strings** where they had been **lists**, so the join iterated them letter by letter. **Fixed in `build/filters/gen_import.py`, mirroring the Schedule fix exactly**, and the generator now carries a **regression guard that aborts the run** if the shredding signature ever reappears, so it cannot ship silently again. Regenerated and verified: **0 shredded fields**, all **110 rows × 3 fields byte-match live TestRail**, import header **sha256 identical to all five peer project imports**, id-map 110/110 with 0 blank C-ids and the `refs` column preserved. Commit `a39c5a2`. | OTHER TEAM | — **closed** | **Nothing.** The Filters import is usable again. | Raised 2026-08-04 · **cleared 2026-08-05** |
| ~~**NEW 2026-08-05 — GO-AHEAD NEEDED: confirm how to word the 8 Filters cases whose tickets you dismissed.**~~ **✅ CLEARED 2026-08-05 — YOU RULED, AND IT IS DONE.** Your two decisions were applied exactly as given: the **3 SV-8844 cases** (FLT-PSRCH-10 [C38900] · 11 [C38901] · 12 [C38902]) had the known-issue line **DELETED outright** — the defect is fixed and re-verified live, so those three now simply pass; the **5 SV-8843/8847 cases** (FLT-BAR-01 [C29557] · FLT-COLL-02 [C29602] · FLT-EMPTY-01 [C29606] · FLT-EMPTY-02 [C29607] · FLT-PSRCH-09 [C38899]) carry your wording verbatim — *"Known and accepted: the product behaves this way on purpose for now. Do not raise this as a new problem."* — with assertions unchanged. **Recorded honestly in the defence register: SV-8843 was closed as "Not Reproducible Anymore" and the build contradicts that reason — our authority for the note is your ruling, not the closing reason.** | GO-AHEAD | — **closed** | **Nothing.** No case points a tester at a dismissed ticket any more. | Raised + **cleared 2026-08-05** |
| **NEW 2026-08-05 — CONFIRM ONE JUDGEMENT CALL OF MINE: I removed the known-issue line from 12 Filters cases on my own initiative.** **(1) THE RULING I APPLIED, VERBATIM:** yours, 2026-08-05 — *"SV-8844's three cases: DELETE the known-issue/accepted line entirely. The defect is fixed, so any line telling a tester to expect a failure is now false."* **(2) WHY I EXTENDED IT:** the identical situation arose on a **second, unrelated** defect. [SV-8824](https://shopview.atlassian.net/browse/SV-8824) (the dropdown shutting on every tick) is **also fixed** — proven live on all five filter buttons, and Jira independently agrees, the ticket is now **Ready for QA**. Twelve of our cases carried a line telling testers to expect a failure that no longer happens. **(3) THE CASES:** FLT-STAT-03 [C29562](https://shopview.testrail.io/index.php?/cases/view/29562) · FLT-STAT-04 [C29563](https://shopview.testrail.io/index.php?/cases/view/29563) · FLT-STAT-05 [C29564](https://shopview.testrail.io/index.php?/cases/view/29564) · FLT-CUST-03 [C29568](https://shopview.testrail.io/index.php?/cases/view/29568) · FLT-CUST-05 [C29570](https://shopview.testrail.io/index.php?/cases/view/29570) · FLT-CUST-07 [C29572](https://shopview.testrail.io/index.php?/cases/view/29572) · FLT-TECH-03 [C29577](https://shopview.testrail.io/index.php?/cases/view/29577) · FLT-TECH-05 [C29579](https://shopview.testrail.io/index.php?/cases/view/29579) · FLT-ADV-03 [C29584](https://shopview.testrail.io/index.php?/cases/view/29584) · FLT-ADV-05 [C29586](https://shopview.testrail.io/index.php?/cases/view/29586) · FLT-ASSET-05 [C29593](https://shopview.testrail.io/index.php?/cases/view/29593) · FLT-CHIP-01 [C29595](https://shopview.testrail.io/index.php?/cases/view/29595). Their assertions are **unchanged**; only the warning line went. **(4) WHY I DID NOT WAIT:** leaving a false "expect this to fail" line in place while Ahtasham is **actively executing run 352** would have him record failures for a fixed defect. **(5) WHAT WOULD SETTLE IT:** one word. If you want the line back it is 12 `update_case` operations. Recorded as entry 19 of `build/filters/viu-2026-08-04/DELIBERATE-DECISIONS.md`. | GO-AHEAD (retrospective) | **You** | Nothing is blocked — this is a confirm-or-reverse, not a wait. | **2026-08-05** |
| **NEW 2026-08-05 — TELL AHTASHAM two things about run 352, and ask him one.** **(a) FIVE of his seven failures are now FIXED** and should be re-run: FLT-STAT-03/04/05 and FLT-CUST-03/05 all failed on the dropdown defect **SV-8824**, which was real on the build he tested and is fixed on the one serving now. **(b) HE WAS RIGHT AND WE WERE WRONG on FLT-PERS-04 [C29616](https://shopview.testrail.io/index.php?/cases/view/29616)** — we had marked it PASS without ever creating the deleted-customer state; seeded properly it fails, and it is his own open ticket [SV-8832](https://shopview.atlassian.net/browse/SV-8832). The case now carries that link. **(c) THE QUESTION: what did he see on FLT-PERS-02 [C29614](https://shopview.testrail.io/index.php?/cases/view/29614)?** We could not reproduce a failure of what that case asks — filters ARE stored against the account and a brand-new browser restored both of them and filtered the list, with no "Back To My Saved Filters" button needed. Two things could make it *look* failed without breaking its stated expectations: the new defect [SV-8871](https://shopview.atlassian.net/browse/SV-8871) (a restored Customer button loses its name, which reads exactly like "my filter was forgotten"), or his own [SV-8828](https://shopview.atlassian.net/browse/SV-8828). **We changed nothing about his results and nothing about that case's assertions** (Rule 33 — judge the claim, not the claimant). | QUESTION | **Ahtasham**, via you | Nothing. But 5 of his 7 failures are now stale, so his run under-reports the feature. | **2026-08-05** |
| **NEW 2026-08-05 — FYI + a question: SV-8843 was closed *"Not Reproducible Anymore"* but it STILL reproduces.** Re-tested live on the newer build `v3.4.2-d00239b`: the filter buttons still share the tab row (button top 90px vs tab top 85px) and collapsing the bar moves the table up by **0 pixels**. Screenshot: `build/filters/ruling-2026-08-05/evidence/shots/q1-layout.png`. **We changed nothing about the ticket** (Jira is yours). **Also: SV-8844 is still Open and you retitled it to "Page Search is not working Anymore" and assigned it to Ahtasham** — the problem *we* reported there is fixed, so whatever the new title describes is a different complaint that only the person who filmed it can pin down. | QUESTION | **You** | Nothing blocked. Raised so a dismissal is not resting on a reason the build contradicts. | **2026-08-05** |
| ~~**NEW 2026-08-05 — GO-AHEAD NEEDED: re-check all 110 Filters cases against the NEW build.**~~ **✅ CLEARED 2026-08-05 — GIVEN AND EXECUTED.** All **110 of 110** rows re-checked live against **`v3.4.2-d00239b`** — **91 CONFIRMED, 19 CHANGED** — and all 110 provenance lines re-stamped (110 × `update_case`, every one HTTP 200 + byte-verified MATCH, 28 fields each; run 352 proven untouched with all 425 result records present by id). Two defects turned out FIXED (SV-8824, SV-8844), two still reproduce (SV-8843, SV-8847), one case we had passed actually FAILS (FLT-PERS-04 — the tester was right), and one NEW defect was filed (SV-8871). Readiness recounted and **every row now adds up**: `build/filters/READINESS-2026-08-05.md`. Papers: `build/filters/recheck-2026-08-05/`. | GO-AHEAD | — **closed** | **Nothing.** The suite is current with the build that is serving. **The Rule-49 queue stays OPEN only because engineering has not declared the branch final.** | Raised + **cleared 2026-08-05** |
| ~~**NEW 2026-08-05 — DECISION NEEDED: one Filters case now contradicts the specification.**~~ **✅ CLEARED 2026-08-05 — YOU RULED "follow spec v18", and the BUILD agrees with the spec.** Driven live on the Timesheet Activities report: the button opens already reading **"Date Range: This month"**, the panel offers **eleven ready-made periods plus Custom and Clear Selection**, a period applies on selection (`?range=today`), and a custom range applies **only when the second date is entered** (From 07/01/2026 alone fired no request; adding To 07/31/2026 gave `?range=custom&range=2026-07-01&range=2026-07-31`). **FLT-RPTS-23 = [C38882](https://shopview.testrail.io/index.php?/cases/view/38882)** (the id-map name; the ask called it FLT-RPTS-13) was rewritten — title, preconditions, steps, expected and `refs` — **scope-conditionally per Rule 42**, so it checks the periods the report actually offers rather than a fixed list, and its `refs` is pinned to this revision of the spec. It moved from **NOT BUILT to PASS**. | DECISION | — **closed** | **Nothing.** No case asserts the opposite of the current specification. | Raised + **cleared 2026-08-05** |
| **NEW 2026-08-04 (recovery) — FYI, no action: a pre-existing shift was left damaged on the branch and has been restored.** The Day-view drag test moved a real shift (`ebdd3e03…`, work order **S-9379**, Xiriver Apparel). Its start time was put back but **its owner and its length were not** — it sat on a different technician at **270 minutes instead of 720**. Both are now restored and **proven byte-identical to the state before the pass on all 14 fields**, with the parent series total back to 1980 minutes. | DECISION | — **closed** | Nothing. Recorded because two data restorations in one pass is a pattern: a restore is not restored until it is compared **field by field**, and that lesson is now in the readiness report for the automation engineer. | Raised **and cleared** 2026-08-04 |
| **NEW 2026-08-04 — BRANKO QUESTION: should a technician with NO working hours saved count as working weekends?** They currently do — the §4.2 general default of 7:00 AM–7:00 PM is applied to all seven days. **This single behaviour is the root cause of two of the twelve tickets**: it is why a Sunday shift raises no clash (SV-8830, which therefore does **not** reproduce as written) and why Sunday capacity reads as full as a Tuesday (SV-8839, which does). It also stops the spread skipping weekends, against §4.5. **14 of 15 technicians on this estate are in that state.** | QUESTION | **Branko** | Touches **SCH-CONF-02 = [C30024](https://shopview.testrail.io/index.php?/cases/view/30024) · SCH-CAP-01 = [C30030](https://shopview.testrail.io/index.php?/cases/view/30030) · SCH-SPREAD-07 = [C29983](https://shopview.testrail.io/index.php?/cases/view/29983)**. It is a product decision, not a code defect, so no ticket was raised for it. | 2026-08-04 |
| **NEW 2026-08-04 — ENGINEERING: declare whether QA branch `sv8685` is final.** Until they do, the Rule-49 queue `build/schedule/viu-2026-08-04/RECHECK-QUEUE.md` stays **OPEN** and all 165 verdicts are **PROVISIONAL**. Build marker `v3.5-4873abe`, identical at the start, middle and end of the pass. | OTHER TEAM | **Dev / engineering** (via you) | No suite may be called VIU-complete while the queue is open. 165 rows to re-check when the build moves. | 2026-08-04 |
| **NEW 2026-08-04 — DECISION: what to do with run 357.** It holds **165 tests and 429 result records, all ours (`created_by: 3`) and all Untested**, and Ayesha has not started it. **Nothing was written to it today** and it was proven untouched afterwards (case-id sets equal both ways, all 429 results present by id). No case was added or retired, so no union sync was needed. | DECISION | **You** | Nothing blocked. It is a reporting decision: leave it, or reset it now that 19 cases are known to fail. | 2026-08-04 |
| **The Branko sheet is written and READY TO SEND — now `PO-Questions-Branko-Schedule-2026-07-31-Round-3.md`/`.xlsx`, 7 questions. SEND ONLY ROUND 3** — it supersedes the never-sent 8-question Round 2 (`…TechPlan_2026-07-30`), so he never holds two versions. Contents: **2 write-up problems he must resolve** (closure days on a multi-day job — his write-up says both things; where closure days are *set*, which names no screen anywhere), **4 genuinely open** (meeting hours vs the OT tag and hover breakdown · department-wide meetings · all-day meetings · does hiding meetings take their hours back out of the bars), **1 request** (three sentences that contradict rulings he already gave us). **3 of Round 2's questions were WITHDRAWN 2026-07-31 — his own live v23 write-up answers them outright** (where working hours live §4.2 · split working days §4.2 · double-bookings in the counter §4.11), each quoted verbatim on the QA-only tab; they are re-routed to **dev** (next row). | QUESTION | **You to send · Branko to answer** | 6 behaviours are asserted from the write-up alone or deliberately left un-asserted. Sending Round 2 now would re-ask him three things his own document states twice over. | 2026-07-27 (first version); **Round 3 2026-07-31** |
| **NEW — 3 build-plan corrections belong to ENGINEERING, not Branko.** His live v23 write-up settles all three, so per Rule 30 the *plan* is what changes: (a) working hours live in **Edit Staff Member** + **Edit Location**, not a new Administration "Schedule Settings" page; (b) a technician's day supports **more than one** working range (`"Add hours" appends more to support split shifts`), so one row per (staff, workplace, day) is the wrong data model; (c) **double-booking IS a counted conflict** (§4.11 *"surfaces them in a toolbar pill"*, and Double-booked is the first row of that table), not a front-end-only soft warning. | OTHER TEAM | **Dev / engineering** (via you, against SV-8685) | **Nothing in our suite** — SCH-HRS-02/03/04, SCH-HRS-05/06 and SCH-CONF-01/05 already follow the write-up. But the build will ship against the plan, so these become three real defects at VIU unless corrected first. | 2026-07-31 (surfaced by the Round-3 withdrawals) |
| ~~**QA branch / environment + feature-flag state (OQ-3).**~~ **✅ THE BRANCH HALF IS CLEARED 2026-08-04 — SCHEDULE HAS A QA BRANCH, ITS FIRST EVER: `https://sv8685.qa.shopview.com`** (API host `https://sv8685api.qa.shopview.com` **inferred from the naming shape, NOT verified**). You supplied it with credentials the same day; they are stored in `/tmp/schedule-viu/cookies.json` only (`chmod 600`) — **no value is in the repo (Rule 6)**. **Not one request has been made to the branch**, per your reservation of VIU permission until Report Suite is finished. Facts recorded in `build/schedule/PROJECT-STATE.md` §0.0-QA-ENV. Context: [SV-8812](https://shopview.atlassian.net/browse/SV-8812) "Set up a dedicated QA environment for testing" (epic's 16th child) was still **Board Backlog** at last read, and all 15 stories had moved Open → `In Progress`. **STILL OPEN: the feature-flag / settings state is unknown.** | ACCESS | — **branch closed**; **flag state still You / dev** | Nothing on the access side any more. All 165 cases stay `VIU-Pending` until the VIU is authorised (see the new row below) — ~18 labels remain design-pinned, and **design-pinned is NOT verified** (Rule 12). | Raised 2026-07-21 · **branch cleared 2026-08-04** |
| **~~NEW 2026-08-04 — GO-AHEAD NEEDED: start the Schedule VIU.~~ ✅ CLEARED THE SAME DAY — you gave the word and the VIU RAN.** Kept for the record because the ruling that held it was a good one and is worth citing. **(1) THE RULING, VERBATIM:** you reserved it — VIU on the two new branches comes **after Report Suite is finished**. **(2) WHEN + WHAT IT ANSWERED:** **2026-08-04**, when you handed over both branches' credentials, instructing that they be stored and documented only — *do not authenticate, do not probe, do not start any verification work*. **(3) WAS IT RIGHT? Yes** — Report Suite had live automation engineers working off it and an OPEN re-check queue; splitting attention across three branches would have put that at risk, and the Schedule cases lost nothing by waiting a few hours. **(4) WHAT HAPPENED WHEN IT WAS LIFTED:** all **165** cases now carry a definite verdict and every provenance line moved to Rule-54 **state 2** (build date + marker `v3.5-4873abe`). | — CLEARED | — | Nothing. | Raised 2026-08-04 · **cleared the same day** |
| **The own-data write-scoping question needs an ENGINEERING answer, not a PO answer.** Branko replied *"I'm not sure if this question is for me Bilal."* — and he is right. Re-routed to dev in `tech-plan-2026-07-29/Questions-for-Branko-dev.md`. **No case was authored for it**; the adjacent case **C30082** covers only the read side. | QUESTION | **Dev / engineering** (via you) | A real backend-scoping behaviour has **no test case at all**. We will not author one against a guess. | 2026-07-31 |
| **Jira SV-8695 is stale versus spec v23.** The story still lists a modal **Reassign** action; Confluence v23 (2026-07-30) deleted it and Branko answered *"B — No button"*. Our cases are already correct (**SCH-MODAL-08 = C30015**, "Delete only"). | OTHER TEAM | **Branko / dev** (we do not edit Jira) | Nothing in our suite — but the Jira story now contradicts both the spec and our cases, so it will be cited against us. | 2026-07-31 |
| **Spec-internal contradiction X1 needs Branko.** §4.5 says shop closures are **not** skipped in V1; §12 still says closures **block** the spread step. Both sentences are live in v23. | OTHER TEAM | **Branko** | This is now spec-vs-spec, not spec-vs-us. It makes the closures confirmation question (sheet Q1) more valuable, not less. | 2026-07-31 |
| **NEW 2026-08-04 — the shop-closure contradiction now has 3 NAMED cases and a HIGH risk, and the sheet that would settle it appears NEVER SENT.** §4.5 says closures are *"not skipped in V1"*, §12 says closures *"block the spread step"* — **both live in v23**. There is **no Branko ruling at all** (question **NQ-1**), so per Rule 15 we did not pick a side silently: all three cases now carry a provenance line saying *the specification describes this point in two different ways … a product owner decision is still awaited*. | QUESTION | **Branko to answer · you to send the sheet** | **3 cases: SCH-EDGE-05 = [C30089](https://shopview.testrail.io/index.php?/cases/view/30089) (HIGH — the whole case inverts) · SCH-SPREAD-07 = [C29983](https://shopview.testrail.io/index.php?/cases/view/29983) (HIGH — one expected result flips) · SCH-SPREAD-08 = [C29984](https://shopview.testrail.io/index.php?/cases/view/29984) (MEDIUM).** **The engineering tech plan AGREES WITH §12**, i.e. against our cases — so §4.5 is the *only* support for our position. That is why this is the highest-risk group in the Schedule suite. Basis: `build/schedule/provenance-2026-08-04/PO-RULING-DEFENCE.md` group B. | Question open since 2026-07-31; **risk quantified 2026-08-04** |
| **Doc hygiene for Branko — 3 spec sentences now contradict his own rulings:** (a) live v23 **§9** still ties the tooltip VIN to the "VIN Number" toggle, contradicting §4.13 and his Q6 answer; (b) **§4.9** still says the shift pop-up lists the lines **"with labor/total figures"**, but he ruled 2026-07-22 (Q3) that no money appears there — our cases follow the ruling (**C30011** shift pop-up = no money fields; **C38874** API = no pricing in any response); (c) **§14.1** still lists a **"right-click context menu"** among the editing controls, after he ruled 2026-07-31 "there is no right click, only left click". | OTHER TEAM | **Branko** | Nothing blocked — resolved for our cases by his rulings (Rule 33). Each mismatch is a future re-review cost and will be cited against us. | (a) 2026-07-31 · (b) + (c) surfaced 2026-07-31 by the coverage re-derivation |
| **Go-ahead: the now-empty TestRail section 5406 "Week Export and Printing".** The Week Export case (C38853) was retired with your authorization; the section is empty but deliberately **not deleted**. | GO-AHEAD | **You** | Nothing blocked — an empty folder in the tree. Wants one authorized cleanup op. | 2026-07-31 |
| ~~**Go-ahead: one latent import defect.** `SCH-HRS-04` precondition 1 leaves a stray `(/02)`~~ **HALF-CLEARED 2026-08-04.** The **live case is FIXED**: the Rule-41 whole-case re-read of all 165 Schedule cases found it independently, and the authorised provenance pass rewrote precondition 1 to *"The shop has business hours set for the shop."* — **SCH-HRS-04 = [C38849](https://shopview.testrail.io/index.php?/cases/view/38849)**, `update_case` HTTP 200, byte-verified, 28 fields compared, every other field proven byte-identical. **STILL OPEN: the generator bug itself.** `gen_import.py`'s `clean()` strips the internal ID but leaves the surrounding bracket, so **a future regeneration would reintroduce it** on any case whose text carries a `(/NN)` cross-reference. | GO-AHEAD | **You** — for the `gen_import.py` `clean()` fix (the case no longer needs one) | Nothing in TestRail. The risk is **recurrence**: the local source and import are correct today only because the case text no longer contains the pattern. | Raised 2026-07-31 · **case fixed 2026-08-04** · generator still unfixed |
| **A5 — what "New Work Order" actually does** (a toast versus opening the WO window) is still unresolved. | QUESTION | **Branko** | **SCH-REAS-06 = C38855** passes either way, so impact is low — but it is an unresolved behaviour we should not forget. | 2026-07-31 |
| **A6 — migration heads-up for product** (not a question, an FYI you may want to pass on): now that events count toward capacity, roughly **9,684** migrated legacy events will raise capacity bars at cutover. Expected, not a bug. | DECISION | **Product** (informational) | Nothing. Recorded so nobody reports it as a defect on day one. | 2026-07-31 |

**In one line:** *The Schedule VIU is **DONE and now verified** — 165 of 165 cases have a definite answer and 161 are ready to automate — so the asks are: **send Branko the Round-3 sheet** (its Q1 is the shop-closure question, still the oldest item and still unsent), **rule on the two tickets that contradict his own earlier rulings**, and give me **one word each** on three small things — author the case another QA proved we were missing, raise an eleventh ticket for the time-logged fault, and let me tidy the 16 cases that show raw page markup.*

---

## 3. FILTERS — PO: Branko · Epic **SV-8785** · **110 active cases** · **LIVE VIU DONE 2026-08-04 against QA branch `sv8785`, build `v3.4.2-4f8211c`** — 60 correct on the build · 32 broken against an open ticket · 8 held for Branko · 9 not built yet · 1 needs a second login · **88 of 110 ready to automate today**

### 2026-08-04 — THE FILTERS VIU RAN. What moved, and the five Rule-48 fields on what did not

**Report for the QA lead: `build/filters/READINESS-2026-08-04.md`** (one table, plain English, ends
with its own outstanding section). Full pass: `build/filters/viu-2026-08-04/`.

**✅ CLEARED — "GO-AHEAD NEEDED: start the Filters VIU".** Given and executed 2026-08-04. All 110
cases now have a definite verdict; **zero are "partly observed" and zero are "blocked"**. 115
`update_case` operations (110 cases + 5 corrective), every one HTTP 200 and byte-verified MATCH with
28 fields compared and every unintended field proven byte-identical. **Run 352 verified untouched** —
110 tests set-equal both ways, **all 398 result records present by id** (the brief said 396;
verified, not trusted). 0 add, 0 delete, 0 section, 0 run writes. 0 foreign cases.

**✅ CLEARED — "the 19 dropdown merges await a live check of the shared-component assumption".** The
assumption is CONFIRMED: every filter dropdown across Work Orders, Parts and Reports is one of just
two shared components, `.filter-option-list-panel` (checkbox lists) or `.filter-search-list-panel`
(search + list + tags). **The merge can go ahead on your word — it is now a decision, not a
question.**

**✅ CLEARED — "the API host `sv8785api` is inferred, not verified".** It answered `quick-login` with
HTTP 200 on the first attempt. The `sv<epic>api` naming pattern is now proven on two of three
branches.

| What is missing | Category | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **NEW — Branko must answer SV-8825: do phone filters apply as you tap, or only on an Apply button?** He answered our sheet on 2026-08-04 saying **"A - no apply button"** (instant, which is what the build does), and then **hours later the same day** added a brand-new requirement **S12-R6** to his own spec saying the exact opposite — *"mobile does not filter in real time… the table updates only when the user taps an \"Apply filters\" button"* (Confluence version 15/16/17, 12:04–12:33 UTC, changelog comment *"Clarify mobile deferred apply"*). **The timing is the crux:** the QA executing run 352 raised **SV-8825** at 10:58 UTC asking exactly this, and Branko's spec edits landed 1h06m later — so S12-R6 reads as his written answer, which would put the **build** in the wrong. But the ticket is still Open with no comment, and his sheet says the reverse. **We are not choosing between two answers from the same person on the same day** (Rule 32(iii)). | QUESTION | **Branko** (via SV-8825) | **8 mobile cases** FLT-MOB-01…07 + FLT-MOB-10 ([C29621](https://shopview.testrail.io/index.php?/cases/view/29621)–[C29627](https://shopview.testrail.io/index.php?/cases/view/29627), [C29630](https://shopview.testrail.io/index.php?/cases/view/29630)) cannot be automated and cannot be given a pass or a fail. Each carries, verbatim, *"DO NOT AUTOMATE YET: this behaviour is waiting on an answer from the product owner."* Also the only **HIGH** risk left in the defence register. | **2026-08-04** |
| **NEW — GO-AHEAD or a "no": one API-only finding, written up and NOT filed.** A nonsense value for the Yes/No filter is silently ignored (`vehicleHere=banana` → HTTP 200, full unfiltered list) while a nonsense field *name* is properly rejected (HTTP 400). **No customer and no manual tester can reach it** — the screen only ever sends `1` or `0` — so under Standing Rule 51 it is never raised without your word, and a batch approval does not cover it. Arguably not a defect at all: silently ignoring an unusable value is what S11-R3/S11-N1 ask for elsewhere. Ready either way at priority Low, parent SV-8785, linked to SV-8796. Paper: `build/filters/viu-2026-08-04/API-ASK.md`. | GO-AHEAD | **You** | Nothing is held. Listed so the decision is visibly yours and provably not taken by us. | **2026-08-04** |
| **NEW — a second test login.** Proving one user's saved filters do not leak to another needs a second sign-in. We could not self-serve it: `POST /api/switch-user` returns **HTTP 400** on this branch, and a freshly created staff member cannot complete sign-up because the invitation email cannot be received here. A manual tester who already has two accounts can run it in a minute. | ACCESS | **You** | **1 case** — FLT-API-06 ([C38895](https://shopview.testrail.io/index.php?/cases/view/38895)) step 3. Steps 1, 2 and 4 were confirmed working, and the case now tells the tester to mark it **blocked, not failed**, if they have no second account. | **2026-08-04** |
| **NEW — tell us when engineering declares the `sv8785` branch FINAL.** It has not been declared final, and a per-epic QA branch of exactly this kind redeployed twice within hours on Report Suite the same day. So **all 110 verdicts are PROVISIONAL** (Standing Rule 49) and the re-check queue is **OPEN**: `build/filters/recheck-2026-08-05/RECHECK-QUEUE.md`, one row per case, **re-run in full on 2026-08-05 against `v3.4.2-d00239b`** (110/110, 91 confirmed / 19 changed — `build/filters/recheck-2026-08-05/RECHECK-QUEUE.md`), and it stays OPEN until the branch is declared final. Re-run it at every session start for Filters and immediately if the app-version marker moves. | SOURCE | **Engineering**, relayed by you | Nothing is blocked from proceeding, but **nothing may be described as VIU-complete while the queue is OPEN** — including the readiness report, which says so. | **2026-08-04** |
| **NEW — GO-AHEAD: the 19 dropdown merges.** The shared-component assumption they waited on is now confirmed live (two panel components serve every dropdown in the app). This is now purely your call. | GO-AHEAD | **You** | 19 cases stay as they are. No authenticity or coverage risk either way. | Raised 2026-07-31 · **unblocked 2026-08-04** |
| **NEW — for Branko, three spec-side corrections we are NOT acting on unilaterally.** (a) **S12-R2 points at the wrong requirement** — it says *"one exception (see S12-R5)"* but S12-R5 is page search; the exception is S12-R6. An editing slip from his own version-17 renumber. (b) **S7-R2 contradicts its own worked example** — the sentence says the chip shows *"the first value followed by a count of additional selections"*, the example shows `"Status: Estimate, In progress, Approved…"`, and **the build matches the example**; we wrote the case to the example. (c) **His 17 July answer is now contradicted by his own spec and by the shipped build** — he said the Status chip would be greyed out and pre-filled on Estimates/Completed and the design frame draws it that way, but S9-R2/S2-N1 say *hidden* and the build hides it; **we corrected six of our own cases to the spec and the build** and he should know. | OTHER TEAM | **Branko** | Nothing testable is blocked. This is the document-versus-decision gap again — the shape of gap that makes us look wrong in public when we are not. | **2026-08-04** |
| **NEW — one question for Ahtasham, not a contradiction of him.** We **could not reproduce SV-8828** (*saved filters do not auto-restore after closing the tab/window*). On the same build, in a brand-new browser context with empty localStorage and sessionStorage, `/workorders` was requested with no query string, the app rewrote the URL to `?status=invoiced&vehicleHere=1&tab=all`, the chips came back active, 16 rows were filtered and **no "Back To My Saved Filters" button appeared**. **We are not saying he is wrong** (Rule 33 — judge the claim, not the claimant): the likeliest difference is that his previous visit was URL-driven, because that button is exactly the S11-R7 shared-link control and it **does** appear for us on a URL-driven visit. **His case keeps its Failed result and we changed nothing about it.** **UPDATED 2026-08-05: re-tested on the rebuilt branch `v3.4.2-d00239b` — still not reproducible.** A second candidate explanation has appeared: the new defect [SV-8871](https://shopview.atlassian.net/browse/SV-8871) makes a restored Customer button lose its value name, which reads exactly like a forgotten filter. | QUESTION | **Ahtasham**, via you | Nothing. FLT-PERS-02 ([C29614](https://shopview.testrail.io/index.php?/cases/view/29614)) is untouched. | 2026-08-04 · **re-confirmed 2026-08-05** |

**Five new dev tickets were filed today** (all priority **Low**, all parent **SV-8785**, each linked
to its owning story): [SV-8843](https://shopview.atlassian.net/browse/SV-8843) filter bar shares the
tab row · [SV-8844](https://shopview.atlassian.net/browse/SV-8844) the page search query is saved to
the account and empties the list on a later visit ·
[SV-8845](https://shopview.atlassian.net/browse/SV-8845) phone shared links list the wrong work
orders · [SV-8846](https://shopview.atlassian.net/browse/SV-8846) no Clear Filters on a phone ·
[SV-8847](https://shopview.atlassian.net/browse/SV-8847) the empty screen offers the one action that
cannot help. **Three already existed and we did not duplicate them** — SV-8824 (12 of our cases,
independently reproduced), SV-8832 (4 cases, reproduced on the shared-link route too), SV-8828 (not
reproduced, see above).

## 3. FILTERS — PO: Branko · Epic **SV-8785** · **110 active cases, all live in TestRail, all VIU-Pending** · **Branko's sheet ANSWERED 9 of 9 on 2026-08-04**

| Item | Category | Who owes it | What it blocks | Outstanding since |
|---|---|---|---|---|
| ~~**There is no Jira epic for Filters at all.**~~ **CLEARED 2026-08-04 — THE EPIC EXISTS: [SV-8785](https://shopview.atlassian.net/browse/SV-8785) "Filters".** Our 2026-07-31 finding was **true when it was made and went stale within hours**: the epic was created **2026-07-31T07:51:51-0500 = 12:51 UTC**, *after* the 170-epic enumeration ran that morning, and Branko linked it into the spec at 13:07 / 13:10 UTC (Confluence v13 → v14, the only content change in either version). Found by the Rule-31 pre-flight of the provenance pass and **verified live** (`GET /rest/api/3/issue/SV-8785` → HTTP 200, type **Epic**, hierarchy level 1, status Open). **Tier-1 currency check (Rule 37):** child set verified two independent ways — `parent = SV-8785` → **14**, `"Epic Link" = SV-8785` → **14**, same keys, no paging remainder. The 14 stories **SV-8786…SV-8799 map 1:1, by title and in order, onto the spec's 14 stories**, so `Story n → SV-(8785+n)` deterministically. **ALL 110 cases now carry a real ticket in `refs` for the first time** (66 single-story keys · 44 the epic, marked `[epic]`, for cross-cutting or unanchored cases) — pushed and byte-verified 2026-08-04, and mirrored into a **new `refs` column** on `build/filters/testrail-id-map.csv` (110/110 populated). **Rule 20 is satisfiable for Filters for the first time.** Two stories are already **Ready for QA** (SV-8795 Filter Persistence, SV-8796 URL State) — the first sign a QA environment may be near. Evidence: `build/filters/provenance-2026-08-04/SOURCE-CURRENCY.md` §FINDING 2. | SOURCE | — **closed** | **Nothing.** This was recorded here as *"the worst authenticity gap we have"*; it is gone. **Lesson: a proven-absence finding has a shelf life — Rule 31 re-checks it, and that is exactly what caught this.** | Raised 2026-07-17 · "proven absent" 2026-07-31 · **cleared 2026-08-04** |
| ~~**NEW ASK — never sent: confirm the mobile "All Filters" sheet and its "Apply filters" button for V1.**~~ **✅ CLEARED 2026-08-04 — and this was the highest-risk item in the Filters suite.** Branko answered: **"A - no apply button"**, choosing the option that named *"the engineering plan's way"* — the model in which a **single**-filter sheet applies instantly and **only the combined "All Filters" window keeps its "Apply filters" button** (engineering plan decision **D15**, verbatim: *"Mobile "All Filters" combined bottom sheet — **IN**, with an "Apply filters" button (batch-apply…). Individual chips/sheets stay real-time."*). **So the concession we had written down was NOT called in:** the defence register conceded *"if Branko says mobile behaves exactly like desktop, these two cases are wrong and we concede them"* — he did not, and **C29622 / C29623 stand as written**. **Group C of the defence register is CLOSED at 0 cases; HIGH ×2 retired.** **TWO CORRECTIONS ON THE RECORD:** (a) this row said *"never sent"* — it **had** been sent, as **Q1 of the 2026-07-30 tech-plan sheet**, and came back **blank three times** before the fourth return carried the answer; (b) the affected cluster is **FLT-MOB-01…08 = C29621–C29628, 8 cases** — not the 7-case range this row implied. **HONEST QUALIFICATION:** the *combined-sheet* half is an **endorsement by option-selection**, not a sentence he typed; three sources agree (his selected option · tech plan D15 · design `11884:13689`), and an **optional** one-line confirmation is drafted in `build/filters/branko-answers-2026-08-04/FOLLOW-UPS-2026-08-04.md` §1. | QUESTION | — **closed** | **Nothing on sourcing.** But **9 cases still carry a provenance line saying a decision is awaited, which is now FALSE** — see the new go-ahead row. And **nothing here is live-verified**: closing this removed a *sourcing* risk, not a *verification* one. | ~~2026-08-04~~ **closed 2026-08-04** |
| ~~**QA branch / environment (OQ-3).**~~ **✅ CLEARED 2026-08-04 — FILTERS HAS A QA BRANCH, ITS FIRST EVER: `https://sv8785.qa.shopview.com`**, with the filter bar on **`/workorders`** (API host `https://sv8785api.qa.shopview.com` **inferred from the naming shape, NOT verified**). You supplied it with credentials the same day; they are stored in `/tmp/filters-viu/cookies.json` only (`chmod 600`) — **no value is in the repo (Rule 6)**. **Not one request has been made to the branch**, per your reservation of VIU permission until Report Suite is finished. Facts recorded in `build/filters/PROJECT-STATE.md` §0-QA-ENV. **Still unknown: the feature-flag / settings state.** | ACCESS | — **closed** | Nothing on the access side any more. All 110 cases stay `VIU-Pending` until the VIU is authorised (see the new row below). | Raised 2026-07-17 · **cleared 2026-08-04** |
| **NEW 2026-08-04 — GO-AHEAD NEEDED: start the Filters VIU.** Everything needed is now in place — branch, credentials, current spec v1.6, epic SV-8785, tech plan — so this is **ready to start and waiting only on your explicit word**. **(1) THE RULING, VERBATIM:** you reserved it — VIU on the two new branches comes **after Report Suite is finished**. **(2) WHEN + WHAT IT ANSWERED:** **2026-08-04**, when you handed over both branches' credentials, instructing that they be stored and documented only — *do not authenticate, do not probe, do not start any verification work*. **(3) WHAT IT BLOCKS:** all **110** active cases stay `VIU-Pending` and spec-verified only; every provenance line stays at Rule-54 **state 1 (no build date)**; and the **19-case dropdown-merge decision** below stays held, because it rests on a shared-component assumption only a live check can settle. **(4) WAS IT RIGHT? Yes** — Report Suite has live automation engineers working off it and an OPEN Rule-49 re-check queue; splitting attention across three branches would put that at risk. **(5) WHAT WOULD UNBLOCK IT:** one word from you, plus a **fresh** cookie set (`/tmp` is ephemeral and these live ~24h). | GO-AHEAD | **You** | 110 cases cannot move off `VIU-Pending`; the 19-case merge decision stays held; ~18 design-sourced labels stay unconfirmed. | 2026-08-04 |
| ~~**The 8-question Branko sheet WAS SENT and CAME BACK BLANK — he has answered nothing in it.**~~ **✅ ANSWERED 2026-08-04 — 9 OF 9.** The **fourth** pull of the same Drive link (`1fkjdt9hoYSGv2MToXUFJ_4tTMzP7a7X2`) and the **first** with anything in it: 13 290 bytes (was 11 968 / 12 299 when blank), md5 `99bf011cc462ca81bc55a7d6532e91e3`. **Proved at XML level, the mirror image of the three blank proofs:** every answer cell is now a shared-string cell **carrying a value** — `<c r="F4" s="5" t="s"><v>11</v></c>` and so on for F4…F12 — where the blank captures held `<c r="Fn" s="4"/>` with **no value element at all**. Three endpoints agree (`export?format=xlsx`, `export?format=csv`, `/gviz/tq?tqx=out:csv`, all HTTP 200). He **changed none of our own text** and **added no row**; no comments part exists; columns G–K are empty. **Answers: Q1=A · Q2=A · Q3 free text (Vendors design exists) · Q4=C · Q5=B · Q6=A · Q7=A · Q8 free text · Q9 free text.** **Row 9 was sent in the CORRECTED wording** our assessment prepared — it credits his write-up and says *"eight Parts pages"* — so he answered the right question, not the wrong one. Ingest, verbatim, with per-answer consequences: **`build/filters/branko-answers-2026-08-04/answers-ingested.md`**. | QUESTION | — **closed** | **Nothing.** 6 of the 9 picked a clean option letter; 3 are free text and are recorded as free text (nothing inferred, Rule 12). **Q8 does not answer the question it was asked** — see the Parts/Reports row. | ~~2026-07-30~~ **answered 2026-08-04** |
| **The page-by-page Parts/Reports filter list — HE ANSWERED, THE LIST STILL DOES NOT EXIST, AND THE OWNER HAS CHANGED.** ~~*Q1 of the earlier sheet came back completely BLANK*~~ — the re-asked version (row 9, in the corrected wording) **was answered on 2026-08-04**, verbatim: *"Same as before, we do not have concrete list. **If this is really necessary i suggest Engineering + PO together make a list for remaining 6 Parts pages i Reports, using same format as Work Orders do.**"* **So it is no longer an ask of Branko alone** — he has named the owners and the format, and his *"remaining 6 Parts pages"* **matches our own count exactly** (v1.6 §2 names buttons for 2 of the 8 Parts views, prefixed *"e.g."*, and names **no individual report**). **Q8 also failed to answer its own question** — asked what the six never-drawn-open buttons offer, he replied *"We do not have list of all filter items. we should have all filters we support now per each page plus we should add new ones"*: no letter picked, nothing about the six. **We are deliberately NOT re-asking** — he has now said twice that no list exists (2026-07-31 Q3, 2026-08-04 Q8) and our case already records that honestly; a third ask would be the fifth withdrawn question on this project. **Two things did improve:** his Q8 example names the **Part Sales** chips *"customer, created by and date"* on top of status — **item for item what [C38904](https://shopview.testrail.io/index.php?/cases/view/38904) expected 2 already asserts**, so one design-derived enumeration is now PO-sourced; and the parity rule behind **[C38908](https://shopview.testrail.io/index.php?/cases/view/38908)** now has **three** agreeing sources, so its defence-register risk drops MEDIUM → LOW. | SOURCE + QUESTION | **You** to arrange the Engineering + PO session · **Engineering + Branko** to produce the list | **9 active Parts/Reports cases** rest on answers + design instead of numbered requirements. **[C38909](https://shopview.testrail.io/index.php?/cases/view/38909)** (23-report presence walk) is now **the single most exposed case in the project** — 24 design-derived enumerations, no report named anywhere in the spec. **[C38904](https://shopview.testrail.io/index.php?/cases/view/38904)** stays design-derived for **5** of 8 views (was 6). Neither can get a numbered spec anchor. | **2026-07-27 — 8 days** |
| ~~12~~ ~~6~~ **ALL 85 Figma design boards now have a rendered PNG — CLEARED 2026-07-31T08:58:40Z. The Rule-35 queue is CLOSED.** You supplied a **Figma personal access token**, and the existing resumable fetcher pulled the last 6 over REST `/v1/images` in **one call with no 429** (`11985:13334` Sorting step 4 · `11829:8908` · `11829:8920` · `11884:15901` · `11842:14069` · `11842:16879`) — **79 → 85 of 85.** **The lesson for next time: ask for a token EARLY.** The Figma MCP needs no token but its per-seat call cap is low (it managed 6 then stopped); a REST token has no such cap and cleared the whole backlog at once. All six were **read, not just downloaded**, and that reading **corrected four of our own claims** — Sorting steps 1 and 2 DO carry the toolbar sort control, and `11884:15901` has **no** sort button and is **not** a duplicate of `11884:20807`; two tree claims survived (`Add Sort` really is absent on step 4; the search box's clear-x really is Filled-state-only). Papers: `build/filters/design-2026-07-31/PENDING-FIGMA-FETCH.md` (CLOSED), `BOARD-NOTES-12-2026-07-31.md` §5–§6, `RECONCILIATION-FINAL-2026-07-31.md`. | SOURCE | — **closed** | **Nothing. The Filters design source can now be called COMPLETE (85/85)** — the first time that is true. No design description rests on a layer tree any more. | Raised 2026-07-30 · **cleared 2026-07-31** |
| ~~**The searchable-field list the spec itself cannot supply.**~~ **✅ CLEARED AS A BLOCKER 2026-08-04.** `S13-R23` is still marked *"Pending — QA has no baseline"* in the PRD, and 5 client-side surfaces still have no field list — but **Branko has ruled that state acceptable for testing.** His **Q5 = "B"**, verbatim: *"B - \*Note - Have Engineering write up that list as technical documentation (**not as a blocker for tests**, but as a reference document). Tests can work with "typing narrows the list" until the list is complete."* **And our suite was already written that way:** checked across all **13** FLT-PSRCH cases — **not one asserts which fields the search matches**; every field name appears as *"for example"* guidance (Rule 42 clean). **His answer validated existing wording rather than changing it — 0 case operations.** The write-up is now owed by **engineering as reference documentation only**. | SOURCE | **engineering** (non-blocking) | **Nothing.** We refused to invent a per-page *"searching X finds Y"* case and that was the right call — the PO has now confirmed that coverage is not expected. | ~~2026-07-17~~ **closed 2026-08-04** |
| **Go-ahead HELD: the 19-case dropdown merges (MG1 / MG2 / MG5 / MG6).** They rest on our audit's own **not-live-verified** assumption that the five filter dropdowns are one shared component. | DECISION | **You** — and it genuinely needs the QA branch first | 19 cases stay as they are. Correctly held: merging on an unverified assumption could delete real coverage. | 2026-07-31 |
| ~~**Go-ahead: 39 over-80-character title trims.**~~ **CLEARED 2026-07-31 — this row was STALE.** Re-measured from `build/filters/testrail-id-map.csv` on 2026-07-31: **0 of 110 titles exceed 80 characters** (longest is exactly 80). The trim ran in the closing authenticity pass earlier the same day (`build/filters/PROJECT-STATE.md` §0 item 4: *"Titles: 37 over 80 chars → 0"*), which also covered the four worst offenders formerly named here. **All three active suites now measure 0 over-cap titles** (Filters 110, Schedule 165, Report Suite 474 — all re-measured the same day). **Do not chase this.** | GO-AHEAD | — **closed** | Nothing. Kept visible so the ask is provably discharged. | Raised 2026-07-31 · **cleared 2026-07-31** |
| **Go-ahead: 3 optional under-merge findings (MG16 / MG17 / MG18) + 2 unapplied fixes (F2, F3).** Not authorized this pass. | GO-AHEAD | **You** | Small consolidation and 2 wording/refs repairs. | 2026-07-31 |
| **PRD-alignment asks for Branko — UPDATED 2026-08-04. He has now RULED on two of these but WRITTEN DOWN neither.** (1) the PRD's *"hidden"* Status-chip prose (5 places) still contradicts his own Q4=B answer of 2026-07-17, which he committed on 2026-07-20 to fix; (2) **Story 12 still has no mobile "All Filters" sheet at all** — a full-text scan of live v1.6 on 2026-08-04 finds **`"Apply filters"` 0 times**, while `S2-R6` still reads *"(no confirm/apply button needed)"* and `S12-R2` still says mobile behaves *"identically to desktop"* — yet he ruled on 2026-08-04 that the combined sheet keeps its button; (3) **the Estimates default tab** is still absent from v1.6, yet he confirmed it on 2026-08-04 (**[C38876](https://shopview.testrail.io/index.php?/cases/view/38876)**). | OTHER TEAM | **Branko** | Nothing testable — our cases follow his rulings (Rules 32/33). **This is the document-versus-decision gap, and it is the shape of gap that makes us look wrong in public when we are not** — which is exactly why `build/filters/provenance-2026-08-04/PO-RULING-DEFENCE.md` exists. | 2026-07-20 / **2026-08-04** |
| ~~**8 staged Filters cases are FROZEN BY YOUR OWN RULING — and it was the right call.**~~ **✅ THE FREEZE'S CONDITION IS NOW MET — 2026-08-04.** **(1) THE RULING, VERBATIM:** *"Lets wait for Brankos answers."* **(2) WHEN + WHAT IT ANSWERED:** you gave it on **2026-07-31**, replying to our direct question *"what would it take to apply each staged group?"* — a deliberate deferral, not an oversight. **(3) WHAT IT BLOCKED:** the mobile Apply-filter cluster, **[C38876](https://shopview.testrail.io/index.php?/cases/view/38876)**'s default-tab assertion, and the undrafted sorting block. **(4) WHY IT WAS RIGHT — and it was, unambiguously:** applying any of them then meant **asserting behaviour no written source supported**, which would have breached Rule 42. **What has changed since is the only thing that could:** *Brankos answers arrived.* **Q1** settles the mobile cluster, **Q2** the default tab, **Q4** cancels sorting. **(5) WHAT WOULD UNBLOCK IT — ✅ NOTHING; IT IS DONE.** You gave the go-ahead on **2026-08-04** and it was executed: **12 × `update_case`**, all HTTP 200 + byte-verified MATCH, **0 run writes**, run 352 re-verified untouched (110 tests / **396** result records, every prior result present by id). Audit: `build/filters/branko-answers-2026-08-04/testrail-execution-log.md`. **The freeze was honoured to the letter throughout** — every Filters pass since made **0 add, 0 update, 0 delete, 0 run writes**, and run **352** was read-only-verified each time. | DECISION (deferred by you) | **You** — the PO half is done | **Nothing outstanding — the freeze is fully discharged.** No case tells a reader a decision is awaited any more (0 of 110). | ruling **2026-07-31**; condition met + **EXECUTED 2026-08-04** |
| ~~**The SORTING block is frozen by the same ruling — and to be exact: NO sorting cases exist.**~~ **✅ CLOSED 2026-08-04 — CANCELLED, not unfrozen.** **(1) THE RULING THAT FROZE IT, VERBATIM:** *"Lets wait for Brankos answers."* — **(2)** given **2026-07-31**. **(3) WHAT IT BLOCKED:** **nothing that existed** — a design-backed proposal for roughly 6–8 cases, **none authored, no internal IDs, no C-ids, nothing ever pushed**. **(4) WHY IT WAS RIGHT:** Branko has now answered **Q4 = "C"** = *"No - sorting is not part of this project (the design pictures are exploration only)."* **Waiting was exactly correct — had we authored the block we would now be deleting 6–8 cases from a live run.** The two signals we trusted both held up: the live PRD contains the token *sort* **once**, incidentally, in `S13-R14`, and the Figma section is still labelled *"Sorting (Work In Progress)"*. **(5) WHAT WOULD UNBLOCK IT:** nothing — **it is out of scope, so there is nothing to unblock.** No `add_case`, and therefore **no UNION `update_run` on run 352**. A sweep of all 110 cases confirmed **no case asserts sorting behaviour**; five incidental mentions are all legitimate (a sort icon observed in a toolbar, or existing sorting used as a stability probe for `S13-R14`) and need **0 edits**. | DECISION (deferred by you) | — **closed** | **A whole area of the Work Orders list has no coverage — BY PRODUCT DECISION, and that is now the permanent answer to "how many sorting cases do we have?": zero.** His ruling removes the new sorting panel from *this project*, not sorting from the *app*. | question open since 2026-07-27; **closed 2026-08-04** |
| **NEW 2026-08-04 — GO-AHEAD NEEDED: 10 `update_case` operations, nothing else.** Staged in full, with current-versus-proposed wording for every field: `build/filters/branko-answers-2026-08-04/staged-case-plan.md`. **8 mobile cases** (C29621–C29628) + **[C38876](https://shopview.testrail.io/index.php?/cases/view/38876)** + **[C38904](https://shopview.testrail.io/index.php?/cases/view/38904)**. Only **two** change a word a tester reads: C29624 (a pure reformat of a paste-corrupted body — **same four assertions, same order, same words**) and C38904 (one false note removed). The other eight are **provenance line and `refs` only**. **0 `add_case`, 0 `delete_case`, and therefore 0 run writes — run 352 needs no `update_run` at all.** | GO-AHEAD | **You** | **9 cases still end with *"a product owner decision is still awaited"*, which became FALSE on 2026-08-04** (Rule 54 — a stale provenance line is itself a finding). **[C38904](https://shopview.testrail.io/index.php?/cases/view/38904)** still carries a hedge whose premise Branko has denied — *"the developers have not been given a design for the Vendors page filters yet, so this page may not have them"* — and while it stands, **a tester finding no filters at all on Vendors would not fail the build** (Rule 45 false all-clear). | **2026-08-04** |
| **NEW 2026-08-04 — a CASE-SOURCE vs LIVE divergence found this pass, and it needs routing to whoever owns `build/filters/cases/`.** For **FLT-MOB-04 = [C29624](https://shopview.testrail.io/index.php?/cases/view/29624)**, live TestRail asserts *"There is **no 'Apply filter' button**… filters the work-order list **immediately**"* — **correct under Branko's ruling** — while `cases/cases-D-mobile-api.json` still asserts the opposite: *"The bottom button reads 'Apply filter' (singular); tapping it applies the selection and filters the list."* Live was changed by a manual TestRail edit (it carries the `data-pasted="true"` artefact) and the local source was never synced back. **Not fixed here — `cases/` was out of this pass's scope.** | OTHER | **You** to route it | **The local source feeds the import and every generated push, so a regeneration could overwrite the correct live text with the stale design version** — i.e. silently undo Branko's ruling. | **2026-08-04** (found this pass) |
| **NEW 2026-08-04 — OPTIONAL, and deliberately marked so: one confirmation line to Branko** asking, in his own words, whether the **combined** "All Filters" window keeps its Apply button. Drafted at `build/filters/branko-answers-2026-08-04/FOLLOW-UPS-2026-08-04.md` §1, with the proof it is unanswered (live v1.6: **`"Apply filters"` 0 hits**; no prior answer of his mentions the screen; today only Q1 touches it, **via the option text he selected rather than a sentence he typed**). **No new sheet** — one line on the existing thread. | QUESTION | **You** to decide whether to send | **Nothing is held for it.** Six cases assert that button and already rest on three agreeing sources (his selected option · tech plan **D15** · design `11884:13689`). This upgrades *"the option he chose named this model"* to *"he said so"* — insurance for a public challenge, not a blocker. | **2026-08-04** |

**In one line:** *Branko answered all nine questions on 2026-08-04 — **sorting is OUT of scope (Q4=C, so the ~6–8 case proposal is cancelled and we hold zero sorting cases by product decision)** and **the mobile Apply-button ruling (Q1=A) retired BOTH of the defence register's HIGH risks** — so what is left is three things from you: the **go-ahead for 10 `update_case` operations** (9 cases still say a decision is awaited when it has arrived), the **go-ahead to start the VIU** on `sv8785`, and **arranging the Engineering + PO session** Branko proposed for the page-by-page Parts/Reports list. Nothing in the suite is live-verified yet.*

---

## 4. CROSS-PROJECT — items that do not belong to one project

| Item | Category | Who owes it | What it blocks | Outstanding since |
|---|---|---|---|---|
| **"Simple Flow V2" — epic SV-8683 is Open with 7 children, against a project we marked COMPLETED.** Milos linked it to SV-7301 on 2026-07-27, the same day he closed SV-7301. **3 of the 7 are already Done** (shipped behaviour changes touching Simple Flow surfaces we have cases for: SV-8497, SV-8581, SV-8680), 1 is Ready-to-Fix (SV-8495), and 2 are net-new enhancement stories (SV-8726 rename "Total Price" → "Total Cost"; SV-8734 Bulk Approve/Decline WO lines). **Nothing was ingested or authored.** | DECISION | **QA lead / you** | If Simple Flow reopens, its 189 cases need a reconciliation pass against 7 stories. If it stays closed, 3 already-shipped changes may silently invalidate existing cases. **This needs a yes/no, not analysis.** | 2026-07-31 |
| **NOTHING outstanding on the three active runs' completeness — VERIFIED LIVE 2026-07-31 07:24 UTC, read-only.** Runs **Filters 352 / Schedule 357 / Reports Suite 359** hold **110 / 165 / 474** tests = **every one of our active cases** under groups 4110 / 4254 / 4281. **0 missing · 0 stale · 0 foreign cases in any run**; three-way set equality with each project's `testrail-id-map.csv`. Results preserved: **395 / 429 / 539** (all Untested or comment-only — no Passed/Failed/Blocked exists). The 2026-07-30 log's smaller sizes (94/165/465) were superseded by the later 2026-07-31 pushes — now confirmed by observation, not inference. **No TestRail write was made.** Report: `build/testrail-run-sync-2026-07-31/RUN-COMPLETENESS-CHECK-2026-07-31.md`; evidence `completeness-2026-07-31/live-evidence-2026-07-31.json`. **Standing risk (not a gap):** all three runs are `include_all: false` = FROZEN, so any future case push needs an authorised **UNION-only** `update_run` (Rule 34/47 — a partial list deletes tests **and results**). | DECISION (settled) | — **closed** | **Nothing.** Listed so "do the runs miss any cases, like Filters yesterday?" has a citable live answer. | Raised + closed 2026-07-31 |
| **The COMPLETED projects' runs (324 Fees & Discounts · 325 Simple Flow) are incomplete BY YOUR RULING — nothing is being asked of you, this row exists so it is never mistaken for a miss.** **(1) THE RULING, VERBATIM:** *"For now do not do anything for the completed test runs."* **(2) WHEN + WHAT IT ANSWERED:** **2026-07-31**, when we asked whether the run-sync duty (Rule 34) should extend to the finished projects' runs after we found cases missing from them. Reinforced the same day by *"ignore any test run which is not created by Bilal Muzamil"* — which you then **clarified** to require that the **three active** projects' runs must still contain every case, *"like it happened with filters yesterday"*. Both halves are recorded as **Standing Rule 47**. **(3) WHAT IT BLOCKS:** run **324** is short **25** cases (185 graded results) and run **325** is short **35** (147 graded results); they stay short. Also out of scope: the 3 Custom Roles regression guards **CR-REG-01 = [C38843](https://shopview.testrail.io/index.php?/cases/view/38843)**, **CR-REG-02 = [C38844](https://shopview.testrail.io/index.php?/cases/view/38844)**, **CR-REG-03 = [C38845](https://shopview.testrail.io/index.php?/cases/view/38845)**, which sit in **no manual run** — in line with the ruling, **not an oversight**. **(4) WHY IT WAS REASONABLE:** both runs hold **graded results** on projects you closed on 2026-07-27; adding cases would turn a *finished* run into an *incomplete* one and distort the reporting for work nobody is executing. That is a reporting decision, not a QA one — and it is yours. **Nothing has changed since.** **(5) WHAT WOULD UNBLOCK IT:** one word from you if a completed project is ever reopened — and then a **UNION-only** `update_run` (a partial `case_ids` list would delete those 332 graded results). | DECISION (settled by you) | — **closed by your ruling** | **Nothing.** Recorded so "why are runs 324/325 missing cases?" always has a citable answer that is not "we forgot". | Ruled **2026-07-31** |
| **TestRail credentials — RE-SUPPLIED 2026-07-31 and used read-only** (`/tmp/testrail/creds.json`, `chmod 600`, uncommitted; the existing helper `build/testing-tools/testrail-api.mjs` reads them from there). Basic auth accepted — no API key was needed. **Jira session cookies are still NOT supplied.** Secrets stay in **`/tmp` only, never committed**; `/tmp` is wiped per container, so they need re-supplying each session. | ACCESS | **You** (Jira cookies only) | TestRail live reads are **unblocked**. Still blocked: Jira reads (ticket status, new tickets, filing) and staging VIU (needs fresh staging cookies). | 2026-07-31 |
| **Fabian's sell-price bug ticket key.** The 3 corrective Simple Flow cases are in TestRail with a **placeholder** ref: *"Fabian 2026-07-29 sell-price concern (ticket TBD)"* — **SF-RCV-14 = C38860**, **SF-RCV-15 = C38861**, **SF-VPART-08 = C38862**. | OTHER TEAM | **Fabian / you** (file it or send the key) | 3 cases fail Rule 20 on the ticket half of their traceability until the real key replaces the placeholder. | 2026-07-29 |
| **Live VIU of those same 3 sell-price cases** — needs fresh staging cookies. | ACCESS | **You** | 3 cases are `VIU-Pending` on a project otherwise finished. | 2026-07-29 |
| **Tech plans (Rule 30) — status check, not a gap right now.** All three active projects have one and it has been reconciled (`build/*/tech-plan-2026-07-29/`, plus the 2026-07-30 Filters/Report Suite/Schedule passes). **Nothing outstanding** — listed here so the Rule-30 reminder is visibly discharged rather than forgotten. | SOURCE | — | Nothing. | — |
| **ONE DECISION: should Standing Rule 43 require EVIDENCE, and one row PER ASSERTION?** The 2026-07-31 root-cause analysis (`build/gap-rootcause-2026-07-31/WHY-VLAD-FOUND-IT-FIRST.md`) found that **Rule 43 as written is not sufficient** — a per-requirement row reading *"covered by C30277"* **passes while being wrong**, which is exactly what happened: the export surface was filed under *"NO-CHANGE (checked, provably fine — not skipped)"* because the requirement's **metadata-line** half was covered, certifying its **column** half as done. The fix is mechanical: **a "covered"/NO-CHANGE verdict must quote the requirement's text beside the covering case's text, and a requirement asserting two things gets one row per assertion.** It is **already installed as Rule 45(e)** (so the audit stage catches it) but it arguably belongs **inside Rule 43 itself**, which is the rule a spec-diff pass actually reads. **Not amended without your say-so.** | DECISION | **You** (QA lead) | Nothing is currently unguarded — Rule 45(e) covers it at audit time. But the cheapest place to catch this class of miss is **when the spec diff is processed**, and today Rule 43 would still let it through there. | 2026-07-31 |
| **NOTHING outstanding on the outside-in / foreign-case check** — recorded so it is not re-asked. Standing Rules **45** (outside-in gap hunt) and **46** (deliberate-decisions register) are installed, propagated into the audit process (as **Dimension 4**), the spec-reconciliation process, the process catalog (row 12), the pipeline explainer (steps 10-11) and the playbook (§J). The **reverse** foreign-coverage checker now exists and was run live read-only over all three active groups: Report Suite **ours 474 / live 479** (5 foreign, all **Vladimir Tomovic**), Filters **110/110** and Schedule **165/165** with **zero** foreign cases. Evidence: `build/gap-rootcause-2026-07-31/`. | DECISION (settled) | — **closed** | **Nothing.** Listed so that "how would you know if someone else found a gap first?" has a citable answer. | Raised + closed 2026-07-31 |

**In one line:** *The three runs are **verified complete live** (110 / 165 / 474 — nothing missing), so what's left is one yes/no on Simple Flow V2 (SV-8683), one on the Rule 43 amendment, and Jira cookies when you want ticket work. **Run 278 is no longer an ask — your 2026-07-31 scope ruling closed it.***

---

## 5. PAUSED / COMPLETED PROJECTS — one line each

| Project | Status | Outstanding, if it is ever resumed |
|---|---|---|
| **Global Search v2** | **POSTPONED** (2026-07-27) | **Epic key still unknown** — the 2026-07-31 sweep found no epic matching the v2 PRD; the only candidates (SV-3770, SV-1495 archived) are not claimed as the project's epic. Also OQ-3 (AI/"ask a question" scope) and no QA env. 86 cases authored, **never pushed to TestRail** (still awaiting permission). Note: the 9 retired Filters palette cases were parked here — and this project is paused, so that coverage is **parked, not running**. |
| **Custom Roles** | **ACTIVE but recurring-only** | **SV-8541 is spec-intended / pending PM.** Standing duty: re-run the permission regression **after every release**, not just on a cadence. SV-8412 ("Sales Representative \| Global Search allows catalog access") is in Code Review under SV-8406. **The run-278 item is GONE — closed 2026-07-31 by the scope ruling (Rule 47); run 278 is another author's run on another project and is ignored entirely. See §7.** |
| **Simple Flow** | **COMPLETED** (2026-07-27) | The SV-8683 V2 decision and the Fabian ticket key, both in §4. 21 Blocked-Env cases remain (QuickBooks not connected; dev-seeded core needed) and 5 questions to Milos were never answered — all frozen with the project. |
| **Fees & Discounts** | **COMPLETED** (2026-07-27) | 21 Blocked-Env cases frozen (a QuickBooks-connected company and a flag-off org were never available). Jira epic SV-7387 is at *Ready for Production*. Nothing being asked of you. |

---

## 6. HOW TO USE THIS FILE IN A REPORT

Copy the project's rows into an **"OUTSTANDING — what I need from you"** section at the END of the
report, in plain language (Rule 7), each item saying: *what is missing · who owes it · what it
blocks · since when.* Lead with the project's one-liner. **If a project genuinely has nothing
outstanding, write "Nothing outstanding" — do not drop the section.**

**If an item is blocked on the QA lead himself, carry the five Rule-48 fields too:** the ruling
**quoted verbatim** · **when** he gave it and **what question it answered** · the **named cases** it
blocks (internal ID + C-id + link) · **why the ruling was reasonable**, or what has **changed since**
· the **one thing** that would unblock it and **from whom**. Never write a bare *"awaiting your
decision"* — he must be able to re-read his own decision and see the reasoning without
reconstructing it, and he must never be left unable to tell a deliberate freeze from a forgotten
task.

Before you write the section, sweep all six categories rather than repeating this file from
memory, because items go stale fast in a multi-worker session:

1. **Missing sources** — spec/PRD stale or unshared, no epic, designs missing or a Rule-35 fetch queue OPEN, tech plan not supplied (Rule 30), a promised video/changelog not delivered.
2. **Unanswered questions** — to a PO or to dev; name the sheet + question number, who owes it, and how long it has been outstanding.
3. **Missing go-aheads** — TestRail pushes, retirements, merges, deletions, run syncs, title trims (Rule 6: nothing moves without them).
4. **Access / credentials** — staging or prod cookies, Atlassian access, Figma token, a QuickBooks-connected company, a QA branch + flag state.
5. **Deferred or HELD decisions** — anything marked HELD, PENDING or "your call".
6. **What another team owes** — a PO's spec correction, a dev fix, a missing ticket key, a stale Jira story.

---

## 7. RECENTLY CLEARED — so nothing is re-asked

Items are moved here (not deleted) when genuinely satisfied. *"We have already embarrassed
ourselves once by asking a question a source had already answered."*

| Item | Project | How it was satisfied | Cleared |
|---|---|---|---|
| **WIP tab-placement spec self-contradiction (S2-R4 / Story 3 status model vs the §3 SV-9027 line-state Key Decision)** *(register rows **RS-WIP-5** and **RS-BV-4(a)**)* | Report Suite / WIP | **✅ RESOLVED BY CHRIS WARD, verbatim (2026-08-19):** *"already tidied on the live Confluence page — Story 3 and S2-R4 now describe the line-state model (new S3-R5, S3-R6). Pull the live page; local copies are behind."* The document conflict is closed at source; our cases already follow the line-state model (Chris's 2026-08-18 answer B), so they now AGREE with the tidied live spec and the Rule-56 divergence note on those cases can be retired in the queued reconciliation pass. **⚠️ WHAT IS *NOT* CLEARED:** the LIVE page (pageId 703660034) must still be **pulled + reconciled** — local baseline is v22, behind the new S3-R5/S3-R6 — carried as new row **RS-WIP-6**. Source: `build/report-suite/chris-answers-2026-08-19/WIP-CHRIS-RULINGS-2026-08-19.md`. | **2026-08-19** |
| **WIP nightly-snapshot granularity — does WIP-API-01 (C30528) record per-line-state/per-bucket rows or per work order?** *(register row **RS-WIP-3**)* | Report Suite / WIP | **✅ ANSWERED/CONFIRMED BY CHRIS WARD, verbatim (2026-08-19):** *"one row per job per tab, keyed by work order + tab + date (S11-R1). Already built that way … Note a job writes at most two rows (its status tab, plus Estimates if it has unapproved lines), and Adjustments belong only on the status-tab row (S3-R6)."* → **[C30528](https://shopview.testrail.io/index.php?/cases/view/30528) is correct as written** (grain = one row per job per tab per date). The queued WIP reconciliation pass adds the max-two-rows + Adjustments-only-on-status-tab-row (S3-R6) detail to the WIP-API family + WIP-ADJ-07. | **2026-08-19** |
| **WIP per-line aging — does an unapproved line age from the LINE's own date rather than the whole job?** *(register row **RS-WIP-4**)* | Report Suite / WIP | **✅ FINALISED BY CHRIS WARD's 2nd message, verbatim (2026-08-19):** *"Aging is per job, and that's final. Days Open is whole days since the work order's date. On a job that produces two rows — its status tab plus Estimates — both rows show the same number. That's correct behavior, not a bug: the money splits by line state, the clock doesn't. The spec's stray line about unapproved lines ageing from the line's creation date is gone as of page v24, so nothing conflicts now."* → aging is **per job, FINAL**; **WIP-COL-07 = [C30472](https://shopview.testrail.io/index.php?/cases/view/30472)** keeps whole-job Days Open (S4-R12) + "both rows of a two-row job show the same number — correct, not a defect"; **NO per-line aging**; **the shared Days Open value must NOT be filed as a defect** (a separate Quote Age column is future SV-9372 — carried as **open row RS-WIP-7**). This supersedes the earlier "confirming intent, will come back" HOLD. Source: `build/report-suite/chris-answers-2026-08-19/WIP-CHRIS-RULINGS-2026-08-19.md` (2nd message). | **2026-08-19** |
| **Does a TECHNICAL DESIGN carry the same authority as the PRD?** *(register row **D1**, open since 2026-08-06 — the load-bearing one)* | cross-project | **✅ ANSWERED BY THE QA LEAD, verbatim:** *"Technical design is the authority but if that contradicts with specs/tickets/answer sheet/claude design/figma (because they are also the authority with the rule that the latest entry for that question wins) I would suggest to consider the specs/tickets/answer sheet/claude design/figma (with the rule that the latest entry for that question wins) as the authority for the test cases but let me know where it contradicts with the tech design."* **BOTH HALVES ARE THE ANSWER.** (a) The technical design **IS** an authority and **sources a case on its own where nothing contradicts it** — this **RELEASES ELEVEN HELD CASES** (nine of class C-3 in `build/unsourced-cases-2026-08-11/CANDIDATES.md`, plus **C29600** and **C29632**), every one of which the technical design sources alone with the other documents **silent, not contradictory**. (b) Where it **CONTRADICTS** the spec, a ticket, an answer sheet, a Claude design or Figma, **those win for the test cases**, latest-wins among them (Rule 32). (c) **And every contradiction is REPORTED TO HIM — applying the order silently is NOT compliance.** **Standing Rule 30's subordination clause is VINDICATED, not overturned** — it was our reading and is now his ruling. Recorded at **Rule 57 follow-up (ii)** (full text, superseded wording kept and dated), **Rule 30**, **Rule 33** and the deliverable conventions. **The list he asked for is delivered:** [`build/rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md`](rulings-2026-08-12/TECH-DESIGN-CONTRADICTIONS.md) — **6 live contradictions · 6 already resolved (five of which closed in the technical design's FAVOUR) · the 11 released cases with what each now needs.** **⚠️ WHAT IS *NOT* CLEARED: successor rows T1 (raise the six with engineering?), T2 (the Location-filter live re-read, on which 4 cases turn) and T3 (three recording fixes) are OPEN.** | **2026-08-12** |
| **What to do about the JWTs in git history, and should the repository be public at all?** *(register row **L2**, and the open question at the end of `build/secret-redaction-2026-08-11/REPORT.md`)* | cross-project | **✅ RULED BY THE QA LEAD, verbatim: *"Do not change anything if that is not blocking you from testing"*** — **and public repositories STAY PUBLIC, because the branches are temporary and non-sensitive.** So: **nothing rotated, no history rewritten, no repository setting changed.** The tracked tree is clean at HEAD across **11,696 files**; all **twelve** tokens are already expired and none ever granted more than *"listen to one user's notification feed"*; rewriting history would have rewritten **~491 commits** and force-pushed a branch several live sessions share, which the standing rules forbid. **⚠️ WHAT THIS DOES *NOT* RELAX — STANDING RULE 6's SECRETS BAR IS UNTOUCHED: secrets stay in `/tmp` only and are NEVER committed, and `build/testing-tools/scan_secrets.py` still runs over every staged diff. His ruling is about not disturbing what is already published and settled; it is NOT permission to publish anything new — and a public repository makes that bar more important, not less.** | **2026-08-12** |
| **IS THE QA BRANCH FINAL? — the question Standing Rule 49 has been asking since 2026-08-03** | Report Suite | **✅ ANSWERED, AND THE ANSWER IS *PER REPORT* — the first finality answer this project has ever had.** QA lead, verbatim: *"they have released just those reports which I mentioned… so the branch is final for those reports only, the remaining reports are yet to be handed of to the QA. Once all 6 reports are handded of to the QA only then we can consider the branch as final."* **FINAL: Work In Progress · Technician Utilization · Sales By Customer** — findings on these are **no longer provisional pending development**, a deviation is a **real defect in a finished feature**, and Rule-49 queue rows for these three **may close** as each case is re-checked. **NOT FINAL: Sales By Representative · Parts Velocity · Inventory Value** — unchanged, still provisional, rows stay open. **⚠️ WHAT IS *NOT* CLEARED: "final" means HANDED OFF / feature-complete, NOT "the code will never change" — the branch can still redeploy, not least to fix the very defects we are reporting, so A REDEPLOY STILL INVALIDATES THE LABELS AND THE PASS/FAIL VERDICT (Rule 60 layers 1–2) EVEN ON A FINAL REPORT.** What finality removes is a different doubt: whether a gap is an unfinished feature or a defect. Recorded in **Standing Rule 49** (full text) with a dated cross-reference at **Rule 60**, whose *"never declared final"* headline was true when written and is now true only per-report. **🔴 UPDATED 2026-08-11 — THE ANSWER IS NO LONGER PARTIAL. ALL SIX REPORTS ARE HANDED OFF AND THE BRANCH IS FINAL.** QA lead, verbatim: *"note that ALL 6 reports have been handed off now."* — satisfying the condition he set the day before. **So the "NOT FINAL: Sales By Representative · Parts Velocity · Inventory Value" line above is SUPERSEDED (kept visible and dated, Rules 31/52/53): those three became final on 2026-08-11, the other three on 2026-08-10.** **Findings on all 476 Report Suite cases are no longer provisional pending development, and Rule-49 queue rows for the whole suite may close as each case is re-checked — on the ORDINARY close condition, the bar unchanged.** **The caveat above carries forward untouched: a redeploy still invalidates the labels and the pass/fail verdict on every one of the six.** **⚠️ AND THIS RAISES THE OUTSTANDING WORK RATHER THAN LOWERING IT — see the new row RS-F1 in §1: 251 of the 476 are FINAL-BUT-NOT-BUILD-VERIFIED.** | **2026-08-10** *(completed **2026-08-11**)* |
| **Schedule: the requirement → case map — "can anyone check our coverage themselves?"** | Schedule | **✅ BUILT — the FIRST ONE SCHEDULE HAS EVER HAD:** `build/schedule/coverage-rederivation-2026-08-10/`. Against live spec **Confluence v27** (2026-08-07T15:01:20Z; the in-body *"Version: 1.0"* is the Rule-31(a) trap again): **234 requirement lines → 397 assertions**, verdicted **282 covered · 4 partial · 19 uncovered · 1 blocked · 91 not independently testable**, with **168 of 168 cases examined in BOTH directions** and **0 stale § anchors**. **Read-only — no TestRail write, no Jira write, no run write, nothing created** (Rules 6/62); every proposed change is **staged and unexecuted**. **⚠️ WHAT IS *NOT* CLEARED, stated plainly: Schedule coverage CANNOT yet be called complete** — the **19 uncovered assertions are all the new §5.3 Panel collapse** and its two cross-references — **and all 168 cases still carry a spec stamp reading "version 23"** while live is v27. **⚠️ UPDATED 2026-08-11: those 19 are NOT held by the creation hold and never were** — the hold is **Jira tickets only** (row H1), so **§5.3 Panel collapse is straightforward outstanding AUTHORING work, `add_case` included**, and the staged changes are unexecuted only for want of running the pass, not for want of permission. | **2026-08-10** *(corrected **2026-08-11**)* |
| **Filters: run 352 was out of sync with the suite (Standing Rules 34/47)** | Filters | **✅ SYNCED — 110 tests → 114, union-only.** `build/filters/run-sync-and-c38909-2026-08-10/RUN-352-SYNC.md`. **Every one of the 473 prior result records survived and was verified present BY ID** — the union law of Rule 34 observed exactly (a partial `case_ids` list would have destroyed them), with `get_tests` + `get_results_for_run` snapshotted before the write and `case_id` sets proven **equal in BOTH directions** after. Test count recorded **before → after** in the execution log as Rule 34 requires. | **2026-08-10** |
| **[C38909](https://shopview.testrail.io/index.php?/cases/view/38909) (`FLT-RPTS-01`) — a case actively misleading a tester** | Filters | **✅ RESCOPED, on his explicit authorisation, verbatim: _"Authorise fixing C38909 — it's actively misleading a tester today --> yes please"_.** One `update_case/38909`, HTTP 200, **byte-verified; nothing else written**. **What was wrong:** the case asserted working filter buttons across **nineteen** report surfaces when only **five** are unambiguously inside this epic — the other **fourteen** are, per the engineering handover, **forbidden** (*"Do not migrate"*), **deferred** (no chip type yet), **nav-orphans**, or **technically incapable** of the filter demanded. A tester would have logged a long row of Blocked results **waiting for a build that is never coming**, with no way to tell that from a genuine regression. **This is the concrete evidence behind Standing Rule 57's 2026-08-10 source-list widening:** the handover `.md` file is what exposed it. Working: `build/filters/run-sync-and-c38909-2026-08-10/C38909-REPAIR.md`. | **2026-08-10** |
| **A Rule-49 re-check queue for the 2026-08-06 Report Suite pass** *(register row **RS2**)* | Report Suite | **✅ THE QUEUE EXISTS AND IS OPEN:** `build/report-suite/full-viu-2026-08-06/RECHECK-QUEUE.md`, opened by a later session the same day, and it covers **both** the earlier verdicts and its own. **The Report Suite queue family is therefore FIVE files, all OPEN** (`full-viu-2026-08-06`, `full-viu-2026-08-05`, `chris-newreqs-2026-08-05`, `final-viu-2026-08-05`, `viu-2026-08-03`), not four. **The identical claim in `CLAUDE.md`'s queue-state pointer was corrected in the same pass** rather than left to contradict this row. **Note what this does NOT clear: the queue being OPEN is the normal steady state under Rule 60, so no verdict became durable** — only the *"queued nowhere"* gap is discharged. | **2026-08-06** |
| **The defect-ticket description rewrite — do all our tickets carry the required source block?** *(no open row was ever raised; recorded here so it is not re-asked)* | cross-project | **✅ COMPLETE — 92 of 92.** Working: `build/ticket-reformat-2026-08-06/` (+ `build/ticket-source-blocks-2026-08-06/` for the source-block retrofit, whose own record is **65 in scope · 64 blocks written · all 65 re-read live = 65 PASS / 0 FAIL**). **Two tickets have NO documented source and their blocks SAY SO — [SV-8821](https://shopview.atlassian.net/browse/SV-8821) and [SV-8822](https://shopview.atlassian.net/browse/SV-8822)** — and **5 more are only PARTLY supported**, all written up decision-ready in `build/ticket-source-blocks-2026-08-06/FLAGGED.md`. **Nothing had a source invented for it.** | **2026-08-06** |
| **The two PO question sheets — are they written?** *(the writing half of rows **C2** and **C4**)* | Report Suite · Schedule + Filters | **✅ BOTH ARE BUILT.** **Chris Ward — 13 items:** `build/report-suite/questions-2026-08-06/Questions-for-Chris-Ward_Report-Suite_2026-08-06.xlsx`. **Branko Cicovic — 21 items:** `build/filters/questions-2026-08-06/Questions-for-Branko-Cicovic_Filters-and-Schedule_2026-08-06.xlsx` (which **supersedes** the unsent 13-item 2026-08-05 sheet — all 13 carried forward from that folder's own generator so the wording could not drift, then 4 added, then 4 Schedule items merged in). **⚠️ WHAT IS *NOT* CLEARED: SENDING THEM, and the answers.** Both remain open as **C2** and **C4** for exactly that reason — a written sheet is not an answered one. | **2026-08-06** |
| **The Schedule specification gap — we were 2 versions behind (v23 → v25)** *(no open row was ever raised; recorded here so it is not re-asked)* | Schedule | **✅ CLOSED — baseline migrated to Confluence version 25.** `build/schedule/requirements.md` now reflects **v25** (upstream last updated **2026-08-06T09:13:51Z** by Branko Cicovic); **all 25 versions were pulled, 25 × HTTP 200**, with verbatim v23/v24/v25 storage bodies and a version-attributed delta in `build/schedule/spec-v25-2026-08-06/`. **v24 and v25 were published 39 minutes apart on the same day.** **⚠️ THE RULE-31(a) TRAP IS CONFIRMED AGAIN ON THIS PAGE: its in-body "Version" field reads `1.0` and "Last Updated" reads July 15, 2026, and has since Confluence version 1 — Branko never bumps them. Only the Confluence version number is a currency marker here.** | **2026-08-06** |
| **[SV-8821](https://shopview.atlassian.net/browse/SV-8821) — keep it with the spec-is-silent note, or withdraw it?** (the invoice-creation server error; one of the **2 tickets of 65 with NO documented source**, written up in `build/ticket-source-blocks-2026-08-06/FLAGGED.md`) | Report Suite | **DECIDED BY THE QA LEAD, verbatim: _"Marked it as AObsolete - ignore it for now."_** He made the transition **himself**. Our recorded recommendation had been **KEEP** — a server error on an ordinary user action, reachable from the product's own screen, is self-evidently wrong regardless of documentation, which is precisely why it was kept when its **API-only** sibling **SV-8822** was withdrawn (Rule 51). **His ruling supersedes our recommendation (Rule 33), and the recommendation stays on the record beside it rather than being erased.** **The ticket's source block is untouched and still says plainly that the specification is silent** — nothing was invented for it. **⚠️ STANDING CAUTION (Rule 53's corollary): he works under OUR SHARED ACCOUNT, so the changelog reads our own name. This transition is HIS TRIAGE — nobody may "restore" it, re-open it, or re-prioritise it.** We have already made that exact mistake once, producing the `High → Low → High → Low` round trip of 2026-08-04. **No test case depends on SV-8821** — nothing in the Report Suite, Schedule or Filters case source names it, so nothing needed revisiting. | **2026-08-06** |
| **Whether Branko returned the Filters tech-plan sheet — and whether he answered ANY of it** | Filters | **✅ YES, on the FOURTH pull: 9 of 9 answered.** Supersedes the three "established, negatively" entries below. Drive `1fkjdt9hoYSGv2MToXUFJ_4tTMzP7a7X2`, 13 290 bytes, md5 `99bf011cc462ca81bc55a7d6532e91e3`; three endpoints agree; every answer cell is now a shared-string cell **with a value** (`<c r="F4" s="5" t="s"><v>11</v></c>`) where the blank captures had `<c r="Fn" s="4"/>` with **no value element**. He altered none of our text and added no row. Ingest: `build/filters/branko-answers-2026-08-04/answers-ingested.md`. | **2026-08-04** |
| **Filters: is SORTING in scope?** *(supersedes the "NOT RESOLVED" entry below)* | Filters | **✅ NO — Branko Q4 = "C"**: *"No - sorting is not part of this project (the design pictures are exploration only)."* The ~6–8 case proposal is **cancelled, not deferred**; it was never authored, so **nothing is deleted and nothing is lost**, and no `update_run` on run 352 is needed. **Waiting was the right call** — had we authored it we would now be deleting cases from a live run. **The permanent answer to "how many sorting cases do we have?" is zero, by product decision.** | **2026-08-04** |
| **Filters: the mobile "Apply filters" button — the suite's highest-risk open question** | Filters | **✅ Branko Q1 = "A - no apply button"** — he chose *"the engineering plan's way"*, in which a **single**-filter sheet applies instantly and **only the combined "All Filters" window keeps its button** (tech plan **D15**). **Both HIGH risks in `PO-RULING-DEFENCE.md` are retired; Group C closed at 0 cases; the concession we had written down was NOT called in.** **Honest residue:** the combined-sheet half rests on the **option text he selected**, not a sentence he typed — an **optional** confirmation line is drafted and **no case is held for it**. | **2026-08-04** |
| **Filters: which tab opens on a first visit** | Filters | **✅ Branko Q2 = "A - it's fine"** — Estimates confirmed, even though the question told him All is first in the row. Converts an **engineering** decision into a **product** one, which is exactly why **[C38876](https://shopview.testrail.io/index.php?/cases/view/38876)** was rated HIGH (Rule 30). **HIGH retired.** He still has not written it into the PRD. | **2026-08-04** |
| **Filters: does the Parts "Vendors" page get filters, and where is its design?** | Filters | **✅ Branko Q3:** *"Disign for vendors exists in figma. Check it"* — **it does, and the premise of our own question was wrong.** We checked as he asked: Figma node **`11903:10461`** shows exactly two chips, **`Vendor`** and **`State/Province`** — read as **pixels**, matching what our case already asserts. **His own PRD §2 had listed Vendors all along.** Consequence: the *"developers have not been given a design"* hedge inside **[C38904](https://shopview.testrail.io/index.php?/cases/view/38904)** was **false and is now REMOVED (executed 2026-08-04, byte-verified)**. **We also settled a contradiction in our own notes:** engineering had recorded that node `11903:10461` is *Vendor Invoices, not Vendors*, and would not build Vendors filters until a design arrived — **that reading is WRONG.** The frame shows page title **Vendors**, the `Vendors` nav item **below a separate `Vendor Invoices` item**, a **New Vendor** button, and the two chips. **Honest cost, and it is yours to accept: the case can now legitimately FAIL if the build has not shipped that filter bar** — the correct outcome, since under Rule 45 the hedge was a false all-clear. | **2026-08-04** |
| **Filters: the searchable-field list (`S13-R23` "QA has no baseline")** | Filters | **✅ CLEARED AS A BLOCKER — Branko Q5 = "B"**, explicitly *"not as a blocker for tests"*; tests may work with *"typing narrows the list"*. **Our 13 FLT-PSRCH cases were already written that way** — none asserts which fields are matched, so **0 case operations**: his answer validated existing wording. Now owed by **engineering** as reference documentation only. | **2026-08-04** |
| **Filters: is spec v1.6 still the current version?** | Filters | **✅ Branko Q6 = "A"**, and **independently verified live** the same day rather than taken on trust: Confluence page **572030978**, **page version 14**, body version string **1.6**. Rule 31's staleness trap (a frozen in-body version number) is **not** firing here — both agree. He also committed to telling us on every change. | **2026-08-04** |
| **The 6 un-rendered Figma design boards (Rule-35 queue)** | Filters | **You supplied a Figma personal access token**, and the existing resumable fetcher `tools/fetch_all.py` pulled all 6 over REST `/v1/images` in one call, no rate limit, exit 0 — **85/85**. Queue `PENDING-FIGMA-FETCH.md` marked **CLOSED** with the method and a clock cross-check. All six were **read**, which corrected 4 of our own tree-derived claims and confirmed 2 others (`BOARD-NOTES-12-2026-07-31.md` §6 re-checks all 11 absence claims we held). **The Filters design source is COMPLETE.** | **2026-07-31** |
| ~~**A decision on TestRail run 278 "Custom Permissions"**~~ **CLOSED 2026-07-31 BY A SCOPE RULING — not by a sync, and no sync will happen.** You ruled that test runs are in scope **only for the projects we are actively working** (**Filters 352 · Schedule 357 · Reports Suite 359**) and that any run **not created by Bilal Muzamil** is **ignored entirely** — not synced, not written to, **and not audited for missing cases**. Run 278 is **Vladimir Tomovic's** run on a **different project**, so the question the decision paper asked is **moot**; **Option B was NOT taken and nothing was written**. Our coverage is measured against the **case suite under our group**, never another author's run selection. Now installed as **Standing Rule 47**, with the scope qualifier added to **Rule 34**. The paper is retained for the record, banner-marked **SUPERSEDED**: `build/testrail-run-sync-2026-07-31/RUN-278-DECISION.md`. **Consequence recorded honestly:** the 3 Custom Roles regression guards **CR-REG-01 = [C38843](https://shopview.testrail.io/index.php?/cases/view/38843)**, **CR-REG-02 = [C38844](https://shopview.testrail.io/index.php?/cases/view/38844)**, **CR-REG-03 = [C38845](https://shopview.testrail.io/index.php?/cases/view/38845)** remain in **no manual run** — Custom Roles is not one of the three active projects, so that is **in line with the ruling, not an oversight**; if a Custom Roles release regression is ever run again, they need a run of ours. **Do not re-raise run 278.** | Custom Roles / cross-project | **Your scope ruling, 2026-07-31** (foreign + non-active runs are out of scope) | 2026-07-31 |
| **3 of the 8 Schedule questions to Branko** — where working hours live · split working days · do double-bookings count in the problem counter | Schedule | **ANSWERED BY HIS OWN LIVE WRITE-UP (Confluence v23)** — found on a re-read while building the Round-3 sheet, so they were withdrawn before being sent rather than re-asked. §4.2: *"Working hours are defined in two places: a technician's custom schedule in **Edit Staff Member**, and the shop's business hours in **Edit Location**"* · §4.2: *"'Add hours' appends more **to support split shifts**, each removable"* + the overlap-validation paragraph · §4.11: *"The system continuously scans for scheduling issues and **surfaces them in a toolbar pill**"* with **Double-booked** as the first row of that table, plus *"listed in a dropdown from the toolbar"*. **No case changed** — ours already matched all three. The tech-plan mismatches were **re-routed to dev** (§2), not dropped. This also **reverses** our own earlier note that the double-booking question was *"not settled by v23"* — reversal cited, not asserted. | 2026-07-31 |
| **Whether Branko returned the Filters sheet** | Filters | **Established, negatively** — the shared Google Sheet was downloaded successfully (3/3 endpoints, HTTP 200, valid workbook), proved to be **our own file** (104/104 content cells identical) and its answer column is **empty on all 8 questions** with no cell comments. So the *question* "did he answer?" is closed: **he has not.** The answers themselves remain outstanding (§3) and **nothing was invented to fill the gap** (Rule 12). **Re-established on a SECOND delivery the same day** — the re-handed file is cell-for-cell identical (0 diffs) to the blank copy, still **0 of 8**. | 2026-07-31 |
| **Filters: is SORTING in scope?** | Filters | **NOT RESOLVED — stated plainly so it is not mistaken for settled.** Branko's Q4 came back **blank**. We hold **ZERO sorting cases** (verified against `testrail-id-map.csv` — no case with "sort" in its ID, title or section among the 110) and **the block stays unwritten**. Live v1.6 contains the word "sort" **exactly once**, incidentally, in **S13-R14** (*"It survives sorting, pagination…"*); there is **no sorting Story**, no sort maximum, no direction mechanism, no column indicator in any source, and the Figma sorting boards are marked **"Work In Progress"**. Authoring against them would breach Rule 42. **This is the highest-value of his 8 unanswered questions** — the only one that creates or cancels a whole block (est. **8–12** cases if multi-level, **5–7** if single-level), and it would need a **UNION `update_run`** on run 352. **STILL UNANSWERED, but the DESIGN SIDE IS NOW PIXEL-CONFIRMED (2026-07-31):** Sorting steps **1–3 were rendered** and the panel is fully pinned — a toolbar **↑↓** icon, a **`Status ↓`** column indicator, a field menu of **exactly `Status` / `WO Number`**, a sort **chip `↑↓ Status ⌄`** inserted first in the filter row, and a panel of *field dropdown* + *direction dropdown (`Ascending`)* + per-row **`✕`** + **`+ Add Sort`** + panel-level **`🗑 Delete sort`**. **`Descending` appears nowhere.** Step 4 (two stacked rows, `Add Sort` gone → possible two-sort cap) is **now RENDERED too (2026-07-31) — all four Sorting boards are pixel-confirmed**, and the `Add Sort`-absent reading was **confirmed**, not overturned. The section is still titled **"Sorting (Work In Progress)"**, so Rule 42 still forbids authoring. A ready-to-write 9-row proposal sits in `build/filters/design-2026-07-31/RECONCILIATION-12-2026-07-31.md` §D-1 — **it is a design-backed proposal for ~6–8 cases and nothing more: 0 authored, no internal IDs, no C-ids, never pushed** (Rule 42). **The staged items are frozen by your ruling of 2026-07-31, verbatim *"Lets wait for Brankos answers"*, given when we asked what it would take to apply each staged group — the right call, because no written source supports the behaviour (Rule 48).** | Open — re-confirmed **2026-07-31**; design side pixel-confirmed **2026-07-31** |
| **Filters: does the page-TOOLBAR search stay in Filters?** | Filters | **YES — the 13 FLT-PSRCH cases STAY, untouched. Nothing moves, nothing retires.** His Q7 came back **blank**, so the default position holds on the strength of **his own PRD v1.6 Story 13 "Page Search" (25 requirements, S13-R1…S13-R25)** and its Key Decision *"Page search is separate from the filter bar, not a filter chip."* The settled palette ruling (row above) covered the **pop-up** control only — a different thing. **Residual ambiguity recorded, not resolved:** his sentence *"This release only removes global search's page-filtering behaviour (Story 14)"*, read literally and in isolation, would also descope Story 13; it is **newer** than v1.6 so Rule 32 keeps it live, but he was answering about the pop-up box and we will not retire 13 cases on inference. **If B ever arrives it is a 13-case retirement needing its own authorisation** (C38883 · C38884 · C38886 · C38888 · C38889 · C38891 · C38893 · C38898 · C38899 · C38900 · C38901 · C38902 · C38903) — and the destination, Global Search, is **POSTPONED**, so the coverage would be parked, not running. | Open (confirmation only) — **2026-07-31** |
| **Ownership of the 9 page-search / command-palette cases** | Filters | **Branko Q6 = A**: *"Test it under Global Search, not here."* Corroborated three ways (spec v1.6 has no palette requirement; the Filters Figma file has no palette board). The conditional user ruling *"do not delete unless Branko confirms"* therefore **resolved to retire** — FLT-SRCH-01…09 retired locally; none had ever been in TestRail, so nothing was deleted. **Thread CLOSED.** | 2026-07-31 |
| **Schedule: the 75/79 over-80-character titles** | Schedule | Verified **done** — live check 2026-07-31 found **0 of 165** titles over 80 characters (pushed in the closing authenticity pass). Go-ahead no longer needed. | 2026-07-31 |
| **Schedule: "is anything missing from coverage?"** | Schedule | Answered by the **coverage re-derivation vs spec v23** (`build/schedule/coverage-rederivation-2026-07-31/`): 243 statements enumerated, **3 genuine gaps found and closed** (1 new case **C38926** + 1 extension **C29961**), reverse check clean, run 357 synced 164 → 165 with all 429 results intact. | 2026-07-31 |
| **Schedule: the two long-HELD items** — D1 events-count-toward-capacity, D4 modal "Reassign" | Schedule | Branko answered both (events **do** consume capacity; **no** Reassign button). Spec v23 backs him. | 2026-07-31 |
| **Schedule: the Week Export retire (C38853)** | Schedule | You authorized *"Retire from test cases and test run"* — `delete_case` HTTP 200, re-GET confirms gone, run 357 165 → 164 with all 429 results intact. | 2026-07-31 |
| **Report Suite: Chris's 5 TechPlan answers** | Report Suite | All five answered = option A (location dropdown hidden · one suite-wide too-large message · 10,000-row cap on all six · ordinary reports permission · "Sales Representative" in full). 70 cases updated + 7 added, all pushed. | 2026-07-31 |
| **Report Suite: the promised spec changelog** | Report Suite | It landed — all six specs moved on 2026-07-29 with dated Change Log rows (SBC v11→12, SBR v14→15, PV v3→4, TU v4→5, WIP v5→6, IV v2→3). Deadline 2026-08-04 **partly** met: the changelog arrived, but 6 of his 12 promised edits did not (still open in §1). | 2026-07-31 |
| **Report Suite: the promised companion video** | Report Suite | Chris delivered the Loom on 2026-07-30; transcript ingested, 3 firm deltas pushed. | 2026-07-30 |
| **Schedule + Report Suite: epic keys** | both | Schedule = **SV-8685** (15 stories), Report Suite = **SV-8582** (97 stories). Both re-verified current on 2026-07-31. | 2026-07-27 / 2026-07-31 |
| **Schedule: designs "missing"** | Schedule | Not missing — the Claude prototype `Schedule.dc.html` is authoritative (Branko Q0). OQ-4 resolved. | 2026-07-22 |
| **Report Suite: Chris to correct SBC spec `S1-R2`** | Report Suite | **DONE — verified live in the spec body, not taken on his word.** SBC spec (Confluence 577634305) now reads *"The report is gated by ordinary reports access, not by a report-specific permission. Any user with standard reports access can open it; there is no dedicated Sales By Customer View permission."*, with a matching change-log row dated 2026-07-31. His ticket comment *"Spec updated accordingly"* checks out. (Checked because on 2026-07-29 he believed he had made the WIP identifier edit and had **not** — that one is still open in §1.) | 2026-08-03 |
| **Report Suite: Question 5 of the 2026-07-31 sheet** (one reports permission or the per-area ones?) | Report Suite | **ANSWERED = A**, *"Collapse all report access into a single Reports permission"* — filled into the separate SBC permissions Google Doc as its Q2. Verbatim export committed. **Do not re-ask.** The surviving follow-on (how far the collapse reaches) is a **new** open row in §1. | 2026-08-03 |
| **Report Suite: the SBC permission ruling on record** | Report Suite | **Q1 = A**, *"Gate SBC on ordinary reports access, like the other five reports"* — his **third** statement of the same ruling (2026-07-28 · 2026-07-31 Q4=A · now). The sheet existed only to capture it against the current source, and it is captured. **The three SBC permission cases needed no change to what the tester checks.** | 2026-08-03 |
| **Report Suite: the SV-8589 QuickBooks / fractional coverage gap** | Report Suite | **FULLY cleared** — 2 cases authored AND pushed: **PV-PREC-01 = [C38924](https://shopview.testrail.io/index.php?/cases/view/38924)**, **PV-PREC-02 = [C38925](https://shopview.testrail.io/index.php?/cases/view/38925)** (verified in the id-map; run 359 verified 474/474 so no resync was needed). *Corrected 2026-07-31 — this row previously read "partially cleared".* | 2026-07-31 |

---

## 2026-08-03 — Report Suite: what moved, and the five Rule-48 fields on what did not

**EXECUTED TODAY (all HTTP 200, all re-GET MATCH, 0 failures).** Authorised push 1: 3 `update_case`
(C30096, C30098, C30099 — stale `refs` corrected against the live SBC spec + tester note extended for
the hidden-and-inert permission) + 1 `add_case` (**SBC-PERM-05 = [C39447](https://shopview.testrail.io/index.php?/cases/view/39447)**,
"No Sales By Customer permission is offered in the role permission editor") + the **run-359 UNION**
(**474 → 475**, 0 prior case_ids lost, **539 prior result records all verified present** — the staged
plan had recorded 0 results, so the write-time snapshot is what protected them). Authorised push 2:
**10 `update_case`** for groups C + D, moving PV / IV / TU / WIP onto the single ordinary reports
permission. Audit logs: `build/report-suite/chris-answers-2026-08-01/testrail-execution-log-2026-08-03.md`
and `…-CD-2026-08-03.md`. Counts reconcile: **local active 475 == live-ours 475 == id-map 475 (0
blanks) == import rows 475**; live folder total **480 = ours 475 + 5 foreign** (Vladimir Tomovic,
untouched, Rule 38).

### INDEPENDENTLY VERIFIED 2026-08-03 — and 6 further items need your go-ahead

A **second pass re-derived the closeout from the primary sources** (live TestRail, live Confluence,
live Jira) rather than trusting it — because a self-certifying verdict is what let the Location-column
defect through. Working paper: `build/report-suite/VERIFICATION-2026-08-03.md`; side-by-side coverage
evidence: `…/SIDE-BY-SIDE-2026-08-03.md`; scripts: `…/verification-scripts-2026-08-03/`.

**CONFIRMED:** counts reconcile as **exact sets** four ways (475/475/475/475; live total 480, 5
foreign untouched) · traceability **475/475** with **0** unresolved anchors · coverage complete both
directions (**0 of 59** changed requirements uncovered) · **the Location-column defect class is
CLOSED** on all six reports · **exactly 1** contradiction (the group-E pair below) · titles max **80**,
**0** over · run 359 set-equal with **539/539** result records intact · **0** unclaimed TestRail writes.

**6 NEW FINDINGS — nothing was fixed; all staged for your authorisation** (≈26 `update_case` + 2
section moves, **0 deletes**, no change to run 359's count):
**(1)** the *mechanism* behind the Location defect is still live — **6** export cases assert the
`"Locations:"` line but do **not** cite the governing anchor, so the next spec change would miss them:
**C30167 · C30277 · C30376 · C30437 · C30511 · C30588**. **(2) Rule 42 is unchecked anywhere** — **27
of 34** closed *"exactly …"* lists carry no version-pinned anchor (~17 genuine), the exact condition
that broke SBR-EXP-10/11. **(3)** the **Rule 46 register is stale** (says "474 cases", no C39447, no
group-E entry) **and its entry B4 wrongly claims the dev ticket is unfiled — SV-8780 is live and
Ready to Fix**. **(4)** **3** cases require measuring a contrast ratio with no method named
(**C30387 · C30309 · C30448**). **(5)** **2** cases need devtools/network reading while sitting in UI
sections (**C30419 · C30424**, Rule 4 inverse). **(6)** 2 coverage rows the earlier extractor missed
(SBC `S14-R4`/`S15-R4` — covered by **C38856**, but hidden by slash-shorthand `refs`).

**SOURCE-CURRENCY CORRECTION (Rule 31):** the **SBC spec is STALE for us** — live **Jul 31**, we hold
**v12 Jul 29**. Two changes positively established live; **the full delta is NOT VERIFIED** because
the capture cookie is gone from ephemeral `/tmp` and CQL phrase search proved unreliable past ~line
560 of these pages. **Needs fresh Confluence cookies.** Both local SBC mirrors still carry the
abolished dedicated-permission text. *(Also: `SPEC-WATCH-2026-07-28.md`'s header says "6 OF 12" while
its rows list **7** open — the 7 is correct.)* **Stale counts elsewhere in this file: rows quoting
"ours 474 / live 479" are now 475/480.**

### BLOCKED ON THE QA LEAD — the 2 group-E cases (Standing Rule 48, all five fields)

| Field | Detail |
|---|---|
| **What I need** | A ruling: **RESCOPE or RETIRE** PV-PERM-03 = [C30327](https://shopview.testrail.io/index.php?/cases/view/30327) and PV-API-04 = [C30391](https://shopview.testrail.io/index.php?/cases/view/30391) |
| **Which ruling froze it — verbatim** | *"DO NOT execute group E (C30327, C30391). Those are retire-or-rescope, and a delete is irreversible and would change run 359's count — bring me the recommendation and I will get his explicit sign-off."* |
| **When / what it answered** | **2026-08-03**, answering how far to take your own ruling *"Yes all the reports will be gated by ONE permission FOR NOW."* |
| **What it blocks** | Both cases test the state *"has Reports access but NOT Inventory Reports View"*, which **cannot exist under one permission — so they are currently unrunnable**, and they now **contradict PV-PERM-01 = [C30325](https://shopview.testrail.io/index.php?/cases/view/30325)**. This is the suite's **only** unresolved contradiction, and it is deliberate (Rules 28/46) |
| **Was it right?** | **Yes, clearly.** `delete_case` is irreversible, your ruling is explicitly *"FOR NOW"* (the model is modular and may expand), and **the build has never been observed** — no QA branch. Deleting on those grounds would be a genuine coverage loss |
| **What unblocks it** | One word from you. **Recommendation: RESCOPE** — repoint both at the surviving, valuable assertion that *an extra permission, if it exists, enforces nothing* (Chris: *"they can exist and not do anything for now"*), which nothing else in the 475 checks. It is an ordinary `update_case`: **no delete, no run change**. Proposed wording: `build/report-suite/chris-answers-2026-08-01/staged-case-plan-CDE-2026-08-03.md` |

### OUT OF SCOPE BY YOUR RULING — SV-8780

**Your ruling 2026-08-03, verbatim: *"Ignore this ticket."*** Not commented on, not transitioned, not
read-and-edited. The drafted comment at `build/dev-tickets-2026-07-31/SV-8780-followup-draft.md` is
retained unposted and banner-marked **NOT TO BE POSTED**. **This row exists so the earlier "post the
SV-8780 comment" ask is provably withdrawn rather than silently dropped.**

### BLOCKED ON CHRIS WARD — 3 items, all on one ready-to-send sheet

`build/report-suite/PO-Questions-Chris-ReportSuite-2026-08-03.md`/`.xlsx` — **17 items** (5 decisions
+ 12 write-downs), **13 sources swept**, **10 candidates withdrawn as already answered**.

| # | What Chris owes | What it blocks | Since |
|---|---|---|---|
| 1 | **5 product decisions** — the SBR export-column contradiction · will the descriptions be updated · where Location sits in the two Summary downloads · the single logo rule · **which SBC features were dropped** (his own *"I own that"*) | ~15 cases stay hedged instead of asserted | 4 of the 5 since **2026-07-31** |
| 2 | **The 7 open SPEC-WATCH items — 1b, 4, 6, 8, 9, 10, 11 — DUE 2026-08-04.** Now asked **individually**, not bundled, because a bundled ask has twice produced a bundled non-answer | The written descriptions contradict rulings our cases follow, so anyone comparing us to the text reads a mismatch as **our** error | **2026-07-28** |
| 3 | **NEW — the 5 non-SBC descriptions still name a per-area report permission** (PV `S1-R4`/`S1-N2`, IV + TU + WIP Story-1 prerequisites; verified live 2026-08-03). Only SBC `S1-R2` has been corrected | The 10 cases reworded today are right per your ruling but disagree with his written text | **2026-08-03** |

**Cleared this pass:** *"Chris to say how far the single-Reports-permission answer reaches"* — **your
own ruling answered it**, so the question was **withdrawn from the sheet** rather than asked.

### THREE SMALL ITEMS BLOCKED ON NOBODY — ours, staged, need a word

1. **Refresh the local SBC spec mirrors** — `build/report-suite/specs/sbc-sales-by-customer.md` and the
   `spec-current-2026-07-31/` capture (**v12, Jul 29**) still carry the **abolished**
   dedicated-permission `S1-R2`; the live page is **Jul 31**. **This risk is live:** the next pass that
   trusts the mirror will read an abolished requirement as current. It nearly happened today.
2. **Add your 2026-08-03 ruling citation** to the 4 cases pushed earlier today (C30096, C30098, C30099,
   C39447) — their tester note already says *"for now"*, but their `refs` cite Chris only. 4 tiny
   `refs` appends, outside that push's authorisation.
3. **`SPEC-WATCH-2026-07-28.md` header says "6 OF 12"** while its own table lists **7** open items.
   **The 7 is correct.** One-line count fix, in a file this pass was not authorised to edit.

---

## 2026-08-03 (later the same day) — GROUP E EXECUTED · SPEC-WATCH RE-VERIFIED LIVE · SBC MIRROR REFRESHED

Three of the asks listed above are now **discharged**, and the SPEC-WATCH picture is now **verified
against live Confluence text** rather than against our own note. Evidence:
`build/report-suite/rescope-2026-08-03/testrail-execution-log.md` and
`build/report-suite/spec-watch-verification-2026-08-03/VERIFICATION.md`.

### ✅ CLEARED — "BLOCKED ON THE QA LEAD: the 2 group-E cases"

**Your ruling, verbatim, 2026-08-03: "Rescope"** — answering the recommendation *"RESCOPE, not
retire"*. **EXECUTED:** PV-PERM-03 = [C30327](https://shopview.testrail.io/index.php?/cases/view/30327)
and PV-API-04 = [C30391](https://shopview.testrail.io/index.php?/cases/view/30391) rescoped by
`update_case` (4 calls incl. 2 self-corrections, **all HTTP 200, all re-GET MATCH**; 0 add, 0 delete,
0 run write). They now assert the surviving, previously untested behaviour — *an extra/per-area report
permission, if it exists, is hidden and enforces nothing*. **Run 359 unchanged: 475 tests / 539
results before and after.** **The suite's last unresolved contradiction is CLOSED** (C30325 / C30327 /
C30391 re-diffed live; a 475-case sweep finds **zero** active cases asserting a per-area permission
gate). Two small flags for your word only: both cases keep `type` "Negative" (not re-classified —
outside the authorisation), and C30327 now sits close to C30325 (distinct because it adds *"no other
reports permission switched on"* — merge only if you prefer).

### ✅ CLEARED — "Refresh the local SBC spec mirror"

`build/report-suite/spec-watch-verification-2026-08-03/live-capture-2026-08-03/` holds the live SBC
(plus SBR and PV) as an **additive, dated** capture — **the 2026-07-31 captures were NOT
overwritten.** Confirmed stale exactly as suspected: the 07-31 mirror line **125** still read *"gated
by a dedicated Sales By Customer report View permission"*. **STILL OPEN (your call):** whether the
older authoring ingest `build/report-suite/specs/*.md` should be refreshed too — it carries the same
abolished sentence at line 111.

### 🔴 THE DEADLINE FRAMING IS GONE — and 8 items are NOT DONE

**Your ruling, verbatim, 2026-08-03:** *"There is nothing due tomorrow, if something he was supposed
to do should have been done by now, you need to check. If that has not been done consider it not
done."* So **every "DUE 2026-08-04" above is superseded** (including item 2 of the Chris table and the
short-version line): an item is in the live spec or it is **NOT DONE**. `SPEC-WATCH-2026-07-28.md` has
been rewritten to match, and its **"6 OF 12" count nit is fixed** by the new header — clearing small
item 3 above.

**Live verdicts, 2026-08-03 (13 items): DONE 5 · NOT DONE 8 · PARTIAL 2.** Not one of our cases needs
changing — every NOT DONE item is one where we correctly followed his newer video/answer ruling
(Rule 32) and **the spec text is what is behind.**

| Still owed by Chris Ward | Live evidence (quoted in full in VERIFICATION.md) | Affected cases |
|---|---|---|
| **The one-permission paragraph on PV, TU, WIP, IV** — SBC got it (v13, 2026-07-31); the other four did not | PV S1-R4 *"require the **Inventory Reports → View** permission"* · TU *"the permission that grants access to the timesheet reports"* · WIP *"the permission that grants access to Work In Progress reports"* · IV *"the existing inventory-reports permission"*. **SBR needs no edit** — it never named one | 16 cases, incl. C30325, C30327, C30391, C30398, C30526, C30527, C30603, C30604 |
| **Location filter hidden when ≤1 location** (his own Q1=A) — **highest risk** | SBR S21-N1 / TU S9-N1 / IV S7-N1 / PV S2-E4 all still *"still sees the filter"* | C30216, C30446, C30577, C30340 |
| **WIP asset identifier = VIN chain** — he has now **twice** believed this was done | WIP S4-R7 *"the unit number on the first line in bold"*; S4-R9 *"sorts by unit number"* | C30470, C30500, C30485, C30516 |
| **SBR export header lists** (S14-R15/R16) — add Location, and `Sales Rep` → `Sales Representative`. **The spec contradicts itself**: S14-R20 says Location is in all four exports | S14-R15 *"Headers, in order: `Sales Rep`, `# Invoices`, …, `Subtotal`"* — no Location | C30285, C30286 — **already fixed on our side** (scope-conditional per Rule 42) |
| **Five one-sentence fixes** — SBC Performance group + anchors (10) · TU "below existing links" (6) · PV "only report" sentence (11) · **NEW:** SBC still lists "Print" as an export at S18-R7/S18-R10 after retiring it in Story 16 · WIP asset-dropdown style (8) | quoted in VERIFICATION.md | C30096, C30195, C30392, C30451, C30322, C30500 |
| **NEW — item 9 is HALF-applied**, which is worse than untouched: SBR S1-R1 already says *"the full word \"Representative,\" not the \"Rep\" shorthand"* while S19-R7, S19-R1/R8 and both CSV header lists still say "Sales Rep" | as above | C30315, C30285, C30286 |

### NEW ACCESS ASK — Confluence session cookies

The Atlassian MCP returns **`lastModified` only, no version number**, and the REST cookie file the
earlier passes used (`/tmp/fd-tickets/all-cookie-header.txt`) is **gone — `/tmp` is ephemeral**. So
today's version column is derived from the live modified date plus the newest in-body Change Log row,
and says so (Rule 12). **Fresh Confluence cookies would restore true version integers.** Also
unrelated-but-noted: the epic **SV-8582** was **not** re-checked this run (6 stories were reopened as
of 2026-07-31) — a cheap Rule-37 Tier-1 check belongs to the next Report Suite touch.

### 2026-08-03 ADDENDUM — true spec versions, the full SBC delta, and the epic Tier-1 check

Source: `build/report-suite/spec-watch-verification-2026-08-03/ADDENDUM-full-versions-SBC-delta-epic.md`

- ✅ **CLEARED — "Confluence session cookies for true version numbers."** **Withdrawn, do not send
  them for this.** `mcp__Atlassian__fetch` with a page ARI returns `metadata.version`. Live
  **SBC 13 · SBR 15 · PV 4 · TU 5 · WIP 6 · IV 3**; our mirrors **SBC 12 · SBR 15 · PV 4 · TU 5 ·
  WIP 6 · IV 3** — **five of six mirrors are the SAME VERSION as live**, only SBC stale by one.
- ✅ **The full SBC v12→v13 delta is now known and it is SMALL:** structural diff of both full bodies
  (234 requirement anchors each; 0 added, 0 removed) found **exactly 3 text changes — `S1-R2`, `S1-N1`
  and the removed Story-1 Prerequisite — all the permission gate**, plus 1 changelog row. **ZERO case
  changes required** (Rule-43 verdicts: all 3 already COVERED by C30098 / C30099 / C39447). This also
  **CONFIRMS** SPEC-WATCH items **1a, 2, 3 DONE and 10 NOT DONE against v13**, which were previously
  uncertain.
- ✅ **SPEC-WATCH "6 OF 12" header count fixed** (7 was correct; 8 once the new one-permission item is
  counted) — clears the small item raised earlier today.
- 🔴 **NEW ASK — go-ahead to author SBC-API-06** (staged, not written): the **API surface of the SBC
  permission gate is uncovered.** SBC has 5 API cases (C30190–C30194), none about permission; PV has
  that half (PV-API-04 = [C30391](https://shopview.testrail.io/index.php?/cases/view/30391)). It is the
  surface **SV-8780** is about — the atom could still be enforced server-side after the front end stops
  offering it. Needs your go-ahead **and** a run-359 sync afterwards (Rule 34/47, `include_all: false`).
- 🔴 **NEW — SV-8614 "SBC - Story 16 - Print the report" is still Open for a RETIRED feature.** Print was
  removed from the spec (v13 Story 16) and our Print case deleted 2026-07-28. Correctly cited by no case.
  **Third live Print inconsistency**, with SBC `S18-R7` and `S18-R10` which still list Print as an export.
  Owed by Chris/dev.
- ⚠️ **NEW — the SBC export-gating asymmetry.** PV `S1-R4` and WIP both say the permission covers
  **exports**; SBC `S1-R2` speaks only of **opening**. Two export surfaces on SBC therefore have nothing
  to verdict against (Rule 40). Your call whether to ask Chris.
- ⚠️ **EPIC SV-8582 Tier-1 (Rule 37): child count verified TWO ways — `parent = SV-8582` and
  `"Epic Link" = SV-8582` both return 97, identical sets, `hasNextPage` false, SV-8583→SV-8679
  contiguous. Live split 90 Open / 6 OBSOLETE / 1 In Progress. 0 new stories, 0 removed. BUT 7 STATUSES
  MOVED since our 2026-07-27 ingest** (which held OBSOLETE 12 / Open 85): **SV-8594–SV-8599, the
  `[B1]`–`[B6]` build stories, were REOPENED OBSOLETE→Open on 2026-07-29**, and **SV-8589 went Open→In
  Progress**. *(Correction to the earlier brief: the six that MOVED are the `[Bn]` stories, not the six
  now-OBSOLETE originals SV-8583–8588.)* **No coverage rides on the 6 OBSOLETE originals — zero cases
  cite them, confirmed.** The reopening is *good* for us: **SBC-PERM-05 = [C39447](https://shopview.testrail.io/index.php?/cases/view/39447)**
  cites **SV-8598**, which is now live again rather than a dead ticket. **No case needs changing.**
  **OPEN QUESTION FOR YOU: does the reopening mean the build plan changed?** None of the six carries a
  comment explaining it — answering that needs a **Tier-2 epic re-read, which requires your
  authorisation** (Rule 37) and was NOT done.

---

## 2026-08-03 (third pass, same day) — THE SIX VERIFIER FINDINGS ARE FIXED · 1 NEW GO-AHEAD NEEDED

Source: `build/report-suite/verifier-fixes-2026-08-03/testrail-execution-log.md` (per-operation) ·
`CONTRADICTION-SWEEP-2026-08-03.txt` (Rule 28 gate).

**Your authorisation, verbatim, 2026-08-03:** *"Do everything which is right to do, I want everything
right to be done for reports suite, I can not take a single risk of mistake."*

**EXECUTED: 47 `update_case` (40 planned + 7 second-pass SBC re-pins), every one HTTP 200 + re-GET
MATCH.** 0 `add_case`, 0 `delete_case`, 0 section change, **0 run writes** — **run 359 verified
unchanged at 475 tests / 539 result records**, before and after. The five foreign cases
(**C38919–C38923**, Vladimir Tomovic) and the two cases another worker owned (**C30327 / C30391**) are
**byte-identical to the pre-write snapshot including `updated_on` and `updated_by`** — the executor
refuses them by assertion, and that was verified after the fact, not asserted.

### ✅ CLEARED — all six findings of `VERIFICATION-2026-08-03.md`

| Finding | What was wrong | Now |
|---|---|---|
| **V-3** (HIGH) | 6 export cases asserted the *"Locations:"* line but never cited the requirement that governs the export surface — **the exact mechanism that let SBR `S14-R20` slip past us on 2026-07-29** | Pinned: C30167 (+`S15-R14` +`S4-R13`) · C30277 (+`S14-R20`) · C30376 (+`S6-R11`) · C30437 (+`S7-R13`) · C30511 (+`S7-R13` +`S9-E1`) · C30588 (+`S10-R15`). **Correction to the verifier:** for WIP the governing export anchor is `S9-R10a`, which C30511 **already** cited — `S7-R13` is the on-screen rule |
| **V-10** (HIGH) | closed enumerations with no version-pinned anchor | **Re-derived from scratch rather than trusting the list of 17: 49 trigger-matched cases → 28 genuine.** All 28 pinned to their governing anchor + spec version, each `refs` stating in words that the closed list IS the requirement |
| **V-9** (MEDIUM) | the deliberate-decisions register was a day stale | Refreshed — see the row below on **SV-8780**, which it got backwards |
| **V-7** (MEDIUM) | 3 cases asked a non-technical tester to measure a contrast ratio with no tool or method | C30387 · C30309 · C30448 now ask what a person can answer by eye; the ratio is recorded as the design-token figure it is. **Nothing deleted** |
| **V-8** (LOW) | 2 cases needed the browser network tab but sat in UI sections (Rule 4) | C30419 · C30424 rewritten UI-observable. **No coverage lost** — the back-end half was already in *"TU — API"* (C30449, C30450 exp 3), quoted side by side in the log |
| **V-2** (LOW) | C38856's slash-shorthand `refs` hid 5 anchors from every anchor-based tool | Expanded — `S14-R2`, `S14-R4`, `S15-R2`, `S15-R4`, `S15-R5` are greppable |

### ✅ CLEARED — "the SBC mirror is one version behind" (was raised earlier today)

Independently re-diffed rather than taken on trust: **222 requirement anchors in v12 and in v13, 0
added, 0 removed**, only `S1-R2` / `S1-N1` / the removed Prerequisite substantively changed — which
**CONFIRMS** the parallel worker's *"exactly 3 changes"*. (`S14-R14` and `S15-R6` look changed but
differ only by markdown escaping of an underscore — a capture artifact.) **All 30 SBC anchors we pin
are byte-identical in v13**, so the 7 SBC pins were upgraded to *"SBC spec v13 2026-07-31"*. Same
diff run on SBR and PV: **0 anchors added/removed**, pinned anchors differ only by whitespace
(similarity 0.992–0.999). **Honest residue: TU v5 / WIP v6 / IV v3 are version-matched to live but
NOT text-diffed** — no live capture of those three exists yet.

### 🔴 NEW GO-AHEAD NEEDED — one case, one `update_case`

| What | Who owes it | What it blocks | Since |
|---|---|---|---|
| **PV-VIS-02 = [C30386](https://shopview.testrail.io/index.php?/cases/view/30386) tells a manual tester to measure paddings in pixels with browser developer tools** — *"(use browser devtools to measure paddings)"*, then asserts *"padding 32px top, 2rem right, 24px bottom, 2rem left"* and *"a 1px top border"*. Found by our own named-entity sweep on 2026-08-03; the verifier did **not** catch it (V-7 looked only for contrast ratios). It is the **same class as V-7** and the fix is identical and already drafted | **YOU** — one word | Nothing in the suite is wrong; the case simply **cannot be run** by the non-technical tester it is written for (Standing Rule 7 / Rule 28 dimension 3). Left untouched **on purpose** because it is outside the six findings you authorised, and a case-body edit needs authorisation (Rule 6) | 2026-08-03 |

### ⚠️ SMALL FLAG, no action needed unless you disagree

Standing Rule 8 asks for a C-id wherever a case is named, so for the record: the two Rule-4 rewrites
put a **sibling case reference inside `refs`** (C30419 → *"covered by C30449"*, C30424 → *"covered by
C30450"*). `refs` is metadata, never tester-facing, so no internal ID reaches the tester — but it is a
first for this suite and you may prefer it stated only in the log.

### 📌 DURABLE FACT LEARNED — now in the playbook (§J), so nobody re-discovers it

TestRail's `refs` field **splits on commas, trims each entry, re-joins with a bare `,`**, and
**rejects the whole write with HTTP 400 `Field :refs does not match the required pattern.` for any
entry over 248 characters** (248 passes, 249 fails; total length is unbounded). Because it reports a
*pattern* error rather than a length error it is easy to misdiagnose. All 475 Report Suite `refs`
values are comma-free single entries, longest 245 — so the house style is **one comma-free entry,
semicolons as separators, ≤ 248 chars**, asserted before every run.

---

## 2026-08-04 — AUTHORISED TIER-2 EPIC RE-READ OF SV-8582 (read-only) — the open question is ANSWERED

Deliverables: `build/report-suite/epic-reread-2026-08-04/` — `EPIC-REREAD.md` ·
`NEW-OR-CHANGED-REQUIREMENTS.md` · `REOPENED-STORIES.md` · `PRINT-INCONSISTENCY.md` · `OUTSTANDING.md`.
**All 98 issues read** (epic + 97 children, contiguous SV-8583→SV-8679). **No Jira/TestRail writes,
nothing authored, nothing staged.**

### ✅ CLEARED — "does the reopening mean the build plan changed?"

That question sat OPEN in this register from the 2026-07-31 Tier-1 check, which could not answer it
because *"none of the six carries a comment explaining it"*. **Answer: NO, the scope did not change.**
The changelog shows a **status-only reversal of an erroneous bulk close** — the *same* developer
(parth fadadu) closed all six as OBSOLETE/Done on **2026-07-27** in 19 seconds, then reopened all six
on **2026-07-29** in ~67 seconds. **No child ticket description has ever been edited** (verified across
all 98; the only body edit in the tree is the epic's own, 2026-07-26). The
PR-1 → A2/A3 → A4/A5 → B1…B6 dependency chain is intact, with PR-1 **In Progress**. **No case needs
changing because of the reopening.**

### Coverage result — 79 verdict rows, 80 assertions, all reconciled

**64 COVERED** with both texts quoted side by side (Rule 45(e)) · **6 case extensions** · **2 new-case
rows** · **2 contradictions** · **1 blocked** · **4 not-independently-testable**. Every actionable row
is a **scope** gap (a shared-shell rule asserted on some reports but not all) — **not one missed
requirement.** Coverage against the engineering stories is strong, frequently verbatim (e.g. PV-COL-01
[C30351](https://shopview.testrail.io/index.php?/cases/view/30351)/PV-COL-02
[C30352](https://shopview.testrail.io/index.php?/cases/view/30352) enumerate B3's 20 columns and 14
defaults exactly; IV-COL-04 [C30554](https://shopview.testrail.io/index.php?/cases/view/30554) matches
B4's Margin/Total-Sell default-off rule word for word).

### 🔴 NEW — 2 epic sentences contradict the epic's OWN child stories (our cases are right)

1. **"All Time stays on WIP only until this lands"** — but SV-8594 (B1) says WIP consumes *"DateRange
   366"* and SV-8590 (A2) defines *"11 presets + Custom, 366-day cap"*. Our WIP-FLT-04 =
   [C30501](https://shopview.testrail.io/index.php?/cases/view/30501) asserts *"'All Time' is NOT
   offered."* **Retain ours (Rule 39); the epic text is stale.**
2. **"single visual theme (two-tone Tech-Efficiency)"** — but the six build stories assign **two**
   themes (PV + SBC two-tone; WIP/TU/IV/SBR all-white), and A5 ships both classes. Our six visual cases
   match their build stories **6/6**.

**Owner: Chris Ward / dev — epic text only, no case change.** Risk if unfixed: a reviewer reading the
epic reports a **false defect**, which is the review-cycle waste Rule 34 exists to prevent.

### 🔴 NEW GO-AHEADS NEEDED — 2 worth arguing for, 4 hygiene

| Ask | Why | Priority |
|---|---|---|
| **3 new cases** — empty-set export = header-only file, on **PV, TU, IV** (covered on SBC/SBR/WIP only) | Failure mode **reads as a bug**: empty-result export either yields a header-only file (correct), nothing, or the *too-large* toast (a real defect — the cap guard firing at zero). No case = whatever happens gets accepted | **Recommended** |
| **Extend 5 persistence cases** — a saved view beats a link/URL parameter; asserted on **SBC only** ([C30179](https://shopview.testrail.io/index.php?/cases/view/30179)) | The most **counter-intuitive** behaviour in the shell — people expect a shared link to win | **Recommended** |
| 4 small extensions + 2 Rule-42 version pins (PV-FILT-03 [C30330](https://shopview.testrail.io/index.php?/cases/view/30330), SBR-STAT-01 [C30208](https://shopview.testrail.io/index.php?/cases/view/30208)) | Hygiene; C30208's *"exactly four options"* rides on a payment mapping engineering calls the *"most bug-prone point"* | Medium |
| **Mark our own `build/report-suite/specs/sbc-sales-by-customer.md` superseded** — it still carries **the full original Story 16 Print** (S16-R1…R6) | Our file, it is wrong, and a reader concludes Print is live. Outside my folder, untouched | **Cheap, worth doing** |

### PRINT / SV-8614 — the evidence is now complete

**SV-8614 is the ONLY Print ticket in all 97 children, and it has never been transitioned** (that is
the new fact the earlier note could not establish). **Five sources agree Print is gone** — the running
build (*"no Print control exists anywhere"*, live 2026-08-03), Chris's 2026-07-29 *"The Print option is
removed."*, SBC v13 *"Story 16: (removed — Print retired)"*, our SBC-EXP-01 =
[C30159](https://shopview.testrail.io/index.php?/cases/view/30159), and the deleted SBC-EXP-13. **Three
stale artefacts disagree** — SV-8614, spec **S18-R7/S18-R10** (*"Exports (CSV, PDF, Print)"*), and our
own old mirror. **Recommend: dev closes SV-8614; Chris sweeps two spec lines.** No case change.
Keeps `viu-2026-08-03/RECHECK-QUEUE.md` row **B9** open — while the ticket lives, Print could be built
and C30159 would fail a build that did what its ticket asked.

### 🔴 NEW ACCESS ASK — an Atlassian credential (the only thing that limits this pass)

**No live Jira read was possible.** No Atlassian MCP is configured and **`/tmp` was wiped**, so
`/tmp/fd-tickets/all-cookie-header.txt` is gone; `GET /rest/api/3/myself` → **HTTP 401**. I searched
`/tmp` and `/root` for any `cloud.session.token` / `atlassian.xsrf.token` / `ATATT*`: **zero matches.**
So this re-read is **PARTIAL, with a 4-day blind spot (2026-07-31 → 2026-08-04)**, and the two-way
child-count JQL could not be re-run for today. **Write a cookie header to
`/tmp/fd-tickets/all-cookie-header.txt`** (the path `build/epic-recheck-2026-07-31/fetch_epic.py`
already reads) and the whole analysis reproduces live in minutes. **An API token would end the
re-supply problem permanently.**

### 2 NEW dev questions worth asking before the next VIU pass

- **A4 backfill-NULL risk** — if the branch deployed before `BackfillInvoiceFinancialColumnsCommand`
  finished, older invoices carry NULL financials and **SBC/SBR money columns read blank or zero, which
  looks exactly like a calculation bug**. Could produce a batch of false VIU failures.
- **B4 sizing gate** — SV-8597 warns `inventory_value_snapshot` could reach 50–200M rows/yr fleet-wide
  without retention. Retention is specified and covered (IV-API-05/06); this is "did they apply it".

### SV-8780 — still OUT OF SCOPE, and correctly so (Rule 48)

Your ruling **2026-08-03, verbatim: *"Ignore this ticket."*** It is the **only** ticket in the tree I
did not read (it post-dates the snapshot). **I did not attempt to read it, and it is NOT counted as a
gap.** The ruling remains right: the question it was filed about was settled the same day by your own
*"Yes all the reports will be gated by ONE permission FOR NOW"* plus Chris's ruling that the built atom
is hidden and inert — and **today's read confirms that call**, because SV-8598's contrary text is the
*older* source and loses under Rule 32. **Nothing would unblock it except you reversing the ruling, and
I see no reason to suggest that.**

---

## 2026-08-04 (final unattended session) — REPORT SUITE: the count settled, the merges closed out, and ELEVEN asks

**Canonical readiness deliverable: `build/report-suite/READINESS-2026-08-04.md`.** Full evidence:
`count-reconciliation-2026-08-04.md` · `rulings-2026-08-04/RULING-3-RECONFIRMED-NEW-BUILD.md` ·
`build-change-2026-08-04/BUILD-MOVED-2026-08-04.md` · `step3-notes-2026-08-04/EXECUTION-LOG.md` ·
`step4-retire-2026-08-04/LOCAL-SOURCE-FIX.md` · `step6-reconcile-2026-08-04/`.

**THE HEADLINE NUMBERS, all set-equal both directions: ours 469 / live 474 / run 359 = 469 tests /
529 results.** 324 verified against the build · 16 failing against an open ticket · 47 held for Chris ·
52 needing a tool · **392 automatable today**.

**⚠️ THE QA BRANCH WAS REDEPLOYED at 10:41:58 UTC** — `v3.4.1-0ed4433` → **`v3.4.1-3d03023`**, all
three markers changed. Every earlier finding is now **provisional against a superseded build**, the
Rule-49 queue is due in full, and **the 469 provenance lines say "the build tested on 8/4/2026", which
is ambiguous because two builds existed that day.** Re-stamping needs a live session.

### What I need from you — eleven items

| # | What | What it blocks | Since |
|---|---|---|---|
| 1 | **Approve the correction to [C30590](https://shopview.testrail.io/index.php?/cases/view/30590)** — the deploy added a `"Date Range:"` first line, so its *"the CSV's first line reads As of:"* note is now **false** and a tester will report a non-defect | one case is actively misleading | 2026-08-04 |
| 2 | **Yes or no on DECLINING `MG-WIP-TOTAL-PINNED`** — keep [C30521](https://shopview.testrail.io/index.php?/cases/view/30521) and [C30494](https://shopview.testrail.io/index.php?/cases/view/30494) separate. Different anchors, a column vs a row, and C30521 is the only Work In Progress case covering the Total column under sideways scroll | only the "recommended count" wording; both cases are live and correct | 2026-08-04 |
| 3 | **A decision on raising the "Inventory Value download ignores your chosen columns and re-orders them" ticket.** User-facing, so Rule 51 does not bar it; **nothing filed** | a real, spec-breaching fault has no ticket | 2026-08-04 |
| 4 | **Should the verbatim "Known and accepted" line also go on [C30588](https://shopview.testrail.io/index.php?/cases/view/30588)?** I put it only on [C30589](https://shopview.testrail.io/index.php?/cases/view/30589), because item 3 asks for a ticket on C30588's problem and telling a tester to ignore it would contradict that | one wording point | 2026-08-04 |
| 5 | **Approve the 3 wording fixes** that make [C30605](https://shopview.testrail.io/index.php?/cases/view/30605), [C30606](https://shopview.testrail.io/index.php?/cases/view/30606), [C30607](https://shopview.testrail.io/index.php?/cases/view/30607) runnable today | 3 cases read as blocked when they are not | 2026-08-04 |
| 6 | **Permission to make the import a ONE-COMMAND wrapper** (sync from live → generate → re-merge C-ids). Today the generator silently deleted **all 47** do-not-automate warnings and I had to restore them | a repeat of today's near-miss, which no output would reveal | 2026-08-04 |
| 7 | **Fresh cookies**, if you want the two carried-forward items re-observed on `3d03023`: the on-screen column order, and a per-cell value comparison of the download. **This was my own error** — I burned the refreshed session by calling `quick-login` about ten times | 2 findings are carried forward rather than re-seen | 2026-08-04 |
| 8 | **The 7 leftover `refs` spec-version pins** — C30519, C30536, C30565, C30574, C30589, C30596, C30597. Still open from 2026-08-03; one word completes it | 7 cases cite a spec with no version | 2026-08-03 |
| 9 | **Chris Ward's answers** to the consolidated workbook | **47 cases cannot be automated** | 2026-08-04 |
| 10 | **Chris Ward to correct the Inventory Value Story 10 context note** so our mirror stops saying the build is wrong on a point you accepted | our own record contradicts your ruling | 2026-08-04 |
| 11 | **From engineering: a nightly-capture trigger · history older than 13 months · and what `3d03023` changed / whether the branch is final** | 6 cases can never run, 2 more not until ~Sept 2027, and the Rule-49 queue cannot close | 2026-08-03/04 |

### Cleared today

- ~~**Rule on the 9 MERGE groups + 1 CUT**~~ — **CLEARED.** Authorised and **8 merges + the cut EXECUTED** (content folded into every survivor first, all byte-verified); the 9th, `MG-WIP-TOTAL-PINNED`, is **declined pending item 2**.
- ~~**Are the counts 478 or 469?**~~ — **CLEARED.** Both were right, an hour apart, across the deletions. `478 − 469 = 9`.
- ~~**Ruling 3: is SV-8823 good to stay closed?**~~ — **ANSWERED, both halves separately, and re-driven live on the new build.** The money **is** visible and correct (zero arithmetic failures across all 9,275 rows), so your condition is met; but it **does** land as text (55,656 of 55,656 money cells), and the spec requires the opposite in writing — so it stays closed **as an accepted deviation**, which is item 10.
- ~~**The local case source was 57 bodies out of step**~~ — **CLEARED and it was a different, worse problem:** the 57 were already marked Retired; the real gap was the **9** from today's merges, **plus** a stale local source that reverted 63 rows of live content on regeneration.

---

## 2026-08-05 — Report Suite: the expected-behaviour correction, and what it left owing

**Context.** The QA lead found that we had been treating **build behaviour as expected behaviour** and said
so plainly: *"I am shocked to see that how come you considered the Build behavior as the expected
behavior?"*, then clarified: *"the case should be matched to the build … meant that the test case should be
VIU'd from the build"* — labels and steps from the build, **never the expectation**. Report Suite was audited
in full (`build/report-suite/expected-behaviour-audit-2026-08-05.md`) and repaired. This section lists only
what is still owed.

### OUTSTANDING — what I need from you

| # | What is missing | Who owes it | What it BLOCKS | Since |
|---|---|---|---|---|
| **1** | **One sentence from Chris Ward on the Location column** — is it in the column list and switchable, or automatic and never in the list? **Q1** of `build/report-suite/rulings-2026-08-05/FOLLOW-UP-QUESTIONS-ROUND-2-2026-08-05.md` | **Chris Ward** | **16 cases** carry `AUTOMATION: HOLD` and cannot be given a verdict. They are named with C-ids in `READINESS-2026-08-05.md` §4.1. Readiness goes **440 → 456** the moment he answers. | 2026-08-04, **re-asked 2026-08-05** |
| **2** | **Chris to fix TWO self-contradictions he has just created.** (a) Sales By Customer **v14 S4-R12** says the Location column is switchable in the column list, while **S13-R4** says that list holds *"exactly nine"* columns and Location is not one of them. (b) The **same requirement is now specified two different ways across the six specs** — SBC v14 uses an *access* gate, the other five an *in-scope* gate, and both were republished today under the same version message. | **Chris Ward** | C30156 and C38912 contradict each other and **each has a live requirement on its side**. Neither can be settled by us. | **2026-08-05** |
| **3** | **Chris to answer the other 6 round-2 questions** (Q3–Q7 + the S14-R14 date-label residue) | **Chris Ward** | 7 further cases; and **Q7 decides whether C43552 is deleted** | **2026-08-05** |
| **4** | **A window on organisation `d55bc308…` with no other worker active**, so the missing-logo state can be seeded | **the QA lead** (scheduling) | **C43553** cannot get a verdict. **Not seeded deliberately:** the Filters and Schedule workers are in the same organisation right now and removing its logo would change what they see. Register entry 3 of `DELIBERATE-DECISIONS.md`. | **2026-08-05** |
| ~~**5**~~ | ~~**Authorisation to sync run 359 (union-only)**~~ | — | **✅ CLEARED 2026-08-05** — authorised (*"Please run this sync and the syncs for other projects too."*) and **executed**: run 359 **469 → 473 tests**, the 4 cases added, all **535** prior result records present by id and byte-identical. See the "cleared" table below and `build/testrail-run-sync-2026-08-05/`. | raised 2026-08-04, **cleared 2026-08-05** |
| **6** | **A yes/no on filing the date-range finding** | **the QA lead** | The server rejects `last_12_months` — the new **first** preset in v14 S2-R2 — and still accepts `today` and `yesterday`, which v14 deleted. **Asked, not filed** (`final-viu-2026-08-05/API-ASK.md` ASK 1): I did not drive the picker, and the requirement is six hours old. | **2026-08-05** |
| **7** | **One live screen check of Inventory Value's page controls** | **me**, next live pass | IV **S1-R8** requires *"the reports suite's standard pagination control"*; a prior pass noted infinite scroll instead. **I did not observe it myself**, so no ticket was filed — filing on another pass's unverified note is exactly what got SV-8821 closed as not reproducible. | **2026-08-05** |
| **8** | **Tell us when the `sv8582` branch is declared FINAL** **✅ ANSWERED 2026-08-11 — IT IS FINAL** (all six reports handed off; 3 of 6 on 2026-08-10, the remaining 3 on 2026-08-11). The row below is kept as the record of the position at the time. | **Engineering**, relayed by you | The queue `build/report-suite/final-viu-2026-08-05/RECHECK-QUEUE.md` is **OPEN**, so **all 473 verdicts are PROVISIONAL** and nothing here may be called VIU-complete. The branch has now redeployed **three times** since 2026-08-03. **⚠️ SUPERSEDED 2026-08-11: those verdicts are no longer provisional pending development and the queue rows may close on the ordinary close condition — but 251 of the 476 have still never been checked against any build (row RS-F1).** | 2026-08-03 *(answered **2026-08-11**)* |

### Things that CLEARED on 2026-08-05 — recorded so they are not re-asked

| What | How it cleared |
|---|---|
| **The run-359 sync (register item 5)** | **Authorised and EXECUTED 2026-08-05** — *"Please run this sync and the syncs for other projects too."* Run 359 went **469 → 473 tests**, gaining [C43550](https://shopview.testrail.io/index.php?/cases/view/43550), [C43551](https://shopview.testrail.io/index.php?/cases/view/43551), [C43552](https://shopview.testrail.io/index.php?/cases/view/43552), [C43553](https://shopview.testrail.io/index.php?/cases/view/43553). **Union-only, one `update_run`, HTTP 200**; all **535** prior result records present **by id** and byte-identical on every graded field; the 5 Vladimir Tomovic cases **excluded from the union, none were in the run to preserve** (Rule 38). **Runs 357 and 352 were checked and already complete (165 / 110) so NO write was made to either.** Full paper: `build/testrail-run-sync-2026-08-05/`. **Standing duty, not closed forever — the run drifts again on the next `add_case` because `include_all` is `false`.** |
| **The logo fallback rule** | **Chris ratified it into the spec today** — SBC **v14 S15-R17**: the ShopView logo stands in *"only when an uploaded logo is set but fails to load"*, and no logo at all when none is uploaded. Our four logo cases already expected exactly that; their provenance now cites the spec instead of his message. **Ticket B5 is therefore NOT a defect and stays unfiled.** |
| **The permission model** | **Parts Velocity v5 S1-R4**, saved today: *"the single reports permission … there is no per-report permission."* The 25 "for now" hedges are gone and the cases cite the spec. **SV-8780 (`Ready to Fix`) is the build failing this now-documented requirement.** |
| **The date-picker shape** | v14 **S2-R2** — nine presets, "Last 12 Months" first, no Today / Yesterday / "Custom" item. |
| **Print in the export requirements** | Removed at v14 **S18-R7** and **S18-R10**. |
| **Whether markers exist on Report Suite** | **473 of 473 now carry exactly one `AUTOMATION:` marker.** Before today, 453 carried none and two competing styles coexisted on the other 20. |
| **Whether a VIU pass had rewritten expectations** | **Checked across all 41 commits that touched the case source: no pass ever changed a case's steps and its expectation body together, and the two pure VIU passes changed ZERO expectations.** The contamination came from an answer-ingest pass. Audit §10. |

---

## 2026-08-06 (end of budget) — COLD-RESUME POINTER

**Work stopped at 98% of the weekly usage budget (resets Monday evening). The entry point for the
next session is `build/RESUME-2026-08-06.md`** — it carries the one thing needed first (a fresh
`sv_sso_session` for `.qa.shopview.com`, which unblocks 47 of the 73 remaining Report Suite cases),
where all three projects stand, the eleven things waiting on the QA lead, the rules added today, and
the traps that cost time. **Nothing in this register is superseded by it; the resume file points here
for the detail.**

**Highest-value thing to check first on FILTERS — an UNCONFIRMED LEAD, not a finding.** The stopped
coverage worker's last output (never committed, never verified) said the specification has stated the
**Reports date-range URL contract verbatim since v5, 20 July**. If true, **row 8 of Vlad's gap table
is settled wrongly** in `build/filters/vlad-gap-review-2026-08-06/ROW-BY-ROW.md` (recorded as "GAP,
not authorable"), **Vlad was right on 7 of 12 rather than 6, and question Q3 on Branko's 21-item
sheet may already be answered by his own spec** — the Rule-55 embarrassment we have had once before.
**Check the requirement's own text across spec versions (Rule 31 trap (c) — never the page version or
date), correct `ROW-BY-ROW.md`, and pull the question off Branko's sheet BEFORE it is sent.**
